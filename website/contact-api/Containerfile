FROM golang:1.22-alpine AS build
WORKDIR /src
COPY go.mod main.go ./
RUN CGO_ENABLED=0 go build -ldflags="-s -w" -o /contact-api .

FROM scratch
COPY --from=build /contact-api /contact-api
EXPOSE 8080
USER 65534
ENTRYPOINT ["/contact-api"]
