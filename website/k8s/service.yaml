apiVersion: v1
kind: Service
metadata:
  name: openova-website
  labels:
    app.kubernetes.io/name: openova-website
spec:
  selector:
    app.kubernetes.io/name: openova-website
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
