apiVersion: v1
kind: Service
metadata:
  name: marketplace-api
  labels:
    app.kubernetes.io/name: marketplace-api
spec:
  selector:
    app.kubernetes.io/name: marketplace-api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
