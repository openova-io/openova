{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "bp-syft-grype.serviceAccountName" . }}
  labels:
    {{- include "bp-syft-grype.labels" . | nindent 4 }}
{{- end }}
