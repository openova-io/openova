# OpenOva UAT Acceptance Document — Part 3

> Session: 2026-06-03 UAT campaign. Founder walks through each row, runs the
> `How YOU verify` command, compares against `What we tested + evidence`, and
> ticks Acceptance. Every fix below was shipped + merged to `main` this session
> and re-confirmed live against the working tree before this document was written.
>
> All Go test commands run **from their owning module root** — `core/controllers/`,
> `core/services/billing/`, etc. are separate Go modules, so `go test ./core/...`
> from the repo root fails with `does not contain main module`. The commands below
> are pre-corrected to the right module root.

## 3. Security & Reliability Hardening (UAT-campaign fixes)

| # | What was hardened (risk closed) | How YOU verify it | What we tested + evidence | Acceptance |
|---|---|---|---|---|
| #2929 (PR #2952) | **Pillar 4 RBAC gap** — the Sandbox ServiceAccount lacked `get`/`list`/`watch` on `apps.openova.io`, `catalyst.openova.io`, `orgs.openova.io` CRs, so the auto-mounted `openova-sandbox-mcp` `k8s.read` tool would 403 against Catalyst CRDs and the AI agent could not see org knowledge. | `grep -n 'apps.openova.io\|catalyst.openova.io\|orgs.openova.io' core/controllers/sandbox/internal/gitops/manifests.go` | PR #2952 merged. All 3 apiGroups present in the generated Role at lines 261/264/267 of `manifests.go`. Audit-agent confirmed 8/8 present, zero theater. | ✅ code-verified |
| #2930 (PR #2959) | **Pillar 4 test gap** — the Playwright sandbox-mcp spec only asserted `mcp.json` file presence; it never exercised a tool call, so a 403 regression (the #2929 risk) would pass CI silently. Added a real `k8s.read.list` invocation against Catalyst CRDs. | `grep -rn 'MCP k8s.read reaches Catalyst CRDs' docs/archive/sessions/2026-06-02-G117-playwright-scaffolds/regression/sandbox-mcp.spec.ts` | PR #2959 merged. New test `MCP k8s.read reaches Catalyst CRDs without 403 (#2929 RBAC, #2930 coverage)` at line 70; invokes the `k8s.read.list` tool (line 84) and asserts no 403. Audit-agent confirmed present, zero theater. | ✅ code-verified |
| #2931 (PR #2956) | **Reload-failure masking** — `patch-existing-nodes-registries-yaml.sh` wrapped `systemctl reload k3s` in `\|\| true`, so the script reported "patched + k3s reloaded" even when the reload failed (memory L7 false-confidence pattern). Now fails loudly with a non-zero exit. | `grep -n 'exit 4\|reload' tools/patch-existing-nodes-registries-yaml.sh` — confirm the reload success-check block (lines 105-109) and that the reload line no longer ends in `\|\| true` | PR #2956 merged. Comment block at lines 105-109 documents the fix; the explicit reload success check replaces the old masking `\|\| true` and exits non-zero when both reloads fail (the two remaining `\|\| true` at lines 60/83 are on node-filter grep + cat, not on reload). Audit-agent confirmed, zero theater. | ✅ code-verified |
| #2933 (PR #2957) | **Switchover RTO risk** — the continuum switchover sequence had no timeout on the PDM-commit and NATS audit-publish steps, so a hung PDM or NATS could block past the ≤60s RTO budget. Added bounded `context.WithTimeout` on both. | `grep -n 'PDMTimeout\|AuditPublishTimeout' core/controllers/continuum/internal/switchover/sequence.go` and `(cd core/controllers && go test ./continuum/internal/switchover/...)` | PR #2957 merged. `PDMTimeout` (default 5s) + `AuditPublishTimeout` (default 3s) declared at sequence.go:116-117, defaulted at 134-138, applied via `context.WithTimeout` at 386 (PDM) and 462 (audit). Test run live: `ok ... switchover 0.017s`. Audit-agent confirmed, zero theater. | ✅ code-verified |
| #2934 (PR #2946) | **Cilium identity trap** — `bp-vpa`'s NetworkPolicy used `ipBlock 0.0.0.0/0`, which Cilium maps to the `reserved:world` identity and **excludes** `reserved:kube-apiserver`, so kube-apiserver → VPA-webhook traffic was silently dropped (same trap as PR #2910). Replaced with an explicit kube-apiserver entity rule. | `helm template platform/vpa/chart --set vpaOverlay.networkPolicy.enabled=true \| grep -vE '^\s*#' \| grep -c ipBlock` → expect **0** | PR #2946 merged. With comment lines stripped the rendered manifest contains **0** real `ipBlock:` keys (the 2 hits in a raw `grep -c ipBlock` are explanatory comments at lines 22-26 / 66-72 documenting why ipBlock was removed). Audit-agent confirmed, zero theater. | ✅ code-verified |
| #2936 (PR #2958 / #2960) | **Admission bounds gap** — Blueprint + Application CRDs accepted arbitrary RTO/RPO/replica values; out-of-range or cross-field-invalid specs were caught only by the controller at reconcile time, not rejected at admission. Added CEL `x-kubernetes-validations` + integer bounds. | `grep -n 'x-kubernetes-validations\|rtoSeconds\|rpoSeconds' products/catalyst/chart/crds/blueprint.yaml` and `grep -n 'x-kubernetes-validations\|minReplicas' products/catalyst/chart/crds/application.yaml` | PRs #2958 + #2960 merged. blueprint.yaml: `x-kubernetes-validations` at line 353; `rtoSeconds`/`rpoSeconds` bounded `minimum: 0, maximum: 86400` (lines 447-454). application.yaml: `minReplicas` (line 192) + `x-kubernetes-validations` immutability rule (line 218). Audit-agent confirmed, zero theater. | ✅ code-verified |
| #2937 (PR #2945) | **Master-admin Secret deletion risk** — `catalyst-kc-master-admin-credentials` lacked `helm.sh/resource-policy: keep`, so a `helm uninstall`/reinstall would drop + regenerate it, drifting from the KC DB credential hash (the chart-cred-persistence failure class). Annotation added to retain it across uninstall. | `grep -n 'resource-policy\|catalyst-kc-master-admin-credentials' platform/keycloak/chart/templates/configmap-sovereign-realm.yaml` | PR #2945 merged. Secret `catalyst-kc-master-admin-credentials` defined at line 953 with `helm.sh/resource-policy: keep` at line 972. Audit-agent confirmed, zero theater. | ✅ code-verified |
| #2939 (PR #2943 / #2944 / #2963) | **CI red-on-main** — 4 lanes were chronically red on `main` (tofu fmt + the Playwright g117 mock group), eroding the green-bar signal. Fixed formatting + mock-backend wiring so all 4 go green. | `gh run list --branch main --limit 12` — expect the latest tofu-fmt + Playwright g117 runs `success`; OR confirm via merge state: `gh pr view 2943 --json state -q .state` (repeat for 2944 / 2963) | PRs #2943, #2944, #2963 merged (all on `main` 2026-06-03). CI lanes verified green via merged-PR required-check gating — the merges themselves required the lanes green. Audit-agent confirmed, zero theater. | ✅ code-verified (CI green confirmed via PR-merge gating) |
| #2940 (PR #2947 / #2948 / #2949 / #2950) | **Pillar 5 hardcoded tethers** — 18 hardcoded `openova.io` references across Go/Helm/cloud-init defeated post-cutover sovereign independence. This batch externalised the PIN-issuer (env), the Huawei PowerDNS host (tf var), and CORS (runtime-config) so they no longer pin to the mothership. | `grep -n 'CATALYST_PIN_ISSUER' products/catalyst/bootstrap/api/internal/handler/auth.go` and `grep -n 'pdns_api_host' infra/providers/huawei/variables.tf` | PRs #2947-#2950 merged. `auth.go:66` reads `os.Getenv("CATALYST_PIN_ISSUER")` (legacy hardcode now a fallback only); `variables.tf:36` declares `variable "pdns_api_host"`, consumed in main.tf:641 + cloudinit at PDNS_API_HOST. Audit-agent confirmed, zero theater. | ✅ code-verified |
| #2941 (PR #2953 / #2954) | **Voucher brute-force risk** — promo codes had no minimum entropy and `/billing/checkout` had no per-customer rate limit, allowing code-guessing + checkout flooding. Added a 16-char minimum + a 5-requests/60s per-user limiter. | `grep -n 'checkoutRateLimiter\|minPromoCodeLength' core/services/billing/handlers/handlers.go` and `(cd core/services/billing && go test ./handlers/...)` | PRs #2953 + #2954 merged. `checkoutRateLimiter` type at handlers.go:42 + `globalCheckoutRateLimiter` at :55; `minPromoCodeLength = 16` enforced at :928-930 with anti-bruteforce error. Test run live: `ok ... billing/handlers 0.020s`. Audit-agent confirmed, zero theater. | ✅ code-verified |
| #2922 (PR #2923 / #2925) | **Pillar 3 auto-enable gap** — `bp-continuum` was not auto-enabled on multi-region Sovereigns (defaulted off), so DR orchestration didn't come up; and enabling it without `bp-cnpg-pair` would crash-loop. Added cloud-init auto-enable on multi-region + a prerequisite-guard initContainer. | `grep -n 'continuum\|CONTINUUM_ENABLED' infra/providers/hetzner/cloudinit-control-plane.tftpl` and `grep -n 'prerequisiteCheck\|prerequisite-check' products/continuum/chart/templates/deployment.yaml` | PRs #2923 + #2925 merged. cloud-init auto-enables bp-continuum on multi-region Sovereigns (`CONTINUUM_ENABLED: "${continuum_enabled}"` at line 564, rationale lines 555-561). Prerequisite-guard initContainer at deployment.yaml:42-56 fails closed when no `bp-cnpg-pair` audit ConfigMap is found cluster-wide. Audit-agent confirmed, zero theater. | ✅ code-verified |

### Acceptance summary

- **✅ code-verified:** 11 / 11 rows.
- **🔄 / ⚠️:** 0 rows.

All 11 hardening fixes are code-verifiable on the merged `main` tree. Two rows
(#2933, #2941) additionally ship Go unit tests that were run live during this
write-up and returned `ok`. The #2939 CI row is the only row whose verification
depends on the CI control plane rather than a static grep; its green status is
gated by the merged PRs' required checks.
