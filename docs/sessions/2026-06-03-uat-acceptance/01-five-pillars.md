# OpenOva UAT Acceptance — Five-Pillar Section

> **How to use this doc**: Each row is a capability the end-user gets. The
> "How YOU verify it" column is a runnable walk-through — execute it yourself
> against the live Sovereign (`hw88`, provisioning now → `console.hw88.omani.works`)
> or run the cited command. The "What we tested" column is what we already
> verified this session, with evidence (PR #, file:line, test output). The
> "Acceptance" column is our honest read; flip it to your own verdict after the walk.
>
> **Acceptance legend**: ✅ Code-verified (ready for your walk) · 🔄 Needs hw88
> live walk · ⚠️ Partial (note why) · ⛔ Open (note).
>
> Pillar definitions are verbatim from [`docs/DOD.md`](../../DOD.md) §1.

## 1. Five-Pillar End-User Acceptance

### Pillar 1 — Marketplace + voucher onboarding (Phase 0 + Phase 1 a–c)

| # | Capability (what the end-user gets) | How YOU verify it (walk-through steps) | What we tested + evidence | Acceptance |
|---|---|---|---|---|
| 1.1 | An anonymous visitor reaches the operator-branded marketplace and sees the catalog. | Open `https://marketplace.hw88.omani.works/` in a fresh incognito window. Expect a non-empty storefront (app cards + branding), HTTP 200, publicly-trusted TLS (no cert warning). | `MARKETPLACE_ENABLED=true` flows provision-body → bp-catalyst-platform → console (DoD D27). marketplace-api projector + `/marketplace` route return non-empty catalog. Console wired live in #2741 (12/12 Playwright specs green). | 🔄 Needs hw88 live walk |
| 1.2 | Operator issues a voucher from the BSS menu (no kubectl, no SQL). | Login `https://console.hw88.omani.works` as owner-tier (PIN-login: email → 6-digit PIN via IMAP → enter). Sidebar → **BSS → Vouchers** → issue voucher to `hatice.yildiz@openova.io`. Expect the voucher to appear in the admin voucher list (DoD D28). | Voucher ops live in BSS menu, not `admin.<fqdn>` (DoD §2 Phase 0). Voucher CR + DB row persist + email via Sovereign outbound SMTP. Owner pre-populated as `tier=owner` post-handover (DoD D21). | 🔄 Needs hw88 live walk |
| 1.3 | Customer receives the voucher email and the redeem link is byte-stable. | Check the `hatice.yildiz@openova.io` inbox. Confirm the link is exactly `https://marketplace.hw88.omani.works/redeem/?code=<CODE>` — **the slash before `?` is mandatory** (DoD §4). | Canonical URL per `core/services/notification/templates/templates.go` (DoD Phase 1 step 1a). Slash-before-`?` is the byte-for-byte contract. | 🔄 Needs hw88 live walk |
| 1.4 | Customer redeems → checkout → picks the Postgres-backed bundle → signs up an Organization → reaches the console. | Click the redeem link → PIN-login as the recipient → pick the canonical Postgres-backed bundle → complete the Org wizard → expect auto-redirect to `https://console.<orgslug>.omani.homes` (DoD Phase 1 step 1c, D29/D30). | organization-controller codifies ADR-0009 iac-bootstrap in Go: Org create runs 6 idempotent steps (Org → repo → robot user → collaborator → branch protection), robot-token persisted to OpenBao `kv/org/<slug>/iac-bot-token` (#2742, #2765). Tenant pool TLD from `core/services/parent-domain/sovereign_parent_domains.go` (`omani.homes` default). | 🔄 Needs hw88 live walk |

### Pillar 2 — Multi-region BCP topology choice at signup

| # | Capability (what the end-user gets) | How YOU verify it (walk-through steps) | What we tested + evidence | Acceptance |
|---|---|---|---|---|
| 2.1 | At signup, the customer picks `active-hot-standby` (2 regions) and **both** materialize — no silent single-region degradation. | In the Org/provision wizard, select `active-hot-standby` and 2 distinct region codes. After provision, confirm both regions exist: `kubectl --context hw88 get clusters.postgresql.cnpg.io -A -o wide` shows instances spread across 2 region labels, and the Cloud canvas (`/cloud?view=graph`) shows `Cluster 2/2` (DoD D15). Cross-check the provision record never collapsed to 1 region. | Admission gate rejects `active-hot-standby` + `<2 regions` at provision time (multi-region hardening #2911/#2916/#2917). 10/10 Hetzner + 3/3 Huawei tofu `for_each` tests green. Canonical pattern = 2 region codes per HCS multi-region mimic (pinned memory `feedback_multiregion_hcs_mimic_violation_2026_05_28.md`). hw88 provisioning now should materialize BOTH region codes in Phase 0. | 🔄 Needs hw88 live walk |
| 2.2 | The topology choice is a signup-time decision, not a Day-2 upgrade. | Confirm the wizard exposes region/topology selection during the signup flow itself (not buried in a post-provision settings page). After signup, `/settings` shows the chosen Region/Capacity/Topology as real values, not `—` placeholders (DoD D22). | Wizard region-selection step covers DoD Phase 1 step 1b (pillar-to-step map, DoD §2). `chooseTopology` reads region count from `Sovereign.spec.regions` via `detectMultiRegion` (not env, locked decision #7) — endpoint pre-check #2780. | 🔄 Needs hw88 live walk |

### Pillar 3 — Two independent CNPG clusters + region-kill failover

| # | Capability (what the end-user gets) | How YOU verify it (walk-through steps) | What we tested + evidence | Acceptance |
|---|---|---|---|---|
| 3.1 | One independent CNPG cluster per region, synchronously replicated (`ReplicaCluster`) over Cilium ClusterMesh on the DMZ WireGuard data plane. | `kubectl --context hw88 get cluster.postgresql.cnpg.io -A` → expect 2 Clusters (one per region). Confirm replication topology: primary in region A, `ReplicaCluster` in region B. Verify ClusterMesh: `cilium clustermesh status` OK for the peer, and `kubectl get svc -A \| grep clustermesh` shows only `LoadBalancer` (no NodePort — DoD A3/D12). | Two-cluster ReplicaCluster wiring + failover orchestration shipped via bp-continuum (#2923/#2925); continuum-controller image bumped to `dedba1c` (HEAD `35727865`). Sync over WG-public-IP DMZ data plane (DoD A2). | 🔄 Needs hw88 live walk |
| 3.2 | Zero-data-loss failover ≤ 60s when a region dies. | Run the §6 counter-test: start a writer doing `INSERT … RETURNING id` every 100ms against the primary CNPG endpoint, logging each `id`+timestamp. Kill the primary region (instance destroy OR NetworkPolicy isolation). Confirm `failover-controller` (reading the Continuum CR) promotes the replica, the writer reconnects via the app FQDN within RTO, and the next written `id` is `last_id + 1` — **no gap, no replay** (DoD D31). | failover-controller reads Continuum CR (never human DNS flip). DoD §6 documents the monotonic-counter contract; the counter-writer local log + post-failover DB state are the only acceptable evidence. bp-continuum #2923/#2925 ships the failover orchestration code. The live region-kill counter-walk is the pillar-shipped proof and has not yet been run on hw88. | 🔄 Needs hw88 live walk |

### Pillar 4 — Sandbox + auto-mounted `openova-sandbox-mcp`

| # | Capability (what the end-user gets) | How YOU verify it (walk-through steps) | What we tested + evidence | Acceptance |
|---|---|---|---|---|
| 4.1 | The Sandbox plane ships on every Sovereign by default (operator does not opt in). | `kubectl --context hw88 get crd sandboxes.sandbox.openova.io` → returns the CRD. `kubectl -n catalyst-system get pod -l app=sandbox-controller` → Ready. Apply a no-op `Sandbox` CR → status transitions `Pending → Reconciling → Ready` within 30s (DoD D32). | Sandbox CRD + controller part of the Sovereign chart by default (DoD D32). Sandbox RBAC fixed this session (#2952) so the MCP server's service account can read org resources. | 🔄 Needs hw88 live walk |
| 4.2 | Customer launches a Sandbox; `openova-sandbox-mcp` auto-mounts with the full 49-tool registry and zero pasted credentials. | Login `https://console.<orgslug>.omani.homes` → open **Sandbox** → confirm agent defaults to `qwen-code` (zero Anthropic cost leak, DoD Phase 2 step 2b). In the session, confirm the agent reports MCP available with 49 tools (`sandbox.db.*`, `sandbox.auth.*`, `marketplace.app.*`, `sandbox.git.*`, `sandbox.iam.*`) with no credential prompt. | sandbox-pty-server writes `mcp.json` to every canonical agent-config path + starts the MCP server as a stdio subprocess (DoD §1 Pillar 4). 49 handlers registered in `products/sandbox/mcp-server/internal/tools/registry.go`. RBAC for org-resource reads fixed (#2952). | 🔄 Needs hw88 live walk |
| 4.3 | The AI assistant sees full Org knowledge and can mutate resources via MCP tool calls (provision an additional app end-to-end). | In the qwen-code session, prompt: *"install a notes app backed by Postgres in my Org, public on `notes.<orgslug>.omani.homes`."* Expect the agent to call `marketplace.app.install` + `sandbox.db.provision` + `sandbox.auth.provisionRealm`, and the new app's CNPG cluster + namespace + HelmRelease + Gitea repo to materialize, reachable at the FQDN with trusted TLS (DoD Phase 2 steps 2d–2e). | RBAC unblock landed (#2952). An MCP tool-exec integration test exists (#2930) but has **not been run live** against a real Sandbox session, and the end-to-end Phase-2 provision-via-agent walk has not yet been performed on hw88 — so we cannot yet claim the mutate-via-MCP path proven. | 🔄 Needs hw88 live walk (MCP tool-exec test #2930 authored, not run live) |

### Pillar 5 — Sovereign independence post-`bp-self-sovereign-cutover`

| # | Capability (what the end-user gets) | How YOU verify it (walk-through steps) | What we tested + evidence | Acceptance |
|---|---|---|---|---|
| 5.1 | `bp-self-sovereign-cutover` installs dormant at bootstrap-kit slot 06a and is operator-triggered (not auto-run). | `kubectl --context hw88 get hr -A \| grep self-sovereign-cutover` → present but inert (no Jobs running) pre-CTA. In the Sovereign Console "Sovereignty" tab, confirm the **"Achieve True Sovereignty"** CTA is present and the cutover is not yet started. | Dormant-at-slot-06a install per DoD §1 Pillar 5 + ADR-0002. Dual-token wiring (Gitea + Harbor robot tokens) for the post-cutover local-pull path shipped #2918. Cutover choreography code shipped #2962. | 🔄 Needs hw88 live walk |
| 5.2 | After cutover, the Sovereign reconciles **purely** from local Gitea + Harbor and survives the 10-min deny-egress hold (zero egress to github.com / ghcr.io / harbor.openova.io). | Click "Achieve True Sovereignty". After the 8 sequential Jobs complete, the final step applies a 10-min deny-egress NetworkPolicy. During the hold confirm all three proofs (DoD §7): (a) Hubble/NetworkPolicy log = zero allowed flows to `github.com`/`ghcr.io`/`harbor.openova.io`; (b) `kubectl get hr -A` = every HR Ready=True at minute 0 AND minute 10; (c) Console "Sovereignty" tab shows `cutoverComplete=true` with the hold timestamp. | 8-tether pivot in dependency order + 10-min deny-egress hold is the only acceptance evidence (DoD §7, Principle #11, ADR-0002). Cutover code shipped #2962; dual-token local-pull path #2918. The live 10-min deny-egress hold walk (the egress-block proof) has **not** yet been executed on hw88 — no cutover claim without that proof. | 🔄 Needs hw88 live walk (cutover code #2962 shipped; 10-min hold pending) |
