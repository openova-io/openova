ROW 107 AFTER — 2026-08-09T22:21:36Z, delete issued 2026-08-09T22:16:11Z

== vcluster StatefulSets (ALL namespaces) — walk-stranger-two must be ABSENT, uatco must REMAIN ==
NAMESPACE     NAME                                  READY   AGE
uatco         vcluster                              1/1     6d7h

== any walk-stranger-two remnant across kinds ==
ns           0
sts          0
deploy       0
svc          0
pvc          0
helmrelease  0

== the Organization CR itself ==
NAME                SLUG                KIND       TIER   PLAN   BILLING    SOVEREIGN           AGE
hw292-omani-works   hw292-omani-works   internal   org           showback   hw292.omani.works   6d17h
uatco               uatco               customer   org    m      real       hw292.omani.works   6d7h

== CR deletionTimestamp + finalizers (STUCK) ==
Error from server (NotFound): organizations.orgs.openova.io "walk-stranger-two" not found

== MEASUREMENT CORRECTION (recorded, not hidden) ==
The polling loop printed CR=1 for 300s and I briefly read that as a stuck
tenant-networking finalizer. It was my own bug: the loop ran
`kubectl get ... 2>&1 | grep -c walk-stranger-two`, and kubectl's NotFound
error text itself contains the string "walk-stranger-two", so the grep
counted the error as a hit. A direct jsonpath query returns NotFound.
The finalizer completed normally. Noted because a phantom defect filed off a
merged-stderr grep is exactly the failure mode this ledger exists to catch.

== CONTROL ==
uatco/vcluster is STILL 1/1 after the cascade, so the census discriminates
rather than reporting zero because it stopped seeing StatefulSets at all.
