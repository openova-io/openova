> **Reference output of the ultracode understanding workflow (`wf_6dd712c8-553`, 10 read-only readers → synthesizer, 2026-09-02 ~09:45Z), kept verbatim for the record. It was written for the hw306 fire and is SUPERSEDED by `train-hw307.md`, which carries the executed evidence and the critics' corrections. Do not execute from this file.**

# Train manifest — hw306.omani.works (v2, 2026-09-02T09:00Z — hw305 WIPED + zero-remnant verified; fire pending gates G6/G7)

Tracking issue: **#6778**. Plan v1 merged as PR #6779 (`63c8a5c0f`, pre-wipe evidence commit `607e08de1`). This v2 is the synthesis of the ten read-only readers of workflow `wf_6dd712c8-553` (delta-classifier, pin-auditor, cutover-2region-analyst, ledger-partitioner, canon-extractor, freshprov-trap-historian, huawei-inventory, agentic-input-checker, walk-tooling-readiness, open-pr-triage) plus the execution evidence the orchestrator produced after they ran (§9). Where a reader and live execution disagree, execution wins; where readers disagree with each other, the safer reading is taken and the disagreement is named. Nothing in this file is a walk stamp.

Founder decision 2026-09-02 (verbatim intent): *"proceed with the new hw306 environment to implement all the fixes piled up in the main branch … make the 306 be the best environment setup so far … follow all the principles … reach the ultimate perfect state with no going in circles."*

---

## 1. Why this fire exists, the founder decision, and the rules it obeys

**Why.** hw305 (`hw305.omantel.biz`, dep `b2b00ce4c833badf`) was fired 2026-08-23T10:03:17Z from main `f03a151ef` (last content commit `e405aec53`). Since then main advanced **1263 commits** = 960 `docs(tracker)` auto-refresh + **303 signal** (35 merges; 268 non-merge = 130 deploy-bot pin/image bumps, 50 `docs(uat)` stamps, 31 other docs, 28 fix, 23 feat, 2 test/ci), touching 591 files (products/chargeback 126, docs/ledger 126, products/catalyst 92, clusters/_template 28, platform/self-sovereign-cutover 12, 17 platform charts, core/ 3) — delta-classifier. hw305 could not receive any of it: region-A's bootstrap-kit Kustomization has been `spec.suspend=true` at `main@35b4e9df` (2026-08-26T10:00Z) since step-06 pushed its rewrite into the local Gitea (rev `aab93a7a`, not on GitHub), and region-B tracks its own Gitea mirror frozen at `main@25eba1a5` (2026-09-01T10:00Z) — so **everything merged after 2026-08-26 (region-A) / 2026-09-01T10:00Z (region-B) has never run on a live Sovereign** (delta-classifier `hw305-delivery-state`; memory `reference_mid_cutover_sovereign_cannot_receive_fixes`). hw305's cutover looped FATAL at step 06 every ~16 min for 10 days (`#5596 secondary me-east-215-b-1: 63 HelmRepository(ies) on oci://ghcr.io/openova-io after the pivot`) and region-B sat at 58/66 with the #6760 rollback class whose fix `ca1546627` was unreachable. A fresh 2-region fire from main is the only delivery path for the train.

**Rules in force (each is a gate, not a preference).**

| # | Rule | Source | Enforcement in this train |
|---|---|---|---|
| R1 | **One environment at a time** — hw305 wiped in the record AND zero-remnant in the cloud before hw306 fires (#5111) | `feedback_one_environment_at_a_time_wipe_before_fire`; PROTOCOL §5.0; `prov-preflight.sh` check 2 | done: §9.2 wipe 202 → `wiped` 07:47:13Z; §9.3 zero-remnant 08:51Z |
| R2 | **Protect-list promotion** — RUNBOOKS.md:43 and PROTOCOL.md:242 say "as of 2026-08-26: **hw305**"; promoting production to hw306 is part of the fire, landed in the same PR as the FIRED/READY lines | RUNBOOKS §0.1 maintenance rule; PROTOCOL §1.2/§2.1a lockstep | lane B PR after `ready` |
| R3 | **Debug-before-wipe** (#3132) — cloud-init log + cutover CM + region-B state banked before the wipe | RUNBOOKS §0.6; RT-9 | done: `docs/sessions/2026-09-02/hw305-forensics/` (10 files, in #6779) |
| R4 | **Max 2 fires/day**; hw306 is fire 1; a failed hw306 re-fires as hw307, never the same number | RUNBOOKS §0.4; RT-8 | §5 gate table |
| R5 | **No catalyst-api / catalyst-ui / umbrella merges while the prov is in flight** — the tofu apply runs inside the mothership catalyst-api pod (`strategy=Recreate`); a roll abandons the apply (hw127, hw265) or reverts a wipe (hw282). `catalyst-build.yaml` fires on `core/console|admin|marketplace|marketplace-api/**`, `products/catalyst/bootstrap/**`, `products/catalyst/chart/**`, `infra/providers/**` — so every lockstep chart PR also re-tags catalyst-api. The mothership Flux `catalyst-platform` Kustomization is `suspend=true`, so only a HAND roll can abandon the prov; the freeze therefore covers hand-rolls and 4b-relevant merges | `feedback_never_merge_catalyst_api_prs_right_before_firing_a_prov`; `reference_deploybot_catalyst_api_roll_mid_fire_abandons_prov`; RT-5 | freeze from the wipe POST (07:33Z) until hw306 `ready`; chart-only PRs that must reach hw306 merge after `ready`, before the cutover start (§3.5) |
| R6 | **NodePorts absolutely forbidden**, including tests | founder 2026-07-03; CLAUDE.md | `check-no-nodeports.sh` = 0 across 72 charts (delta-classifier, historian); `check-live-nodeports.sh` on hw306-a/-b at `ready`; hw305 ELB pools carried only 80/443/8080/8443 members (huawei-inventory) |
| R7 | **Carry-forward UAT runs AFTER `ready`**, never before | RUNBOOKS §0.3 step 2; PR #6330 lesson | `reset-uat.py hw306` only once the record holds `ready` (§7.1) |
| R8 | **Canonical lifecycle endpoints only**; every POST gated on the response body, never the echo | `feedback_canonical_wipe_endpoints`; `feedback_wipe_fire_posts_swallowed_by_deploy_roll_502` | `hw306-lifecycle.sh` (scratchpad) wraps wipe/preflight/fire/poll |
| R9 | **Agent reports are claims** — the orchestrator re-queries live state before propagating | PRINCIPLES Part III | every §9 line carries the command |
| R10 | **Walkers are read-only, each with its own headless Chromium**; the Playwright MCP browser is one shared session | `reference_parallel_uat_walkers_own_browser`; `feedback_parallel_playwright_agents_share_one_browser` | §7.6 walker brief |
| R11 | **File-editing agents in worktree isolation**; ≤6 concurrent on this host | `feedback_always_dispatch_agents_with_worktree_isolation` | lanes A/C |
| R12 | **Never `AskUserQuestion`**; auto-pick highest-ICE, file the rest | CLAUDE.md autonomy mandate | — |
| R13 | `Refs #N` in PR bodies, never `Closes #N` | CLAUDE.md | — |
| R14 | Banned terms: Organization, sovereign-admin, Blueprint, Environment, Application | GLOSSARY | — |
| R15 | **A denied tool call is one command, never a blocked goal**; no security-shaped gates on our own pre-live infra | founder 2026-08-06 | — |
| R16 | **Mid-cutover merge-hold** (RT-11): no chart publishes / kit-pin bumps from cutover step-03 through step-08; step-03 mirrors the pins frozen by step-01 and step-06 Phase-3a asserts that set | PROTOCOL §4 RT-11; `reference_cutover_step06_stall_prewarm_catalog_latest_vs_flux_pinned_drift` | hold from `POST /api/v1/sovereign/cutover/start` until `cutoverComplete=true` |
| R17 | **Zero-touch**: no kubectl apply/patch/helm on hw306 to make it converge; fix at source, let Flux heal, or re-fire | `feedback_zero_touch_never_manually_unwedge_prov` | walker briefs; the only in-place lever is the console Retry/suspend-resume |

---

## 2. Live state

### 2.1 At plan time (2026-09-02T06:49Z, orchestrator-verified; readers re-verified ~07:00–07:20Z)

| Surface | Measured | Command / source |
|---|---|---|
| Mothership deployments | exactly one: `b2b00ce4c833badf` `ready` `hw305.omantel.biz`, startedAt 2026-08-23T10:03:17Z, `phase1FinishedAt` 11:48:07Z (**105 min**), `handoverFiredAt` 11:48:09Z; `secondaryDegraded:true` | owner `GET /sovereign/api/v1/deployments`; `hw305-forensics/deployment-record.json` |
| hw305 regions | `me-east-215-a` primary 65/65; `me-east-215-b-1` secondary **58/66 degraded** (6 non-ready: bp-external-secrets rollback→v8, bp-external-secrets-stores, bp-mimir rollback→v7, bp-grafana, bp-harbor, bp-oidc-gate dependency chain) | `~/.kube/sovereigns/hw305-b.yaml` `get hr -A` |
| hw305 cutover | `cutoverStartedAt` 2026-08-23T11:48:29Z (22 s after phase-1 ready — handover auto-fire works), `cutoverComplete=false`, progress 45 %, steps 01–05 `success` (2026-09-01 10:05–11:53Z; step-03 106 min after the #6004 ghcr freeze was worked around by PR #6762), step 06 FATAL every ~16 min (Jobs 1788332116 → 1788332476 → 1788332836, each BackoffLimitExceeded) | `hw305-forensics/cutover-status-region-a.yaml`, `cutover-step06-helmrepository-patches-FATAL.log` |
| step-06 mechanism (measured, 4 live reads + source) | region-B GitRepository `flux-system/openova` → `http://gitea-http.gitea.svc.cluster.local:3000/openova/openova` = region-B's OWN Gitea (chart 0.1.193, #6490), populated from GitHub by step-01's secondary Job (`main@25eba1a5`); step-06 Phase 2 pushes the kit rewrite only to `${GITEA_INTERNAL_URL}` = region-A (`06-helmrepository-patches-job.yaml:2484 git push origin main`; 0 hits for any secondary push); region-B's kit still carries the 65 literal `url: oci://ghcr.io/openova-io` lines, so the #5596 poke re-applies them: `pivot ok=65` → `63 HelmRepository(ies) on oci://ghcr.io/openova-io after the pivot`. Region-A survives only because its kit Kustomization is suspended. The `sed edited 0 / git diff empty` lines are the idempotent re-run path, NOT a sed-shape mismatch (all 65 kit files match the Phase-2 grep exactly); `Phase-2.5 WARN: sed rewrite of existing block failed` reproduces rc=0 in `alpine/k8s:1.31.4` under podman → root cause NOT MEASURED, harmless on a fresh prov | cutover-2region-analyst, delta-classifier (agree) |
| Region-B auth at pivot time | GitRepository `Ready=False authentication required` against `cutover-gitea-git-auth` (0.1.199 per-region admin password drifted over the shared Gitea DB = #6754 BUG 1); fixed on main by `5501fed08` (0.1.201) which hw305 cannot receive; Ready=True again by 06:49Z | ledger-partitioner, analyst |
| Mothership catalyst-api at 06:49Z | image `32ef8cc` (commit `32ef8cc94`, 2026-08-21); `infra/providers/**`, `internal/provisioner`, `internal/providers` byte-identical to main (only `infra/providers/hetzner/README.md` differs); handler delta 19 files incl. `sovereign_dns_records.go` +9 (adds `chargeback` to the parent-zone A-record allowlist — absent from 32ef8cc, hw306 would have fired without `chargeback.<fqdn>`) | `git diff --stat 32ef8cc94..origin/main` (canon-extractor, historian, huawei-inventory agree) |
| Mothership Flux | `catalyst-platform` Kustomization `spec.suspend=true` (lastApplied `main@2ccac962`) → nothing rolls automatically; `openova-harbor` Kustomization READY=False is a health-check artifact, Harbor `/api/v2.0/health` healthy | historian, canon-extractor |
| Phase-1 image/DNS path | bastion proxy `http://212.72.24.20:5000/v2/` and `:5002/v2/` → 200; `harbor.openova.io/api/v2.0/health` healthy; proxy-dockerhub/ghcr/quay/gcr/k8s projects 200; powerdns/pdns-pg/dnsdist Running; node `vmi3116389` DiskPressure=False, nodefs 71.3 % (<85 % #3889 high-water) | historian, pin-auditor |
| Huawei kom4dc quotas (07:14:48Z) | vpc 3/5, **publicIp 10/10 FULL**, subnet 5/100, loadbalancer 2/50; ECS 13 (12 hw305 + bastion), VPC 3, EIP 10 = 6 hw305 in service + bastion + **3 orphan DOWN `catalyst-b2b00ce4-nat-preflight-{1,2,3}`** (212.72.24.25 / 5.37.79.43 / 212.72.24.35 — a code leak in `preflight_eip.go` allocateCleanEIP's error path, see §4 M8), NAT 2, ELB 2, EVS 112 (12 system + 96 CSI attached + 2 bastion + 2 detached hw305-era pvc-*), OBS 49 (1 hw305 + 48 archive), 2 vpc-less orphan subnets/networks + 1 SG + 2 keypairs from kv030704/kv184704/hw284 (pre-existing, excluded from the diff) | huawei-inventory `inventory.json` |
| Chart pins vs ghcr | 68/68 bootstrap-kit pins PUBLISHED; 76/76 resolvable catalog-seed pins (5 unlisted scaffolds :1.0.0 declared in `expected-unpublished-catalog-seed-pins.yaml` = WARN-only at prewarm); umbrella 1.4.1623 digest `sha256:15cb3507…` byte-identical seed/values; cutover 0.1.201 (ghcr 2026-09-01T17:14:48Z); all 21 umbrella image tags PRESENT; Blueprint Release 23 consecutive `success` since 2026-09-01T06:42Z | pin-auditor; `check-bootstrap-kit-pin-sync.sh --check-ghcr`; `check-catalog-seed-lockstep.sh --check-ghcr`; `check-train-coherent.sh --ref origin/main` COHERENT 4/4 |
| Payload gate (#6004) | cutover chart 85.60 % (87.18 % with the 2-region overlay; CI measures defaults only), umbrella **88.77 %** of the 1 MiB ceiling vs 90 % budget (12,916 bytes headroom), kyverno 80.4 %, gitea 7.4 % | `check-helm-release-payload-size.py` (historian, analyst) |
| DNS | `omani.works` / `omantel.biz` / `omani.homes` / `omani.rest` delegated to `ns1-3.openova.io`; `omani.homes` SOA mname `a.misconfigured.dns.server.invalid.` (PowerDNS default-SOA marker; org-pool worked on hw305 — open); no residual hw302 records; no `hw306.*` records yet | canon-extractor `dig` |
| LE rotation | hw302 `omani.works` 2026-08-20, hw305 `omantel.biz` 2026-08-23 → both outside the 7-day window by date (crt.sh count NOT MEASURED); **hw306 → `omani.works`** | RUNBOOKS §0.3 L3 |
| UAT ledger (main) | 286 rows: ✅250 ❌12 ⚠️7 ⏳17 (header hw305, walked from 2026-08-23). Non-green: RED G3 G8 G9 G11 67 166 219 220 221 222 227 235; PARTIAL R21 16 89 223 225 232 241; CARRIED W2 3 8 52 56 60 62 91 94 109 133 159 189 212 213 234 242 | `docs/ledger/UAT.md:1-3` |
| Scheduler | `uat-confidence.py --self-test` 21/21; `--due --env hw306` = **183 of 286** (FAILING 6: 166 219 220 221 222 G11; DUE 177; SKIPPED 103). #6778's "177" is the DUE partition alone. Of the 36 non-green rows, **17 are NOT due** (G3 67 227 235 16 232 241 W2 8 52 56 62 109 212 213 234 242) because `--observe` never parsed the HTML ledger (only 2 hw305 observations exist) | measured 2026-09-02T08:5xZ; walk-tooling, ledger-partitioner |
| Anthropic credential | mothership `catalyst/sovereign-anthropic-credentials` (created 2026-08-13T23:53:17Z): `expiresAt` 2026-08-14T04:28:12Z; GET `api.anthropic.com/v1/models` → **401 "OAuth access token has been revoked"**; refresh token SPENT (hw305-a catalyst-api `[ANTHROPIC-REFRESH] … HTTP 400` ×6 / 522 ×1 in 24 h); same bytes (sha `b46b5a27…`/`ade7d0df…`) in every copy; hw305 `chepherd-rtz-a-bp-agenity-0` Init:CrashLoopBackOff ×2708 `#6163 EXPIRED (~458h)` | agentic-input-checker (fingerprints only) |
| Walk toolchain | node v22.22.2, playwright 1.59.1, chromium-1217 + google-chrome 148 launch headless (2.6 s screenshot); pyjwt 2.13 / cryptography 48; IMAP 45.151.123.50:993 reachable; guards `uat-drift-guard --self-test` 4/4, `reset-uat --self-test` PASS, `check-uat-fix-sha-citations --self-test` 5/5; **broken on the HTML ledger:** `uat-confidence.py --observe` (:620), `uat-tally.py` (:76 → reports 0/0), `check-walk-respects-scheduler.sh` (:302 reinstatement set always empty; `ALLOW_FULL_WALK` not wired in the workflow) | walk-tooling-readiness |

### 2.2 Execution state now (2026-09-02T09:00Z — supersedes the readers where different)

| Step | Evidence (details in §9) |
|---|---|
| Mothership roll | catalyst-api AND catalyst-ui hand-rolled 32ef8cc → **b27d54a** at 07:26:04–07:26:56Z; rollout settled; owner `whoami` 200; `prov-preflight.sh` check 4b now PASSES; Flux `catalyst-platform` stays suspended |
| Manifest v1 + LANE-B-EXTRACTED | PR #6779 merged `63c8a5c0f`; `LANE-B-EXTRACTED: 05f983d8b` (RT-7/RT-10 wipe gate satisfied) |
| hw305 wipe | `POST /deployments/b2b00ce4c833badf/wipe -d '{}'` → **202** at 07:33:24Z; record `status=wiped` at **07:47:13Z**; providerPurge removed 2 ELBs, 2 VPCs, 2 subnets, 5 floating IPs (incl. the 3 orphan nat-preflight EIPs) |
| Zero-remnant | signed GET at **08:51Z**: quotas vpc 1/5, publicIp 1/10, subnet 3/100, loadbalancer 0/50; ECS 1 (bastion), VPC 1, EIP 1 (212.72.24.20 only), NAT 0, ELB 0, EVS 2 — equals `wipe-targets-and-postwipe-baseline.json` `expected_counts_after_full_wipe` exactly |
| Cooldown | ≥15 min since `wiped` (07:47Z → 08:51Z = 64 min) — the EIP-pool / VPC-NAT release lags (RUNBOOKS §0.2) have elapsed by wall-clock; preflight 3g is vacuous after the record reap (§5) |
| Fire decision | **`fireCutoverOnHandover:false`**; cutover started later from the sovereign-admin CTA / `POST /api/v1/sovereign/cutover/start` once `bp-self-sovereign-cutover 0.1.202` (region-B durable pivot) is pinned on main and delivered to hw306 pre-cutover |
| Fixer PRs | #6780 (newapi funnel pin 1.4.155 + pin-parity guard on Blueprint Release completion; touches `core/services` → org-services rebuild → **merge only after `ready`**); #6781 (`uat-confidence.py` HTML-ledger parser, scripts-only, MERGEABLE/UNSTABLE); #6783 **HELD** (hw305 observations CSV: with it, `uat-drift-guard` withdraws consent on 16 rows R12 R16 R22 G2 34 41 69 70 71 187 188 207 229 236 237 239 because never-discounted hw293 FAILs drop them to box 0 — scoring decision pending); step-06 fixer (0.1.202) running, no PR yet |
| Anthropic | EXPIRED and REVOKED by use; founder notified; rows G8 G9 219 220 221 222 223 INPUT-gated (223: ledger-partitioner says its curl legs need only the chart-rendered `agenity-mcp-bearer`, agentic-input-checker says the clause needs an authenticated agent — safer reading: count it gated, still attempt the curl legs, §7.3) |

---

## 3. Release train (PROTOCOL §4 RT-1…RT-11)

**RT-1 manifest** = this file. **RT-2 ≥2 independent passengers** = yes (§3.1, 12 groups). **RT-3 what the converged env could still verify** = nothing new (hw305 severed; 250 greens banked; forensics banked under RT-9). **RT-4 harness cases merged before the fire** = cutover-contract.sh Cases 96/97 (#6754/#6511), `step6764-secondary-harbor-prewarm.sh`, `jobs_async_seed_6749_test.go`, `check-train-coherent.sh` COHERENT. **RT-5** = §1 R5 freeze. **RT-6 every live patch → merged PR** = the mothership hand-roll is convergence to the declared pin (no source change; §9.1). **RT-7** = `LANE-B-EXTRACTED: 05f983d8b`. **RT-8** = fire 1 of 2. **RT-9** = `hw305-forensics/`. **RT-10** = n/a (fresh prov, not a cutover re-fire). **RT-11** = §1 R16.

### 3.1 Passengers — every fix on main since hw305, by pillar, with the rows it should flip

Consumption mechanism (verified): cloud-init writes the Flux GitRepository `https://github.com/openova-io/openova@main` (`infra/providers/_shared/cloudinit-control-plane.tftpl:514-565`; hw305 tfvars `gitops_branch=main`) → hw306 consumes main AT FIRE TIME and keeps following main until cutover step-05 pivots the source to the local Gitea → **every kit-pin bump merged during Phase 1 is a live helm upgrade mid-bootstrap** (the #6760 class) — hence R5.

| Pillar | Passenger (sha · issue/PR) | Reaches hw306 via | Rows to walk on hw306 | First live run? |
|---|---|---|---|---|
| 5 Sovereignty | `587c2440e` step-01 activeDeadline 2700 s (#6511, 0.1.198); `6dce604d5` region-local PAT mint (#6645, 0.1.199, superseded); `464e9bba3` step-03 secondary-region Harbor prewarm + coredns pin + self-verify (#6764, PR #6770, 0.1.200); `5501fed08` step-01 shared `catalyst-gitea-token` PAT for secondaries + batched default-branch-first push (#6754, PR #6771, 0.1.201); `8ee4abb7e` render test | slot `06a` pin 0.1.201 → **0.1.202** once merged (§4 M1) | G11 166 227 159 162 165 181/32/111 (Harbor post-pivot) 243 (split-horizon) | 0.1.200/0.1.201 secondary legs NEVER ran live (hw305 froze at 0.1.199) |
| 3 DR / region-B | `79d1d213b` bp-postgres 0.2.25: consumer `-mesh/-mesh-rw` aliases follow a #6149 promotion (#6753, PR #6756) — measured live on hw305: old pin NXDOMAIN-crashlooped region-A grafana/powerdns; `ca1546627` bp-external-secrets 1.1.6 / bp-mimir 1.0.11 / bp-vpa 1.0.6 upgrade-clean hooks + kubectl-entrypoint image (#6760, PR #6768); `35ca70338` bp-seaweedfs 1.2.6 explicit maxVolumes (#6766 — `-max 0` filled 6 slots, every S3 write failed) | slots 16a/16c/16d, 15, 23, 29, 18 | G3 67 235 14 R13 104 198 237; region-B `hrReady == hrTotal`; seaweedfs volumes >2, openbao-snapshot-save Completed | 0.2.25 upgrade proven on hw305-B; `ca1546627` NEVER ran live (not in region-B mirror 25eba1a5); 1.2.6 never ran live |
| 1 Marketplace / funnel | chargeback Blueprint ADR-0014 (#6723: `92faa4ea5` slot 13f, `109627c02`, `3de212baa`, `346da5a99`, `21ca53641`; chart 0.1.2 + image `chargeback:0.1.2`; DNS allowlist `sovereign_dns_records.go` on the rolled mothership); `20f094538` + `6525289fd` bitnamilegacy/kubectl → `harbor.openova.io/proxy-dockerhub/alpine/k8s:1.31.4` across 16 charts (#6755, #6769); `6b10c7524` gitea `.helmignore` (#6762) + `27cf6e4ff` payload model excludes subchart source (#6763); newapi `b27d54a23` admin-promote CNPG image `:16` (#6759, PR #6775) + `80b66b87e` upstream v0.13.2 (1.4.155) | umbrella 1.4.1623 catalog seed; slots 02 02a 03 08 10 14 15a 16 19c 27 52 55a 55b; slot 80 | 75-82 84 85 88 89 91 93 94 (funnel), 225 (needs a cart with openclaw/newapi), 3 8, spine 104/198/R13/237 (+1 HR for chargeback), new rows proposed for chargeback (§7.5) | chargeback = first-ever fresh-prov install; 16 repinned charts' fresh-INSTALL hooks untested (upgrade proven hw305-B for cnpg/gitea/guacamole/kyverno/postgres) |
| 4 Agenity / MCP | `fbe899159` bp-agenity 0.5.33 `spec.consoleUI` sidebar registration (#6723 lane C); `f2105df68` per-Sovereign sidebar overrides; `6a700f5ca` Org-scoped sidebar; `fd1d40322` renew-before-expiry (#6317, PR #6498, already in 32ef8cc) | umbrella + funnel pin agenity=0.5.33 | 218 2 R19/M4; **G8 G9 219 220 221 222 223 INPUT-gated** | agenity runtime cannot start without a fresh credential |
| 2 / topology | `f951e92` organization-controller: `Organization.spec.costSources[]` typed round-trip + CRD +53 lines (#6733) | umbrella image pin | regression watch 5 9 10 11 12 99 101 107 R17 | — |
| Sovereign-admin console (tertiary, not pillar work) | `7d25c2908` /jobs served from the durable jobs.Store first, async refresh (#6749, 60 s → 47 ms); `44005b070` suspended HRs render Dormant (#6767); `0bc54baf5` k8scache watches CronJob/Job (#6705); #6696 treemap real health; #6698 no-cache index; **#6699 Cloud → Resources rename** (row 2 literal + rows 198/203 `/cloud` — re-baseline, not a regression); #6700-#6702 engine names/Kind chips; #6704; #6706/#6708/#6710/#6717 chips + List/Graph toggle on the openova-flow DAG; #6709 CronJob schedule view; #6727 phase barriers; #6742 #6744 #6747 | catalyst-api/ui `b27d54a` in umbrella 1.4.1623 | 17 18 20 97 102 105 (treemap); 161 162 165 168 173 190 191 (/jobs); 2 118 160 198 203 (nav); 16 | never ran live (hw305 console image 56b748b) |
| Unchanged since hw305 | marketplace-api `0d9531a`, console `c290d0a`, admin `06ea9d4`, environment-controller `e6c1533`; openova-mcp 0.1.7, wordpress-tenant 0.4.26, openclaw 0.2.19, stalwart-tenant 0.1.16 | — | 75-82 91 94 212 213 223 89 232 234 = carry-forward walks only | — |

### 3.2 Chart pins that moved (23 of 66 kit charts) — all PRESENT on ghcr (pin-auditor, per-package `gh api /orgs/openova-io/packages/container/<pkg>/versions`; the org-level listing hides public bp-* packages)

bp-catalyst-platform 1.4.1557→**1.4.1623** (52 kit bumps; hw305-A frozen at 1.4.1563, -B at 1.4.1603) · bp-self-sovereign-cutover 0.1.197→**0.1.201** · bp-postgres ×3 0.2.24→0.2.25 · bp-external-secrets 1.1.4→1.1.6 · bp-external-secrets-stores 1.0.9→1.0.10 · bp-mimir 1.0.9→1.0.11 · bp-vpa 1.0.4→1.0.6 · bp-seaweedfs 1.2.5→1.2.6 · bp-newapi 1.4.153→1.4.155 · bp-gitea 1.2.49→1.2.50 · bp-kyverno 1.3.8→1.3.9 · bp-cnpg 1.0.14→1.0.15 · bp-cert-manager 1.2.6→1.2.7 · bp-cert-manager-issuers 1.0.0→1.0.1 · bp-flux 1.2.7→1.2.8 · bp-openbao 1.2.67→1.2.68 · bp-opentelemetry-operator 1.0.5→1.0.6 · bp-crossplane-claims 1.3.4→1.3.5 · bp-guacamole 0.2.43→0.2.45 · bp-huawei-evs-csi 1.0.9→1.0.10 · bp-hcloud-csi 1.2.1→1.2.2 (suspended on Huawei) · **new slot 13f bp-chargeback 0.1.2** (host ns `chargeback`, dependsOn bp-cnpg + bp-catalyst-platform, ClusterIP only, 2 hook Jobs, HTTPRoute `chargeback.<fqdn>`). Umbrella images: catalyst-api/ui `b27d54a`, services-* `b27d54a`, organization-controller `f951e92`, continuum-controller `f0ab8a5`, others unchanged — all resolve to main-ancestor commits. Publish-vs-pin order measured: ghcr `created_at` precedes the auto-bump commit by ~10 s in every recent case (1.4.1623: 01:04:51Z vs `8e46052af` 01:05:02Z), so main's pins never point at an unpublished tag unless a Blueprint Release run fails.

### 3.3 Cutover chain — per-step verdict for a fresh 2-region hw306 on chart 0.1.201 (cutover-2region-analyst; engine `handler/cutover.go` unchanged since 08-23)

| Step | Verdict | Basis |
|---|---|---|
| 01 gitea-mirror | UNKNOWN, lean PASS | hw305 success on 0.1.199 (2m17s); 0.1.201 shared-PAT secondary leg never live; capture region-B `cutover-gitea-mirror-secondary-<region>` log (ns gitea, ttl 3600 s) |
| 02 harbor-projects | PASS-EXPECTED | hw305 success 5 s; no change |
| 03 harbor-prewarm | UNKNOWN (primary lean PASS; secondary leg unproven) | hw305 106 min after the #6004 workaround; first pod burnt ~90 min on docker.io `toomanyrequests` + proxy-dockerhub 503; retry 13 min (`chart_ok=102 fail=0 soft=5`, A2-guard 27/27, A3 158/158); #6764 secondary Harbor prewarm never live; **budget ~110 min** |
| 04 registry-pivot | PASS-EXPECTED | hw305 15 s, 6/6 nodes; accepted gap: region-B nodes keep `harbor.openova.io` routing (no DaemonSet there — "the remaining #5359 increment") |
| 05 flux-gitrepository-patch | PASS-EXPECTED (0.1.201 path unproven) | hw305 success; 0.1.201 secretRef = shared PAT; **severance point** (#6307): nothing merged upstream reaches the Sovereign afterwards |
| 06 helmrepository-patches | **FAIL-EXPECTED on 0.1.201** → PASS-EXPECTED only with 0.1.202 | §2.1 mechanism; no commit on main since 08-23 touches the step-06 Job manifest; #6202 (engine re-adopts a failed Job) open → a post-start fix is unreachable |
| 07 catalyst-api-env-patch | PASS-EXPECTED | hw298 success on 0.1.190; rolls the Sovereign catalyst-api (engine host) — #6288/#6289 open; if the engine rewinds, re-POST `/api/v1/sovereign/cutover/start` |
| 08 egress-block-test (runs 11th) | UNKNOWN (last measured FAIL) | hw298 FAILED at 138 s of the 600 s hold on 0.1.190; the FATAL line was never recorded — capture it first this time; both regions must be 100 % Ready before the hold |
| 09 gitea-token-mint | PASS-EXPECTED | hw298 success |
| 10 vcluster-registry-pivot | PASS-EXPECTED | hw298 success incl. region-B leg (#6334) |
| 11 crossplane-provider-pivot | PASS-EXPECTED | hw298 success; #5204 closed |

Reader disagreement: delta-classifier and the analyst both read step 06 as structurally failing; ledger-partitioner read G11/166 as FIX-MERGED-AWAITING-ENV on 0.1.201 (auth fix only). Safer reading taken: **step 06 fails on 0.1.201**; hence `fireCutoverOnHandover:false` + 0.1.202 before the cutover start.

### 3.4 Open PRs (open-pr-triage, 50 open: 34 non-dependabot, 16 dependabot) — none is a required passenger

Superseded/stale → close: #6758 (→#6771), #6322 (→#6498), #6320 (→#6500), #5985 (→#6094), #5952, #6181, #6486, #6481, #6479, #6359, #6346, #6343, #6341, #6340, #6337, #6304, #5329 (dup of #5356). LEAVE (would FATAL the cutover: 07a probes a relay nothing installs): #6473, #5959. Hold until `ready` (catalyst-api/ui/chart roll class): #6176, #6174 [DO NOT MERGE], #6168, #5992, #5968, #6365, #6470, #5964, #6480, #5978, #6474, #6178 (its bp-harbor half is a live defect: `harbor-alias-redirect` HTTPRoute `Accepted=False/NoMatchingParent`, `https://harbor.hw305.omantel.biz/` → 404 — re-cut chart-only as bp-harbor 1.2.48 after `ready`). Dependabot ×16: leave (6 UNSTABLE). Three dirty worktrees hold no unlanded work (stale-index artifacts).

### 3.5 Merge-window map (the one table every lane obeys)

| Window | Allowed | Forbidden |
|---|---|---|
| now → FIRE | docs/scripts/workflow PRs (#6781 parser; guard wiring §4 M4; manifest v2) | anything on `core/**`, `products/catalyst/bootstrap/**`, `products/catalyst/chart/**`, `infra/providers/**`; umbrella/kit-pin bumps; mothership rolls |
| FIRE → `ready` (Phase 0/1, ~25–105 min) | docs/scripts/workflow PRs only | every kit-pin/umbrella bump (live helm upgrade mid-bootstrap, #6760 class); catalyst-api/ui merges or hand-rolls (abandon the apply) |
| `ready` → cutover start (the pre-cutover window this fire deliberately keeps) | chart-only fixes that must reach hw306: **0.1.202** (+umbrella 1.4.1624), #6780 newapi pin (org-services rebuild → orgTag → umbrella), bp-harbor 1.2.48 re-cut; each re-runs the payload gate (umbrella at 88.77 %) and `check-train-coherent.sh`; wait for Flux on hw306 to reconcile the new pins (`kubectl get hr -A` shows the new chart versions Ready) | mothership hand-rolls (nothing needs them); `reset-uat` before `ready` |
| cutover start → `cutoverComplete=true` | docs/scripts PRs; walks of non-cutover rows | chart publishes / kit-pin bumps (RT-11); mothership rolls |
| after `cutoverComplete=true` | everything, subject to R5 for future fires | — |

---

## 4. MUST-FIX-BEFORE-FIRE and SHOULD-FIX (blocks_fire = would make the fire itself wrong; the rest gates the cutover, the walk, or the ledger)

| # | Item | blocks_fire | Why | Smallest fix | Kind | Status |
|---|---|---|---|---|---|---|
| M1 | **bp-self-sovereign-cutover 0.1.202 — secondary-region DURABLE rewrite in step 06** (#5359/#5596) | **no** (fire proceeds with `fireCutoverOnHandover:false`); **yes for the cutover start** | on 0.1.201 step 06 loops FATAL on any 2-region env; a Sovereign past step-05 cannot receive the fix (#6307, #6202) | factor the Phase-2 transform (url sed + `inject_trust_ref` awk + Phase-2.5 catalog block) into a ConfigMap script; in `pivot_secondary_regions()` after the live patch loop and before `assert_secondary_pivot_durable`, copy it into each secondary's gitea ns and stamp an in-region Job (shape of `cutover-gitea-mirror-secondary-<region>`, PAT `cutover-secondary-gitea-pat`) that clones region-B's `gitea-http` main, rewrites, commits, pushes; gate on GitRepository revision change before the assert; 5 lockstep sites + umbrella 1.4.1624; run the payload gate with the 2-region overlay (<90 %); merge after `ready`, before cutover start | publish-chart | fixer running in a worktree; no PR yet; ghcr has 0.1.201 only |
| M2 | Fire-body correctness | **yes** | hw303 fired with string `"false"` → LE-staging certs + 62/65 stall (provisioner.go:2859 derives `wildcard_cert_use_staging` from `QATestEnabled`); `openovaFlowEnabled` is NOT a Request field (silently ignored) | `jq -e '.qaTestEnabled==false and .fireCutoverOnHandover==false and .sovereignFQDN=="hw306.omani.works" and (.sshPublicKey|length)==104 and (.regions|length)==2 and (.regions[0].cloudRegion)=="me-east-215-a"' body.json` before the POST; body in §5 | config | body drafted (§5); the scratchpad copy `hw306-fire-body.json` still says `true` + carries `openovaFlowEnabled` — regenerate |
| M3 | `prov-preflight.sh` exit 0 on the corrected body | **yes** | RUNBOOKS §0.3 step 3 / PROTOCOL §5.2: non-zero = NO fire; check 4b now green (roll done), checks 2/3/3f/3b were red only because hw305 existed | `HW_TFVARS=$S/hw305-tfvars.json BEARER=$BEARER bash scripts/prov-preflight.sh hw306.omani.works catalyst-hw306-omani-works false false; echo exit=$?` — expect only 3g to print a vacuous `999min` (record reaped) and 3e possibly `⚠ SKIPPED`; enforce the cooldown by wall-clock (done: 64 min) | config | pending (§5 G6) |
| M4 | Ledger guards on the HTML ledger: (a) `uat-confidence.py --observe` (PR #6781, scripts-only); (b) `check-walk-respects-scheduler.sh`: apply `to_pipe` at :302, parse due IDs only from the FAILING/DUE lines (rows 64/177 wrongly admitted), and wire `ALLOW_FULL_WALK` from the PR body in `check-walk-respects-scheduler.yaml:43`; (c) `uat-tally.py:76` `to_pipe` | no | without (a) no hw306 observation can be recorded and the Leitner clock stays frozen; without (b) the 17 non-due non-green rows cannot be flipped ✅ in CI (SIM 4: `TREADMILL` exit 1) and the ⏳→✅ reinstatement carve-out is dead; without (c) the DoD "≥270 green" is unmeasurable (tally prints 0/0) | merge #6781; one scripts+workflow PR for (b)+(c) | doc/config (scripts) | #6781 open MERGEABLE; (b)(c) not yet cut |
| M5 | Scoring semantics for predecessor FAILs (PR #6783 HELD) | no | with real hw305 PASS observations the guard withdraws consent on 16 rows (never-discounted hw293 FAILs); CLAUDE.md canon already says "a wiped environment discounts its evidence and reschedules the row" | implement the wiped-env discount in `uat-confidence.py` (FAILs from an env that is `wiped` decay/are discounted; DECAY 0.85 keeps 44/180 PASS rows stuck) in #6783 before it lands; until then the 17 not-due rows are flipped only via M4(b) `ALLOW_FULL_WALK=1` with a per-row justification | doc/config | HELD |
| M6 | Reset-PR procedure wart (#6634→#6656) | no | `reset-uat.py hw306` writes `(RESET … — pending hw306)`; `uat-drift-guard.py` `header_env()` → None on `pending` → bare CI run exits 1 on every carried ✅ | in ONE PR after `ready`: run reset, rewrite line 1 to `# UAT — Sovereign acceptance walk on \`hw306.omani.works\` (walked from 2026-09-02)`, hand-update the Tally line (expected ✅94 · ❌12 · ⚠️7 · ⏳173), run the six guards bare | doc | procedure |
| M7 | Anthropic `credentialsJson` (founder input) | no | EXPIRED + REVOKED by use; hw306's seed at the primary kubeconfig PUT reads only the mothership env (boot-time snapshot) → `SKIPPED — cannot authenticate` warn event; 7 rows stay red | founder: fresh `claude` login → whole `{"claudeAiOauth":{…}}` → **post-fire path (no mothership roll, no re-prov):** create `catalyst-system/sovereign-anthropic-credentials` (keys `apiKey`/`credentialsJson`) on hw306 region-A; seed reconciler ≤10 min → OpenBao → ESO ≤15 min → delete the crashing agenity pod; verify by the seed labels/events | config (input) | founder notified; not before the fire (a mothership restart mid-prov is forbidden) |
| M8 | `preflight_eip.go` held-candidate leak (3 DOWN EIPs per fire) | no | `allocateCleanEIP(…, maxAttempts=8)` holds blocklisted candidates and returns `err` without releasing them (`RotateBlocklistedNATEIPs` ~L203-206); the wipe blocklists all 9 released hw305 addresses for 24 h (`eip_blocklist_store.go:73`) so a fresh draw is likely blocklisted → rotation → project hits 10/10 → 3 leaked again (non-fatal: provisioner.go:2281-2289 warns) | ~5-line fix: `defer` release of `held` on the error path + cap attempts by free quota; catalyst-api change → **after `ready`**, roll only outside a fire; file the issue now | pr-catalyst-api | no issue yet |
| M9 | newapi funnel pin drift (`helmrelease_apps.go:730` 1.4.154 vs seed 1.4.155; `TestDefaultHRAppPins_MatchCatalogSeed` red on main; deploy-bot push never triggered `test-provisioning-service.yaml`) | no | row 225 needs a funnel Org whose HR pin equals the seed | PR #6780 (+ workflow trigger on Blueprint Release completion) — **merge after `ready`, before cutover start** (org-services rebuild → orgTag → umbrella bump) | pr-catalyst-api (core/services) | open, mergeable UNKNOWN |
| M10 | Payload headroom | no | umbrella at 88.77 % of the #6004 ceiling; one prose bump crosses 90 % and kills Phase 1 deterministically (hw293 shape) | no umbrella prose changes in the window; trim after; add the 2-region overlay run to `blueprint-release.yaml:502` | pr-chart | — |
| M11 | Housekeeping that reduces collision risk for walkers | no | 12 stale open PRs touch `docs/ledger/UAT.md` (§3.4); `sso-uat-flip` bot pushes UNVERIFIED flips on SSO-chain merges | close the stale set before the reset PR; walkers rebase before push | doc | — |

Re-verify at T-0 (procedural, ~3 min, read-only): `scripts/check-bootstrap-kit-pin-sync.sh --check-ghcr` exit 0; `scripts/check-catalog-seed-lockstep.sh --check-ghcr` exit 0; `scripts/check-train-coherent.sh --ref origin/main` COHERENT; `gh run list --workflow blueprint-release.yaml -L 5` all `success`; the four Phase-1 image/DNS probes (§2.1). If a Blueprint Release run is red, publish with `gh workflow run blueprint-release.yaml -f blueprint=<dir> -f tree=<platform|products>` before firing.

---

## 5. The fire — exact body and the pre-flight gate table

**Auth**: `Authorization: Bearer <RS256 owner JWT>` or the `catalyst_session` cookie (`auth/session.go:392-428`); minted from the mothership handover key with claims `email/sub=emrah.baysal@openova.io`, `realm_access.roles [catalyst-owner]`, `tier owner`, `email_verified:true`, unique `jti`, `exp ≤ 2 h` (`scripts/sovereign-lifecycle.sh` `_tok()` shape; memory `reference_handover_mint_needs_email_verified_and_jti`); key shredded after minting; bearer length reported only (808 B at 07:14Z).

**Body** (`provisioner.Request`, `provisioner.go:403` + `RegionSpec:369` + `ParentDomain:169`; field-for-field from hw305's tfvars with TLD/FQDN changed and the cutover flag flipped; Huawei AK/SK, GHCR pull token and Harbor robot token are stamped server-side from the mothership env; `objectStorageBucket` omitted → server derives `catalyst-hw306-omani-works-<8hex>`; `clusterMesh*`, `qaFixturesNamespace`, `qaOrganization` derived; legacy singular `region/controlPlaneSize/workerSize/workerCount` mirrored from `regions[0]` by `Validate()`; `RegionSpec` has no `role` — `regions[0]` is primary and the secondary's live key becomes `me-east-215-b-1`):

```json
{"orgName":"Omantel","orgEmail":"emrah.baysal@openova.io","provider":"huawei",
 "sovereignDomainMode":"byo","sovereignFQDN":"hw306.omani.works",
 "parentDomains":[{"name":"omani.works","role":"primary","registrarKind":"dynadot"},{"name":"omani.homes","role":"org-pool"},{"name":"omani.rest","role":"org-pool"}],
 "sshPublicKey":"<hw305 ssh_public_key verbatim — ssh-ed25519 … openova@bastion-openova, 104 bytes>",
 "regions":[{"provider":"huawei","cloudRegion":"me-east-215-a","controlPlaneSize":"m7n.xlarge.8","workerSize":"m7n.2xlarge.8","workerCount":5},
            {"provider":"huawei","cloudRegion":"me-east-215-b","controlPlaneSize":"m7n.xlarge.8","workerSize":"m7n.2xlarge.8","workerCount":5}],
 "storageClass":"evs-ssd","haEnabled":false,"bcpTopology":"active-hotstandby",
 "marketplaceEnabled":true,"consoleIsolationEnabled":true,"enableSharedPostgres":true,
 "qaTestEnabled":false,"fireCutoverOnHandover":false}
```

Validate-reject cases avoided: `bcpTopology` outside `single-region|active-hotstandby|active-active` (explicit `single-region` with 2 regions is rejected); missing `sshPublicKey`/`sovereignFQDN`/`orgName`/`orgEmail`; `parentDomains` with ≠1 primary; `orgEmail` ≠ session email (403); <2 regions without explicit `bcpTopology` (handler 400, `deployments.go:1506-1515`). Huawei clamps: `workerCount<3 → 3`, CP floor `m7n.xlarge.8`, worker floor `m7n.2xlarge.8` (hw305 values are at/above the floors). `fireCutoverOnHandover:false` is the server default and the #4061 product default — it keeps row 159's TETHERED + CTA state observable and the GitHub-delivery rows walkable.

**Gate table** (in order; the helper gates on the HTTP body):

| Gate | Command | Pass criterion | State |
|---|---|---|---|
| G0 must-fix M2/M3 | §4 | body validated with `jq -e`; preflight exit 0 | pending |
| G1 hw305 forensics | `GET …/deployments/b2b00ce4c833badf/cloudinit-log`; cutover CM; region-B HR/HelmRepository/Flux census | 10 files under `hw305-forensics/` on main (#6779) | **done** |
| G2 hw305 wiped | `POST …/deployments/b2b00ce4c833badf/wipe -d '{}'` → poll `GET` | 202 `{status:wiping}` then record `status=wiped` | **done** 07:33:24Z → 07:47:13Z |
| G3 zero-remnant | signed Huawei GETs (`hw_inventory.py`) vs `expected_counts_after_full_wipe` | ECS 1, VPC 1, EIP 1 (212.72.24.20), NAT 0, ELB 0, EVS 2, subnet 3, OBS 48; quotas vpc 1/5 publicIp 1/10 | **done** 08:51Z (OBS count NOT re-read — expected 48) |
| G4 cooldown + headroom | wall-clock since `wiped` ≥15 min (check 3g is vacuous once the record is reaped: `store.Delete` + 60 s in-memory reap → `999min`); VPC ≥2 free, EVS ≥100 free, publicIp ≥6 free (2-region fire needs cp+nat per region + elb_primary + elb_console = 6, plus up to 3 rotation candidates) | 64 min elapsed; vpc 4 free, EVS 398 free, EIP 9 free | **done** |
| G5 mothership stable | `kubectl -n catalyst rollout status deploy/catalyst-api`; `gh api actions/runs?status=in_progress`; pod name unchanged since the 07:26Z roll | settled >5 min; zero in-progress `catalyst-build`; `kubectl -n flux-system get kustomization catalyst-platform -o jsonpath={.spec.suspend}` == true | verify at T-0 |
| G6 preflight | `HW_TFVARS=$S/hw305-tfvars.json BEARER=$BEARER bash scripts/prov-preflight.sh hw306.omani.works catalyst-hw306-omani-works false false; echo exit=$?` | prints `PRE-FLIGHT PASS — clear for takeoff.` **and exit 0**; append `PRE-FLIGHT PASS vpc=1/5 evs=2/400 eip=bastion-only obs=48/100 — <HH:MM>Z (cooldown wall-clock: wiped 07:47:13Z)` to §9; check 5 is transient on the `sso-zero-click-probe` cron (`17 2 * * *`) — re-run, never waive | pending |
| G7 fire | `T_FIRE=$(date -u +%FT%TZ); curl -s -w '\n%{http_code}' -X POST -H "Authorization: Bearer $BEARER" -H 'Content-Type: application/json' --data @hw306-fire-body.json https://console.openova.io/sovereign/api/v1/deployments` | HTTP **201** `{id,status:"provisioning",streamURL}`; failure map: 400 = Validate message (fix body, re-POST — byo burns no subdomain); 403 = orgEmail≠session; 503 = #3889 disk ≥85 %; append `FIRED <T_FIRE>: POST → 201 dep <id>` to §9 and tick #6778 | pending |

---

## 6. Phase-0/1 WATCH-LIST (detection commands + timeline; freshprov-trap-historian)

Poll every 60–120 s: `curl -s -H "Authorization: Bearer $BEARER" …/deployments/<id> | python3 -c 'import sys,json;d=json.load(sys.stdin);print(d["status"],d.get("phase1Substate"),d.get("secondaryDegraded"),[(r["region"],r["hrReady"],r["hrTotal"],r["degraded"]) for r in d.get("regions",[])],d.get("error"))'`; live stream `curl -N …/deployments/<id>/logs`.

Timeline baselines: Phase-0 tofu apply ≈6–8 min (13 ECS); kubeconfig PUT ≤15 min after (`DefaultKubeconfigArrivalTimeout`, phase1_watch.go:162, else `OutcomeKubeconfigMissing` → failed); first HelmRelease ≤15 min after the PUT (helmwatch.go:189) with a 15-min Flux-CRD probe (phase1_watch.go:213); overall WatchTimeout 120 min + stall window 30 min, guard ceiling 240 min (#5283); console probe 10 min non-fatal (`consoleDegraded`, #4706/#5253); handover cert wait 10 min. Ready wall-clock history: hw285 51 min (record), hw91 65, hw300 ~25 (then cyclic), hw305 **105** (cause NOT MEASURED — mothership logs from 08-23 rotated). Secondary lags 10–20 min and reads `degraded:true` until it catches up (rule: shortfall ≥3 AND <90 % of primary, region_health.go:38-47).

| T+ | Watch | Detection | If it fires |
|---|---|---|---|
| 0–3 min | tofu-init GitHub 504 (#3126/#3128, fixed: plugin cache + 4× backoff) | SSE `transient provider-install/network failure on tofu init` | if all retries fail: record DELETE (no infra), re-fire as hw307 once the provider SHA256SUMS curl from the mothership pod is 200 |
| 0–8 min | EIP conflict / VPC quota (P0-02/03) | SSE `error allocating EIP … conflict` (6×3 min retries), `HCS VPC quota exhausted: project at N/5` | must not happen post-cooldown; if it does: read error, wipe, wait, re-fire hw307 — that is the last fire today |
| ~1–5 min after apply | nat-preflight EIP leak (M8) | Huawei GET publicips shows `catalyst-<dep8>-nat-preflight-*` DOWN | non-fatal; record the ids for the M8 issue; publicIp will read ≤10/10 for hw306's life |
| 6–21 min | kubeconfig never PUT (P1-01) | `phase1Substate=kubeconfig-file-missing`; `GET …/kubeconfig` 409; **`GET …/cloudinit-log` 404 = VM never phoned home (P0 by itself, RUNBOOKS §0.6)** | fetch cloud-init log FIRST (`?node=<cp-hostname>` per region; sentinel `CLOUDINIT-SENTINEL: cloud-init-complete`); never unwedge; PR then wipe + hw307 |
| 10–25 min | cilium CNI never installs (P1-02, fixed r4()/rt() retries) | cloud-init sentinel `cilium-bootstrap-FAILED exit 91`; nodes NotReady `cni plugin not initialized` | same as above |
| 15–40 min | Flux 0 HelmReleases (P1-03): #6508 OPEN anonymous ~470 MB clone rate-limited from a Huawei IP (hw302 stalled ~56 min then converged) | `hrTotal 0/0`; Kustomizations `Source artifact not found`; GitRepository clone errors | wait (self-heals when the rate window resets; #6338 20 m timeout on the GitRepository); if >60 min with 0 HRs, treat as the #6508 stall and keep waiting up to the 120-min budget |
| 20–60 min | Helm release Secret >1 MiB (P1-04) | `GET …/events` contains `Too long: must have at most 1048576 bytes`; one component failed | deterministic; PR to trim, wipe, hw307 — cannot recur unless an umbrella prose bump merged (R5) |
| 20–60 min | StorageClass / PVC binding (55b huawei-evs-csi 1.0.10 fresh-install hook: first live run of alpine/k8s `default-class-hook`) | `kubectl get pvc -A \| grep -v Bound`; `kubectl get sc` default = evs-ssd | chart fix + PR; never local-path |
| 20–60 min | first fresh-INSTALL of the 16 #6755 repinned hooks (cert-manager crd-gate, cnpg webhook-gate, kyverno webhook-gate, gitea gitdata-preservation, evs-csi reclaim) + mothership Harbor proxy dependency | `kubectl -n flux-system get hr` reasons; hook Job logs; `harbor.openova.io/api/v2.0/health` | if a hook image fails to pull through the proxy, that is a mothership-Harbor incident (#6482 class), not a chart bug |
| 30–70 min | keycloak realm import / region-B secret materialisation (P1-06, fixed #6177/#6021/#6342) | `describe pod keycloak-0` FailedMount; HR `dependency bp-keycloak is not ready` | regression of a merged fix → file with evidence |
| 40–90 min | region-B non-ready HRs (P1-10: the #6760 rollback class, first validation of `ca1546627`) | `kubectl --kubeconfig hw306-b.yaml -n flux-system get hr \| grep -v True` | expect zero rollback conditions; any non-ready HR other than the by-design suspended set is a passenger regression |
| 40–90 min | region-B ClusterMesh / Gateway-API listener parity (P1-11, fixed #4831/#6042/#6259/#6116/#6120) | mothership log `clustermesh … fullyMeshed=2`; region-B Gateway `status.listeners == spec.listeners`; hw305 showed a ~50 % TLS-reset flap until a cilium roll at 11:56Z | sample doors with fresh TCP (HTTP/1.1, `Connection: close`) 20× per host — HTTP/2 pins one backend |
| 25–105 min | chargeback slot 13f first-ever fresh install | HR `bp-chargeback` Ready (ns chargeback: CNPG Cluster, 2 CiliumNetworkPolicies, HTTPRoute); `dig @ns1.openova.io chargeback.hw306.omani.works` (proves the b27d54a DNS allowlist) | chart/seed fix as PR |
| at `ready` | premature ready / console-EIP reuse (#6191 OPEN, no fix) | record `result.consoleDegraded`; `curl --resolve console.hw306.omani.works:443:<consoleLoadBalancerIP>`; compare record IPs with hw305's released set (212.72.24.24/.85/.74/.1/.49/.25/.35, 5.37.79.59/.43) | if the console EIP is a just-released hw305 address and doors 000 → escalate #6191 with the ids; do not stamp UAT until doors serve |
| `ready` + 0–60 min | **#6485 cyclic re-bootstrap gate** (hw300: ready+200 ×3 then k3s reinstalled every 20–30 min; ~2 h backward clock step; OPEN, no root cause, no fix, did not recur on hw301–hw305) | `stability_watch` (lifecycle helper): every 5 min `kubectl get nodes` AGE only grows; `kubectl -n flux-system get kustomization` creationTimestamps unchanged; three doors 200 via fresh TCP | **no `reset-uat`, no protect-list promotion until 60 min stable**; if it recurs the only forensic is the per-node cloudinit-log push loop (main.tf:997-1005) + bastion-jumped journalctl (founder-gated) |
| at `ready` | handover + cutover must NOT auto-fire (`fireCutoverOnHandover:false`) | `kubectl -n catalyst get cm self-sovereign-cutover-status` absent or `cutoverStartedAt` empty; Settings → Sovereignty shows TETHERED + CTA (row 159) | if the CM shows a start, the flag was misread — record it; the cutover then runs on 0.1.201 and will FATAL at step 06 (M1) |

Post-ready reads to bank before the cutover severs GitHub: region-B HR list with reasons; region-B Gateway listener parity; seaweedfs volume count >2 and `openbao-snapshot-save` success (#6766 validation); shared-pg/-b/-c HR revision v1 (no #6582 churn); `kubectl get svc -A` zero NodePort both regions; phase2b Kyverno Enforce in region-B (#5592); `GET …/cloudinit-log` (+`?node=` per region) as baseline; the Phase-0/PUT/first-HR/ready/secondary-ready timestamps.

---

## 7. Post-ready WALK PLAN

### 7.1 Order of operations (RUNBOOKS §0.3)
1. `ready` accepted (§5 G7 + §6 ready checks: both regions `hrReady == hrTotal` or non-ready explained; doors 200; issuer Let's Encrypt prod; `dig` console + chargeback resolve) → 60-min #6485 stability watch → stage kubeconfigs `GET …/deployments/<id>/kubeconfig[?region=me-east-215-b-1]` → `~/.kube/sovereigns/hw306-{a,b}.yaml` (0600) → `scripts/check-live-nodeports.sh --kubeconfig <each>` (any hit = defect, never a fix) → `bash scripts/check-uat-walkability.sh` with `KUBECONFIG=hw306-a.yaml`.
2. Protect-list promotion PR (R2): RUNBOOKS.md:43 + PROTOCOL.md:242 → "as of 2026-09-02: **hw306**", PROTOCOL §1.2/§2.1a, TRACKER lane, with the FIRED/READY lines and this file's §9.
3. Ledger reset in ONE PR (M6): `python3 scripts/reset-uat.py hw306` (carry-forward: ~156 rows ✅→⏳, 94 ✅ stand with scheduler consent, evidence kept under the CARRY_PREFIX) → rewrite line 1 → Tally line ✅94 · ❌12 · ⚠️7 · ⏳173 → `python3 scripts/uat-drift-guard.py` (bare) + `test_uat_table_shape.py` + `test_uat_clause_identity.py` + `check-uat-fix-sha-citations.py` + `uat-partition.py --check` + `bash scripts/check-walk-respects-scheduler.sh origin/main` all exit 0 → push. Never `--flush`; never before `ready`; never `git stash pop` (stash@{0} holds stale hw302 ledger edits).
4. Work-list: `python3 scripts/uat-confidence.py --due --env hw306` (183 = 6 FAILING + 177 DUE at 08:5xZ) and `--schedule --env hw306 --limit 40` for ordering (top: 222 221 220 219 W1 G8 G9 G11 223 R17 60 218 …). Once #6781 is merged, record every cycle: `python3 scripts/uat-confidence.py --observe --env hw306 --cycle <ISO8601>` then `--snapshot`.
5. Pre-cutover window work (this fire keeps it deliberately): walk row 159 (TETHERED + CTA), 227's negative control (pre-pivot: a Blueprint CR DOES move on an upstream bump), all GitHub-delivery-dependent rows; merge M1/M9 (0.1.202, #6780) and watch Flux on hw306 reconcile the new pins; only then start the cutover from the CTA / `POST /api/v1/sovereign/cutover/start` (runner-SA internal trigger per RUNBOOKS §0.5 as fallback) and enter the RT-11 hold.

### 7.2 The 36 non-green rows, partitioned (ledger-partitioner + agentic-input-checker + cutover analyst; due-status measured 08:5xZ)

| Partition | Rows | Gate | Method | Procedure (evidence = screenshot/JSON under `docs/ledger/{screenshots,evidence}/hw306-row<N>-<slug>.*`) |
|---|---|---|---|---|
| A. FIX-MERGED-AWAITING-ENV, region-B/DR | **67, 235** (not due → M4/M5), G3 (not due) | none (walk right after Phase-1 convergence; G3 again after cutover) | 67: kubectl + browser; 235: 3 independent Playwright contexts (fresh handover token each); G3: kubectl only | 67: `kubectl -n grafana get pods` both regions 3/3 Running 0 restarts, no `cannot resolve write host`; grafana status/overview Healthy screenshot. 235: three fresh SSO logins → 200 each; sso-bridge tick log. G3: `kubectl get continuums.dr.openova.io -A` — every CR non-empty `hotStandbyRegions`, cnpgPair-backed ones `StandbyAvailable=True` (dr-shared-pg-b on hw305 was StandbyAbsent after a #6149 auto-promotion at 2026-08-23T23:36Z — env history, not a controller defect; walk early to bound exposure) |
| B. CUTOVER-GATED | **G11, 166** (FAILING), **227** (not due) | `cutoverComplete=true` after the 600 s step-08 hold | G11 kubectl/CM; 166 browser (/jobs cutover group all-11 green); 227 kubectl + git | G11: `kubectl -n catalyst get cm self-sovereign-cutover-status -o yaml` all 11 steps success, `cutoverComplete=true`, step-08 FATAL line captured if it fails (hw298's was never recorded); region-B `kubectl get helmrepository -A` = 0 on ghcr after two re-applies. 166: owner browser → /sovereign/jobs → cutover group 11 Succeeded (screenshot). 227: bump a Blueprint pin upstream → the Sovereign's Blueprint CR does NOT move (both regions) |
| C. INPUT-GATED (Anthropic) | **G8 G9 219 220 221 222** (FAILING/DUE), **223** (DUE; curl legs attempted anyway) | founder `credentialsJson` (M7 post-fire path) | browser + kubectl; 223 curl | 219 install half first (Application chepherd → HR Ready → pod exists); after the Secret lands: init container exits 0 (R19), dashboard worker online, `logs -c chepherd \| grep -i model` = claude-opus-4-7 (220), chat "create a wordpress app in my org" → `create_application` tool-call line + Application CR appears (G9/221) → HR Ready within the 900 s fuse + endpoint 200 (222); T+6 h re-probe proves the #6317 refresh loop. 223/212/213 in one curl session with the chart-rendered `agenity-mcp-bearer` of TWO Orgs: `list_applications` Org-scoped; cross-Org `get_application` → -32000 naming only the caller's Org; cross-Org `create_application` → -32003/403, nothing created; own-Org 200/201 controls |
| D. FIX-MERGED, funnel | **225** (DUE) | funnel Org whose cart contains openclaw (implies newapi: `helmrelease_apps.go:114`) and #6780 merged pre-cutover | browser checkout + kubectl | per-Org `bp-newapi` HR Ready; admin-promote CronJob image `postgresql:16` pulls; if the admin-SEED hook fails (hw302 shape, PATH-TO-100.md:15) capture the Job log before any retry |
| E. ENV-STATE / walk-only, funnel + per-Org | **89 91 94 3** (DUE), **232 234** (not due), 16 (not due) | two funnel Orgs on different pool TLDs (omani.homes, omani.rest); customer PIN sessions via the `OTP` IMAP folder | browser (own Chromium per walker) + curl | Org A cart: wordpress + openclaw + stalwart-mail (+ agenity) → 89 (Applications view card Running → Open), 232 (openclaw `/readyz` 200, issuer stamped — the hw305 ⚠️ was the Application-CR door with `spec.parameters={}`, not the funnel), 234 (`mail.<slug>.<pool>` 200 SnappyMail; clause names dead bundles → clause-only adjudication PR); Org B on the other TLD → 94 (zero-click console on a different TLD), 91 (marketplace root → own Org console, final URL chain). 3: mint a voucher in BSS → `https://marketplace.<fqdn>/redeem/?code=<CODE>` in the owner session → client-side bounce to /dashboard. 16: /app/<singleton> → Topology → change → Save persists (mutating on our own env is allowed) |
| F. ENV-STATE, sovereign-admin console topology | **60** (DUE), **52 56 62** (not due) | subject: `/app/shared-pg` exists by construction (authoritative `/api/v1/sovereign/apps`); 60 creates a second pair | browser + kubectl control | 60: Catalog → bp-postgres → "New instance" (UI literal) → active-hot-standby → Provision → Topology shows region-a ● PRIMARY / region-b ○ STANDBY + armed Switchover. 52/56/62 on /app/shared-pg: role asymmetry + replication arrow; numeric lag vs `kubectl -n shared-data get continuum dr-shared-pg -o jsonpath='{.status.replicationLagSeconds}'`; DR section values equal `leaseHolder/phase/standbyAvailable` across two reloads |
| G. ENV-STATE, misc walk-only | **133 159 189** (DUE), **109 8 W2 242 241** (not due; W2/8/109/189 already PASSED live on hw305 and were scheduler-held) | 159 needs the pre-cutover window; 242/241 need the mothership owner cookie (orchestrator, not a walker) | browser / curl / kubectl | 133: Edit-IaC `card.iconLight` → Save → hero re-renders (kubectl shows the new value). 159: Settings → Sovereignty panel TETHERED + "Achieve True Sovereignty" CTA (screenshot before clicking it in §7.1 step 5). 189: `cutover-secondary-kubeconfigs` key `me-east-215-b-1.yaml` server = private CP IP. 109: /settings without a second login + `curl -sI https://auth.<fqdn>/realms/sovereign/account/` 302 in one hop. 8: /organizations/new full form. W2: mothership wizard step 3 provider/region empty until chosen. 242: 1-region POST with no `bcpTopology` → 400 (validation-only; mothership; run only when no fire/wipe is in flight). 241: `GET /deployments/<id>` `consoleDegraded` vs live door, sampled mid-Phase-1 and at ready |
| H. PRODUCT-UNBUILT | **R21** (DUE) | optional chart-only delist of the 5 unpublished :1.0.0 scaffolds (bp-clickhouse/n8n/opensearch/prometheus/redis) from the catalog seed | curl `gh api` | stays ⚠️ by construction unless delisted (umbrella bump → after `ready`, before cutover) |

Ceiling arithmetic: 286 − 7 INPUT-gated (C) − 1 product-unbuilt (H, unless delisted) = **278 reachable**; DoD target ≥270 (§10).

### 7.3 Browser vs curl
Browser (own headless Chromium per walker, `shot.js` draft, storageState per walker, 8 h session TTL): G8 G9 219 220 221 222 235 16 89 W2 3 8 52 56 60 62 91 94 109 133 159 166 225 232 234. Curl/kubectl only: G3 G11 67 (control) 227 R21 223 212 213 241 242 189. Judge health by endpoint probe + `result.regions[].hrReady`, never the console glyph (memory `reference_console_app_status_glyph_lags_installed_hrs_probe_endpoints`); probe each host's OWN A record, not the console EIP (`reference_sso_gateway_own_eip_not_console_eip_probe_per_host_record`).

### 7.4 Auth for walkers
Preferred (no key extraction): mothership PIN session (`POST /sovereign/api/v1/auth/pin/issue`; PIN mail lands in the `OTP` IMAP folder — `EXAMINE "OTP"`, never INBOX; 60 s/email rate limit) → `POST /sovereign/api/v1/deployments/<id>/mint-handover-token` (RequireSession + ownership; 5-min RS256 token; returns `handoverURL`) → one handover URL per walker → `catalyst_session` persisted as Playwright storageState. Pre-check once with the owner cookie: `GET /sovereign/api/v1/handover/public-key` `md5(n)[:8]` equals the Secret `catalyst-system/catalyst-handover-jwt-public` on hw306-a (hw305 was `904c5bcd`; hw306 receives the current key at fire by construction). Fallback: mint from the mothership key inside the catalyst-api pod, shred after. The silent front door on hw305 today lands on /login from a fresh browser — do not assume it. Customer-Org sessions (89/91/94/234): PIN for the Org admin email, which must be readable in the mothership mailbox.

### 7.5 Stamp discipline (every row)
Three `<td>`s carrying four fields (📷 link → `hw306-row<N>-<slug>.png`, verdict, `hw306-YYYY-MM-DD` env-date, evidence text); the 📷 path is load-bearing (a kept hw305 thumbnail is refused as predecessor evidence); fix-SHA citations only as `MERGED on main as <sha> (#N)` on real fix commits (e.g. `ca1546627` (#6760), `7d25c2908` (#6749), `79d1d213b` (#6753)); never edit the `#` or Test-case cell; a ❌-set change regenerates WBS-TO-100.md §1; guards in ONE command before commit; path-scoped commit immediately (the harness linter reverts uncommitted ledger edits under concurrency); rebase on the `sso-uat-flip` bot before push. New rows to propose (chart-only additions to the ledger, RT-4 style): chargeback HR Ready + `chargeback.<fqdn>` 200 + PIN sign-in; #6709 CronJob schedule view; #6705 CronJob rows in /jobs; spine rows 104/198 +1 HR. Re-baseline (clause-only adjudication, not regression): row 2 "Cloud" → "Resources" (#6699), rows 198/203 `/cloud` routes, row 234 dead bundle names.

### 7.6 Collision avoidance
≤6 concurrent walkers; disjoint row lists from `--schedule`; each walker owns ONE branch `uat/hw306-<lane>` and one scratchpad dir `$W` (0600 state/cookie files, `shred -u` at end of shift); the shared Playwright MCP browser is forbidden (`feedback_parallel_playwright_agents_share_one_browser`); the sovereign-admin SPA holds an SSE stream — wait for `domcontentloaded` only (`reference_sovereign_admin_spa_wedges_playwright_backend_domcontentloaded`); walkers never write to any cluster (R17) and never POST except the auth calls above; row 242 and the mothership record reads (241) belong to the orchestrator (owner cookie).

---

## 8. PARALLEL LANES — next 3–4 hours

| Lane | Owner | Work | Depends on | Done when |
|---|---|---|---|---|
| **A — pre-fire / pre-cutover fixes + publishes** | fixer agents (worktree, ≤2 concurrent) + orchestrator merges | A1 (running) bp-self-sovereign-cutover **0.1.202** secondary durable pivot (M1) + 2-region payload overlay run; A2 #6780 newapi pin (M9); A3 bp-harbor 1.2.48 alias-redirect re-cut (from #6178); A4 optional R21 delist; **all merge after `ready`, before the cutover start**; A5 close the 17 stale/superseded PRs (§3.4); A6 file + fix M8 (`preflight_eip.go` held-EIP release) — catalyst-api, merge after `ready`, roll only outside a fire | ghcr publish verified per pin (`gh api packages`) | tags present on ghcr; `check-train-coherent.sh` COHERENT; hw306 `kubectl get hr -A` shows the new versions Ready |
| **B — lifecycle (orchestrator only, never delegated)** | orchestrator | B1 done: roll 07:26Z; B2 done: manifest v1 + LANE-B-EXTRACTED; B3 done: wipe 07:33→07:47Z; B4 done: zero-remnant 08:51Z + cooldown; **B5 regenerate the body (§5) + `jq -e`; B6 G5/G6 preflight exit 0; B7 G7 fire → 201; B8 §6 watch to `ready`; B9 60-min stability watch; B10 stage kubeconfigs + NodePort audit; B11 protect-list promotion PR + §9 READY line; B12 reset PR (M6); B13 pre-cutover merges (lane A) → Flux reconcile → cutover start → RT-11 hold → step watch (01 ≤15 min, 03 ~110 min ×2 legs, 05 severance, 06 region keys, 08 FATAL line)** | A1 for B13 only | `cutoverComplete=true` with the 600 s egress proof; §9 complete |
| **C — NO-FIX-YET rows that need no catalyst-api change (worktree-isolated; safe during Phase 1)** | fixer agents (≤2) | C1 #6781 merge + M4(b)(c) scripts/workflow PR (`check-walk-respects-scheduler.sh:302` to_pipe, due-ID parsing, `ALLOW_FULL_WALK` env wiring, `uat-tally.py:76`); C2 #6783 scoring semantics (wiped-env FAIL discount) then land the hw305 CSV (M5); C3 clause-only adjudications: row 2 nav literal, rows 198/203, row 234 bundles; C4 PATH-TO-100.md hw306 partition (current top partition is hw302-era and cites none of the delta issues); C5 `prov-preflight.sh` 3g vacuity fix (accept a `WIPED_AT` env); C6 docs: RUNBOOKS §0.2 publicIp hard quota = 10 and the 6-EIP 2-region footprint; C7 issue for the mothership-side Anthropic seed gap (boot-time env snapshot, no renewal loop) | none | PRs merged; guards green on main |
| **D — walkers (after `ready` + stability watch + reset PR)** | ≤6 read-only walker agents, own browser each | D1 curl/kubectl rows first: 189 G3 67 (control) 223/212/213 241; D2 sovereign-admin browser rows: 159 (before the cutover start) 60 52 56 62 16 133 109 8 W2 2 198 203 + the 177 DUE rows in `--schedule` order; D3 funnel Org A + Org B → 89 91 94 3 232 234 225; D4 235 (3 contexts); D5 after the credential (M7): 219 220 221 222 G8 G9 223; D6 after `cutoverComplete=true`: G11 166 227 | lane B `ready`; M4 for the 17 not-due rows | every stamp guard-green; observations recorded per cycle |
| **E — 6-hourly convergence report** | session cron `13 */6 * * *` (job `c70240c3`) | tally (post-M4c) + `--observe`/`--snapshot` per cycle + delivered-vs-reclassified separation + founder decisions pending (credential; #6783 scoring) | — | report posted every 6 h until 100 % or the ceiling is named |

---

## 9. Evidence appendix (appended at execution — commands, HTTP codes, timestamps)

### 9.1 Mothership roll (RT-6: convergence to the declared pin, no source change)
- 07:26:04Z `kubectl -n catalyst set image deploy/catalyst-api catalyst-api=ghcr.io/openova-io/openova/catalyst-api:b27d54a`; 07:26:56Z same for `deploy/catalyst-ui …:b27d54a`; `rollout status` settled; Flux `catalyst-platform` Kustomization left `suspend=true`; owner `whoami` 200; `GET /deployments` listed hw305 `ready`; zero in-flight operations at roll time. Effect: `prov-preflight.sh` check 4b PASSES (deployed == pinned b27d54a); the `chargeback` parent-zone A-record allowlist (`sovereign_dns_records.go`, `92faa4ea5`) is now in the fire path. Fire not before 07:32Z (5-min settle) — satisfied.

### 9.2 Wipe (R1/RT-7)
- `LANE-B-EXTRACTED: 05f983d8b` (last origin/main commit touching `docs/ledger/UAT.md` at the time; manifest v1 merged as `63c8a5c0f` PR #6779 with `hw305-forensics/` 10 files).
- 07:33:24Z `POST https://console.openova.io/sovereign/api/v1/deployments/b2b00ce4c833badf/wipe -d '{}'` → **HTTP 202** `{status:wiping}`.
- 07:47:13Z record `status=wiped`; providerPurge removed 2 ELBs, 2 VPCs, 2 subnets, 5 floating IPs (incl. the 3 orphan `catalyst-b2b00ce4-nat-preflight-{1,2,3}` EIPs). `WIPED hw305 b2b00ce4c833badf 2026-09-02T07:47:13Z` (tofuDestroyed/verifiedZeroOrphans flags: NOT re-read from `lastWipeReport` — the record is reaped 60 s after `wiped`; the cloud enumeration below is the authoritative proof).

### 9.3 Zero-remnant + cooldown (R1, RUNBOOKS §0.2)
- 08:51Z signed Huawei GETs (project `f27698137bdc4b00ad509cf27f1e5547`): quotas **vpc 1/5, publicIp 1/10, subnet 3/100, loadbalancer 0/50**; ECS 1 (bastion-openova), VPC 1 (bastion-openova-vpc), EIP 1 (212.72.24.20), NAT 0, ELB 0, EVS 2 (bastion volumes) — equal to `wipe-targets-and-postwipe-baseline.json` `expected_counts_after_full_wipe`. Pre-existing non-hw305 leftovers (2 vpc-less subnets/networks kv030704/kv184704, 1 SG, 2 keypairs, 48 archive OBS buckets) unchanged and excluded by design. Bastion untouched.
- Cooldown: 07:47:13Z → 08:51Z = 64 min ≥ 15 min (EIP free-pool) and ≥ 20 min (VPC/NAT teardown lag) — elapsed by wall-clock; preflight 3g will print `999min` (vacuous after the record reap) and is superseded by this line.

### 9.4 Pending (to be appended verbatim by lane B)
- `PRE-FLIGHT PASS vpc=1/5 evs=2/400 eip=bastion-only obs=48/100 — <HH:MM>Z (cooldown wall-clock: wiped 07:47:13Z)` + `exit=0`
- `FIRED <ts>: POST /sovereign/api/v1/deployments -> 201 dep <id>` (body sha256 of the §5 JSON with the real key, `fireCutoverOnHandover:false`)
- Phase-0 → PUT → first HR → `READY <ts> a=<hrReady/hrTotal> b=<hrReady/hrTotal> degraded=<bool>`; record `consoleLoadBalancerIP`/`loadBalancerIP`/`controlPlaneIP` (compared with the released hw305 set); `dig` console + chargeback; issuer; 60-min stability watch result; kubeconfig staging paths; `check-live-nodeports.sh` both regions = 0
- Protect-list promotion PR number; reset PR number (carried N); first `--observe` cycle id
- Pre-cutover merges (0.1.202 / 1.4.1624, #6780, bp-harbor 1.2.48) with ghcr timestamps and the hw306 HR versions after reconcile; `CUTOVER START <ts> source=<cta|api>`; per-step `success` lines; step-08 hold result (first FATAL line if any); `cutoverComplete=true <ts>`; region-B HelmRepository census after two re-applies
- Anthropic: founder re-issue timestamp; hw306 Secret creation; agenity init exit 0; T+6 h refresh probe
- Walk PRs by lane with the tally after each

---

## 10. DoD — what "the best environment so far" means, in numbers

| Criterion | Target | Measured by | Notes / disagreements |
|---|---|---|---|
| Phase-1 | `status=ready` ≤105 min (hw305) and ideally ≤60 min; primary `hrReady == hrTotal`; secondary `degraded:false` within 60 min of primary ready | deployment record `regions[]` | readers disagree whether by-design-suspended secondary HRs count toward `hrReady` (historian: coerced Ready, steady ~58-59/66; partitioner: 66/66). Safer reading: judge region-B by `kubectl get hr -A` reasons — **zero HRs Ready=False other than the provider/secondary-suspended set** — not by the badge |
| Stability | 60 min post-ready with node AGE only growing, Kustomization creationTimestamps unchanged, doors 200 via fresh TCP (#6485 gate) | `stability_watch` | prerequisite for reset-uat and the protect-list promotion |
| Doors + DNS | console/api/marketplace/chargeback.hw306.omani.works 200; issuer Let's Encrypt prod (not STAGING); console served on the record's `consoleLoadBalancerIP` (#6191 probe) | curl/openssl/dig | — |
| Region-B delivery | `ca1546627` (#6760), `79d1d213b` (#6753), `35ca70338` (#6766) each proven live: no rollback conditions; grafana 3/3 both regions; seaweedfs volumes >2 | kubectl | first live validation of all three |
| Sovereignty | `cutoverComplete=true` with the step-08 600 s deny-egress proof (both regions green through the hold); region-B HelmRepositories 0 on ghcr after two re-applies; the step-08 FATAL line captured if it fails | cutover-status CM; `kubectl get helmrepository -A` on region-B | requires 0.1.202 (M1) delivered pre-cutover; hw305 never reached step 07 |
| Ledger | ≥ **270** green of 286 on hw306 stamps (hw305 peak 270 on 2026-08-24; main 250 today; reachable ceiling 278 = 286 − 7 INPUT-gated − R21); 0 rows lost green without a named regression; every stamp env=hw306 + observation recorded | `uat-tally.py` (after M4c) + `uat-observations.csv` | the 17 not-due non-green rows need M4/M5 to be flippable in CI |
| Agentic | G8 G9 219 220 221 222 223 green **if** the founder re-issues the credential (post-fire per-Sovereign path) and the T+6 h refresh probe holds; else recorded INPUT-gated naming Secret `catalyst-system/sovereign-anthropic-credentials` keys `apiKey`/`credentialsJson` | agenity init exit 0 + MCP tool call + Application CR | ceiling 279/286 without it |
| Hygiene | 0 NodePorts live (both regions); 0 hw305 remnants (done); ≤3 leaked nat-preflight EIPs recorded with ids for M8; no live patch without a merged PR; hold list untouched during the freeze | `check-live-nodeports.sh`, inventory diff, PR list | — |
| Process | 1 fire (≤2); zero `AskUserQuestion`; every agent claim re-verified; every stamp guard-green before push; 6-hourly report on schedule | this file | — |

---

## 11. Risks and the decision each forces

| Risk | Likelihood / basis | Decision it forces |
|---|---|---|
| 0.1.202 is not landable before the walk window closes | fixer running; the chart is at 85.6 % (87.2 % 2-region) of the payload ceiling; 5 lockstep sites | fire anyway with `fireCutoverOnHandover:false` (decided); start the cutover ONLY after 0.1.202 is Ready on hw306; if it slips past today, hw306 still delivers every non-cutover passenger and the cutover-gated rows (G11 166 227) stay ❌ with the fix named — never start the cutover on 0.1.201 (it would loop at step 06 and #6202 makes it unrecoverable) |
| A kit-pin/umbrella bump merges during Phase 1 (live helm upgrade mid-bootstrap, #6760/#6582 class) or an umbrella prose bump crosses the 90 % payload gate | deploy-bot cadence ~6 kit commits/day; hold list has 15 PRs | merge freeze (§3.5) declared and enforced by the orchestrator; any bump → re-run payload + coherence gates; if a bump lands during Phase 1 and wedges an HR, that is a passenger regression to file, not a hand-fix |
| Phase-1 fails (kubeconfig never PUT / CNI / 0 HRs / payload) | all known roots fixed; #6508 anonymous-clone stall can cost ~1 h | cloud-init log FIRST (§6), never unwedge; fix as PR; second fire = hw307 (last fire today) only if the fix is delivered |
| #6485 cyclic re-bootstrap recurs | OPEN, no root cause, not seen on hw301–hw305 | 60-min stability gate before any reset/promotion; if it recurs: capture the per-node cloudinit-log loop, escalate for the bastion-jump forensic (founder-gated), and treat the env as failed |
| Console EIP reuse (#6191, no fix) | mitigated only by the cooldown (64 min elapsed) | post-ready `curl --resolve` on the record's IP; if doors are 000 on a released hw305 address, escalate with ids; do not stamp |
| Step-03 secondary Harbor prewarm (#6764) or step-01 shared-PAT leg (#6754) fail on their first live run | never live-validated | capture the region-B Job logs within the ttl (3600 s); the engine recreates step Jobs per attempt so a chart fix delivered pre-step-05 re-asserts automatically; past step-05 the fix rides the next fire |
| Step-08 hold fails (last measured FAIL at 138 s on hw298, cause unrecorded) | UNKNOWN | require both regions 100 % Ready before the hold; record the first FATAL line into the ledger; G11 stays ❌ with the line cited |
| Docker Hub throttling during step-03 (hw305 lost ~90 min) | proxy-dockerhub via mothership Harbor; health green now | budget ~110 min for step-03; re-probe Harbor health before the cutover start |
| Scheduler cannot mark the 17 non-due non-green rows due; CI refuses their ✅ flips | measured (`--due --env hw306`; `--observe` broken; #6783 held) | land #6781 + M4(b); decide #6783 by implementing the wiped-env FAIL discount the canon already promises; until then flip those rows only with `ALLOW_FULL_WALK=1` justified per row |
| Anthropic credential not re-issued today | founder input; refresh spent; mothership seed path reads a boot-time env snapshot | walk everything else; report 7 rows INPUT-gated with the Secret/keys named; when it lands use the per-Sovereign path (no mothership roll); file the mothership-side seed gap (C7) |
| nat-preflight EIP leak recurs at the hw306 apply (blocklist holds all 9 hw305 addresses for 24 h) | code-inferred, log NOT confirmed | non-fatal; record ids; fix M8 after `ready`; publicIp may read 10/10 for hw306's life — no second env can be fired anyway (R1) |
| LE rate limit on `omani.works` | by date both windows clear; count NOT MEASURED | if LE 429s at handover cert wait, the canon is a TLD flip on the NEXT fire, never staging |
| Walkers collide (shared browser / same rows / stale PRs / sso-uat-flip bot) | 12 stale ledger PRs open; bot pushes to main | close the stale set before the reset PR; disjoint row lists; own browser; rebase before push |
