<!-- Produced 2026-08-02 by workflow wf_fd06ff0e-603: 5-reader gap census (211 items) -> 3 independent plan lenses -> 3 adversarial verifiers (scores 7.5/8.5/9; all canon violations + reality errors fed into synthesis) -> synthesis. Companion: carry-forward completion matrix (142/249 = 57.0%, durable ~88) booked in the completion-matrix memory baseline 2026-08-02. -->

# PATH TO 100 — master plan

## 1. Thesis

Every green cell between the current standing (carry-forward 142/249 = 57.0%, durable Overall ~88, UAT.md reset to 286 data rows / 11 green at 429a39f76) and 100% flows through exactly ONE choke point — the founder-fired hw292 fresh prov — so the plan is a diamond-shaped DAG. Two fully parallel pre-fire lanes: (A) an agent-only train bake that merges every CI-provable PR, verifies (not re-ships) the already-aboard durable fixes, and freezes a pin-proven train manifest; and (B) a SINGLE batched founder escalation carrying all nine founder/operator gates, critical-path items first. Then a strictly sequential spine — fire → converge → auto-cutover → cc=true under total merge freeze — followed by ONE maximal evidence walk ordered non-destructive-first with the gitea singleton-roll re-proof at the END of the non-destructive lane and the G12 region-kill dead LAST. Post-cc=true, the held queue thaws, the #5301 Day-2 Harbor-pin reconciler gets its live delivery proof (the cycle-cost killer: if Day-2 roll works, most survivor fixes never need another fresh prov), and founder-dependent tails (#4277 credential rows, denominator verdict, #5573 mothership revival) close the arithmetic. Critically, this plan does NOT re-ship delivered work: #5525's and #5527's fixes are ALREADY merged (PR #5528 @3a860b801, PR #5529 @3d6b321c1) and ancestry-proven aboard image pin fb41faf30 — Phase A verifies them aboard and ships only genuine residuals. The engineering-only ceiling is ~93%; the last ~7% is arithmetically founder-gated and is stated as such, never faked green.

## 2. The ONE consolidated founder-ask block

One `chepherd.alert_human` escalation, urgency=high, all nine asks in one message, critical-path first. No second escalation; follow-ups ride the same thread.

1. **[CRITICAL PATH #1 — blocks the fire itself]** Scale the mothership catalyst namespace back up (#5558): `kubectl -n catalyst scale deploy/catalyst-api deploy/catalyst-ui deploy/openova-flow-server --replicas=1`. The fire endpoint `POST /sovereign/api/v1/deployments` is verified 503. If the 0/0 was deliberate maintenance, say so and we hold.
2. **[CRITICAL PATH #2]** Execute the staged hw292 fire keystroke (`POST /sovereign/api/v1/deployments`, hw292.omani.works, 2-region me-east-215-a/b) — ONLY after our "train baked + fresh PRE-FLIGHT PASS logged" signal on this thread. Wipe-before-fire and reset-uat (429a39f76) are already banked.
3. **Revive mothership Flux (#5573)**: scale the 4 controllers 0→1 and resume the flux-system Kustomization. On revival, the powerdns OCIRepository (floating semver `1.x` — no pin commit needed or possible) auto-pulls the latest chart carrying the merged #5570/#5565 §854 fixes and releases the last OpenOva-owned NodePorts; #5551 (janitor honesty) delivers on the next catalyst-api roll. Zero further action from you.
4. **Anthropic credential (#4277/#4111)** — the one genuinely external dependency: `kubectl -n catalyst-system create secret generic sovereign-anthropic-credentials --from-literal=apiKey='sk-ant-…' --from-literal=credentialsJson='{"claudeAiOauth":{…}}'` (full OAuth blob; bare sk-ant is rejected). Zero engineering clears rows 219/220/222/G8/G9 without it.
5. **Plan-s quota ruling (#5393**, 4 questions in the issue**) + #5391 gating semantics** (exclude customer HRs from cutover readiness vs accept coupling). Our delegation default: exempt overhead + derive plan floors; we implement whichever ruling lands.
6. **Your PR #5386** (§854 tamper-evidence) now CONFLICTS with its merged successors (#5518/#5544/#5548/#5565/#5570). Rebase yourself, authorize us, or close as superseded — residual-delta comment is on the PR; one word decides.
7. **Denominator verdict** on the ~19-20 not-achievable-as-written ⛔ rows (98-109 per your #4325 reclassification; R1/M1/G5 janitor; R19 sandbox-removed; R20; 94/95) plus Funnel ⛔ 85/88/91/93/G7/217/221/223 and Robustness R11: per row-group, rewrite-to-shipped-design or formal exclusion RECORDED in UAT.md. Without this, 100% is arithmetically unreachable.
8. **Mothership §854 tail (#5348)**: merge iogrid#844; fix or delegate cinova catalog-svc → ClusterIP (never-touch for us); approve gatewayHTTPRoute HTTP01 solvers — or formally scope §854 to platform-owned namespaces and we record the exclusion.
9. **Pick ONE of #5329 vs #5356** (duplicate break-glass wipe PRs; recommend #5329, hw284-proven). We merge the pick, close the other.

## 3. Phased plan

### Phase A — Pre-fire train bake (agent-parallel; the ONLY legal merge window until cc=true)

**Goal**: a fully-baked, pin-proven train so nothing needs to merge between the fire keystroke and cutoverComplete=true.

Steps:

1. **Verify-aboard sweep (do NOT re-ship delivered work).** Confirm by `git merge-base --is-ancestor <sha> fb41faf30` that the delivered fixes are ancestors of the image pin: #5528 (=#5525 scarf.sh fallback + toomanyrequests latch + FATAL on durable_miss only, chart 0.1.158) @3a860b801; #5529 (=#5527 org-tenant HelmRepository cutover-aware, organization_gitops.go:369-382) @3d6b321c1; #5506 (=#5505 Kyverno APICall RBAC) @f27302982; #5446 (=#5445 pgdata subPath) @68ee149c5; #5424 (=#5423 cart shape-validation) @fe6f1a288; #5438 (=#5436) @32ee47a8d; #5441 (=#5437) @d202fcc1a; #5411 (=#5389 launch-URL leg) @c7c00d771; #5366+#5405 (=#5364 partial); plus the prior train set #5519/#5526/#5490/#5524/#5478/a854cb7ae+6844b38f6/#5533(bp-gitea 1.2.49)/#5522/#5507 and cnpg-pair 0.2.23. **Residual check on #5525**: diff the hw291 live-patch against chart 0.1.158 — if the copy_fail-vs-durable_miss fatal-gate residual is not fully covered by #5528, ship ONLY that delta. Evidence: ancestry output pasted into the train manifest.
2. **Merge the mergeable wave** in dependency-safe order, serializing chart-version bumps: #5553 FIRST (vitest gate fail-closed — confirm via its red-run evidence the gate CAN fail), then #5536, #5538 (row 60 needs it with #5519), #5539, #5540 (row 9 honesty), #5541, #5552 + #5556 (A16 CI guards), #5554, docs-only #5549/#5550. Exit per PR: squash-merged, main CI green.
3. **Provenance-gated merge of #5543**: its cited runner `fast_executor.py` exists nowhere in git history (confirmed). Independently re-run the handler tests (`go test ./...` in the touched module); if absent, write them in the PR first. No merge on the PR's own evidence.
4. **Rebase the CONFLICTING pair**: #5537 (SIGPIPE sweep, 247 sites/34 scripts — rebase over merged #5544/#5548/#5565/#5570, re-run the sweep; bf59fb5f0 is the idiom template) and #5546 (janitor walk — re-verify the M1/G5 stamps still hold given mothership went 0/0 AFTER that walk). Rebase+merge #4074 (ListParentDomains fallback, verified absent from main) and #3725 (cloud-init 32256B CI gate).
5. **Ship the genuinely-open walk-unblockers** (each: merge → ghcr publish → 5-site lockstep pin incl `clusters/_template/bootstrap-kit/` → `scripts/check-bootstrap-kit-pin-sync.sh` PASS → bootstrap-kit go test PASS): #5433/#5419 (bp-postgres fullname collision + AHS affinity), #5467 (PAT redaction line 581), #5426 (org-delete cascade remainder beyond #5366/#5405), #5385 (health-aggregate freshness — restores badge-as-evidence), #5480 A16 class sweep across the 13 region-blind secret generators (guards #5552/#5556 now merged; unlocks #5406/#5414/#5416/#5499/#5394/#5358/#5466 classes), row-238 requests==limits in the per-Org CNPG chart, row-55 data-layer region emit, #5504, #5563, #5559, #5520 regenerate + drift guard, #5575/#5408 (wizard — git-side merge now; mothership-side delivery stays gated on #5573/#5558 and is recorded as frozen, not failed). Cap the list here — the fire date wins over train completeness; anything else defers to post-cc.
6. **PR-queue hygiene**: close-as-superseded with pointer comments (all verified PRs, not issues): #5304, #4135, #4109, #3812, #3787, #3762, #3746, #3732, #3719, #3350; adjudicate #3920/#3918/#3917/#3915/#3745 against main, salvaging residual hunks into at most one fresh small PR. Batch-merge the **38** CI-green dependabot PRs oldest-security-first (x/net, x/crypto, grpc, glog, protobuf); hand-rebase #4701.
7. **Commit the RT-1 manifest** `docs/sessions/<fire-date>/train-hw292.md`: derive from (don't duplicate) the ancestry table already in PATH-TO-100.md @53bdf8052; list every rider with merge SHA + ancestry proof against the FINAL image-pin tip this phase produces. Note precisely: the prior train is 10 PR/commit entries + the cnpg-pair 0.2.23 chart; #5485 rides as two direct commits (a854cb7ae, 6844b38f6), not a PR.

**Exit criteria**: main green; every listed PR merged or explicitly deferred-to-post-cc with a comment; verify-aboard table complete; pin-sync + go-test guards PASS; ghcr tags confirmed for every bumped chart (hollow-pin check); train-hw292.md committed; real-PR count reduced to held (#5534/#5535) + founder-gated (#5386, #5329/#5356).
**Rows banked**: 0 directly — determines what the ~111 env-gated rows can yield and blocks re-import of the hw291 failure classes.
**Failure branch**: red main → fix-forward or revert same session, never fire on red; chart-version collision → serialize and rebump; if the A16 sweep or any high-blast item slips, it moves to the Phase E train — the fire is NOT held for it (only a confirmed #5525 residual gap would hold the fire).

### Phase B — Single batched founder escalation (fires in parallel with Phase A)

**Goal**: put the founder on the critical path exactly once.

Steps:

1. Send the §2 block via `chepherd.alert_human`, urgency=high, as ONE message — before escalating, post the #5386 residual-delta comment on the PR so the founder's decision is one word.
2. While waiting: continue Phase A; verify readiness mechanically, never by claim: `curl -s -o /dev/null -w '%{http_code}' https://console.openova.io/sovereign/` flips 503→200 = catalyst up; `flux get kustomizations` via mothership kubeconfig READ-ONLY = Flux revived. Zero kubectl writes against contabo-mkt ever.

**Exit criteria**: sovereign endpoint returns 200/302 (curl-verified); fire authorization received in the pane; remaining asks acknowledged or explicitly deferred — all on one thread.
**Rows banked**: 0 directly; unlocks ~111 env-gated rows + the 5 credential rows + ~19-20 denominator rows.
**Failure branch**: founder answers only the critical-path subset → proceed to Phase C anyway; the off-path rows stay honestly red with their gate named in PATH-TO-100.md. Endpoint still 503 after scale-up → read catalyst-api pod logs via mothership kubeconfig (read-only), reply new evidence on the same thread.

### Phase C — Freeze, fire, converge, cutover (strictly sequential; total merge freeze keystroke→cc=true)

**Goal**: hw292 at cutoverComplete=true, 11/11 steps, 600s deny-egress hold green in BOTH regions.

Steps:

1. **Fire-day gates**: (a) confirm no other env exists via the **deployment registry enumeration** (and Huawei API listing) — curl 000 on hw291/hw292 is corroboration, not the instrument (L4 lesson); (b) re-run `scripts/prov-preflight.sh` and commit the fresh `PRE-FLIGHT PASS vpc=X/5 evs=Y/400 eip=Z` line in `docs/sessions/<fire-date>/` — the 07-31 16/16 is stale; (c) confirm reset-uat is NOT re-run (429a39f76 banked; NOTE: `scripts/reset-uat.py` has no --help guard and mutates UAT.md on ANY argument — no exploratory invocations); (d) declare MERGE FREEZE in the session log: no merges, no deploy-bot rolls, no chart publishes until cc=true.
2. Signal "train baked, preflight PASS, fire when ready" on the Phase B thread. Founder fires. TLD rotation already applied in the staged body; if a re-fire ever accumulates LE usage on omani.works, rotate to omantel.biz BEFORE the re-POST.
3. **Converge (~45min)**. Failure branch: RT-9 debug-before-wipe — fetch `GET /api/v1/deployments/{id}/cloudinit-log` FIRST, pattern-match kom4dc 7-fault + cilium bootstrap-DNS classes, only then canonical wipe (`POST .../{id}/wipe` — autonomous, non-bastion) → fix → re-bake → `reset-uat.py hw293` → re-fire as hw293.
4. **Auto-cutover, 11-step chain** via the self-sovereign-cutover-status ConfigMap. Step-03: slow-but-moving (~50min DIRECT fallback) is healthy; FLAT count = stall — #5528's fatal-gate should now fail loud. Step-06: never merge anything (catalog-vs-pinned drift class); watch #5214 secret-vanish shape. Step-07: CSI EVS wedge class (#5215). Step-08: RWO singleton desync → re-fire the step canonically, never hand-heal, never delete step Jobs (stub-Job recovery only).
5. **Cutover-abort branch** (catalyst-api restart, #5488 — its fix is deliberately HELD in #5535): re-POST via the runner-SA `/internal/cutover/trigger`. If a customer HR blocks the settled-roll pre-flight (#5391 undecided), use the hw291-proven prune workaround and file the evidence on #5391 — never invent gating semantics.
6. On cc=true: commit the timestamp + 11/11 step evidence + both regions' 600s deny-egress hold logs to `docs/sessions/<fire-date>/evidence/` before anything else touches the env.

**Exit criteria**: cutoverComplete=true, 11/11, hold green both regions, evidence committed; merge-freeze log shows zero merges keystroke→cc=true.
**Rows banked**: ~2-4 (G11/166 cutover restamp class); the value is unlocking Phase D.
**Failure branch**: named per-step above; #5465 OOMKilled-at-4Gi mid-cutover → heap-profile during the prov, never another limit bump; budget one re-fire in the cycle.

### Phase D — hw292 evidence walk (non-destructive first; gitea roll LAST-of-non-destructive; G12 dead LAST)

**Goal**: convert the merged-undelivered backlog + ~100 unrooted env-gated rows into UAT.md stamps (env+date+evidence). The ONLY phase that legitimately moves the durable number (88→90-91+).

Steps (in order):

1. **Delivery-verification closes** (live re-check + UAT stamp + `/close` skill, evidence dated post-deploy): #5515+#5538→row 60; #5489 (#5526/#5490/#5524)→row 9 + four vCluster surfaces; #5485 d1-3; #5477→four Continuum CRs show MEASURED lag, never fabricated 0; #5522→rows 212/213 reclassified out of ⛔ on evidence; #5524→per-Org door 200 from BOTH regions via **fresh-TCP curl** (never a browser loop — HTTP/2 pins one backend) + a late-Org create proving listeners written in both regions (#5395/#5511); #5529→post-cutover org MUTATION, then `kubectl get helmrepository -A | grep -v ghcr` proves the generator stays on local Harbor (Pillar 5 durability; closes #5527 on runtime evidence); #5528→prewarm behavior stamp (closes #5525); #5506→Org/env create succeeds under Kyverno (closes #5505); #5446/#5424/#5438/#5441/#5411 runtime stamps; status/uat set #5453/#5451/#5482/#5475/#5400/#5394/#5341.
2. **Funnel e2e on canon domains ONLY** (`https://marketplace.hw292.omani.works/redeem/?code=…`, Orgs on `*.omani.homes` — NEVER openova.io): voucher → org-create (#5540 honesty) → app install incl. one REJECTED install proving true status (#5541) → app RUNS (#5446 subPath) → per-Org door. Use a vcluster-tier Org to clear Bucket-2 rows (89/90/232-234) around #5393; the #5424 shape-validation fix is aboard — walk it deliberately with a HelmRelease-shaped cart item to VERIFY quarantine works, but only AFTER the happy-path funnel rows are stamped (if quarantine fails, one poisoned Kustomization must not cost the earlier stamps). Org-delete full teardown (R17 + 224/232/233/234 post-#5366/#5405/#5426).
3. **SSO walks with wire-capture per row** ("landed logged-in" is not evidence): rows 31/32/33/34/35/37/R3/R4/R5/R9 — Harbor #5406, guacamole #5416/#5358/#5509, newapi #5466/#5414, OpenBao #5459, silent re-establish row 29 (#5460 config-cli chain). Any residual failure: check the A16 CONSUMED secret copy in BOTH regions FIRST; root only with a CONTROL sharing the suspect property.
4. **Unrooted residue sweeps** (Console 36, DR non-destructive 17, Robustness 14, R18, G4): every "missing UI" verdict must cite all THREE console trees (`core/console/`, `products/catalyst/console/`, `products/catalyst/bootstrap/ui/src/pages/sovereign/`) with paths recorded; trust kubectl over the health badge until #5385's stamp lands; rows 240/241/242 adjudicated via `scripts/check-no-nodeports.sh` + `scripts/check-live-nodeports.sh` against the live cluster (raw grep is NOT the audit). **Row 22**: record as a ledger-adjudicated rewrite per the #5493 test-encoded contract with the adjudication note IN the row (row-185 precedent) — not a silent green restamp. Row 238: `kubectl get pod -o jsonpath='{.status.qosClass}'` = Guaranteed. Wizard: W4/W5 ALREADY walked 2026-08-01/02 (da3e25112 — W4 ✅, W5 ⚠️→#5575; rows 47/48/49 walked @b7d130a24) — do not re-plan as unwalked; walk W1/W2 only, and mark any mothership-side wizard row frozen-behind-#5573, not failed. Consolidate ALL survivors into ONE checklist issue per family (L8) — ≤6 new issues total.
5. **END of non-destructive lane — gitea singleton-roll SSO re-proof (#5531/bp-gitea 1.2.49)**: sequenced HERE, after every other non-destructive stamp is committed, because gitea is a stateful RWO-EVS singleton with a documented NodeStage-desync wedge class (#5091). Prefer evidence from a naturally-occurring roll if one happened during the walk; otherwise perform the single deliberate roll with the recovery pre-named: on desync, re-fire canonically, never hand-heal. Then complete a fresh OAuth round-trip with wire capture (R8/row 31).
6. **DEAD LAST — G12 region-kill + failback** (cnpg-pair 0.2.23 aboard, `scripts/region-kill-drill.sh`): write-marker before kill, read-marker after failback, leg 6/6 no split-brain, RPO/RTO evidence; flip #4901 standby-absent and #5477 lag conditions during the outage; #5514/#5513 phantom-standby honesty. Failure branch: leg-6 split-brain recurs → file evidence on #5388, park G12 as a named survivor, NEVER wipe hw292 to retry.
7. **Cycle bookkeeping**: PROTOCOL §5.0 protect-list single-live-Sovereign row → hw292 (+ RUNBOOKS twin); refresh PATH-TO-100.md off the walk; every restamp updates all THREE cells (env-column guard before push — hit twice on hw291); tallies count ALL row-ID formats (R#/G#). Every sub-agent "done" re-queried live before any stamp — agent reports are CLAIMS.

**Exit criteria**: every walked row carries an hw292-dated stamp with evidence link; delivery-verification issues closed via `/close`; survivors in ≤6 family checklist issues; G12 stamped or parked-with-evidence; durable Overall moves 88→90-91+ on completion-matrix rules (walk evidence only).
**Rows banked**: ~90-110 of the ~111 env-gated rows (Console ~36 + Funnel ~24 + DR ~17 + Robustness ~14 + SSO ~6 + singles), minus survivors.
**Failure branch**: per-family survivors graduate to Phase E; no mid-walk wipe under any circumstance (a new env flushes ALL evidence).

### Phase E — Post-cc=true thaw: held PRs, Day-2 proof, survivors, founder tails, optional hw293

**Goal**: merge everything the freeze held, prove Day-2 delivery, fix survivors, close to 100% — with at most ONE more wipe→fire cycle.

Steps:

1. The moment cc=true + step-08 hold complete (never earlier): merge held #5534 (#5485 d4-6 — then walk them on hw292: real suspended HR badge, ?status= filter, fleet dedup row 205) and #5535 (#5488 hardening — protects the next cycle); merge Phase A deferrals.
2. **Day-2 delivery proof (#5301 mechanism, cycle-cost killer)**: merge one post-fire chart pin bump, watch the `12-daytwo-harbor-pin-reconciler` pull it into hw292's LOCAL Harbor and roll the HR, then walk row 205 + jobs filter live. Proof passes → the roll path becomes the delivery vehicle for every remaining fix (#5265 stays OPEN until its full class is closed — never close on the mechanism alone). Proof fails → filed root-cause on #5265, and it heads the hw293 train.
3. **Survivor fixes with fixes-in-hand**, each through the full gate then Day-2-rolled (preferred) or next-cycle: #5389 remaining legs (rows 110/112/114/115), #5426 remainder, A16 stragglers, #5542 200-over-error sweep, #5476 ownerRefs backfill, #5465 heap-profile root-cause, #5471 gateway status taught the DIRECT hostPort path (any nodePort-shaped fix is the wrong fix by definition), #5461.
4. **Founder tails as gates clear** (no re-escalation — Phase B thread): #4277 secret lands → ExternalSecret READY=True, Org-create auto-seed, chat→provision `create_application` proof, walk 219/220/222/G8/G9, close #4277/#4111; denominator verdict lands → restate/exclude the ⛔ rows IN UAT.md with the ruling quoted; #5393/#5391 rulings → implement + rewalk plan-s rows; Flux revival → floating-semver powerdns pulls the §854-fixed chart + #5551 delivers, verify with `scripts/check-live-nodeports.sh` on mothership READ-ONLY, stamp #5348/#5088/#5545; mothership up → walk the mothership/console rows + frozen wizard rows. **#3969 placement EPIC closes only when its FULL scope is walked** — G10 alone is insufficient.
5. **IF survivors force another env cycle** (only when chart/image-borne fixes cannot Day-2-roll): full canon — walk-everything-before-wipe on hw292 → extract evidence → `python3 scripts/reset-uat.py hw293` (exact arg; the script mutates on any argument) → wipe hw292 via the canonical endpoint → fresh prov-preflight PASS → train-hw293.md with ancestry proofs → founder fires hw293 → repeat C/D against the residue. #5535 aboard closes the known cutover-abort branch.

**Exit criteria**: held queue merged post-cc only; Day-2 verdict recorded on #5265; open-issue count tracks the backlog (no L8 inflation); every remaining non-green row maps to a named founder gate — zero rows blocked on engineering.
**Rows banked**: ~30-50 (survivors + 5 credential rows + ~19-20 denominator rows + mothership-gated rows).
**Failure branch**: Day-2 path fails → batch ALL chart-borne fixes into ONE hw293 train, never serial cycles; founder never answers an off-path ask → its rows stay honestly red with the gate named (six-pillars target-state law — never a workaround substituted for a founder decision).

## 4. Dependency notes

- **Parallel**: Phase A ∥ Phase B (start both immediately). Within A: PR-queue hygiene ∥ mergeable wave ∥ verify-aboard sweep; chart-version bumps SERIALIZE (collision class).
- **Serialize**: A-complete AND B-critical-path → C. C is strictly sequential internally. D only after cc=true evidence is committed. Within D: delivery-verification → funnel → SSO → residue sweeps → gitea roll → G12 (hard order; destructive-last is law). E's held-PR thaw keys on cc=true, not env-ready.
- **Cross-phase**: #5535 deliberately NOT aboard C (held) — its absence defines C's abort branch; it merges in E and protects hw293. The #5424 quarantine verification in D deliberately follows the happy-path funnel stamps. Founder tails in E are asynchronous — each clears independently as its Phase B ask lands; none blocks the others.
- **Mothership**: git-side merges legal anytime (Phase A); DELIVERY of anything mothership-side is frozen behind #5573/#5558 and recorded frozen-not-failed; zero kubectl writes against contabo-mkt, ever.

## 5. The finish line

100% is reached when, and only when, `docs/ledger/UAT.md` — the north-star ledger, 286 data rows counted across ALL row-ID formats (numeric/R#/G#) by `scripts/test_uat_tally.py` — shows every row in exactly one of two states: (a) **green with hw292/hw293-era evidence** (env + date + evidence link, stamp dated after the relevant fix's deploy commit, all three cells updated), or (b) **carrying the founder's written rewrite-or-exclude verdict quoted in the row**. Corroborating gates that must simultaneously hold: durable Overall = 100 on the completion-matrix rules (moved only by walk evidence, never mid-conversation); zero NodePort Services on any OpenOva-owned cluster including mothership (`check-live-nodeports.sh` evidence from BOTH contexts); G12 stamped 6/6 with RPO/RTO numbers; cutoverComplete=true with the 600s deny-egress hold on the live env; #4277/#4111 closed on the auto-seed walk; open-issue count matching the real backlog; and the protect-list naming the single live Sovereign. The engineering-only ceiling is ~93% — the plan reports that number honestly until founder asks #4 and #7 land, and never fakes a row green to close the gap.
