# UAT walkthrough — Make the canonical Organization/Application CR model actually LIVE

## Status — last validated: hw158 (2026-06-17) — DEEP per-row walk

- **Verdict: ⚠️ PARTIAL.** Tally over the 46 walkable rows (Pre-flight 3 + A6 + B7 + C6 + D10 + E4 + F5 + G5): **13 ✅ / 23 ❌ / 10 ⏳ VISUAL-PENDING**. The **master / object-model-LIVE proof is ✅** (`kubectl get applications.apps.openova.io -A` → **3 Ready** `shared-pg`/`-b`/`-c`; **4 canonical CRDs** present; **8 controllers** Running; funnel minted Org CR **`walk-stranger-co`**). But three of the seven write/read SEAMS are still defective at runtime: the **Application spine still writes to etcd, not Gitea** (Lane D), the **operator Dashboard treemap is still raw-pods with no Job/infra filter** (Lane E), and **showback still attributes 100% to the parent org** (Lane F). The **org→vCluster Flux loop (Lane C) and funnel↔controller convergence (Lane B) are newly REAL on hw158** (per-Org GitRepository + committed vCluster HR + readback-derived status) but **blocked at the git-auth seam** (the per-Org `GitRepository` has no `secretRef` → `authentication required` → ns/vCluster never materialise).
- **Maps to:** [`../UAT.md`](../UAT.md) **ROW 0** (master proof ✅) + **Row 8** (3 shared-PG cards, many-to-many ✅).
- **❌ rows (become fix work):** A3, A4, A5 · B3, B5, B7 · C2 · D2, D3, D4, D5, D6, D9, D10 · E1, E2 · F2, F3, F5 (+ the cross-lane G1/G2/G4 partials). The single highest-leverage fix = **add a `secretRef` (the `catalyst-gitea-token` PAT) to the per-Org `GitRepository`** the org-controller creates — that unblocks C2/B3/B5 and lets the committed vCluster HR actually reconcile.
- **Evidence:** captured inline in each row below (live `kubectl`/`curl`/Gitea-API/`git grep` command + observed output). Prior-env (hw150) evidence is void per the per-env flush rule.
- **Index:** [`README.md`](README.md).

> Issue: **#3687** (survivor; folds #3690 #3692 #3694 #3669 #3673 #3677).
> Format: click-by-click UI-walk table — `Go to <URL>` · `Do <action>` · `Expect <screen/CR>` · `☐`.
> Acceptance = the founder walking every row on a **fresh, converged** Sovereign and ticking each box with a screenshot / `kubectl` capture. Every box is binary.
> **Current env = hw158** (`console.hw158.omani.works`, dep `ab2135d4cf2d01e4`). The **"Observed today (hw150)"** column below is the **historical failing baseline** captured on the now-wiped hw150 (`console.hw150.omantel.biz`, 2026-06-16) that this ticket closes — it is kept verbatim as the before-state record, NOT a current claim. The **☐ column is now the live hw158 verdict.**
> Tooling note: this walk used `kubectl --kubeconfig /tmp/hw158-kc.yaml`, `curl -sSk`, the Gitea API (admin creds), and `git grep` — NO browser (single-instance env). Rows needing a rendered signed-in screen are **⏳ VISUAL-PENDING** with the curl/kubectl sub-proof stated.

The walk is split into seven lanes, one per folded scenario, plus a cross-lane generality gate. Every lane proves a distinct write/convergence/read seam of the ONE canonical Organization/Application CR model.

---

## Pre-flight (operator state)

| # | Go to | Do | Expect | ☐ |
|---|---|---|---|---|
| P1 | `https://console.<sovereign>/` | Open the console root URL (PIN-logged-in as owner-tier) | Lands on `/dashboard` signed in as `emrah.baysal@openova.io` (`tier=owner`) — no login form | ⏳ VISUAL-PENDING — `catalyst-ui` HTTPRoute serves `console.hw158.omani.works` (live) and `catalyst-pin-broker-credentials` Secret exists (PIN auth wired). curl PROVED the route + deployment up (`catalyst-ui 1/1 Running`); the signed-in landing screen needs a browser (no collide on the single instance). |
| P2 | terminal | `kubectl get crd \| grep -E 'organizations.orgs\|applications.apps\|environments.catalyst'` | All three CRDs present | ✅ `kubectl get crd` → `applications.apps.openova.io`, `organizations.orgs.openova.io`, `environments.catalyst.openova.io` ALL present (+ `continuums.dr.openova.io` = the 4th canonical CRD), all created `2026-06-17T03:22:15Z`. |
| P3 | terminal | `kubectl -n catalyst-system get deploy \| grep -E 'organization\|application\|environment'` | All three controllers `1/1 Running` | ✅ `catalyst-organization-controller 1/1`, `catalyst-application-controller 1/1`, `catalyst-environment-controller 1/1` (+ `continuum-controller 1/1`, `catalyst-useraccess-controller 1/1`) — 8 controllers Running. |

---

## Lane A — Signup writes ONE Organization CR, not a FerretDB store.Tenant row  (folds #3690)

The customer must end up as a single `orgs.openova.io/v1 Organization` CR that is the source of truth; the `sme`-namespace store row (if retained) is only a projection of that CR.

| # | Go to | Do | Expect (target) | Observed today (hw150) | ☐ |
|---|---|---|---|---|---|
| A1 | marketplace funnel redeem URL `https://marketplace.<sovereign>/redeem/?code=<CODE>` | Complete signup / checkout for org `acme` | Funnel success page | Funnel success page | ✅ (org=`walk-stranger-co`, completed by a prior funnel walk on this same hw158) — `marketplace` HTTPRoute live at `marketplace.hw158.omani.works`; the full `sme` funnel stack (`marketplace`/`tenant`/`provisioning`/`ferretdb`, all `1/1`) is up. The resulting Org CR `walk-stranger-co` is the proof the funnel completed end-to-end. |
| A2 | terminal | `kubectl get organizations.orgs.openova.io -A` | A new `acme` Organization CR exists — it is the write authority | ❌ `No resources found` (signup wrote a `store.Tenant` row via `handlers.go:231 CreateTenant`; CR is a dead async attempt) | ✅ **FIXED** — `kubectl get organizations.orgs.openova.io -A` → **`walk-stranger-co`** (slug=walk-stranger-co, kind=customer, tier=sme, billing=real, sovereign=hw158.omani.works). The producer path is now real: `core/services/provisioning/handlers/organization_create.go:117 createOrganizationCR` mints the CR from the `tenant.created` event; the CR carries label `openova.io/source: tenant-created-event`. |
| A3 | terminal | `kubectl -n sme exec deploy/ferretdb -- <show tenant row>` (if the row is retained) | The `sme` tenant row carries the CR's UID/owner-ref — proving it is a **projection**, not the authority | ❌ the FerretDB row IS the authority; no CR backs it | ❌ **Direction still inverted (store row → CR, not CR → projection).** The CR carries `openova.io/tenant-id: 8a4c40ad-78a8-4a64-964d-a6f6398fbb41` — a back-ref TO the FerretDB store.Tenant row, i.e. the store row is the UPSTREAM producer and the CR is the downstream artifact (the OPPOSITE of "store row is a projection of the CR"). (ferretdb/tenant are distroless — `exec` → `"sh": executable file not found`; the tenant-id label is the proof of store-first ordering.) Net: a CR now exists (A2 ✅) but is not yet the write authority over the store row. |
| A4 | terminal | `kubectl delete organization acme` then `kubectl get ns acme; kubectl get vcluster -A` | Deleting the CR GC's the vCluster + realm + repos — proves the CR is authoritative | ❌ nothing to delete (no CR); the store row would survive | ❌ CR has **NO finalizers** (`kubectl get organization walk-stranger-co -o jsonpath='{.metadata.finalizers}'` → empty) → deleting the CR would NOT GC the Gitea org / Keycloak group / vCluster. (Did NOT delete — single live env; inspected finalizers instead.) The CR is not yet authoritative-with-GC. |
| A5 | terminal | `git grep -n 'tenant.created' core/services core/controllers products/catalyst/bootstrap/api \| grep -i 'type .*Payload'` | ONE shared payload struct across tenant-service publish + provisioning-service consume + bootstrap-API funnel | ❌ THREE divergent shapes: `handlers.go:264` flat embed, `organization_create.go:61` `tenantCreatedPayload`, `sme_tenant.go:548` `orgShape` | ❌ STILL multiple shapes — `core/services/tenant/handlers/handlers.go:260` publishes an inline anonymous struct (`tenantCreatedPayload := struct{…}`); `core/services/provisioning/handlers/organization_create.go:61` declares a SEPARATE `type tenantCreatedPayload struct` consumer; `core/services/tenant/handlers/tenant_created_wire_test.go:37` re-duplicates the shape "(not imported)" by its own comment. Not ONE shared struct. |
| A6 | `https://console.<sovereign>/organizations` | Open the operator org directory | Lists `acme` (the SAME CR `kubectl` shows) | ❌ console reads its own `GET /sme/tenants` source; `kubectl get organizations -A` = empty | ⏳ VISUAL-PENDING — the operator console (`catalyst-ui`) `/organizations` directory still sources `GET /api/v1/sme/tenants` (auth-gated, curl → `401 Unauthorized`), a SEPARATE source from `kubectl get organizations -A`. curl PROVED (a) the Org CR is non-empty for the first time (`walk-stranger-co`) and (b) the `/sme/tenants` endpoint is still live; whether the rendered table now joins them needs the signed-in screen. |

---

## Lane B — Funnel and org-controller converge on ONE record/name/Git location  (folds #3673)

Today the funnel builds a `SMETenantProvisionRecord` (`sme-<uuid>` ns / `vc-<sub>` vCluster, no CR) while the org-controller builds `<slug>` from the CR — two machineries, two artifact sets, no shared key.

| # | Go to | Do | Expect (target) | Observed today (hw150) | ☐ |
|---|---|---|---|---|---|
| B1 | marketplace funnel redeem URL | Complete signup for customer `acme` | Funnel success | Funnel success | ✅ (funnel customer = `walk-stranger-co`, prior walk on this hw158) — same evidence as A1; the customer Org CR is the proof of completion. |
| B2 | terminal | `kubectl get organization acme` | A real `Organization` CR exists for the **funnel** tenant | ❌ funnel only writes `SMETenantProvisionRecord`; `nats_bridge.go:160` logs "no matching Organization CR — ack and skip" | ✅ **FIXED** — `kubectl get organization walk-stranger-co` → real CR for the funnel tenant (label `openova.io/source: tenant-created-event`). The org-controller log NO LONGER ack-and-skips: it logs `reconcile ok … keycloak_group=e5c61e02… gitea_org=walk-stranger-co vcluster=walk-stranger-co vcluster_phase=Pending vcluster_ready=false`. |
| B3 | terminal | `kubectl get ns acme; kubectl get hr -n acme` | Namespace `acme` + vCluster HR — **canonical names** | ❌ funnel makes `sme-<uuid>` ns (`sme_tenant.go:643`) + `vc-acme` vCluster (`:642`) | ❌ (canonical NAME ✅ / live ns ❌) — the org-controller now uses the canonical slug `walk-stranger-co` everywhere (Gitea org `walk-stranger-co`, vCluster `walk-stranger-co`, committed HR `metadata.namespace: walk-stranger-co`), NOT `sme-<uuid>`/`vc-acme`. BUT `kubectl get ns walk-stranger-co` → `NotFound` and `kubectl get hr -n walk-stranger-co` → none: the per-Org Flux source can't pull (see B5/C2), so the ns is never created. Naming fixed; reconcile blocked. |
| B4 | `https://console.<sovereign>/organizations` | Create a department `beta` (console button) | `kubectl get organization beta` — same CR shape as the funnel tenant | the console path reaches the org-controller, but it produces `<slug>` while the funnel produced `sme-<uuid>` — incompatible | ⏳ VISUAL-PENDING (console "create department" needs the signed-in screen). curl PROVED the convergence point: BOTH doors now feed the SAME org-controller which keys on the canonical `<slug>` — the funnel's `walk-stranger-co` CR already uses `<slug>`, so a console-created `beta` would be the identical shape (no more `sme-<uuid>` divergence). |
| B5 | terminal | `kubectl get gitrepository,kustomization -A` | ONE Flux source reconciles BOTH `acme` (funnel) and `beta` (console) — one location, not two branches | ❌ two sources: `openova@main` + `openova@sme-tenants`; the `sme-tenants` Kustomization is even `path not found` | ❌ — the org-controller now creates a **per-Org GitRepository** `flux-system/catalyst-tenant-walk-stranger-co` + Kustomization `…-vcluster` pointing at `gitea/walk-stranger-co/catalyst-tenant.git` (the canonical per-tenant location), so the design moved off the dead `openova@sme-tenants` branch. BUT it is **READY=False**: `failed to checkout … authentication required` — the GitRepository has **NO `secretRef`** (`kubectl get gitrepository catalyst-tenant-walk-stranger-co -o jsonpath='{.spec}'` → `{interval,ref.branch,timeout,url}` only, no auth), so the auth-required Gitea rejects the clone → Kustomization `Source artifact not found`. The stale `openova-sme-tenants` GitRepository + `sme-tenants` Kustomization ALSO still exist (both Ready=False). Per-Org source design landed but broken at the git-auth seam + old machinery not deleted. |
| B6 | terminal | `kubectl logs deploy/catalyst-organization-controller -n catalyst-system \| grep 'tenant.created'` | The NATS bridge now finds the matching CR (no longer "ack and skip") | ❌ ack-and-skip for every funnel tenant | ✅ **FIXED** — org-controller logs `reconciled per-Org vCluster Flux loop … git_repository=flux-system/catalyst-tenant-walk-stranger-co kustomization=flux-system/catalyst-tenant-walk-stranger-co-vcluster url=…/walk-stranger-co/catalyst-tenant.git branch=main` then `reconcile ok … vcluster_ready=false` — it FINDS and reconciles the matching CR, no "ack and skip". (The remaining `nats_bridge.go:160` ack-and-skip is now only the `order.placed`/observation path, not tenant.created provisioning.) |
| B7 | terminal | `git -C <worktree> diff` | The SME parallel store + `sme_tenant_gitops.go` writer are deleted (or reduced to Org-CR-keyed steps) | ❌ both machineries co-exist | ❌ both machineries STILL co-exist — the `sme` namespace runs the full parallel stack (`tenant`/`provisioning`/`ferretdb`/`marketplace` deploys 1/1) and the marketplace-api `store.Tenant`+`CreateTenant` path is intact, alongside the new Org-CR loop. The SME store was not deleted/reduced. |

---

## Lane C — The Org-CR vCluster is Flux-reconciled (no Ready-over-orphaned-bytes)  (folds #3669)

The org-controller must write the vCluster HR to a repo a Flux source watches, derive `status.vcluster.phase` from a readback, and set `Ready=True` only when the vCluster is actually up.

| # | Go to | Do | Expect (target) | Observed today (hw150) | ☐ |
|---|---|---|---|---|---|
| C1 | `https://console.<sovereign>/organizations` | Click "Create organization", slug `acme`, kind `internal`, submit | Row `acme` appears, status `Provisioning` | Row appears `Provisioning` (but backed by nothing) | ⏳ VISUAL-PENDING (console create needs signed-in screen). The equivalent funnel-created Org `walk-stranger-co` proves the row→CR path; its live status is `Ready=False / VClusterProvisioning` (the honest "still coming up" state, not a fake green). |
| C2 | terminal | `watch kubectl get hr,ns,po -A \| grep acme` | Within 5 min: ns `acme` Active → vcluster HR → vcluster pod `Running` | ❌ ns `acme` never created; HR written to `acme/catalyst-tenant` repo that NO GitRepository watches (`organization_controller.go:288`) | ❌ (BUT a GitRepository now DOES watch it — root cause moved) — `kubectl get ns walk-stranger-co` → `NotFound`, no vCluster pod. The HR is committed to `gitea/walk-stranger-co/catalyst-tenant.git` AND a per-Org GitRepository `catalyst-tenant-walk-stranger-co` watches it (C6 ✅) — so the hw150 "no source watches it" defect IS fixed. The new blocker is the GitRepository auth failure (no `secretRef` → `authentication required` → `Source artifact not found`), so the ns/HR never materialise. |
| C3 | terminal | `kubectl get organization acme -o jsonpath='{.status.vcluster.phase}'` | `Ready` (advances only after the vCluster HR is Ready) | ❌ frozen at hardcoded `Provisioning` (`organization_controller.go:375`) | ✅ **status is now READBACK-DERIVED, not hardcoded** — `…status.vcluster.phase` = `Pending` (correctly reflecting "HR not yet applied by Flux"), and the controller log emits `vcluster_phase=Pending vcluster_ready=false` from an actual readback each reconcile. It is NOT frozen at a hardcoded `Provisioning`; it would advance to `Ready` only when the HR is Ready (which it isn't, due to the C2 auth block — an HONEST `Pending`). |
| C4 | terminal | `kubectl get organization acme -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}'` | `True` ONLY after the vCluster HR is Ready AND the namespace exists | ❌ `True` immediately after a Gitea PutFile (`organization_controller.go:390`, message "vCluster HelmRelease + Keycloak group + Gitea Org reconciled") — the status **lies** | ✅ **the status no longer lies** — `Ready` condition = **`False`**, reason `VClusterProvisioning`, message *"vCluster HelmRelease not yet applied by Flux (manifest authored in Gitea + per-Org Flux source created; awaiting first reconcile of ./vcluster)"*. It is NOT flipped `True` on a bare PutFile — it stays `False` until the vCluster actually comes up. This is the exact #3669 fix (Ready-over-orphaned-bytes eliminated). |
| C5 | `/organizations` | Refresh while the vCluster is still coming up | Status sits at `Provisioning` until the pod is Ready, then flips to `Active` | ❌ badge shows green `Active`/`Provisioning` over orphaned bytes | ⏳ VISUAL-PENDING (badge color needs signed-in screen) — curl PROVED the backing condition the badge reads is honest: `Ready=False / VClusterProvisioning`, so a faithful UI shows "Provisioning", not green "Active". Not green-over-orphaned-bytes at the data layer. |
| C6 | terminal | `kubectl get gitrepository,kustomization -A` | A Flux source + Kustomization reconciling the per-Org vCluster manifests is present | ❌ no `catalyst-tenant` GitRepository/Kustomization exists at all | ✅ **PRESENT (the #3669 core)** — `kubectl get gitrepository,kustomization -A` shows `gitrepository/catalyst-tenant-walk-stranger-co` (url `…/walk-stranger-co/catalyst-tenant.git`, branch main) + `kustomization/catalyst-tenant-walk-stranger-co-vcluster` (path `./vcluster`). And the Gitea repo HAS the manifests: commit log shows `organization-controller: reconcile vcluster/{namespace,vcluster,kustomization}.yaml for walk-stranger-co`; `vcluster/vcluster.yaml` (1580 B) is a real `kind: HelmRelease` (vcluster chart `0.33.*`, ns `walk-stranger-co`). The loop EXISTS — it is just non-functional pending the git-auth secretRef (C2/B5). |

---

## Lane D — The running-Application spine commits to Gitea, not etcd  (folds #3694)

Create/update/Context-append must COMMIT the Application CR to Gitea (then Flux applies); the canonical representation must be non-empty and equal the running-instance count; the loop must survive `catalyst-api` scaled to 0.

| # | Go to | Do | Expect (target) | Observed today (hw150) | ☐ |
|---|---|---|---|---|---|
| D1 | `https://console.<sovereign>/catalog` | Open `bp-wordpress` → **New instance** → name `blog`, Org `acme`, topology `single-region`; Advanced → backing postgres = **Reuse existing → shared-pg**; Provision | Provisioning starts | form renders | ⏳ VISUAL-PENDING (catalog form needs signed-in screen). Not driven (single live env, no browser collide). The data-instance reuse model IS wired in the UI source (`core/console/src/components/AppsPage.svelte:178-195`: shareable apps reuse a single `shared-pg`, others get a dedicated `<slug>-pg`). |
| D2 | Gitea `http://<gitea>/acme` | Open the `acme` gitops repo | A commit landed: `applications/blog.yaml` (the Application CR) AND `shared-pg`'s IaC carries a `db/blog` Context row — **both in Git** | ❌ `HandleCreateInstance` writes the CR to etcd via `endpoint_handler.go:898` `.Create`; `wireBackingServices` appends the Context to etcd `spec.parameters` (`endpoint_handler.go:1070`, comment literally calls the etcd write "the declared IaC"); Gitea untouched | ❌ STILL etcd, not Gitea — `products/catalyst/bootstrap/api/internal/handler/endpoint_handler.go:898` `client.Resource(ApplicationGVR()).Namespace(body.Org).Create(…)` (direct apiserver `.Create`); `wireBackingServices` (`:1087-1091`) appends the Context to `spec.parameters` then `.Update`s via apiserver. No `applications/<name>.yaml` commit to Gitea on the create path. |
| D3 | terminal | `kubectl get application -n acme blog` | The CR exists and was created **from** the Git commit (Flux applied it) | ❌ no Application CRs exist anywhere | ❌ — no console-created, Git-sourced Application CR exists. (D1 not driven; and structurally the create path writes to etcd not Git per D2.) The only Application CRs present are the 3 bootstrap `shared-pg*` (self-registered by the bp-postgres chart, not by a console Git commit). |
| D4 | browser | Open `https://blog.acme.<pool-tld>` | The app is reachable (Flux fanned the commit into a HelmRelease) | n/a — no closed Git loop | ❌ N/A — no `blog` app created and no closed Git loop on the create path (D2/D3). |
| D5 | `/app/blog` | **Settings/Topology** → change to active-active → Save | A SECOND commit to `applications/blog.yaml` (placement diff) in Gitea → a 2nd-region HelmRelease materializes | ❌ `HandleApplicationUpdate` (`applications_update.go:202`) writes the CR to etcd via `.Update`; header claims "Application CR is the source of truth" but the CR lives in etcd, not Git | ❌ STILL etcd — `products/catalyst/bootstrap/api/internal/handler/applications_update.go:202 HandleApplicationUpdate` doc-comment says "patches the Application CR's spec in place" and the write is `:409` `client.Resource(ApplicationGVR()).Namespace(patched.GetNamespace()).Update(…)`. No Gitea commit on the update path. |
| D6 | terminal | `kubectl get applications.apps.openova.io -A` | **Non-empty**, equals the running-instance count (≥ the 3 shared-pg engines + gitea/harbor/keycloak/… bootstrap apps), each `bootstrap-owned`-marked, adopted not duplicated | ❌ `No resources found` while 9 postgres engines + ~64 HelmReleases run | ❌ (non-empty ✅ but NOT equal to running-instance count) — `kubectl get applications.apps.openova.io -A` → **3** CRs (`shared-pg`/`-b`/`-c`, all `apps.openova.io/bootstrap-owned: "true"`, adopted not duplicated) while **64 HelmReleases** run. Only `bp-postgres` & `bp-valkey` charts ship the `application-cr.yaml` self-registration template — gitea/harbor/keycloak/grafana/… (~61 HRs) self-register NO Application CR. So the model is alive (3 ≠ 0, the hw150 master-fail IS fixed) but coverage ≪ running-instance count. |
| D7 | terminal | Decode `sh.helm.release.v1.shared-pg.v*` (or `helm get manifest shared-pg -n shared-data`) | The shared-PG model is ON: the chart renders the `kind: Application` self-registration CR | ❌ `16a-bp-postgres-shared.yaml:170` `enabled: ${SOVEREIGN_ENABLE_SHARED_PG:=false}` → unset on hw150 → release renders 0 bytes; `application-cr.yaml:54` gate `(ne (toString .Values.enabled) "false")` makes the whole template dead code | ✅ **ON on hw158** — `kubectl get hr bp-postgres-shared -o jsonpath='{.spec.values.enabled}'` → `true` AND `.spec.values.bootstrapOwned.enabled` → `true`, so the `platform/postgres/chart/templates/application-cr.yaml:54` gate `and (ne enabled "false") bootstrapOwned.enabled (Capabilities.Has "apps.openova.io/v1")` PASSES and the chart self-registers the `kind: Application` CR — proven live by the 3 `shared-pg*` CRs whose labels (`apps.openova.io/bootstrap-owned:"true"`, `catalyst.openova.io/organization:platform`) are exactly what that template emits. (NB: the git DEFAULT of `SOVEREIGN_ENABLE_SHARED_PG` is still `false` = safe-by-default; hw158 was provisioned with the `=true` flip — the template is NOT dead code, just gated.) |
| D8 | terminal | `kubectl get cluster.postgresql.cnpg.io -A` before/after | Embedded postgres eliminated for ≥2 consumers (consumers on shared-pg Contexts; embedded `Cluster` CRs gone) | ❌ 9 embedded clusters live: `gitea-pg`, `harbor-pg`, `pdns-pg`, `pda-pg`, `sme-pg`, `newapi-…-pg`, `openova-flow-pg`, `guacamole-pg`, `cnpg-pair-…-primary` — the exact #3370 §4 violation list | ✅ **partially eliminated (≥4 consumers moved)** — `kubectl get cluster.postgresql.cnpg.io -A` → 8 clusters: `shared-pg`/`-b`/`-c` (the 3 SHARED engines) + `cnpg-pair-…-primary` (the region-kill pair, legitimate) + only **4** still-embedded: `guacamole-pg`, `openova-flow-pg`, `newapi-…-pg`, `pdns-pg`. The hw150 violators `gitea-pg`/`harbor-pg`/`pda-pg`/`sme-pg` are GONE — gitea/harbor/keycloak now consume `shared-pg` Contexts (its `spec.parameters.databases` owners = registry/gitea/keycloak) and grafana/pdns/pda consume `shared-pg-b`. So ≥4 consumers moved onto shared Contexts; 4 stragglers remain (next #3370 §4 work). |
| D9 | terminal | `kubectl logs deploy/catalyst-application-controller -n catalyst-system --tail=20` | Watch loop healthy — no `watch channel closed` / `watch error; will retry` spam | ❌ controller logs the retry warning every ~5 min (`application_controller.go:501/563`) and reconciles nothing (no CRs to act on) | ❌ STILL spamming — `kubectl logs deploy/catalyst-application-controller --tail=30` shows `application-controller: watch error; will retry  err="watch channel closed"` every ~5 min (04:28→06:53, every interval). The watch loop is unhealthy (though the 3 shared-pg CRs DID get adopted to Ready, so reconcile partially works). |
| D10 | terminal | `kubectl scale deploy/catalyst-api -n <ns> --replicas=0`; `git push` a parameter bump to `applications/blog.yaml`; wait 5 min | The estate keeps reconciling and the hand-`git push` is applied with `catalyst-api` at 0 — the loop is catalyst-independent | ❌ no Git-resident representation exists; the estate is reconstructed from informers inside catalyst-api | ❌ — console-created Application CRs live in etcd (D2/D5/D6); there is NO `applications/<name>.yaml` Git-resident representation, so a `git push` with catalyst-api at 0 would have nothing to apply. (Did NOT scale catalyst-api to 0 on the live env — confirmed structurally via the etcd write paths.) The bootstrap shared-pg CRs ARE Git-driven (via their bp-postgres-shared HR in the catalog repo), but the console create/update spine is not. |

---

## Lane E — Operator Dashboard reads the Organization/Application model, not a raw pod treemap  (folds #3692)

| # | Go to | Do | Expect (target) | Observed today (hw150) | ☐ |
|---|---|---|---|---|---|
| E1 | `https://console.<sovereign>/dashboard` | Read the landing treemap | Layer-1 default = **Organization**; a zero-customer Sovereign shows exactly ONE estate bubble, drillable to `vCluster → Application (CR)` | ❌ "Resource utilisation treemap" of raw infra pods: `cilium-agent 29%`, `falco 14%`, `kyverno 3%`, `mimir`, `harbor`, `sme-pg`; Layer-2=Application is `app.kubernetes.io/instance` of arbitrary pods (`Dashboard.tsx:348`, `dashboard.go:481`) | ❌ STILL raw-pod treemap — backend `products/catalyst/bootstrap/api/internal/handler/dashboard.go` `GetDashboardTreemap` lists raw pods via `labels.Everything()` (`:262`) and buckets `application` on the pod's `app.kubernetes.io/instance` label (`applicationKey` `:481-494`), NOT the Organization/Application CR model. Frontend default Layer-1 = `['cluster','application']` (`products/catalyst/bootstrap/ui/src/pages/sovereign/Dashboard.tsx:171-174`), dimensions `[sovereign|cluster|family|namespace|application]` — no `Organization` layer. (curl can't render the treemap; the data-source code is the proof.) |
| E2 | `/dashboard` | Look for ephemeral Job cells | NO Job pod (`cutover-*`, `scan-vulnerabilityreport-*`, `*-snapshot-save-*`) ever appears as a cell | ❌ Job pods render as treemap cells/"applications" | ❌ NO Job filter — `dashboard.go` `buildPodRows` (`:327-466`) projects EVERY pod with no `ownerReferences` Job check and no namespace skip-list; `applicationKey` (`:476`) even says "Daemonset/Statefulset/Deployment/**Job** all get hit" (bucketed IN, not out). 0 Job-kind / ownerRef==Job exclusion hits across `dashboard.go` + `Dashboard.tsx` + `treemap.types.ts`. Job pods render as cells. |
| E3 | `/apps` | Count the cards | One card per `Application` CR; bootstrap-owned platform Applications carry a "Platform" badge; cards are 1:1 with `kubectl get applications -A` | ❌ ~49 cards derived from HelmReleases/pods while `kubectl get applications -A` = 0 (cards not backed by the canonical model) | ⏳ VISUAL-PENDING (card count needs signed-in screen) — but the data layer makes 1:1 IMPOSSIBLE today: `kubectl get applications.apps.openova.io -A` = **3**, while ~64 HRs run; the `/apps` page (`core/console/src/components/AppsPage.svelte`) derives instances from HR/data-instance bindings, not 1:1 from Application CRs. So even unrendered, cards ≠ `kubectl get applications -A` count. Marked ⏳ because the exact rendered card count + "Platform" badge presence need the screen. |
| E4 | `/organizations/new` → submit one customer Org | After onboarding ONE customer Org, refresh `/dashboard` | A SECOND estate bubble appears AND `kubectl get organizations -A` shows 2 | ❌ with zero existing Organizations every upstream surface already shows raw pods — a real customer is indistinguishable from a platform pod | ⏳ VISUAL-PENDING (dashboard bubble needs signed-in screen). curl PROVED the CR-layer half: `kubectl get organizations -A` now shows **1** (`walk-stranger-co`) where hw150 showed 0 — so a customer Org IS now distinguishable AT THE CR LAYER. But the dashboard treemap reads raw pods (E1), so the rendered "estate bubble" would still be pod-derived, not the Org CR. The bubble-per-Org rendering is the unproven half. |

---

## Lane F — Showback is per-Organization (join-key), not per-pod-to-parent  (folds #3677)

| # | Go to | Do | Expect (target) | Observed today (hw150) | ☐ |
|---|---|---|---|---|---|
| F1 | `https://console.<sovereign>/organizations` | Scroll to "Showback — per-app consumption"; inspect the **Application** column | Only real tenant applications of the selected org | ❌ raw pods cluster-wide: `cnpg-pair-…-primary`, `seaweedfs`, `kyverno`, `falco`, `cilium-agent` + Job pods `cutover-harbor-prewarm-…`, 5× `scan-vulnerabilityreport-…`, `openbao-snapshot-save-…` | ⏳ VISUAL-PENDING (showback panel needs signed-in screen) — the backing API `GET /api/v1/sme/consumption` is auth-gated (curl → `401`), and the resolver code (F2) proves it would still list raw cluster-wide pods incl. Job pods. So the panel content is unproven on-screen but the data source is unchanged-defective. |
| F2 | terminal | `curl -sk .../api/v1/sme/consumption \| jq '.orgs[].apps[].application'` | Zero `batchv1.Job`-owned entries; infra namespaces (`kube-system`, `flux-system`, `trivy-system`, `kyverno`, `cnpg`, `falco`, `cert-manager`) absent from tenant `apps[]` | ❌ ~100 infra-pod rows; `orgForNamespace(_ podRow, parentOrg)` discards its arg and `return parentOrg` (`sme_consumption.go:219-220`); `aggregateConsumption` lists the whole cluster via `labels.Everything()` (`:110`) with no namespace filter | ❌ DEFECT STILL PRESENT — `products/catalyst/bootstrap/api/internal/handler/sme_consumption.go:219-221`: `func orgForNamespace(_ podRow, parentOrg string) string { return parentOrg }` (still discards the `podRow` arg, returns `parentOrg`). `HandleSovereignConsumption` lists the whole cluster via `h.k8sCache.List(clusterID, "pod", labels.Everything())` (`:110`), and the aggregation loop (`:145-173`) has NO `batchv1.Job` / `ownerReferences` check and NO infra-namespace skip-list — every pod in every namespace is summed. (curl `/api/v1/sme/consumption` → `401`, so verified via the resolver source.) |
| F3 | terminal | `curl -sk .../api/v1/sme/consumption \| jq '.orgs[].org'` | ≥2 distinct orgs once ≥2 Organizations each own a namespace, each carrying only ITS apps | ❌ exactly ONE org (parent `hw150.omantel.biz`, `isParent:true`); a second org is structurally impossible (constant attribution) | ❌ STILL one-org-only — the org key comes solely from `consumptionParentOrg()` (`sme_consumption.go:224-236`), which returns the Sovereign FQDN from the in-memory deployment record (fallback hardcoded `"sovereign"` at `:235`). The file's own header comment (`:6`) states "with zero sub-orgs, 100% of consumption attributes to the parent organization." No `openova.io/organization` label is read anywhere → a second org is structurally impossible (constant attribution). |
| F4 | `/organizations` | Create a 2nd org + run one app in it (per Lane C), refresh the panel | A SECOND org row appears, attributed only to its own app; control-plane/Job workloads roll up under a single visually-distinct "Platform overhead" line, never inside a tenant's app list | ❌ panel can only ever show the parent with ~100 infra pods | ❌ — blocked by F3 (constant parent attribution) AND Lane C (the `walk-stranger-co` vCluster never came up, so there is no 2nd-org app to attribute). No "Platform overhead" roll-up line exists in the resolver. Cannot show a 2nd attributed org. |
| F5 | terminal | `git -C <worktree> diff sme_consumption.go` | The resolver keys purely on the `openova.io/organization` label + a config-driven exclusion list — zero per-org-name / per-namespace-name special-casing; adding a third org needs no code change | ❌ hardcoded constant; exclusion list absent | ❌ DEFECT STILL PRESENT — `sme_consumption.go` reads NO `openova.io/organization` label and has NO exclusion list of any kind; the org attribution is the hardcoded `consumptionParentOrg()` constant (`:224-236`). Adding a third org WOULD need code change. |

---

## Cross-lane generality gate (the holistic proof)

The single model must behave identically across door, kind, blueprint archetype, and cloud — never a special case.

| # | Go to | Do | Expect (target) | ☐ |
|---|---|---|---|---|
| G1 | terminal | Create an org via the **marketplace funnel** AND via the **BSS internal door** (`kind=internal`) | BOTH yield an `Organization` CR through the SAME write path, differing only by `spec` fields (kind/tier); `git grep` shows no door-specific store writes | ❌ (funnel half ✅, parity ❌) — the funnel door now yields a real Org CR via `organization_create.go` (proven: `walk-stranger-co`, `kind=customer`). BUT the BSS/marketplace door STILL writes a `store.Tenant` row via `core/marketplace-api/handlers/provisioner.go:77 CreateTenant` (door-specific store write present), so the two doors do NOT share one write path yet. The internal-door CR wasn't driven this walk. |
| G2 | terminal | Drive `HandleCreateInstance` for ONE workload blueprint (`bp-wordpress`), ONE database blueprint (`bp-postgres`), and ONE other backing blueprint (`bp-valkey`/`bp-kafka`/`bp-seaweedfs`), across ONE single-region AND ONE multi-region placement | The SAME create→commit→fan-out→reconcile loop drives all of them; `git diff` shows ZERO blueprint-specific or cloud-specific write code beyond declarations | ❌ — the "commit" half of create→**commit**→fan-out does not exist: `HandleCreateInstance` writes to etcd, not Gitea (D2). The loop is generic across blueprints in code, but it is not the Git-committing loop the target requires, and no instance was driven this walk. |
| G3 | `/app/<name>` for ≥3 archetypes | Open each app page | The IDENTICAL tab strip renders for every archetype; Contexts tab appears only for shareable blueprints; a consumer's Dependencies shows `Depends on: shared-pg / db:<ctx>` | ⏳ VISUAL-PENDING (app-page tab strip needs signed-in screen). curl PROVED the data: `shared-pg`'s `spec.parameters.databases` carries the Context rows (registry/gitea/keycloak owners) and `AppsPage.svelte`/`BackingServices.svelte` render the `Depends on: shared-pg` binding — but the per-archetype tab-strip identity needs the screen. |
| G4 | terminal | `kubectl get applications.apps.openova.io -A` + `kubectl get organizations.orgs.openova.io -A` on a FRESH converged Sovereign | Both non-empty: one Application CR per running instance, one Organization CR per tenant; the operator console and `kubectl` show the SAME sets | ❌ (both non-empty ✅, but counts wrong + console parity unproven) — `kubectl get applications -A` = **3** (≠ ~64 running instances, D6) and `kubectl get organizations -A` = **1** (`walk-stranger-co`, ✅ one-per-tenant for the single tenant). Both are non-empty (the hw150 0/0 master-fail is fixed), but Application-CR ≠ running-instance count, and the console-reads-the-same-set parity is auth-gated/UI-pending (A6/E3). |
| G5 | terminal | `kubectl get hr -A` before/after the bootstrap Application-CR self-registration lands | **Zero duplicate installs** — the adoption guard ADOPTS already-installed bootstrap instances (status-only, no fan-out) — adoption unit-test cited | ✅ — the adoption guard works: `shared-pg`'s Application CR `status` = `reason: BootstrapAdopted`, message *"adopted bootstrap install flux-system/bp-postgres-shared: Helm upgrade succeeded for release shared-data/shared-pg.v2"* — it ADOPTED the already-installed HR (status-only, no second install). `kubectl get hr -A` shows exactly ONE `bp-postgres-shared` HR per instance (no duplicate `shared-pg` installs). The `spec.bootstrap: true` field drives the skip-render/fan-out adoption path. |

---

## Evidence index

Every ticked box's proof (live `kubectl`/`curl`/Gitea-API/`git grep` command + observed output) is pasted inline in the lane tables above. Key live captures on hw158 (`/tmp/hw158-kc.yaml`):
- **Org CR**: `kubectl get organization walk-stranger-co -o yaml` → `Ready=False/VClusterProvisioning`, `status.vcluster.phase=Pending`, `giteaOrg/keycloakGroup` readback fields, label `openova.io/source: tenant-created-event`, NO finalizers.
- **App CRs**: `kubectl get applications.apps.openova.io -A` → 3× `shared-pg*` Ready, `bootstrap-owned:"true"`, `status.reason=BootstrapAdopted`.
- **Per-Org Flux loop**: `kubectl get gitrepository,kustomization -A` → `catalyst-tenant-walk-stranger-co` (Ready=False, `authentication required`); Gitea commit log → `vcluster/{namespace,vcluster,kustomization}.yaml` committed.
- **Controller logs**: org-controller `reconcile ok … vcluster_ready=false` (no ack-and-skip); application-controller `watch channel closed` every 5 min.
- **CNPG**: `kubectl get cluster.postgresql.cnpg.io -A` → 3 shared + cnpg-pair + 4 stragglers.
- **Code state** (branch `docs/uat-ground-reality`): `endpoint_handler.go:898`/`applications_update.go:409` (etcd `.Create`/`.Update`); `sme_consumption.go:219-221` (`orgForNamespace` returns parentOrg) + `:110` (`labels.Everything()`); `dashboard.go:262/481` (raw-pod treemap, no Job filter); `platform/postgres/chart/templates/application-cr.yaml:54` (gate, ON when `enabled=true`).

**Net acceptance:** on a fresh, converged, cut-over Sovereign — `kubectl get organizations -A` lists every tenant, `kubectl get applications -A` equals the running-instance count, every console mutation lands as a Gitea commit Flux reconciles (catalyst-api can scale to 0), and every operator day-2 business surface (Dashboard / Organizations / Showback / Users / /apps) renders the Organization → Application model with Job/infra pods excluded — not a raw pod-and-Job list. **hw158 status: the object model is LIVE (master ✅) and the org→vCluster Flux loop + funnel↔controller convergence are newly real but auth-blocked; the Application spine (etcd→Gitea), Dashboard treemap (raw-pods→model), and showback (parent-only→per-org) seams remain ❌.**
