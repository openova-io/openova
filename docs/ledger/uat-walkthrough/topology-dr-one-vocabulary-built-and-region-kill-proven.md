# UAT walkthrough — TOPOLOGY/DR: one vocabulary, built end-to-end, region-kill proven

## Status — last validated: hw158 (2026-06-17) — RE-VALIDATED on the RECOVERED env (keycloak cascade healed; curl/kubectl/grep + authenticated console session)

- **Tally: 5 ✅ · 29 ❌ · 8 N/A · 10 ⏳ VISUAL-PENDING** (52 rows). **Headline verdict UNCHANGED: ❌ FAIL as a "one-vocabulary, built end-to-end" claim.** This is the **second pass** on hw158 — the first walk happened while the keycloak HR was `False` (a cascade that wedged grafana/gitea/sso-bridge dependents). The env has since **partially recovered**: `bp-keycloak`/`bp-grafana`/`bp-gitea`/`bp-sso-bridge` HRs are all `True`, keycloak-0 is `1/1 Running` (JGroups discovery enabled, **0 `UnknownHostException`**, started 7.9 s). **Crucially, the DR/Continuum spine did NOT recover** — `bp-catalyst-platform` is still `Unknown` ("Running 'upgrade' action, timeout 30m"), so `bp-continuum` stays `False` (dep not ready) and the lone `cnpg-pair…continuum` is still **Degraded / Ready=False** (cnpg-pair-primary 2/3, `externalClusters` empty). **Net effect of the recovery on the tally: ZERO flips** — the keycloak heal only strengthens the *region-a side* of G3 (was "not provable", now a clean region-a pass) and re-confirms the still-❌ rows on a healthier spine; no dual-region/UI row can reach full ✅. The prior `✅ PASS` header recorded only the region-kill capstone (which genuinely passed — H1/H3/H4/H7 ✅) and ignored Sections A–G, most of which are **not built on this env**. ✅ = G4 (`-mesh-rw` services) + H1/H3/H4/H7 (region-kill capstone, cited from the evidence file). The 8 N/A are E3 (depends on the unprovisionable D3) + the entire Section F (no separate capped-region Sovereign was provisioned). The 10 ⏳ are rendered-UI rows (the frontend renders the editor-dialect — a React-source fact unchanged by env recovery; an authenticated console session was established this pass via `/auth/handover`, but the no-browser directive still blocks DOM-render confirmation) + region-b pod rows (the region-a kubeconfig sees only `me-east-215-a` nodes; no region-b kubeconfig is fetchable).
- **Re-validation deltas (2026-06-17, recovered env):** (1) **Continuum re-checked → STILL Degraded** — `kubectl get continuum -n cnpg cnpg-pair-bp-cnpg-pair-continuum -o jsonpath` → `phase=Degraded, LeaseHeld=False, Ready=False`; root cause is `bp-catalyst-platform` HR `Unknown`/upgrade-wedged → `bp-continuum` `False`. C1/C2/D5/F3/H2/H5 stay ❌/⏳. (2) **G3 region-a now PASSES** — keycloak-0 `1/1 Running`, `grep -ciE 'UnknownHostException|could not translate host'` over its log → **0**; the region-b half stays ⏳ (no region-b kubeconfig). (3) **H4 corroboration strengthened** — `show synchronous_standby_names` on the shared-pg primary → `FIRST 1 ("shared-pg-1","shared-pg-3","shared-pg-2")` + `pg_stat_replication` shows `shared-pg-1 streaming sync`, confirming the RPO=0 mechanism the capstone rests on. (4) **G1/G2/G6 region-a re-confirmed healthy** (grafana 3/3, powerdns-admin 1/1, guacd 1/1 — all 0 DB-host errors); region-b half still ⏳. (5) **Authenticated console session established** — `/auth/handover?token=<RS256 JWT>` → 302 `/dashboard` + `catalyst_session` cookie; `/sovereign/self` + `infrastructure/topology` now return data (the latter reports `pattern:"multi-region"`, both `me-east-215-a` + `-b` nodes in `compute`), but the per-app Topology-tab DOM render remains browser-only.
- **What is REAL (✅):** the live **region-kill capstone** (H1–H7 — RTO ≈ 1.4 s / RPO 0, cited from `hw158-region-kill-walk-PASS.md` + live `pg_stat_replication`); the `-mesh-rw` ClusterMesh write Services exist for all 3 shared-pg instances (G4); the 3 shared-pg CRs carry `active-hotstandby` (substrate); the catalog `raw.spec.topology` carries the canonical `supported:[singleton,active-hot-standby]` (A1 data side); `promoter.go`/`sequence.go` carry **zero app-name literal** (C4 code side).
- **What is NOT built / regressed (❌ highlights):**
  - **A1/A5/A8 vocabulary is NOT one-vocab on the wire** — the live catalog serves `placementSchema.modes:["single-region","active-active"]` and the New-instance `<select>` + Topology editor render the **editor dialect** `single-region`/`active-hotstandby` (the exact spellings A1 says must NOT appear). The 3 live Application CRs carry `active-hotstandby`, not `active-hot-standby`.
  - **A6 `active-passive` is NOT selectable** and **A7 `singleton` is NOT a separate selectable option** — `ALL_MODES = ['single-region','active-active','active-hotstandby']` (3 editor modes only); `active-passive`/`singleton` exist only as canonicaliser *inputs*.
  - **A3/A4/D1/D6 cannot be performed** — `bp-grafana` + `bp-gitea` are `multiInstance.enabled:false` (singleton-per-Org); the UI does not offer "New instance" for them.
  - **B1/B2 no per-app mgmt-A/mgmt-B HR fan-out** (grafana/sso-bridge are single bare `bp-*` HRs in flux-system). **B4 placementSchema still has live controller consumers** (`application_controller.go`, `placement.go` read `placementSchema.*`, not `topology.defaults`).
  - **C1 only ONE Continuum exists** (`cnpg-pair-bp-cnpg-pair-continuum`, **Degraded**, Ready=False) — not one-per-DR-app; `dr-grafana`/`dr-sso-bridge` are **absent**. **C3/C4 no `stateless` promoter** is registered (`promoter.go` mechanisms = `cnpg-pair` + `raft-transition` only). **C5 no decorative-declaration validator rule.**
  - **G5 openbao-snapshot-save is FAILING** (`Could not resolve host openbao-openbao…` + `bao login returned no client_token`).
- **Not directly walkable from this kubeconfig (⏳/N/A):** the kubeconfig is the **region-a primary only** — no region-b kubeconfig present in `/tmp`, so Section G region-b pod rows (G1/G2/G3/G6) + the live cross-region replica side are not inspectable here (current `pg_stat_replication` on the region-a primary shows 3 **region-a-local** HA replicas only, not a region-b stream). All **rendered-UI rows** are ⏳ because the deployment-scoped console APIs (`/deployments/{id}/infrastructure`, `/applications/{name}/status`) require an authenticated console session (master-realm `admin-cli` token 401s on them) and no-browser is mandated.
- **Maps to:** [`../UAT.md`](../UAT.md) **Row 9** (Q1/Q2 vocabulary + picker) — **❌**, **Row 10** (multi-region substrate) — **⏳ partial** (region-a side only), **Row 11** (region-kill EXECUTION) — **✅**.
- **Evidence:** `../../sessions/2026-06-17/evidence/hw158-region-kill-walk-PASS.md` (H rows); live kubectl/curl transcripts pasted inline per row below.
- **Index:** [`README.md`](README.md). Prior-env evidence is void.

**Issue:** #3375
**Slug:** `topology-dr-one-vocabulary-built-and-region-kill-proven`
**Env under test:** a fresh 2-region active-hot-standby Sovereign (`console.<fqdn>`; current = `hw158.omani.works`, kubeconfig `/tmp/hw158-kc.yaml` = the **primary region (region-a) only**). Where a step needs a deliberately-broken case, it is provisioned separately ("capped-region" Sovereign — region-B intentionally not provisioned).
**Acceptance rule:** the founder walks every row below. Each row is ONE action. A box is ticked only when the "You should see" column is observed LIVE on the current env (per memory: each new env flushes all prior evidence — no carried ✅). Automated `go test`/`kubectl` rows are the proof, but the *acceptance* is the founder observing the screens.

This walkthrough covers EVERY scenario folded into #3375: one-vocabulary create/strip (was #3648), one placement model with `replicas:0` on the fanned-out passive HR (was #3675), per-app Continuum CRs + a generic stateless promoter (was #3666), the journey-built 2-region pair (was #3680), the live-DR UI honesty (was #3684), the region-absence integrity gate (was #3688), the cross-region `-mesh-rw` / openbao-snapshot / guacd wiring on a 2-region env (was #3629), and the full region-kill within the agreement RTO/RPO (Pillar 3 / D31).

---

## Section A — ONE vocabulary, end-to-end (was #3648 / #3675 UI)

| # | Go to | Do | You should see | Verdict + live evidence |
|---|---|---|---|---|
| A1 | `https://console.<fqdn>/catalog/bp-postgres` | click **New instance** → topology `<select>` | options are exactly the canonical set the blueprint supports — `singleton`, `active-hot-standby` (NOT `single-region`/`active-hotstandby`) | ❌ The live catalog serves `placementSchema.modes=["single-region","active-active"]`; the New-instance `<select>` (`InstancesSection.tsx:530-538,731-734`) maps `raw.spec.topology.supported` through `canonicalEditorMode` → `singleton`→`single-region`, `active-hot-standby`→`active-hotstandby`, then `value={m}` renders those **editor-dialect** strings. So the dropdown shows `single-region` + `active-hotstandby` — the EXACT spellings A1 forbids. `curl -H "Bearer $TOK" .../api/v1/catalog/bp-postgres` → `placementSchema={"modes":["single-region","active-active"],"default":"single-region"}`; `raw.spec.topology.supported=["singleton","active-hot-standby"]` (canonical only inside `raw`, not on the rendered select). |
| A2 | same | pick `active-hot-standby`, name it, **Provision** | the create succeeds — NO `topology "active-hotstandby" not in supported [...]` (invalid-topology) error | ⏳ VISUAL-PENDING (create is auth-gated; no browser). kubectl PROVED the editor never posts the literal `active-hot-standby` — it posts `active-hotstandby` (canonicaliser output, `InstancesSection.tsx:587 body.topology=topology`), and the 3 existing `applications.apps.openova.io` (`shared-pg`/`-b`/`-c`) carry PLACEMENT `active-hotstandby` and are `Ready`, so the editor-dialect value IS accepted by the controller. No new instance could be provisioned via curl to confirm the end-to-end create path. |
| A3 | `https://console.<fqdn>/catalog/bp-grafana` → New instance | pick `active-hot-standby`, provision | succeeds, no invalid-topology error | ❌ NOT PERFORMABLE — `curl .../catalog/bp-grafana` → `raw.spec.multiInstance={"enabled":false}` (singleton-per-Org). `InstancesSection.tsx:107` "never offer New instance for a singleton" → there is no New-instance button on bp-grafana. |
| A4 | `https://console.<fqdn>/catalog/bp-gitea` → New instance | pick `active-hot-standby`, provision | succeeds, no invalid-topology error | ❌ NOT PERFORMABLE — `curl .../catalog/bp-gitea` → `raw.spec.multiInstance={"enabled":false}`. Same singleton-per-Org gate as A3; no New-instance button. |
| A5 | `https://console.<fqdn>/app/grafana` → **Topology** tab | read the placement strip vs the page header | strip offers exactly grafana's `SupportedTopologies`; the header "Effective / Supported" matches the strip — NO contradiction, NO `single-region/active-active/active-hotstandby` strip against an `active-hot-standby·singleton` header | ❌ / ⏳ contradiction by code. The header `DeclaredTopologyPanel` (`TopologyTab.tsx:520-524`) renders canonical `active-hot-standby · singleton` (from `TOPOLOGY_BY_ID`/raw), but the editor strip below (`TopologyEditor` `ALL_MODES`, line 227 + 731-734) renders the **3 editor-dialect** chips `single-region/active-active/active-hotstandby`. So the header dialect (`active-hot-standby·singleton`) and the strip dialect (`active-hotstandby`) DO diverge — the exact contradiction A5 says must not occur. Both dialects co-exist on the wire (proven); the rendered visual is browser-only. |
| A6 | `https://console.<fqdn>/app/openbao` → Topology tab | open the mode picker | `active-passive` **is selectable** (today `ALL_MODES` omits it) | ❌ `grep -n "ALL_MODES" TopologyEditor.tsx` → `export const ALL_MODES = ['single-region', 'active-active', 'active-hotstandby']`. The picker maps `ALL_MODES` (line 227); `active-passive` is only a `canonicalizeMode`/`canonicalEditorMode` *input* (folded into `active-hotstandby`), never a selectable option. Still omitted exactly as the row's "today" notes. |
| A7 | `https://console.<fqdn>/app/cilium` → Topology tab | open the mode picker | `singleton` is selectable; the editor no longer hides two of the four canonical classes | ❌ `ALL_MODES` (3 entries) offers `single-region` (the editor spelling of singleton) but NOT a `singleton` option, and offers only 3 of the 4 canonical classes — `active-passive` is hidden. The editor still hides a class. (cilium also has live `placementSchema.modes=["single-region","active-active"]`, narrowing the set further.) |
| A8 | terminal | `grep -rn "active-hotstandby\|'single-region'" products/catalyst/bootstrap/ui/src/lib/fleet.api.ts` and the create wizard | every topology POST site routes through the canonicaliser — no raw divergent value posted (audit proof) | ❌ `grep -rn "active-hotstandby\|'single-region'" .../lib/fleet.api.ts` → line 105 `export type TopologyMode = 'single-region' \| 'active-active' \| 'active-hotstandby'` — the divergent editor dialect IS the wire type, and `InstancesSection.tsx:587` posts `body.topology = topology` where `topology ∈ ALL_MODES` (editor dialect). The "canonicaliser" maps the blueprint's canonical token TOWARD the editor dialect, then posts the editor dialect — so a raw divergent value (`active-hotstandby`/`single-region`) IS what reaches the wire (confirmed by the 3 live CRs carrying `active-hotstandby`). |

---

## Section B — ONE placement model: standby `replicas:0` reaches the fanned-out HRs (was #3675)

| # | Go to | Do | You should see | Verdict + live evidence |
|---|---|---|---|---|
| B1 | terminal (primary kubeconfig) | `kubectl get hr -A -l catalyst.openova.io/app=grafana` | the mgmt-A (active) HR AND the mgmt-B (passive) HR both present | ❌ `kubectl --kubeconfig /tmp/hw158-kc.yaml get hr -A -l catalyst.openova.io/app=grafana` → **`No resources found`**. grafana exists only as a single bare `flux-system/bp-grafana` HR (`False`, dependency bp-keycloak not ready) — no per-app mgmt-A/mgmt-B labelled fan-out. |
| B2 | terminal | `kubectl get hr -A -l catalyst.openova.io/app=grafana -o yaml \| grep -A2 -i replicas` | the mgmt-B **passive** HR carries `replicas: 0` in its values; the mgmt-A active HR carries N (≥1) — NOT byte-identical hot copies | ❌ No labelled grafana HRs exist (B1) → no passive `replicas:0` HR to inspect. The fan-out-with-passive-`replicas:0` mechanism is not present on this env. |
| B3 | terminal | inspect the Gitea catalog repo manifests for grafana | exactly **ONE** manifest set per Application — no parallel Axis-B per-region manifest set | ⏳ VISUAL-PENDING (no Gitea repo clone performed). kubectl context: the local Gitea `flux-system/openova-catalog-sovereign` GitRepository is `True` (synced); grafana renders as ONE `bp-grafana` HR (consistent with one manifest set), but this is the singleton install path, not a per-Application Axis-A/B fan-out, so the row's premise (a fanned-out Application) does not apply on this env. |
| B4 | terminal | `grep -rn "placementSchema" core/` | **zero** controller consumers (only a deprecated-field shim if kept) — `EffectiveDefault` reads `topology.defaults` | ❌ `grep -rln "placementSchema" core/` returns **live controller consumers**: `core/controllers/application/internal/controller/application_controller.go` (line 686 `placement.EffectiveDefault(...)`, 698 "Validate placement against Blueprint.placementSchema.modes", 1245 `statusPlacementSource="blueprint-placementSchema"`), `core/controllers/internal/placement/placement.go` (`EffectiveDefault` reads `placementSchema.default`/`defaultOnMultiRegion`, NOT `topology.defaults`), plus `blueprint/internal/validate/validate.go`. placementSchema is still the live placement source — not reduced to a deprecated shim. |
| B5 | terminal | repeat B2 for a STATELESS app (`sso-bridge`) and `git diff` the fan-out change | the passive copy is scaled per its blueprint with **zero per-app code** — the scale-down keys on the role label, not the app name (generality) | ❌ `kubectl get hr -A` → sso-bridge exists only as a single `flux-system/bp-sso-bridge` HR (`False`, bp-keycloak not ready); no passive copy / role-labelled fan-out. The generality demonstration cannot be made because the base fan-out (B1/B2) is absent. |

---

## Section C — Per-app Continuum CRs + generic stateless promoter (was #3666)

| # | Go to | Do | You should see | Verdict + live evidence |
|---|---|---|---|---|
| C1 | terminal | `kubectl get continuums.dr.openova.io -A` | **one Continuum CR per DR-capable Application** — `dr-grafana` AND `dr-sso-bridge` present alongside the cnpg-pair CR — NOT one total | ❌ **RE-VALIDATED on the recovered env — STILL one total.** `kubectl --kubeconfig /tmp/hw158-kc.yaml get continuums.dr.openova.io -A` → **exactly ONE**: `cnpg/cnpg-pair-bp-cnpg-pair-continuum` (APP `catalyst-platform`, PHASE **Degraded**; `…-o jsonpath` of conditions → `LeaseHeld=False reason=Reconciled msg="" / Ready=False reason=Reconciled msg=""`, `leaseHolder=` empty). `dr-grafana` and `dr-sso-bridge` are **absent**. The keycloak cascade recovery did NOT heal this — `bp-catalyst-platform` HR is `Unknown` ("Running 'upgrade' action, timeout 30m") → `bp-continuum` HR `False` (dep not ready), so the per-app Continuum fan-out the row asserts never reconciles. This is the "one total" anti-state the row forbids. |
| C2 | `https://console.<fqdn>/app/grafana` → Topology → **Switchover** | click | a real promotion result `applied:true` — NOT `"no live 2-region cnpg-pair backing"` 404 | ❌ / ⏳ The DRSection derives `continuumName = continuumName ?? \`dr-${applicationName}\`` (`TopologyTab.tsx:404`) = `dr-grafana`, which does not exist (C1). So a Switchover click resolves against a missing CR → the 404-equivalent the row forbids. (The click itself is browser-only ⏳; the missing backing CR is proven by kubectl.) |
| C3 | `https://console.<fqdn>/app/sso-bridge` → Topology → **Switchover** | click | `applied:true` via the **stateless promoter** (HTTPRoute weight-flip + DNS flip + lease swap, no state step) | ❌ No `stateless` promoter exists. `grep -rln "stateless" core/controllers/continuum/` → empty; `promoter.go:47-69` defines only `MechanismCNPGPair="cnpg-pair"` + `MechanismRaftTransition`. There is no HTTPRoute-weight-flip / DNS-flip promoter, and no `dr-sso-bridge` Continuum to drive it. |
| C4 | terminal | `git diff` the Promoter enum + the engine | the new `stateless` promoter is registered generically — **zero app-name literal** in `promoter.go`/`sequence.go` (generality proof) | ❌ (mixed) The **zero-app-name-literal** half HOLDS: `grep -niE '"grafana"\|"gitea"\|"keycloak"\|"sso-bridge"\|"openbao"' core/controllers/continuum/internal/switchover/promoter.go sequence.go` → **empty**. But the **`stateless` promoter is NOT registered** (C3) — the new promoter the row asserts does not exist, so the registration claim fails. |
| C5 | terminal | `grep -n "backingServices\|replication.backend" platform/grafana/blueprint.yaml` + run the blueprint validator on a deliberately-decorative blueprint | a `replication.backend: cnpg-pair` with NO `backingServices[]` of `type: postgres` and no cnpg-pair template is **rejected** by validation — the decorative-declaration hole is closed | ❌ `grep -rniE "backingServices\|replication.backend\|cnpg-pair\|decorative" core/controllers/blueprint/internal/validate/validate.go` → **empty**. No validator rule rejects a decorative `replication.backend: cnpg-pair` that lacks a `type:postgres` backingService — the decorative-declaration hole is NOT closed in the validator. |
| C6 | `https://console.<fqdn>/app/cilium` (singleton) → Topology tab | read | **no** Switchover button; "no cross-region failover" — the DR section does not render for a singleton | ⏳ VISUAL-PENDING. Code PROVES the gate: `TopologyTab.tsx:274 showDR = isDRCapableClass(effectiveDRClass)`, and cilium's live `placementSchema.modes=["single-region","active-active"]` + no Application CR (`kubectl get applications -A \| grep cilium` → none) → `liveClass`/`declaredClass` resolve non-DR → `showDR=false` → DRSection not rendered. Visual confirmation needs a browser. |

---

## Section D — The app journey BUILDS the 2-region pair (was #3680)

| # | Go to | Do | You should see | Verdict + live evidence |
|---|---|---|---|---|
| D1 | `https://console.<fqdn>/catalog/bp-grafana` | **New instance** → select `active-hot-standby` → **Provision** | the wizard auto-creates the backing as its OWN card (per #3370); the consumer's Dependencies tab reads `Depends on: shared-pg (active-hot-standby) / db:grafana` | ❌ NOT PERFORMABLE — `bp-grafana` is `multiInstance.enabled:false` (A3); there is no New-instance wizard for it, so no auto-create-backing flow to walk. |
| D2 | `https://console.<fqdn>/apps` | — | the grafana card AND its backing postgres card, the backing showing an active-hot-standby badge | ⏳ VISUAL-PENDING (the `/apps` grid is auth-gated, browser-only). kubectl context: no grafana-backing postgres Application CR exists — only the 3 bootstrap `shared-pg*` CRs. No grafana→backing card pair to render. |
| D3 | terminal | `kubectl get cluster.postgresql.cnpg.io -A \| grep <grafana-backing>` | a **primary** Cluster (region-A) AND a **replica** Cluster (region-B) — a real 2-region pair, not a single-region Database | ❌ No grafana-specific backing Cluster exists. `kubectl get cluster.postgresql.cnpg.io -A -o custom-columns=...,REPLICA:.spec.replica.enabled` → **every** cluster has `replica.enabled=<none>` (shared-pg/-b/-c, cnpg-pair, pdns-pg, newapi, guacamole, openova-flow). No `spec.replica`-configured cross-region replica Cluster is present in THIS (region-a) kubeconfig's view. |
| D4 | terminal | `kubectl get cluster <replica> -o jsonpath='{.spec.externalClusters}'` and `psql -c "select * from pg_stat_wal_receiver"` on the replica | `externalClusters` non-empty AND a live WAL stream from the primary | ❌ The 2-region pair CR `cnpg/cnpg-pair-bp-cnpg-pair-primary` has **empty `.spec.externalClusters`** and is stuck "Waiting for the instances to become active" (2/3 ready). No replica Cluster is reachable from this kubeconfig to run `pg_stat_wal_receiver` against. |
| D5 | terminal | `kubectl get continuums.dr.openova.io -A \| grep <grafana>` | a Continuum CR with `applicationRef=<grafana>` and `cnpgPair` pointing at the generated pair | ❌ Only `cnpg-pair-bp-cnpg-pair-continuum` (applicationRef `catalyst-platform`) exists; no grafana-keyed Continuum (C1). |
| D6 | `https://console.<fqdn>/catalog/bp-grafana` → New instance → **single-region** | provision a second instance single-region | `kubectl get cluster.postgresql.cnpg.io` for it shows a **single-region Database** (no pair) — the topology choice genuinely drives the backing shape | ❌ NOT PERFORMABLE — bp-grafana has no New-instance flow (A3/D1). The topology-drives-backing-shape demonstration cannot be made via grafana. |
| D7 | terminal | provision a DIFFERENT postgres app hot-standby; `git diff` | it produces its OWN pair + Continuum with **zero per-app code** — topology propagates from the consumer's resolved variant, keyed on no app name (generality) | ❌ NOT PERFORMABLE this session — no second hot-standby postgres app could be provisioned via curl (deployment-scoped create is auth-gated; no console session). The generality demonstration is unwalked. |

---

## Section E — UI mirrors LIVE DR state, never a build-time constant (was #3684)

| # | Go to | Do | You should see | Verdict + live evidence |
|---|---|---|---|---|
| E1 | `https://console.<fqdn>/app/grafana` → Topology tab, on a prov where grafana has **no** live pair yet | read the DR section | the honest **"Declared active-hot-standby — no live DR backing on this Sovereign. Switchover unavailable…"** state — and **NO armed Switchover button** (today it shows an armed button that 404s) | ❌ Code shows the OPPOSITE: grafana declares `multiRegion:active-hot-standby` (live catalog `raw.spec.topology`), and this is a 2-region prov → `effectiveDRClass='active-hot-standby'` → `showDR=true` and `hasSwitchover=true` (`TopologyTab.tsx:274,281-284`), so the DRSection renders an **armed** Switchover that points at the non-existent `dr-grafana` Continuum (C1/C2). There is no live `dr-grafana` backing yet the button is armed — exactly the "armed button that 404s" the row says must be replaced. (Rendered text is browser-only ⏳; the armed-without-backing logic + missing CR are proven.) |
| E2 | same | try to click Switchover | there is no clickable Switchover when no backing is live | ❌ Per E1 the Switchover renders armed (`hasSwitchover=true`) even though `dr-grafana` is absent — a clickable control against no live backing. |
| E3 | (after D3 provisions a real pair) `https://console.<fqdn>/app/grafana` → Topology | read the DR section | the **live** Continuum status, a **live** replication-lag number (seconds), and an **armed** Switchover | N/A — depends on D3 (a real grafana pair), which is not provisionable on this env (D1/D3 ❌). Cannot be walked. |
| E4 | same | confirm replication lag | a live numeric value (or "no replica" when none) — never the hardcoded `—` | ❌ `TopologyTab.tsx:380-383` renders `Replication lag: {effectiveDRClass === 'active-hot-standby' ? '—' : 'n/a (mode)'}` — for an active-hot-standby app the value is the **hardcoded `—`**, never a live number sourced from the Continuum `replicationLagSeconds`. The row's "never the hardcoded —" is violated. |
| E5 | terminal | `git diff` TopologyTab | `showDR`/`hasSwitchover` gate on the live API response; the `continuumName` is the REAL CR name (or "none"→no button); **zero `TOPOLOGY_BY_ID`-driven render of the button** (generality) | ❌ `TopologyTab.tsx` imports `TOPOLOGY_BY_ID` (line 34) and `declaredTopology`/`declaredClass`/`effectiveDRClass` derive the Switchover render from it (`isMultiRegion ? declaredTopology.multiRegion …`, lines 232-284). `continuumName` defaults to the **constructed** `dr-${applicationName}` (line 404), not a verified real CR name. So the button render IS partly `TOPOLOGY_BY_ID`/declared-constant-driven, not gated purely on a live Continuum existence check — the generality/honesty assertion fails. |

---

## Section F — Integrity gate REFUSES a half-built topology (was #3688)

> This section uses a **separate, deliberately-broken Sovereign**: requested active-hot-standby with region-B capped so it never provisions. (Capture its cloud-init log first per DEBUG-BEFORE-WIPE.)

| # | Go to | Do | You should see | Verdict + live evidence |
|---|---|---|---|---|
| F1 | mothership wizard | request a Sovereign, pick **active-hot-standby**, 2 regions, but cap region-B so it cannot come up | provisioning starts one pass | N/A — no separate capped-region Sovereign was provisioned this session (the directive scoped the walk to the existing hw158 env, curl/kubectl/grep only). Section F requires a second deliberately-broken prov that does not exist. |
| F2 | provisioning watch / deployment record | wait for the watch to resolve | the record is **`failed`, NOT `ready`**, with reason "standby region not provisioned" — the phase-1 watch log line names region-B missing | N/A — no capped Sovereign (F1). |
| F3 | terminal (capped Sovereign) | `kubectl get continuums.dr.openova.io -A` | the Continuum CR is **absent** OR `Degraded` with `reason=standby-region-absent` (not an empty message) | ❌ (best available signal) **RE-VALIDATED on the recovered env — still an empty message.** On hw158 the single Continuum is `Degraded` but its condition `message` is **empty string**: `kubectl --kubeconfig /tmp/hw158-kc.yaml -n cnpg get continuums.dr.openova.io cnpg-pair-bp-cnpg-pair-continuum -o jsonpath='{range .status.conditions[*]}{.type}={.status} reason={.reason} msg="{.message}"{"\n"}{end}'` → `LeaseHeld=False reason=Reconciled msg="" / Ready=False reason=Reconciled msg=""`. The row wants a populated `reason=standby-region-absent` (not an empty message) — the live Continuum carries `reason=Reconciled` + an empty message, so the message-honesty sub-assertion still fails. (The capped-Sovereign case itself is N/A — no such prov.) |
| F4 | `https://console.<capped-fqdn>/settings` (or Sovereign Topology) | read | a **red banner**: "Active-hot-standby requested; standby region `…-b` was not provisioned. Disaster-recovery is INACTIVE. This Sovereign is running single-region." — NEVER a green active-hot-standby badge | N/A — no capped Sovereign (F1); banner not walkable. |
| F5 | `https://console.<capped-fqdn>/app/grafana` → Topology | read the Switchover control | the Switchover button is **disabled** with the region-missing reason — never armed against a phantom region | N/A — no capped Sovereign (F1). |
| F6 | `https://console.<capped-fqdn>/app/keycloak` → Topology | read | the SAME disabled Switchover + the same honest state on a SECOND agreed app — `git diff` shows the gate keys on `bcp_topology` + region presence + Continuum phase with **zero app-name literal** (generality) | N/A — no capped Sovereign (F1). |
| F7 | `https://console.<capped-fqdn>/cloud` | read region count | `Cluster 1/1` (the TRUE count) — no phantom region-B bubble | N/A — no capped Sovereign (F1). |
| F8 | a fresh **single-region** prov (control case) | `kubectl get hr -n flux-system bp-cnpg-pair -o jsonpath='{.spec.values.cnpgPair.enabled}'` and `kubectl get continuums.dr.openova.io -A` | `enabled=false` AND zero Continuum CRs — single-region renders zero DR (safe-default re-proven) | N/A — hw158 is a 2-region prov, not the single-region control case this row requires; no fresh single-region prov was made. (On hw158 the cnpg-pair IS enabled and 1 Continuum exists, consistent with 2-region — but that is not the single-region control the row asks for.) |

---

## Section G — Cross-region data wiring works on a 2-region env (was #3629)

> Run on the healthy 2-region Sovereign, against the secondary-region (region-b) workloads. **Note:** only the region-a primary kubeconfig is available in `/tmp` — region-b pods (G1/G2/G3/G6) are not inspectable from here.

| # | Go to | Do | You should see | Verdict + live evidence |
|---|---|---|---|---|
| G1 | terminal (region-b) | `kubectl get pods -n grafana -o wide` and the grafana log | grafana **Running** (1/1) — no `lookup shared-pg-b-rw.shared-data.svc.cluster.local … no such host` crashloop; it resolves the write host via `<instance>-mesh-rw` | ⏳ VISUAL-PENDING (region-b kubeconfig absent). **Re-confirmed region-a on the recovered env:** grafana is `grafana-5c88cb955c-gbtwj 3/3 Running` (5h30m, on `me-east-215-a`); `grep -ciE 'no such host\|could not translate'` over the grafana log → **0** — no crashloop region-a-side. The region-b grafana pod cannot be inspected from this kubeconfig (region-a kubeconfig sees only `me-east-215-a` nodes). |
| G2 | terminal (region-b) | `kubectl get pods -n powerdns-admin` + log | powerdns-admin **Running** — no `could not translate host name "shared-pg-b-rw…"`; the CNPG-minted `uri` host is rebuilt onto `-mesh-rw` | ⏳ VISUAL-PENDING (region-b absent). **Re-confirmed region-a on the recovered env:** `powerdns-admin-bp-powerdns-admin-754bc5c8b9-dbhb2 1/1 Running` (5h31m); `grep -ciE 'could not translate host\|no such host'` over its log → **0** — healthy region-a-side. Region-b not inspectable. |
| G3 | terminal (region-b) | `kubectl logs keycloak-0 -n keycloak \| grep -i JGroups` | no `java.net.UnknownHostException: shared-pg-rw.shared-data…` — keycloak's background DB host resolves | ⏳ **region-a side now PASSES on the recovered env (region-b still absent).** First walk: region-a keycloak HR was `False`, "not provable here". Post-recovery: `bp-keycloak` HR is `True` (chart 1.4.30) and `keycloak-0` is `1/1 Running` (79m). `kubectl --kubeconfig /tmp/hw158-kc.yaml logs keycloak-0 -n keycloak \| grep -i JGroups` → `JGroups JDBC_PING discovery enabled` + `Starting JGroups channel 'ISPN' with stack 'jdbc-ping'`; `grep -ciE 'UnknownHostException\|could not translate host'` over the full log → **0**; Keycloak 26.3.3 `started in 7.935s`. So region-a keycloak's background DB host resolves cleanly — the row's condition holds region-a-side. The **region-b** half stays ⏳ (region-a kubeconfig sees only `me-east-215-a` nodes; no region-b kubeconfig is fetchable). |
| G4 | terminal | `kubectl get svc -A \| grep -- -mesh-rw` and `kubectl get svc <instance>-mesh-rw -o yaml \| grep selectorType` | a **write-capable** (`selectorType: rw`) ClusterMesh-global `<instance>-mesh-rw` Service exists in BOTH regions | ✅ `kubectl --kubeconfig /tmp/hw158-kc.yaml get svc -A \| grep -- -mesh-rw` → `shared-data/shared-pg-mesh-rw` (10.96.142.99), `shared-pg-b-mesh-rw` (10.96.195.236), `shared-pg-c-mesh-rw` (10.96.21.64), all `ClusterIP :5432`. The write-capable `-mesh-rw` Services exist for all 3 shared-pg instances in region-a. (The "in BOTH regions" half is ⏳ — region-b svc list not inspectable — but the region-a side of the mechanism is present and correct.) |
| G5 | terminal | `kubectl get jobs -n openbao -A \| grep snapshot` (both regions) | `openbao-snapshot-save-*` (region-a) + `openbao-snapshot-fetch-*` (region-b) **Complete** — no `CreateContainerConfigError` (the `seaweedfs-s3-secret` is mirrored into ns `openbao`) | ❌ **RE-VALIDATED on the recovered env — STILL Failing.** `kubectl --kubeconfig /tmp/hw158-kc.yaml get jobs -n openbao \| grep snapshot` → `openbao-snapshot-save-29694440 Failed 0/1` (5h29m) + a FRESH `openbao-snapshot-save-29694760 Failed 0/1` (9m26s) — the cron is still re-firing and failing post-recovery. Pod logs (prior pass): `curl: (6) Could not resolve host: openbao-openbao.openbao.svc.cluster.local` + `FATAL: bao login returned no client_token`. The save job is FAILING, not Complete. |
| G6 | terminal (both regions) | `kubectl get pods -n catalyst-system \| grep guacd` | guacd **Running** in both regions — no `persistentvolumeclaim "guacamole-recordings" not found` | ⏳ VISUAL-PENDING (region-b absent). **Re-confirmed region-a on the recovered env:** `guacd-84b4695f59-vfcnc 1/1 Running` (5h32m) in ns `catalyst-system` — healthy, no PVC error region-a-side. Region-b guacd not inspectable. |

---

## Section H — Region-kill proven WITHIN the agreement RTO/RPO (Pillar 3 / D31, RTO ≤ 30 s, zero loss)

> The capstone. Real region kill (instance destroy or NetworkPolicy isolation per `docs/DOD.md` D31 §6) — NOT a Pod restart or a Deployment scale-down. **These rows were executed live earlier this day; the primary artifact is `../../sessions/2026-06-17/evidence/hw158-region-kill-walk-PASS.md` (PR #3742). Cited as ✅ per the orchestrator's instruction; corroborated by live `pg_stat_replication` on the region-a primary this session.**

| # | Go to | Do | You should see | Verdict + live evidence |
|---|---|---|---|---|
| H1 | terminal | start the D31 counter-writer against the proven postgres app's `<instance>-mesh-rw` write endpoint (monotonic id insert loop) | ids increment, committed against the primary (region-A) | ✅ Evidence file §"The kill walk" step 2: "Seeded **5 monotonic rows** against the primary write endpoint — each COMMIT was synchronously acked by region-b before returning." Live corroboration: `shared-pg-mesh-rw` write Service exists (G4); the primary `shared-pg-2` accepts writes (`pg_stat_replication` shows sync standby). |
| H2 | `https://console.<fqdn>/app/grafana` → Topology | read before the kill | live Continuum `Ready`, lease held by region-A, standby region present | ⏳ VISUAL-PENDING (UI browser-only). Substrate caveat **re-checked on the recovered env — Continuum STILL Degraded**: `kubectl … get continuum cnpg-pair-bp-cnpg-pair-continuum -n cnpg` → `phase=Degraded, LeaseHeld=False, Ready=False` (the keycloak recovery did not heal the `bp-catalyst-platform`→`bp-continuum` spine). The evidence file's region-kill operated directly on the `shared-pg` CNPG cluster (patch `replica.enabled`), not via a `Ready` grafana Continuum. The UI "Ready before kill" state is not separately confirmable. |
| H3 | terminal | **kill region-A** (the real region kill per D31 §6) | region-A unreachable within ~5 s | ✅ Evidence file step 3: "**Severed region-a:** force-deleted the primary CNPG pods + cordoned the region-a nodes (a real region kill per DOD D31 §6, not a pod restart or a scale-down)." |
| H4 | terminal (counter-writer) | observe | switchover completes **≤ 30 s** (kill → counter writer reconnects to the new primary); the replica is promoted; `id = last_id + 1`, **zero gap** (zero transactions lost) | ✅ Evidence file step 5-6 + Measured-outcome table: `pg_is_in_recovery` flipped `t → f` in **≈ 1.4 s** (RTO, ≤ 30 s target); **all 5 seeded rows survived** (RPO = 0, zero rows lost); a post-kill write was accepted. The #3742 fix (`standbyNamesPre:[replica]` + `maxStandbyNamesFromCluster:0`) made `synchronous_standby_names` list the region-b replica → every COMMIT blocked until region-b acked → RPO=0. **Re-corroborated live on the recovered env**: `psql -c "show synchronous_standby_names"` on the `shared-pg` primary → `FIRST 1 ("shared-pg-1","shared-pg-3","shared-pg-2")` and `pg_stat_replication` → `shared-pg-1 streaming sync` + `shared-pg-3 streaming potential` — the synchronous-standby mechanism the RPO=0 claim rests on is still in force. |
| H5 | `https://console.<fqdn>/app/grafana` → Topology | read after switchover | primary is now region-B; a switchover audit event is present; the app is reachable on its FQDN | ⏳ VISUAL-PENDING (UI browser-only). Substrate: the evidence file confirms region-b became the writable primary (`replica.enabled=false` patch promoted it); the UI audit-event render is not separately confirmable. |
| H6 | `https://console.<fqdn>/app/keycloak` | hit `auth.<fqdn>` after the kill | the keycloak realm/sessions survive the region-A kill — a second agreed app proven (generality across the agreed set) | ❌ / ⏳ Not demonstrated. The evidence file proves the region-kill on the **postgres/shared-pg** data path only; it does not record a keycloak-survives-the-kill walk. Live now: `curl -sSk -L https://auth.hw158.omani.works/ ` is reachable (keycloak Running), but the kill happened earlier and the keycloak-specific survival was not the artifact's subject. The "second agreed app" generality row is unproven for keycloak. |
| H7 | terminal | rejoin region-A | recovery completes without split-brain (one primary, the rejoined region becomes follower) — walked once | ✅ (with caveat) Evidence file step 7: "**Restored region-a** writable (rejoined the topology)." The file's own out-of-scope note flags "Day-2 cross-region rejoin (split-brain avoidance on the rejoined region)" as a follow-up — i.e. the rejoin was performed once but robust split-brain avoidance is tracked separately. Cited ✅ for the single walked rejoin. |

---

## Evidence index

Every ticked box above is backed by the inline live transcript (kubectl/curl/grep) or, for the H-section capstone, by `docs/sessions/2026-06-17/evidence/hw158-region-kill-walk-PASS.md` (RTO ≈ 1.4 s / RPO 0, PR #3742). The deployment-scoped console UI rows (rendered selects, badges, banners) are marked **⏳ VISUAL-PENDING** because (a) the no-browser directive and (b) the deployment-scoped APIs (`/deployments/{id}/infrastructure`, `/applications/{name}/status`) reject the master-realm `admin-cli` token with 401 — only an authenticated console session reaches them. For each ⏳ row the inline note states exactly what curl/kubectl/grep DID prove (the code-path, the live CR/Service state, the catalog wire shape).

**Honest summary of generality rows (B5, C4, D7, E5, F6, H6):** C4's zero-app-name-literal half is the only generality assertion that holds (empty grep over `promoter.go`/`sequence.go`); B5/D7/F6 are unwalkable because the base mechanism (fan-out / second pair / capped Sovereign) is absent; E5 fails because the Switchover render is partly `TOPOLOGY_BY_ID`-driven; H6 (keycloak survives the kill) was not demonstrated. A mechanism that is asserted but absent fails the ticket (founder rule #4).
