# #3379 SOVEREIGNTY — user acceptance walk (web UI)

**Honest framing:** the cutover (cutting the cord to the mothership) is a behind-the-scenes process the user never drives, so there is **almost no end-user UI**. The user-facing acceptance is the **negative** one — after the Sovereign goes independent, **nothing they touch breaks**. The cutover engine itself is verified from the operator side (handover finalise → 11-step engine → `cutoverComplete`).

## 2026-06-15 — hw139 (`c89aa7059556b342`, kom4dc 2-VPC) — #3558 re-attempt: step-03 deadline/parallelism/resume fixes ALL proved live; engine self-healed past step-03 and reached **step 10 of 11 (9/11 success)**; two new catches: 5th (step-08 deny-egress CCNP never applied — read-only-fs) and **6th (step-10 vcluster-registry-pivot halts on an unrewritten `registryMirror` tether)**

Evidence dir: `docs/sessions/2026-06-15/evidence/3379-cutover-reattempt/`. No-fresh-prov re-attempt: hw139's cutover had wedged at step-03 harbor-prewarm with `DeadlineExceeded` (old Job carried `activeDeadlineSeconds=900`; resume refused to re-run a Failed step). PR #3558 (`9c6f864`) fixed both. hw139's Flux tracks `main`, so catalyst-api rolled `eaab0b3`→`9c6f864` and the engine resumed on restart.

| Step | What was checked | Status | Evidence |
|---|---|---|---|
| #3558 build rolls onto hw139 | catalyst-api `eaab0b3`→`ghcr.io/openova-io/openova/catalyst-api:9c6f864` (deploy-bot `1a61e5593` image-bump + `bb7665e7a` bootstrap-kit pin 1.4.633); cutover chart `bp-self-sovereign-cutover@0.1.61`. | ✅ | `00d-preroll-image.txt` |
| Engine **resume** (#3558 Defect-2) | catalyst-api startup: `cutover-resume: in-flight cutover detected on startup` (idx=2, failedStep=harbor-prewarm) → `spawning runCutover to resume interrupted cutover`. Engine **deleted** the old DeadlineExceeded Job + minted a fresh one — transient-retry path works. | ✅ | `02-catalyst-api-cutover-resume.log` |
| Step-03 per-step deadline (#3558 Defect-1a) | New Job `…-1781473205` minted with `activeDeadlineSeconds=**5400**` (old failed Job `…-1781464129` had the inert **900**s global floor). `cutoverStepDeadline()` now honors the chart `stepTimeouts.harborPrewarmSeconds`. | ✅ | `00b-…json` (900) vs `03-new-prewarm-job-deadline5400.json` (5400) |
| Step-03 parallel push (#3558 Defect-1b) | `found 29 unique openova-io images` → `pushing 29 image(s) with parallelism=6`. Local Harbor climbed **6/29 → 27/29 in one pass** vs the old serial 6/29 wedge. | ✅ | `04-prewarm-parallelism6-29images.log` |
| Step-03 **reaches 29/29** (4th catch — egress flake, self-healed) | A repeating drop on the **same guacd:1.5.5 blob** `sha256:0343bcd5…` at the **identical byte offset 31457281** (`unexpected EOF` → `use of closed network connection`): attempt-1 `push_ok=27 push_fail=2`, attempt-2 `push_ok=28 push_fail=1`, attempt-3 fail (3rd guacd drop). **Attempt-4 landed it: `push_ok=29 push_fail=0`** (egress recovered). Job `backoffLimit=3` self-heal worked end-to-end, **zero-touch**. NOT a #3558 logic bug; each retry also re-pays the slow 38 MB skopeo-binary download (~8 min/pod on the throttled egress). | ✅ (self-healed) | `05-…`, `07-…`, `08-prewarm-attempt3-guacd-3rd-drop.log`, `09-prewarm-attempt4-SUCCESS-29of29.log`, `06-4th-catch-analysis.md` |
| Step-04 registry-pivot | `ghcr-pull` dockerconfigjson auths = `['registry.hw139.omani.works']` — registry tether pivoted to local Harbor, ghcr.io stripped. | ✅ | `10-step04-ghcr-pull-registry-pivot.txt` |
| Step-05 flux-gitrepository-patch | Flux `GitRepository/openova` URL = `http://gitea-http.gitea.svc.cluster.local:3000/openova/openova` (local Gitea, NOT github.com) **with `secretRef=cutover-gitea-git-auth`** — the #3536 secretRef wiring that hw138 lacked is now present and load-bearing. | ✅ | `11-step05-gitrepository-pivot.txt` |
| Step-06 helmrepository-patches | **61 HelmRepositories on local Harbor, 0 on ghcr.io.** This is the step that was INEFFECTIVE on hw136/hw138 (Flux reverted to ghcr w/o a secretRef); with the secretRef the pivot **holds**. | ✅ | `12-step06-helmrepo-pivot-count.txt` |
| Step-07 catalyst-api-env-patch | catalyst-api rolled to `registry.hw139.omani.works/openova-io/openova/catalyst-api:9c6f864` — pulled FROM the local Harbor, **no ImagePullBackOff** (hw138 Defect-A fixed; the image landed in step-03). | ✅ | `13-step07-catalyst-api-local-harbor-image.txt` |
| Step-08 egress-block-test — HelmRepository pivot assert | `external HelmRepositories still pointing at ghcr.io/openova-io … 0` → `HelmRepository pivot converged`. **The exact #3526 assertion that FAILED on hw136 + hw138 now PASSES** (because step-05's secretRef lets Flux pull local). | ✅ | `14-step08-…log` |
| Step-08 egress-block-test — **the deny-egress CCNP hold (5TH CATCH)** | ⚠️ The step's hardened pod has `readOnlyRootFilesystem:true` and only mounts an emptyDir at `/work`, but the chart writes the policy to **`/tmp/egress-deny.yaml`** → `can't create /tmp/egress-deny.yaml: Read-only file system` → `kubectl apply` fails → **`cutover-egress-block` CiliumClusterwideNetworkPolicy is NEVER applied** → enforce mode silently degrades to "assertion-only fallback". The 600 s survival window then ran and **PASSED** (`new-regression-cycles=0` → `sovereignty proof PASSED`), so step-08's `result=success` — **but the real deny-egress NetworkPolicy hold (the headline sovereignty proof) did NOT execute** (CCNP absent throughout, verified live). So step-08 "passes" on the assertion-only path. Fix: chart should write to the writable `/work/egress-deny.yaml`, not `/tmp/`. | ⚠️ | `15-step08-window-PASSED-assertion-only.log`, `14-…` |
| Step-09 gitea-token-mint | `result=success` — admin API token minted against local Gitea. | ✅ | `17-FINAL-cutover-status-halt-step10.json` |
| Step-10 vcluster-registry-pivot — **(6TH CATCH, the halt)** | ❌ **Job `BackoffLimitExceeded` (genuine, not transient) → engine halts.** The step DID pivot the vcluster image fields (live HR `image.repository=harbor.hw139.omani.works/proxy-ghcr/loft-sh/vcluster`, so it SKIPs them: `live-K8s ok=0 skip=4 fail=0`) — **but its own `RESIDUAL-TETHER` self-check greps the whole HR `spec.values` for `harbor.openova.io` and finds it in a field the pivot never rewrites: `"registryMirror": "harbor.openova.io"`** (present in all 4 flagged HRs: bp-mgmt/rtz/dmz-vcluster + bp-sso-bridge) → `FATAL: 4 HelmRelease(s) still tethered to mothership Harbor after pivot — Pillar 5 independence not met`. Real defect: the pivot rewrites `image.{repository,registry}` but misses `registryMirror`, so its own assertion correctly fails it. | ❌ | `16-step10-6th-catch-residual-harbor-tether.log`, `17-FINAL-cutover-status-halt-step10.json` |
| Step-11 crossplane-provider-pivot | ⛔ never reached — engine halted at step-10. | ⛔ | `17-FINAL-cutover-status-halt-step10.json` |
| `cutoverComplete` | **false** — engine halts at step-10 (`failedStep=vcluster-registry-pivot`, `currentStepIndex=9`, `progressPercent=81`, `registriesYamlActive=v1`). **9/11 steps succeeded.** | ❌ | `17-FINAL-cutover-status-halt-step10.json` |
| Safety / cnpg-pair caveat | The cnpg-pair DR-demo split-brain is **NOT involved in any step**. Step-08's baseline-NotReady set (excluded from the regression check) = `flux-system/sme-tenants` + `flux-system/sovereign-tls` only — cnpg-pair is NOT in it, and it read `3/3 healthy` throughout the 600 s window (`new-regression-cycles=0`). The step-10 halt is a pure values-rewrite defect (`registryMirror`), orthogonal to cnpg-pair. The 3 NotReady HRs at resume (`bp-cluster-autoscaler-hcloud`, `bp-hcloud-ccm`, `bp-velero`) are Huawei cloud-provider-mismatch noise. | ✅ | `01-cnpg-state-at-resume.txt`, `01-baseline-notready-hr-set.txt` |

**Verdict (hw139):** PR #3558 is **validated** — the engine (a) **resumes** an interrupted cutover on catalyst-api restart, (b) **re-runs** a transiently-failed (DeadlineExceeded) step, (c) mints step-03 with the real **5400 s** per-step deadline (not the inert 900 s floor), and (d) **fans out the copies (parallelism=6)** — taking step-03 from a hard 6/29 wedge through a `backoffLimit` self-heal to **29/29 (zero-touch)**. The engine then **executed steps 04–09 for the first time on any env** — registry-pivot, the Flux GitRepository pivot **with the #3536 secretRef**, the **61/61 HelmRepository pivot to local Harbor**, the catalyst-api roll from local Harbor, the step-08 HelmRepository-pivot assertion **that blocked hw136/hw138 (now PASSES)**, the 600 s survival window (PASSED, `new-regression-cycles=0`), and gitea-token-mint. It reached **step 10 of 11** before halting. **`cutoverComplete=false` (9/11, pct=81).** Two new catches: **(5th)** step-08's hardened pod's `readOnlyRootFilesystem` blocks `/tmp/egress-deny.yaml`, so the `cutover-egress-block` CCNP is never applied — the survival window passes assertion-only, so `cutoverComplete` would NOT represent a witnessed egress-block hold even if it were reached; **(6th, the halt)** step-10 vcluster-registry-pivot fails its own `RESIDUAL-TETHER` self-check because the pivot rewrites the vcluster image fields but misses `registryMirror: harbor.openova.io` (present in all 4 vcluster/sso-bridge HRs). Fix directions (not implemented mid-walk per zero-touch): (5th) write the egress policy to the writable `/work/` mount; (6th) extend step-10's pivot to rewrite `registryMirror` (and any other `harbor.openova.io` value path) to the local mirror, not just `image.{repository,registry}`; (4th, already self-healed) persist/skip the skopeo binary across retries or source the residual image from the node containerd cache.

## 2026-06-14 — hw136 (`3a2ee904b1d0366a`, kom4dc 2-VPC) — cutover FIRED for the first time on a real Sovereign; halted at the egress-block fail-safe (NO half-pivot)

This is the first execution of the sovereignty cutover on a handed-over Sovereign. PR #3521 (catalyst-api `9de0b65`, in chart `bp-catalyst-platform` 1.4.629) fixed the root cause that previously made it **impossible** — the handover `tofu-archive` receiver was behind `RequireSession`, so on a real Sovereign the mother→child archive POST was 401'd before the handler and the cutover never auto-fired. #3519 flipped `egressTest.enforceCIDRBlock` default→true (bp-self-sovereign-cutover 0.1.57).

| Step | What was checked | Status | Evidence |
|---|---|---|---|
| Handover receiver reachable (#3521) | `POST api.hw136.omani.works/api/v1/handover/tofu-archive` (cookieless) → **400 "deploymentId and sovereignFqdn are required"** — handler reached (content validation). Before #3521 this was **401 "unauthenticated"** from the middleware, before the handler. | ✅ | `00-handover-receiver-reachable-400.json` |
| Handover finalise fires the cutover (#3521) | `POST console.openova.io/sovereign/api/v1/handover/finalise/3a2ee904b1d0366a` (RS256 owner JWT) → 200 with **`"tofuArchiveSubmitted": true`**. Before #3521: `false` + `errors:["submit tofu archive: status 401: unauthenticated"]`. Archive sealed → cutover auto-fired (`cutoverStartedAt 2026-06-14T10:05:13Z`). | ✅ | `01-handover-finalise-response.json` |
| Zero-click SSO into the console | `console.hw136.omani.works` → lands on `/dashboard` signed in as **emrah.baysal@openova.io** (`role: sovereign-admin`, roles `[catalyst-owner, catalyst-admin, sovereign-admins]`), no login form. Live treemap renders the primary region (`hw-me-east-215-a-rtz-prod`). | ✅ | `dashboard-zero-click-sso.png` |
| Cutover engine — steps 1–8 of 11 | gitea-mirror, harbor-projects, harbor-prewarm, registry-pivot, flux-gitrepository-patch (GitRepository `openova` → `http://gitea-http.gitea.svc.cluster.local:3000/openova/openova`), helmrepository-patches (kubectl-patched 39 live HelmRepos → local + git-pushed 60 rewritten files to local Gitea), catalyst-api-env-patch (rolled catalyst-api twice from the **local Harbor**, pivoted console/api URLs to local) — all **success**. | ✅ | `02-cutover-events-stream.txt`, `03-cutover-status-FAILED.json` |
| Cutover engine — egress-block-test (the deny-egress hold) | **FAILED at 10:17:08 (~74s, NOT the 600s survival window).** Its pre-flight URL assertion found **59 HelmRepositories reverted to `oci://ghcr.io/openova-io`** → `FAIL — at least one HelmRepository did not pivot`. The `cutover-egress-block` CiliumClusterwideNetworkPolicy was therefore **never applied** and the 600s hold never ran. (Root cause below.) | ❌ | `job-logs-raw.json` |
| `cutoverComplete` | **false.** Engine halted at `egress-block-test`; the post-egress steps (crossplane-provider-pivot, gitea-token-mint, vcluster-registry-pivot) never ran. | ❌ | `03-cutover-status-FAILED.json` |
| Safety — NOT half-pivoted | `state=tethered`, `registriesYamlActive=v1`, and console/api/registry/gitea/bao all 200/303 throughout and after the halt. The fail-safe assertion refused to enter the deny-egress hold while tethers remained, so hw136 stayed fully intact. | ✅ | `03-cutover-status-FAILED.json` |

**Root cause of the egress-block-test failure (a real cutover ordering defect, distinct from #3521):**
The `helmrepository-patches` step (succeeded 10:13:02) kubectl-patched the 39 live HelmRepositories to local AND git-pushed 60 rewritten files (ghcr→local) to local Gitea — but its own log notes "commit will reconcile via bootstrap-kit Kustomization", i.e. the durable Git-source rewrite is **asynchronous**. `egress-block-test` ran ~3 minutes later (10:15:54) and asserted **once**, with no wait/retry — by then a Flux reconcile had re-applied the OLD `oci://ghcr.io/openova-io` HelmRepository manifests (reverting the kubectl patches) and the bootstrap-kit Kustomization had not yet pulled the new local-pivot commit. So the assertion fails on the transient Flux-revert window. This is the documented kubectl-patch-then-Flux-reverts race (`feedback_cutover_bootstrap_halfpivot`). Fix direction: egress-block-test (08) should force-reconcile the bootstrap-kit Kustomization + GitRepository and **poll the HelmRepository pivot to convergence with a bounded timeout** before failing; or helmrepository-patches (06) should block on that convergence before returning success. Tracked in the follow-up issue.

**Verdict (hw136):** #3521 is **validated** — the cutover now fires on a real Sovereign for the first time, and 8/11 steps execute cleanly (including the registry pivot of catalyst-api itself from local Harbor). It then halts at the egress-block fail-safe on a real, separate ordering defect (HelmRepository pivot not yet reconciled when the assertion runs), with **no half-pivot**. `cutoverComplete` and the witnessed 600s deny-egress hold remain pending that fix + a re-run.

## 2026-06-14 (earlier) — hw133 baseline (cutover never run there)

Baseline only — at the time, the cutover had never been run on any handed-over environment. The user-facing surfaces all worked (console signed-in, Apps full catalog, grafana SSO, gitea from local services), but the post-cutover rows were unexecuted and there is no console UI/badge surfacing "this Sovereign is now independent".

| Tested page | Description | Status | Evidence |
|---|---|---|---|
| [console.hw133.omani.works](https://console.hw133.omani.works/) | After cutover, the console still loads and you are signed in, as before. **Baseline:** console loads signed-in as emrah.baysal (sovereign-admin), dashboard treemap renders 97 items. | ✅ | [![](../../sessions/2026-06-14/evidence/3379/r1.png)](../../sessions/2026-06-14/evidence/3379/r1.png) |
| [Apps](https://console.hw133.omani.works/apps) | Navigate Dashboard / Apps / Organizations → all render normally. Apps page renders Deployments (49) / Catalog (63). | ✅ | [![](../../sessions/2026-06-14/evidence/3379/r2.png)](../../sessions/2026-06-14/evidence/3379/r2.png) |
| [grafana.hw133.omani.works](https://grafana.hw133.omani.works/) | App still loads signed-in, unchanged. Grafana Home renders, SSO-authenticated (no login form). | ✅ | [![](../../sessions/2026-06-14/evidence/3379/r3.png)](../../sessions/2026-06-14/evidence/3379/r3.png) |
| [gitea.hw133.omani.works](https://gitea.hw133.omani.works/) | App works (served from the Sovereign's own local services). Gitea dashboard signed-in as `emrah.baysal`; Gitea v1.22.3. | ✅ | [![](../../sessions/2026-06-14/evidence/3379/r4.png)](../../sessions/2026-06-14/evidence/3379/r4.png) |
| [console.hw133.omani.works](https://console.hw133.omani.works/) | ❌ **GAP** — no UI page/badge shows "this Sovereign is now independent / cutover complete". | ❌ | [![](../../sessions/2026-06-14/evidence/3379/r5.png)](../../sessions/2026-06-14/evidence/3379/r5.png) |
| [Apps](https://console.hw133.omani.works/apps) | Install/update an app **after** cutover → pulls from the Sovereign's own local registry. **N/A on hw133** (no cutover run there). | ❌ | [![](../../sessions/2026-06-14/evidence/3379/r6.png)](../../sessions/2026-06-14/evidence/3379/r6.png) |

**Verdict (overall):** sovereignty cutover is an **ops process with almost no end-user UI** — the only positive UI acceptance is the negative one (nothing the user touches breaks), which holds. The operator-side engine now **fires** (#3521) and runs 8/11 steps on a real Sovereign; `cutoverComplete` + the 600s deny-egress hold are pending the egress-block ordering fix.
