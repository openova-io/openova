# UAT — Sovereign acceptance walk (consolidated 2026-06-27 — 22 issues closed live + honest gating map) — live (2026-07-10 — hw235 CONCLUSION prov READY, walk consolidated into rows: 44 verdicts stamped — 38 ☐→✅, 3 ☐→❌, row3 ◑→✅ owner-redeem, G10 ◑→❌ Edit-placement, G12 ⚠️→✅ region-kill) (hw236 CONCLUSION prov READY 2026-07-10 — dep d412876d4c00c8fb; 22 backing-state rows stamped ☐→✅ from 5 read-only kubectl/kcadm walkers; render/funnel/DR-tab/catalog rows held for the browser walk) (RESET 2026-07-15 — hw255 FLEET WALK consolidated: 10 read-only walkers × 277 rows — 39 ☐→✅ backing-state, 12 ❌ honest (MCP-runtime/founder-cred family + console-bundle recon controls #3996 + tenant-string residue), 132 session-walled for the authed browser wave, 72 mutation-gated post-cutover) (RESET 2026-07-16 — superseded by hw261) (hw258 AUTHED CONSOLE WALK 2026-07-16 — dep 4a0390c4eb0380ca, console.hw258.omani.works, region-a 63/63; PIN-minted owner session, browser-driven: **27 rows stamped — 23 ✅ · 3 ⚠️ · 1 ⛔ (incl. row200 ❌→✅ flip)** across dashboard/cloud/jobs/orgs/showback/catalog/SSO. Headline: jobs Re-run/Retry control finally exercised on GENUINE Failed rows (174/175 ✅ — first-ever, cutover #5114 failed here); cloud Networking lens empty-data regression #3987/#3998 ABSENT (hw271 FRESH-PROV 2026-07-18 (RESET 2026-07-21 — pending hw282)

> **hw269 AUTHED CONSOLE + FUNNEL + MCP WALK — 2026-07-18 (dep `a47b82b25fbd5baf`, hw269.omani.works, 2-region me-east-215-a/b, cutover mid-flight at step-03 harbor-prewarm/cutoverComplete=false — driven separately, NOT claimed here).** Owner session minted via the real passwordless-PIN flow, browser-driven this time end-to-end (POST /auth/pin/issue triggered from the actual login form → IMAP PIN over mothership Stalwart → PIN typed into the actual verify form → RS256 catalyst_session cookie set by the real browser flow; whoami→200 tier=owner). 19 screenshots `docs/sessions/2026-07-18/hw269-authed-console/`. No cluster mutation beyond the funnel Org provisioning itself (the walk); NO NodePort (row 240 already ✅ pre-stamped, re-confirmed 0 nodePorts both regions); bastion untouched. **Dashboard** (167 ✅) owner treemap 193 items + identity chip, no login form. **Cloud** graph live 2-region (Cloud 1/1·Region 2/2·Cluster 2/2·NodePool 4/4·WorkerNode 12/12·LoadBalancer 2/2) → rows 65/66/199-204 ✅ (Gateway Programmed=False is expected §854 DIRECT-serving cosmetic, not a fault; HTTPRoutes/NetworkPolicies/CiliumNetworkPolicies live counts 17/30/44 cross-checked against kubectl). **Jobs** (#3646) full typed table 167 rows → 168/169/170/171/172/173/175 ✅ (Status=failed filter honestly empty-state, 0 genuine Failed rows this env — cutover healthy); 174 ⚠️ (no Failed row to exercise Re-run, same as hw268). **Catalog** (#3668) 124/125 ✅ (Alloy card + detail hero/About/Instance). **SSO** (#3374) 30/31/32/34/37 ✅ (grafana /api/user 200 isGrafanaAdmin=true; gitea "emrah.baysal - Dashboard"; harbor /harbor/projects; **Keycloak sovereign-realm admin console landed with NO master-realm login** — a first-time clean flip vs prior envs' session-walled ⛔; newapi "Login successful!" account sovereign_2). **Orgs/Showback** (#3383/#3687) 116/117/118/25 ✅, **21/22/23 ✅ — row 23 FLIPPED ⛔→✅**: the funnel-provisioned customer Org `hw269uatwalk` produced the first-ever 2nd-Org showback row this cycle (353.25 units, mysql+wordpress apps, distinct from `__platform__` and `sovereign (parent)`). **Funnel E2E (#5104/#5123, marquee pair)**: owner BSS mint voucher HW269UATWALK (20 OMR) → stranger redeem (brand-clean, no openova.io leak) → plans/apps/addons/bcp/review/checkout (rows 72/75/77-85 ✅, row 83 ⚠️ slug/TLD shown one step later than "review", row 86 ⚠️ generic spinner not a labelled step-timeline) → PIN-less emailed-code sign-in (no password) → credit covers 100% (0 OMR due) → Org `hw269uatwalk` provisioned (customer/plan=s/real billing) → per-Org console landed zero-click (rows 87/88 ✅) → **row 89 FLIPPED ❌→✅**: Applications view shows WordPress card INSTALLED, live `GET /api/v1/org/applications` wire-proof (finding: `generatedAt` field is an identical hardcoded literal across hw256/hw269, cosmetic not correctness) → **row 90 ✅**: `kubectl -n hw269uatwalk get deploy,pods` wordpress+mysql both 1/1 (mysql image-pull stalled ~16min on the pre-cutover harbor.openova.io/proxy-dockerhub path, same root cause as harbor-prewarm's own logged #5095 note — resolved on its own, not an architectural fault) → `https://wordpress.hw269uatwalk.omani.rest/` → HTTP 302→wp-admin/install.php→200, live WordPress Installation page rendered. #5123-B confirmed: member session still fires owner-scoped `/api/v1/deployments/{id}` + `/console-ui/sidebar-entries` → 403 on every page load (known cosmetic frontend gap, recorded not fixed). **MCP north-star (#5175/#5167)**: row 211 FLIPPED ⚠️→✅ — real owner RS256 bearer → `tools/list` full 7-tool surface + `list_applications` → 200 real 8-app JSON (spine-gitea/harbor/keycloak/openbao, newapi, shared-pg trio) — the whoami-fallback data round-trip the prior hw266 stamp said "pends the real PIN-login session token" is now proven end-to-end; rows 212/213 stay ⚠️ (Org-scoped/cross-Org RBAC checks need a distinct RS256 Org-member bearer this session could not extract — the httpOnly cookie can't be read client-side and the marketplace HS256 token is correctly rejected by MCP). NO fabrication.

> **hw258 AUTHED CONSOLE WALK — 2026-07-16 (dep `4a0390c4eb0380ca`, hw258.omani.works, 2-region me-east-215-a/b, region-a 63/63 healthy; cutover FAILED #5114, env to be wiped after #5128 — evidence banked now, no cutover-gated surfaces claimed).** Owner session minted via the real passwordless-PIN flow (POST /auth/pin/issue → IMAP PIN over mothership Stalwart → /auth/pin/verify → RS256 catalyst_session injected into Playwright; whoami→200 tier=owner). Browser-driven, no cluster mutation, no NodePort, bastion untouched. 9 screenshots `docs/sessions/2026-07-15/hw258-authed-console/`. **Dashboard** (167 ✅) owner treemap 190 items + identity chip. **Cloud** graph live 2-region (Cloud 1/1·Region 2/2·Cluster 2/2·NodePool 4/4·WorkerNode 8/8·LoadBalancer 2/2, 18 nodes/10 edges) → rows 65/66/199/200/203/204 ✅ (200 ❌→✅ flip: Networking lens empty-data regression #3987/#3998 absent here), 201/202 ⚠️ (NP+CNP folded into one "NetworkPolicy 35/35" chip, no split-by-kind page). **Jobs** (#3646) full typed table 167 rows → 168/169/171/173 ✅; **174/175 ✅ FIRST-EVER exercise of the Re-run/Retry control on genuine Failed rows** (cutover-harbor-prewarm→"Re-run", cutover-step-gitea-token-mint→"Retry reconcile"; all prior envs 0-failed ⛔); 172 ⚠️ (Status=failed filter returned 0 while a churning cutover had just healed the 2 Failed rows — ambiguous filter-scope-vs-live-heal). **Orgs/Showback** (#3383/#3687) 116/117/118/21/22/25 ✅ (directory 2/2 internal Orgs, __platform__ 20475.91-unit overhead roll-up + real per-app attribution), 23 ⛔ (no customer Org sample). **Catalog** (#3668) 124/125 ✅ (94 cards, Alloy detail hero+v1.0.2). **SSO** (#3374) 30/31 ✅ (grafana /api/user 200 isGrafanaAdmin=true; gitea "emrah.baysal - Dashboard" :443, both 0-password-input). NO fabrication.

> **hw236 CONCLUSION PROV — 2026-07-10 (dep `d412876d4c00c8fb`, hw236.omani.works, 2-region me-east-215-a/b, console READY).** Backing-state consolidation from 5 read-only kubectl/kcadm walkers (no browser, no cluster mutation): **22 rows stamped ☐→✅** with a `hw236-2026-07-10:` evidence prefix — cloud per-kind **199-203** (Gateways 2 · HTTPRoutes 15 · NetworkPolicies 30 · CiliumNetworkPolicies 42-all-VALID · LoadBalancer clustermesh-apiserver 212.72.24.31; stale expected-counts refreshed to live), jobs data-model **171** (`openova_flow` store populated 47 nodes/4097 events, typed Kinds from real HR/Job/CronJob observations, zero synthetic), CRD tier-enum **215** (`[org,corporate]`), Keycloak-realm SSO backing **41-44** (owner→/sovereign-admins→catalyst-admin), robustness sso-bridge/plane-isolation/postgres/gitea/sandbox **R3-R9/R11/R19/R22**, and region-b steady-state **R13** — **the recurring region-b Degraded / #3380 host-path black-hole (#4656/#4942) is RESOLVED on hw236** (cilium-health 11/11 reachable both regions, ClusterMesh 1/1 :12379 both directions, cross-region pod→primary:5432 datapath OPEN, cnpg-pair replica SYNC/remote_apply RPO=0). Cutover DORMANT + handover SEALED → region-kill/cutover GO. Landing-render SSO (36-40/45), funnel (72-95), topology-DR tab (51-64), catalog (124-158), showback (21-25), keycloak-admin (34), Contexts tab (4/14/24 — data-layer real but the Application-CR `dependsOn` binding is empty so the tab may render empty), and MCP rows held for the browser walk (render-owned) or genuinely gated. Reflector row 230 backing (ghcr-pull reflected into per-Org `acme236` ns) is proven but its full PASS awaits a per-Org app pod pulling a private image → held ⚠️. NO fabrication, NO NodePort, bastion untouched.

> **hw235 CONCLUSION PROV — 2026-07-10 (dep `a3818d29c833ab1e`, hw235.omantel.biz, console→200 READY, 2-region me-east-215-a/b, clean prov on the COMPLETE 12-fix train).** The definitive fresh-prov validation of this session's deploy-lagged fixes — everything baked in, zero live-patching. Live evidence re-queried directly. NO fabrication, NO NodePort, bastion untouched.
> - 🎯 ✅ **region-b Degraded ELIMINATED (#4943 CONFIRMED)** — the recurring multi-week issue is FIXED. cilium cluster-health region-b **12/12 reachable** (was 6/12 pre-#4943); the #4942 host→remote-pod black-hole is CLEARED (region-b CP-node HOST netns → region-A keycloak pod TCP-OK — the exact native-routing-CP-node SG gap #4943's `mesh_ingress_cidrs` fan-out fixes). ClusterMesh 1/1 both regions. The cnpg:5432 host-netns denial is a CORRECT #4869 CNP restriction (pod-identity only), not a black-hole. Cosmetic residue only: region-A 11/12 = a `:4240` health-responder RST on the region-b CP node (TCP RST, not a datapath drop) — minor follow-up.
> - ✅ **Convergence FULL** — region-a 62/65 HR (3 non-Ready = by-design hcloud no-ops), region-b 58/65 (3 hcloud + 4 by-design suspend on the DR-secondary). 12/12 nodes.
> - ✅ **Pillar-3 DR (#3374/#3375)** — 5 continuum CRs Healthy (primary me-east-215-a), cnpg primary 3/3, region-b sync replica **lag 0 → RPO=0s**, cluster healthy → RTO≈0s. Cross-region POD datapath (#4656) region-b→region-a:5432 OK, streaming.
> - ✅ **useraccess grants (#4841/#4935) — bind-verb fix WORKED** — UserAccess CRs Phase=Active (none Failed), owner grant materialized a real `useraccess-owner-…-tier → openova:tier-owner` ClusterRoleBinding (the RBAC escalation-prevention `bind` fix validated live on a fresh prov).
> - ✅ **Showback #3687** real (`/api/v1/org/consumption` = 22685 units, per-app breakdown). **Cutover #3379 dormant** (11 step-CMs present, 0 executed, Tethered — ready for Wave F).
> - Process: this hw235 conclusion prov is the corrected re-fire after a `parentDomains.primary` create.json input-error (root-caused + fixed + memorized); the DNS fix is end-to-end proven (console→200 at the correct Huawei EIP). Conclusion browser walk (owner session, 2026-07-10 — CORRECTED, honest): ✅ **Funnel E2E** (Org acme235 created), ✅ **#4928 PIN**, ✅ **#4925 per-Org console** (console.acme235→200), ✅ **#4937 customer self-install** (201), ✅ **#4938 owner-redeem-redirect**, ✅ **#4935 useraccess grants (admin)**; SSO 5/6, Catalog/Jobs/Treemap(WorkerNode 12/12)/Orgs-showback/Cutover-Tethered all ✅. **❌ 3 GENUINE FAILURES FOUND (filed):** ❌ **G10 #4840** placement Edit→Apply STILL HTTP 400 'placement.mode required' — the fold IS in the deployed image (2d41a36⊃e937cda9d) but doesn't populate mode on the PUT (real fold gap → **#4950**); ❌ **per-Org WordPress #4936/#4939 does NOT serve externally** — pod runs 1/1 but the `oidc-config` post-install hook Job hangs 13m → HR post-install timeout → no HTTPRoute → Envoy 404 (→ **#4951**); ❌ **bp-openclaw@0.2.16** install fails `namespaces "org-example" not found` (hardcoded placeholder → **#4952**). Console reads Degraded (per-Org app HR stalls, NOT the #4943 datapath which is GREEN). NOTE: my earlier "WordPress PASS" (pod 1/1) was WRONG — pod-running ≠ app-serving; the walk corrected it. Cutover machinery all 5 tickets verified sound (#4660/#4666/#4632/#4692/#4652). **WAVE-F FINALE (agent a0826100):** ✅ **Region-kill #4275 PASS** — killed region-a primary, region-b promoted **RTO=1.3s** (≤30s target), **RPO=0** (1000 rows + post-kill write), split-brain-guarded restore + re-gate green (Pillar-3 fresh-prov failover proof). ⚠️ **Cutover #3379 wedged at step-03** harbor-prewarm — `skopeo copy` FATALs on a `tag@digest` ref (first openova-io image pinned both ways); fail-closed abort (env NOT degraded, pre-registry-pivot). Fixed durably: **#4955/PR #4957** `normalize_ref()` chart 0.1.108→0.1.109; cutoverComplete=true roll-gated to next prov. Conclusion-walk defects all fixed: G10 #4950→PR#4958, WordPress #4951→#4954✅, OpenClaw #4952→#4953✅.

> **hw234 KEYSTONE PROV — 2026-07-10 (dep `d71e9c7e8d0a15da`, hw234.omantel.biz, 2-region me-east-215-a/b, converged zero-touch on the audited 10-fix train).** Live evidence re-queried directly (both region kubeconfigs pulled from the mothership `catalyst-api-deployments` PVC + curl) — NO fabricated passes, NO NodePort, bastion untouched. Walkers: convergence-sweep `a4f290064e3e6d049`, MCP `a56c402400637cbb9`, funnel Wave-D `aaf419937f4d69710` (in flight), + orchestrator row-3 probe.
> - ✅ **Convergence FULL both regions** — region-a 64/68 HR True, region-b 58/65 True; every non-True is a by-design hcloud no-op (`bp-cluster-autoscaler-hcloud`/`bp-hcloud-ccm`/`bp-velero` — Huawei variants `-hcs`/`bp-huawei-evs-csi` active) or region-a-only control-plane (bp-catalyst-platform/continuum/sandbox/cutover). **12/12 nodes Ready, ZERO unhealthy pods.**
> - ✅ **Region-b #4918 validation PASS** — `keycloak-0` Running **1/1 stable** (NOT 401-wedged; the shared-keycloak-DB fix held on this prov); full region-b app-chain Running (grafana 3/3, gitea, guacamole, powerdns); region-b CNPG replicas 3/3.
> - ✅ **Pillar-3 topology #3375 DATA** — CNPG pair primary 3/3 (region-a) + replica 3/3 (region-b), continuum PHASE=Healthy **RPO=0s**; shared-pg/-b/-c + spine-gitea/harbor/keycloak all active-hot-standby with region-b replicas. Topology-*tab* UI render = remaining row-60 leg (browser walk).
> - ◑ **Placement #3969 backend reconciles LIVE** — `.spec.placement` first-class on the Application CRD (targets[]/regions model), `continuum-controller` 1/1, every Application populated (`singleton`/`active-hot-standby`/`active-passive`). Remaining G10 bar = console Edit-placement Apply→200 re-walk on this post-#4840 image (browser).
> - ✅ **Funnel E2E COMPLETES + all 3 hw233 blockers GREEN** (Wave-D authed funnel walk, customer Org `acme`/acme.omani.homes/Plan M, voucher `VCH-8TW6B3AWJRSS`): redeem→plans→apps→bcp→review→checkout→launch. **#4928/#4924 PIN funnel ✅** (code→emrah.baysal+acme, IMAP PIN 607201→signed in — the EXACT hw233 wedge point, now completes); **#4925 per-Org gateway ✅** (console.acme.omani.homes→EIP 212.72.24.12 HTTP 200 via envoy, valid LE cert CN=acme.omani.homes, HTTPRoute Accepted); **#4929 per-Org CNPG ✅** (two healthy per-Org Postgres in ns acme). Distinct customer Org proven: acme kind=customer/billing=real/isolation=vcluster/Active, showback 525.5 units real-metered; vcluster StatefulSet Ready (own KC/kube-system). 11 screenshots `hw234-waveD/`.
> - ❌ **Row-3 authed-owner redirect NOT implemented** (#3687/#4546) — unauthed SSR redeem HEALTHY (200/0.05s, real form; hw228 hang→000 GONE), BUT Wave-D proved with a live authed-OWNER session (marketplace PIN msg968) that `/redeem/?code=` shows the redeem FORM and **never redirects to /dashboard** (also FAILs for the customer session). Marketplace auth is client-side → no server-side owner redirect exists. The redirect leg is unimplemented. FAIL.
> - ❌ **Rows 87-90 per-Org WordPress not live** — TWO `bp-wordpress-tenant@0.4.20` chart defects distinct from #4929: (1) configSchema *requires* `orgDomain`+`adminUser.email` but neither the console "New instance" dialog NOR the funnel collects them → Application fails admission `missing properties: 'orgDomain','adminUser'`; (2) DB-secret reflector RACE — chart creates an empty `wordpress-database-secret` (reflector-mirrored from `wordpress-db-app`) populated ~6min AFTER Helm's 5m `--wait` → HR `blog-rtz-a` `InstallFailed: context deadline exceeded` → external HTTPRoute never created (`wordpress.acme.omani.homes`→404). Also: customer self-service app install → `getApplicationInstances` 403 (marketplace HS256 member session not authorized for app mgmt; installs need the sovereign-admin owner console).
> - ⛔ **MCP RBAC #3988 rows 211-213/221-223 = BY-DESIGN gated (NOT a prov gap)** — investigation (agent a97f2d65) confirms: the **Pillar-4 Sandbox substrate IS deployed + healthy** (`bp-sandbox` HR Ready chart 0.3.12, `sandbox-controller` 1/1, CRD installed, lockstep intact). Zero Sandbox CRs is EXPECTED — Sandboxes are on-demand, minted only when the marketplace cart contains the "sandbox" product (acme bought WordPress). `openova-sandbox-mcp` is a **bundled stdio binary** in the pty-server image by design (the standalone MCP Deployment was removed in chart 0.3.4 — a pod has no stdin), so "no MCP pod/Service/HTTPRoute" is correct even with a Sandbox. Rows 211-213/221-223 target a DIFFERENT MCP (`openova-mcp`/`bp-agenity`, per-Org installable Blueprint) gated on **founder Anthropic cred (#4277)** + a per-Org `bp-agenity` install + #4624 — none is a prov deployment gap. The catalyst-api RBAC layer the MCP facades IS verified live+correct (sovereign token → 200/10-apps; unauth → 401; org seam → 400). Walk path to instantiate: funnel with the sandbox product OR apply a `Sandbox` CR in `catalyst-system` (controller's watched ns) with `spec.owner.orgRef.slug`.
> - ✅ **10th defect #4841 fixed+merged** — useraccess-controller granted `bind` on the 8 canonical IAM ClusterRoles (RBAC escalation-prevention, `f7fe1c3`/#4935) + CD-rolled into the deploy pins (`deploy: bump useraccess-controller image to f7fe1c3`). Validates live on the next fresh prov (hw234 was provisioned pre-#4935).
> - ✅ **Infra sweep (kubectl+curl, agent a19a341b) — cutover-dormant/spine/DR/showback/datapath all PASS:** cutover #3379 dormant (chart 0.1.108 Ready, cutoverComplete=false, 11 steps un-started, Tethered, dragonflyUpstream=ghcr); spine #3374/#3375 Ready (gitea 1.2.47/harbor 1.2.41/keycloak 1.5.5/openbao 1.2.60), DR continuum CRs all Healthy lease-held, cnpg-pair RTO=0s/RPO=0s, shared-pg replica streaming lag 0.52s; showback #3687 real (`/api/v1/org/consumption` acme=1235.75 units, per-app attribution); **#4656 cross-region datapath VERIFIED WORKING no-deploy-lag** (ClusterMesh 1/1 both regions, no stale :2379, `vpc-podcidr-route-reconciler` 'routes in sync', region-B pod→region-A pod TCP:5432 + CNPG streaming proven).
> - ⚠️ **Robustness #3380 = HONEST degraded (no false-green), root-caused → #4942:** cilium-health reads region-b Degraded because **host-network→remote-pod is black-holed** on Huawei VPC (host-sourced traffic not delivered to remote pod CIDRs) — a false-RED at the health-probe layer that OVERSTATES impact; the app pod datapath the DoD needs (pod-to-pod, RPO=0) is healthy. This is the recurring "region-b degraded" root cause; #4942 investigates a durable host-route fix so a fresh prov reads green. NOT a regression, NOT false-green.
> - ✅ **Authed console walk (browser, agent a7a650fd) — every walkable UI surface PASS:** SSO 6/6 silent-SSO (grafana/gitea/openbao/guacamole/keycloak-admin/harbor land authed, no login prompt; powerdns/newapi/hubble N/A = no external HTTPRoute by design); Catalog #3668 (94 blueprints, bp-wordpress detail, New-instance + Edit-IaC config-schema dialogs render); Jobs #3646 (Kind col task/cron/step/lifecycle, Provision-Huawei DAG all Succeeded, JobDetail+Logs; Retry not exercisable = zero Failed jobs on a healthy env); Treemap #4731/#3969 (Region 2/2, WorkerNode 12/12, Layer1=Progress/Layer2=Kind, Region-lens per-region placement); Orgs #3378/#3687 (3/3, acme customer real showback 1235.75, console.acme.omani.homes serves own PIN login + valid TLS); **Users #3374 = 3 UserAccess grants render "(admin)" NOT Failed → #4841 fix is NOT deploy-lag-blocked on this env**; Cutover #3379 Settings→Sovereignty "Tethered". Cosmetic-only: static "Hetzner provider token" label on Huawei Settings→Cloud (display-only), transient `/app/bp-X` 404s (UI falls back correctly). 20 screenshots `hw234-walk2/`.
> - **Net hw234 disposition:** every LIVE-testable surface is GREEN. Remaining non-green = deploy-lag validated on the hw235 conclusion prov (G10 console Edit-placement #4840, row-3 redirect #4938) · by-design/founder-gated (MCP #4277, Sandbox on-demand) · honest host-path false-RED (#3380→#4942) · Wave-F destructive (region-kill #4275, cutover-execute #3379). NO architectural faults; NO false-green.
> - **Process note:** this session's hw233/hw234 walk commits were initially stranded on a 610-commit-stale branch; reconciled onto real main 2026-07-10 (no clobber of the ✅155 base).

> **hw224 VERIFICATION PROV — 2026-07-04 (dep `964e9ca501951f54`, hw224.omani.works, 2-region me-east-215-a/b, LE-prod cert).** Fresh prov on the train carrying the two deepest fixes from the hw223 walk: **#4756** (provisioning-token outage) + **#4758** (vcluster-LimitRange brick). Fixes confirmed DEPLOYED + partially verified live by the orchestrator; the full authenticated funnel walk (customer WordPress serving in-vcluster) is in flight (walker `a2a68a8ce9ec0b96b`).
> - ✅ **#4756 provisioning-outage fix — VERIFIED LIVE**: `org-services/provisioning` init went `1/1 Running` at **5m52s with 0 restarts** (vs the old ~30-min self-heal outage that recovered only at the 1800s init-timeout restart). The init now reads GITHUB_TOKEN from the kubelet-synced FILE each poll (`GITHUB_TOKEN_FILE` env + `provisioning-github-token` volume both present on the running pod), so the late-minted gitea token is picked up in ~60s.
> - ✅ **#4758 vcluster-LimitRange fix — CONFIRMED WORKING** (authenticated funnel walk, customer Org `uatwp4758`, plan m, isolation=vcluster): `plan-limits` LimitRange has **NO `maxLimitRequestRatio`** (only default/defaultRequest), and the vcluster's synced `coredns-…-x-kube-system-x-vcluster` pod — the exact pod the old ratio=1 rejected — is now **admitted + scheduled + Running** in the host ns. The admission-rejection #4758 targeted is resolved. (org-controller `organization-controller:99cab49`.)
> - ❌ **WordPress last-mile still 404** — but by TWO NEW defects distinct from #4758: (1) **PRIMARY [#4656]** the customer vcluster's apiserver is unreachable cross-node (`dial tcp 10.96.80.71:443: i/o timeout`) — Cilium VXLAN-over-WireGuard MTU stacking (measured live: cilium_host 1370 + VXLAN 50 = 1420 > cilium_wg0 1290 → DF-drop); coredns stays 0/1, WordPress never applies into the vcluster. (2) **secondary** `core/services/provisioning/gitops/gitops.go:800-803` hardcodes `sourceRef: flux-system` which doesn't exist on a Sovereign → the generated `tenant-<slug>-apps` Kustomization is FALSE (`GitRepository "flux-system" not found`) — a broken duplicate of the org-controller's `catalyst-tenant-<slug>-apps`.
> - ✅ **Pillars 2/3, funnel E2E, PIN login** re-confirmed (BCP active-hot-standby picker; shared-pg/-b/-c + org-pg healthy; full funnel plans→checkout→launch with voucher `VCH-QFP52J4S2KEX`; `/auth/pin/verify`→200). Env converged (console 200, region-a 63/63 HRs; region-b `secondaryDegraded=true` — honest).
> - ✅ **COMPREHENSIVE authenticated UAT walk — 26 ✅ · 0 ❌ · 3 ⛔ · 4 ⚠️, NO new bugs, NO fabricated-green** (walker `a453392c2a1f7a514`, 12 screenshots `.playwright-mcp/uat-hw224-01..12-*.png`). Confirmed live at HTTP/kubectl/wire layer: **SSO** (per-app launch-url→real OIDC; gitea 307→`realms/sovereign`, grafana 302 w/ `kc_idp_hint=catalyst-pin` silent-SSO); **Topology/DR** (per-app placement from runtime, active-active, Region 2/2, 24/24 workers); **Jobs lens #3646** (Kind col task/cron/step/lifecycle + Re-run on a *Failed* mimir-buckets job — genuinely real, not all-green); **Catalog** (81 blueprints, bp-wordpress detail); **Orgs** (directory lists customer `uatwp4758`, per-Org detail, **showback 525.5 units real-metered ≠ 0**, `console.uatwp4758.omani.homes`→200 w/ own LE cert); **Treemap** (206-node live util; **#4731 Progress/Kind layers VERIFIED** — L1 progress buckets Reconcilers/Cutover/Provision-Huawei, L2 Kind Task/Cron/Step/Lifecycle, status-color legend, job-tree source `/jobs`→200, leaf→JobDetail+logs, no regression to org/utilization default; `.playwright-mcp/4731-hw224-0[1-4]`); **Robustness #3380** (Degraded badge honestly propagated everywhere — no false-green; root = `clustermesh-apiserver` Init:0/1 both regions, #4656 family); **Cutover** (Tethered, 11 steps Pending, cutoverComplete=false); **Users** (3 real UserAccess CRs); **Agenity** (6-agent picker). 3 ⛔ = pre-existing non-product gates (headless-KC silent-SSO landing, Agenity live-run Anthropic cred #4277, uatwp4758 vcluster 0/0 #4656). 4 ⚠️ minor (HR-only DR-switchover panel absent; catalog IaC write-gated on live env; vcluster-showback in graph; ~~"Sandbox"→"Agenity" nav leak~~ **FIXED #4762**).
> - ✅ **FULL-TABLE walk — all 243 canonical rows populated on hw224: `101 ✅ · 11 ❌ · 26 ⛔ · 105 ⚠️ · 0 ☐`** (from 0✅/122☐ at reset; every row a live hw224 verdict, **0 fabricated passes**, 17 screenshots `uat-hw224-fulltable-*`, commits 15c5332d3/9b2d3898f/73a65b568). Silent-SSO brokered from the console session unlocked app-landings session-walled ⛔ on prior envs. **Flipped ❌/⛔→✅ this env**: Harbor SSO wired (was ❌412 db_auth), Grafana 3/3 + silent-SSO (was ❌503 CrashLoop), all app SSO landings live (gitea/openbao/guacamole/pdns/newapi/hubble/KC-admin), customer Org uatwp4758 real (525.5-unit showback). **11 ❌ = all previously-filed knowns, NO new bugs**: #4656 (WordPress in-vcluster, 7 rows), F8 topology placement-from-pod-only, row-23 directory-showback, #4620 crossplane adoption. **105 ⚠️** = region-b Degraded (clustermesh-apiserver Init:0/1, #4656 family) + write-mutation rows (durable-commit proofs stand). **26 ⛔** = honest walls (headless-KC session, Anthropic-cred Agenity chat #4277, destructive cutover, write-gated funnel). Path to higher ✅: fix #4656 (unblocks WordPress + region-b) + a throwaway write-mutation walk.
> **Provenance:** `hw224-verify-2026-07-04` per surface (walker `a2a68a8ce9ec0b96b`, 6 screenshots `.playwright-mcp/4758-0[1-6]-*.png`). Net: the two deepest fixes shipped this session (#4756, #4758) are VERIFIED; the WordPress headline is now gated on #4656 (MTU) + the gitops.go sourceRef bug — both filed, both distinct from the verified fixes. hw223 (below) is prior-env history — wiped 2026-07-04 (STALE by construction; each new env flushes evidence).

> **hw223 AUTHENTICATED PILLAR WALK — 2026-07-04 (dep `9224ebd66e0602b4`, hw223.omani.works, real LE-prod cert, FIRST reliably-converged prov after the walk-blocker fix-trio #4748+#4746+#4752).** The whole prior round was auth-gated by broken operator login; hw223 cleared it. **~13 ✅ · 0 ❌ · 0 ⛔ · 3 ⚠️**, + 1 real bug (#4756) + 2 findings. All verdicts reproduced live at the HTTP/kubectl/CR layer — no fabricated passes (12 screenshots under `.playwright-mcp/uat-hw223-*.png`).
> - ✅ **Operator PIN-login end-to-end** (#4748, the prior blocker): `pin/issue`→200 `dispatched` → mailbox PIN → `pin/verify`→200 + RS256 `catalyst_session` cookie (roles catalyst-admin/owner) → authed console renders. (hw221 was 502; fix validated live.)
> - ✅ **Pillar 1 — customer-Org onboarding walked E2E** (first customer-KIND Org on any prov this cycle): funnel plans→apps→addons→bcp→review→checkout; operator issues voucher `UATWALK223VOUCHER` (BSS UI); redeem fully covers OMR 9 → Org CR `uatwalk223` (kind=customer, plan=m, billing=real, isolation=vcluster) Ready in ~2 min → **`console.uatwalk223.omani.homes` → HTTP 200** on its own Org host.
> - ✅ **Pillar 2/3** — cloud graph Region 2/2 (me-east-215-a+b both healthy), 24/24 workers, LB 2/2; Continuum DR CRs (gitea/harbor/keycloak) phase Healthy + LeaseHeld; 9 CNPG clusters healthy (shared-pg/-b/-c Guaranteed + DR pair). Signup BCP offers active-hot-standby.
> - ✅ **W-2** per-Org `plan-limits` LimitRange forces Guaranteed QoS + `plan-quota` present; **W-3** per-Org agenity org-host routing proven (Org console on the Org host, not Sovereign FQDN) + `OPENOVA_MCP_TENANT_HOST` fix in source; **W-11** Organizations directory shows the new customer Org.
> - ✅ **Pillar 5** cutover dormant (`bp-self-sovereign-cutover@0.1.103`, cutoverComplete=false, 11 steps un-started; Settings→Sovereignty "Tethered").
> - 🐞 **BUG #4756** — `org-services/provisioning` init reads GITHUB_TOKEN via secretKeyRef env (empty at start), can't see the late-minted gitea token → ~30-min self-heal outage (recovers at 1800s init-timeout restart). Findings: funnel checkout showed `.omani.rest` but provisioning used `omani.homes` (redirect TLD mismatch); a duplicate `tenant-uatwalk223-apps` Kustomization is broken (GitRepository not found); funnel-picked WordPress still converging in-vcluster at cutoff. (⚠️ = per-Org keycloak realm tier-gated + WordPress converging + showback 0-units-metered.)
> **Provenance:** `hw223-walk-2026-07-04` per surface; the three walk-blocker fixes (#4748/#4746/#4752) all validated live on this prov. Prior hw221/hw220 blocks below are prior-env history (STALE by construction — each new env flushes evidence).

> **hw220 FULL-TABLE walk — 2026-07-04 (dep `907629bc3577355b`, hw220.omani.works, 2-region converged, complete #4706+#4732 train live).** ALL 243 canonical rows walked by two parallel read-only walkers (rows 1–120 + 121–239, minted-owner + org-scoped sessions, wire evidence per row): **122 ✅ · 21 ❌ · 37 ⛔ · 63 ⚠️ · 0 ☐**. The 21 ❌ consolidate into the #4739 fault wave (headline: hourly external :443 outage loop from the TTL'd envoy-restart Job — live-mitigated + durable fix PR'd; per-Org LimitRange wedge blocking every funnel app; harbor SSO db_auth never flipped; grafana region-a credential CrashLoop; 1.4.993 invalid env render; adoption ProviderConfig group mismatch; false-green health panel; hardcoded Hetzner name in DR panel; visible APPDETAIL debug marker). 37 ⛔ are honest walls (KC browser-SSO unmintable headless, founder-mailbox PIN, write-gated funnel mutations, no customer-kind Org sample). 63 ⚠️ dominated by catalog write-mutation rows (write-gated on the live env; prior 25aadcfc durable-commit passes stand) + cutover-partial context (step-08 wedged, cutoverComplete=false). Verdict provenance: `hw220-walk-2026-07-04(A|B)` stamps in each row's Evidence column.

> **hw218 walk — 2026-07-03 (dep `25b49b8534ab98db`, hw218.omantel.biz, FIRST-EVER zero-touch 2-region convergence on the merged #4706 train).** Four wire-captured verdicts, no hand-patching anywhere in the env:
> 1. ✅ **No-nodePort gateway serves externally** — `gitea.hw218.omantel.biz → HTTP 303`, `auth.hw218.omantel.biz → HTTP 302` (off-cluster, ELB 212.72.24.33 → node:443 hostPort → hostNetwork envoy); gateway Service `type=ClusterIP`, **`nodePorts=<none>`** (kubectl wire-capture). Console-isolation leg 404 (two Gateways can't both bind node:443) → fixed in #4715 (merged, 1.4.987 roll-gated).
> 2. ✅ **Readiness gate (#4707) enforced** — `status=ready` written only at 00:37:03Z after the external console probe answered (<500); the gate code is proven present in the RUNNING mothership binary by capture 3. Hardening note: 404 satisfies the current <500 bar — tighten to <400 as a follow-up.
> 3. ✅ **2-region admission floor (#4708) live in production** — negative test against the LIVE mothership: `POST /deployments` (1 region, no topology) → **HTTP 400** `"deployment has 1 region(s) and no explicit bcpTopology — the BCP agreement default is multi-region (>=2 regions, Pillar 2)…"`. Nothing created.
> 4. ✅ **Tenant DNS split-horizon correct** — `gitea./auth.hw218 → 212.72.24.33` (primary ELB), `console.hw218 → 212.72.24.12` (console ELB).
> Pre-walk hygiene: hw217 wiped via canonical endpoint with **API-audited zero orphans** (0 ELBs, bastion-only ECS, 1 EIP, 1/5 VPCs). Session train: #4707 #4708 #4709 #4710 #4711 #4714 #4715 all MERGED.


> **Pillar 1 (Marketplace + voucher onboarding) fully live-walked 2026-06-27 — funnel + plan catalog + voucher issuance/redeem E2E.**

> **Pillar 4 (Agenity workspace + auto-mounted bp-openova-mcp) materially live-walked 2026-06-27 — controller + per-Org provision + MCP auto-mount + RBAC Org-scope verified; agentic chat-run gated on Anthropic credential.** *(Terminology per DOD.md §Pillar 4 + GLOSSARY: the user-facing product is **Agenity** / **bp-openova-mcp**; the legacy "Sandbox" concept + menu are dead/removed — founder 2026-06-30. Internal `sandbox-controller`/`Sandbox` CR plumbing Agenity is built on stays internal.)*

> **Authed operator-console walk 2026-06-27 (throwaway-mailbox PIN session) — 10 interactive-UI rows verified; 2 product faults exposed (rows 33, 176).**

> **Authed live-walk sweep 2026-06-27 — ~33 interactive rows verified (SSO/topology-dialog/per-app-health/catalog-IaC/cutover-UI/jobs/orgs); env healthy post bp-catalyst-platform 1.4.915 roll.** (Row 83 funnel /review = fix-merged-pending #4526; row 170 install-openbao = NA, superseded by the #3916 jobs-lens split.)

> **Write-mutation walk on fresh prov 25aadcfc 2026-06-27 — catalog IaC-commit (durable Gitea) + instance-provision rows verified (safe on the disposable throwaway).** Catalog Save/commit rows 127/128/129/132–140/142/143/145/147/148/149/153–156 flipped ✅ via `PUT /api/v1/catalog/<bp>/iac` → 200 `{committed:true}` + durable bare-repo Gitea commit, on two structurally-different blueprints (bp-alloy + bp-wordpress). Instance-provision rows 50/59/60/61 flipped ✅ via `POST /catalyst/v1/apps/instances` → 201 + CR Ready + matching topology badge (singleton + active-hot-standby).

> **3-agent live-walk batch 2026-06-27 — topology/recon/sso ✅ flips + owner-redirect fix (#4547) + 2 ❌→⛔ reclassifications (session-walled) + topology DR-panel product-gap recorded.** On dep `91dc05917e44d1c1`: 6 rows flipped ✅ (53/54 topology, 193/195 recon, 29/38 sso); row 3 owner-redirect = ⚠️ fix-merged-pending (#4546/#4547); rows 109 + 183 reclassified ❌→⛔ (KC browser-SSO-cookie / OpenBao final-render, both session-walled, not product faults); topology rows 51/52/56/57 held ⚠️ as a REAL product gap (DR/switchover UI panel not built for HR components) + 58/63 held ⚠️ as stale expectations (cilium/grafana render honestly active-active); recon 196/197 held ⚠️ read-only-gated. New canonical tally 193 ✅ · 0 ❌ · 9 ⛔ · 33 ⚠️ · 4 ☐ = 239.

## Session resolution — 2026-06-27 (Refs #909)

**Env:** live omantel.biz Sovereign, dep `91dc05917e44d1c1`, 2-region converged (region-a 62/62 HRs · region-b 56/56 HRs, me-east-215-a + me-east-215-b). All verdicts below are against THIS env unless a row carries its own labeled prior-env evidence.

**Headline:** 22 issues CLOSED this session with independently-audited live evidence (reproduced the live signal, no false-closes) · 4 more code-done + MERGED (roll-gated, NOT yet live-proven — recorded as merged-pending, not pass) · 13 remain open behind a recorded gate.

**The single decisive gate:** kom4dc EIP quota is **10** (free 3); a fresh post-fix 2-region prov needs **6** EIPs — a HARD WALL with zero orphans to reclaim. Raising the quota **10 → ≥16** would unblock the 7 roll-gated tickets in one fresh prov. The remaining 6 are Anthropic-credential-walled (2), a real remaining build (1), or destructive-walk-gated (2, need a throwaway env [itself EIP-walled] or founder clearance).

### Live-walk consolidation — 2026-06-27 (4-section read-only walk of `91dc05917e44d1c1`, region-a)

Four parallel read-only walkers (SSO/auth · Orgs/Contexts · Topology/DR/robust · Jobs/Cloud/Market) re-dialed the live 2-region omantel.biz Sovereign and reported **119 verdicts** (28 + 22 + 39 + 30): **75 PASS · 5 FAIL · 9 NA · 30 BLOCKED**. Consolidated into the numbered table below, the run flips **16 rows to ✅** (rows 5/6/25/32/41/42/43/44/64/71/77/111/189/205/229/236) and resolves 17 more rows from `☐`/`❌`/`⚠️` into their honest gate (⛔) or product-fault (❌) state, leaving the canonical table at **81 ✅ · 9 ❌ · 26 ⛔ · 42 ⚠️ · 81 ☐ = 239** at that snapshot. (A subsequent 2026-06-27 Pillar-1 funnel+voucher walk flipped 4 more rows to ✅ — 72/73/74 `☐`→✅ + 75 `⛔`→✅ — carrying the canonical table to **85 ✅ · 9 ❌ · 25 ⛔ · 42 ⚠️ · 78 ☐ = 239**.) Genuine product faults (FAIL): **newapi** serves 200 but generic-OAuth provider rows are never seeded (`Unknown OAuth provider`, rows 37/114); **Hubble** authed shell renders but the flow XHR (`/api/control-stream`) 302-bounces to KC so data never loads (row 39, carried ⚠️); and the **uatwalk91** Org reports `isolation=vcluster` while the live backing is host-namespace (rows 9/10/11/12/216 — the #3642 de-vcluster re-baseline, not a fresh regression). The 30 BLOCKED are honest gates: a kid-less handover *session* JWT is rejected by this catalyst-api build (whoami 401) and the SPA login guard clears injected cookies, so interactive-session UI renders (treemap/showback DOM, OpenBao authed UI, Topology-tab content) can't be asserted headless — their underlying data is proven at the API/kubectl/psql layer. Row 236 (region-B keycloak) **flips ⛔→✅** (the #4470 sandbox-ns deadlock did not recur; region-b 57 HRs True). No false-pass: every ✅ reproduced the live signal directly.

### Closed this session — 22, audited live (✅ VERIFIED-PASS)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| R1 | janitor | [#4454](https://github.com/openova-io/openova/issues/4454) | catalyst-api orphan-sweep no longer reaps a `ready` Sovereign — denylist inversion (`case "wiped": eligible; default: protect`) holds on the live env. | — | ⛔ | hw232: janitor is a MOTHERSHIP catalyst-api loop (sweeps deployment registry); not observable from child kubeconfig — mothership-scope | — | hw227-reconfirm-2026-07-06: hw227 (dep 504e330d86aaefb4) console 200 + Sovereign ready after 5h — janitor did NOT reap the ready env; protect-side inversion holds. 
| R2 | network | [#4467](https://github.com/openova-io/openova/issues/4467) | Cilium VXLAN+WireGuard MTU set to **1370** in BOTH regions — cross-node pod TCP no longer DF-drops; full-size data packets traverse. | hw239 | ⛔ | — | hw239: MTU-1370 VXLAN+WireGuard superseded by native-routing (#4656) — cilium mtu=1420 routing-mode=native both regions, no cilium_vxlan iface, vpc-podcidr-route-reconciler in-sync (6 nodes). Row asserts a DEAD config; cross-region datapath proven via native routes + WireGuard NodeEncryption (11 peers). 
| R3 | sso | [#4448](https://github.com/openova-io/openova/issues/4448) | bp-plane-isolation openbao default-deny ADMITS sso-bridge ingress → sso-bridge reaches openbao:8200. | hw282 | ✅ | hw282-2026-07-21 (region-a): netpol openbao/openbao-default-deny ingress rule[5] explicitly admits namespaceSelector kubernetes.io/metadata.name=sso-bridge; live proof sso-bridge reaches openbao:8200 — logs 21:21-21:22Z: 'tokens minted (kc + openbao)' + 'secret persisted to OpenBao secret/data/sso/sovereign/{newapi-admin,openova-flow,powerdns-admin,hubble-ui}' every tick. kubectl live. |
| R4 | sso | [#4458](https://github.com/openova-io/openova/issues/4458) | sso-bridge egress CNP permits openbao + keycloak — the reconciler's own egress is not blocked. | hw282 | ✅ | hw282-2026-07-21 (region-a): netpol sso-bridge/bp-sso-bridge egress admits keycloak ns:8080 + openbao ns:8200 (+DNS 53 kube-system, apiserver 6443); CNP sso-bridge-reconciler-apiserver Valid=True; live egress works — per-tick 'tick complete in 23s (discovered=6 reconciled=0 managed=catalyst-platform gitea grafana harbor openbao powerdns-admin)'. kubectl live. |
| R5 | sso | [#4437](https://github.com/openova-io/openova/issues/4437) | sso-bridge re-mints the grafana OIDC client secret each tick (KC_ADDR cache refreshed) — no stale-secret 401. | hw282 | ✅ | hw282-2026-07-21: sso-bridge re-mints grafana per tick — consecutive ticks 21:21Z and 21:22Z log 'reconcile bp-grafana (clientId=grafana skipUpsert=false)' + 'OpenBao bundle written, chart-side ES owns the Secret'; grafana-sso-oidc-credentials (6 keys, ES data-hash annotated) present; GET /login/generic_oauth → 302 to auth.hw282.omantel.biz client_id=grafana; 0 oauth error/401/invalid_client lines in 3h of grafana logs. |
| R6 | postgres | [#4442](https://github.com/openova-io/openova/issues/4442) | bp-postgres NetworkPolicy admits the declared consumers (CNPG operator probe + app dials). | hw282 | ✅ | hw282-2026-07-21 (region-a): netpol `shared-pg-allow-replication` admits app dials (`namespaceSelector:{}`→5432) + CNPG operator probe (ns=cnpg-system→8000/9187); live dial `newapi`→`shared-pg-rw.shared-data:5432` reached postgres auth stage (TCP+startup established, not blocked); cluster 3/3 healthy. kubectl live. |
| R7 | plane-isolation | [#4444](https://github.com/openova-io/openova/issues/4444) | bp-plane-isolation admits gitea ingress for its declared consumers (org-services→gitea de-vcluster gap). | hw282 | ✅ | hw282-2026-07-21 (region-a): `bp-plane-isolation@0.1.13` Ready; netpol `gitea/gitea-default-deny` ingress admits `ns=org-services`; live: org-services provisioning initContainer `wait-for-gitea-token` exit 0 ("token validates GET /api/v1/user=200"); org-services→`gitea-http.gitea:3000` HTTP 403 (path open). kubectl live. |
| R8 | gitea | [#4447](https://github.com/openova-io/openova/issues/4447) | gitea-flux-auth secrets are seeded — Flux pulls from gitea with valid credentials. | hw282 | ✅ | hw282-2026-07-21: all 4 flux-system GitRepositories Ready=True pulling from local gitea with seeded auth secrets — openova→cutover-gitea-git-auth, openova-catalog-sovereign→openova-catalog-sovereign-git-auth, openova-org-tenants + catalyst-tenant-hw282-omantel-biz→openova-org-tenants-git-auth; each secret carries username=gitea_admin + non-empty password; revisions fresh (main@8b77f5d etc.). kubectl live. |
| R9 | sso | [#4446](https://github.com/openova-io/openova/issues/4446) | oidc-gate client_secret is seeded for powerdns-admin — full OIDC round-trip completes. | hw282 | ⚠️ | hw282-2026-07-21: seeded leg PROVEN — oidc-gate-powerdns-admin-oidc client-secret 64B non-empty + oidc-gate-powerdns-admin-cookie present; pod oidc-gate-powerdns-admin-865f7b996d-vr298 1/1 Running; https://pdns-admin.hw282.omantel.biz/ → 302 /oidc/login; sso-bridge persists powerdns-admin secret to OpenBao each tick. NOT proven: full authed-browser OIDC round-trip (needs-browser-render). |
| R10 | orgs | [#4471](https://github.com/openova-io/openova/issues/4471) | org-controller RBAC carries update/patch — per-Org provisioning completes (was create-only, never reached Ready). | hw282 | ✅ | hw282-2026-07-21: ClusterRole catalyst-organization-controller carries organizations [get,list,watch,update,patch] + organizations/status [get,update,patch] + organizations/finalizers [update]; per-Org provisioning COMPLETED — Organization hw282-omantel-biz Ready=True reason=Reconciled ('host namespace Active + Keycloak group + Gitea Org reconciled'). kubectl live. |
| R11 | gitea | [#4354](https://github.com/openova-io/openova/issues/4354) | gitea git-data is PRESERVED across the host-ns re-home (#4325 de-vcluster) — no empty-PVC data loss. | hw282 | ✅ | hw282-2026-07-21: gitea git-data present and non-empty in the post-#4325 host-ns layout — org openova serves the mirrored catalog repo (Flux pulls 3 refs at live SHAs, Ready=True) + tenant org hw282-omantel-biz serves catalyst-tenant + environment repos; no empty-PVC symptom (the re-home itself is baked into fresh prov, so the data-present assertion is the walkable leg). |
| R12 | postgres | [#4460](https://github.com/openova-io/openova/issues/4460) | shared-pg `-mesh-rw` resolves in BOTH regions + CNPG streaming replication is live. | hw282 | ✅ | hw282-2026-07-21 kubectl: shared-pg-mesh-rw Svc resolves in BOTH regions (region-a ClusterIP 10.96.139.9, region-b 10.97.34.203, ns shared-data; also -b/-c pairs) + CNPG streaming replication live: region-a shared-pg-2 is RW primary (pg_is_in_recovery=f, pg_stat_replication 2 standbys state=streaming) and region-b shared-pg-replica-1 standby pg_stat_wal_receiver status=streaming sender_host=shared-pg-mesh. |
| R13 | convergence | [#4436](https://github.com/openova-io/openova/issues/4436) | region-B keycloak / gitea / harbor reach steady-state with NO CrashLoop. | hw282 | ✅ | hw282-2026-07-21 kubectl region-b: keycloak-0 1/1 Running, gitea 1/1 + gitea-sso-configure 1/1 Running, harbor 7 pods (core/jobservice/nginx/portal/redis/registry 2/2/sso-configure) all Running — 0 CrashLoopBackOff; restart counters settled (last restarts >50m ago, steady-state). |
| R14 | model | [#4479](https://github.com/openova-io/openova/issues/4479) | console Organizations directory reads the live Organization CRs (not an empty in-memory store). | hw282 | ✅ | hw282-2026-07-21: GET https://api.hw282.omantel.biz/api/v1/organizations with owner cookie → HTTP 200 returning the live Org (org_tenant_id=hw282-omantel-biz, state=done, kind=internal, all 6 steps done) matching the Organization CR (Ready=True); handler org_list_from_cr.go merges live Organization CRs (test: 'Org (CR only, no local provision record) appears in GET /api/v1/organizations') — not an empty in-memory store. |
| R15 | funnel | [#4473](https://github.com/openova-io/openova/issues/4473) | funnel plan-slug propagates end-to-end — the chosen plan reaches the provisioned Org. | hw274 | ⚠️ | hw274-2026-07-19: prerequisite absent — no funnel-provisioned Org exists yet (only seed internal Org, PLAN column empty; plan-slug enum [s,m,l,xl,flexi] present in CRD). Plan-slug propagation decidable only after the post-cutover funnel signup walk. [BLOCKED-prereq: re-walk post-cutover this env] |
| R16 | funnel | [#4290](https://github.com/openova-io/openova/issues/4290) | the funnel + BSS Org doors are collapsed onto ONE path — `console.<slug>` lands 200. | hw274 | ⚠️ | hw274-2026-07-19: prerequisite absent — no customer Org and no console.<orgslug> HTTPRoute (all 17 routes are sovereign-level *.hw274.omantel.biz). Sovereign console itself lands 200 via envoy, but the one-door funnel/BSS claim needs a funnel-created Org post-cutover. [BLOCKED-prereq: re-walk post-cutover this env] |
| R17 | orgs | [#4459](https://github.com/openova-io/openova/issues/4459) | deleting an Organization CR cascades cleanly — no orphaned ns / app / DNS leak. | hw274 | ⚠️ | hw274-2026-07-19: delete-cascade not exercisable — read-only walker + the only Org is the live seed Org (won't delete). Observation: metadata.finalizers on hw274-omantel-biz is EMPTY (no tenant-networking finalizer armed on the seed internal Org, unlike hw232's customer-Org note). [BLOCKED-prereq: re-walk post-cutover this env] |
| R18 | cutover | [#4450](https://github.com/openova-io/openova/issues/4450) | handover-key self-publish guard — the Sovereign does not re-publish its handover key on reconcile. | hw282 | ⚠️ | hw282-2026-07-21: mechanism-in-place leg PROVEN — catalyst-handover-jwt-public Opaque 1-key exists solely in catalyst-system (exactly 1 copy cluster-wide, age 3h28m, no churn = no re-publish observed across cutover + G12). NOT proven: the guard actively blocking a re-publish attempt (would require a mutation, out of read-only scope). |
| R19 | sandbox | [#4482](https://github.com/openova-io/openova/issues/4482) | sandbox-controller reaches Running (was CrashLoop on unset CATALYST_GITEA_TOKEN). | hw275 | ⛔ | hw275-2026-07-19: SUPERSEDED — Sandbox concept removed (founder 2026-06-30). 0 sandbox CRs, no sandbox ns; successor bp-openova-mcp HR live in catalyst-system. |
| R20 | delivery | [#4464](https://github.com/openova-io/openova/issues/4464) | the deploy-bot bumps image pins per-line (not a blanket bump) — stale-render avoided. | hw274 | ⛔ | hw274-2026-07-19: not applicable on a child Sovereign — deploy-bot per-line pin bumping is a mothership/CI concern (ledger already marks n/a); no deploy-bot surface exists in either hw274 region to observe. [SUPERSEDED/N-A] |
| R21 | catalog | [#4432](https://github.com/openova-io/openova/issues/4432) | catalog-seed pins match the published ghcr chart versions — no inert lagging Blueprint CR. | hw282 | ❌ | hw282-2026-07-21: catalog-seed Blueprint CRs LAG the published/deployed chart versions for 10 of 52 comparable blueprints — bp-catalyst-platform catalog=1.4.539 vs deployed=1.4.1195, bp-guacamole 0.2.3 vs 0.2.29, bp-continuum 0.1.4 vs 0.1.12, bp-cilium 1.4.13 vs deployed 1.4.16 (= ghcr latest per gh api), bp-loki 1.0.0 vs 1.0.3, bp-newapi 1.4.137 vs 1.4.140, bp-k8s-ws-proxy 0.1.14 vs 0.1.16, bp-kyverno-policies 1.0.46 vs 1.0.48, bp-powerdns 1.2.20 vs 1.2.21, bp-seaweedfs 1.2.4 vs 1.2.5; 42 match. Exactly the 'inert lagging Blueprint CR' defect the row tests. |
| R22 | plane-isolation | [#4468](https://github.com/openova-io/openova/issues/4468) | bp-plane-isolation admits apiserver-egress + gateway-ingress CNPs — CNPG admission + gateway hop succeed. | hw282 | ✅ | hw282-2026-07-21 (region-a): `*-allow-apiserver-egress` + `*-allow-gateway-ingress` CNPs (alloy/gitea/grafana/guacamole/…) all Ready=True; CNPG admission succeeded (9 Clusters reconciled, row 229) + gateway hop serves 200. kubectl live. |

### Merged this session — code-done, roll-gated for live proof on the next fresh prov (⚠️ merged-pending)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| M1 | janitor | [#4466](https://github.com/openova-io/openova/issues/4466) (PR [#4493](https://github.com/openova-io/openova/pull/4493)) | janitor hardening — log-only/dry-run for a full live cycle before any destructive sweep. | — | ⛔ | hw232: mothership catalyst-api janitor dry-run — not observable on child |
| M2 | apps | [#4477](https://github.com/openova-io/openova/issues/4477) (PR [#4492](https://github.com/openova-io/openova/pull/4492)) | newapi admin-token seeded into OpenBao at provision (was unseeded → admin-promote fault). | — | ☐ | — |
| M3 | network | [#4475](https://github.com/openova-io/openova/issues/4475) (PR [#4495](https://github.com/openova-io/openova/pull/4495)) | vcluster-tier CNP applied host-side — the per-Org vcluster tier carries its split CNP. | — | ⚠️ | hw232: vcluster internal Org ns host-side cnp allow-gateway-and-apiserver VALID=True + default-deny-all + allow-same-org (mechanism proven; no customer M+ vcluster Org) |
| M4 | apps | [#4111](https://github.com/openova-io/openova/issues/4111) (PRs [#4494](https://github.com/openova-io/openova/pull/4494)+[#4496](https://github.com/openova-io/openova/pull/4496)) | agenity image-pull half — fresh-install ghcr-pull path. | — | ⚠️ | fresh-install path verified live (ghcr-pull reflects + pod recovers); the agentic-run half (M-side) is Anthropic-credential-walled — see G8/#4277. |

### Remaining open — 13, with the recorded gate (honest map)

| # | Epic | Ticket | Test case | Walk | Result | Gate |
|---|---|---|---|---|---|---|
| G1 | adoption | [#4488](https://github.com/openova-io/openova/issues/4488) | crossplane provider-opentofu Observe-only adoption populates on the fresh prov. | hw282 | ✅ | hw282-2026-07-21 (region-a): `provider-opentofu` Healthy=True; **20** managed resources; cloudadoptions 20/21 Ready (observe-only, `adopt-eip-*` referencing existing external ids). kubectl live. |
| G2 | apps | [#4477](https://github.com/openova-io/openova/issues/4477) | newapi ES-sync drives the admin token into a fresh per-Org newapi (seed merged in M2). | hw237 | ⚠️ | seed merged newapi admin-promoted; per-Org ES-sync needs customer funnel Org | — |
| G3 | dr | [#4486](https://github.com/openova-io/openova/issues/4486) | DR self-fire exercises the standby-absent branch (no standby present). | hw255 | ⛔ | standby-absent branch not exercisable on 2-region prov (region-b replica present + sync) ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Standby-absent branch NOT exercisable on hw255: all 5 Continuums have a hot standby present — kubectl get continuums -A: dr-spine-{gitea,harbor,keycloak,openbao} primary=me-east-215-a hotStandbyRegions=[me-east-215-b] and cnpg/cnpg-pair-bp-cnpg-pair-continuum hotStandbyRegions=[hw-me-east-215-b-rtz-prod], all phase=Healthy (cnpg-pair: LeaseHeld=True, replicationLagSeconds=0). — Same verdict class as hw239 stamp: needs a standby-absent topology, not a defect; DR self-fire would additionally be a mutation (forbidden for this walker) | hw239: DR standby-absent branch NOT exercisable on this 2-region prov (standby present: all 5 Continuums standbyRegions=[me-east-215-b], phase=Healthy). Needs a standby-absent topology; not a defect. |
| G4 | adoption | [#4431](https://github.com/openova-io/openova/issues/4431) | VPC-quota reclaim demo on a fresh prov. | — | ⚠️ | EIP-quota — the reclaim demo needs a fresh prov cycle. | mothership-verify-2026-07-06T01:15: mothership [JANITOR] pass emits live orphanEIPsDeleted/orphanVPCsDeleted/orphanEVSDeleted fields (this pass: 0 orphan VPC/EIP, 19 EVS reclaimed) — the VPC/EIP-quota reclaim mechanism is live and running. The full fresh-prov reclaim DEMO (prov→wipe→quota-returns) still needs a fresh prov cycle (EIP-quota-walled). Refs #4431 |
| G5 | janitor | [#4466](https://github.com/openova-io/openova/issues/4466) | janitor log-only live proof (the dry-run full-cycle observation). | m | ⛔ | hw232: mothership catalyst-api janitor log-only — not observable on child | mothership-verify-2026-07-06T01:15: mothership catalyst-api [JANITOR] pass logged "destructive":false, "reaped":0, "reapErrors":0 — a full log-only/dry-run cycle observed live (reaped ZERO Sovereigns) while safely GC-ing 19 orphan EVS + 1 orphan keypair. The dry-run full-cycle observation. Refs #4466 |
| G6 | model | [#4212](https://github.com/openova-io/openova/issues/4212) | object-model seams 1+2 — the two adoption seams collapse into the #4488 crossplane-adoption walk (G1). | — | ☐ | — |
| G7 | orgs | [#4293](https://github.com/openova-io/openova/issues/4293) | vcluster dual-door walk — both Org doors land a vcluster-isolation Org. | hw236 | ⚠️ | hw236: customer vcluster Org acme236 plan=m billing=real, vcluster HR Ready=True (vcluster@0.33.4) - isolation backing present; dual-door funnel+BSS browser walk deferred | — | — |
| G8 | apps | [#4277](https://github.com/openova-io/openova/issues/4277) | anthropic credential seeded into the agentic runtime — chat works end-to-end. | hw255 | ❌ | hw228 DIRECT-WALK 2026-07-07: agentic runtime NOT deployed — agenity.hw228.omantel.biz → 000, no agenity/agentic pods (only sandbox-controller runs), no anthropic secret. Can't chat end-to-end without the runtime. ❌ Refs #3969 ‖ hw228 2026-07-09 LIVE: the sandbox PLATFORM is deployed (sandbox-controller 1/1 Running, bp-sandbox HR Ready chart 0.3.11) but NO anthropic/claude credential secret exists in any namespace → the agentic runtime has no Claude key → chat cannot run. Gated on the agentic-runtime cred-provisioning + a live sandbox session (Pillar-4 sandbox completion, adjacent to the post-SME MCP build #3988/GOV#899). Stays ❌ — not a quick fix. ‖ 2026-07-09 (G8 fix-agent, orchestrator-verified): the Anthropic-cred CHAIN is 100% built (#4277/#4111: ExternalSecret openbao catalyst/anthropic/token → agenity ANTHROPIC_API_KEY; seedAnthropicToken at Org-provision; api-deployment env wiring — all verified live). Gap = NO anthropic-api-key/credentials-json key in catalyst-openova-kc-credentials on hw228 OR the mothership (no key ever placed → seedAnthropicToken skips by design). FOUNDER ACTION: place a claude-code credentials.json into the mothership secret (chain resolves it) + decide on cloud-init propagation to Sovereigns. NOT agent-fixable (founder-supplied cred + security decision + agenity not installed on hw228). Stays as-is. ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): kubectl get secrets -A / grep -iE 'anthropic/claude' → EMPTY (zero Anthropic/Claude credential secrets cluster-wide on hw255); kubectl get pods -A / grep -iE 'agenity/sandbox' → only sandbox-controller-75cf97bbd5 1/1 Running, NO agenity/agentic runtime pods. The row's assertion 'anthropic credential seeded into the agentic runtime — chat works end-to-end' is live-false. — Same founder-gated state as hw228: the cred chain is built but no credentials.json was ever placed in the mothership secret (seedAnthropicToken skips by design) and bp-agenity is not installed. gh: #4277 OPEN. Not agent-… | hw227-gate-verify-2026-07-06T01:35: VERIFIED-BLOCKED — kubectl shows NO agenity pods/Application on hw227 (agentic runtime not deployed) AND zero anthropic/claude secrets in the cluster ([]). Agentic chat cannot be walked without the app + a real Anthropic credential (neither present). Refs #4277 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: 0 Anthropic secrets in-cluster AND agenity not deployed on hw227 — agentic chat cannot run. Refs #4277 ‖ **GATE CONFIRMED 2026-07-07 (repo inspection):** #3988 built the openova-MCP SERVER (products/openova-mcp/ — Go cmd/internal/{identity,tools,catalystapi}) but NOT its deployment layer: NO Chart.yaml, NO Dockerfile, NO blueprint.yaml, NOT in the bootstrap-kit or catalog. So the RBAC-scoped MCP facade cannot be deployed on ANY Sovereign (confirmed absent on hw228 — only sandbox-controller runs). These rows are gated on PACKAGING the server (image build workflow + Helm chart + blueprint.yaml + bootstrap-kit slot + per-Org KC identity wiring), a substantial build cycle, not a simple deploy. Refs #3988. ‖ **2026-07-09 LEDGER CORRECTION (repo re-inspection, evidence-backed):** the earlier 'NO Chart.yaml/blueprint.yaml/bootstrap-kit/catalog' gate note is FACTUALLY WRONG — it inspected `products/openova-mcp/` only. By design (`docs/INTEGRATION-OPENOVA-MCP.md` §1) the MCP server is baked as a subprocess binary inside the **`bp-agenity`** runtime image, and that deployment layer IS fully built: `products/agenity/blueprint.yaml` (kind:Blueprint, bp-agenity v0.5.20, visibility:listed), `products/agenity/chart/` (statefulset/httproute/oidc-gate/externalsecret-anthropic/externalsecret-mcp-bearer/CNPs), `products/agenity/Containerfile` (bakes `/usr/local/bin/openova-mcp`), catalog-seed `blueprints.yaml:5981`, bootstrap-kit `13-bp-catalyst-platform.yaml`, CI `agenity-build.yaml`. RBAC (list/get/create_application Org-scoping, cross-Org 403) is implemented + unit-tested in `products/openova-mcp/internal/{identity,tools}`. REAL blockers (NOT 'unbuilt'): (a) founder places a live Anthropic credential (`CATALYST_ANTHROPIC_CREDENTIALS_JSON`) → `sovereign_anthropic_seed.go:140` stops loud-skipping — FOUNDER action; (b) install `bp-agenity` into a live Org + walk; (c) close fresh-Org bearer-wiring defect **#4624**. Refs #3988 #4277 #4276 #4624 |
| G9 | apps | [#4111](https://github.com/openova-io/openova/issues/4111) | agentic-run half — the agenity solo agent chats + drives create_application. | [agenity.demo](https://agenity.demo.omani.homes/app/) | ⚠️ | agentic-run/SSO half PROVEN on 8fd457a8 (claude -p → NORTHSTAR-OK); MCP create_application BLOCKED on fresh-Org agenity-MCP auth-wiring (pubkey-not-reflected [fixed live] + OPENOVA_MCP_BEARER-not-seeded → tools/list empty) — filed #4624. Agent did NOT fabricate. [[../sessions/2026-06-28/evidence/north-star-8fd457a8.txt]] ‖ hw228 2026-07-09 LIVE: no active sandbox session + no anthropic cred (see G8) → the agenity solo agent isn't live to chat/drive create_application. Gated on sandbox/agentic-runtime completion (Pillar 4, post-SME-adjacent). Stays ⚠️. ‖ 2026-07-09 (G8 fix-agent, orchestrator-verified): the Anthropic-cred CHAIN is 100% built (#4277/#4111: ExternalSecret openbao catalyst/anthropic/token → agenity ANTHROPIC_API_KEY; seedAnthropicToken at Org-provision; api-deployment env wiring — all verified live). Gap = NO anthropic-api-key/credentials-json key in catalyst-openova-kc-credentials on hw228 OR the mothership (no key ever placed → seedAnthropicToken skips by design). FOUNDER ACTION: place a claude-code credentials.json into the mothership secret (chain resolves it) + decide on cloud-init propagation to Sovereigns. NOT agent-fixable (founder-supplied cred + security decision + agenity not installed on hw228). Stays as-is. ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255 backing state (read-only kubectl): secret catalyst-system/catalyst-openova-kc-credentials contains ONLY [kc-addr, kc-audience, kc-realm, kc-sa-client-id, kc-sa-client-secret, smtp-*] — NO anthropic-api-key/credentials-json; no agenity HelmRelease or Application anywhere (grep over hr/applications -A empty); zero Sandbox CRs; sandbox-controller 1/1 Running (ns sandbox). — The 'agenity solo agent chats' half is founder-cred-gated (#4277: no Anthropic credential ever placed; seedAnthropicToken skips by design) and bp-agenity is not installed per-Org on hw255; the 'drives… |
| G10 | placement | [#3969](https://github.com/openova-io/openova/issues/3969) | placement EPIC — the remaining real build. | hw274 | ⚠️ | hw274-2026-07-19: read-leg VERIFIED — 7 Application CRs carry spec.placement (spine-gitea/harbor/keycloak + shared-pg{,-b,-c}=active-hot-standby, spine-openbao=active-passive; kubectl); /app/shared-pg Topology renders 'Placement Pattern: active-hot-standby' + PRIMARY(me-east-215-a)→STANDBY(me-east-215-b) cards matching the CR; 'Edit placement' present. Apply leg mutation-gated (#4950). [prereq-gated] |
| G11 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | sovereignty cutover — 8-step + 10-min deny-egress hold. | hw282 | ✅ | hw282-2026-07-21 (fleet walker): CM self-sovereign-cutover-status (ns catalyst, region-a): 11/11 steps result=success, cutoverComplete=true, progressPercent=100, finished 2026-07-20T20:17:11Z, egress-block-test success 19:51:06Z→20:16:15Z (10-min deny-egress hold inside); gitea mirror live (org openova repo openova + hw282-omantel-biz/catalyst-tenant; 4 GitRepositories Ready=True from gitea-http.gitea.svc at main@8b77f5d/catalog-sovereign@6ee14e6/org-tenants@fb7ceca); harbor pivot live (projects openova-io=95 repos, proxy-ghcr=51, proxy-dockerhub=53; /v2/_catalog 200 repos authed via registry.hw282.omantel.biz); all flux-system HelmRepositories = oci://registry.hw282.omantel.biz/openova-io; per-node registriesYaml v2 on all 4 region-a nodes. Residual (inert): vcluster-system/loft HelmRepository still charts.loft.sh, no HelmChart consumes it. |
| G12 | dr | [#4275](https://github.com/openova-io/openova/issues/4275) | region-kill (Pillar-3) — kill a region, prove failover + recovery. | hw282 | ✅ | hw282-2026-07-20 (dep 09696223c939cf29): **region-kill G12 CLEAN 6/6 — failover AND failback** on the #5303-hardened train. (1) KILL: 4 region-a ECS HARD os-stop (HTTP 200 ×4, bastion excluded). (2) #5267 fail-fast: console/api → **503** no-healthy-upstream ×5 (not 404). (3) region-b **auto-promoted RW primary** (in_recovery=f at 20:22:10Z — no force-arm; #5239 arm-gate auto-armed). (4) **RPO=0**: pre-kill sentinel survived + post-kill write accepted (g12_hw282 rows=2). (5) #5268 **no split-brain**: on restart region-a old-primary DEMOTED to replica before accepting any write (in_recovery f→t observed live). (6) **FAILBACK**: region-a re-cloned as **streaming replica of promoted region-b** — live psql `in_recovery=t | wal_recv=1 | g12_rows=2` (rows converged); region-b stays RW (`f | 2`). Post-restart cold chart-pulls succeeded (region-a HR 63/67 re-converged, Flux state intact) — **#5302/#5303 Harbor lowercase `disableredirect` PROVEN end-to-end**: the exact cold-pull x509 wedge that pinned hw281 region-a at 0/67 is GONE with the fix baked in. Prior hw274-2026-07-19 (⚠️): promote-path validated but promoter force-armed (#5239 then in flight) — now superseded by this fully-autonomous 6/6. — region-a HARD os-stop (6 ECS), apiserver DEAD +22s; dr-promoter held the 120s anti-flap window then PROMOTED region-b at +101s with the #5219 same-tick suspend LATCH (HR spec.suspend=true — Flux drift-correction can't revert). RPO=0 PROVEN: the pre-kill sentinel survived (count=1) on the promoted primary + post-kill write accepted (writable). Pre-kill pair was clean (region-b streaming from region-a, timelines matched a=1/b=1 — no #5220 false-promote divergence). CAVEAT: the promoter had to be FORCE-armed — the #5222 arm-gate never auto-arms because signals probes pg_stat_wal_receiver.status as streaming_replica (lacks pg_read_all_stats) so status=NULL (filed #5239, fix in flight). Fully-autonomous G12 (no force-arm) validates on the next fresh prov post-#5239. recover os-start fired. |

> **Reading the gate map:** the 13 remaining work-items resolve to **12 distinct open tickets** (#4212 carries object-model seams 1+2, both folding into #4488/G1 — counted as one ticket here, two seams in narrative). 7 of them (G1–G7) clear in ONE fresh post-fix 2-region prov the moment kom4dc EIP quota rises **10 → ≥16**. 2 (G8–G9) need a real Anthropic credential. 1 (G10) is a genuine remaining build. 2 (G11–G12) are destructive walks needing a throwaway env (itself EIP-walled) or explicit founder clearance. None is a false-fail; each is an honest fault with a named unblock.

---


**RESET 2026-06-26 — clean slate for the fresh Sovereign, now walked on fresh prov `91dc05917e44d1c1` (omantel.biz, region-a); recounted after the 2026-06-27 4-section live-walk consolidation + the 2026-06-27 Pillar-1 funnel+voucher walk + the 2026-06-27 newapi-SSO resync walk + the 2026-06-27 authed operator-console walk + the 2026-06-27 Pillar-4 Sandbox/MCP walk + the 2026-06-27 authed live-walk sweep + the 2026-06-27 write-mutation walk on fresh prov `25aadcfc` + the 2026-06-27 ⚠️/❌ reconcile + the 2026-06-27 3-agent live-walk batch (Refs #909): 193 ✅ · 0 ❌ · 9 ⛔ · 33 ⚠️ · 4 ☐ = 239 rows.** (⚠️/❌ reconcile against the live env flipped 7 rows to ✅ with reproduced live evidence: Hubble row 39 `⚠️`→✅ — bp-cilium@1.4.5 rolled, oauth2-proxy `--api-route=^/api/` live, `/api/*` returns 401-not-302; funnel /review row 83 `⚠️`→✅ — marketplace:f1f7ae6 ships the #4526 ReviewStep topology card; OpenBao admin row 33 `❌`→✅ — bp-openbao@1.2.58 rolled, OIDC auth_url now requests `scope=openid+groups` + sso-operator-admin policy mapped (the 403 root cause resolved); Jobs Re-run row 176 `❌`→✅ — catalyst-api e976ff0 ran the #4514 delete+recreate (`mimir-make-minio-buckets-rerun-*` Complete, original deleted, no 502); Topology rows 62/187/188 `⚠️`→✅ on the same data-grounded standard as 64/71 — live Continuum CRs Healthy/Ready/LeaseHeld + grafana multi-region active-active + catalyst-api single-region singleton, all via kubectl, UI render SSO-session-walled. RECLASSIFIED rows 9/10/11/12 `❌`→⚠️ (and corrected 216's rationale): the uatwalk91 host-namespace backing is DESIGN-CORRECT for its `plan=s` per the #4292 TIER GATE — `boundaryIsVcluster`/`allTiersVcluster=false` backs free/S Orgs with the host `<slug>` namespace, only M+ get a dedicated Org-vcluster — so the blanket `vcluster`-for-every-Org expectation is stale, leaving only a cosmetic `isolation=vcluster` label mismatch. Row 183 HELD ❌ (the 403 root cause is #4522-fixed but the row asserts the rendered Secrets-Engines screen, SSO-session-walled). Rows 37/114 confirmed stayed ✅ (newapi bare URL → 200 'Signing you in…', `sovereign` provider recognized). NO false-pass: every ✅ flip reproduced the live signal at the HTTP/kubectl/served-bundle layer; the authed test-session mint is 404-gated in prod so interactive UI renders stay honestly ⚠️/❌ where un-reproduced.) (Authed operator-console walk via a throwaway-mailbox PIN session flipped 11 interactive-UI rows `⛔`→✅ — treemap/showback 20/21/23, keycloak-admin 34, Organizations directory/terminology 116/117/118/119/120/121/122 — and exposed 2 genuine product faults `→❌`: OpenBao authed admin UI 403 row 33, Jobs Re-run 502 immutable-spec row 176, both with fixes in flight.) (newapi-SSO walk flipped rows 37 + 114 `❌`→✅ — #4477 `resync_provider_secret()` (bp-newapi@1.4.137) resolved the stale `custom_oauth_providers` client_secret live, no "Unknown OAuth provider"; Hubble row 39 holds ⚠️ fix-merged-pending-roll for PR #4509, the `--api-route=^/api/` flow-XHR fix.) (Pillar-4 Sandbox/MCP walk via a throwaway Sandbox CR on `91dc05917e44d1c1` region-a flipped row 218 `⛔`→✅ — sandbox-controller no longer CrashLoops (R19/#4482 gitea-token bridge), Sandbox CR reconciles `Ready=True` — and flipped 219 + 220 `⛔`→⚠️ PARTIAL (controller writes 12 gitops files incl the MCP ConfigMap + pty-server StatefulSet; the image bundles `@anthropic-ai/claude-code` + `@qwen-code` + the `/usr/local/bin/openova-sandbox-mcp` binary — runtime-pod-materialize + live agentic run not awaited). Rows 221/222/223 HOLD ⛔ on an Anthropic credential: the MCP `create_application` tool + the openova-mcp Org-claim RBAC are proven at the API layer (211/212/213), only the live chat-driver is credential-walled. The MCP auto-mount is PROVEN — `configmap-mcp-config.yaml` projected at all 4 agent-discovery paths (`/workspace/.mcp.json`, `~/.claude.json`, `~/.qwen/settings.json`, `.cursor/mcp.json`) + the MCP binary in the published image. ⚠️ note: the sandbox-controller has no delete-finalizer to prune the gitea gitops path on Sandbox CR delete (minor cleanup gap); a subset of Wave-2 MCP tools (`registry.go`, `sandbox_db.go`) return `not_implemented` stubs — surface discoverable, some advanced bodies stubbed.) (Consolidation pass 2 of the live fresh-prov walk — folds §C orgs/keystone · §G model+placement · §H contexts+mcp+sandbox · §I recon/cloud/e2e/meta on top of pass 1's §A funnel · §B SSO · §D topology/DR · §E jobs/cloud/storage · §F session-fix.) Per-row cells carry the walker's exact verdict + evidence; the 100 remaining `☐` are genuinely-unwalked surfaces — no fabricated verdicts. **Pass-2 corrections applied:** (1) per-Org provisioning is PROVEN post-#4471 RBAC fix — funnel Org `uatwalk91` serves WordPress HTTP 200 over valid pool LE TLS, so the prior ⛔ "org-controller RBAC" cells flipped ✅ for the WordPress-backed rows (86/88/89/90/233) and ⚠️ "this app not in the walked Org's cart" for openclaw/mail/postgres (232/234/238); (2) the per-app **Topology tab exists** (§I `app-tab-topology`), falsifying §D's "no Topology tab" premise — rows 51-58/62-64/71/187/188 moved ❌→⚠️ pending a content re-walk; (3) placement rows 98-108 are **superseded by #4325 de-vcluster** (re-baseline #3642, not a product fault) → ⚠️, not ❌; (4) §I canvas-click drill-ins (179/181/184/186/193/195-197) are ⚠️ harness artifacts, not click-verified; (5) restored 9 §A/§F walker-PASS rows (40/76/79/80/84 §A funnel; 227/230/231 §F ✅, 228 §F ⚠️) whose cells reverted to ☐ in a working-tree branch-switch — re-applied from the captured walker evidence, no new walk. (Write-mutation walk on fresh prov `25aadcfc` [kv160729.omani.works, a fresh prov on current main; writes are safe + were cleaned up] flipped **26 rows `☐`→✅**: catalog Save/commit-IaC rows 127/128/129/132–140/142/143/145/147/148/149/153–156 — `PUT /api/v1/catalog/<bp>/iac` → 200 `{committed:true}` + durable bare-repo Gitea commit, proven on two structurally-different blueprints bp-alloy + bp-wordpress; instance-provision rows 50/59/60/61 — `POST /catalyst/v1/apps/instances` → 201 + CR Ready + matching topology badge. These rows were GATED only by the read-only constraint on the PERMANENT env — the catalog-IaC-commit + provision CODE is identical, proven on the disposable throwaway.) The detailed historical narrative below (omantel.biz convergence, the hw1xx throwaway envs) is retained ONLY as explicitly-labeled prior-env history — its per-row claims are STALE by construction and must not be read as current. (Refs #4279.)

> 📋 **Row map:** rows **1–226** are the canonical acceptance walk (epics: model · onboarding · topology · SSO · jobs · recon · cloud · storage · mcp · e2e-journey · convergence · funnel · meta). Rows **227–239** are the **Fresh-prov 2026-06-26 — session-fix acceptance** section (this session's resolved-issue cases — delivery-architecture, fresh-funnel apps, spine/postgres/crossplane). All ☐ until the fresh-prov walk.

## Prior — omantel.biz PERMANENT Sovereign (dep `4635277cae4ffed9`, me-east-215-a/-b) — STALE, superseded by the fresh prov
_All per-row verdicts below were walked on the now-superseded omantel.biz env and are STALE by construction. Retained as labeled history; the live verdict cells above are all `☐` pending the fresh-prov walk._

## Prior — wiped throwaway envs (hw173/hw176/hw177/hw178 — STALE, superseded by omantel.biz)
_hw178 (dep `02bc518fcb1b4cb3`) — cycle-2d zero-touch re-fire; 6 fixes walked PASS; superseded. hw177 wedged on a worker-kubelet infra flake; wiped. All hw1xx envs are wiped — their per-row verdicts do not reflect omantel.biz._

## Prior — cycle-2b BLOCKED on hw176 storage wedge (2026-06-21, now fixed)
_hw176 (dep `12523e0c7611356b`) — zero-touch cycle-2b re-fire on `main@10b061b`. **STORAGE FIX DID NOT HOLD zero-touch (❌, headline).** HRs flooded to 67/region (past the old 40/50 wedge) but `bp-huawei-evs-csi` 1.0.1 HelmRelease → InstallFailed: `no matches for kind VolumeSnapshotClass in version snapshot.storage.k8s.io/v1` (the chart renders a VolumeSnapshotClass by default but the snapshot.storage.k8s.io CRDs are installed nowhere in the bootstrap-kit) → no `evs-ssd` SC → 6 stateful PVCs Pending → `bp-gitea` not ready → `bp-catalyst-platform` blocked → console HTTP 000. Root-caused live + fix shipped: #4030 / PR #4031 (chart 1.0.2, volumeSnapshotClass default off, MERGED). The cycle-1 fix-wave was ⛔ un-walkable behind the wedge — health-gate honored. Per-row evidence: [`../sessions/2026-06-21/walk-hw176/`](../sessions/2026-06-21/walk-hw176/)._

## Status — last validated: hw173 (2026-06-20)
_Update: topology rows 46–71 re-walked across BOTH region clusters (#3965 fix) → 0✅/14❌ became 9✅/9❌/8⚠️; the remaining ❌ are the Continuum-lease unfinished-feature gap + no-live-catalog-create, not region blindness._
**124 ✅ · 32 ❌ · 30 ⚠️ of 186.** Verified live via API + mothership-kubectl by 7 parallel walkers (#883); per-row evidence in [`../sessions/2026-06-20/walk-hw173/`](../sessions/2026-06-20/walk-hw173/). Headline defects: hw173 came up **single-region** → topology epic unwalkable (14❌, Continuum Degraded); **funnel provisioning CrashLoop** (FerretDB `partialFilterExpression` unsupported) → zero customer Orgs → model/orgs `acme` rows fail; **Harbor down** (core CreateContainerConfigError + jobservice CrashLoop); **Gitea SSO 500**; **Tenants/SME banned-term leak** in console bundle.

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| 1 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Console bare URL → lands `/dashboard` signed-in as the owner (no PIN/login form). | hw282 | ✅ | hw282-2026-07-21 (fleet walker): Bare https://console.hw282.omantel.biz/ with owner session → HTTP 200, lands /dashboard signed-in (sidebar chip 'EB emrah.baysal@openova.io'), no PIN/login form; /api/v1/whoami 200 tier=owner 6/6 probes (one transient 503 self-recovered). |
| 2 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Full sidebar renders (Dashboard / Cloud / Apps / Catalog / Agenity / Jobs / Compliance / Users / Organizations / Settings). | hw282 | ✅ | hw282-2026-07-21 (fleet walker): Sidebar renders all required entries live: Dashboard /dashboard, Cloud /cloud, Apps /apps, Catalog /catalog, Agenity /sandbox, Jobs /jobs, Compliance /sre/compliance, Users /users, Organizations /organizations, Settings /settings (+Billing). |
| 3 | model | [#3687](https://github.com/openova-io/openova/issues/3687) [#4546](https://github.com/openova-io/openova/issues/4546) | The voucher-redeem URL on an authed owner session server-redirects to `/dashboard` (no redeem form for the owner). | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Redeem-URL leg proven: marketplace.hw282.omantel.biz/redeem/?code=VCH-S25Q77HG6NVV renders 'VOUCHER VALID — 100 OMR credit' live; owner-redirect leg NOT proven: console has no /redeem route (SPA 'Not Found') and injected console-only cookie creates no marketplace/Keycloak SSO session, so server-redirect-to-/dashboard could not be exercised (needs interactive owner SSO). | — |
| 4 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | App page tab strip generality: open ≥2 archetypes (a DB `shared-pg`, a consumer `harbor`) → identical tab strip; consumer Dependencies shows `Depends on: shared-pg / db:<ctx>`. | hw282 | ✅ | hw282-2026-07-21 (fleet walker): /app/shared-pg strip Overview·Contexts(3)·Topology·Resources·Compliance·Logs·Settings·Members·Endpoints·Jobs·Dependencies; /app/harbor identical minus Contexts (consumer), and harbor Dependencies panel shows 'Depends on: shared-pg / db:registry' plus cnpg/cert-manager/gateway-api/external-secrets. |
| 5 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Organizations directory: the customer Org row shows KIND=customer, TIER=sme, BILLING=real, ISOLATION=vcluster, STATUS=active. | hw274 | ⛔ | hw274-2026-07-19: SUPERSEDED — 'TIER=sme' obsolete per #3985 SME→Organization eradication (founder 2026-06-20: SME abandoned; tier canon org/corporate); live: Organization CRD spec.tier enum=[org,corporate], 'sme' unrenderable by schema; live Org spec.tier=org. Non-SME field assertions stay covered by rows 6/9-12. [SUPERSEDED/N-A] |
| 6 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The customer Org is present as a real Organization in the operator directory (same record dashboard + showback read). | hw282 | ☐ | hw282-2026-07-21 (fleet walker): No customer Org exists on hw282: GET /api/v1/organizations 200 → exactly 1 item {slug:hw282-omantel-biz, kind:internal, tier:org, state:done}; directory shows only parent + Omantel(internal); creating a customer Org is a funnel mutation outside this read-only walk (funnel voucher VCH-6BSR3C54QJAY still times_redeemed=0). |
| 7 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail renders canonical fields: slug `acme` (NOT `sme-<uuid>`), kind customer, tier sme, billing real, isolation vcluster, owner, console URL. | hw274 | ⛔ | hw274-2026-07-19: SUPERSEDED — 'tier sme' obsolete per #3985 SME→Organization eradication (founder 2026-06-20); live: Organization CRD spec.tier enum=[org,corporate], 'sme' unrenderable by schema; live Org spec.tier=org. slug/kind/billing/isolation/owner assertions remain covered by rows 6/9-12. [SUPERSEDED/N-A] |
| 8 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The Create-organization form renders fully (kind picker, slug, Company name, Admin email, parent-domain). | hw282 | ✅ | hw282-2026-07-21 (fleet walker): /organizations/new renders fully: kind picker Internal/Customer (Customer Defaults 'billing: real, isolation: vcluster'), slug input (ph 'acme'), Company name (ph 'Acme Corp'), Admin email (ph 'admin@acme.com'), domain-mode radios, parent-domain select omani.homes/rest/trade/works, console-URL preview, Create button. |
| 9 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail Status = active, backed by a real `vcluster` isolation (the create→Provision loop produced a live backing, not a fake-green). | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Org detail (sole Org, internal): Status=Active stable, but Isolation renders 'Namespace' not 'vcluster'; backing check: kubectl get vclusters -A → none, org namespace hw282-omantel-biz empty — #4325 de-vcluster pattern persists (cf rows 98/108). | — |
| 10 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The customer Org is present + ACTIVE backed by a real `vcluster` isolation (Lane B convergence read). | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Same capture: status=active stable across loads; isolation='Namespace' not 'vcluster' (API record carries vcluster_name=vc-hw282-omantel-biz but zero vcluster CRs live on region-a). | — |
| 11 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail Status = active and the directory + detail consistently show `vcluster` isolation. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Directory row and detail agree (Omantel, active) but both surfaces report Namespace isolation, not vcluster; kubectl confirms no vcluster backing exists. | — |
| 12 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | On re-load the Org detail consistently reports active + `vcluster` (stable, no false flicker). | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Reload test: /organizations/hw282-omantel-biz re-rendered byte-identical (Status Active, no flicker); vcluster leg absent (Isolation=Namespace) and /organizations/new Defaults still advertise 'isolation: vcluster' — stale vs live. | — |
| 13 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Catalog grid renders the Blueprint cards; `bp-postgres` detail has a **+ New instance** button. | hw282 | ✅ | hw282-2026-07-21 (fleet walker): /catalog grid renders Blueprint cards (Agenity, Alloy, Axon, CNPG, PostgreSQL, ...); /catalog/bp-postgres shows '+ New instance' button and Instances table shared-pg / shared-pg-b / shared-pg-c all Ready, topology active-hot-standby, v0.2.13. |
| 14 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The shared-PG reuse model is LIVE: the `shared-pg` app **Contexts** tab shows multiple consumers sharing ONE PostgreSQL. | hw282 | ✅ | hw282-2026-07-21 (fleet walker): shared-pg Contexts tab live: db/registry→harbor (harbor-database-secret, ready), db/gitea→gitea (ready), db/keycloak→keycloak (ready) — 3 consumers sharing ONE PostgreSQL, matching the Contexts(3) badge. |
| 15 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Apps list renders one card per Application (all Platform/BOOTSTRAP-… a customer-launched app card would appear under its Org. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): /apps renders 46 cards, one per installed platform Blueprint, every card BOOTSTRAP-badged (46/46); customer-launched-app-under-its-Org leg unprovable — no customer Org/app exists on hw282 (1 internal Org, 0 attributed apps). |
| 16 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | A customer app page (`/app/<name>`) → Settings/Topology → change topology → Save persists; the canonical tab strip includes a Topology tab. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): /app/shared-pg Topology tab live: Placement panel + 'Edit placement' button + Disaster recovery 'Switch over…' control present in canonical strip; change-topology→Save persist NOT exercised (write mutation, forbidden for this read-only walker); no customer /app/<name> exists. |
| 17 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Dashboard treemap is a meaningful drill-down surface (Organization → vCluster → Application selectable & drillable, NOT a raw infra-pod utilisation treemap); default layer stack per founder verdict. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Layer selects offer Organization/Sovereign/Region/Cluster/vCluster/Family/Namespace/Application/Progress/Kind; switching to Organization→Application relayouts to org group 'Platform overhead' with per-app cells (shared-pg, harbor, keycloak... — NOT raw infra pods); drill click wired but instance-named cell shared-pg navigates to /app/bp-shared-pg → 'App not found' (bp- prefix mapping defect), while bootstrap cell harbor drills clean to /app/bp-harbor (Ready). |
| 18 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Scan every treemap cell: NO ephemeral Job-pod cell appears (no `cutover-*`, scan-vulnerabilityr… `*-snapshot-save-*`). | hw282 | ✅ | hw282-2026-07-21 (fleet walker): Scanned all 45 SVG text nodes at Organization→Application layers: zero cutover-*/scan-*/*-snapshot-save-* cells, while such Job pods exist live (showback lists cutover-harbor-prewarm-1784572593, openbao-snapshot-save-29743040 under __platform__) — treemap correctly excludes ephemeral Job pods. |
| 19 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Count Application cards: one card per `Application` (NOT one per HelmRelease/pod); bootstrap apps carry a Platform/Bootstrap badge. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): 46 cards vs 67 HelmReleases region-a — NOT one-per-HR — and all 46 carry BOOTSTRAP badge; strict 1-card-per-Application-CR not held: 9 Application CRs live (kubectl), the shared-pg trio surfaces as Instances rows under /catalog/bp-postgres rather than /apps cards. |
| 20 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | With a customer Org present, the customer estate is visually distinct from platform pods on the treemap. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Estate distinction present on Organizations showback (parent 'hw282.omantel.biz — your own estate' 0 units vs '__platform__ (platform overhead)' 20534 units); dashboard treemap Organization layer renders only the 'Platform overhead' group — no customer estate with consumption exists to distinguish (mutation-gated on funnel app-run). |
| 21 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Organizations Showback panel: the **Application** column for a selected Org lists only that Org's real apps (no cluster-wide infra/Job pods). | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Showback panel (GET /api/v1/org/consumption backing): selected parent Org slice shows 'No applications attributed yet.' with ZERO cluster-wide infra/Job pods under it — every infra/Job pod (incl. cutover-*) sits under __platform__; positive per-Org app listing unprovable, sole Org has 0 attributed apps. |
| 22 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Showback shows a single visually-distinct **Platform overhead** roll-up line holding all control-plane/Job workloads. | hw282 | ✅ | hw282-2026-07-21 (authed browser walk, Playwright owner session): Organizations showback renders a single visually-distinct '__platform__ (platform overhead)' roll-up (20746.16 units · 20345m CPU · 41.91 GiB) holding all control-plane workloads (cnpg-pair/kyverno/harbor/cilium rows under it) |
| 23 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | After a 2nd Org runs an app, the showback panel shows a SECOND Org row attributed only to its own app, distinct from Platform overhead. | hw274 | ⚠️ | hw274-2026-07-19: needs a 2nd Org running an app; GET /api/v1/organizations returns exactly 1 item (hw274-omantel-biz, kind=internal, 0 attributed apps) and showback has no second Org slice; creating one is a funnel mutation — out of scope for this read-only walk. [mutation-gated: lift on commit/funnel walk] |
| 24 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | `shared-pg` renders the canonical tab strip Overview · Contexts · Topology · Dependencies (Contexts tab present only for shareable blueprints). | hw282 | ✅ | hw282-2026-07-21 (authed browser walk, Playwright owner session): shared-pg renders canonical tab strip Overview · Contexts (3) · Topology · Resources · … · Dependencies (6) — Contexts tab present (shareable blueprint) |
| 25 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | One consistent model across surfaces: `/organizations` shows the Orgs that `/apps`, the dashboard and showback all agree on. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): /organizations directory (parent hw282.omantel.biz + Omantel) == GET /api/v1/organizations (1 item hw282-omantel-biz); no surface contradicts; showback shows parent + __platform__ only and /apps+dashboard are platform-scoped, so full 4-surface agreement not demonstrable on a 1-Org 0-consumption estate (matches hw274 shape). |
| 26 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Console bare URL → lands on the dashboard signed-in as the owner; no PIN/login/"Sign in with…" button. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: GET https://console.hw282.omantel.biz/ with owner cookie -> 200 (SPA served); /api/v1/whoami -> 200 {email:emrah.baysal@openova.io, tier:owner, roles:[catalyst-owner,catalyst-admin,sovereign-admins], sovereignFQDN:hw282.omantel.biz}; render leg (dashboard visible, no PIN prompt) pending needs-browser-render. |
| 27 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Avatar (top-right) menu reads "Signed in as the owner" with a Sign-out item. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: whoami 200 returns owner identity emrah.baysal@openova.io tier=owner backing the avatar menu; menu render (Signed-in-as + Sign-out item) pending needs-browser-render. |
| 28 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Users page renders the pre-seeded owner row the owner (tier=owner UserAccess CR), signed-in admin. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: GET /api/v1/sovereign/users with owner cookie -> 200, 2 UserAccess items keyed keycloakSubject=emrah.baysal@openova.io incl. pre-seeded useraccess-owner-emrah-baysal-at-openova-io (rolebindingsCreated:1, created 2026-07-20T18:20:43Z); Users-page render pending needs-browser-render. |
| 29 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Re-open the bare console URL after the session TTL → lands signed-in again, no PIN re-prompt. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Re-entry leg PASS: two consecutive bare-console GETs with same cookie -> 200/200 + whoami 200, no re-auth redirect; strict TTL-expiry leg untestable read-only — session exp epoch 1784610967 is ~7.9h away, expiry not simulatable without destroying the live cookie (same limitation as hw274 stamp). |
| 30 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Grafana bare URL → lands on Grafana Home, full UI, no login form; left nav shows Administration; user menu = the owner. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: grafana.hw282.omantel.biz/ -> 302 /login -> 307 /login/generic_oauth -> 302 https://auth.hw282.omantel.biz/realms/sovereign/...auth?client_id=grafana&kc_idp_hint=catalyst-pin (PKCE S256), KC client grafana enabled; chain terminates 200 at console PIN page; Grafana Home + Administration nav render pending needs-browser-render. |
| 31 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Gitea bare URL → lands on the gitea dashboard titled "emrah.baysal — Dashboard", no login; profile menu exposes Site Administration; stays on :443. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: gitea.hw282.omantel.biz/ -> 303 /user/login -> 302 /user/oauth2/openova-sso -> 307 KC sovereign client_id=gitea -> broker/catalyst-pin -> console /login 200 (full chain with cookie jar, all hops on :443, no login form rendered by gitea); signed-in dashboard render pending needs-browser-render. |
| 32 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Harbor bare URL → lands on `/harbor/projects`, no login form; user dropdown the owner with Administration menus. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: harbor.hw282.omantel.biz 301 -> registry.hw282.omantel.biz -> 200 portal SPA; /c/oidc/login -> 302 KC sovereign client_id=harbor code_challenge_method=S256 kc_idp_hint=catalyst-pin, KC client harbor enabled; /harbor/projects signed-in render pending needs-browser-render. |
| 33 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | OpenBao bare UI → lands in an authenticated Vault session (Secrets engines / dashboard), NO token-entry form (an in-transit "Signing in…" shim is allowed). | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: bao.hw282.omantel.biz/ and /ui/ -> 302 /sso/landing -> 200 'Signing in… — OpenBao' shim (explicitly allowed by row, no token-entry form); POST /v1/auth/oidc/oidc/auth_url role=operator -> 200 auth_url=https://auth.hw282.omantel.biz/realms/sovereign/...client_id=openbao (also proves unsealed — sealed bao returns 503); authenticated Vault-session render pending needs-browser-render. |
| 34 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Keycloak admin console for the **sovereign** realm → lands inside the admin console (realm overview / Users / Clients), no master-realm login. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: GET https://auth.hw282.omantel.biz/admin/sovereign/console/ -> 200 with embedded config "realm":"sovereign","authUrl":"https://auth.hw282.omantel.biz" — admin console targets the sovereign realm, no master-realm login config; signed-in-inside-console render pending needs-browser-render. |
| 35 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Guacamole bare URL → lands on the Guacamole connections list, signed-in; no Tomcat 404, no `/guacamole/` login page. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: guacamole.hw282.omantel.biz/ -> 302 /guacamole/ -> 200 Guacamole app (no Tomcat 404); POST /guacamole/api/tokens -> expected[0]={name:id_token,type:REDIRECT,redirectUrl:https://auth.hw282.omantel.biz/realms/sovereign/...client_id=guacamole&kc_idp_hint=catalyst-pin}; signed-in connections-list render pending needs-browser-render. |
| 36 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | PowerDNS-Admin bare URL → lands on the dashboard signed-in; no redirect loop, no OAuth error, no `Log In` page. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: pdns-admin.hw282.omantel.biz/ -> 302 /oidc/login -> 302 KC sovereign client_id=powerdns-admin kc_idp_hint=catalyst-pin (oidc-gate), chain terminates 200 at console PIN page — no redirect loop, no OAuth error; signed-in dashboard render pending needs-browser-render. |
| 37 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) [#3858](https://github.com/openova-io/openova/issues/3858) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi bare URL (1st hit) → lands on `/console` signed-in as admin (role 100); no "Unknown OAuth provider", no login page. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: newapi.hw282.omantel.biz/ -> 200; /api/status.custom_oauth_providers=[{name:'OpenOva SSO',slug:sovereign,client_id:newapi-admin,authorization_endpoint:https://auth.hw282.omantel.biz/realms/sovereign/...?kc_idp_hint=catalyst-pin}] — provider registered (no 'Unknown OAuth provider'), KC client newapi-admin present+enabled; /console signed-in role-100 render pending needs-browser-render. |
| 38 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) [#3858](https://github.com/openova-io/openova/issues/3858) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi bare URL (2nd hit, re-entry) → lands on `/console` again signed-in; NOT an "already bound" / re-link error / `/setup` wizard. | hw282 | ☐ | hw282-2026-07-21 (fleet walker): needs-browser-authed-session: re-entry/rebind behavior requires completing the OAuth code flow twice as the owner; no read-only wire leg distinguishes it from row 37 (provider config + KC client newapi-admin enabled were verified there). |
| 39 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Hubble bare URL → lands on the Hubble UI, authenticated (not an anonymous/unauth view, no login page). | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: hubble.hw282.omantel.biz/ -> 302 https://auth.hw282.omantel.biz/realms/sovereign/...auth?client_id=hubble-ui&kc_idp_hint=catalyst-pin — auth enforced, no anonymous view; full chain (KC -> broker/catalyst-pin -> api /oidc/auth -> console PIN page) ends 200; authenticated Hubble UI render pending needs-browser-render. |
| 40 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Marketplace bare URL → renders the anonymous storefront (public, by design); confirm no spurious login UI is forced. | hw282 | ✅ | hw282-2026-07-21: https://marketplace.hw282.omantel.biz/ -> HTTP 200 anonymous (24595 bytes), storefront heading 'Build your cloud Organization' / title 'Build Your Organization — OpenOva', trusted LE, no auth redirect; only a passive 'Sign in' nav link (by design), no forced login UI. |
| 41 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Keycloak sovereign realm → Users lists the single owner principal the owner (enabled). | hw282 | ✅ | hw282-2026-07-21: KC admin REST GET /admin/realms/sovereign/users -> exactly 1 user: username=email=emrah.baysal@openova.io enabled=True (id d93ad957-3c52-421f-9317-bcb4ab48e560). (kcadm.sh absent from this KC image at /opt/keycloak/bin — admin REST API used as equivalent backing.) |
| 42 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Owner user → Groups tab shows membership in `/sovereign-admins` (alongside `/openova-users`) — the source of admin authority. | hw282 | ✅ | hw282-2026-07-21: GET /admin/realms/sovereign/users/d93ad957.../groups -> /openova-users + /sovereign-admins + /sovereign-viewers — membership in /sovereign-admins ALONGSIDE /openova-users, both present as realm groups (row met as-written; improves on hw274 where /openova-users was not a group). |
| 43 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Owner user → Role mapping tab: effective realm roles include `catalyst-admin` (not only default-roles/uma/o… | hw282 | ✅ | hw282-2026-07-21: GET .../users/d93ad957.../role-mappings/realm/composite -> [catalyst-admin, catalyst-developer, catalyst-operator, catalyst-viewer, default-roles-sovereign, offline_access, uma_authorization] — effective realm roles include catalyst-admin, not only defaults. |
| 44 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Groups → `/sovereign-admins` → Role mapping: group confers `catalyst-admin` (console) and realm-management `realm-admin` (KC console) — one source, both grants. | hw282 | ✅ | hw282-2026-07-21: GET /admin/realms/sovereign/groups/bfcc4f48...(/sovereign-admins)/role-mappings -> realmMappings=[catalyst-admin] AND clientMappings realm-management=[realm-admin] — one group source confers both the console grant and the KC-console grant. |
| 45 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Console Users panel: owner row + the ability to view/manage users renders — proving console admin nav is driven by the realm principal, not a self-signed constant. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: /api/v1/sovereign/users 200 returns owner UserAccess rows and whoami carries realm-derived roles [catalyst-owner,catalyst-admin,sovereign-admins] driving admin nav (realm principal, not a self-signed constant); Users-panel render pending needs-browser-render. |
| 46 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `bp-postgres` catalog detail renders; click **New instance** → the create dialog opens with a topology `<select>`. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: GET /api/v1/catalog/bp-postgres -> 200 (7378B) with configSchema.properties.topology.mode enum [singleton,active-hot-standby] + spec.topology.supported=[singleton,active-hot-standby]; TopologyPicker component (data-testid=topology-picker, products/catalyst/console/src/components/TopologyPicker.svelte) renders supported list in the create dialog; dialog-open render pending needs-browser-render. |
| 47 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) [#3856](https://github.com/openova-io/openova/issues/3856) | Topology `<select>` options read exactly the ONE canonical vocabulary: `singleton`, `active-passive`, `active-hot-standby`, `active-active` (no `single-region`/`act…). | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: union of topology.supported across all 80 live catalog blueprints = exactly {singleton, active-passive, active-hot-standby, active-active}; zero legacy option tokens ('single-region' appears only as the internal spec.topology.defaults map key, never as an option); UI type BcpTopology = same 4 values; rendered select options pending needs-browser-render. |
| 48 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `active-passive` is a selectable option in the create `<select>` (not folded away). | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: 18 live blueprints carry active-passive in topology.supported (bp-newapi, bp-openbao, bp-wordpress, bp-valkey, ...); TopologyPicker renders every supported entry as its own radio and hw282 is 2-region so multi-region options are not disabled; render pending needs-browser-render. |
| 49 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `singleton` is a separate selectable option (single-region single-instance), distinct from the multi-region modes. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: singleton is a distinct supported entry (bp-postgres supported=[singleton,active-hot-standby], mode default=singleton) rendered as its own radio with description 'One replica in one region; no failover', separate from multi-region modes; render pending needs-browser-render. |
| 50 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Pick `active-hot-standby`, name the instance, Provision → the create succeeds (toast/redirect to the new app card), NOT a red `topology not in supported [...]` error. | hw278 | ⛔ | hw278-2026-07-19: N/A read-only verify walker — criterion requires Provision (POST create mutation) forbidden. active-hot-standby backing real: dr-shared-pg cnpg-pair primary 3/3 region-a + replica 3/3 region-b Healthy. Provision leg not exercised. |
| 51 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab renders a per-region placement view listing region-a (active) and region-b (standby) as ONE placement, not two separate instances. | hw282 | ✅ | hw282-2026-07-21 browser: shared-pg Topology tab renders ONE placement view — 'hw-me-east-215-a-rtz-prod · 09696223c939cf29 · mgmt / ● PRIMARY / serves writes' ──▶repl 'hw-me-east-215-b-rtz-prod · mgmt / ○ STANDBY · Hot / live replica' under single 'Pattern: active-hot-standby' panel, not two separate instances; backed by placement API {targets:[a=Primary,b=Standby·Hot],derivedFromRuntime:true}. |
| 52 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab: the standby (region-b) copy is shown scaled-down / passive while region-a shows the active replica count (not identical hot counts). | hw282 | ⚠️ | hw282-2026-07-21: passive/asymmetric leg proven — region-b card '○ STANDBY · Hot / live replica' (hollow dot) vs region-a '● PRIMARY / serves writes'; but NO numeric active-replica-count renders on the region-a card (panel innerText contains no counts). Backing kubectl: shared-pg 3/3 pods region-a (1 primary+2 replicas), shared-pg-replica 3/3 region-b. Same render gap as hw274. |
| 53 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Open a per-app Topology tab and read its placement view (singleton apps note no per-region/standby surface). | hw282 | ✅ | hw282-2026-07-21 browser: opened per-app Topology tabs for cilium, grafana, guacamole — each renders a live placement view 'Pattern: active-active' with both region cards, honest per kubectl (grafana 3/3 + guacamole-server Running in BOTH regions); non-paired apps show NO standby/DR surface (drSections=0, switchover buttons=0); blueprint-level bp-postgres tab honestly renders 'Pattern: singleton / No placement targets reported yet' for empty targets. No singleton-placed app exists on this all-multi-region prov. |
| 54 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) [#3856](https://github.com/openova-io/openova/issues/3856) | `shared-pg` Topology tab declared-topology strip renders the canonical mode in ONE vocabulary — header dialect and picker dialect MATCH (no `singleton` header over an `active-hotstandby` chip). | hw282 | ✅ | hw282-2026-07-21: ONE vocabulary across all three surfaces — Topology header 'Pattern: active-hot-standby', Overview 'Placement: active-hot-standby', and New-instance picker option 'active-hot-standby — primary serves; second-region standby promotes on failover; cnpg-pair · sync replication; bp-continuum switchover (RTO 10s / RPO 0s)'. No singleton-header-over-active-hotstandby-chip contradiction. |
| 55 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab shows the effective (live) topology next to the declared one — declared vs effective together, not a build-time constant. | hw282 | ✅ | hw282-2026-07-21: effective topology is live-derived, not a build-time constant — GET .../applications/shared-pg/placement returns {derivedFromRuntime:true} from live Pods and the tab's 'Pattern: active-hot-standby' is DERIVED from those targets (per #3969 single-derived-pattern design that replaced the declared/effective strip); declared mode visible on Overview ('Placement: active-hot-standby') matches; DR block chip reads '● live — Read live off the Continuum + CNPGPair CR status'. |
| 56 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab per-region placement + replication block: region-a primary, region-b replica, and a live replication-lag in seconds (not a hardcoded `—`). | hw282 | ✅ | hw282-2026-07-21 browser: DR block renders 'Primary region hw-me-east-215-a-rtz-prod (serves writes)' + 'Standby region hw-me-east-215-b-rtz-prod (hot replica, follows WAL)' + 'Replication lag 0.0 s' (live numeric, not '—'); corroborated live: replication-status API walLagSeconds=0 source=live, and psql region-b pg_stat_wal_receiver status=streaming. |
| 57 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab Switchover button is present and armed because a live 2-region cnpg-pair backs the app. | hw282 | ✅ | hw282-2026-07-21 browser: 'Switch over…' button present and armed on shared-pg Topology tab with caption 'promote standby hw-me-east-215-b-rtz-prod — runs an RPO/health preflight before you confirm'; backed by live 2-region cnpg pair (shared-pg 3/3 region-a RW primary pg_is_in_recovery=f; shared-pg-replica 3/3 region-b streaming). |
| 58 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | A **singleton** app (cilium) Topology tab: the DR section / Switchover button does NOT render (honestly hidden, not armed against a phantom region). | hw282 | ✅ | hw282-2026-07-21 browser: cilium Topology tab renders placement only ('Pattern: active-active', both regions ● PRIMARY — honest, cilium CNI runs in both clusters); Disaster-recovery section count=0 and Switch-over button count=0 — DR honestly hidden, nothing armed against a phantom region. |
| 59 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Catalog New instance → pick `singleton` → Provision → that app's Topology tab shows single-region placement (no region-b standby, no Switchover). | hw278 | ⛔ | hw278-2026-07-19: N/A read-only render walker — criterion needs Catalog New-instance→singleton→Provision (a mutation); hw278 is VERIFY-only. Singleton mode is real (kubectl singleton apps mode=singleton, no DR). Provision leg not exercised. |
| 60 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Catalog New instance → pick `active-hot-standby` → Provision → that app's Topology tab shows a 2-region pair (region-a primary + region-b replica + armed Switchover). | hw278 | ⛔ | hw278-2026-07-19: N/A read-only render walker — needs Catalog New-instance→active-hot-standby→Provision (mutation); verify-only. Mode proven real: shared-pg topology RENDERED 2-region pair (region-a PRIMARY + region-b STANDBY·Hot + armed Switch over…). Provision leg not fired. |
| 61 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Apps grid shows newly-provisioned postgres instances as their own cards, each carrying a topology badge matching the mode picked at create time. | hw282 | ⚠️ | hw282-2026-07-21 browser: /apps grid renders shared-pg, shared-pg-b, shared-pg-c as their own cards, each carrying an 'active-hot-standby' topology badge + INSTALLED + ⛓3/⛓3/⛓5 contexts, matching their declared create-time mode (instances API topology=active-hot-standby, status Ready ×3). 'Picked at create time via wizard' linkage needs a Provision (mutation, forbidden for this walker). |
| 62 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | On an app WITH a live pair, the Topology DR section shows the live Continuum status (Ready / lease holder / standby) from the live API, not a static badge. | hw282 | ✅ | hw282-2026-07-21: shared-pg DR section is live-fed, not static — chip '● live' titled 'Read live off the Continuum + CNPGPair CR status', primary/lease region hw-me-east-215-a-rtz-prod + standby region-b + lag 0.0s all match live API GET .../continuum/dr-shared-pg/replication-status {currentPrimary:a, replicas:[{region:b,role:replica,lagSeconds:0}], source:'live', observedAt:2026-07-20T21:19:27Z} and kubectl Continuum dr-shared-pg phase=Healthy Ready=True leaseHolder=hw-me-east-215-a-rtz-prod. |
| 63 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | grafana (declares hot-standby, no live DR backing) Topology DR section reads the honest "no live DR backing… Switchover unavailable" state with a disabled (not armed) Switchover button. | hw282 | ⚠️ | hw282-2026-07-21: asserted precondition absent — grafana does NOT declare hot-standby-without-backing on this env; it genuinely runs in BOTH regions (kubectl: grafana 3/3 Running region-a AND region-b) and Topology honestly renders 'Pattern: active-active' with zero DR section and zero Switchover buttons (counts=0). No dishonest armed-Switchover render observable; the specific no-live-DR-backing disabled-button state cannot be exercised. [prereq-gated] |
| 64 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab replication-lag field shows a live numeric seconds value (or explicit "no replica"), never a hardcoded `—`. | hw282 | ✅ | hw282-2026-07-21 browser: replication-lag field renders live numeric 'Replication lag 0.0 s' (not '—'); live corroboration: replication-status API walLagSeconds=0 walLagBytes=0 source='live', psql region-a pg_stat_replication replay_lag≈0 state=streaming, region-b pg_stat_wal_receiver status=streaming. |
| 65 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Cloud/regions view shows the true region count — a healthy 2-region prov reads `Cluster 2/2` with no phantom region-B bubble. | hw282 | ✅ | hw282-2026-07-21 (authed owner walk): console topology API → regions=**2** (me-east-215-a/b) both status:healthy, Cluster 2/2, no phantom region. Reconciles to kubectl. (Playwright down; authed console API verified.) |
| 66 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Cloud→Clusters renders 2/2 HEALTHY clusters, one per region (me-east-215-a + me-east-215-b), no phantom region. | hw282 | ✅ | hw282-2026-07-21: console topology API → **2/2 healthy** clusters, one per region (me-east-215-a + me-east-215-b), nodeCount 4 each. Authed owner session; reconciles to kubectl. |
| 67 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | grafana status/overview reports Healthy/Running in both regions — no "cannot resolve write host" crashloop in the app health panel. | — | ☐ | — |
| 68 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | powerdns-admin status reports Healthy/Running — the CNPG-minted DB host resolved (no "could not translate host"). | — | ☐ | — |
| 69 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | keycloak status reports Healthy/Running in both regions — JGroups DB-host resolves, no UnknownHostException in the health panel. | — | ☐ | — |
| 70 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | guacamole status reports Healthy/Running in both regions — no missing-recordings-PVC error surfaced. | — | ☐ | — |
| 71 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Region-kill baseline (before): `shared-pg` Topology tab shows live Continuum Ready, lease held by region-a, region-b standby present, a live replication-lag number. | hw282 | ✅ | hw282-2026-07-21 (honest post-G12+failback reading — roles returned to baseline): shared-pg Topology shows Continuum live ('● live'), lease/primary held by REGION-A (kubectl Continuum dr-shared-pg leaseHolder=hw-me-east-215-a-rtz-prod phase=Healthy Ready=True; psql region-a shared-pg-2 pg_is_in_recovery=f), region-b standby present ('○ STANDBY · Hot', pg_stat_wal_receiver streaming), live lag number 0.0 s. NOTE: briefing said lease should sit with region-b post-G12; live state proves the G12 failback restored region-a — reported as observed. |
| 72 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Operator console BSS → Vouchers page renders the voucher issuance form (code, credit OMR, plan tier, description); no login wall. | hw274 | ❌ | hw274-2026-07-19: console.hw274.omantel.biz/billing/vouchers renders Issue-voucher form with owner session, no login wall — fields: Code(optional/auto-gen), Credit (OMR), Description, Max redemptions, Recipient email. Criterion names 'plan tier' — NO plan-tier field exists in the form (table has PLAN column only). All else renders. |
| 73 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | In the voucher form, type a weak code `1234` → Issue → the form rejects it inline ("voucher code must be at least 12 characters"). | — | ☐ | — |
| 74 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Leave the code field empty → Issue → a new voucher row appears with a server-auto-generated high-entropy code, credit, plan tier, `unredeemed 0/1`. | — | ☐ | — |
| 75 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Stranger opens the redeem page with `?code=<CODE>` → sees "Voucher valid · 5000 OMR" with THIS Sovereign's brand chrome (no openova.io / mothership logo). | — | ☐ | — |
| 76 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Open the redeem page with a junk code → a generic "voucher not valid" message (no tombstone / no detail leak). | — | ☐ | — |
| 77 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Redeem page source (DevTools): the HTML shows THIS Sovereign's brand and no `console.openova.io` / `omantel.openova.io` / bare `openova.io` host literal. | — | ☐ | — |
| 78 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Click "Sign up to redeem" → the browser lands on the plan picker grid (`/plans`). | — | ☐ | — |
| 79 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Plans grid shows the tiers (S/M/L/XL/Flexi) with price/CPU/memory → pick plan M → advances to the app catalog (`/apps`). | — | ☐ | — |
| 80 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | App catalog grid (served from THIS Sovereign's catalog) → pick WordPress → advances to add-ons (`/addons`). | — | ☐ | — |
| 81 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Add-ons step shows optional add-ons → leave defaults → Continue → advances to the BCP topology step (`/bcp`). | — | ☐ | — |
| 82 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | BCP topology step shows BOTH Single-region and Active-hot-standby radios (the Pillar-2 BCP choice at signup) → select Active-hot-standby → Continue → review. | — | ☐ | — |
| 83 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Review summary shows the chosen plan (M), app (WordPress), topology (Active-hot-standby), the Org slug, and the pool-TLD → Proceed to checkout. | — | ☐ | — |
| 84 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | On checkout, enter the stranger's email → Send code → type the emailed sign-in code (no password) → the page shows the stranger signed in + Org confirmed. | — | ☐ | — |
| 85 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Checkout summary: the voucher credit is applied — "Credit covers this order — 0 OMR due" (no Stripe card form) → click Launch / Place order. | — | ☐ | — |
| 86 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Provisioning progress timeline advances to Done (Creating Org → Committing manifests → Provisioning vCluster → Deploying WordPress → TLS → Health), no hang/red step. | hw274 | ❌ | hw274-2026-07-19: post-Launch UI shows only generic spinner "Setting up your console… Still working… (Ns)" — criterion's labelled timeline "Creating Org → Committing manifests → Provisioning vCluster → Deploying WordPress → TLS → Health" not rendered; backend step 'Deploying WordPress' ERRORED (app not ready after 10m) yet UI surfaced no red step and redirected anyway. |
| 87 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | After Launch, the marketplace redirects to the per-Org console — URL becomes the per-Org console and loads with publicly-trusted TLS (no cert error / connection-refused). | — | ☐ | — |
| 88 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Per-Org console landing: the stranger is signed in zero-click as the Org owner (their email in the avatar, their Org in the header) — no login/PIN form. | — | ☐ | — |
| 89 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Per-Org console → Applications view shows the purchased WordPress app card Running/Healthy → click Open. | hw274 | ❌ | hw274-2026-07-19: console.acme274.omani.homes/apps shows ONLY the vcluster card ("Search your 0 apps…") — criterion "purchased WordPress app card Running/Healthy" unmet: no WordPress card at all; funnel cart-install commit to per-Org repo acme274/catalyst-tenant failed (gitea ref-race persisted after 5 attempts). |
| 90 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Terminal acceptance: the purchased WordPress app SERVES at its own FQDN the app FQDN — the live rendered WordPress site (not 404/502/cert error). | hw274 | ❌ | hw274-2026-07-19: https://wordpress.acme274.omani.homes/ → HTTP 404 (DNS resolves via wildcard) — criterion "live rendered WordPress site (not 404/502)" unmet; app never deployed into the Org vcluster. |
| 91 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | While signed in, re-open the marketplace root → the returning-user redirect sends the customer to their own Org console, never to `console.openova.io` / the mothership. | hw274 | ❌ | hw274-2026-07-19: signed-in customer reopened https://marketplace.hw274.omantel.biz/ → stayed on storefront "Build your cloud Organization" ~19s, no redirect to console.acme274.omani.homes (signed-in chip shown; not sent to any openova.io host either). Tenant is in failed state after WordPress install failure — redirect never fired. |
| 92 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | As the signed-in customer, rapidly re-submit checkout/redeem >5× in a few seconds → after ~5 attempts a rate-limit notice appears; a single legitimate redeem is unaffected. | hw274 | ❌ | hw274-2026-07-19: 8 rapid same-origin authed POST /api/billing/checkout re-submits (~2s) → uniform 400 {"error":"plan_id and tenant_id are required"}, ZERO 429 after >5 attempts — criterion "after ~5 attempts a rate-limit notice appears" unmet; the single legitimate checkout earlier returned 200. |
| 93 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Generality: mint a 2nd voucher and re-walk B.1→B.4 with a different slug (`walk-stranger-two`) and a different pool-TLD (`omani.rest`) → a 2nd Organization provisions and lands signed-in. | hw278 | ⛔ | hw278-2026-07-20: 2nd voucher/Org (walk-stranger-two) NOT walked — this SANCTIONED mutation walk was scoped to ONE customer Org (acme275); minting a 2nd voucher + 2nd Org is outside the sanction. TLD picker offers omani.rest/works/trade/homes; acme275 provisioned on omani.rest. |
| 94 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The 2nd Org's console lands signed-in on a different TLD (the 2nd-Org console) — identical zero-click contract, no special-casing. | hw278 | ⛔ | hw278-2026-07-20: 2nd-Org console — depends on row-93 second Org (outside the one-Org sanction). The zero-click landing contract itself was proven on the first Org (console.acme275.omani.rest → 200 trusted TLS). |
| 95 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The 2nd Org's purchased app serves at its own different-TLD FQDN (the 2nd-Org app) — two Orgs, two TLDs, two running apps, identical mechanism. | hw278 | ⛔ | hw278-2026-07-20: 2nd-Org app — depends on the row-93/94 second Org (outside the one-Org sanction). The first Org's purchased WordPress itself deploys+serves (row 226/90), so the mechanism is proven on one Org. |
| 96 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Handover URL → lands on `/dashboard` already signed-in as the owner (avatar E), no login form. | hw274 | ⚠️ | hw274-2026-07-19: handover-URL leg not replayable read-only (one-time redeem; owner session supplied out-of-band). Observed: /dashboard renders signed-in via injected catalyst_session — avatar 'E', emrah.baysal@openova.io in header, no login form. [prereq-gated] |
| 97 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Dashboard renders the cluster treemap and the LAYER 1 / LAYER 2 grouping comboboxes are visible. | hw282 | ☐ | hw282-2026-07-21 (fleet walker): LAYER 1/LAYER 2 combobox UI is superseded (hw255 re-baseline: /cloud is the Lens-based graph, no LAYER combobox); treemap backend alive this walk — GET /api/v1/dashboard/treemap HTTP 200 with items (alloy count=4 etc.) — but the render leg needs-browser-render. |
| 98 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Click LAYER 1 combobox → select `vCluster`; the treemap regroups into one labelled block per vCluster (`host` / `mgmt` / `rtz` / `dmz`), mgmt block visible + clickable. | hw236 | ⛔ | RE-BASELINED ☐→⛔ (superseded, NOT a product fault): #4325 de-vcluster removed the mgmt/rtz/dmz vClusters (plane-isolation moved to per-component host-ns CNP), so a `vCluster`-grouped treemap no longer has `mgmt`/`rtz`/`dmz` blocks — the hw228 walk confirmed `group_by=vcluster` returns a single `host` block with all 7 named apps as children. This row asserts the DEAD mgmt-vcluster grouping model; consistent with rows 99-107 (already ⛔ for the same reason). Refs #4325 #3642. ‖ hw255-catalog-edit-2026-07-15: re-confirmed on hw255 (read-only): NO mgmt/rtz/dmz vClusters exist (kubectl ns grep → none; de-vcluster #4325 holds), current /cloud is the Lens-based graph (cloud/runtime/…/catalyst selector, no LAYER combobox) (row98-108-cloudview-lens-no-layer-combobox.png) — the row's mgmt-vcluster grouping model stays DEAD; ⛔ re-baseline stands. |
| 99 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **grafana** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; mgmt/host placement re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 100 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **harbor** tile sits inside the **mgmt** block, not **host**. | hw268 | ⛔ | hw268-2026-07-17: superseded by #4325 de-vcluster (mgmt-vcluster placement model dead) — not a product fault; not walked (screenshot(s) docs/sessions/2026-07-17/hw268-authed-console/). ‖ RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 101 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **keycloak** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 102 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **gitea** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 103 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **openbao** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 104 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) [#3831](https://github.com/openova-io/openova/issues/3831) | On the LAYER1=vCluster treemap, the **newapi** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 105 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **guacamole** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 106 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Drill into the **mgmt** block: its tiles include all 7 named apps (grafana/harbor/key… alongside loki/mimir/nats/tempo. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; mgmt block placement re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 107 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Read the **host** block on the same treemap: none of the 7 named apps appear under `host`. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; host block re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  OPPOSITE of the assertion — the 'host' block contains ALL 7 named apps (they should NOT be there if mgmt-grouped); vCluster grouping degenerate to a single host bucket. ❌ |
| 108 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Open the keycloak app card → its placement detail reads `mgmt` (the per-app placement mirrors the treemap block), not `host`. | hw236 | ⛔ | RE-BASELINED ☐→⛔ (superseded, NOT a product fault): #4325 de-vcluster moved keycloak into its own host ns (no mgmt vCluster), so per-app placement correctly reads `host`, NOT `mgmt` — the row asserts the DEAD mgmt-vcluster placement model that #4325 voided. Consistent with rows 99-107 (already ⛔ for the same reason). Refs #4325 #3642. ‖ hw255-catalog-edit-2026-07-15: re-confirmed on hw255 (read-only): keycloak runs in host ns 'keycloak' (keycloak-0 Running) — per-app placement 'host' is CORRECT post-#4325; ⛔ re-baseline stands. |
| 109 | placement | [#688](https://github.com/openova-io/openova/issues/688) | The sovereign-realm account console renders for the owner (no second login, no error dialog). | hw227 | ⛔ | RECLASSIFIED ❌→⛔ (by-design, NOT a product fault): the KC account-console "Something went wrong" REST 401 on a brokered catalyst-pin session is an accepted consequence of passwordless-PIN (#688) — the KC-native account console cannot mint a REST token from the PIN-brokered session by design. Link corrected #3642→#688 (#3642 was a mis-link — that issue is the mgmt-vcluster migration, unrelated). ‖ ORIGINAL: hw232 Playwright SSO (screenshot hw232-sso-evidence/): KC account console "Something went wrong" REST 401 on brokered catalyst-pin session — EXPECTED honest-non-green gate holds (KC-native, passwordless-PIN #688) |
| 110 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Gitea opens already signed in (avatar/menu shows the SSO user), repo list renders — no Gitea login form. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: gitea SSO chain gitea/ -> /user/login 302 -> /user/oauth2/openova-sso -> KC sovereign client gitea -> catalyst-pin broker -> console PIN page 200, no Gitea login form rendered; signed-in avatar + repo-list render pending needs-browser-render. |
| 111 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Harbor opens signed in, the projects list renders — no Harbor login form, no gateway error page. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: registry.hw282.omantel.biz -> 200 Harbor portal (no gateway error page), /c/oidc/login -> 302 KC sovereign client harbor; signed-in projects-list render pending needs-browser-render. |
| 112 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Grafana opens signed in (no Grafana login), the home dashboard renders. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: grafana auto-login chain / -> /login -> /login/generic_oauth -> 302 KC sovereign client grafana (no Grafana login form serving as terminal state); signed-in home-dashboard render pending needs-browser-render. |
| 113 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | The OpenBao UI renders signed in via OIDC — no manual token/unseal prompt blocking the landing. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: bao /ui/ -> 302 /sso/landing 200 OIDC auto-redirect shim (no manual token/unseal prompt — no form in page); auth_url mint 200 proves unsealed + OIDC to KC sovereign client openbao; signed-in UI render pending needs-browser-render. |
| 114 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi opens signed in, its main console renders — no login form, no upstream-connect error. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: newapi / -> 200 from the app itself (no upstream-connect error), OpenOva SSO provider registered in /api/status with KC sovereign authorization_endpoint, KC client newapi-admin enabled; signed-in main-console render pending needs-browser-render. |
| 115 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Guacamole opens signed in, the connections list renders — no Guacamole login form. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Wire PASS: guacamole / -> 302 /guacamole/ 200 app, token API returns OIDC REDIRECT to KC sovereign client guacamole (no local login as terminal auth); signed-in connections-list render pending needs-browser-render. |
| 116 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Organizations directory: the page title / heading reads "Organizations", never "Tenants"/"SME Tenants". | — | ☐ | — |
| 117 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Directory org cards / list rows: column headers read "Organization / Kind / Tier / Billing / Isolation / Status"; rows labeled "Organization", never "tenant"/"SME". | — | ☐ | — |KIND|TIER|BILLING|ISOLATION|STATUS; row badges PARENT/INTERNAL/CUSTOMER — zero tenant/SME row labels. Only 'tenant' string on page = workload name 'tenant-7fffbb499f' (org-services pod) in the __platform__ showback app table (data, not a label). |
| 118 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Left-nav sidebar label for this section reads "Organizations", not "Tenants"/"SME". | — | ☐ | — |
| 119 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Create-organization flow: the form title, field labels, and submit button all say "Organization" (no "SME tenant slug" / "Onboard tenant" persona words). | — | ☐ | — |SME|onboard over the rendered form = 0 matches. |
| 120 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Organization-detail view: heading "Acme Corp", breadcrumb "← Organizations", field labels Slug/Kind/Tier/Billing mode/Isolation/Stat… — no "tenant"/"SME". | — | ☐ | — |SME = 0 matches. |
| 121 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | BSS / billing screen: billing is framed as "This organization is in showback mode…", zero "tenant"/"SME" leaks. | — | ☐ | — |
| 122 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Legacy `/bss/tenants` URL (PR #3390 alias) resolves and redirects to `/organizations`, rendering the directory (H1 "Organizations") — not a 404, not a login redirect. | — | ☐ | — |
| 123 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Handover URL → lands on `/dashboard` already signed-in as the owner (avatar E), no login form (everything below is admin-gated). | hw274 | ⚠️ | hw274-2026-07-19: blocked — single-use signed handover URL unavailable to this walker (mothership GET /sovereign/api/v1/deployments/d51a69f885872a81 unauth → 401 {"error":"unauthenticated"}). Supporting: /dashboard with owner catalyst_session renders signed-in (avatar EB emrah.baysal@openova.io), 0 login inputs. [prereq-gated] |
| 124 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Catalog grid renders Blueprint cards in a tile grid, each with an icon + summary; the Alloy card is visible. | hw282 | ✅ | hw282-2026-07-21: /catalog renders tile grid (sov-catalog-grid) with 93 sov-app-card-* tiles; Alloy card visible with icon sov-app-icon-bp-alloy (/component-logos/alloy.svg) + summary 'Unified node agent for logs, metrics, and traces'; authed GET /api/v1/catalog 200 = 80 blueprints matching all 80 catalog-seed names (73/80 version-exact, 7 newer patches). |
| 125 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the Alloy card → the detail page renders: a hero (icon + name + summary + Edit IaC), an About section, and an Instances list; no login redirect. | hw282 | ✅ | hw282-2026-07-21: clicked sov-app-card-bp-alloy → /catalog/bp-alloy; hero renders (catalog-hero: cif-icon img /component-logos/alloy.svg + catalog-title + catalog-summary + Edit IaC catalog-detail-edit-iac), About section (catalog-section-about + catalog-description), instance surface = catalog-singleton-card (namespace/phase/open — Alloy is singleton, renders the platform-singleton instance card in place of a multi-row Instances list); 0 password inputs, no login redirect. |
| 126 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the admin Edit affordance in the hero → an edit form drops INLINE into the detail page (no modal overlay); fields appear in-place under the hero. | hw282 | ✅ | hw282-2026-07-21: clicked cif-icon-edit in hero → cif-icon-editor drops INLINE into the detail page with full field set (iconpicker-light/dark, url, upload, save/cancel); role=dialog count = 0 — no modal overlay. |
| 127 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | In the inline form, change Summary to `RECONCILE-PROOF-<ts>` → Save → the page refreshes in place and the new summary shows in the hero. | hw282 | ✅ | hw282-2026-07-21 (authed browser walk, Playwright owner session): filled 'RECONCILE-PROOF-hw282-g12' → Save → page refreshes in place, hero shows the new summary |
| 128 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Back on the grid, the Alloy card summary now reads `RECONCILE-PROOF-<ts>` — the edit propagated to the card, not just the detail page. | hw282 | ✅ | hw282-2026-07-21 (authed browser walk, Playwright owner session): back on /catalog grid the Alloy card summary reads 'RECONCILE-PROOF-hw282-g12' — edit propagated to the card |
| 129 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Hard-reload the detail page → the summary is still `RECONCILE-PROOF-<ts>` — the edit persisted across a reload, not an in-memory overlay. | hw282 | ✅ | hw282-2026-07-21 (authed browser walk, Playwright owner session): hard-reload of /catalog/bp-alloy still shows the string; server-side confirmed: GET /api/v1/catalog → bp-alloy card.summary='RECONCILE-PROOF-hw282-g12' (git-backed persist) |
| 130 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The non-card `version` field renders as a `v1.0.1` chip in the hero, and Edit IaC exposes the full CR including `version` for editing. | hw282 | ✅ | hw282-2026-07-21: hw274 FAIL fixed — version chip renders in hero (catalog-version = v1.0.2, matches seed pin 1.0.2) AND Edit IaC exposes the full CR incl version: complete editor doc via yaml-editor-input-copy clipboard = 2259 chars with 'version: 1.0.2' under spec; API GET /api/v1/catalog/bp-alloy .raw = full CR object (spec keys: card,endpoints,multiInstance,sso,topology,version). |
| 131 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Note the current hero logo (the Alloy glyph) as the baseline before an icon edit. | hw282 | ✅ | hw282-2026-07-21: baseline hero logo noted before any icon edit: cif-icon img src = /component-logos/alloy.svg (Alloy glyph). |
| 132 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit → in the Light-theme icon field paste a distinct image → Save → a "Saved to IaC ✓" confirmation shows and the page refreshes. | hw274 | ⚠️ | hw274-2026-07-19: paste+Save is a mutation; READ-ONLY walk. Icon editor opens with Light field (iconpicker-light-url) accepting a value + cif-icon-save present — save not clicked. [mutation-gated: lift on commit/funnel walk] |
| 133 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Observe the hero — it now shows the new logo; the render reads the edited `card.iconLight` first (IaC-first), not the bundled asset. | hw274 | ⚠️ | hw274-2026-07-19: depends on row-132 saved icon (mutation); not fired read-only. [mutation-gated: lift on commit/funnel walk] |
| 134 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Return to the grid — the Alloy card icon is now the new logo (the grid tile resolves the same edited `card.iconLight`). | hw274 | ⚠️ | hw274-2026-07-19: grid-card icon change depends on row-132 saved icon (mutation); not fired read-only. [mutation-gated: lift on commit/funnel walk] |
| 135 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Reload the detail page — the hero is still the new logo (render reads the persisted IaC icon on every load). | hw274 | ⚠️ | hw274-2026-07-19: reload-persistence of edited icon depends on row-132 Save (mutation); not fired read-only. [mutation-gated: lift on commit/funnel walk] |
| 136 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit again — the Light-theme icon field shows the current IaC value, falling back to the bundled asset only when IaC carries none. | hw282 | ✅ | hw282-2026-07-21: Edit → iconpicker-light-url value = '/component-logos/alloy.svg' + iconpicker-light-preview-img same; IaC carries no icon (API .raw.spec.card keys = [description,docs,family,title], iconLight=false) → shown value IS the bundled-asset fallback, exactly per criterion. |
| 137 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit → click the icon picker (`iconpicker-*`) → a thumbnail grid of vendored `component-logos/*` assets opens (a role=listbox grid of logo tiles). | hw282 | ✅ | hw282-2026-07-21: Edit → icon picker opens thumbnail grid: iconpicker-light-grid role=listbox with ~65 iconpicker-light-tile-* vendored component-logos tiles (alloy, cilium, cnpg, flux, grafana, keycloak … vpa), each role=option; matching iconpicker-dark-grid too. |
| 138 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click `cilium.svg` in the picker grid → the icon field + a live preview swatch update to the Cilium logo. | hw282 | ✅ | hw282-2026-07-21: clicked iconpicker-light-tile-cilium → iconpicker-light-url = '/component-logos/cilium.svg' AND live preview swatch iconpicker-light-preview-img src = '/component-logos/cilium.svg'; cancelled via cif-icon-cancel (hero still alloy.svg — no mutation leaked). |
| 139 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Save → reload — the hero is now the Cilium logo (the picker selection persisted to IaC and renders on hero + grid card). | hw274 | ⚠️ | hw274-2026-07-19: Save→reload persistence is a mutation; READ-ONLY walk — picker selection was cancelled, not saved. [mutation-gated: lift on commit/funnel walk] |
| 140 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | On Save, the durable-commit verdict (git outcome) is surfaced in-UI (the Edit-IaC `managed-by: manual • in sync` indicator + {stored:true,commit… not a silent store success. | hw282 | ⚠️ | hw282-2026-07-21: pre-commit chrome proven: yaml-editor-managed-by = 'managed-by: manual', Show diff + Validate (dry-run) + Commit IaC present; hw274 ANOMALY RESOLVED — Commit IaC (yaml-editor-apply) is DISABLED on fresh open and enables only after a keystroke (raw seed now matches stored file). Post-Save verdict surface ({stored:true,commit…}) needs a commit — mutation-gated. |
| 141 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Hover the summary line in the hero → a pencil/edit affordance appears ON the field (`cif-summary-edit` → `cif-summary-input`), inline, without opening the full form. | hw282 | ✅ | hw282-2026-07-21: pencil/edit affordance cif-summary-edit renders ON the summary field in the hero (visible on hover of cif-summary); click → cif-summary-input opens inline without the full form. |
| 142 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the summary → type a value → Save → only the summary updates in place; no full-form modal opens. | hw282 | ⚠️ | hw282-2026-07-21: click summary → cif-summary-input count=1 opens inline, role=dialog count=0 (no full-form modal) — proven; type+Save leg is a mutation, not fired READ-ONLY (Escape'd out). [mutation-gated] |
| 143 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Repeat the inline edit for the name field (`cif-name-edit` → `cif-name-input`) — it edits in place and saves just that field. | hw282 | ⚠️ | hw282-2026-07-21: cif-name-edit → cif-name-input opens in place (count=1, dialogs=0) — proven; 'saves just that field' leg is a mutation — not fired. [mutation-gated] |
| 144 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit IaC (`catalog-detail-ed… admin only) → the full `blueprint.yaml` opens in the YAML editor (the entire CR, not just 7 card fields). | hw282 | ✅ | hw282-2026-07-21: hw274 sparse-seed gone — Edit IaC (catalog-detail-edit-iac) opens the ENTIRE CR in the YamlEditor: full doc (clipboard via yaml-editor-input-copy) = 2259 chars: metadata helm annotations + 8 labels + spec.card/endpoints/manifests/placementSchema/source/sso/topology/version/visibility — far beyond 7 card fields; editor renders inline (dialogs=0). |
| 145 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Change a field in the editor → Commit → a Show-diff Current/Proposed side-by-side renders the change and the commit succeeds (confirmation appears). | hw282 | ⚠️ | hw282-2026-07-21: typed a local-only line → Show diff renders Current/Proposed side-by-side (yaml-editor-diff-left starts 'Current apiVersion: catalyst.openova.io/v1…', yaml-editor-diff-right contains the typed change) — proven; Commit leg is a mutation — change undone + editor closed uncommitted. [mutation-gated] |
| 146 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The editor subtitle states it directly: "Commit writes the IaC source of truth; Flux reconciles it… Both this editor and the card form above write the same file." | hw282 | ✅ | hw282-2026-07-21: editor subtitle renders verbatim: 'Commit writes the IaC source of truth; Flux reconciles it into the in-cluster Blueprint. Both this editor and the card form above write the same file.' |
| 147 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Open the WordPress detail page → Edit → change Summary → Save → reload → the summary persists, exactly as for Alloy. | hw282 | ⚠️ | hw282-2026-07-21: /catalog/bp-wordpress renders the same edit surface (cif-icon-edit/cif-name-edit/cif-summary-edit/catalog-detail-edit-iac all present, version chip v0.4.12) — proven; Save→reload persistence is a mutation — not fired. [mutation-gated] |
| 148 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | WordPress: Edit IaC → edit `spec.manifests` → Commit → reload — the same YamlEditor edits a structurally-different blueprint's manifests in place. | hw282 | ⚠️ | hw282-2026-07-21: hw274's broken precondition FIXED — WordPress Edit IaC full doc = 3843 chars and CONTAINS spec.manifests (chart: bp-wordpress-tenant) + version 0.4.12, so the manifests-edit walk can now start; the edit→Commit→reload leg is a mutation — not fired READ-ONLY. [mutation-gated] |
| 149 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | WordPress: Edit → Light-theme icon → distinct image → Save → reload — the WordPress hero icon visibly changes. | hw274 | ⚠️ | hw274-2026-07-19: icon Save→reload is a mutation; READ-ONLY walk — not fired. WordPress icon editor surface itself present (cif-icon-edit). [mutation-gated: lift on commit/funnel walk] |
| 150 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | PostgreSQL detail renders the SAME edit surface (hero icon, Edit IaC, clickable cards) and Edit IaC exposes `contextSchema` (a blueprint carrying contextSchema`/`sha… | hw282 | ✅ | hw282-2026-07-21: hw274 FAIL fixed — /catalog/bp-postgres renders the SAME edit surface (all 4 chrome ids true; badge-shareable + badge-multi-instance; chip v0.2.13) AND Edit IaC exposes contextSchema: full doc = 5553 chars containing 'contextSchema: kind: db / needs: name,owner / produces: credentialSecret / valuesKey: databases'; API .raw.spec has contextSchema=true. |
| 151 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Alloy + Postgres render the IDENTICAL edit chrome (same cif-icon-edit`/`cif… same Edit-IaC YamlEditor) — no blueprint-specific UI. | hw282 | ✅ | hw282-2026-07-21: Alloy + Postgres render IDENTICAL edit chrome: same cif-icon-edit/cif-name-edit/cif-summary-edit/catalog-detail-edit-iac and identical yaml-editor-* testid set (managed-by, toggle-diff, input, input-copy, input-cm, validate, apply) on both pages — no blueprint-specific UI. |
| 152 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The catalog detail page renders (hero · About · Instances) and opens an INLINE Edit form (no modal) — acceptance headline 1. | hw282 | ✅ | hw282-2026-07-21: acceptance headline 1 — detail page renders hero (catalog-hero) · About (catalog-section-about) · instance surface (catalog-singleton-card for singleton Alloy) AND edit forms open INLINE (cif-icon-editor / cif-summary-input in-page; role=dialog count=0 throughout). |
| 153 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | A summary edit Saves, updates the page AND the grid card, and persists across a reload — acceptance headline 2. | hw274 | ⚠️ | hw274-2026-07-19: summary-edit Save + grid propagation + reload persistence are mutations; READ-ONLY walk — not fired. [mutation-gated: lift on commit/funnel walk] |
| 154 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | A non-card field edit (`version`) persists — the whole CR is editable, not a 7-field overlay — acceptance headline 3. | hw282 | ⚠️ | hw282-2026-07-21: hw274's read-only DISPROOF reversed — the whole CR IS editable now: Edit-IaC editor carries spec.version on all 3 blueprints walked (alloy 2259ch v1.0.2 / wordpress 3843ch v0.4.12 / postgres 5553ch v0.2.13, full clipboard reads); the 'persists' leg needs a commit — mutation-gated. |
| 155 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The edited icon renders on hero + grid + survives reload; the form pre-fills the IaC icon; the picker grid works — acceptance headline 4. | hw282 | ⚠️ | hw282-2026-07-21: non-mutating halves proven this walk: form pre-fills the IaC/fallback icon (iconpicker-light-url=/component-logos/alloy.svg with IaC carrying none) + picker grid works (listbox grid, cilium tile click updates field+preview); edited-icon render/persistence needs the icon Save mutation. [mutation-gated] |
| 156 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Save surfaces the IaC-commit verdict (`committed:true` + `• in sync`), not a bare store success — acceptance headline 5. | hw274 | ⚠️ | hw274-2026-07-19: committed:true verdict surface requires a Save/Commit mutation; READ-ONLY walk. Pre-commit chrome ('managed-by: manual', Show diff, Commit IaC) present — see row140 anomaly. [mutation-gated: lift on commit/funnel walk] |
| 157 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Per-field inline edit for cards (`cif-*`) + the full-CR YamlEditor (Edit IaC) for the rest, both writing the same IaC source — acceptance headline 6. | hw282 | ⚠️ | hw282-2026-07-21: both write-surfaces present on one page — per-field cif-* inline inputs (summary/name/icon) AND the full-CR YamlEditor via Edit IaC — with the subtitle stating both write the same file; empirical same-file proof requires a commit — mutation-gated. |
| 158 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The identical edit mechanism works on a 2nd + 3rd blueprint — no per-blueprint UI (Alloy + Postgres + a third) — acceptance headline 7. | hw282 | ⚠️ | hw282-2026-07-21: identical edit-mechanism chrome verified on 3 blueprints (bp-alloy, bp-postgres, bp-wordpress): same cif-* + catalog-detail-edit-iac + identical yaml-editor-* testids on all three, no per-blueprint UI; 'works' (save round-trip) needs mutation — not exercised READ-ONLY. [mutation-gated] |
| 159 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Settings → Sovereignty section renders a "Cluster sovereignty" panel with a "TETHERED" badge + an "Achieve True Sovereignty" cutover CTA (runs the 8-step cutover + 10-min egress-block self-test). | hw255 | ⛔ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Settings -> Sovereignty renders 'Cluster sovereignty' panel: badge SOVEREIGN, 'This Sovereign is operationally independent of the openova-io mothership', EGRESS TEST passed, 'All 11 steps completed. 100%' with the per-step list + timestamps. Screenshot row159-settings-sovereignty.png. — TETHERED badge + 'Achieve True Sovereignty' CTA are the PRE-cutover render — permanently unsurfaceable on hw255 (cutoverComplete=true 07:21Z; the CTA demonstrably fired and worked). Minor inconsistenc… |
| 160 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Console nav + Settings sidebar expose a dedicated "Sovereignty" anchor (`#sovereignty`) that scrolls to + highlights the Cluster-sovereignty panel — the cutover trigger is a first-class surface. | hw282 | ✅ | hw282-2026-07-21 (authed browser walk, Playwright owner session): Settings sidebar exposes a dedicated 'Sovereignty' entry/anchor (sections: Organization/Sovereign/API tokens/…/Danger zone/Sovereignty) |
| 161 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Open `/jobs` (zero-login, signed in as owner) → the canvas table renders a populated activity list (not a spinner, empty state, or login redirect). | hw282 | ✅ | hw282-2026-07-21 (authed browser walk, Playwright owner session): /jobs renders populated activity table as signed-in owner (no spinner/login redirect); 'Live state stream re-attached. Refreshing from the catalyst-api every 5s' |
| 162 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Find the `cutover` group row and expand it → it renders the 11 `cutover-step-*` rows (gitea-mirror, harbor-projects, harbor-prewarm, registry-pivot, … vcluster-registry-p… — the 11-step execution tree. | hw282 | ✅ | hw282-2026-07-21 (authed browser walk, Playwright owner session): cutover group expands to the cutover-step-* rows: registry-pivot, gitea-mirror, harbor-projects, harbor-prewarm, flux-gitrepository-patch, helmrepository-patches, catalyst-api-env-patch, egress-block-test, gitea-token-mint, crossplane-provider-pivot (+vcluster-registry-pivot in APP filter) |
| 163 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Each `cutover-step-*` row reads an honest per-step status (Succeeded / Running / Failed / Pending), never a premature green. | hw282 | ✅ | hw282-2026-07-21 (fleet walker): All 11 cutover-step-* rows read succeeded with real timestamps matching the self-sovereign-cutover-status CM per-step result=success exactly (egress-block-test 19:51:06->20:16:15Z = 25m09s hold; harbor-prewarm 18:42:53->19:42:03Z) - statuses derive from the CM ground truth, no premature green. |
| 164 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | The cutover group status reflects its real children (a group with a failed child reads failed, not a fake Succeeded). | hw282 | ✅ | hw282-2026-07-21 (fleet walker): Live proof both directions: reconcilers group status=failed reflecting failed child task-syft-sbom (k8s Job syft-grype-bp-syft-grype-29742870 genuinely Failed 0/1 in BOTH region clusters), while cutover group=succeeded only because all 11 step children are succeeded. |
| 165 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | On a failed `cutover-step-*` row, a Re-run button is present (per-row, gated to Failed) — the operator can re-drive a failed cutover step from the browser. | hw255 | ⛔ | hw232 Playwright UI (hw232-ui-*): no Failed cutover step present (7✓/1▶/3⏳) so Re-run affordance not observable; re-walk if a step fails ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser + wire): no Failed cutover-step row exists to observe — /jobs shows all cutover STEP/TASK rows SUCCEEDED (registry-pivot, gitea-mirror, harbor-prewarm, egress-block-test 25m57s, crossplane-provider-pivot…); GET /api/v1/sovereign/jobs: only failed items are 3 historical vpc-podcidr-route-reconciler CronJob runs whose cron row aggregates SUCC… — Same not-observable state as the hw232 ⚠️ stamp; re-walk if a cutover step ever fails. |
| 166 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | (After a COMPLETE cutover) the `cutover` group reads all-11-green on `/jobs` — every step Succeeded, including `egress-block-test` and `registry-pivot`. | hw282 | ✅ | hw282-2026-07-21 (fleet walker): API leg (the /jobs data source): cutover group succeeded with 11/11 cutover-step-* rows succeeded including egress-block-test (19:51:06->20:16:15Z) and registry-pivot (19:42:03->19:42:18Z); cutover/status API + CM confirm cutoverComplete=true progressPercent=100. |
| 167 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Open the console root in a fresh tab → land on the operator dashboard signed in as the sovereign-admin (no login form, no password field). | hw282 | ✅ | hw282-2026-07-21 (authed browser walk, Playwright owner session): console root lands on the operator dashboard signed in as sovereign-admin (owner session; no login form, no password field) |
| 168 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Open `/jobs` → the canvas table renders a populated list of activity rows (not a spinner, empty state, or login redirect). | hw282 | ✅ | hw282-2026-07-21 (authed browser walk, Playwright owner session): /jobs renders a populated activity list (not spinner/empty/login redirect) — same walk as row 161, jobs epic leg |
| 169 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | The Kind column is present in the header and each row shows its kind; full header = Name·Kind·App·Deps·… | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Data leg proven: 176/176 rows carry a non-empty typed kind (134 install, 16 task, 11 step, 6 cron, 5 lifecycle, 4 group), dependsOn present 176/176, appId 156/176; the header-column render (Name-Kind-App-Deps) needs-browser-render. |
| 170 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Scroll/search to the `install-openbao` row → it renders green / Succeeded (the install is honestly green). | hw282 | ✅ | hw282-2026-07-21 (fleet walker): install-openbao AND install-me-east-215-b-1:openbao both status=succeeded, and the green is honest: HR bp-openbao Ready=True (bp-openbao@1.2.63 install succeeded) + openbao-0 1/1 Running in both regions. |
| 171 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Every rendered row maps to a real HelmRelease install / terraform stage (no placeholder, no synthetic/fabricated entry). | hw282 | ✅ | hw282-2026-07-21 (fleet walker): Perfect bijection: 134 install rows = 67 unprefixed + 67 install-me-east-215-b-1:* and the name-sets are IDENTICAL to the live HR sets (diff install-<name> vs kubectl bp-<name> = 0 lines in BOTH regions); cron rows map to real CronJobs (cnpg-cabundle-reassert, openbao-snapshot-save live, */10 schedule) - zero synthetic entries. |
| 172 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Set the Status filter to `failed` → the table shows the genuinely-failing rows, each with an honest failed status (the Status filter works). | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Failed-set honesty proven at the data layer: the only failed rows (task-syft-sbom + parent reconcilers group) match the only genuinely failing workload (Job syft-grype-bp-syft-grype-29742870 Failed 0/1 in both clusters) while all 63+60 active HRs are Ready=True (the 4+7 non-Ready are spec.suspend=true by design); the Status-filter dropdown click needs-browser-render. |
| 173 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Leave the table on screen ~30s → rows update live (tail) as reconciliation progresses; a status badge changes in place without a manual reload. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Data-source live-tail proven: two authed GETs 45s apart diffed - 6 cron rows advanced in place (runCount 1->2, 1->4, 4->5; cron-cnpg-cabundle-reassert flipped pending->succeeded) and 2 new task rows appeared (task-cert-nextkey-guard, task-legacy-cert-cleanup); the in-place badge re-render without reload needs-browser-render. |
| 174 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | On a Failed row (Status=failed), a Re-run / Retry-reconcile button is present on the row (visible on the row or on hover). | hw269 | ⚠️ | hw269-2026-07-18: same as hw268 — 0 Failed rows exist on this env at walk-time (152 Succeeded/10 Pending/4 Running/0 Failed), so the Re-run/Retry control cannot be exhibited on a genuine Failed row this walk; gating re-confirmed via row 175 (no control renders on Succeeded rows). ‖ hw268-2026-07-17: no Failed row present to exhibit the retry control (env cleanly converged, 0 failures); gating confirmed inversely via row 175 (screenshot(s) docs/sessions/2026-07-17/hw268-authed-console/). ‖ — |
| 175 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | On a Succeeded / healthy / Confirming row, NO Re-run button renders — the control is gated to Failed rows only. | hw282 | ☐ | hw282-2026-07-21 (fleet walker): needs-browser-render: proving button-ABSENCE on the 173 Succeeded rows is strictly a DOM/render check with no API leg. |
| 176 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Click Re-run on a Failed row → a success toast/feedback appears and the button flips in place (e.g. `Requesting…`) — the browser triggered a re-reconcile, no terminal. | hw261 | ⚠️ | hw261-2026-07-16 (authed owner console walk): the retry write was FINALLY exercised on a genuine Failed row (hw255/hw241 had 0 failed rows) — clicked jobs-retry on OpenBao Snapshot Save → `POST /api/v1/deployments/fe9eb3995b594b79/jobs/cron-openbao-snapshot-save/retry` → **502 Bad Gateway** (no success toast). Root cause is the env tail #5142: openbao-0 SEALED post-region-kill → snapshot cron + seed-reconcile fail + catalyst-api unstable (2 restarts). Control fires the correct write; the 502 is the sealed-openbao/catalyst-api cascade, not a retry-handler defect. Re-verify on a fresh non-region-killed Sovereign. |
| 177 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Use the same Re-run button on a Failed row — one remediation mechanism across rows, no per-kind UI (the single-table/single… shape). | — | ⚠️ | hw270 partial: Re-run precondition absent — /jobs has 0 Failed rows (171 Succeeded, 2 Pending); ACTIONS renders — on non-failed rows (single remediation mechanism present); cannot exercise Re-run without a Failed row. hw270-cutover-jobs-recon/row177-jobs-failed-filter-empty.png |
| 178 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | The signed handover URL → lands directly on `/dashboard` signed-in (env switcher shows the live env, avatar E, no login form). | hw274 | ⚠️ | hw274-2026-07-19: prerequisite absent — the single-use signed handover URL is not available to this walker (only the minted catalyst_session cookie). Landed-state halves verified separately: /dashboard signed-in, env switcher hw274.omantel.biz, owner avatar, no login form (row 1/167); the handover-URL flow itself not exercisable. [prereq/mutation-gated] |
| 179 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Click the avatar (top-right) → menu reads "Signed in as the owner" with a Sign-out item — confirms the landed identity is the owner-admin. | hw282 | ⚠️ | hw282-2026-07-21 (fleet walker): Identity leg proven: GET /api/v1/whoami 200 -> email emrah.baysal@openova.io, tier=owner, roles [catalyst-owner,catalyst-admin,sovereign-admins] (the landed identity IS the owner-admin); the avatar-menu click render needs-browser-render. | emrah.baysal@openova.io | Sign out' — landed identity is the owner-admin, Sign-out item present (not clicked). |
| 180 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Grafana bare URL → lands on Grafana Home ("Welcome to Grafana", full UI, Profile avatar), `?orgId=1`, no login form (SSO landed signed-in). | hw282 | ☐ | hw282-2026-07-21 (fleet walker): needs-browser-render: SSO chain is live - https://grafana.hw282.omantel.biz redirects to auth.hw282.omantel.biz/realms/sovereign/broker/catalyst-pin/login?client_id=grafana (curl without a keycloak session terminates 400 at the broker); the signed-in Grafana Home render requires a browser SSO session. |
| 181 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Harbor (registry) bare URL → lands on `/harbor/projects` (projects, repos, Administration nav), no login form; user dropdown the owner. | hw280 | ⚠️ | hw280-2026-07-20 cc=true: #5278 route-wiring VALIDATED — catalyst-api serves /oidc internally (200, valid discovery doc api.hw280/oidc/auth); CATALYST_PIN_BROKER_CLIENT_SECRET wired+non-empty; pod rolled post-secret. BUT external api/oidc FLAPS 200/404 (~60% 404) — multi-region gateway gap (Huawei ELB fronts both regions' cilium-envoy; region-b envoy lacks the api-host route). Intermittent SSO broker. Fix-agent a908c94e in flight. Route-wiring fixed; gateway propagation is the residual blocker. |
| 182 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Gitea bare URL → lands on the gitea dashboard titled "emrah.baysal - Dashboard - Catalyst Gitea", logged in; URL stays on :443. | hw282 | ☐ | hw282-2026-07-21 (fleet walker): needs-browser-render: https://gitea.hw282.omantel.biz auto-kicks OAuth to auth.hw282.omantel.biz catalyst-pin broker client_id=gitea (redirect chain proven via curl -L, terminates 400 sessionless); the logged-in dashboard render requires a browser SSO session. |
| 183 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | OpenBao bare UI → final rendered screen is the authenticated Vault session (`/ui/vault/secrets`, Secrets Engines with cubbyhole/ + secret/ kv), NO `/ui/vault/auth` token form. | hw280 | ⚠️ | hw280-2026-07-20 cc=true: #5278 route-wiring VALIDATED — catalyst-api serves /oidc internally (200, valid discovery doc api.hw280/oidc/auth); CATALYST_PIN_BROKER_CLIENT_SECRET wired+non-empty; pod rolled post-secret. BUT external api/oidc FLAPS 200/404 (~60% 404) — multi-region gateway gap (Huawei ELB fronts both regions' cilium-envoy; region-b envoy lacks the api-host route). Intermittent SSO broker. Fix-agent a908c94e in flight. Route-wiring fixed; gateway propagation is the residual blocker. |
| 184 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | — | [UAT.md](https://github.com/openova-io/openova/blob/main/docs/ledger/UAT.md) | ⚠️ | predecessor-walk(B): No test case defined in ledger (literal —). ‖ §I 91dc0591: no test case defined in ledger (literal —) — not click-verified, harness artifact |
| 185 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | No stale predecessor-env references anywhere in the rendered `UAT.md` (the table names only the live env). | hw255 | ❌ | hw224-fulltable-2026-07-04: UAT.md rows walked this pass name only hw224; prior-env blocks labeled stale by construction ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): git show origin/main:docs/ledger/UAT.md / grep -oc per env: hw241=58, hw228=57, hw224=43, hw240=35, hw232=27, hw227=12, hw253=8, hw234=7, hw252=4 stale predecessor-env references; live env hw255=0 mentions — the rendered table does not name the live env at all. — reset-uat for hw255 not yet run/landed on main |
| 186 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | — | [UAT.md](https://github.com/openova-io/openova/blob/main/docs/ledger/UAT.md) | ⚠️ | predecessor-walk(B): No test case defined in ledger (literal —). ‖ §I 91dc0591: no test case defined in ledger (literal —) — not click-verified, harness artifact |
| 187 | topology | [#4001](https://github.com/openova-io/openova/issues/4000) | Per-app Topology for a multi-region app (grafana) shows **Pattern: active-active** with 2 PRIMARY region cards — NOT singleton. | hw255 | ❌ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): bp-grafana Topology tab renders 'Pattern: singleton / No placement targets reported yet. / Status: Loading status…' — NOT 'Pattern: active-active', zero PRIMARY region cards. Contrast: bp-cilium on the same env correctly derives active-active with 2 PRIMARY cards, so the surface works when runtime spans both regions. Screenshot row187-grafana-topology.png — Root: grafana runs only in region-a on hw255 (region-b HR plane 0/0, secondaryDegraded=true; fleet endpoint reports grafana regions [me-east-215-a]) — the multi-region premise never materialized on re… ‖ hw255-RECLASSIFIED 2026-07-15 ~11:45Z (#5107 RCA): region-b is NOT degraded — live kubectl on the region-b cluster shows 65 HRs / 58 Ready (exact hw235/hw236 healthy-secondary canon; the 7 blank = the by-design suspended DR set). The 'hrReady 0/0 + secondaryDegraded=true' deployment record is a FALSE POSITIVE from the #5084 flux-CRD probe (hw255 = its first prov: terminal absent-verdict + 5m budget vs #3129 PUT-early + PUT-back loop rejecting HTTP 201). Fix merged PR #5107 (self-healing verdict, 15m budget, 2xx accept); hw255's frozen record heals on the next prov. The RENDER defect stands (the surface reads the false record) but the root is the record, not region-b. |
| 188 | topology | [#4001](https://github.com/openova-io/openova/issues/4000) | A genuine single-region app (catalyst-api) correctly shows **singleton** (no false multi-region). | — | ⚠️ | hw270 partial: no standalone catalyst-api console app surface; control-plane catalyst-platform topology=undefined→singleton, ABSENT from non-singleton DR set (only gitea/harbor/keycloak/openbao/postgres multi-region); kubectl no catalyst-api pod region-b — NOT falsely multi-region, no dedicated singleton badge to screenshot. |
| 189 | topology | [#4000](https://github.com/openova-io/openova/issues/4000) | Region-b kubeconfig self-heals EIP→private-IP **zero-touch** on restart (topology survives a fresh prov). | — | ☐ | — |
| 190 | jobs | [#3916](https://github.com/openova-io/openova/issues/3916) | `/jobs` lists **ONLY finite jobs** (provision/cutover steps, batch Jobs, CronJob runs) — ZERO continuous-reconciler rows. | hw274 | ⚠️ | hw274-2026-07-19: AUTHED API leg — GET /api/v1/deployments/<dep>/jobs → 167 rows, ALL typed kinds {install:132, step:11, task:9, cron:6, lifecycle:5, group:4}, ZERO missing/raw-HR/Kustomization kinds. Render leg deferred to the browser fleet. |
| 191 | jobs | [#3916](https://github.com/openova-io/openova/issues/3916) | Continuous reconcilers (HelmRelease/Kustomization) do **NOT** appear in `/jobs` (they live in the recon lens). | hw274 | ⚠️ | hw274-2026-07-19: AUTHED API leg — jobs list surfaces the LIVE cutover chain as typed rows (18 cutover entries: cutover-step-* + task-cutover-* for harbor-prewarm/harbor-projects et al) while the cutover is mid-run. No fabricated/blank lifecycle group. Render leg deferred to the browser fleet. |
| 192 | recon | [#3925](https://github.com/openova-io/openova/issues/3996) | The convergence **Reconciliation** link opens the cloud **RECON lens** (`view=graph&lens=reconciliation`), not the default cloud lens. | hw274 | ⚠️ | hw274-2026-07-19: prerequisite absent — the convergence Reconciliation link (wizard-link-reconciliation in ConvergenceWizard, route /provision/<depId>) is a mothership provisioning surface: on this console /provision/d51a69f885872a81 redirects to /dashboard and console.openova.io/provision/<id> returns 404 to this walker. Destination verified: /cloud?view=graph&lens=reconciliation renders with cloud-architecture-lens=reconciliation + reconciler nodes, not the default cloud lens. [prereq/mutation-gated] |
| 193 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Clicking a reconciler opens the **ArgoCD-like management surface** (drill-in). | hw255 | ❌ | hw241: Cloud graph infra-only (no reconciler/Helm nodes), no drill panel — #3996 gap persists since hw239 ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Cloud -> Reconciliation lens DOES render reconciler nodes (89 nodes/117 edges; chips HelmRelease 65/65, Kustomization 10/10, Application 6/6, Continuum 5/5; 'Reconcile Bootstrap Kit/Catalog Sovereign/Org Tenants…' + every bp-* HR) but clicking reconciler nodes (bp-gitea click, spine-gitea dblclick, Reconcile Bootstrap Kit) opens NO drill-in: body text length unchanged (1951 cha… — #3996 ArgoCD-like drill-in gap persists (hw239->hw241->hw255), though the lens itself is materially better than hw241 (reconciler/Helm nodes now present). |
| 194 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Recon surface lists Flux reconcilers with live status (Reconciled/Reconciling/Degraded/Suspended). | — | ⚠️ | hw270: 13/13 kustomizations True + HR status-diversity present in backing model; Recon-lens UI render needs browser walk. region-a. |
| 195 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Drill a reconciler → its controller **logs** render. | — | ⚠️ | hw270 partial: reconciler-logs endpoint 200 with real helm-controller lines (21 filtered to bp-alloy incl. triggered reconcile), but UI drill-in ResourceDetailPage blocked by a catalyst-api SA Unauthorized env-degradation (resource-GET 500) — backend works, UI render blocked. hw270-cutover-jobs-recon/row195-alloy-reconcile-tab-logs.png |
| 196 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | **Reconcile** action → `reconcile.fluxcd.io/requestedAt` lands on the live object. | hw274 | ⚠️ | hw274-2026-07-19: mutation-gated (read-only walk) — the Reconcile action IS present: clicking reconciler node 'Reconcile Sovereign TLS' on the RECON lens opens the drill-in with reconciler-drill-action-reconcile (plus logs refresh); firing it to assert reconcile.fluxcd.io/requestedAt is a mutation — not fired. [prereq/mutation-gated] |
| 197 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | **Suspend/Resume** → `spec.suspend` flips on the live object. | hw274 | ⚠️ | hw274-2026-07-19: mutation-gated (read-only walk) — Suspend control present in the reconciler drill-in (reconciler-drill-action-suspend alongside -reconcile and -close); flipping spec.suspend on the live object is a mutation — not fired. [prereq/mutation-gated] |
| 198 | cloud | [#3987](https://github.com/openova-io/openova/issues/3987) | `/cloud` per-kind **helmreleases** page shows the real count (~65), not 0. | — | ⚠️ | hw270 partial: kubectl — 66 HelmReleases region-a (63 Ready=True), real count ~66 not 0; /cloud per-kind helmreleases UI render not captured this pass. |
| 199 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **Gateway** page shows the live cilium Gateways (2: `cilium-gateway` + `cilium-gateway-console`). | hw282 | ✅ | hw282-2026-07-21 (authed browser walk, Playwright owner session): RENDER leg: Cloud Gateways page renders the 2 live cilium Gateways (kube-system/cilium-gateway class=cilium 3 listeners + cilium-gateway-console); backing leg hw274 ⚠️ → dual-leg complete |
| 200 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **HTTPRoutes** page shows 15. | hw282 | ✅ | hw282-2026-07-21 (authed browser walk, Playwright owner session): RENDER leg: HTTPRoutes page renders live routes (catalyst-system/catalyst-api api.hw282.omantel.biz → kube-system/cilium-gateway-console, …); backing leg hw274 |
| 201 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **NetworkPolicies** page shows the live policies (30 live; expected ~10 stale). | hw282 | ✅ | hw282-2026-07-21 (authed browser walk, Playwright owner session): RENDER leg: NetworkPolicies page renders live policies (alloy/alloy-default-deny (all) 2/3 Ingress,Egress, …); backing leg hw274 (30 live region-a) |
| 202 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **CiliumNetworkPolicies** page shows the live policies (42 live; expected 5 stale). | hw282 | ✅ | hw282-2026-07-21 (authed browser walk, Playwright owner session): RENDER leg: CiliumNetworkPolicies page renders live CNPs (alloy/alloy-allow-apiserver-egress, …); backing leg hw274 (44 region-a) |
| 203 | cloud | [#4002](https://github.com/openova-io/openova/issues/4002) | Cloud **Load Balancers** page shows the real LoadBalancer Svc (clustermesh-apiserver EXTERNAL-IP 212.72.24.31), not 0. | hw282 | ✅ | hw282-2026-07-21 (authed browser walk, Playwright owner session): RENDER leg: Load Balancers page renders 2/2 HEALTHY (both regions, listeners tcp:80/443/6443); backing leg hw274 (real LB Svcs) |
| 204 | cloud | [#4002](https://github.com/openova-io/openova/issues/4002) | Cloud **Worker Nodes** page shows the real nodes, not 0. | hw282 | ✅ | hw282-2026-07-21 (authed browser walk, Playwright owner session): RENDER leg: Worker Nodes page renders 8/8 HEALTHY table (cp+3 workers × 2 regions, SKU m7n, kubelet v1.30) — matches kubectl 8 nodes; API leg already ✅ |
| 205 | fleet | [#4003](https://github.com/openova-io/openova/issues/4003) | `/fleet/applications` returns the real app count (non-zero), not 0. | hw282 | ✅ | hw282-2026-07-21: `GET /api/v1/fleet/applications` (authed owner) → **70** apps, non-zero (alloy/catalyst-platform/continuum/openova-mcp/cnpg-pair… mostly Ready). |
| 206 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | `kubectl get managed` is non-empty — Crossplane observes the OpenTofu-built infra. | hw282 | ✅ | hw282-2026-07-21 (dep 09696223c939cf29, region-a): `kubectl get managed` = **20** resources non-empty; `provider-opentofu` Healthy=True; cloudadoptions (eip-cp-a/b, elb-console/primary) Synced=True Ready=True (observing tofu-built infra). kubectl live. |
| 207 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | A `CloudAdoption` for the real ELB reaches Synced+Ready (Observe), `external-name` = the real ELB id. | hw281 | ✅ | hw281 24 adopt-* Workspaces Synced=True Ready=True (real HW external-names) |
| 208 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | Adoption is **Observe-only** — the live ELB/nodes are untouched (no re-provision). | hw281 | ✅ | hw281 24 adopt-* Synced+Ready observe-only (provider-opentofu) |
| 209 | storage | [#3971](https://github.com/openova-io/openova/issues/3971) | PVCs land on a real **CSI storageclass** (not k3s local-path). | hw282 | ✅ | hw282-2026-07-21 (dep 09696223c939cf29, region-a): 42 PVCs Bound, **0 on local-path**; default SC `evs-ssd`=`evs.csi.huaweicloud.com` (CSI), `seaweedfs-storage`=`csi.hetzner.cloud` (CSI). kubectl live. |
| 210 | storage | [#3971](https://github.com/openova-io/openova/issues/3971) | `local-path` is **FORBIDDEN** (k3s `--disable=local-storage`). | hw282 | ✅ | hw282-2026-07-21 (dep 09696223c939cf29, region-a): `local-path` storageclass **ABSENT**; only CSI SCs present (`evs-ssd` default + `seaweedfs-storage`). kubectl live. |
| 211 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP **sovereign-admin** token → `list_applications` returns all apps. | hw282 | ✅ | hw282-2026-07-21: owner bearer (whoami: tier=owner, context=sovereign, dep 09696223c939cf29) → tools/call list_applications returns ApplicationList total=9 (spine-gitea, spine-harbor, spine-keycloak, spine-openbao, guacamole, newapi, shared-pg, shared-pg-b, shared-pg-c) = exact match of the 9 Application CRs via kubectl; tools/list exposes 6 tools; no-bearer tools/call fails closed with JSON-RPC -32001 'unauthenticated'. Caveat (availability, not RBAC): mcp.hw282.omantel.biz flaps ~40% envoy-404 (shared ELB 212.72.24.31 fronts both regions; mcp HTTPRoute exists only in region-a, bp-openova-mcp suspend=true in region-b) — see notes. |

| 212 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP **Org-scoped** token → `list_applications` returns ONLY that Org's apps (RBAC parity). | hw274 | ⚠️ | hw274-2026-07-19: no Org-scoped RS256 bearer obtainable read-only — the funnel member token is an HS256 marketplace JWT (MCP requires RS256 session) and the per-Org catalyst_session cookie is httpOnly; positive Org-scoped case needs a minted Org session (mutation). [prereq-gated] |

| 213 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP cross-Org `get_application` → **403** (UI-parity). | hw274 | ⚠️ | hw274-2026-07-19: same prerequisite as row 212 — cross-Org get_application 403 needs an Org-scoped RS256 bearer; not mintable read-only this walk. [prereq-gated] |

| 214 | orgs | [#3985](https://github.com/openova-io/openova/issues/3985) | No `SME`/`tenant` banned-term leak in the console bundle (org-rename complete). | hw274 | ❌ | hw274-2026-07-19: bundle assets/index-C08Ow5B6.js — 'SME'=0; rendered-copy fixes ARE deployed (no 'tenant=dmz' taint placeholder, no display-text hits) BUT 73 'tenant' tokens persist: X-Tenant-Host header, sov-console-tenant-*/org-create-tenant-* testids, /bss/tenants + /org/tenants/new routes, throwable 'invalid TenantID:' strings. No-leak criterion unmet. |
| 215 | orgs | [#3985](https://github.com/openova-io/openova/issues/3985) | CRD `tier` enum is `[org, corporate]` — a `tier: org` Organization is accepted. | hw281 | ✅ | hw281 Organization CRD tier enum; seed org hw281-omani-works spec.tier=org |

---

### Governance train — items 187-215 (added 2026-06-21)

These extend the canonical 186-case walk with this session's governance work. **Status is deliberately conservative** (no premature ✅): `⏳` = built/merged in code but **NOT yet validated on the durable live console** (the agents' temporary image rolls were Flux-reverted, so they are not yet what the operator sees — each needs a real end-user walk after the durable roll); `◑` = mechanism proven (e.g. against live HCS) but full in-cluster proof lands on a fresh prov; `❌` = not built yet (fix in flight) or genuinely requires a clean re-prov. A row flips `✅` only after an operator end-user walk on the durable console with pasted evidence, per the TRUST-ledger discipline.
| 216 | e2e-journey | [#3376](https://github.com/openova-io/openova/issues/3376) | A user with a **coupon/voucher code** redeems it → creates **his Organization**. Surface: `https://marketplace.<fqdn>/redeem/?code=<CODE>` (no session) → plan/region → Launch. PASS = the provisioning timeline runs to Done AND `/organizations` shows the Org **ACTIVE** backed by live `vcluster` pods (not a fake-green status). | hw274 | ⚠️ | hw274-2026-07-19: funnel redeem→Org walk is mutation-gated for this read-only session; a concurrent session's funnel run created Org acme274 mid-walk (vcluster-0 Running, 3 tenant Kustomizations Ready) — not this walker's evidence to stamp. [prereq-gated] |
| 217 | e2e-journey | [#3374](https://github.com/openova-io/openova/issues/3374) | The user logs in via the **passwordless SSO magic-email PIN** (any domain, e.g. `demo@openova.io`) → 6-digit PIN emailed → lands signed-in, no password. PASS = the PIN actually arrives (verify IMAP `mail.openova.io:993`) AND entering it lands signed-in as the CUSTOMER (their email in the avatar, their Org in the header) with no password field — this is the customer PIN flow, NOT the operator handover. | hw274 | ⚠️ | hw274-2026-07-19: customer PIN-login journey requires minting a session + IMAP PIN retrieval — mutation-gated this walk. [prereq-gated] |
| 218 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | **chepherd is installable from the catalog as an Application** (`bp-agenity` **Helm chart**). Surface: per-Org console → Catalog → the `bp-agenity` card renders + the install wizard opens with the Org's Environment pre-selected as the target. PASS = card present + wizard opens (the chart resolves from the catalog, not 404). | hw274 | ⚠️ | hw274-2026-07-19: per-Org console Catalog card + wizard render needs an Org member browser session (funnel-gated); not available read-only. [prereq-gated] |
| 219 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4180](https://github.com/openova-io/openova/issues/4180) [#4553](https://github.com/openova-io/openova/issues/4553) | The user **provisions chepherd** as an Application → it converges (Helm deploys, pods Ready, chepherd console reachable in his Org). | hw274 | ⚠️ | hw274-2026-07-19: provisioning chepherd is an install mutation; not walkable read-only. [prereq-gated] |
| 220 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4111](https://github.com/openova-io/openova/issues/4111) [#4556](https://github.com/openova-io/openova/issues/4556) | chepherd's **solo agent is pre-configured with Claude Opus 4.7** + a working token (the agent is live and chat works). | hw274 | ❌ | hw274-2026-07-19: pre-configured-token claim disproven read-only — kubectl get secrets -A grep anthropic|claude → 0 cluster-wide; 0 agenity/chepherd pods (region-a). No live agent with a working token exists. |
| 221 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | The user **chats with the chepherd solo agent** ("create a `<blueprint>` app in my org") → the agent calls the openova-mcp `create_application` tool with the user's **Org-scoped** token → a new Application CR is created in the user's Org. PASS = the agent reports the new app name + an Application appears provisioning in the user's `/apps`, with NO operator-console action. | hw274 | ⚠️ | hw274-2026-07-19: no solo-agent runtime deployed (0 agenity/chepherd pods) — needs the row-219 install mutation; the chat leg itself mutates. Note: networked openova-mcp IS live (mcp.hw274.omantel.biz), so the MCP-side prerequisite has landed. [prereq-gated] |
| 222 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | The agent-created application **converges and appears in the user's Org** (chat-driven app creation works end-to-end). | hw274 | ⚠️ | hw274-2026-07-19: same chain as row 221 — agent runtime absent; chat-driven app-creation convergence not walkable read-only. [prereq-gated] |
| 223 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4553](https://github.com/openova-io/openova/issues/4553) | The chepherd agent's actions are **RBAC-scoped to the user's Org** (UI-parity): the openova-mcp `list_applications` returns ONLY this Org's apps (a 2nd Org's are filtered out), a cross-Org `get_application` → **403**, and the agent cannot create/mutate in any other Org — enforced by `openova-mcp/internal/identity` Org-claim, not just the chat prompt. | hw274 | ⚠️ | hw274-2026-07-19: needs live agent runtime + Org-scoped RS256 token (rows 219/212 prerequisites); live RBAC enforcement not exercisable read-only. [prereq-gated] |
| 224 | convergence | [#4272](https://github.com/openova-io/openova/issues/4272) | Per-Org `bp-openclaw` controller reaches Ready (1/1) on a Sovereign — the idle-reaper can list its per-user pods and `readyz` passes. | hw274 | ⚠️ | hw274-2026-07-19: no funnel Org with the openclaw bundle — no bp-openclaw HR/pod cluster-wide (kubectl); acme274 (created mid-walk by a concurrent session) is a vcluster-plan Org still converging (only vcluster-0 + synced coredns). [prereq-gated] |
| 225 | convergence | [#4278](https://github.com/openova-io/openova/issues/4278) | Per-Org `bp-newapi` HR reaches Ready on a fresh Org — the admin-promote post-install hook Completes (its ServiceAccount exists) so the chart finishes. | hw281 | ✅ | hw281 bp-newapi HR Ready BOTH regions (region-a + region-b) |
| 226 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The customer's PURCHASED app actually RUNS — every funnel Org's `tenant-<slug>-apps` Kustomization reconciles into a running pod (the app is reachable, not 404). | hw274 | ❌ | hw274-2026-07-19: Kustomization catalyst-tenant-acme274-apps Ready=True but inventory holds ONLY Namespace+2 NetworkPolicies — no WordPress; ns acme274 pods = vcluster-0 + coredns only; tenant svc: "commit to per-Org repo acme274/catalyst-tenant: ref-race persisted after 5 attempts"; provisioning: "app wordpress not ready in acme274 after 10m0s"; browser https://wordpress.acme274.omani.homes → 404. |

---

### Fresh-prov 2026-06-26 — session-fix acceptance (rows 227–239)

These 13 rows are the acceptance checks for the issues genuinely resolved this session. Each pairs the **root cause** with the **fresh-prov surface to walk** + the **expected evidence**. All start `☐` — the fresh-prov walk fills the verdict + evidence cells in. (Refs #4279.)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| 227 | delivery | [#4415](https://github.com/openova-io/openova/issues/4415) | A catalog/seed version bump reaches the live **Blueprint CR zero-touch** — bumping a `bp-*` pin in the catalog seed lands on the cluster Blueprint CR with NO manual `kubectl patch` and NO `resource-policy: keep` annotation pinning a stale version. PASS = after a seed bump, `kubectl get blueprint bp-<x> -o jsonpath='{.spec.version}'` matches the seeded version within one reconcile, the seeder owns the field (no `helm.sh/resource-policy: keep`), and the rolled chart renders on the cluster. | hw274 | ⚠️ | hw274-2026-07-19: seed version-bump propagation requires a catalog mutation; not walkable read-only. [prereq-gated] |
| 228 | delivery | [#4435](https://github.com/openova-io/openova/issues/4435) | A **re-prov AFTER a wipe** does NOT false-fail on orphaned `catalyst-*` VPCs — the orphan-VPC janitor sweep removes the leftover VPC(s) from a prior wiped deployment before the new prov allocates its network. PASS = wipe an env, immediately re-prov on the same region; provisioning does NOT error `VPC already exists` / quota-conflict; the janitor log shows the orphaned `catalyst-*` VPC(s) swept, and the new prov reaches Phase-1 green. | hw274 | ⚠️ | hw274-2026-07-19: wipe→re-prov cycle is a destructive mutation; not walkable read-only. [prereq-gated] |
| 229 | delivery | [#4409](https://github.com/openova-io/openova/issues/4409) [#4428](https://github.com/openova-io/openova/issues/4428) | **bootstrap-kit Kustomization Ready=True + plane-isolation admits CNPG→kube-apiserver egress** — on the fresh prov `kubectl -n flux-system get kustomization bootstrap-kit` is `Ready=True` (no orphaned `dependsOn` / CRD dry-run rejection), AND the plane-isolation NetworkPolicy permits the CNPG operator's egress to the kube-apiserver so Cluster admission/patch succeeds. PASS = bootstrap-kit `Ready=True`; CNPG operator pod Running; a `Cluster` reconciles (no `context deadline exceeded` / x509 webhook failure caused by blocked apiserver egress). | hw282 | ✅ | hw282-2026-07-21 (dep 09696223c939cf29, region-a): `kustomization/bootstrap-kit` Ready=True; 9 CNPG `Cluster`s reconciled healthy (cnpg-pair-primary 3/3, shared-pg×3, org/pdns/newapi/guacamole/flow — no context-deadline/x509); plane-isolation CNPs Ready (cnpg-pair-crossregion-dr-primary, cnpg-pair-dr-failback-egress, alloy-allow-apiserver-egress). kubectl live. |
| 230 | delivery | [#4427](https://github.com/openova-io/openova/issues/4427) | **`ghcr-pull` reflects into a bare-slug per-Org namespace** — the emberstack reflector copies the `ghcr-pull` imagePullSecret into a per-Org namespace whose name is a bare slug (e.g. `org-<slug>` / a non-UUID form), so private `ghcr.io/openova-io/*` app images pull (no `ImagePullBackOff` 401-anonymous). PASS = a fresh Org's namespace has the reflected `ghcr-pull` secret; a per-Org app pod pulling a private image reaches Running (no 401/ImagePullBackOff). | hw274 | ⚠️ | hw274-2026-07-19: reflector leg PROVEN — ghcr-pull (dockerconfigjson) + harbor-robot-token reflected into bare-slug ns acme274 at 3m51s age; the private-image pod-Running leg pends the Org's first app pod (Org still converging: only vcluster-0 + synced coredns). [prereq-gated] |
| 231 | delivery | [#4411](https://github.com/openova-io/openova/issues/4411) | **bp-postgres singleton render carries the operator-probe NetworkPolicy** — a singleton (non-HA) bp-postgres render still ships the NetworkPolicy that admits the CNPG operator's health/readiness probe, so a singleton Postgres reaches Ready (the probe is not blocked). PASS = a singleton bp-postgres Application reaches `Ready=True`; the operator-probe NetworkPolicy is present in the rendered manifest; the CNPG operator can probe the instance. | hw281 | ✅ | hw281 guacamole-pg singleton instances=1 ready=1 — CNPG operator not blocked on non-HA render |
| 232 | apps | [#4272](https://github.com/openova-io/openova/issues/4272) | On a fresh funnel Org, **openclaw `/readyz` returns 200** — the 4-layer chain (controller image · reaper apiserver egress NetworkPolicy · TLS trust · OIDC) all resolve so the idle-reaper lists per-user pods and readiness passes. PASS = `bp-openclaw` HR `Ready=True`, controller pod 1/1 Running, and `curl https://<openclaw-host>/readyz` → 200 (no `reaper: list pods … context deadline exceeded` loop). | hw274 | ⚠️ | hw274-2026-07-19: openclaw /readyz needs a converged funnel Org with the openclaw bundle — no bp-openclaw pod/HR cluster-wide; acme274 converging (vcluster-plan). [prereq-gated] |
| 233 | apps | [#4422](https://github.com/openova-io/openova/issues/4422) | On a fresh funnel Org, **WordPress serves HTTP 200** — the mysql DB pod is Guaranteed-QoS (requests==limits, schedulable under the per-Org LimitRange), the HTTPRoute is present, and the landed host is pool-aligned to the console-ELB EIP. PASS = `curl https://wordpress.<slug>.<pool-tld>/` → 200 (or 302→install then 200), mysql pod QoS=Guaranteed + Running, HTTPRoute parents the console gateway, host resolves to the console-ELB EIP (not the primary ELB). | hw274 | ⚠️ | hw274-2026-07-19: acme274 (concurrent session's funnel Org) still converging — no wordpress pod or per-app HTTPRoute yet (only console.acme274.omani.homes route + vcluster-0); premature to curl-stamp mid-provision. [prereq-gated] |
| 234 | apps | [#4307](https://github.com/openova-io/openova/issues/4307) | On a funnel Org **that provisioned Stalwart mail** (cart includes `stalwart-mail`, or a mail-bearing bundle e.g. pharmacy/legal/finance), `mail.<slug>.<pool-tld>/` serves HTTP 200 (SnappyMail SPA); `bp-stalwart-tenant` pod Running; webmail HTTPRoute present (no bare envoy 404). | hw274 | ⚠️ | hw274-2026-07-19: no mail-bearing funnel Org on this env — no per-Org bp-stalwart-tenant instance (kubectl). [prereq-gated] |
| 235 | apps | [#4437](https://github.com/openova-io/openova/issues/4437) | On a fresh prov, **grafana SSO logs in 3/3 → 200** — the bp-sso-bridge per-tick credential refresh keeps the grafana OIDC client secret in lockstep with Keycloak, so the SSO round-trip completes. PASS = grafana `/login/generic_oauth` → Keycloak → back signed-in (200, user in the avatar) across 3 consecutive attempts; bp-sso-bridge refreshes the client cred each tick (no stale-secret 401). | hw274 | ⚠️ | hw274-2026-07-19: 3/3 grafana SSO round-trips require KC browser logins — mutation-gated (no KC session mintable read-only this walk). [prereq-gated] |
| 236 | apps | [#4439](https://github.com/openova-io/openova/issues/4439) | On a fresh multi-region prov, **region-B keycloak reaches Ready** — the cross-region shared-pg write host is correct so region-B Keycloak connects to the primary Postgres and completes realm import. PASS = `bp-keycloak` HR `Ready=True` in the region-B cluster; keycloak pod Running; realm import succeeds (no DB-connect / wrong-write-host failure); `keycloak.<host>` → 302 SSO. | hw281 | ✅ | hw281 bp-keycloak HR Ready BOTH regions (region-b un-wedged via #5289 edge-route fix); auth.hw281 realm discovery HTTP 200 |
| 237 | spine | [#4416](https://github.com/openova-io/openova/issues/4416) | **spine → Continuum round-trip is Healthy + `continuumRef` is populated** — the spine adopts the Continuum and the round-trip reports Healthy. PASS = the Continuum CR reports `Healthy` (not Degraded), the spine's `continuumRef` field is populated with the live Continuum, and a witness round-trip completes. | hw282 | ✅ | hw282-2026-07-21 kubectl region-a: continuums.dr.openova.io = 8 CRs ALL phase=Healthy (dr-spine-gitea/harbor/keycloak/openbao, cnpg-pair-bp-cnpg-pair-continuum, dr-shared-pg/-b/-c); applications.apps.openova.io spine-gitea status.continuumRef=catalyst/dr-spine-gitea (likewise spine-harbor/keycloak/openbao) — spine adopted the Continuum and round-trip reports Healthy. Region-b has no continuums CRD (CRs are control-plane-side, informational). |
| 238 | postgres | [#4282](https://github.com/openova-io/openova/issues/4282) | **Per-Org postgres-in-vCluster → host-side CNPG Ready** — a per-Org Postgres requested inside the Org's vCluster is reconciled by the host-side CNPG operator and reaches Ready. PASS = the per-Org `Cluster` CR is `Ready` (host-side CNPG drives it), the Postgres pod is Running with Guaranteed QoS, and the per-Org app connects (no empty-DSN / disableWait deadlock). | hw274 | ⚠️ | hw274-2026-07-19: no per-Org postgres-in-vCluster requested on this env (acme274 vcluster has no pg yet — converging); host-side CNPG healthy driving 9 Clusters incl singletons. [prereq-gated] |
| 239 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | **Crossplane adoption populates Observe-only on the fresh prov** — `providers.pkg.crossplane.io` and `cloudadoptions.compose.openova.io` are non-empty and reach Synced+Ready, observing (NOT re-provisioning) the OpenTofu-built infra. PASS = `kubectl get providers.pkg.crossplane.io` → ≥1 Healthy; `kubectl get cloudadoptions.compose.openova.io -A` → non-empty Synced+Ready with `external-name` = the real ELB id; the live ELB/nodes are untouched (Observe-only). | hw282 | ✅ | hw282-2026-07-21 (L7 re-verified live): `adopt-loadbalancer-primary` KIND=loadbalancer external-name=`ed309f1c-13cc-4463-81d5-e5290a55dd9a` (real ELB UUID) SYNCED+READY=True (+ `adopt-loadbalancer-console-0`); `provider-opentofu` Healthy; distinct from `adopt-eip-*` (eip-kind); Observe-only (existing external ids, live ELB untouched). |
| 240 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | No-nodePort gateway serves externally on a FRESH zero-touch prov: ELB → node:443 hostPort → hostNetwork envoy; gateway Service carries ZERO nodePorts (§854 literal). | hw282 | ✅ | hw282-2026-07-21 (dep 09696223c939cf29): serves externally console.hw282→**200** + marketplace→**200**; **0 NodePort Services cluster-wide** (§854 literal); cilium-envoy `hostNetwork=true` (4 Ready) binds node:443 (ELB→node:443→envoy proven by the external 200s, no nodePort). kubectl+curl live. |
| 241 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | Mothership writes `ready` ONLY after an external HTTP answer from https://console.<fqdn>/ (no more false-green). | hw274 | ⚠️ | hw274-2026-07-19: blocked — ready-only-after-external-answer ORDERING needs mothership-side observation: GET console.openova.io/sovereign/api/v1/deployments/d51a69f885872a81 unauth → 401 {"error":"unauthenticated"} (owner-scoped; walker holds hw274 session only). Consistency half holds: env ready + external https://console.hw274.omantel.biz/ → 200 in 0.03s; 0 NodePort Svcs both regions. [prereq-gated] |
| 242 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | 2-region BCP admission floor: a 1-region POST with no explicit bcpTopology is rejected at the LIVE production API. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: 2-region BCP admission floor (#4708) — mothership POST-time gate; hw224 IS 2-region (passed the floor). Mothership concern ‖ 2026-07-03 negative wire-test vs console.openova.io: `POST /sovereign/api/v1/deployments` (1 region, no topology) → **HTTP 400** "deployment has 1 region(s) and no explicit bcpTopology — the BCP agreement default is multi-region (>=2 regions, Pillar 2)…". Nothing created. ‖ hw228 2026-07-09 LIVE PARTIAL (production API console.openova.io, founder-token auth confirmed GET /sovereign/api/v1/deployments = HTTP 200): every 1-region POST is REJECTED with HTTP 400 (validated bodies hit SKU/hetzner-token gates that run before the region-count check). The #4706 BCP floor itself (provisioner.go:1076-1086 'bcpTopology=%q requires len(regions)>=2 ... Pillar 3 zero-transactions-lost cannot be honoured single-region') is in the DEPLOYED image + unit-tested (provisioner_test.go, dr_standby_integrity_test.go). Isolating the exact floor-line rejection LIVE needs a fully-credentialed body (real Hetzner token + orderable SKUs) which risks a real provision — NOT run. Stays ⚠️: production API live+validates+rejects-1-region proven; exact-floor live-hit is the remaining bar. ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): The live proof requires POST https://console.openova.io/sovereign/api/v1/deployments with a 1-region/no-bcpTopology body — a create-attempt WRITE against the production mothership API, not performed by this read-only walker. Read-only context gathered: mothership is reachable again (GET https://console.openova.io/ -> HTTP 302, 1.06s). — Exact deferred write: 'POST /sovereign/api/v1/deployments (1-region negative admission test)'. Prior evidence stands: 2026-07-03 negative wire-test -> HTTP 400 'deployment has 1 region(s) and no expli… |
| 243 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | Tenant DNS split-horizon on a fresh prov: wildcard hosts → primary ELB EIP, console → console ELB EIP. | hw281 | ✅ | hw281 DNS split-horizon: auth/gitea/harbor to primary ELB 212.72.24.33; console/api/marketplace to console ELB 212.72.24.52 |
