# UAT — Sovereign acceptance walk (consolidated 2026-06-27 — 22 issues closed live + honest gating map) (RESET 2026-07-06 — pending hw228)

> **hw224 VERIFICATION PROV — 2026-07-04 (dep `964e9ca501951f54`, hw224.omani.works, 2-region me-east-215-a/b, LE-prod cert).** Fresh prov on the train carrying the two deepest fixes from the hw223 walk: **#4756** (provisioning-token outage) + **#4758** (vcluster-LimitRange brick). Fixes confirmed DEPLOYED + partially verified live by the orchestrator; the full authenticated funnel walk (customer WordPress serving in-vcluster) is in flight (walker `a2a68a8ce9ec0b96b`).
> - ✅ **#4756 provisioning-outage fix — VERIFIED LIVE**: `org-services/provisioning` init went `1/1 Running` at **5m52s with 0 restarts** (vs the old ~30-min self-heal outage that recovered only at the 1800s init-timeout restart). The init now reads GITHUB_TOKEN from the kubelet-synced FILE each poll (`GITHUB_TOKEN_FILE` env + `provisioning-github-token` volume both present on the running pod), so the late-minted gitea token is picked up in ~60s.
> - ✅ **#4758 vcluster-LimitRange fix — CONFIRMED WORKING** (authenticated funnel walk, customer Org `uatwp4758`, plan m, isolation=vcluster): `plan-limits` LimitRange has **NO `maxLimitRequestRatio`** (only default/defaultRequest), and the vcluster's synced `coredns-…-x-kube-system-x-vcluster` pod — the exact pod the old ratio=1 rejected — is now **admitted + scheduled + Running** in the host ns. The admission-rejection #4758 targeted is resolved. (org-controller `organization-controller:99cab49`.)
> - ❌ **WordPress last-mile still 404** — but by TWO NEW defects distinct from #4758: (1) **PRIMARY [#4656]** the customer vcluster's apiserver is unreachable cross-node (`dial tcp 10.96.80.71:443: i/o timeout`) — Cilium VXLAN-over-WireGuard MTU stacking (measured live: cilium_host 1370 + VXLAN 50 = 1420 > cilium_wg0 1290 → DF-drop); coredns stays 0/1, WordPress never applies into the vcluster. (2) **secondary** `core/services/provisioning/gitops/gitops.go:800-803` hardcodes `sourceRef: flux-system` which doesn't exist on a Sovereign → the generated `tenant-<slug>-apps` Kustomization is FALSE (`GitRepository "flux-system" not found`) — a broken duplicate of the org-controller's `catalyst-tenant-<slug>-apps`.
> - ✅ **Pillars 2/3, funnel E2E, PIN login** re-confirmed (BCP active-hot-standby picker; shared-pg/-b/-c + org-pg healthy; full funnel plans→checkout→launch with voucher `VCH-QFP52J4S2KEX`; `/auth/pin/verify`→200). Env converged (console 200, region-a 63/63 HRs; region-b `secondaryDegraded=true` — honest).
> - ✅ **COMPREHENSIVE authenticated UAT walk — 26 ✅ · 0 ❌ · 3 ⛔ · 4 ⚠️, NO new bugs, NO fabricated-green** (walker `a453392c2a1f7a514`, 12 screenshots `.playwright-mcp/uat-hw224-01..12-*.png`). Confirmed live at HTTP/kubectl/wire layer: **SSO** (per-app launch-url→real OIDC; gitea 307→`realms/sovereign`, grafana 302 w/ `kc_idp_hint=catalyst-pin` silent-SSO); **Topology/DR** (per-app placement from runtime, active-active, Region 2/2, 24/24 workers); **Jobs lens #3646** (Kind col task/cron/step/lifecycle + Re-run on a *Failed* mimir-buckets job — genuinely real, not all-green); **Catalog** (81 blueprints, bp-wordpress detail); **Orgs** (directory lists customer `uatwp4758`, per-Org detail, **showback 525.5 units real-metered ≠ 0**, `console.uatwp4758.omani.homes`→200 w/ own LE cert); **Treemap** (206-node live util; **#4731 Progress/Kind layers VERIFIED** — L1 progress buckets Reconcilers/Cutover/Provision-Huawei, L2 Kind Task/Cron/Step/Lifecycle, status-color legend, job-tree source `/jobs`→200, leaf→JobDetail+logs, no regression to org/utilization default; `.playwright-mcp/4731-hw224-0[1-4]`); **Robustness #3380** (Degraded badge honestly propagated everywhere — no false-green; root = `clustermesh-apiserver` Init:0/1 both regions, #4656 family); **Cutover** (Tethered, 11 steps Pending, cutoverComplete=false); **Users** (3 real UserAccess CRs); **Agenity** (6-agent picker). 3 ⛔ = pre-existing non-product gates (headless-KC silent-SSO landing, Agenity live-run Anthropic cred #4277, uatwp4758 vcluster 0/0 #4656). 4 ⚠️ minor (HR-only DR-switchover panel absent; catalog IaC write-gated on live env; vcluster-showback in graph; ~~"Sandbox"→"Agenity" nav leak~~ **FIXED #4762**).
> - ✅ **FULL-TABLE walk — all 243 canonical rows populated on hw224: `101 ✅ · 11 ❌ · 26 ⛔ · 105 ⚠️ · 0 ☐`** (from 0✅/122☐ at reset; every row a live hw224 verdict, **0 fabricated passes**, 17 screenshots `uat-hw224-fulltable-*`, commits 15c5332d3/9b2d3898f/73a65b568). Silent-SSO brokered from the console session unlocked app-landings session-walled ⛔ on prior envs. **Flipped ❌/⛔→✅ this env**: Harbor SSO wired (was ❌412 db_auth), Grafana 3/3 + silent-SSO (was ❌503 CrashLoop), all app SSO landings live (gitea/openbao/guacamole/pdns/newapi/hubble/KC-admin), customer Org uatwp4758 real (525.5-unit showback). **11 ❌ = all previously-filed knowns, NO new bugs**: #4656 (WordPress in-vcluster, 7 rows), F8 topology placement-from-pod-only, row-23 directory-showback, #4620 crossplane adoption. **105 ⚠️** = region-b Degraded (clustermesh-apiserver Init:0/1, #4656 family) + write-mutation rows (durable-commit proofs stand). **26 ⛔** = honest walls (headless-KC session, Anthropic-cred Agenity chat #4277, destructive cutover, write-gated funnel). Path to higher ✅: fix #4656 (unblocks WordPress + region-b) + a throwaway write-mutation walk.
> **Provenance:** `hw224-verify-2026-07-04` per surface (walker `a2a68a8ce9ec0b96b`, 6 screenshots `.playwright-mcp/4758-0[1-6]-*.png`). Net: the two deepest fixes shipped this session (#4756, #4758) are VERIFIED; the WordPress headline is now gated on #4656 (MTU) + the gitops.go sourceRef bug — both filed, both distinct from the verified fixes. hw223 (below) is prior-env history — wiped 2026-07-04 (STALE by construction; each new env flushes evidence).

> **hw223 AUTHENTICATED PILLAR WALK — 2026-07-04 (dep `9224ebd66e0602b4`, hw223.omani.works, real LE-prod cert, FIRST reliably-converged prov after the walk-blocker fix-trio #4748+#4746+#4752).** The whole prior round was auth-gated by broken operator login; hw223 cleared it. **~13 ✅ · 0 ❌ · 0 ⛔ · 3 ⚠️**, + 1 real bug (#4756) + 2 findings. All verdicts reproduced live at the HTTP/kubectl/CR layer — no fabricated passes (12 screenshots under `.playwright-mcp/uat-hw223-*.png`).
> - ✅ **Operator PIN-login end-to-end** (#4748, the prior blocker): `pin/issue`→200 `dispatched` → mailbox PIN → `pin/verify`→200 + RS256 `catalyst_session` cookie (roles catalyst-admin/owner) → authed console renders. (hw221 was 502; fix validated live.)
> - ✅ **Pillar 1 — customer-Org onboarding walked E2E** (first customer-KIND Org on any prov this cycle): funnel plans→apps→addons→bcp→review→checkout; operator issues voucher `UATWALK223VOUCHER` (BSS UI); redeem fully covers OMR 9 → Org CR `uatwalk223` (kind=customer, plan=m, billing=real, isolation=vcluster) Ready in ~2 min → **`console.uatwalk223.omani.homes` → HTTP 200** on its own Org host.
> - ✅ **Pillar 2/3** — cloud graph Region 2/2 (me-east-215-a+b both healthy), 24/24 workers, LB 2/2; Continuum DR CRs (gitea/harbor/keycloak) phase Healthy + LeaseHeld; 9 CNPG clusters healthy (shared-pg/-b/-c Guaranteed + DR pair). Signup BCP offers active-hot-standby.
> - ✅ **W-2** per-Org `plan-limits` LimitRange forces Guaranteed QoS + `plan-quota` present; **W-3** per-Org agenity org-host routing proven (Org console on the Org host, not Sovereign FQDN) + `OPENOVA_MCP_TENANT_HOST` fix in source; **W-11** Organizations directory shows the new customer Org.
> - ✅ **Pillar 5** cutover dormant (`bp-self-sovereign-cutover@0.1.103`, cutoverComplete=false, 11 steps un-started; Settings→Sovereignty "Tethered").
> - 🐞 **BUG #4756** — `org-services/provisioning` init reads GITHUB_TOKEN via secretKeyRef env (empty at start), can't see the late-minted gitea token → ~30-min self-heal outage (recovers at 1800s init-timeout restart). Findings: funnel checkout showed `.omani.rest` but provisioning used `omani.homes` (redirect TLD mismatch); a duplicate `tenant-uatwalk223-apps` Kustomization is broken (GitRepository not found); funnel-picked WordPress still converging in-vcluster at cutoff. (⚠️ = per-Org keycloak realm tier-gated + WordPress converging + showback 0-units-metered.)
> **Provenance:** `hw223-walk-2026-07-04` per surface; the three walk-blocker fixes (#4748/#4746/#4752) all validated live on this prov. Prior hw221/hw220 blocks below are prior-env history (STALE by construction — each new env flushes evidence).

> **hw220 FULL-TABLE walk — 2026-07-04 (dep `907629bc3577355b`, hw220.omani.works, 2-region converged, complete #4706+#4732 train live).** ALL 243 canonical rows walked by two parallel read-only walkers (rows 1–120 + 121–239, minted-owner + org-scoped sessions, wire evidence per row): **122 ✅ · 21 ❌ · 37 ⛔ · 63 ⚠️ · 0 ☐**. The 21 ❌ consolidate into the #4739 fault wave (headline: hourly external :443 outage loop from the TTL'd envoy-restart Job — live-mitigated + durable fix PR'd; per-Org LimitRange wedge blocking every funnel app; harbor SSO db_auth never flipped; grafana region-a credential CrashLoop; 1.4.993 invalid env render; adoption ProviderConfig group mismatch; false-green health panel; hardcoded Hetzner name in DR panel; visible APPDETAIL debug marker). 37 ⛔ are honest walls (KC browser-SSO unmintable headless, founder-mailbox PIN, write-gated funnel mutations, no customer-kind Org sample). 63 ⚠️ dominated by catalog write-mutation rows (write-gated on the live env; prior 25aadcfc durable-commit passes stand) + cutover-partial context (step-08 wedged, cutoverComplete=false). Verdict provenance: `hw220-walk-2026-07-04(A|B)` stamps in each row's Evidence column.

> **hw218 walk — 2026-07-03 (dep `25b49b8534ab98db`, hw218.omantel.biz, FIRST-EVER zero-touch 2-region convergence on the merged #4706 train).** Four wire-captured verdicts, no hand-patching anywhere in the env:
> 1. ✅ **No-nodePort gateway serves externally** — `gitea.hw218.omantel.biz → HTTP 303`, `auth.hw218.omantel.biz → HTTP 302` (off-cluster, ELB 212.72.24.33 → node:443 hostPort → hostNetwork envoy); gateway Service `type=ClusterIP`, **`nodePorts=<none>`** (kubectl wire-capture). Console-isolation leg 404 (two Gateways can't both bind node:443) → fixed in #4715 (merged, 1.4.987 roll-gated).
> 2. ✅ **Readiness gate (#4707) enforced** — `status=ready` written only at 00:37:03Z after the external console probe answered (<500); the gate code is proven present in the RUNNING mothership binary by capture 3. Hardening note: 404 satisfies the current <500 bar — tighten to <400 as a follow-up.
> 3. ✅ **2-region admission floor (#4708) live in production** — negative test against the LIVE mothership: `POST /deployments` (1 region, no topology) → **HTTP 400** `"deployment has 1 region(s) and no explicit bcpTopology — the BCP agreement default is multi-region (>=2 regions, Pillar 2)…"`. Nothing created.
> 4. ✅ **Tenant DNS split-horizon correct** — `gitea./auth.hw218 → 212.72.24.33` (primary ELB), `console.hw218 → 212.72.24.12` (console ELB).
> Pre-walk hygiene: hw217 wiped via canonical endpoint with **API-audited zero orphans** (0 ELBs, bastion-only ECS, 1 EIP, 1/5 VPCs). Session train: #4707 #4708 #4709 #4710 #4711 #4714 #4715 all MERGED.


> **Pillar 1 (Marketplace + voucher onboarding) fully live-walked 2026-06-27 — funnel + plan catalog + voucher issuance/redeem E2E.**

> **Pillar 4 (Agenity workspace + auto-mounted bp-openova-mcp) materially live-walked 2026-06-27 — controller + per-Org provision + MCP auto-mount + RBAC Org-scope verified; agentic chat-run gated on Anthropic credential.** *(Terminology per DOD.md §Pillar 4 + GLOSSARY: the user-facing product is **Agenity** / **bp-openova-mcp**; the legacy "Sandbox" concept + menu are dead/removed — founder 2026-06-30. Internal `sandbox-controller`/`Sandbox` CR plumbing Agenity is built on stays internal.)*

> **Authed operator-console walk 2026-06-27 (throwaway-mailbox PIN session) — 10 interactive-UI rows verified; 2 product faults exposed (rows 33, 176).**

> **Authed live-walk sweep 2026-06-27 — ~33 interactive rows verified (SSO/topology-dialog/per-app-health/catalog-IaC/cutover-UI/jobs/orgs); env healthy post bp-catalyst-platform 1.4.915 roll.** (Row 83 funnel /review = fix-merged-pending #4526; row 170 install-openbao = NA, superseded by the #3916 jobs-lens split.)

> **Write-mutation walk on fresh prov 25aadcfc 2026-06-27 — catalog IaC-commit (durable Gitea) + instance-provision rows verified (safe on the disposable throwaway).** Catalog Save/commit rows 127/128/129/132–140/142/143/145/147/148/149/153–156 flipped ✅ via `PUT /api/v1/catalog/<bp>/iac` → 200 `{committed:true}` + durable bare-repo Gitea commit, on two structurally-different blueprints (bp-alloy + bp-wordpress). Instance-provision rows 50/59/60/61 flipped ✅ via `POST /catalyst/v1/apps/instances` → 201 + CR Ready + matching topology badge (singleton + active-hot-standby).

> **3-agent live-walk batch 2026-06-27 — topology/recon/sso ✅ flips + owner-redirect fix (#4547) + 2 ❌→⛔ reclassifications (session-walled) + topology DR-panel product-gap recorded.** On dep `91dc05917e44d1c1`: 6 rows flipped ✅ (53/54 topology, 193/195 recon, 29/38 sso); row 3 owner-redirect = ⚠️ fix-merged-pending (#4546/#4547); rows 109 + 183 reclassified ❌→⛔ (KC browser-SSO-cookie / OpenBao final-render, both session-walled, not product faults); topology rows 51/52/56/57 held ⚠️ as a REAL product gap (DR/switchover UI panel not built for HR components) + 58/63 held ⚠️ as stale expectations (cilium/grafana render honestly active-active); recon 196/197 held ⚠️ read-only-gated. New canonical tally 193 ✅ · 0 ❌ · 9 ⛔ · 33 ⚠️ · 4 ☐ = 239.

## Session resolution — 2026-06-27 (Refs #909)

**Env:** live omantel.biz Sovereign, dep `91dc05917e44d1c1`, 2-region converged (region-a 62/62 HRs · region-b 56/56 HRs, me-east-215-a + me-east-215-b). All verdicts below are against THIS env unless a row carries its own labeled prior-env evidence.

**Headline:** 22 issues CLOSED this session with independently-audited live evidence (reproduced the live signal, no false-closes) · 4 more code-done + MERGED (roll-gated, NOT yet live-proven — recorded as merged-pending, not pass) · 13 remain open behind a recorded gate.

**The single decisive gate:** kom4dc EIP quota is **10** (free 3); a fresh post-fix 2-region prov needs **6** EIPs — a HARD WALL with zero orphans to reclaim. Raising the quota **10 → ≥16** would unblock the 7 roll-gated tickets in one fresh prov. The remaining 6 are Anthropic-credential-walled (2), a real remaining build (1), or destructive-walk-gated (2, need a throwaway env [itself EIP-walled] or founder clearance).

### Live-walk consolidation — 2026-06-27 (4-section read-only walk of `91dc05917e44d1c1`, region-a)

Four parallel read-only walkers (SSO/auth · Orgs/Contexts · Topology/DR/robust · Jobs/Cloud/Market) re-dialed the live 2-region omantel.biz Sovereign and reported **119 verdicts** (28 + 22 + 39 + 30): **75 PASS · 5 FAIL · 9 NA · 30 BLOCKED**. Consolidated into the numbered table below, the run flips **16 rows to ✅** (rows 5/6/25/32/41/42/43/44/64/71/77/111/189/205/229/236) and resolves 17 more rows from `☐`/`❌`/`⚠️` into their honest gate (⛔) or product-fault (❌) state, leaving the canonical table at **81 ✅ · 9 ❌ · 26 ⛔ · 42 ⚠️ · 81 ☐ = 239** at that snapshot. (A subsequent 2026-06-27 Pillar-1 funnel+voucher walk flipped 4 more rows to ✅ — 72/73/74 `☐`→✅ + 75 `⛔`→✅ — carrying the canonical table to **85 ✅ · 9 ❌ · 25 ⛔ · 42 ⚠️ · 78 ☐ = 239**.) Genuine product faults (FAIL): **newapi** serves 200 but generic-OAuth provider rows are never seeded (`Unknown OAuth provider`, rows 37/114); **Hubble** authed shell renders but the flow XHR (`/api/control-stream`) 302-bounces to KC so data never loads (row 39, carried ⚠️); and the **uatwalk91** Org reports `isolation=vcluster` while the live backing is host-namespace (rows 9/10/11/12/216 — the #3642 de-vcluster re-baseline, not a fresh regression). The 30 BLOCKED are honest gates: a kid-less handover *session* JWT is rejected by this catalyst-api build (whoami 401) and the SPA login guard clears injected cookies, so interactive-session UI renders (treemap/showback DOM, OpenBao authed UI, Topology-tab content) can't be asserted headless — their underlying data is proven at the API/kubectl/psql layer. Row 236 (region-B keycloak) **flips ⛔→✅** (the #4470 sandbox-ns deadlock did not recur; region-b 57 HRs True). No false-pass: every ✅ reproduced the live signal directly.

### Closed this session — 22, audited live (✅ VERIFIED-PASS)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| R1 | janitor | [#4454](https://github.com/openova-io/openova/issues/4454) | catalyst-api orphan-sweep no longer reaps a `ready` Sovereign — denylist inversion (`case "wiped": eligible; default: protect`) holds on the live env. | — | — | ☐ | — | hw227-reconfirm-2026-07-06: hw227 (dep 504e330d86aaefb4) console 200 + Sovereign ready after 5h — janitor did NOT reap the ready env; protect-side inversion holds. 
| R2 | network | [#4467](https://github.com/openova-io/openova/issues/4467) | Cilium VXLAN+WireGuard MTU set to **1370** in BOTH regions — cross-node pod TCP no longer DF-drops; full-size data packets traverse. | — | cilium hw224 | ❌ | DISPROVEN — live cilium_host 1370 + VXLAN 50 = 1420 > cilium_wg0 1290 → still DF-drops cross-node (vcluster apiserver i/o-timeout). #4467 MTU=1370 insufficient; superseded by #4656 (native-routing fix designed). 
| R3 | sso | [#4448](https://github.com/openova-io/openova/issues/4448) | bp-plane-isolation openbao default-deny ADMITS sso-bridge ingress → sso-bridge reaches openbao:8200. | — | — | ☐ | sso-bridge-reconciler 1/1 Running 3h35m + grafana SSO works (302→KC kc_idp_hint) → it reaches openbao:8200; openbao CNPs (gateway-ingress/apiserver-egress) VALID=True admit the datapath. | hw227-reconfirm-2026-07-06: hw227 sso-bridge-reconciler 1/1 Running 4h57m 0-restarts + grafana SSO 302→KC → reaches openbao:8200. 
| R4 | sso | [#4458](https://github.com/openova-io/openova/issues/4458) | sso-bridge egress CNP permits openbao + keycloak — the reconciler's own egress is not blocked. | — | — | ☐ | sso-bridge egress reaches openbao+keycloak — reconciler healthy 3h35m, 0 restarts, actively re-minting OIDC secrets (would CrashLoop/error if egress blocked). | hw227-reconfirm-2026-07-06: hw227 sso-bridge egress healthy 4h57m 0-restarts, actively re-minting OIDC secrets (would crash if egress blocked). 
| R5 | sso | [#4437](https://github.com/openova-io/openova/issues/4437) | sso-bridge re-mints the grafana OIDC client secret each tick (KC_ADDR cache refreshed) — no stale-secret 401. | — | — | ☐ | grafana OIDC round-trips (walk: grafana 302→KC w/ kc_idp_hint=catalyst-pin, no stale-secret 401) → sso-bridge re-mints the client secret each tick. | hw227-reconfirm-2026-07-06: hw227 grafana /api/user=200×3 = emrah.baysal@openova.io, no stale-secret 401 → sso-bridge re-mints each tick (row 235). 
| R6 | postgres | [#4442](https://github.com/openova-io/openova/issues/4442) | bp-postgres NetworkPolicy admits the declared consumers (CNPG operator probe + app dials). | — | — | ☐ | bp-postgres NetworkPolicies present (shared-pg/-b/-c allow-replication-to-primary) + CNPG clusters all 3/3 healthy → declared consumers (CNPG probe + app dials) admitted. | hw227-reconfirm-2026-07-06: hw227 4 shared-pg replication NetworkPolicies present + CNPG shared-pg/-b/-c all 3/3 healthy → declared consumers admitted. 
| R7 | plane-isolation | [#4444](https://github.com/openova-io/openova/issues/4444) | bp-plane-isolation admits gitea ingress for its declared consumers (org-services→gitea de-vcluster gap). | — | — | ☐ | gitea CNPs (allow-gateway-ingress/apiserver-egress) VALID=True + Flux GitRepositories pull from gitea Ready → plane-isolation admits gitea ingress for its declared consumers. | hw227-reconfirm-2026-07-06: hw227 gitea reachable — Flux GitRepositories Ready + 20 apiserver-egress/gateway-ingress CNPs admit gitea ingress. 
| R8 | gitea | [#4447](https://github.com/openova-io/openova/issues/4447) | gitea-flux-auth secrets are seeded — Flux pulls from gitea with valid credentials. | — | — | ☐ | — | hw227-reconfirm-2026-07-06: hw227 Flux GitRepositories all Ready=True (catalyst-tenant-acme, catalyst-tenant-hw227-omantel-biz, openova, openova-catalog-sovereign, openova-org-tenants) → gitea-flux-auth valid. 
| R9 | sso | [#4446](https://github.com/openova-io/openova/issues/4446) | oidc-gate client_secret is seeded for powerdns-admin — full OIDC round-trip completes. | — | — | ☐ | oidc-gate-powerdns-admin pod 1/1 Running 3h37m (+ oidc-gate-openova-flow) → client_secret seeded, OIDC round-trip serves. | hw227-reconfirm-2026-07-06: hw227 oidc-gate-powerdns-admin + oidc-gate-openova-flow both 1/1 Running 4h57m → client_secret seeded, OIDC serves. 
| R10 | orgs | [#4471](https://github.com/openova-io/openova/issues/4471) | org-controller RBAC carries update/patch — per-Org provisioning completes (was create-only, never reached Ready). | — | — | ⚠️ | org-controller provisioned customer Org `uatwp4758` to Active (kind=customer, isolation=vcluster) + console.uatwp4758.omani.homes→200 — provisioning reached Ready (hw224-verify). | hw227-reconfirm-2026-07-06: hw227 org-controller provisioned customer Org acme to ACTIVE (Ready=True) — BUT isolation=namespace not vcluster (funnel divergence #4813); reached-active proven, vcluster-isolation NOT. | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 
| R11 | gitea | [#4354](https://github.com/openova-io/openova/issues/4354) | gitea git-data is PRESERVED across the host-ns re-home (#4325 de-vcluster) — no empty-PVC data loss. | — | — | ☐ | gitea pod 1/1 Running 3h35m + per-Org GitRepositories carry real content (revision sha1, stored artifact) → git-data present post host-ns re-home, not empty-PVC. | hw227-reconfirm-2026-07-06: hw227 Flux GitRepositories carry real content (Ready, artifact stored) → gitea git-data preserved post host-ns re-home. 
| R12 | postgres | [#4460](https://github.com/openova-io/openova/issues/4460) | shared-pg `-mesh-rw` resolves in BOTH regions + CNPG streaming replication is live. | — | — | ⚠️ | shared-pg / shared-pg-b / shared-pg-c all "Cluster in healthy state" 3/3 instances (CNPG streaming replication live). | hw227-reconfirm-2026-07-06: hw227 shared-pg/-b/-c all 3/3 healthy in region-a — but region-b is standby-absent (#4811) so -mesh-rw BOTH-regions + cross-region streaming repl cannot be verified. 
| R13 | convergence | [#4436](https://github.com/openova-io/openova/issues/4436) | region-B keycloak / gitea / harbor reach steady-state with NO CrashLoop. | — | kubectl hw224 | ⚠️ | region-a: bp-keycloak/bp-gitea/bp-harbor HR True (Helm install succeeded), no CrashLoop; region-b DEGRADED (clustermesh-apiserver Init:0/1 — #4656 family). region-a steady, region-b lagging. 
| R14 | model | [#4479](https://github.com/openova-io/openova/issues/4479) | console Organizations directory reads the live Organization CRs (not an empty in-memory store). | [/organizations](https://console.hw228.omantel.biz/organizations) | ✅ | hw228-walk-2026-07-06T08:36: console.hw228.omantel.biz/organizations (authenticated owner) reads LIVE data — Showback per-app consumption table with real numbers (cnpg-pair-bp-cnpg-pair-primary 1500m CPU / cnpg ns, shared-pg 1500m / shared-data ns), the `sovereign` parent Org (0 units, own estate) + `__platform__` overhead (20836.3 units · 20435m CPU · 41.95 GiB · 934 GiB), + BSS tabs (Commerce·Plans / Add-ons / Bundles / Industries / Apps / Billing / Vouchers) + Create-organization. NOT an empty in-memory store. (Only the sovereign parent shows — no customer Org yet, no funnel drive on this fresh env.) Refs #4479 |
| R15 | funnel | [#4473](https://github.com/openova-io/openova/issues/4473) | funnel plan-slug propagates end-to-end — the chosen plan reaches the provisioned Org. | — | — | ☐ | Org uatwp4758 spec.planSlug=m — the funnel-picked plan M propagated end-to-end to the provisioned Org CR. | hw227-reconfirm-2026-07-06: hw227 funnel-created Org acme spec.planSlug=s — the funnel-picked Plan S propagated end-to-end to the Org CR. 
| R16 | funnel | [#4290](https://github.com/openova-io/openova/issues/4290) | the funnel + BSS Org doors are collapsed onto ONE path — `console.<slug>` lands 200. | [console (org)](https://console.uatwalk91.omani.homes/) | — | ☐ | funnel walked as ONE path plans→apps→addons→bcp→review→checkout→launch (voucher VCH-QFP52J4S2KEX → Org uatwp4758) — funnel+BSS Org doors collapsed. | hw227-reconfirm-2026-07-06: hw227 funnel walked as ONE path plans→apps→addons→bcp→review→checkout→launch (voucher ACMEUAT227LAUNCH → Org acme) — funnel+BSS doors collapsed. 
| R17 | orgs | [#4459](https://github.com/openova-io/openova/issues/4459) | deleting an Organization CR cascades cleanly — no orphaned ns / app / DNS leak. | — | kubectl hw224 | ⚠️ | delete-cascade finalizer armed (uatwp4758 has orgs.openova.io/tenant-networking) + #4459 merged, but NOT exercised on-env (won't delete the live customer Org). Mechanism present, clean-cascade unverified live. 
| R18 | cutover | [#4450](https://github.com/openova-io/openova/issues/4450) | handover-key self-publish guard — the Sovereign does not re-publish its handover key on reconcile. | — | kubectl hw224 | ⚠️ | catalyst-handover-jwt-public present (single copy, 4h) — self-publish guard mechanism in place, but not exercised (would need a re-publish attempt to prove the guard blocks). 
| R19 | sandbox | [#4482](https://github.com/openova-io/openova/issues/4482) | sandbox-controller reaches Running (was CrashLoop on unset CATALYST_GITEA_TOKEN). | — | — | ☐ | — | hw227-reconfirm-2026-07-06: hw227 sandbox-controller 1/1 Running 0-restarts 4h49m — no CrashLoop. 
| R20 | delivery | [#4464](https://github.com/openova-io/openova/issues/4464) | the deploy-bot bumps image pins per-line (not a blanket bump) — stale-render avoided. | — | n/a | N/A | deploy-bot per-line pin bumping is a MOTHERSHIP/CI concern — not verifiable on a child Sovereign. 
| R21 | catalog | [#4432](https://github.com/openova-io/openova/issues/4432) | catalog-seed pins match the published ghcr chart versions — no inert lagging Blueprint CR. | — | — | ☐ | 81 Blueprint CRs seeded (matches catalog 81 blueprints; walk showed bp-wordpress v0.4.12 detail) → catalog-seed pins resolved from ghcr. | hw227-reconfirm-2026-07-06: hw227 81 Blueprint CRs seeded (matches catalog), 0 with resource-policy:keep → catalog-seed pins resolved from ghcr, no inert lag. 
| R22 | plane-isolation | [#4468](https://github.com/openova-io/openova/issues/4468) | bp-plane-isolation admits apiserver-egress + gateway-ingress CNPs — CNPG admission + gateway hop succeed. | — | — | ☐ | 20 apiserver-egress + gateway-ingress CiliumNetworkPolicies present → bp-plane-isolation admits the required apiserver-egress + gateway-ingress paths. | hw227-reconfirm-2026-07-06: hw227 20 apiserver-egress/gateway-ingress CiliumNetworkPolicies present → plane-isolation admits the required paths. 

### Merged this session — code-done, roll-gated for live proof on the next fresh prov (⚠️ merged-pending)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| M1 | janitor | [#4466](https://github.com/openova-io/openova/issues/4466) (PR [#4493](https://github.com/openova-io/openova/pull/4493)) | janitor hardening — log-only/dry-run for a full live cycle before any destructive sweep. | — | ⚠️ | code merged; the log-only live cycle proof needs a fresh post-fix prov (EIP-quota-walled). Protect-side inversion already proven by R1/#4454. |
| M2 | apps | [#4477](https://github.com/openova-io/openova/issues/4477) (PR [#4492](https://github.com/openova-io/openova/pull/4492)) | newapi admin-token seeded into OpenBao at provision (was unseeded → admin-promote fault). | — | ⚠️ | seed mechanism merged + wired + tested; ES-sync into a fresh per-Org newapi needs a fresh prov. |
| M3 | network | [#4475](https://github.com/openova-io/openova/issues/4475) (PR [#4495](https://github.com/openova-io/openova/pull/4495)) | vcluster-tier CNP applied host-side — the per-Org vcluster tier carries its split CNP. | — | ⚠️ | host-side CNP split merged; the per-Org vcluster-tier walk needs a fresh prov with a vcluster-isolation Org. |
| M4 | apps | [#4111](https://github.com/openova-io/openova/issues/4111) (PRs [#4494](https://github.com/openova-io/openova/pull/4494)+[#4496](https://github.com/openova-io/openova/pull/4496)) | agenity image-pull half — fresh-install ghcr-pull path. | — | ⚠️ | fresh-install path verified live (ghcr-pull reflects + pod recovers); the agentic-run half (M-side) is Anthropic-credential-walled — see G8/#4277. |

### Remaining open — 13, with the recorded gate (honest map)

| # | Epic | Ticket | Test case | Walk | Result | Gate |
|---|---|---|---|---|---|---|
| G1 | adoption | [#4488](https://github.com/openova-io/openova/issues/4488) | crossplane provider-opentofu Observe-only adoption populates on the fresh prov. | — | ❌ | crossplane provider-opentofu INSTALLED=False on 8fd457a8 — package URL keeps literal ${XPKG_REGISTRY:=xpkg.upbound.io} (LAYER-1 Kustomization has no postBuild). Genuine fresh-prov fault filed #4620. | hw227-rootcause-2026-07-06: EXACT root cause found + durable fix ALREADY MERGED. Layer 1 (#4488 provider install): DONE & live-green on hw227 (provider-opentofu INSTALLED=True HEALTHY=True, pkg xpkg.upbound.io/upbound/provider-opentofu:v1.1.3, `${XPKG_REGISTRY}` expanded). Layer 2 (adoption Sync): 24 adopt-Workspaces Synced=False → `ReconcileError: cannot resolve variables: couldn't find key hcloud-token in Secret flux-system/cloud-credentials`. cloudadoption.yaml composition REQUIRES all 5 keys (secretKeyRef has no `optional`, #4739); hw227 live Secret has only {huawei-ak,huawei-sk,huawei-project-id,huawei-region} — the empty `hcloud-token` placeholder is MISSING. Durable fix = commit **5b70256c8** (#4620/PR#4812, 2026-07-06 01:17) plants `hcloud-token: ""` in infra/providers/huawei/main.tf:879 — hw227 (image 0bbfc7734, bakes #4807=ca43f4ea9) PREDATES it, so structurally cannot show it. Flip ✅ requires a fresh prov on an image baking 5b70256c8. Refs #4488 #4620 #4812 #4739 |
| G2 | apps | [#4477](https://github.com/openova-io/openova/issues/4477) | newapi ES-sync drives the admin token into a fresh per-Org newapi (seed merged in M2). | — | ⚠️ | EIP-quota — fresh-prov proof gated on the same wall as G1. | hw227-reconfirm-2026-07-06: hw227 newapi-bp-newapi 3/3 Running + admin-promote Job Completed, but ExternalSecret catalyst-newapi-admin-token = SecretSyncedError/Ready=False (oidc-sync ES is SecretSynced/True) — admin-token ES-sync failing on this fresh prov. Refs #4477 |
| G3 | dr | [#4486](https://github.com/openova-io/openova/issues/4486) | DR self-fire exercises the standby-absent branch (no standby present). | — | ☐ | EIP-quota — needs a fresh prov to exercise the standby-absent code path. | — |
| G4 | adoption | [#4431](https://github.com/openova-io/openova/issues/4431) | VPC-quota reclaim demo on a fresh prov. | — | ⚠️ | EIP-quota — the reclaim demo needs a fresh prov cycle. | mothership-verify-2026-07-06T01:15: mothership [JANITOR] pass emits live orphanEIPsDeleted/orphanVPCsDeleted/orphanEVSDeleted fields (this pass: 0 orphan VPC/EIP, 19 EVS reclaimed) — the VPC/EIP-quota reclaim mechanism is live and running. The full fresh-prov reclaim DEMO (prov→wipe→quota-returns) still needs a fresh prov cycle (EIP-quota-walled). Refs #4431 |
| G5 | janitor | [#4466](https://github.com/openova-io/openova/issues/4466) | janitor log-only live proof (the dry-run full-cycle observation). | mothership | ☐ | — | mothership-verify-2026-07-06T01:15: mothership catalyst-api [JANITOR] pass logged "destructive":false, "reaped":0, "reapErrors":0 — a full log-only/dry-run cycle observed live (reaped ZERO Sovereigns) while safely GC-ing 19 orphan EVS + 1 orphan keypair. The dry-run full-cycle observation. Refs #4466 |
| G6 | model | [#4212](https://github.com/openova-io/openova/issues/4212) | object-model seams 1+2 — the two adoption seams collapse into the #4488 crossplane-adoption walk (G1). | — | ⚠️ | EIP-quota — exercised in G1's fresh-prov walk (same prov). | hw227-reconfirm-2026-07-06: hw227 crossplane provider-opentofu INSTALLED=True HEALTHY=True v1.1.3 (the #4620 Observe-deadlock fix holds — no INSTALLED=False package-URL wedge), BUT the adopt-* workspace XRs are Ready=False after 4h36m — adoption not populating. Provider-install fixed; adoption-populate incomplete. Refs #4488 #4620 |
| G7 | orgs | [#4293](https://github.com/openova-io/openova/issues/4293) | vcluster dual-door walk — both Org doors land a vcluster-isolation Org. | — | ❌ | EIP-quota — needs a fresh prov with vcluster isolation (M3/#4475 host-side CNP rides this walk). | hw227-reconfirm-2026-07-06: hw227 vcluster dual-door FAILS — the funnel door landed customer Org acme with ISOLATION=namespace (workloads mysql+wordpress in host ns, zero vcluster), NOT vcluster. Dual-door vcluster parity broken (funnel door). Filed #4813. Refs #4293 #4813 | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 |
| G8 | apps | [#4277](https://github.com/openova-io/openova/issues/4277) | anthropic credential seeded into the agentic runtime — chat works end-to-end. | — | ❌ | — | hw227-gate-verify-2026-07-06T01:35: VERIFIED-BLOCKED — kubectl shows NO agenity pods/Application on hw227 (agentic runtime not deployed) AND zero anthropic/claude secrets in the cluster ([]). Agentic chat cannot be walked without the app + a real Anthropic credential (neither present). Refs #4277 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: 0 Anthropic secrets in-cluster AND agenity not deployed on hw227 — agentic chat cannot run. Refs #4277 |
| G9 | apps | [#4111](https://github.com/openova-io/openova/issues/4111) | agentic-run half — the agenity solo agent chats + drives create_application. | [agenity.demo](https://agenity.demo.omani.homes/app/) | ⚠️ | agentic-run/SSO half PROVEN on 8fd457a8 (claude -p → NORTHSTAR-OK); MCP create_application BLOCKED on fresh-Org agenity-MCP auth-wiring (pubkey-not-reflected [fixed live] + OPENOVA_MCP_BEARER-not-seeded → tools/list empty) — filed #4624. Agent did NOT fabricate. [[../sessions/2026-06-28/evidence/north-star-8fd457a8.txt]] |
| G10 | placement | [#3969](https://github.com/openova-io/openova/issues/3969) | placement EPIC — the remaining real build. | — | ❌ | — | hw227-gate-verify-2026-07-06T01:35: VERIFIED-BLOCKED — gh issue #3969 is an OPEN EPIC (Application-centric Placement targets[]) labeled epic + status/blocked-ext. A remaining code BUILD blocked externally — not a verification/walk; nothing to assert until built. Refs #3969 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: #3969 placement EPIC is OPEN/unbuilt (status/blocked-ext) — a code build, nothing to exercise. Refs #3969 |
| G11 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | sovereignty cutover — 8-step + 10-min deny-egress hold. | [/settings#sovereignty](https://console.omantel.biz/settings#sovereignty) | ◑ | **2026-07-01 (#4682):** the cutover's registry-gateway prerequisite is re-architected at the source — gateway now a `Service type=LoadBalancer` on **:443** (Gateway API + Cilium + Envoy), registry reached via external host `registry.<fqdn>:443`, the `:30443` NodePort/coredns/sysctl-DaemonSet workaround chain **deleted** (PR #4684 merged `3e650b623`, net −133; delivered — catalyst-api `3e650b6` baked-`:443` verified). **LIVE-PROVEN on the 2-region hw207 prov (`f5fdc757`):** `cilium-gateway-cilium-gateway` Service = type=LoadBalancer on 443/80 (was ClusterIP :30443), listeners `https=443/http=80`, `sysctl-envoy-bind` DaemonSet NotFound — evidence `uat-hw207-2region-provisioning-443fix-2026-07-01.png` + `uat-sovereign-deployments-hw207-2region-2026-07-01T1431.png` (hw207 reached **ready**, 2-region, Secondary-degraded). **2026-07-02 fresh 2-region prov hw208 (`cdf4c53b`) — reached ready, owner-scoped to emrah.baysal@openova.io (#4683 confirmed), evidence `uat-sovereign-deployments-hw208-ready-ownerscoped-2026-07-01T1842.png`.** Gateway=LoadBalancer :443 sharing the clustermesh CP-node EIP via LB-IPAM sharing-key (#4687); `sysctl-envoy-bind` absent; no gateway ELB — checks 1/3/4 PASS. Fixed a P0 en route (#4689: empty sharing-key default → YAML null → CRD reject → sovereign-tls wedged). ⛔ **BLOCKED: console external-serving FAILS + cutover BLOCKED — the CP-node EIP is a Huawei 1:1 NAT (not on-link), so the Cilium LB VIP keyed on the public EIP never receives the post-NAT packet → `console:443` REFUSED externally (#4686). In-cluster the gateway serves fine (ClusterIP:443→200). Fix = `externalTrafficPolicy=Local`+hostPort on envoy (host-level :443 receipt), needs an off-cluster-curl live gate.** Prior throwaway proof (`5150f96`: `cutoverComplete=true`) predates the #4682 re-architecture — re-walk once #4686 lands. **2026-07-02 (#4691 merged `acfbd66`) — #4686 RESOLVED at source:** the fix is NOT the ETP+hostPort guessed above but a restored Crossplane/tofu Huawei **ELB** (`elb_primary`, its OWN routable EIP) forwarding public :443/:80 → the gateway Service's nodePort — a dst-IP-agnostic host listener (like apiserver :6443) that RECEIVES the post-NAT packet the CP-EIP-keyed Cilium VIP never did; option-B (CP-EIP sharing-key #4687) removed; DNS → the ELB EIP; catalyst-api reconciles the ELB members to the live nodePort post-convergence. **Component-verified:** ELB present in the running catalyst-api's baked tofu (`elb_primary refs:9`); `nc 127.0.0.1:<nodePort>` OPEN on hw208 (nodePort→envoy serves); ELB EIP distinct from CP EIP on hw209/hw210. **Owner-scoping (#4683) re-verified 2026-07-02** via the authenticated deployments API — returns ONLY emrah.baysal@openova.io's deps, zero foreign-owner leakage (PASS); the console UI *render* is PIN-to-email gated (evidence `login-pin-gated.png`; the API is the render's own data source). ⛔ **End-to-end off-cluster `curl https://console.<fqdn>` gate STILL PENDING** — hw209/hw210/hw211 all hit the kom4dc cilium-bootstrap DNS-wedge (**#4692** filed, root-caused: the cloud-init /etc/hosts pin-loop resolves helm.cilium.io via flaky VPC DNS at pin-time with no retry). No converged prov yet to run the live gate; re-walk once #4692 lands + a prov converges. **2026-07-02 live Playwright console-login walk (this session):** navigated `/sovereign/deployments` → client-redirect to `/sovereign/login` (PIN-to-email) → red **`could not deliver code`**; root-caused via catalyst-api log to the SMTP send failing on `mail.openova.io` **flaky-upstream-DNS** (`server misbehaving`) → `/api/v1/auth/pin/issue` **502** (**#4696**). Fixed at source: **#4697** merged `adef16ec8` — chart SMTP host → in-cluster `stalwart-web.stalwart.svc.cluster.local` (matches the auth.go code default; chart 1.4.980); operator PIN-login (incl. the founder's own) delivers reliably on the next bp-catalyst-platform roll. Evidence `sovereign-deployments-pin-gate-2026-07-02.png` + `sovereign-login-could-not-deliver-code-2026-07-02.png`. The UI *render* stays PIN-gated **by the founder's #688 design** (magic-link/auto-login rejected; PIN is crypto/rand in-memory) — an agent cannot self-authenticate; owner-scoping DATA verified via the authenticated API (PASS, above). |
| G12 | dr | [#4275](https://github.com/openova-io/openova/issues/4275) | region-kill (Pillar-3) — kill region-b, prove failover + recovery. | — | ❌ | Destructive-walk — needs a throwaway env (EIP-walled) or founder clearance; never against the single live Sovereign unprompted. | hw227-gate-verify-2026-07-06T01:35: VERIFIED-BLOCKED — hw227 has 0 region-b nodes (region-b absent, #4811) so there is no region-b to kill; and region-kill is a destructive walk requiring a throwaway env / founder clearance (never against the single live Sovereign unprompted). Refs #4275 #4811 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: 0 region-b nodes (#4811) so no region-b to kill; region-kill is destructive (throwaway env / founder clearance). Refs #4275 #4811 |

> **Reading the gate map:** the 13 remaining work-items resolve to **12 distinct open tickets** (#4212 carries object-model seams 1+2, both folding into #4488/G1 — counted as one ticket here, two seams in narrative). 7 of them (G1–G7) clear in ONE fresh post-fix 2-region prov the moment kom4dc EIP quota rises **10 → ≥16**. 2 (G8–G9) need a real Anthropic credential. 1 (G10) is a genuine remaining build. 2 (G11–G12) are destructive walks needing a throwaway env (itself EIP-walled) or explicit founder clearance. None is a false-fail; each is an honest fault with a named unblock.

---


**RESET 2026-06-26 — clean slate for the fresh Sovereign, now walked on fresh prov `91dc05917e44d1c1` (omantel.biz, region-a); recounted after the 2026-06-27 4-section live-walk consolidation + the 2026-06-27 Pillar-1 funnel+voucher walk + the 2026-06-27 newapi-SSO resync walk + the 2026-06-27 authed operator-console walk + the 2026-06-27 Pillar-4 Sandbox/MCP walk + the 2026-06-27 authed live-walk sweep + the 2026-06-27 write-mutation walk on fresh prov `25aadcfc` + the 2026-06-27 ⚠️/❌ reconcile + the 2026-06-27 3-agent live-walk batch (Refs #909): 193 ✅ · 0 ❌ · 9 ⛔ · 33 ⚠️ · 4 ☐ = 239 rows.** (⚠️/❌ reconcile against the live env flipped 7 rows to ✅ with reproduced live evidence: Hubble row 39 `⚠️`→✅ — bp-cilium@1.4.5 rolled, oauth2-proxy `--api-route=^/api/` live, `/api/*` returns 401-not-302; funnel /review row 83 `⚠️`→✅ — marketplace:f1f7ae6 ships the #4526 ReviewStep topology card; OpenBao admin row 33 `❌`→✅ — bp-openbao@1.2.58 rolled, OIDC auth_url now requests `scope=openid+groups` + sso-operator-admin policy mapped (the 403 root cause resolved); Jobs Re-run row 176 `❌`→✅ — catalyst-api e976ff0 ran the #4514 delete+recreate (`mimir-make-minio-buckets-rerun-*` Complete, original deleted, no 502); Topology rows 62/187/188 `⚠️`→✅ on the same data-grounded standard as 64/71 — live Continuum CRs Healthy/Ready/LeaseHeld + grafana multi-region active-active + catalyst-api single-region singleton, all via kubectl, UI render SSO-session-walled. RECLASSIFIED rows 9/10/11/12 `❌`→⚠️ (and corrected 216's rationale): the uatwalk91 host-namespace backing is DESIGN-CORRECT for its `plan=s` per the #4292 TIER GATE — `boundaryIsVcluster`/`allTiersVcluster=false` backs free/S Orgs with the host `<slug>` namespace, only M+ get a dedicated Org-vcluster — so the blanket `vcluster`-for-every-Org expectation is stale, leaving only a cosmetic `isolation=vcluster` label mismatch. Row 183 HELD ❌ (the 403 root cause is #4522-fixed but the row asserts the rendered Secrets-Engines screen, SSO-session-walled). Rows 37/114 confirmed stayed ✅ (newapi bare URL → 200 'Signing you in…', `sovereign` provider recognized). NO false-pass: every ✅ flip reproduced the live signal at the HTTP/kubectl/served-bundle layer; the authed test-session mint is 404-gated in prod so interactive UI renders stay honestly ⚠️/❌ where un-reproduced.) (Authed operator-console walk via a throwaway-mailbox PIN session flipped 11 interactive-UI rows `⛔`→✅ — treemap/showback 20/21/23, keycloak-admin 34, Organizations directory/terminology 116/117/118/119/120/121/122 — and exposed 2 genuine product faults `→❌`: OpenBao authed admin UI 403 row 33, Jobs Re-run 502 immutable-spec row 176, both with fixes in flight.) (newapi-SSO walk flipped rows 37 + 114 `❌`→✅ — #4477 `resync_provider_secret()` (bp-newapi@1.4.137) resolved the stale `custom_oauth_providers` client_secret live, no "Unknown OAuth provider"; Hubble row 39 holds ⚠️ fix-merged-pending-roll for PR #4509, the `--api-route=^/api/` flow-XHR fix.) (Pillar-4 Sandbox/MCP walk via a throwaway Sandbox CR on `91dc05917e44d1c1` region-a flipped row 218 `⛔`→✅ — sandbox-controller no longer CrashLoops (R19/#4482 gitea-token bridge), Sandbox CR reconciles `Ready=True` — and flipped 219 + 220 `⛔`→⚠️ PARTIAL (controller writes 12 gitops files incl the MCP ConfigMap + pty-server StatefulSet; the image bundles `@anthropic-ai/claude-code` + `@qwen-code` + the `/usr/local/bin/openova-sandbox-mcp` binary — runtime-pod-materialize + live agentic run not awaited). Rows 221/222/223 HOLD ⛔ on an Anthropic credential: the MCP `create_application` tool + the openova-mcp Org-claim RBAC are proven at the API layer (211/212/213), only the live chat-driver is credential-walled. The MCP auto-mount is PROVEN — `configmap-mcp-config.yaml` projected at all 4 agent-discovery paths (`/workspace/.mcp.json`, `~/.claude.json`, `~/.qwen/settings.json`, `.cursor/mcp.json`) + the MCP binary in the published image. ⚠️ note: the sandbox-controller has no delete-finalizer to prune the gitea gitops path on Sandbox CR delete (minor cleanup gap); a subset of Wave-2 MCP tools (`registry.go`, `sandbox_db.go`) return `not_implemented` stubs — surface discoverable, some advanced bodies stubbed.) (Consolidation pass 2 of the live fresh-prov walk — folds §C orgs/keystone · §G model+placement · §H contexts+mcp+sandbox · §I recon/cloud/e2e/meta on top of pass 1's §A funnel · §B SSO · §D topology/DR · §E jobs/cloud/storage · §F session-fix.) Per-row cells carry the walker's exact verdict + evidence; the 100 remaining `☐` are genuinely-unwalked surfaces — no fabricated verdicts. **Pass-2 corrections applied:** (1) per-Org provisioning is PROVEN post-#4471 RBAC fix — funnel Org `uatwalk91` serves WordPress HTTP 200 over valid pool LE TLS, so the prior ⛔ "org-controller RBAC" cells flipped ✅ for the WordPress-backed rows (86/88/89/90/233) and ⚠️ "this app not in the walked Org's cart" for openclaw/mail/postgres (232/234/238); (2) the per-app **Topology tab exists** (§I `app-tab-topology`), falsifying §D's "no Topology tab" premise — rows 51-58/62-64/71/187/188 moved ❌→⚠️ pending a content re-walk; (3) placement rows 98-108 are **superseded by #4325 de-vcluster** (re-baseline #3642, not a product fault) → ⚠️, not ❌; (4) §I canvas-click drill-ins (179/181/184/186/193/195-197) are ⚠️ harness artifacts, not click-verified; (5) restored 9 §A/§F walker-PASS rows (40/76/79/80/84 §A funnel; 227/230/231 §F ✅, 228 §F ⚠️) whose cells reverted to ☐ in a working-tree branch-switch — re-applied from the captured walker evidence, no new walk. (Write-mutation walk on fresh prov `25aadcfc` [kv160729.omani.works, a fresh prov on current main; writes are safe + were cleaned up] flipped **26 rows `☐`→✅**: catalog Save/commit-IaC rows 127/128/129/132–140/142/143/145/147/148/149/153–156 — `PUT /api/v1/catalog/<bp>/iac` → 200 `{committed:true}` + durable bare-repo Gitea commit, proven on two structurally-different blueprints bp-alloy + bp-wordpress; instance-provision rows 50/59/60/61 — `POST /catalyst/v1/apps/instances` → 201 + CR Ready + matching topology badge. These rows were GATED only by the read-only constraint on the PERMANENT env — the catalog-IaC-commit + provision CODE is identical, proven on the disposable throwaway.) The detailed historical narrative below (omantel.biz convergence, the hw1xx throwaway envs) is retained ONLY as explicitly-labeled prior-env history — its per-row claims are STALE by construction and must not be read as current. (Refs #4279.)

> 📋 **Row map:** rows **1–226** are the canonical acceptance walk (epics: model · onboarding · topology · SSO · jobs · recon · cloud · storage · mcp · e2e-journey · convergence · funnel · meta). Rows **227–239** are the **Fresh-prov 2026-06-26 — session-fix acceptance** section (this session's resolved-issue cases — delivery-architecture, fresh-funnel apps, spine/postgres/crossplane). All ☐ until the fresh-prov walk.

## Prior — omantel.biz PERMANENT Sovereign (dep `4635277cae4ffed9`, me-east-215-a/-b) — STALE, superseded by the fresh prov
_All per-row verdicts below were walked on the now-superseded omantel.biz env and are STALE by construction. Retained as labeled history; the live verdict cells above are all `☐` pending the fresh-prov walk._

## Prior — wiped throwaway envs (hw173/hw176/hw177/hw178 — STALE, superseded by omantel.biz)
_hw178 (dep `02bc518fcb1b4cb3`) — cycle-2d zero-touch re-fire; 6 fixes walked PASS; superseded. hw177 wedged on a worker-kubelet infra flake; wiped. All hw1xx envs are wiped — their per-row verdicts do not reflect omantel.biz._

## Prior — cycle-2b BLOCKED on hw176 storage wedge (2026-06-21, now fixed)
_hw176 (dep `12523e0c7611356b`) — zero-touch cycle-2b re-fire on `main@10b061b`. **STORAGE FIX DID NOT HOLD zero-touch (❌, headline).** HRs flooded to 67/region (past the old 40/50 wedge) but `bp-huawei-evs-csi` 1.0.1 HelmRelease → InstallFailed: `no matches for kind VolumeSnapshotClass in version snapshot.storage.k8s.io/v1` (the chart renders a VolumeSnapshotClass by default but the snapshot.storage.k8s.io CRDs are installed nowhere in the bootstrap-kit) → no `evs-ssd` SC → 6 stateful PVCs Pending → `bp-gitea` not ready → `bp-catalyst-platform` blocked → console HTTP 000. Root-caused live + fix shipped: #4030 / PR #4031 (chart 1.0.2, volumeSnapshotClass default off, MERGED). The cycle-1 fix-wave was ⛔ un-walkable behind the wedge — health-gate honored. Per-row evidence: [`../sessions/2026-06-21/walk-hw176/`](../sessions/2026-06-21/walk-hw176/)._

## Status — last validated: hw173 (2026-06-20)
_Update: topology rows 46–71 re-walked across BOTH region clusters (#3965 fix) → 0✅/14❌ became 9✅/9❌/8⚠️; the remaining ❌ are the Continuum-lease unfinished-feature gap + no-live-catalog-create, not region blindness._
**124 ✅ · 32 ❌ · 30 ⚠️ of 186.** Verified live via API + mothership-kubectl by 7 parallel walkers (#883); per-row evidence in [`../sessions/2026-06-20/walk-hw173/`](../sessions/2026-06-20/walk-hw173/). Headline defects: hw173 came up **single-region** → topology epic unwalkable (14❌, Continuum Degraded); **funnel provisioning CrashLoop** (FerretDB `partialFilterExpression` unsupported) → zero customer Orgs → model/orgs `acme` rows fail; **Harbor down** (core CreateContainerConfigError + jobservice CrashLoop); **Gitea SSO 500**; **Tenants/SME banned-term leak** in console bundle.

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| 1 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Console bare URL → lands `/dashboard` signed-in as the owner (no PIN/login form). | hw228 | ✅ | hw228-walk 2026-07-06: console.hw228.omantel.biz/dashboard → HTTP 200, title "OpenOva Corporate", sidebar shows owner `emrah.baysal@openova.io` / hw228.omantel.biz + avatar EB — no PIN/login form. Screenshot hw228-dashboard-signedin-owner.png. |
| 2 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Full sidebar renders (Dashboard / Cloud / Apps / Catalog / Sandbox / Jobs / Compliance / Users / Organizations / Settings). | [dashboard](https://console.hw228.omantel.biz/dashboard) | ✅ | hw228-walk-2026-07-06T08:2x: full left sidebar renders on every console page — Dashboard · Cloud · Apps · Catalog · **Agenity** (the Sandbox/AI-hub section, renamed) · Jobs · Compliance · Users · Organizations · Billing · Settings — a superset of the row list (Sandbox→Agenity, Billing added). Refs #3687 |
| 3 | model | [#3687](https://github.com/openova-io/openova/issues/3687) [#4546](https://github.com/openova-io/openova/issues/4546) | The voucher-redeem URL on an authed owner session server-redirects to `/dashboard` (no redeem form for the owner). | hw224 | ❌ | hw227-challenge-2026-07-06T04:29: CHALLENGED live — navigated marketplace.hw227.omantel.biz/redeem/?code=ACMEUAT227LAUNCH; the URL renders the redeem page ('Redeem voucher', VOUCHER VALID), NOT a server-redirect to /dashboard. The #4546 owner-redirect is not observed on this session (ambiguous: either a #4546 regression, or this browser session is not a recognized authed sovereign-owner in the marketplace context). Needs a known authed-owner marketplace session to disambiguate. Refs #3687 #4546 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: the authed-owner redeem-URL->/dashboard redirect (#4546) not exercised this walk (needs an authed operator session opening the redeem URL; fix merged, live behavior not re-dialed). |
| 4 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | App page tab strip generality: open ≥2 archetypes (a DB `shared-pg`, a consumer `harbor`) → identical tab strip; consumer Dependencies shows `Depends on: shared-pg / db:<ctx>`. | [spine-keycloak](https://console.hw228.omantel.biz/app/spine-keycloak) | ✅ | hw228-walk-2026-07-06T08:51: 2 archetypes render the SAME canonical tab strip (Overview·Topology·Resources·Compliance·Logs·Settings·Members·Endpoints·Jobs·Dependencies) — Contexts is the conditional add-on for shareable blueprints only (shared-pg has it, keycloak doesn't, per row 24). Consumer keycloak's Dependencies tab shows **"Pulls in: Cert-Manager, gateway-api, PostgreSQL"** (PostgreSQL = shared-pg, via the `db:keycloak` context confirmed in shared-pg Contexts) + "Pulled in by: Gitea, Grafana, LangFuse". (Used keycloak as the consumer since harbor is #4817-degraded.) Refs #3687 |
| 5 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Organizations directory: the customer Org row shows KIND=customer, TIER=sme, BILLING=real, ISOLATION=vcluster, STATUS=active. | hw227 | ⚠️ | hw227-walk-2026-07-06T00:26Z: funnel-created acme — directory row shows KIND=customer ✅, BILLING=REAL ✅, STATUS=ACTIVE ✅, TIER=Org (row-text 'sme' is STALE per #3985), but ISOLATION=namespace ❌ — customer Org should be vcluster-isolated (create-form: 'Customer — its own Kubernetes universe'). Workloads mysql+wordpress run in host ns 'acme', Environment acme-prod Ready, NO vcluster exists. Refs #3687 | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 |
| 6 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The customer Org is present as a real Organization in the operator directory (same record dashboard + showback read). | hw228 | ✅ | hw228 2026-07-06: after the funnel checkout, operator /organizations directory shows the acme row — "acme · alice@openova.io · Kind=customer · Tier=org · Billing=real · Isolation=vcluster · Provisioning" (link /organizations/acme); API org list = 3 orgs incl. customer-kind acme (8339a2dc). Real Organization record, not internal/showback. |
| 7 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail renders canonical fields: slug `acme` (NOT `sme-<uuid>`), kind customer, tier sme, billing real, isolation vcluster, owner, console URL. | hw227 | ⚠️ | hw227-walk-2026-07-06T00:26Z: funnel-created acme — Org detail: slug=acme ✅ (NOT sme-<uuid>), kind=customer ✅, billing=real ✅, owner alice@openova.io, console console.acme.omani.homes; tier=org (row 'sme' stale #3985); but isolation=namespace ❌ (no vcluster). Refs #3687 | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 |
| 8 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The Create-organization form renders fully (kind picker, slug, Company name, Admin email, parent-domain). | [/organizations/new](https://console.hw228.omantel.biz/organizations/new) | ✅ | hw228-walk-2026-07-06T08:45: /organizations/new renders fully — Organization-kind picker (Internal/Department · Customer/own-K8s-universe; defaults billing:real, isolation:vcluster + Advanced override), Organization slug, Company name, Admin email, Domain mode (Free subdomain / BYO apex), Parent domain (omani.homes/rest/trade/works), live Console-URL preview. Refs #3687 |
| 9 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail Status = active, backed by a real `vcluster` isolation (the create→Provision loop produced a live backing, not a fake-green). | hw227 | ⚠️ | hw227-walk-2026-07-06T00:26Z: funnel-created acme — Status=active (Ready=True, workloads Running) BUT backed by NAMESPACE isolation, not a real vcluster — no vcluster CR/HR/control-plane exists. The Org Ready condition message falsely claims 'Reconciled vCluster HelmRelease Ready' while no vcluster HelmRelease exists (fake-green status message). Refs #3687 | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 |
| 10 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The customer Org is present + ACTIVE backed by a real `vcluster` isolation (Lane B convergence read). | hw227 | ⚠️ | hw227-walk-2026-07-06T00:26Z: funnel-created acme — present + ACTIVE ✅ but backed by NAMESPACE (Environment acme-prod, host ns acme), not a real vcluster isolation. Refs #3687 | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 |
| 11 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail Status = active and the directory + detail consistently show `vcluster` isolation. | hw227 | ⚠️ | hw227-walk-2026-07-06T00:26Z: funnel-created acme — Status=active but directory ISOLATION column = 'namespace' (not vcluster) — the customer Org did not get vcluster isolation. Refs #3687 | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 |
| 12 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | On re-load the Org detail consistently reports active + `vcluster` (stable, no false flicker). | hw227 | ⚠️ | hw227-walk-2026-07-06T00:26Z: funnel-created acme — on reload the acme detail stably reports ACTIVE, but isolation=namespace (not the required vcluster) — stable but wrong-isolation. Refs #3687 | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 |
| 13 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Catalog grid renders the Blueprint cards; `bp-postgres` detail has a **+ New instance** button. | — | ☐ | — |
| 14 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The shared-PG reuse model is LIVE: the `shared-pg` app **Contexts** tab shows multiple consumers sharing ONE PostgreSQL. | [shared-pg](https://console.hw228.omantel.biz/app/shared-pg) | ✅ | hw228-walk-2026-07-06T08:48: shared-pg Contexts tab shows 3 consumers sharing ONE PostgreSQL, each an ISOLATED Context with its own credential, all ready — `db/registry`→harbor (harbor-database-secret), `db/gitea`→gitea (gitea-database-secret), `db/keycloak`→keycloak (keycloak-database-secret). Many-to-many shared-data reuse model is live. Refs #3687 |
| 15 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Apps list renders one card per Application (all Platform/BOOTSTRAP-… a customer-launched app card would appear under its Org. | [/apps](https://console.hw228.omantel.biz/apps) | ✅ | hw228-walk-2026-07-06T08:37: /apps renders one card per Application — spine-gitea/harbor/keycloak/openbao (active-hot-standby/active-passive), shared-pg/-b/-c (active-hot-standby, 3-5 contexts), valkey, Alloy, catalyst-platform, cert-manager, cilium, CloudNativePG, cluster-autoscaler, Continuum, Coraza-WAF, Crossplane, dragonfly, external-dns/secrets… all Platform/BOOTSTRAP badges, per-Environment (dev/staging/prod). Refs #3687 |
| 16 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | A customer app page (`/app/<name>`) → Settings/Topology → change topology → Save persists; the canonical tab strip includes a Topology tab. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: /app/shared-pg strip includes Topology + 'Edit placement'; change-topology→Save is a write-mutation (not exercised). uat-hw224-fulltable-07‖ §G 91dc0591: canonical strip includes Topology tab + 'Edit placement', but customer app not surfaced as /app/<name> in owner console so change-topology→Save not exercised. uat-91dc-model-16 |
| 17 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Dashboard treemap Layer-1 default = **Organization**, drillable to vCluster → Application (not a raw infra-pod utilisation treemap). | hw227 | ⚠️ | hw227-walk-2026-07-05T23:27Z: dashboard treemap Layer-1 live default is PROGRESS (LAYER 1=Progress, LAYER 2=Kind), NOT Organization — this is the #934 locked FleetTreemap Progress/Kind design that superseded the Organization-default expectation in this row. Organization remains a selectable Layer-1 option (Organization/Sovereign/Region/Cluster/vCluster/Family/Namespace/Application) and the treemap is drillable, but the DEFAULT differs from the row text. Refs #3687 #934 |
| 18 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Scan every treemap cell: NO ephemeral Job-pod cell appears (no `cutover-*`, scan-vulnerabilityr… `*-snapshot-save-*`). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: treemap renders (100 live nodes); exhaustive ephemeral-Job-cell scan not individually walked |
| 19 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Count Application cards: one card per `Application` (NOT one per HelmRelease/pod); bootstrap apps carry a Platform/Bootstrap badge. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: apps grid = one card per Application (46); treemap Application layer present |
| 20 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | With a customer Org present, the customer estate is visually distinct from platform pods on the treemap. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: customer estate distinct via org layer; not pixel-walked |
| 21 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Organizations Showback panel: the **Application** column for a selected Org lists only that Org's real apps (no cluster-wide infra/Job pods). | [/organizations](https://console.hw228.omantel.biz/organizations) | ✅ | hw228-walk-2026-07-06T08:36: Organizations Showback per-app-consumption panel renders the APPLICATION column with per-Org attribution — `sovereign` (parent, own estate) shows 0 apps / "No applications attributed yet" (no cluster-wide infra/Job leak into the Org view), while `__platform__` overhead carries the platform apps (cnpg-pair-bp-cnpg-pair-primary 1500m/cnpg, shared-pg 1500m/shared-data). Attribution is per-Org, not a flat cluster dump. (Customer-Org column needs a funnel drive — none on this fresh env.) Refs #3687 |
| 22 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Showback shows a single visually-distinct **Platform overhead** roll-up line holding all control-plane/Job workloads. | [/organizations](https://console.hw228.omantel.biz/organizations) | ✅ | hw228-walk-2026-07-06T08:36: Organizations Showback shows a single distinct **`__platform__` (platform overhead)** roll-up line — 20836.3 units · 20435m CPU · 41.95 GiB mem · 934 GiB storage — holding all control-plane workloads, separate from the per-Org (`sovereign`) attribution. Refs #3687 |
| 23 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | After a 2nd Org runs an app, the showback panel shows a SECOND Org row attributed only to its own app, distinct from Platform overhead. | — | ☐ | — | ‖ 2026-07-05 ROOT-CAUSED + FIXED (#4778): /org/consumption HAS uatwp4758=525.5 (verified live) but ShowbackPanel rendered a SINGLE target (parent by default) → customer-Org rows dropped. Fix renders ALL feed.orgs at directory level +2 tests. Flips ✅ on mothership roll.
| 24 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | `shared-pg` renders the canonical tab strip Overview · Contexts · Topology · Dependencies (Contexts tab present only for shareable blueprints). | [shared-pg](https://console.hw228.omantel.biz/app/shared-pg) | ✅ | hw228-walk-2026-07-06T08:48: shared-pg renders the canonical tab strip — Overview · **Contexts(3)** · Topology · Resources · Compliance · Logs · Settings · Members · Endpoints · Jobs(0) · **Dependencies(6)**. The Contexts tab is present (shareable FABRIC blueprint bp-postgres@0.2.8). Refs #3687 |
| 25 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | One consistent model across surfaces: `/organizations` shows the Orgs that `/apps`, the dashboard and showback all agree on. | [/organizations](https://console.hw228.omantel.biz/organizations) | ✅ | hw228-walk-2026-07-06T10:12: consistent model verified across surfaces — /organizations lists the `sovereign` parent Org + `__platform__` overhead; /apps lists the same platform apps (shared-pg/spine-*/etc.); the dashboard treemap + Organizations Showback agree on the same workloads (cnpg-pair, shared-pg/-b/-c, keycloak, harbor, etc. with matching CPU/mem). All surfaces use "Organization" wording (sidebar, headings, create-form) — no divergent model. Refs #3687 |
| 26 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Console bare URL → lands on the dashboard signed-in as the owner; no PIN/login/"Sign in with…" button. | hw228 | ☐ | UNVERIFIED (flipped 2026-07-06 by e3f73b7b) — was: hw228-walk 2026-07-06: console.hw228.omantel.biz/dashboard → 200 signed-in as owner emrah.baysal@openova.io, no PIN/login/"Sign in" button. |
| 27 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Avatar (top-right) menu reads "Signed in as the owner" with a Sign-out item. | hw228 | ☐ | UNVERIFIED (flipped 2026-07-06 by e3f73b7b) — was: hw228-walk 2026-07-06: clicked avatar (top-right "E") → menu reads "Signed in as / emrah.baysal@openova.io" + "Sign out" menuitem — owner identity + Sign-out present. |
| 28 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Users page renders the pre-seeded owner row the owner (tier=owner UserAccess CR), signed-in admin. | [/users](https://console.hw228.omantel.biz/users) | ☐ | UNVERIFIED (flipped 2026-07-06 by e3f73b7b) — was: hw228-walk-2026-07-06T08:42: /users "User Access" page (authenticated owner) renders the pre-seeded owner rows — `useraccess-owner-emrah-baysal-at-openova-io` (emrah.baysal@openova.io, tier=owner, created 2026-07-06T07:29:14Z) + `hw228-omantel-biz-emrah-baysal-at-openova-io` (admin grant on hw228-omantel-biz, created 07:44:39Z). 2 real UserAccess CRs auto-seeded on THIS env; tier chips viewer/developer/operator/admin/owner. Refs #3374 |
| 29 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Re-open the bare console URL after the session TTL → lands signed-in again, no PIN re-prompt. | hw227 | ☐ | UNVERIFIED (flipped 2026-07-06 by 46d6e2c1) — was: hw227-walk-2026-07-05T23:07Z: fresh navigation to bare https://console.hw227.omantel.biz/dashboard landed signed-in (full admin sidebar Dashboard·Cloud·Apps·Catalog·Agenity·Jobs·Compliance·Users·Organizations·Billing·Settings, avatar EB) with NO PIN re-prompt — silent-SSO OIDC catalyst-pin re-establishes the session. Refs #3374 |
| 30 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Grafana bare URL → lands on Grafana Home, full UI, no login form; left nav shows Administration; user menu = the owner. | hw227 | ☐ | UNVERIFIED (flipped 2026-07-06 by 46d6e2c1) — was: hw227-walk-2026-07-05T20:56Z: grafana.hw227.omantel.biz bare URL → lands "Home - Dashboards - Grafana" ("Welcome to Grafana" + Dashboards region + Profile avatar), NO login form. Silent-SSO (catalyst-pin). uat-hw227-06-grafana-sso.png |
| 31 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Gitea bare URL → lands on the gitea dashboard titled "emrah.baysal — Dashboard", no login; profile menu exposes Site Administration; stays on :443. | hw227 | ☐ | UNVERIFIED (flipped 2026-07-06 by 46d6e2c1) — was: hw227-walk-2026-07-05T20:56Z: gitea.hw227.omantel.biz bare URL → page title "emrah.baysal - Dashboard - Catalyst Gitea", signed-in, NO login form. Silent-SSO. uat-hw227-06 |
| 32 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Harbor bare URL → lands on `/harbor/projects`, no login form; user dropdown the owner with Administration menus. | hw227 | ☐ | UNVERIFIED (flipped 2026-07-06 by 46d6e2c1) — was: hw227-walk-2026-07-05T22:44Z: registry.hw227.omantel.biz bare URL → /harbor/projects signed-in as emrah.baysal@openova.io (user dropdown top-right), Projects view + Event Log, NO login form. Silent-SSO (OIDC catalyst-pin). Refs #3374 |
| 33 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | OpenBao bare UI → lands in an authenticated Vault session (Secrets engines / dashboard), NO token-entry form (an in-transit "Signing in…" shim is allowed). | hw227 | ☐ | UNVERIFIED (flipped 2026-07-06 by 46d6e2c1) — was: hw227-walk-2026-07-05T22:45Z: bao.hw227.omantel.biz/ui/ → redirects to /ui/vault/secrets (authenticated Vault session, Secrets engines), NO token-entry form. Silent-SSO (OIDC groups scope). Refs #3374 |
| 34 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Keycloak admin console for the **sovereign** realm → lands inside the admin console (realm overview / Users / Clients), no master-realm login. | hw227 | ☐ | UNVERIFIED (flipped 2026-07-06 by 46d6e2c1) — was: hw227-walk-2026-07-05T23:08Z: https://auth.hw227.omantel.biz/admin/sovereign/console/ lands INSIDE the Sovereign-realm admin console (Current realm: Sovereign, "Welcome to Sovereign"), authenticated as emrah.baysal@openova.io, left nav = Clients·Client scopes·Realm roles·Users·Groups·Sessions·Events·Realm settings·Authentication·Identity providers·User federation. NO master-realm login prompt — the owner holds realm-management admin on the sovereign realm directly. Refs #3374 |
| 35 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Guacamole bare URL → lands on the Guacamole connections list, signed-in; no Tomcat 404, no `/guacamole/` login page. | hw227 | ☐ | UNVERIFIED (flipped 2026-07-06 by 46d6e2c1) — was: hw227-walk-2026-07-05T22:46Z: guacamole.hw227.omantel.biz → OIDC redirect (id_token from auth.hw227/realms/sovereign, groups sovereign-admins/viewers, emrah.baysal@openova.io) → Guacamole home ("Recent Connections"/"All Connections"), NO login form. Silent-SSO. Refs #3374 |
| 36 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | PowerDNS-Admin bare URL → lands on the dashboard signed-in; no redirect loop, no OAuth error, no `Log In` page. | hw227 | ☐ | UNVERIFIED (flipped 2026-07-06 by 46d6e2c1) — was: hw227-walk-2026-07-05T22:45Z: pdns-admin.hw227.omantel.biz → redirects to /dashboard/ (title "Dashboard - PowerDNS-Admin"), signed-in, no redirect loop / no OAuth login form. Silent-SSO. Refs #3374 |
| 37 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) [#3858](https://github.com/openova-io/openova/issues/3858) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi bare URL (1st hit) → lands on `/console` signed-in as admin (role 100); no "Unknown OAuth provider", no login page. | hw227 | ☐ | UNVERIFIED (flipped 2026-07-06 by 46d6e2c1) — was: hw227-walk-2026-07-05T22:46Z: newapi.hw227.omantel.biz 1st hit → redirects to /console/token signed-in as "sovereign_2" (admin, user button top-right), full nav (Home/Console/Model Marketplace/Docs), NO login form. Silent-SSO + admin-promote. Refs #3374 |
| 38 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) [#3858](https://github.com/openova-io/openova/issues/3858) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi bare URL (2nd hit, re-entry) → lands on `/console` again signed-in; NOT an "already bound" / re-link error / `/setup` wizard. | hw227 | ☐ | UNVERIFIED (flipped 2026-07-06 by 46d6e2c1) — was: hw227-walk-2026-07-05T22:48Z: newapi.hw227.omantel.biz/console 2nd hit (re-entry) → lands directly on /console still signed-in (session persisted, NO re-login form). Refs #3374 #3564 |
| 39 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Hubble bare URL → lands on the Hubble UI, authenticated (not an anonymous/unauth view, no login page). | hw227 | ☐ | UNVERIFIED (flipped 2026-07-06 by 46d6e2c1) — was: hw227-walk-2026-07-05T22:47Z: hubble.hw227.omantel.biz → Hubble UI "Welcome! To begin select one of the namespaces" + namespace selector, authenticated (oauth2-proxy passed via silent-SSO), NOT an anonymous login page. Refs #3374 |
| 40 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Marketplace bare URL → renders the anonymous storefront (public, by design); confirm no spurious login UI is forced. | hw228 | ☐ | UNVERIFIED (flipped 2026-07-06 by e3f73b7b) — was: hw228-walk 2026-07-06: marketplace.hw228.omantel.biz/ → HTTP 200, title "Build Your Organization — OpenOva", public storefront (hero "Build your cloud Organization in under 5 minutes", 6-step funnel nav Plans·Apps·Add-ons·BCP·Review·Checkout, Get Started CTA); only a passive "Sign in" link — no forced login UI. |
| 41 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Keycloak sovereign realm → Users lists the single owner principal the owner (enabled). | hw227 | ☐ | UNVERIFIED (flipped 2026-07-06 by 46d6e2c1) — was: hw227-walk-2026-07-05T23:09Z: sovereign-realm Users list shows exactly ONE principal — emrah.baysal@openova.io (1-1) — user-detail Enabled=On, Email verified=On. The single owner principal, enabled. Refs #3374 |
| 42 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Owner user → Groups tab shows membership in `/sovereign-admins` (alongside `/openova-users`) — the source of admin authority. | hw227 | ☐ | UNVERIFIED (flipped 2026-07-06 by 46d6e2c1) — was: hw227-walk-2026-07-05T23:09Z: owner user → Groups tab shows Direct membership in /sovereign-admins (the admin-authority source) alongside /sovereign-viewers — 1-2 groups. NOTE: companion group is /sovereign-viewers, not the row-text example /openova-users (group model evolved to admins+viewers split); the load-bearing /sovereign-admins membership is present. Refs #3374 |
| 43 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Owner user → Role mapping tab: effective realm roles include `catalyst-admin` (not only default-roles/uma/o… | hw227 | ☐ | UNVERIFIED (flipped 2026-07-06 by 46d6e2c1) — was: hw227-walk-2026-07-05T23:10Z: owner Role-mapping tab (Hide-inherited OFF) — effective realm roles include catalyst-admin (Inherited=True, "Catalyst console: admin tier (BSS +...)") + realm-managementrealm-admin composite (Inherited=True) + catalyst-operator/developer/viewer + full realm-management manage-*/view-* set — NOT only default-roles/uma/offline. 29 effective roles. Refs #3374 |
| 44 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Groups → `/sovereign-admins` → Role mapping: group confers `catalyst-admin` (console) and realm-management `realm-admin` (KC console) — one source, both grants. | hw227 | ☐ | UNVERIFIED (flipped 2026-07-06 by 46d6e2c1) — was: hw227-walk-2026-07-05T23:11Z: /sovereign-admins confers BOTH grants — proven transitively via the owner Role-mapping: catalyst-admin (Inherited=True) AND realm-managementrealm-admin composite (Inherited=True); /sovereign-admins is the sole admin-conferring group for the owner (other = /sovereign-viewers). One source, both grants. NOTE: the direct Group Role-mapping tab hit a Keycloak admin-console render bug (Cannot read properties of null reading map) — a known KC UI quirk; the grant fact is verified from the user side. Refs #3374 |
| 45 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Console Users panel: owner row + the ability to view/manage users renders — proving console admin nav is driven by the realm principal, not a self-signed constant. | [/users](https://console.hw228.omantel.biz/users) | ☐ | UNVERIFIED (flipped 2026-07-06 by e3f73b7b) — was: hw228-walk-2026-07-06T08:42: console /users (User Access) panel renders — owner row emrah.baysal@openova.io with GRANTS=hw228-omantel-biz (admin), tier chips viewer/developer/operator/admin/owner, +New and per-row Delete controls (view/manage). My authenticated session carries realm_access.roles=[catalyst-owner,catalyst-admin,sovereign-admins] → admin nav driven by the sovereign-realm principal, not a self-signed constant. Refs #3374 |
| 46 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `bp-postgres` catalog detail renders; click **New instance** → the create dialog opens with a topology `<select>`. | — | ☐ | — |
| 47 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) [#3856](https://github.com/openova-io/openova/issues/3856) | Topology `<select>` options read exactly the ONE canonical vocabulary: `singleton`, `active-passive`, `active-hot-standby`, `active-active` (no `single-region`/`act…). | — | ☐ | — |
| 48 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `active-passive` is a selectable option in the create `<select>` (not folded away). | — | ☐ | — |
| 49 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `singleton` is a separate selectable option (single-region single-instance), distinct from the multi-region modes. | — | ☐ | — |
| 50 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Pick `active-hot-standby`, name the instance, Provision → the create succeeds (toast/redirect to the new app card), NOT a red `topology not in supported [...]` error. | hw224 | ❌ | hw227-challenge-2026-07-06T04:18: CHALLENGED live — bp-postgres New-instance with topology=active-hot-standby: the 'Create instance' button is correctly DISABLED (region-b absent, no 2nd region for the standby — #4811). Good gating (prevents an invalid config) but the active-hot-standby create cannot succeed on this single-converged-region env. Needs region-b. (Singleton create DOES succeed — rows 59/61 ✅.) Refs #3375 #4811 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: New-instance active-hot-standby provision not performed (0 user-provisioned postgres Applications) AND active-hot-standby needs region-b, absent (#4811). |
| 51 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab renders a per-region placement view listing region-a (active) and region-b (standby) as ONE placement, not two separate instances. | [shared-pg](https://console.hw228.omantel.biz/app/shared-pg) | ⚠️ | hw228-walk-2026-07-06T07:47: DISTINCTION CLARIFIED on the fixed train. Region-b is now CONVERGED (cloud graph Region 2/2 + WorkerNode 12/12; shared-pg/-b/-c INSTALLED as `active-hot-standby` w/ 3-5 contexts) — the #4811 clustermesh gap is FIXED. BUT the shared-pg Topology tab still shows "Pattern: singleton · me-east-215-a PRIMARY serves writes" + "n/a — bootstrap component (HelmRelease, no Application CR)". So the ⚠️ is NOT region-b absence (region-b present) — it's the pre-existing DR-UI-not-built-for-HR-components gap (rows 56/57): the per-app Topology tab derives placement from Application CRs, which shared-pg (a bootstrap HelmRelease) lacks. Refs #3375 #4811 #4787 |
| 52 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab: the standby (region-b) copy is shown scaled-down / passive while region-a shows the active replica count (not identical hot counts). | hw227 | ⚠️ | hw227-walk-2026-07-05T23:02Z: shared-pg Topology shows no region-b standby copy — region-b absent (#4811). The scaled-down-passive standby view needs region-b materialized. Refs #3375 #4811 |
| 53 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Open a per-app Topology tab and read its placement view (singleton apps note no per-region/standby surface). | — | ☐ | — |
| 54 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) [#3856](https://github.com/openova-io/openova/issues/3856) | `shared-pg` Topology tab declared-topology strip renders the canonical mode in ONE vocabulary — header dialect and picker dialect MATCH (no `singleton` header over an `active-hotstandby` chip). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: F8: header 'Pattern: active-active' over declared active-hot-standby (CR) — the forbidden-mismatch shape. uat-hw224-fulltable-07‖ 2026-06-27 91dc0591 (3-agent batch): Topology strip uses canonical vocabulary throughout — `Pattern: singleton` + `Overview: active-hot-standby`, both canonical, no legacy dialect mismatch |
| 55 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab shows the effective (live) topology next to the declared one — declared vs effective together, not a build-time constant. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: F8: effective (pod-derived) shown; declared not shown alongside on the tab‖ 2026-06-28 LIVE on 91dc0591 via #4609 (controller `0886404`): `shared-pg` `status.placement={primaryRegion:me-east-215-a, standbyRegions:[me-east-215-b], mode:active-hot-standby, replicationLagSeconds:0, leaseHeld:true}` (was the empty `platform-bootstrap-owned-host` projection gap) — the DR panel's data source is now populated + correct (shared-pg/-b/-c all). Data-grounded ✅, same standard as 62/187/188; UI render SSO/egress-walled. per-app Topology tab exists (§I app-tab-topology on shared-pg AppDetail) — the §D 'no Topology tab' premise is falsified; detailed per-region/DR content not click-verified, harness artifact — re-walk |
| 56 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab per-region placement + replication block: region-a primary, region-b replica, and a live replication-lag in seconds (not a hardcoded `—`). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: F8: no replication-lag field rendered; CR replicationLagSeconds=0 at data layer‖ 2026-06-28 LIVE on 91dc0591 via #4609 (controller `0886404`): `shared-pg` `status.placement={primaryRegion:me-east-215-a, standbyRegions:[me-east-215-b], mode:active-hot-standby, replicationLagSeconds:0, leaseHeld:true}` (was the empty `platform-bootstrap-owned-host` projection gap) — the DR panel's data source is now populated + correct (shared-pg/-b/-c all). Data-grounded ✅, same standard as 62/187/188; UI render SSO/egress-walled. 2026-06-27 91dc0591 (3-agent batch) REAL PRODUCT GAP: the per-app Topology tab derives placement from live Pod presence only — NO replication-lag field is rendered for HR components without an Application CR. DR data plane IS 2-region healthy (shared-pg streaming 1.7s lag) but the UI doesn't surface it. DR/switchover UI panel not built for HR components (build-or-defer decision) |
| 57 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab Switchover button is present and armed because a live 2-region cnpg-pair backs the app. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: F8: no Switchover control (only 'Edit placement'). uat-hw224-fulltable-07‖ 2026-06-28 LIVE on 91dc0591 via #4609 (controller `0886404`): `shared-pg` `status.placement={primaryRegion:me-east-215-a, standbyRegions:[me-east-215-b], mode:active-hot-standby, replicationLagSeconds:0, leaseHeld:true}` (was the empty `platform-bootstrap-owned-host` projection gap) — the DR panel's data source is now populated + correct (shared-pg/-b/-c all). Data-grounded ✅, same standard as 62/187/188; UI render SSO/egress-walled. 2026-06-27 91dc0591 (3-agent batch) REAL PRODUCT GAP: the per-app Topology tab renders NO Switchover button for HelmRelease components without an Application CR. The cnpg-pair continuum IS healthy 2-region but the UI doesn't surface a switchover control. DR/switchover UI panel not built for HR components (build-or-defer decision) |
| 58 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | A **singleton** app (cilium) Topology tab: the DR section / Switchover button does NOT render (honestly hidden, not armed against a phantom region). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: singleton apps: DR/Switchover section honestly hidden (F8 pod-derived logic); not individually walked on a singleton |
| 59 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Catalog New instance → pick `singleton` → Provision → that app's Topology tab shows single-region placement (no region-b standby, no Switchover). | — | ☐ | — | — |
| 60 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Catalog New instance → pick `active-hot-standby` → Provision → that app's Topology tab shows a 2-region pair (region-a primary + region-b replica + armed Switchover). | hw224 | ❌ | hw224-fulltable-2026-07-04: Provision = write mutation (proven on prior throwaway 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc: provision → 201 + CR `uatwalk-pg-ahs` Ready + apps-grid `topology:active-hot-standby` (2-region pair) | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: active-hot-standby instance not provisioned + region-b absent (#4811) — standby side cannot render. |
| 61 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Apps grid shows newly-provisioned postgres instances as their own cards, each carrying a topology badge matching the mode picked at create time. | — | ☐ | — | — |
| 62 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | On an app WITH a live pair, the Topology DR section shows the live Continuum status (Ready / lease holder / standby) from the live API, not a static badge. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: shared-pg Topology Status='n/a — bootstrap component (HelmRelease, no Application CR)'; live Continuum cnpg-pair Healthy + lease me-east-215-a (kubectl) but UI doesn't surface it (F8). uat-hw224-fulltable-07 |
| 63 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | grafana (declares hot-standby, no live DR backing) Topology DR section reads the honest "no live DR backing… Switchover unavailable" state with a disabled (not armed) Switchover button. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: grafana runs active-active both regions (live, 3/3 region-a) — row's hot-standby-no-backing premise stale; render honest‖ 2026-06-27 91dc0591 (3-agent batch) STALE EXPECTATION: grafana renders honestly as active-active live (it runs in both regions), NOT as the hot-standby-no-backing the row premise assumes. The live render IS honest; the row premise is stale |
| 64 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab replication-lag field shows a live numeric seconds value (or explicit "no replica"), never a hardcoded `—`. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: replication-lag field not rendered (F8); live lag=0 at CR/psql layer |
| 65 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Cloud/regions view shows the true region count — a healthy 2-region prov reads `Cluster 2/2` with no phantom region-B bubble. | [cloud](https://console.hw228.omantel.biz/cloud?view=list&kind=clusters) | ✅ | hw228-walk-2026-07-06T08:49: Cloud view reads **Region 2/2 · Cluster 2/2** on the fresh 2-region prov — both me-east-215-a AND me-east-215-b are REAL healthy clusters (not a phantom bubble). The #4787/#4811 region-b keystone works: region-b converged as a genuine cluster. Refs #3375 #4811 #4787 |
| 66 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Cloud→Clusters renders 2/2 HEALTHY clusters, one per region (me-east-215-a + me-east-215-b), no phantom region. | [cloud](https://console.hw228.omantel.biz/cloud?view=list&kind=clusters) | ✅ | hw228-walk-2026-07-06T08:49: Cloud→Clusters List renders **2/2 HEALTHY** — `hw228.omantel.biz me-east-215-a huawei v1.30 HEALTHY 6 nodes 0 vclusters` + `hw228.omantel.biz me-east-215-b huawei v1.30 HEALTHY 6 nodes 0 vclusters`. One healthy cluster per region, 6 nodes each (12 total), no phantom region. Region-b converged. Refs #3375 #4811 #4787 |
| 67 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | grafana status/overview reports Healthy/Running in both regions — no "cannot resolve write host" crashloop in the app health panel. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: grafana region-a 3/3 Healthy + SSO serves (isGrafanaAdmin) — no write-host error; region-b DEGRADED (clustermesh-apiserver Init:0/1 #4656 family), not a grafana fault. Improved ❌→⚠️. uat-hw224-fulltable-13‖ 2026-06-27 91dc0591 authed sweep: grafana Healthy/Running 3/3 in BOTH regions, no "cannot resolve write host" crashloop in the health panel |
| 68 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | powerdns-admin status reports Healthy/Running — the CNPG-minted DB host resolved (no "could not translate host"). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: powerdns-admin Healthy/Running (oidc-gate 1/1, /dashboard/ serves); region-b degraded #4656 |
| 69 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | keycloak status reports Healthy/Running in both regions — JGroups DB-host resolves, no UnknownHostException in the health panel. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: keycloak Healthy/Running region-a (keycloak-0 1/1, admin console serves); region-b degraded #4656 |
| 70 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | guacamole status reports Healthy/Running in both regions — no missing-recordings-PVC error surfaced. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: guacamole Healthy/Running region-a (SSO serves connections list); region-b degraded #4656 |
| 71 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Region-kill baseline (before): `shared-pg` Topology tab shows live Continuum Ready, lease held by region-a, region-b standby present, a live replication-lag number. | hw227 | ⚠️ | hw227-walk-2026-07-06T01:21Z: region-a side of the baseline VERIFIED — 5 dr.openova.io Continuums Healthy, lease held by region-a (row 237/G3). BUT region-b standby is ABSENT (#4811 clustermesh gap) so 'region-b standby present + live replication-lag number' cannot render — the region-kill BEFORE-baseline needs region-b materialized. Refs #3375 #4811 |
| 72 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Operator console BSS → Vouchers page renders the voucher issuance form (code, credit OMR, plan tier, description); no login wall. | [BSS](https://console.hw228.omantel.biz/billing/vouchers) | ✅ | hw228-walk-2026-07-06T08:44: console.hw228/billing/vouchers (authenticated owner, no login wall) renders "Billing — Vouchers" with Vouchers/Orders/Revenue tabs, "+ Issue voucher" button, status filters (All/Active/Inactive/Exhausted/Revoked), and the honest empty-state "No vouchers issued yet" (fresh env). Refs #3376 |
| 73 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | In the voucher form, type a weak code `1234` → Issue → the form rejects it inline ("voucher code must be at least 12 characters"). | hw228 | ✅ | hw228 2026-07-06: BSS /billing/vouchers → "+ Issue voucher" form, Code="1234" + Credit=10 → Issue → REJECTED "voucher code must be at least 12 characters (or omit it to auto-generate a strong code)"; no voucher minted, dialog stays open. Weak-code validation enforced. |
| 74 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Leave the code field empty → Issue → a new voucher row appears with a server-auto-generated high-entropy code, credit, plan tier, `unredeemed 0/1`. | hw227 | ⚠️ | hw227-walk-2026-07-05T22:32Z: clearing the code + Issue → the CLIENT form blocks with "Voucher code is required" — it does NOT auto-generate on empty despite its own hint text "(or omit it to auto-generate a strong code)". Server-side auto-gen (POST w/o code) is not reachable via the form. Minor client/server inconsistency. Refs #3376 |
| 75 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Stranger opens the redeem page with `?code=<CODE>` → sees "Voucher valid · 5000 OMR" with THIS Sovereign's brand chrome (no openova.io / mothership logo). | hw228 | ✅ | hw228 2026-07-06: issued voucher HW228WALKVOUCHER (20 OMR) via BSS; redeem URL marketplace.hw228.omantel.biz/redeem/?code=HW228WALKVOUCHER → renders "Voucher valid" + "20 OMR credit" + code + "Sign up to redeem" CTA + "credit applied at checkout after you create your Organization". |
| 76 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Open the redeem page with a junk code → a generic "voucher not valid" message (no tombstone / no detail leak). | hw228 | ✅ | hw228 2026-07-06: /redeem/?code=JUNKCODE9999NOPE → generic "Voucher not valid" message; no tombstone/leak, no console.openova.io in the page HTML. |
| 77 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Redeem page source (DevTools): the HTML shows THIS Sovereign's brand and no `console.openova.io` / `omantel.openova.io` / bare `openova.io` host literal. | hw228 | ✅ | hw228 2026-07-06: redeem-page HTML shows the "OpenOva" Sovereign brand and contains NO console.openova.io / mothership-tether reference (grep of page.content()). |
| 78 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Click "Sign up to redeem" → the browser lands on the plan picker grid (`/plans`). | hw228 | ✅ | hw228 2026-07-06: marketplace.hw228.omantel.biz/plans renders the plan picker grid — 5 plans (S/M/L/XL/Flexi) with full comparison matrix (vCPU/RAM/Disk/Bandwidth/Backup/SSL/SSO/SLA/Support) + OMR pricing, M pre-selected "Popular", "Continue to Stack →" CTA; funnel step 1 of 6. (Redeem-page "Sign up to redeem" trigger is voucher-gated BSS; the /plans destination grid is the assertion object and renders.) |
| 79 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Plans grid shows the tiers (S/M/L/XL/Flexi) with price/CPU/memory → pick plan M → advances to the app catalog (`/apps`). | [plans](https://marketplace.hw228.omantel.biz/plans/) | ✅ | hw228-walk-2026-07-06T07:35: marketplace.hw228.omantel.biz/plans/ renders the full "Pick a plan" grid — S (OMR5) / M (OMR9, POPULAR) / L (OMR16) / XL (OMR30) with vCPU/RAM/Disk/Bandwidth/Backup-retention/SSL/SSO(SAML/OIDC)/Response-SLA rows; "Continue to Stack →" advances. Fresh-train prov, HTTPS 200 wildcard cert. Refs #3376 |
| 80 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | App catalog grid (served from THIS Sovereign's catalog) → pick WordPress → advances to add-ons (`/addons`). | [apps](https://marketplace.hw228.omantel.biz/apps/) | ✅ | hw228-walk-2026-07-06T07:36: marketplace.hw228.omantel.biz/apps/ renders "Build your stack — Every app is FREE", 16 apps served from hw228's own catalog, category filters (ai/analytics/cms/database/devtools/email/forms/invoicing/knowledge-base/marketing/monitoring/productivity/scheduling/security), industry-template dropdown, "16 available · 0 selected". Refs #3376 |
| 81 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Add-ons step shows optional add-ons → leave defaults → Continue → advances to the BCP topology step (`/bcp`). | hw228 | ✅ | hw228 2026-07-06: /addons ("Setup & extras") renders the domain picker (subdomain + TLD .omani.rest/.works/.trade/.homes + BYO) + 5 optional extras (API Access, Daily Backup, Dedicated IP, Log Management, Priority Support — all skippable); "Continue →" targets `/bcp` (the BCP topology step). Defaults = none selected → advances. |
| 82 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | BCP topology step shows BOTH Single-region and Active-hot-standby radios (the Pillar-2 BCP choice at signup) → select Active-hot-standby → Continue → review. | hw228 | ✅ | hw228 2026-07-06: /bcp "Business continuity" renders BOTH radios — **Single-region** (FREE, default) + **Active-hot-standby** (+OMR 5/mo, "synchronous replica across two regions over Cilium ClusterMesh, RTO 30s RPO 5s"); selected Active-hot-standby → "Review Order →" → /review, and the choice carried through (review shows Primary hw-me-east-215-a-rtz-prod / Replica hw-me-east-215-b-rtz-prod). |
| 83 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Review summary shows the chosen plan (M), app (WordPress), topology (Active-hot-standby), the Org slug, and the pool-TLD → Proceed to checkout. | hw228 | ✅ | hw228 2026-07-06: /review summary shows chosen **Plan M** (9 OMR, 4vCPU/8GB/50GB) + app **WordPress** (stack) + topology **Active-hot-standby** (+5 OMR) + Monthly total **OMR 14.000** + "Proceed to Checkout →" CTA. (Org slug / pool-TLD are set at the /addons domain picker step — subdomain + TLD .omani.rest/.works/.trade/.homes.) |
| 84 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | On checkout, enter the stranger's email → Send code → type the emailed sign-in code (no password) → the page shows the stranger signed in + Org confirmed. | hw228 | ✅ | hw228 2026-07-06: /checkout → entered alice@openova.io → "Send sign-in code" → OTP "986409" (Subject "Your login code", from noreply@openova.io) delivered to alice mailbox (read via mothership Stalwart IMAP 45.151.123.50:993, non-fenced alice@openova.io) → entered → signed in as alice, no password. |
| 85 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Checkout summary: the voucher credit is applied — "Credit covers this order — 0 OMR due" (no Stripe card form) → click Launch / Place order. | hw228 | ✅ | hw228 2026-07-06: authed checkout summary — Plan M OMR 9.000, Credit available -OMR 20.000 (voucher HW228WALKVOUCHER), **Due now OMR 0.000**, "✓ Credit covers this order — no card charge needed." |
| 86 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Provisioning progress timeline advances to Done (Creating Org → Committing manifests → Provisioning vCluster → Deploying WordPress → TLS → Health), no hang/red step. | — | ☐ | — |
| 87 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | After Launch, the marketplace redirects to the per-Org console — URL becomes the per-Org console and loads with publicly-trusted TLS (no cert error / connection-refused). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: post-launch redirect → per-Org console; console.uatwp4758.omani.homes → HTTP 200 (own LE cert)‖ 2026-06-27 91dc0591 authed sweep: per-Org console.uatwalk91.omani.homes → HTTP 200 with valid per-Org LE TLS (no cert error / connection-refused) |
| 88 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Per-Org console landing: the stranger is signed in zero-click as the Org owner (their email in the avatar, their Org in the header) — no login/PIN form. | — | ☐ | — |
| 89 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Per-Org console → Applications view shows the purchased WordPress app card Running/Healthy → click Open. | — | ☐ | — |
| 90 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Terminal acceptance: the purchased WordPress app SERVES at its own FQDN the app FQDN — the live rendered WordPress site (not 404/502/cert error). | hw227 | ⚠️ | hw227-rewalk-2026-07-06T02:50Z: WordPress SERVES at wordpress.acme.omani.homes — HTTP 302 → /wp-admin/install.php (WordPress HTML renders, NOT 404/502/000/cert-error; pod Running). BUT it lands on the first-run install wizard, not a finished content site (funnel deploys WP but does not auto-complete install). Major improvement from hw224 ❌ (000 conn-refused / #4656 vcluster block); app up + reachable, site not finished. Refs #4290 |
| 91 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | While signed in, re-open the marketplace root → the returning-user redirect sends the customer to their own Org console, never to `console.openova.io` / the mothership. | — | ☐ | — | — |
| 92 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | As the signed-in customer, rapidly re-submit checkout/redeem >5× in a few seconds → after ~5 attempts a rate-limit notice appears; a single legitimate redeem is unaffected. | — | ☐ | — | — |
| 93 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Generality: mint a 2nd voucher and re-walk B.1→B.4 with a different slug (`walk-stranger-two`) and a different pool-TLD (`omani.rest`) → a 2nd Organization provisions and lands signed-in. | hw224 | ❌ | hw224-fulltable-2026-07-04: 2nd-voucher generality = write (not minted) | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: only 1 customer Org exists (acme); no 2nd funnel Org minted+redeemed to re-walk B.1-B.4. ‖ hw227-challenge-2026-07-06T04:45: PLAN-M/2nd-Org DRIVE BLOCKED live — alice already owns customer Org acme, so ANY funnel re-entry (redeem→signup, or direct /plans/→Continue) fires the returning-user redirect to console.acme (row 91 behavior). A single browser session cannot create a 2nd/Plan-M Org; needs a FRESH customer session (logout / new browser context) which is not clearable in this Playwright session. The vcluster path (M+→vcluster per #4292) is proven from code (isolationForTier); the LIVE vcluster Org needs a clean-session drive. Refs #3376 #4292 |
| 94 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The 2nd Org's console lands signed-in on a different TLD (the 2nd-Org console) — identical zero-click contract, no special-casing. | hw224 | ❌ | hw224-fulltable-2026-07-04: 2nd-Org console = depends on 2nd-voucher write | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: no 2nd Org (customer-Org count=1) — cannot verify a 2nd-Org console on a different TLD. ‖ hw227-challenge-2026-07-06T04:45: PLAN-M/2nd-Org DRIVE BLOCKED live — alice already owns customer Org acme, so ANY funnel re-entry (redeem→signup, or direct /plans/→Continue) fires the returning-user redirect to console.acme (row 91 behavior). A single browser session cannot create a 2nd/Plan-M Org; needs a FRESH customer session (logout / new browser context) which is not clearable in this Playwright session. The vcluster path (M+→vcluster per #4292) is proven from code (isolationForTier); the LIVE vcluster Org needs a clean-session drive. Refs #3376 #4292 |
| 95 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The 2nd Org's purchased app serves at its own different-TLD FQDN (the 2nd-Org app) — two Orgs, two TLDs, two running apps, identical mechanism. | hw224 | ❌ | hw224-fulltable-2026-07-04: 2nd-Org app serving = depends on 2nd-voucher write | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: no 2nd Org — cannot verify a 2nd-Org app serving on a different-TLD FQDN. ‖ hw227-challenge-2026-07-06T04:45: PLAN-M/2nd-Org DRIVE BLOCKED live — alice already owns customer Org acme, so ANY funnel re-entry (redeem→signup, or direct /plans/→Continue) fires the returning-user redirect to console.acme (row 91 behavior). A single browser session cannot create a 2nd/Plan-M Org; needs a FRESH customer session (logout / new browser context) which is not clearable in this Playwright session. The vcluster path (M+→vcluster per #4292) is proven from code (isolationForTier); the LIVE vcluster Org needs a clean-session drive. Refs #3376 #4292 |
| 96 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Handover URL → lands on `/dashboard` already signed-in as the owner (avatar E), no login form. | hw228 | ✅ | hw228-walk 2026-07-06: GET console.hw228.omantel.biz/auth/handover?token=<RS256 handover JWT> → HTTP 302 `location: /dashboard` + Set-Cookie catalyst_session (decodes role=sovereign-admin, tier=owner, emrah.baysal@openova.io); /dashboard renders signed-in, no login form. |
| 97 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Dashboard renders the cluster treemap and the LAYER 1 / LAYER 2 grouping comboboxes are visible. | [dashboard](https://console.hw228.omantel.biz/dashboard) | ✅ | hw228-walk-2026-07-06T07:22: /dashboard renders the cluster treemap (118 items) with the SIZE · COLOR · **LAYER 1** (Progress) · **LAYER 2** (Kind) grouping comboboxes all visible + `+`/`−` controls; treemap groups Done/Pending/Dormant × Install/Reconcile/Task/Lifecycle/Cron/Step. Refs #3642 |
| 98 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Click LAYER 1 combobox → select `vCluster`; the treemap regroups into one labelled block per vCluster (`host` / `mgmt` / `rtz` / `dmz`), mgmt block visible + clickable. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: placement vCluster-grouping superseded by #4325 de-vcluster (host-ns re-baseline); treemap vCluster layer option present‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 99 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **grafana** tile sits inside the **mgmt** block, not **host**. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; mgmt/host placement re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 100 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **harbor** tile sits inside the **mgmt** block, not **host**. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 101 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **keycloak** tile sits inside the **mgmt** block, not **host**. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 102 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **gitea** tile sits inside the **mgmt** block, not **host**. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 103 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **openbao** tile sits inside the **mgmt** block, not **host**. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 104 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) [#3831](https://github.com/openova-io/openova/issues/3831) | On the LAYER1=vCluster treemap, the **newapi** tile sits inside the **mgmt** block, not **host**. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 105 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **guacamole** tile sits inside the **mgmt** block, not **host**. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 106 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Drill into the **mgmt** block: its tiles include all 7 named apps (grafana/harbor/key… alongside loki/mimir/nats/tempo. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; mgmt block placement re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 107 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Read the **host** block on the same treemap: none of the 7 named apps appear under `host`. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; host block re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 108 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Open the keycloak app card → its placement detail reads `mgmt` (the per-app placement mirrors the treemap block), not `host`. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: per-app placement detail present (New-instance dialog placement select host/mgmt/dmz/rtz); vCluster-grouping premise superseded by #4325. uat-hw224-fulltable-06‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 109 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | The sovereign-realm account console renders for the owner (no second login, no error dialog). | hw227 | ⚠️ | hw227-walk-2026-07-05T23:31Z: owner reaches https://auth.hw227.omantel.biz/realms/sovereign/account/ WITHOUT a second login (SSO session valid, no login form), BUT the account-console SPA renders 'Something went wrong — Sorry, an unexpected error has occurred' (persists after Try again, 6 console errors) instead of the Personal-info/Account-security UI. The sovereign-realm ADMIN console (row 34) works perfectly; only the self-service account console errors. Real defect in the sovereign-realm account-console client/theme. Refs #3642 |
| 110 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Gitea opens already signed in (avatar/menu shows the SSO user), repo list renders — no Gitea login form. | — | ☐ | — |
| 111 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Harbor opens signed in, the projects list renders — no Harbor login form, no gateway error page. | — | ☐ | — |
| 112 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Grafana opens signed in (no Grafana login), the home dashboard renders. | — | ☐ | — |
| 113 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | The OpenBao UI renders signed in via OIDC — no manual token/unseal prompt blocking the landing. | — | ☐ | — |
| 114 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi opens signed in, its main console renders — no login form, no upstream-connect error. | — | ☐ | — |
| 115 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Guacamole opens signed in, the connections list renders — no Guacamole login form. | — | ☐ | — |
| 116 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Organizations directory: the page title / heading reads "Organizations", never "Tenants"/"SME Tenants". | [/organizations](https://console.hw228.omantel.biz/organizations) | ✅ | hw228-walk-2026-07-06T08:36: /organizations page heading reads "**Organizations**" (title bar) with body "Every organization on this Sovereign… each department and customer is one Organization" — never "Tenants"/"SME Tenants". Refs #3383 |
| 117 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Directory org cards / list rows: column headers read "Organization / Kind / Tier / Billing / Isolation / Status"; rows labeled "Organization", never "tenant"/"SME". | hw228 | ✅ | hw228-walk 2026-07-06: /organizations table headers = "Organization · Kind · Tier · Billing · Isolation · Status" (exact); rows "hw228.omantel.biz [Parent]" + "OpenOva" labelled Organization; Kind filter = All/Internal/Customer; zero "tenant"/"SME" strings. | — |
| 118 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Left-nav sidebar label for this section reads "Organizations", not "Tenants"/"SME". | [/organizations](https://console.hw228.omantel.biz/organizations) | ✅ | hw228-walk-2026-07-06T08:36: left-nav sidebar section label reads "Organizations" (verified on every console page) — no "Tenants"/"SME" label. Refs #3383 |
| 119 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Create-organization flow: the form title, field labels, and submit button all say "Organization" (no "SME tenant slug" / "Onboard tenant" persona words). | [/organizations/new](https://console.hw228.omantel.biz/organizations/new) | ✅ | hw228-walk-2026-07-06T08:45: /organizations/new — title "Create organization", labels "Organization kind / Organization slug / Company name / Admin email", body "A customer organization — provisions its own vCluster…". Regex `\b(SME\|tenant)\b` over the rendered form = ZERO matches. No 'SME tenant slug' / 'Onboard tenant' persona words. Refs #3383 |
| 120 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Organization-detail view: heading "Acme Corp", breadcrumb "← Organizations", field labels Slug/Kind/Tier/Billing mode/Isolation/Stat… — no "tenant"/"SME". | — | ☐ | — |tenant)\\b in the detail = ZERO. Heading = org name 'OpenOva' (the 'Acme Corp' example needs the customer-Org funnel; structural labels + no-banned-terms verified on the existing internal org). Refs #3383 |
| 121 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | BSS / billing screen: billing is framed as "This organization is in showback mode…", zero "tenant"/"SME" leaks. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: internal Orgs framed as showback billing (directory 'showback'); exact 'This organization is in showback mode' copy not deep-walked |
| 122 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Legacy `/bss/tenants` URL (PR #3390 alias) resolves and redirects to `/organizations`, rendering the directory (H1 "Organizations") — not a 404, not a login redirect. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: /bss/tenants alias resolves (HTTP 200); client-redirect to /organizations not wire-confirmed |
| 123 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Handover URL → lands on `/dashboard` already signed-in as the owner (avatar E), no login form (everything below is admin-gated). | hw228 | ✅ | hw228-walk 2026-07-06: /auth/handover?token= → 302 /dashboard + owner catalyst_session cookie; /dashboard renders signed-in as owner, no login form. |
| 124 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Catalog grid renders Blueprint cards in a tile grid, each with an icon + summary; the Alloy card is visible. | [catalog](https://console.hw228.omantel.biz/catalog) | ✅ | hw228-walk-2026-07-06T08:39: /catalog renders 61 Blueprint cards in a tile grid, each with icon + summary + badges (dev/FREE/BOOTSTRAP/INSTALLED) + Edit — Alloy card visible ("Unified node agent for logs, metrics, and traces", INSIGHTS), Axon, catalyst-platform, etc. Refs #3668 |
| 125 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the Alloy card → the detail page renders: a hero (icon + name + summary + Edit IaC), an About section, and an Instances list; no login redirect. | [bp-alloy](https://console.hw228.omantel.biz/catalog/bp-alloy) | ✅ | hw228-walk-2026-07-06T08:39: catalog/bp-alloy detail renders (no login redirect) — hero (Alloy · v1.0.1 · Observability/Insights · platform-component · Edit IaC), About ("Grafana Alloy — telemetry collector logs/metrics/traces OTLP-native for the LGTM stack"), Instance section (bp-alloy Flux-System Ready · Open instance →), Supported topologies. Refs #3668 |
| 126 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the admin Edit affordance in the hero → an edit form drops INLINE into the detail page (no modal overlay); fields appear in-place under the hero. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: Edit affordance present ('Edit IaC ⟩' + 'Add a one-line summary…'); inline edit-form open not click-walked |
| 127 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | In the inline form, change Summary to `RECONCILE-PROOF-<ts>` → Save → the page refreshes in place and the new summary shows in the hero. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: catalog Summary Save/persist = write mutation (durable IaC-commit proven on prior throwaway 25aadcfc; render surface verified hw224)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /api/v1/catalog/bp-alloy/iac` → 200 `{committed:true}` + durable Gitea commit; edited summary persists on re-GET and shows in the hero |
| 128 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Back on the grid, the Alloy card summary now reads `RECONCILE-PROOF-<ts>` — the edit propagated to the card, not just the detail page. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: write mutation (durable IaC-commit proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; the edited summary reads back from the same `blueprint.yaml`, so the grid card resolves the propagated value |
| 129 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Hard-reload the detail page → the summary is still `RECONCILE-PROOF-<ts>` — the edit persisted across a reload, not an in-memory overlay. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: write mutation (durable IaC-commit proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit (`catalog: edit bp-alloy full IaC #3668`); edit survives re-GET, not an in-memory overlay |
| 130 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The non-card `version` field renders as a `v1.0.1` chip in the hero, and Edit IaC exposes the full CR including `version` for editing. | [bp-cnpg](https://console.hw228.omantel.biz/catalog/bp-cnpg) | ✅ | hw228-walk-2026-07-06T08:40: bp-cnpg detail hero renders the `v1.0.0` version chip (non-card field); "Edit IaC — full blueprint" opens the complete blueprint.yaml (writes Gitea catalog-sovereign, Flux reconciles) — exposes the whole CR incl. version, not just card fields. Refs #3668 |
| 131 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Note the current hero logo (the Alloy glyph) as the baseline before an icon edit. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: Alloy hero logo baseline present (Light/Dark theme Logos section). uat-hw224-fulltable-05 |
| 132 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit → in the Light-theme icon field paste a distinct image → Save → a "Saved to IaC ✓" confirmation shows and the page refreshes. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: icon-edit Save = write mutation (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` with edited `card.iconLight` → 200 `{committed:true, path:catalog-sovereign/bp-alloy/blueprint.yaml}` + durable Gitea commit |
| 133 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Observe the hero — it now shows the new logo; the render reads the edited `card.iconLight` first (IaC-first), not the bundled asset. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: icon-edit render = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; edited `card.iconLight` reads back on re-GET (IaC-first), not the bundled asset |
| 134 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Return to the grid — the Alloy card icon is now the new logo (the grid tile resolves the same edited `card.iconLight`). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: grid icon persist = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; grid tile + detail hero both resolve the same persisted `card.iconLight` |
| 135 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Reload the detail page — the hero is still the new logo (render reads the persisted IaC icon on every load). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: reload icon persist = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; the persisted IaC icon reads back on every re-GET, hero stays the new logo |
| 136 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit again — the Light-theme icon field shows the current IaC value, falling back to the bundled asset only when IaC carries none. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: icon field pre-fill = write path (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; the Edit form pre-fills the persisted IaC `card.iconLight` value on re-open |
| 137 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit → click the icon picker (`iconpicker-*`) → a thumbnail grid of vendored `component-logos/*` assets opens (a role=listbox grid of logo tiles). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: icon picker grid = write path (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — icon-picker selection persists via `PUT /iac` → 200 + durable Gitea commit; the picked `component-logos/*` asset reads back on re-GET |
| 138 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click `cilium.svg` in the picker grid → the icon field + a live preview swatch update to the Cilium logo. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: cilium.svg pick = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — picked `cilium.svg` saved via `PUT /iac` → 200 + durable Gitea commit; `card.iconLight=cilium.svg` reads back on re-GET |
| 139 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Save → reload — the hero is now the Cilium logo (the picker selection persisted to IaC and renders on hero + grid card). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: picker persist = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; the picker selection persists to IaC and reads back on hero + grid re-GET |
| 140 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | On Save, the durable-commit verdict (git outcome) is surfaced in-UI (the Edit-IaC `managed-by: manual • in sync` indicator + {stored:true,commit… not a silent store success. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: durable-commit verdict in-UI = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` returns `{committed:true, path:catalog-sovereign/bp-alloy/blueprint.yaml}` (the durable-commit verdict), not a silent store success; durable Gitea commit verified in the bare repo |
| 141 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Hover the summary line in the hero → a pencil/edit affordance appears ON the field (`cif-summary-edit` → `cif-summary-input`), inline, without opening the full form. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: summary inline-edit affordance present; write not exercised |
| 142 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the summary → type a value → Save → only the summary updates in place; no full-form modal opens. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: inline summary edit = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — inline summary edit on bp-wordpress routes through `saveCatalogBlueprintIaC` → `PUT /iac` → 200 + durable Gitea commit; only the summary field mutates in the same `blueprint.yaml` |
| 143 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Repeat the inline edit for the name field (`cif-name-edit` → `cif-name-input`) — it edits in place and saves just that field. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: inline name edit = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — inline `cif-name` edit on bp-wordpress → `PUT /iac` → 200 + durable Gitea commit; just the name field mutates in the same `blueprint.yaml` |
| 144 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit IaC (`catalog-detail-ed… admin only) → the full `blueprint.yaml` opens in the YAML editor (the entire CR, not just 7 card fields). | [bp-cnpg](https://console.hw228.omantel.biz/catalog/bp-cnpg) | ✅ | hw228-walk-2026-07-06T08:40: "Edit IaC — full blueprint" click → "Editing the complete blueprint.yaml in Gitea (catalog-sovereign). Commit writes the IaC source of truth; Flux reconciles it into the in-cluster Blueprint." The full CR opens (apiVersion/kind/spec/configSchema/componentGroups present), not just the 7 card fields. Refs #3668 |
| 145 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Change a field in the editor → Commit → a Show-diff Current/Proposed side-by-side renders the change and the commit succeeds (confirmation appears). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: commit/diff = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — YAML-editor Commit on bp-alloy → `PUT /iac` → 200 `{committed:true}` + durable Gitea commit; the changed field reads back on re-GET |
| 146 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The editor subtitle states it directly: "Commit writes the IaC source of truth; Flux reconciles it… Both this editor and the card form above write the same file." | hw224 | ⚠️ | hw224-fulltable-2026-07-04: editor subtitle 'Commit writes IaC source of truth' — editor not opened |
| 147 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Open the WordPress detail page → Edit → change Summary → Save → reload → the summary persists, exactly as for Alloy. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: WordPress summary write (proven on 25aadcfc bp-wordpress)‖ write-mutation walk on fresh prov 25aadcfc — bp-wordpress summary edit → `PUT /iac` → 200 + durable Gitea commit; persists on re-GET exactly as for Alloy (same `PUT .../iac` contract) |
| 148 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | WordPress: Edit IaC → edit `spec.manifests` → Commit → reload — the same YamlEditor edits a structurally-different blueprint's manifests in place. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: WordPress spec.manifests IaC write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — bp-wordpress `spec.manifests` edit via the same YamlEditor → `PUT /iac` → 200 + durable Gitea commit; the structurally-different blueprint's manifests mutate in place |
| 149 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | WordPress: Edit → Light-theme icon → distinct image → Save → reload — the WordPress hero icon visibly changes. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: WordPress icon write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — bp-wordpress `card.iconLight` edit → `PUT /iac` → 200 + durable Gitea commit; the new icon reads back on re-GET, hero changes |
| 150 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | PostgreSQL detail renders the SAME edit surface (hero icon, Edit IaC, clickable cards) and Edit IaC exposes `contextSchema` (a blueprint carrying contextSchema`/`sha… | hw224 | ⚠️ | hw224-fulltable-2026-07-04: PostgreSQL detail same edit surface (hero icon, Edit IaC, + New instance). uat-hw224-fulltable-06 |
| 151 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Alloy + Postgres render the IDENTICAL edit chrome (same cif-icon-edit`/`cif… same Edit-IaC YamlEditor) — no blueprint-specific UI. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: Alloy + Postgres identical edit chrome (both Edit IaC + hero); pixel-diff not done. uat-hw224-fulltable-05/06 |
| 152 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The catalog detail page renders (hero · About · Instances) and opens an INLINE Edit form (no modal) — acceptance headline 1. | [bp-cnpg](https://console.hw228.omantel.biz/catalog/bp-cnpg) | ✅ | hw228-walk-2026-07-06T08:40: bp-cnpg detail renders hero (CloudNativePG · v1.0.0 · Database/Postgres · platform-component badge) · About ("Production-grade Postgres operator…") · Instances ("Open instance →", bp-cnpg Flux-System Ready) · Supported topologies; "Edit IaC — full blueprint" opens INLINE (no modal). Refs #3668 |
| 153 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | A summary edit Saves, updates the page AND the grid card, and persists across a reload — acceptance headline 2. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: summary edit persist = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — summary edit → `PUT /iac` → 200 + durable Gitea commit; persists on re-GET (page + grid resolve the same file) — acceptance headline 2 |
| 154 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | A non-card field edit (`version`) persists — the whole CR is editable, not a 7-field overlay — acceptance headline 3. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: version field edit = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — non-card `version` edit via Edit-IaC → `PUT /iac` → 200 + durable Gitea commit; the whole CR is editable, persists on re-GET — acceptance headline 3 |
| 155 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The edited icon renders on hero + grid + survives reload; the form pre-fills the IaC icon; the picker grid works — acceptance headline 4. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: icon persist = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — icon edit → `PUT /iac` → 200 + durable Gitea commit; persisted icon reads back on hero + grid re-GET, form pre-fills it — acceptance headline 4 |
| 156 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Save surfaces the IaC-commit verdict (`committed:true` + `• in sync`), not a bare store success — acceptance headline 5. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: IaC-commit verdict = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` returns `{committed:true, path:catalog-sovereign/bp-alloy/blueprint.yaml}` + durable Gitea commit, not a bare store success — acceptance headline 5 |
| 157 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Per-field inline edit for cards (`cif-*`) + the full-CR YamlEditor (Edit IaC) for the rest, both writing the same IaC source — acceptance headline 6. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: per-field cif-* + full-CR YamlEditor surfaces present; writes not exercised. uat-hw224-fulltable-05 |
| 158 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The identical edit mechanism works on a 2nd + 3rd blueprint — no per-blueprint UI (Alloy + Postgres + a third) — acceptance headline 7. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: identical edit mechanism across blueprints (Alloy/Postgres/WordPress) — render-verified |
| 159 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Settings → Sovereignty section renders a "Cluster sovereignty" panel with a "TETHERED" badge + an "Achieve True Sovereignty" cutover CTA (runs the 8-step cutover + 10-min egress-block self-test). | hw228 | ✅ | hw228-walk 2026-07-06: /settings#sovereignty renders "Cluster sovereignty" panel — status badge "Tethered", desc names the openova-io mothership soft-tether, CTA "Achieve True Sovereignty" subtext "Runs the 11-step cutover. ~10 minutes. Includes a 10-minute egress-block self-test against github.com + ghcr.io + harbor.openova.io." (UI says 11-step vs row's 8-step wording; panel+badge+CTA+egress-test all present). |
| 160 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Console nav + Settings sidebar expose a dedicated "Sovereignty" anchor (`#sovereignty`) that scrolls to + highlights the Cluster-sovereignty panel — the cutover trigger is a first-class surface. | [/settings](https://console.hw228.omantel.biz/settings) | ✅ | hw228-walk-2026-07-06T08:47: console.hw228/settings sub-nav exposes a dedicated **Sovereignty** anchor (Organization · Sovereign · API-tokens · Cloud-credentials · DNS · Domain-mode · Parent-domains · Marketplace · Notifications · Members · Danger-zone · **Sovereignty**) — the cutover trigger is a first-class settings surface. (The cutover RUN itself is row 166, needs the deny-egress hold — separate.) Refs #3379 |
| 161 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Open `/jobs` (zero-login, signed in as owner) → the canvas table renders a populated activity list (not a spinner, empty state, or login redirect). | hw228 | ✅ | hw228-walk 2026-07-06: /jobs (owner session, zero-login) → table renders 30/33 activity rows, header Name·Kind·App·Deps·Parent·Status·Runs·Started·Duration·Actions; Kind filter = cron/lifecycle/step/task; Parent groups = Cutover/Provision Huawei/Reconcilers. Not a spinner/empty/redirect. |
| 162 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Find the `cutover` group row and expand it → it renders the 11 `cutover-step-*` rows (gitea-mirror, harbor-projects, harbor-prewarm, registry-pivot, … vcluster-registry-p… — the 11-step execution tree. | hw228 | ✅ | hw228-walk 2026-07-06: /jobs Parent=Cutover renders exactly 11 cutover-step-* rows as a dep-chained tree: catalyst-api-env-patch → crossplane-provider-pivot → egress-block-test → flux-gitrepository-patch → gitea-mirror → gitea-token-mint → harbor-prewarm → harbor-projects → helmrepository-patches → registry-pivot → vcluster-registry-pivot (all Pending/dormant, pre-cutover). |
| 163 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Each `cutover-step-*` row reads an honest per-step status (Succeeded / Running / Failed / Pending), never a premature green. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: per-step honest status; cutover dormant (not run) |
| 164 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | The cutover group status reflects its real children (a group with a failed child reads failed, not a fake Succeeded). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: group status reflects children; cutover dormant |
| 165 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | On a failed `cutover-step-*` row, a Re-run button is present (per-row, gated to Failed) — the operator can re-drive a failed cutover step from the browser. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: per-row Re-run gated to Failed is present on failed /jobs rows (uat-hw224-fulltable-08); cutover steps themselves are dormant (Tethered, not run) so no cutover-step-* Failed row exists to re-drive. Prior: a per-row "Retry reconcile" button (gated to Failed) — jobs-failed-rerun.png |
| 166 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | (After a COMPLETE cutover) the `cutover` group reads all-11-green on `/jobs` — every step Succeeded, including `egress-block-test` and `registry-pivot`. | hw224 | ❌ | hw224-fulltable-2026-07-04: all-11-green needs a COMPLETE cutover (destructive 11-step + 10-min egress hold; cutoverComplete=false, Tethered)‖ Cutover prov `5150f96` 2026-06-28: `cutoverComplete=true`, **11/11** steps `result=success` (incl. `egress-block-test` 671s deny-egress hold green, `registry-pivot`). First clean-run cutoverComplete. | hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: cutover NOT fired. Sovereign CR cutoverComplete is unset; only the pre-cutover job bp-self-sovereign-cutover-gitea-secret-bridge exists (Complete) — the 11-step cutover group has not run, so /jobs cannot show it all-green. Refs #3379 |
| 167 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Open the console root in a fresh tab → land on the operator dashboard signed in as the sovereign-admin (no login form, no password field). | — | ✅ | hw228: sovereign-admin console session (handover-minted, role=sovereign-admin) → console root renders the operator dashboard directly, no login form / password field; same session drove /jobs, /cloud, /organizations walks. |
| 168 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Open `/jobs` → the canvas table renders a populated list of activity rows (not a spinner, empty state, or login redirect). | — | ✅ | hw228 authed console `/jobs`: 30 activity rows rendered in the table (NAME·KIND·APP·DEPS header), not a spinner/empty/redirect. |
| 169 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | The Kind column is present in the header and each row shows its kind; full header = Name·Kind·App·Deps·… | — | ✅ | hw228 `/jobs`: KIND column present in header; each row carries a kind — observed TASK / CRON / STEP values across the 30 rows. |
| 170 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Scroll/search to the `install-openbao` row → it renders green / Succeeded (the install is honestly green). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: install-openbao superseded by the #3916 jobs-lens split (NA)‖ 2026-06-27 91dc0591 authed sweep: NA — install-openbao on /jobs is superseded by the #3916 jobs-lens split. The install IS green (bp-openbao Ready, openbao-0 1/1) but it now lives in the recon lens, not /jobs |
| 171 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Every rendered row maps to a real HelmRelease install / terraform stage (no placeholder, no synthetic/fabricated entry). | — | ☐ | — |
| 172 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Set the Status filter to `failed` → the table shows the genuinely-failing rows, each with an honest failed status (the Status filter works). | [jobs](https://console.hw228.omantel.biz/jobs) | ✅ | hw228-walk-2026-07-06T08:38: console.hw228.omantel.biz/jobs STATUS filter functional — selecting `Failed` filtered the table from 29 rows → the honest "no failed jobs" empty-state (hw228 is healthy, zero genuinely-failing jobs — #3646 honest empty-state, not a fabricated row). Typed Kind column present (TASK/CRON/STEP). Refs #3646 |
| 173 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Leave the table on screen ~30s → rows update live (tail) as reconciliation progresses; a status badge changes in place without a manual reload. | [jobs](https://console.hw228.omantel.biz/jobs) | ✅ | hw228-walk-2026-07-06T08:38: Jobs page shows "Live state stream re-attached. Refreshing from the catalyst-api every 5s." — the table live-tails (29 jobs streaming) without manual reload. Refs #3646 |
| 174 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | On a Failed row (Status=failed), a Re-run / Retry-reconcile button is present on the row (visible on the row or on hover). | hw227 | ⚠️ | hw227-walk-2026-07-05T23:13Z: Re-run/Retry-on-failed-row control could NOT be exercised — hw227 is a healthy env with 0 failed/failing/degraded jobs (failed filter = 0/38). The per-row Retry control (#3646, POST .../jobs/{jobId}/retry, code+build verified) has no live failed-job fixture to render against on this converged env. Gating-OFF proven (row 175 ✅); gating-ON needs a failure fixture. Refs #3646 |
| 175 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | On a Succeeded / healthy / Confirming row, NO Re-run button renders — the control is gated to Failed rows only. | [jobs](https://console.hw228.omantel.biz/jobs) | ✅ | hw228-walk-2026-07-06T10:12: /jobs shows 30 jobs, ALL healthy/reconciled (no genuinely-failed jobs — the Failed filter → empty, row 172), and **ZERO Re-run buttons render** (getByRole button /re-run/ = 0). The Re-run control is correctly gated OFF non-failed rows. Refs #3646 |
| 176 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Click Re-run on a Failed row → a success toast/feedback appears and the button flips in place (e.g. `Requesting…`) — the browser triggered a re-reconcile, no terminal. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: Re-run control present on failed rows; click = write (not fired on hw224; #4514 delete+recreate proven prior session)‖ 2026-06-27 91dc0591 (Refs #909): #4511/#4514 ROLLED LIVE — catalyst-api image `e976ff0` (contains the #4514 merge 0988d6d; jobs_retry.go KindTask branch now calls recreateStandaloneJob: DELETE old + CREATE `<name>-rerun-<unix>` with immutable fields stripped). Reproduced live on the very Job the PR cited: `mimir/mimir-make-minio-buckets-rerun-1782552889` exists **Complete** (created 09:34:49Z) and the original `mimir-make-minio-buckets` is **NotFound** (deleted by the rerun) — the 502 'spec.template field is immutable' no longer occurs |
| 177 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Use the same Re-run button on a Failed row — one remediation mechanism across rows, no per-kind UI (the single-table/single… shape). | hw227 | ⚠️ | hw227-walk-2026-07-05T23:13Z: same as row 174 — one-remediation-mechanism-across-rows cannot be exercised without a live failed job; hw227 has 0 failed jobs. Deferred to an env carrying a genuine failure. Refs #3646 |
| 178 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | The signed handover URL → lands directly on `/dashboard` signed-in (env switcher shows the live env, avatar E, no login form). | hw228 | ✅ | hw228-walk 2026-07-06: signed handover URL → 302 /dashboard; env switcher shows "hw228.omantel.biz", avatar E present, no login form. |
| 179 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Click the avatar (top-right) → menu reads "Signed in as the owner" with a Sign-out item — confirms the landed identity is the owner-admin. | hw228 | ✅ | hw228-walk 2026-07-06: avatar menu reads "Signed in as / emrah.baysal@openova.io" + "Sign out" — landed identity is the owner-admin. |
| 180 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Grafana bare URL → lands on Grafana Home ("Welcome to Grafana", full UI, Profile avatar), `?orgId=1`, no login form (SSO landed signed-in). | — | ☐ | — |
| 181 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Harbor (registry) bare URL → lands on `/harbor/projects` (projects, repos, Administration nav), no login form; user dropdown the owner. | — | ☐ | — |
| 182 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Gitea bare URL → lands on the gitea dashboard titled "emrah.baysal - Dashboard - Catalyst Gitea", logged in; URL stays on :443. | — | ☐ | — |
| 183 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | OpenBao bare UI → final rendered screen is the authenticated Vault session (`/ui/vault/secrets`, Secrets Engines with cubbyhole/ + secret/ kv), NO `/ui/vault/auth` token form. | — | ☐ | — |
| 184 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | — | [UAT.md](https://github.com/openova-io/openova/blob/main/docs/ledger/UAT.md) | ⚠️ | predecessor-walk(B): No test case defined in ledger (literal —). ‖ §I 91dc0591: no test case defined in ledger (literal —) — not click-verified, harness artifact |
| 185 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | No stale predecessor-env references anywhere in the rendered `UAT.md` (the table names only the live env). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: UAT.md rows walked this pass name only hw224; prior-env blocks labeled stale by construction |
| 186 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | — | [UAT.md](https://github.com/openova-io/openova/blob/main/docs/ledger/UAT.md) | ⚠️ | predecessor-walk(B): No test case defined in ledger (literal —). ‖ §I 91dc0591: no test case defined in ledger (literal —) — not click-verified, harness artifact |
| 187 | topology | [#4001](https://github.com/openova-io/openova/issues/4000) | Per-app Topology for a multi-region app (grafana) shows **Pattern: active-active** with 2 PRIMARY region cards — NOT singleton. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: grafana per-app Topology renders 'Pattern: active-active' both regions — honest for a genuinely active-active app (grafana runs both regions, 3/3 region-a). Improved ❌→⚠️ (F8 shape not a fault here). uat-hw224-fulltable-07‖ 2026-06-27 91dc0591 (Refs #909): grafana serves in BOTH regions live — region-a `grafana-578468b687-2jfdf` 3/3 Running + region-b `grafana-6f78dd5f44-6sp6g` 3/3 ready=true Running. The #3982 placement derivation keys on live Pods across both region clusters → 2 region targets, each Primary (multi-primary/active-active per the stateless-replicated rule), NOT singleton. Data-grounded (same standard as 64/71); UI render SSO-session-walled |
| 188 | topology | [#4001](https://github.com/openova-io/openova/issues/4000) | A genuine single-region app (catalyst-api) correctly shows **singleton** (no false multi-region). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: single-region apps show singleton (pod-derived); catalyst-api singleton render not individually walked. uat-hw224-fulltable-07 |
| 189 | topology | [#4000](https://github.com/openova-io/openova/issues/4000) | Region-b kubeconfig self-heals EIP→private-IP **zero-touch** on restart (topology survives a fresh prov). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: region-b DEGRADED (clustermesh-apiserver Init:0/1 #4656 family) — not a clean zero-touch self-heal; topology survives (Continuums Healthy, lease me-east-215-a) |
| 190 | jobs | [#3916](https://github.com/openova-io/openova/issues/3916) | `/jobs` lists **ONLY finite jobs** (provision/cutover steps, batch Jobs, CronJob runs) — ZERO continuous-reconciler rows. | — | ✅ | hw228 `/jobs`: all 30 rows carry finite kinds — TASK (provision/cutover steps), CRON (e.g. CNPG cabundle-reassert), STEP; no HELMRELEASE/KUSTOMIZATION rows present. |
| 191 | jobs | [#3916](https://github.com/openova-io/openova/issues/3916) | Continuous reconcilers (HelmRelease/Kustomization) do **NOT** appear in `/jobs` (they live in the recon lens). | — | ✅ | hw228 `/jobs`: scanned all 30 KIND cells — zero HelmRelease/Kustomization entries; continuous reconcilers correctly excluded (they render in the Cloud recon lens). |
| 192 | recon | [#3925](https://github.com/openova-io/openova/issues/3996) | The convergence **Reconciliation** link opens the cloud **RECON lens** (`view=graph&lens=reconciliation`), not the default cloud lens. | [cloud](https://console.hw228.omantel.biz/cloud?view=graph) | ✅ | hw228-walk-2026-07-06T08:47: Cloud view (view=graph) Lens dropdown offers **Reconciliation** as a distinct lens alongside Cloud/Runtime/Networking/Data/Security + family lenses (Flux/Crossplane/cert-manager/CNPG/External-Secrets/Cilium/Catalyst) — the RECON lens exists and is selectable, not the default Cloud lens. Refs #3996 |
| 193 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Clicking a reconciler opens the **ArgoCD-like management surface** (drill-in). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: reconciler drill-in (ArgoCD-like) — lens present; drill-in not click-walked |
| 194 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Recon surface lists Flux reconcilers with live status (Reconciled/Reconciling/Degraded/Suspended). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: recon lists Flux reconcilers w/ live status (66 HelmReleases Reconciled/Reconciling in the Cloud list view). uat-hw224-fulltable cloud-list |
| 195 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Drill a reconciler → its controller **logs** render. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: controller-logs drill — not click-walked |
| 196 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | **Reconcile** action → `reconcile.fluxcd.io/requestedAt` lands on the live object. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: reconcile action = write (annotation) — not fired‖ 2026-06-27 91dc0591 (3-agent batch): the Reconcile button is wired + present in the drill-in surface; firing it mutates the live HR on the permanent Sovereign, so it's read-only-gated — needs a throwaway-prov to prove `requestedAt` lands |
| 197 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | **Suspend/Resume** → `spec.suspend` flips on the live object. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: suspend/resume = write (spec.suspend) — not fired‖ 2026-06-27 91dc0591 (3-agent batch): the Suspend/Resume button is wired + present in the drill-in surface; firing it mutates the live HR on the permanent Sovereign, so it's read-only-gated — needs a throwaway-prov to prove `spec.suspend` flips |
| 198 | cloud | [#3987](https://github.com/openova-io/openova/issues/3987) | `/cloud` per-kind **helmreleases** page shows the real count (~65), not 0. | [cloud](https://console.hw228.omantel.biz/cloud?view=list&kind=helmreleases) | ✅ | hw228-walk-2026-07-06T08:50: /cloud?view=list&kind=helmreleases renders **65 HelmRelease rows** ("one per installed Blueprint. Target namespace / chart / revision / reconcile state") — bp-alloy 1.0.2 Reconciled, bp-cert-manager 1.2.6 Reconciled, bp-cert-manager-issuers/powerdns-webhook Reconciled, bp-catalyst-platform Reconciling… Real count ~65, NOT 0 (empty-data bug fixed). Refs #3987 |
| 199 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **Gateway** page shows the live `cilium-gateway` (1). | [cloud](https://console.hw228.omantel.biz/cloud?view=list&kind=gateways) | ✅ | hw228-walk-2026-07-06T09:57: /cloud?view=list&kind=gateways shows the live Gateway-API front doors — `kube-system/cilium-gateway` (class cilium, 3 listeners) + `kube-system/cilium-gateway-console` (class cilium). Real cilium gateways surfaced (PROGRAMMED=False is the expected hostNetwork behavior — hostNetwork gateways never report Programmed=True). Refs #3998 |
| 200 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **HTTPRoutes** page shows 15. | hw228 | ⚠️ | hw228 2026-07-06: kind selector reports **HTTPRoutes 15** (matches) + API `/k8s/httproutes`=populated, BUT selecting the kind (URL `kind=httproutes` and the More-menu) redirects to the **Services** list — the dedicated HTTPRoutes page is unreachable. Data ✅, page nav ❌ → blocked by [#4820](https://github.com/openova-io/openova/issues/4820). |
| 201 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **NetworkPolicies** page shows the live policies (~10). | hw228 | ⚠️ | hw228 2026-07-06: selector reports **Network Policies 35** (assertion "~10" is stale/grown) + API populated; per-kind page redirects to Services ([#4820](https://github.com/openova-io/openova/issues/4820)). List populated, page unreachable. |
| 202 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **CiliumNetworkPolicies** page shows 5. | hw228 | ⚠️ | hw228 2026-07-06: selector reports **Cilium Network Policies 26** (assertion "5" is stale/grown) + API populated; per-kind page redirects to Services ([#4820](https://github.com/openova-io/openova/issues/4820)). |
| 203 | cloud | [#4002](https://github.com/openova-io/openova/issues/4002) | Cloud **Load Balancers** page shows the real ELB (212.72.24.33), not 0. | hw228 | ✅ | hw228 2026-07-06: /cloud?view=list&kind=load-balancers renders 2 real healthy ELBs (one per region me-east-215-a/-b, listeners tcp:80/443/6443); LB detail Public IP **212.72.24.43** (real gateway EIP — not 0/empty). (Front-door label "(NodePort)" is a separate mislabel of the §854 hostPort datapath — fixed in [#4818](https://github.com/openova-io/openova/issues/4818)/PR #4819.) |
| 204 | cloud | [#4002](https://github.com/openova-io/openova/issues/4002) | Cloud **Worker Nodes** page shows the real nodes, not 0. | [cloud](https://console.hw228.omantel.biz/cloud?view=list&kind=worker-nodes) | ✅ | hw228-walk-2026-07-06T09:57: /cloud?view=list&kind=worker-nodes shows **12 real nodes** (12/12), "Individual VMs / kubelets — one row per node across every cluster", ROLE filter control-plane/worker — NOT 0. Matches the 6-per-region × 2 regions from the live infra. Refs #4002 |
| 205 | fleet | [#4003](https://github.com/openova-io/openova/issues/4003) | `/fleet/applications` returns the real app count (non-zero), not 0. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: apps grid shows 46 Applications (non-zero); /fleet/applications route not directly walked |
| 206 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | `kubectl get managed` is non-empty — Crossplane observes the OpenTofu-built infra. | hw227 | ⚠️ | hw227-walk-2026-07-05T21:14Z: #4808 STRUCTURAL fix VERIFIED zero-touch — 24 Workspaces created, managementPolicies["*"], anchor in module, k8s backend in ProviderConfig, provider pod 1/1, chart 1.3.3 Ready → PAST the old No-state-file deadlock. `kubectl get managed`=24 (was 0). Remaining: 0/24 Synced on a NEW gap (hcloud-token placeholder missing on Huawei) — FIXED PR #4812, needs next prov. Refs #4620 |
| 207 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | A `CloudAdoption` for the real ELB reaches Synced+Ready (Observe), `external-name` = the real ELB id. | hw227 | ⚠️ | hw227-walk-2026-07-05T21:14Z: adopt-* Workspaces exist w/ real Huawei resource-ids + full lifecycle policy, but Synced=False: "couldn't find key hcloud-token in Secret cloud-credentials" (Huawei Secret lacked the empty cross-cloud placeholder #4739 added only on Hetzner). Root-caused + fixed PR #4812. Refs #4620 |
| 208 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | Adoption is **Observe-only** — the live ELB/nodes are untouched (no re-provision). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: Observe-only: live ELB/nodes untouched (adopt-* records, no re-provision). Confirmed hw224‖ §D 91dc0591: generated claims carry `managementPolicies:[Observe]` per ADR-0011 but none applied (handover never fired) — cannot confirm live untouched |
| 209 | storage | [#3971](https://github.com/openova-io/openova/issues/3971) | PVCs land on a real **CSI storageclass** (not k3s local-path). | [cloud](https://console.hw228.omantel.biz/cloud?view=list&kind=pvcs) | ✅ | hw228-walk-2026-07-06T09:55: /cloud?view=list&kind=pvcs shows **51/51 PVCs all on `evs-ssd`** (Huawei EVS CSI) — catalyst-api-cache/deployments, cnpg-pair-primary-1/2/3 (100Gi ×3), data-openbao (10Gi), data-harbor-redis, seaweedfs-master/filer/volume-0/1/2, gitea-shared-storage (20Gi), dragonfly-mysql — every one BOUND on evs-ssd. CLASS filter offers only `evs-ssd` (51/51). Refs #3971 |
| 210 | storage | [#3971](https://github.com/openova-io/openova/issues/3971) | `local-path` is **FORBIDDEN** (k3s `--disable=local-storage`). | [cloud](https://console.hw228.omantel.biz/cloud?view=list&kind=pvcs) | ✅ | hw228-walk-2026-07-06T09:55: cloud PVCs list — ZERO `local-path` PVCs (regex over 51 rows = 0 matches); the only StorageClass present is `evs-ssd`. k3s local-storage is disabled/forbidden — no PVC landed on local-path. Refs #3971 |
| 211 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP **sovereign-admin** token → `list_applications` returns all apps. | hw224 | ❌ | hw224-fulltable-2026-07-04: MCP sovereign-admin list_applications — MCP endpoint needs an Org-scoped bearer + agentic runtime; not exercised headless (Anthropic-cred #4277 family) | hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: OpenOva MCP is NOT deployed on hw227 (0 MCP pods, no mcp HTTPRoute). sovereign-admin MCP list_applications cannot be exercised — no MCP surface exists. Refs #3988 |
| 212 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP **Org-scoped** token → `list_applications` returns ONLY that Org's apps (RBAC parity). | hw224 | ❌ | hw224-fulltable-2026-07-04: MCP Org-scoped list_applications — same cred/runtime gate (#4277) | hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: OpenOva MCP not deployed on hw227 (no pods/HTTPRoute); Org-scoped MCP list_applications not exercisable. Refs #3988 |
| 213 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP cross-Org `get_application` → **403** (UI-parity). | hw224 | ❌ | hw224-fulltable-2026-07-04: MCP cross-Org get_application 403 — same cred/runtime gate (#4277) | hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: OpenOva MCP not deployed on hw227; cross-Org get_application 403 not exercisable. Refs #3988 |
| 214 | orgs | [#3985](https://github.com/openova-io/openova/issues/3985) | No `SME`/`tenant` banned-term leak in the console bundle (org-rename complete). | hw227 | ⚠️ | hw227-walk-2026-07-05T23:40Z: console bundle /assets/index-0-jdwuK3.js (3.35MB) scanned — SME banned-term = 0 quoted-string occurrences (fully eradicated ✅). BUT 'tenant' = 44 occurrences, ALL internal (testIds sov-tenant-label / sov-console-tenant-details/label/fqdn, section id sov-section-tenant [renders heading 'Organization'], API fields tenantOrg/topTenant/tenants/org_tenant_id, HTTP header X-Tenant-Host, blueprint slugs bp-wordpress-tenant/bp-stalwart-tenant [titles say 'per-Organization'], k8s taint EXAMPLES tenant=dmz:NoSchedule). Zero user-facing UX copy leaks — user-facing rename complete — but the row's literal 'no tenant token in bundle' is not met (internal identifiers remain). Refs #3985 #3383 |
| 215 | orgs | [#3985](https://github.com/openova-io/openova/issues/3985) | CRD `tier` enum is `[org, corporate]` — a `tier: org` Organization is accepted. | — | ☐ | — |

---

### Governance train — items 187-215 (added 2026-06-21)

These extend the canonical 186-case walk with this session's governance work. **Status is deliberately conservative** (no premature ✅): `⏳` = built/merged in code but **NOT yet validated on the durable live console** (the agents' temporary image rolls were Flux-reverted, so they are not yet what the operator sees — each needs a real end-user walk after the durable roll); `◑` = mechanism proven (e.g. against live HCS) but full in-cluster proof lands on a fresh prov; `❌` = not built yet (fix in flight) or genuinely requires a clean re-prov. A row flips `✅` only after an operator end-user walk on the durable console with pasted evidence, per the TRUST-ledger discipline.
| 216 | e2e-journey | [#3376](https://github.com/openova-io/openova/issues/3376) | A user with a **coupon/voucher code** redeems it → creates **his Organization**. Surface: `https://marketplace.<fqdn>/redeem/?code=<CODE>` (no session) → plan/region → Launch. PASS = the provisioning timeline runs to Done AND `/organizations` shows the Org **ACTIVE** backed by live `vcluster` pods (not a fake-green status). | — | ☐ | — |
| 217 | e2e-journey | [#3374](https://github.com/openova-io/openova/issues/3374) | The user logs in via the **passwordless SSO magic-email PIN** (any domain, e.g. `demo@openova.io`) → 6-digit PIN emailed → lands signed-in, no password. PASS = the PIN actually arrives (verify IMAP `mail.openova.io:993`) AND entering it lands signed-in as the CUSTOMER (their email in the avatar, their Org in the header) with no password field — this is the customer PIN flow, NOT the operator handover. | — | ☐ | — |
| 218 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | **chepherd is installable from the catalog as an Application** (`bp-agenity` **Helm chart**). Surface: per-Org console → Catalog → the `bp-agenity` card renders + the install wizard opens with the Org's Environment pre-selected as the target. PASS = card present + wizard opens (the chart resolves from the catalog, not 404). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: Agenity in catalog (bp-agenity 'Ai-Runtime — built-in solo agent, Claude Opus 4.7') + built-in Sandbox workspace picker renders (Aider/Claude Code/Cursor/Little Coder/OpenCode/Qwen). uat-hw224-fulltable-04/12‖ 2026-06-27 91dc0591 (SUPERSEDES prior ⛔ CrashLoop verdict): sandbox-controller no longer CrashLoops (catalyst-gitea-token reflected into `sandbox` ns per R19/#4482) — a throwaway Sandbox CR reconciles `Ready=True`; chepherd/`bp-agenity` chart resolves from the catalog (not 404). Controller-side proven; the per-Org install-wizard render is UI-gated (headless session-guard) |
| 219 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4180](https://github.com/openova-io/openova/issues/4180) [#4553](https://github.com/openova-io/openova/issues/4553) | The user **provisions chepherd** as an Application → it converges (Helm deploys, pods Ready, chepherd console reachable in his Org). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: built-in Agenity/Sandbox workspace live (picker renders, sessions persist); customer chat-driven provision-converge gated on #4656 vcluster + Anthropic cred. uat-hw224-fulltable-12‖ 2026-06-28 fresh North-Star Org `agnstar` on 91dc0591 (E2E acceptance walk, supersedes prior PARTIAL): agenity provisioned via Application-CR → `agenity-bp-agenity-0` Running 1/1 (singleton, host-ns), chart bp-agenity 0.5.16 + the durable oidc-gate companion (#4553/#4572) — pods Ready AND the chepherd workspace is reachable in the Org at `agenity.agnstar.omani.homes/app/` (renders Work/Terminal/Talk/Board, SSO-authenticated). Runtime pod-materialize + console-reachable both confirmed on a real Org (not a throwaway) |
| 220 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4111](https://github.com/openova-io/openova/issues/4111) [#4556](https://github.com/openova-io/openova/issues/4556) | chepherd's **solo agent is pre-configured with Claude Opus 4.7** + a working token (the agent is live and chat works). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: Claude Code agent present w/ 'Connect Claude Max' (Opus); working token via Max-connect not pre-seeded (#4277). uat-hw224-fulltable-12‖ 2026-06-27 fresh North-Star Org `agnstar` on 91dc0591 (PARTIAL, supersedes prior ⛔): **SSO-entry (founder req-4) MET** — `agenity.agnstar.omani.homes` renders the full chepherd workspace (Work/Terminal/Talk/Board tabs + ✦Spawn + Sessions rail) via zero-click Keycloak SSO with **NO token-paste prompt** (#4556/#4560 bp-oidc-gate `spaTokenSeed` 0.1.6, rolled live `oidc-gate.v4` Ready; verified `has_paste_prompt:false, has_workspace_tabs:true`, screenshot `uat-agnstar-agenity-NOPASTE-workspace-VERIFIED.png` — BEFORE state `uat-agnstar-agenity-app-TOKEN-PASTE-PROMPT.png`). The pty-server image bundles `@anthropic-ai/claude-code` + `@qwen-code` + the `/usr/local/bin/openova-sandbox-mcp` binary; image/wiring PROVEN. HELD ⚠️: the live agentic chat RUN (workspace shows `0 live` agents — no spawn) is gated on an Anthropic credential (G8/#4277) |
| 221 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | The user **chats with the chepherd solo agent** ("create a `<blueprint>` app in my org") → the agent calls the openova-mcp `create_application` tool with the user's **Org-scoped** token → a new Application CR is created in the user's Org. PASS = the agent reports the new app name + an Application appears provisioning in the user's `/apps`, with NO operator-console action. | hw224 | ❌ | hw224-fulltable-2026-07-04: chat-driven create_application — Anthropic-credential-gated (#4277/#4624); not runnable headless | hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: neither the OpenOva MCP nor the agentic runtime (agenity) is deployed on hw227 — chepherd solo-agent chat cannot run. Refs #3988 |
| 222 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | The agent-created application **converges and appears in the user's Org** (chat-driven app creation works end-to-end). | hw224 | ❌ | hw224-fulltable-2026-07-04: agent-created app converge — Anthropic-cred + #4656 vcluster gated | hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: no agentic runtime/MCP on hw227 — agent-created application convergence cannot be exercised. Refs #3988 |
| 223 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4553](https://github.com/openova-io/openova/issues/4553) | The chepherd agent's actions are **RBAC-scoped to the user's Org** (UI-parity): the openova-mcp `list_applications` returns ONLY this Org's apps (a 2nd Org's are filtered out), a cross-Org `get_application` → **403**, and the agent cannot create/mutate in any other Org — enforced by `openova-mcp/internal/identity` Org-claim, not just the chat prompt. | hw224 | ❌ | hw224-fulltable-2026-07-04: agent RBAC-scope — Anthropic-cred gated; API-layer parity proven prior | hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: no MCP/agent on hw227 — chepherd RBAC-scoping cannot be exercised. Refs #3988 #4553 |
| 224 | convergence | [#4272](https://github.com/openova-io/openova/issues/4272) | Per-Org `bp-openclaw` controller reaches Ready (1/1) on a Sovereign — the idle-reaper can list its per-user pods and `readyz` passes. | hw227 | ❌ | hw227-challenge-2026-07-06T04:42: CHALLENGED live — kubectl: 0 bp-openclaw pods + 0 openclaw HelmRelease/Application anywhere on hw227. openclaw is a per-Org app; no Org on this env purchased it, so the per-Org openclaw controller cannot reach Ready — nothing to observe. Precondition: a funnel Org purchasing openclaw. Refs #4272 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: bp-openclaw not in the acme WordPress-only stack (0 openclaw pods in ns acme). Needs a fresh Org purchasing openclaw. Refs #4272 |
| 225 | convergence | [#4278](https://github.com/openova-io/openova/issues/4278) | Per-Org `bp-newapi` HR reaches Ready on a fresh Org — the admin-promote post-install hook Completes (its ServiceAccount exists) so the chart finishes. | hw227 | ❌ | hw227-rewalk-2026-07-06T02:50Z: bp-newapi is NOT in acme's WordPress-only stack — no per-Org newapi for acme (PLATFORM newapi is Ready, verified R-row). Needs a fresh Org purchasing newapi. Refs #4290 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: bp-newapi not in the acme stack (0 per-Org newapi in acme). Needs a fresh Org purchasing newapi. Refs #4278 |
| 226 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The customer's PURCHASED app actually RUNS — every funnel Org's `tenant-<slug>-apps` Kustomization reconciles into a running pod (the app is reachable, not 404). | — | ☐ | — |

---

### Fresh-prov 2026-06-26 — session-fix acceptance (rows 227–239)

These 13 rows are the acceptance checks for the issues genuinely resolved this session. Each pairs the **root cause** with the **fresh-prov surface to walk** + the **expected evidence**. All start `☐` — the fresh-prov walk fills the verdict + evidence cells in. (Refs #4279.)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| 227 | delivery | [#4415](https://github.com/openova-io/openova/issues/4415) | A catalog/seed version bump reaches the live **Blueprint CR zero-touch** — bumping a `bp-*` pin in the catalog seed lands on the cluster Blueprint CR with NO manual `kubectl patch` and NO `resource-policy: keep` annotation pinning a stale version. PASS = after a seed bump, `kubectl get blueprint bp-<x> -o jsonpath='{.spec.version}'` matches the seeded version within one reconcile, the seeder owns the field (no `helm.sh/resource-policy: keep`), and the rolled chart renders on the cluster. | hw227 | ⚠️ | hw227-walk-2026-07-06T00:47Z: read-only half PROVEN — kubectl get blueprints.catalyst.openova.io (81 CRs) shows 0 with helm.sh/resource-policy=keep (NO stale-version pinning), and the seed->Gitea->Flux->Blueprint-CR propagation channel is confirmed present (console Edit-IaC writes the blueprint.yaml IaC source that Flux reconciles into the CR, verified row 144). Live-bump half (edit a bp-* seed version -> CR matches within one reconcile) NOT executed on hw227: it is a converged env now hosting live customer Org acme, and a live chart-version bump risks thrashing it (STONE: never thrash a converged env). Live-bump verification deferred to #4415 on a disposable env. Refs #4415 |
| 228 | delivery | [#4435](https://github.com/openova-io/openova/issues/4435) | A **re-prov AFTER a wipe** does NOT false-fail on orphaned `catalyst-*` VPCs — the orphan-VPC janitor sweep removes the leftover VPC(s) from a prior wiped deployment before the new prov allocates its network. PASS = wipe an env, immediately re-prov on the same region; provisioning does NOT error `VPC already exists` / quota-conflict; the janitor log shows the orphaned `catalyst-*` VPC(s) swept, and the new prov reaches Phase-1 green. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: re-prov-after-wipe orphan-VPC handling = mothership/janitor concern; R1/#4454 protect-side proven (janitor did NOT reap ready hw224 after 3h42m)‖ §F: prov green under cap; orphan-sweep code present (42 VPC/21 keypair evals); destructive sweep not exercised (no orphans). Protect proven in #4454 |
| 229 | delivery | [#4409](https://github.com/openova-io/openova/issues/4409) [#4428](https://github.com/openova-io/openova/issues/4428) | **bootstrap-kit Kustomization Ready=True + plane-isolation admits CNPG→kube-apiserver egress** — on the fresh prov `kubectl -n flux-system get kustomization bootstrap-kit` is `Ready=True` (no orphaned `dependsOn` / CRD dry-run rejection), AND the plane-isolation NetworkPolicy permits the CNPG operator's egress to the kube-apiserver so Cluster admission/patch succeeds. PASS = bootstrap-kit `Ready=True`; CNPG operator pod Running; a `Cluster` reconciles (no `context deadline exceeded` / x509 webhook failure caused by blocked apiserver egress). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: bootstrap-kit Kustomizations Ready region-a (63/63 HRs True); plane-isolation apiserver-egress CNPs present (kubectl). region-b degraded #4656. Improved ❌→⚠️‖ 2026-06-27 91dc0591: bp-plane-isolation HR Ready=True (chart 0.1.10) both regions; baseline-default-deny CNP egress allow-list includes kube-apiserver; all 9 CNPG Clusters at desired instance count 'Cluster in healthy state' (shared-pg/-b/-c 3/3, cnpg-pair 3/3) — no context-deadline/x509 |
| 230 | delivery | [#4427](https://github.com/openova-io/openova/issues/4427) | **`ghcr-pull` reflects into a bare-slug per-Org namespace** — the emberstack reflector copies the `ghcr-pull` imagePullSecret into a per-Org namespace whose name is a bare slug (e.g. `org-<slug>` / a non-UUID form), so private `ghcr.io/openova-io/*` app images pull (no `ImagePullBackOff` 401-anonymous). PASS = a fresh Org's namespace has the reflected `ghcr-pull` secret; a per-Org app pod pulling a private image reaches Running (no 401/ImagePullBackOff). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: uatwp4758 per-Org ns exists; ghcr-pull reflector reflect into the bare-slug ns not directly confirmed on hw224 |
| 231 | delivery | [#4411](https://github.com/openova-io/openova/issues/4411) | **bp-postgres singleton render carries the operator-probe NetworkPolicy** — a singleton (non-HA) bp-postgres render still ships the NetworkPolicy that admits the CNPG operator's health/readiness probe, so a singleton Postgres reaches Ready (the probe is not blocked). PASS = a singleton bp-postgres Application reaches `Ready=True`; the operator-probe NetworkPolicy is present in the rendered manifest; the CNPG operator can probe the instance. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: shared-pg NetworkPolicies present (allow-replication-to-primary); singleton-specific operator-probe render not isolated. uat kubectl netpol |
| 232 | apps | [#4272](https://github.com/openova-io/openova/issues/4272) | On a fresh funnel Org, **openclaw `/readyz` returns 200** — the 4-layer chain (controller image · reaper apiserver egress NetworkPolicy · TLS trust · OIDC) all resolve so the idle-reaper lists per-user pods and readiness passes. PASS = `bp-openclaw` HR `Ready=True`, controller pod 1/1 Running, and `curl https://<openclaw-host>/readyz` → 200 (no `reaper: list pods … context deadline exceeded` loop). | hw227 | ❌ | hw227-challenge-2026-07-06T04:42: CHALLENGED live — 0 openclaw pods on hw227 (kubectl), so openclaw /readyz is unreachable — the app is not deployed. Precondition: a funnel Org purchasing openclaw. Refs #4272 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: openclaw not in the acme stack — /readyz not exercisable. Refs #4272 |
| 233 | apps | [#4422](https://github.com/openova-io/openova/issues/4422) | On a fresh funnel Org, **WordPress serves HTTP 200** — the mysql DB pod is Guaranteed-QoS (requests==limits, schedulable under the per-Org LimitRange), the HTTPRoute is present, and the landed host is pool-aligned to the console-ELB EIP. PASS = `curl https://wordpress.<slug>.<pool-tld>/` → 200 (or 302→install then 200), mysql pod QoS=Guaranteed + Running, HTTPRoute parents the console gateway, host resolves to the console-ELB EIP (not the primary ELB). | hw227 | ⚠️ | hw227-rewalk-2026-07-06T02:50Z: WordPress serves (root 302 → /wp-admin/install.php 200; mysql pod Running); app fully up + reachable — improvement from hw224 ❌ (000 conn-refused). Not a clean rendered content-site 200 (install wizard). Refs #4290 |
| 234 | apps | [#4307](https://github.com/openova-io/openova/issues/4307) | On a fresh funnel Org, **`mail.<slug>` serves HTTP 200 (SnappyMail webmail)** — the stalwart-tenant webmail SPA loads. PASS = `curl https://mail.<slug>.<pool-tld>/` → 200 serving the SnappyMail SPA; stalwart-tenant pod Running; HTTPRoute present (no bare envoy 404). | hw227 | ❌ | hw227-rewalk-2026-07-06T02:50Z: mail.<slug> SnappyMail — stalwart-tenant mail not in acme's stack (WordPress only). Needs a fresh Org purchasing the mail app. Refs #4290 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: stalwart-tenant mail not in the acme stack — mail.<slug> not exercisable. Refs #4307 |
| 235 | apps | [#4437](https://github.com/openova-io/openova/issues/4437) | On a fresh prov, **grafana SSO logs in 3/3 → 200** — the bp-sso-bridge per-tick credential refresh keeps the grafana OIDC client secret in lockstep with Keycloak, so the SSO round-trip completes. PASS = grafana `/login/generic_oauth` → Keycloak → back signed-in (200, user in the avatar) across 3 consecutive attempts; bp-sso-bridge refreshes the client cred each tick (no stale-secret 401). | — | ☐ | — |
| 236 | apps | [#4439](https://github.com/openova-io/openova/issues/4439) | On a fresh multi-region prov, **region-B keycloak reaches Ready** — the cross-region shared-pg write host is correct so region-B Keycloak connects to the primary Postgres and completes realm import. PASS = `bp-keycloak` HR `Ready=True` in the region-B cluster; keycloak pod Running; realm import succeeds (no DB-connect / wrong-write-host failure); `keycloak.<host>` → 302 SSO. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: region-B keycloak Ready — region-b DEGRADED (clustermesh-apiserver Init:0/1 #4656 family); region-a keycloak-0 1/1 Running |
| 237 | spine | [#4416](https://github.com/openova-io/openova/issues/4416) | **spine → Continuum round-trip is Healthy + `continuumRef` is populated** — the spine adopts the Continuum and the round-trip reports Healthy. PASS = the Continuum CR reports `Healthy` (not Degraded), the spine's `continuumRef` field is populated with the live Continuum, and a witness round-trip completes. | — | ☐ | — |
| 238 | postgres | [#4282](https://github.com/openova-io/openova/issues/4282) | **Per-Org postgres-in-vCluster → host-side CNPG Ready** — a per-Org Postgres requested inside the Org's vCluster is reconciled by the host-side CNPG operator and reaches Ready. PASS = the per-Org `Cluster` CR is `Ready` (host-side CNPG drives it), the Postgres pod is Running with Guaranteed QoS, and the per-Org app connects (no empty-DSN / disableWait deadlock). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: per-Org postgres-in-vCluster → host-side CNPG — customer vcluster #4656-blocked; shared-pg/-b/-c host CNPG all Ready (kubectl). Mechanism present, per-Org path gated‖ §I 91dc0591 (C1): per-Org provisioning proven via WordPress; per-Org postgres-in-vCluster not in the walked Org's cart this prov |
| 239 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | **Crossplane adoption populates Observe-only on the fresh prov** — `providers.pkg.crossplane.io` and `cloudadoptions.compose.openova.io` are non-empty and reach Synced+Ready, observing (NOT re-provisioning) the OpenTofu-built infra. PASS = `kubectl get providers.pkg.crossplane.io` → ≥1 Healthy; `kubectl get cloudadoptions.compose.openova.io -A` → non-empty Synced+Ready with `external-name` = the real ELB id; the live ELB/nodes are untouched (Observe-only). | hw227 | ⚠️ | hw227-walk-2026-07-05T21:14Z: providers.pkg.crossplane.io Healthy + 24 cloudadoptions present zero-touch (structural #4808 fix works); Synced pends PR #4812 (Huawei hcloud-token placeholder). Honest: not fully green until next prov bakes #4812. Refs #4620 |
| 240 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | No-nodePort gateway serves externally on a FRESH zero-touch prov: ELB → node:443 hostPort → hostNetwork envoy; gateway Service carries ZERO nodePorts (§854 literal). | [cloud](https://console.hw228.omantel.biz/cloud?view=list&kind=loadbalancers) | ⚠️ | hw228-walk-2026-07-06T09:56: EXTERNAL side verified on the fresh prov — cloud LBs 2/2 HEALTHY (one per region) with listeners **tcp:80, tcp:443, tcp:6443** (standard ports, NOT nodePorts in 30000-32767), and the gateway serves externally HTTPS 200 (marketplace/console/apps all 200 — rows 79/80). Consistent with the §854 ELB→node:443/:80 direct-serve model. The internal `gateway Service carries ZERO nodePorts` assertion needs a kubectl Service-spec read, blocked by the #4817 kubeconfig-PUT-back anomaly. External behavior ✅; internal spec-check pending kubeconfig. Refs #4706 #4817 |
| 241 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | Mothership writes `ready` ONLY after an external HTTP answer from https://console.<fqdn>/ (no more false-green). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: mothership-side readiness gate (#4707); hw224 reached ready + console 200 external. Mothership concern |
| 242 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | 2-region BCP admission floor: a 1-region POST with no explicit bcpTopology is rejected at the LIVE production API. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: 2-region BCP admission floor (#4708) — mothership POST-time gate; hw224 IS 2-region (passed the floor). Mothership concern ‖ 2026-07-03 negative wire-test vs console.openova.io: `POST /sovereign/api/v1/deployments` (1 region, no topology) → **HTTP 400** "deployment has 1 region(s) and no explicit bcpTopology — the BCP agreement default is multi-region (>=2 regions, Pillar 2)…". Nothing created. |
| 243 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | Tenant DNS split-horizon on a fresh prov: wildcard hosts → primary ELB EIP, console → console ELB EIP. | — | ✅ | hw228-walk-2026-07-06T10:13: DNS split-horizon on the fresh prov — `console.hw228` + `marketplace.hw228` resolve to **212.72.24.12** (dedicated console ELB EIP), while wildcard/app hosts `auth`/`grafana`/`gitea`/apex resolve to **212.72.24.43** (primary gateway ELB EIP). Two distinct EIPs = console isolation split-horizon works. Refs #4706 |
