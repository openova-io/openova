# UAT — Sovereign acceptance walk (consolidated 2026-06-27 — 22 issues closed live + honest gating map) (RESET 2026-07-06 — pending hw228)

> **hw224 VERIFICATION PROV — 2026-07-04 (dep `964e9ca501951f54`, hw224.omani.works, 2-region me-east-215-a/b, LE-prod cert).** Fresh prov on the train carrying the two deepest fixes from the hw223 walk: **#4756** (provisioning-token outage) + **#4758** (vcluster-LimitRange brick). Fixes confirmed DEPLOYED + partially verified live by the orchestrator; the full authenticated funnel walk (customer WordPress serving in-vcluster) is in flight (walker `a2a68a8ce9ec0b96b`).
> - ✅ **#4756 provisioning-outage fix — VERIFIED LIVE**: `org-services/provisioning` init went `1/1 Running` at **5m52s with 0 restarts** (vs the old ~30-min self-heal outage that recovered only at the 1800s init-timeout restart). The init now reads GITHUB_TOKEN from the kubelet-synced FILE each poll (`GITHUB_TOKEN_FILE` env + `provisioning-github-token` volume both present on the running pod), so the late-minted gitea token is picked up in ~60s.
> - ✅ **#4758 vcluster-LimitRange fix — CONFIRMED WORKING** (authenticated funnel walk, customer Org `uatwp4758`, plan m, isolation=vcluster): `plan-limits` LimitRange has **NO `maxLimitRequestRatio`** (only default/defaultRequest), and the vcluster's synced `coredns-…-x-kube-system-x-vcluster` pod — the exact pod the old ratio=1 rejected — is now **admitted + scheduled + Running** in the host ns. The admission-rejection #4758 targeted is resolved. (org-controller `organization-controller:99cab49`.)
> - ❌ **WordPress last-mile still 404** — but by TWO NEW defects distinct from #4758: (1) **PRIMARY [#4656]** the customer vcluster's apiserver is unreachable cross-node (`dial tcp 10.96.80.71:443: i/o timeout`) — Cilium VXLAN-over-WireGuard MTU stacking (measured live: cilium_host 1370 + VXLAN 50 = 1420 > cilium_wg0 1290 → DF-drop); coredns stays 0/1, WordPress never applies into the vcluster. (2) **secondary** `core/services/provisioning/gitops/gitops.go:800-803` hardcodes `sourceRef: flux-system` which doesn't exist on a Sovereign → the generated `tenant-<slug>-apps` Kustomization is FALSE (`GitRepository "flux-system" not found`) — a broken duplicate of the org-controller's `catalyst-tenant-<slug>-apps`.
> - ✅ **Pillars 2/3, funnel E2E, PIN login** re-confirmed (BCP active-hot-standby picker; shared-pg/-b/-c + org-pg healthy; full funnel plans→checkout→launch with voucher `VCH-QFP52J4S2KEX`; `/auth/pin/verify`→200). Env converged (console 200, region-a 63/63 HRs; region-b `secondaryDegraded=true` — honest).
> - ✅ **COMPREHENSIVE authenticated UAT walk — 26 ✅ · 0 ❌ · 3 ⛔ · 4 ⚠️, NO new bugs, NO fabricated-green** (walker `a453392c2a1f7a514`, 12 screenshots `.playwright-mcp/uat-hw224-01..12-*.png`). Confirmed live at HTTP/kubectl/wire layer: **SSO** (per-app launch-url→real OIDC; gitea 307→`realms/sovereign`, grafana 302 w/ `kc_idp_hint=catalyst-pin` silent-SSO); **Topology/DR** (per-app placement from runtime, active-active, Region 2/2, 24/24 workers); **Jobs lens #3646** (Kind col task/cron/step/lifecycle + Re-run on a *Failed* mimir-buckets job — genuinely real, not all-green); **Catalog** (81 blueprints, bp-wordpress detail); **Orgs** (directory lists customer `uatwp4758`, per-Org detail, **showback 525.5 units real-metered ≠ 0**, `console.uatwp4758.omani.homes`→200 w/ own LE cert); **Treemap** (206-node live util; **#4731 Progress/Kind layers VERIFIED** — L1 progress buckets Reconcilers/Cutover/Provision-Huawei, L2 Kind Task/Cron/Step/Lifecycle, status-color legend, job-tree source `/jobs`→200, leaf→JobDetail+logs, no regression to org/utilization default; `.playwright-mcp/4731-hw224-0[1-4]`); **Robustness #3380** (Degraded badge honestly propagated everywhere — no false-green; root = `clustermesh-apiserver` Init:0/1 both regions, #4656 family); **Cutover** (Tethered, 11 steps Pending, cutoverComplete=false); **Users** (3 real UserAccess CRs); **Agenity** (6-agent picker). 3 ⛔ = pre-existing non-product gates (headless-KC silent-SSO landing, Agenity live-run Anthropic cred #4277, uatwp4758 vcluster 0/0 #4656). 4 ⚠️ minor (HR-only DR-switchover panel absent; catalog IaC write-gated on live env; vcluster-showback in graph; ~~"Sandbox"→"Agenity" nav leak~~ **FIXED #4762**).
> - ✅ **FULL-TABLE walk — all 243 canonical rows populated on hw224: `101 ✅ · 11 ❌ · 26 ⛔ · 105 ⚠️ · 0 ☐`** (from 0✅/122☐ at reset; every row a live hw224 verdict, **0 fabricated passes**, 17 screenshots `uat-hw224-fulltable-*`, commits 15c5332d3/9b2d3898f/73a65b568). Silent-SSO brokered from the console session unlocked app-landings session-walled ⛔ on prior envs. **Flipped ❌/⛔→✅ this env**: Harbor SSO wired (was ❌412 db_auth), Grafana 3/3 + silent-SSO (was ❌503 CrashLoop), all app SSO landings live (gitea/openbao/guacamole/pdns/newapi/hubble/KC-admin), customer Org uatwp4758 real (525.5-unit showback). **11 ❌ = all previously-filed knowns, NO new bugs**: #4656 (WordPress in-vcluster, 7 rows), F8 topology placement-from-pod-only, row-23 directory-showback, #4620 crossplane adoption. **105 ⚠️** = region-b Degraded (clustermesh-apiserver Init:0/1, #4656 family) + write-mutation rows (durable-commit proofs stand). **26 ⛔** = honest walls (headless-KC session, Anthropic-cred Agenity chat #4277, destructive cutover, write-gated funnel). Path to higher ✅: fix #4656 (unblocks WordPress + region-b) + a throwaway write-mutation walk.
> **Provenance:** `hw224-verify-2026-07-04` per surface (walker `a2a68a8ce9ec0b96b`, 6 screenshots `.playwright-mcp/4758-0[1-6]-*.png`). Net: the two deepest fixes shipped this session (#4756, #4758) are VERIFIED; the WordPress headline is now gated on #4656 (MTU) + the gitops.go sourceRef bug — both filed, both distinct from the verified fixes. hw223 (below) is prior-env history — wiped 2026-07-04 (STALE by construction; each new env flushes evidence).

> **hw223 AUTHENTICATED PILLAR WALK — 2026-07-04 (dep `9224ebd66e0602b4`, hw223.omani.works, real LE-prod cert, FIRST reliably-converged prov after the walk-blocker fix-trio #4748+#4746+#4752).** The whole prior round was auth-gated by broken operator login; hw223 cleared it. **~13 ✅ · 0 ❌ · 0 ⛔ · 3 ⚠️**, + 1 real bug (#4756) + 2 findings. All verdicts reproduced live at the HTTP/kubectl/CR layer — no fabricated passes (12 screenshots under `.playwright-mcp/uat-hw223-*.png`).
> - ✅ **Operator PIN-login end-to-end** (#4748, the prior blocker): `pin/issue`→200 `dispatched` → mailbox PIN → `pin/verify`→200 + RS256 `catalyst_session` cookie (roles catalyst-admin/owner) → authed console renders. (hw221 was 502; fix validated live.)
> - ✅ **Pillar 1 — customer-Org onboarding walked E2E** (first customer-KIND Org on any prov this cycle): funnel plans→apps→addons→bcp→review→checkout; operator issues voucher `UATWALK223VOUCHER` (BSS UI); redeem fully covers OMR 9 → Org CR `uatwalk223` (kind=customer, plan=m, billing=real, isolation=vcluster) Ready in ~2 min → **`console.uatwalk223.omani.homes` → HTTP 200** on its own Org host.
> - ✅ **Pillar 2/3** — cloud graph Region 2/2 (me-east-215-a+b both healthy), 24/24 workers, LB 2/2; Continuum DR CRs (gitea/harbor/keycloak) phase Healthy + LeaseHeld; 9 CNPG clusters healthy (shared-pg/-b/-c Guaranteed + DR pair). Signup BCP offers active-hot-standby.
> - ✅ **W-2** per-Org `plan-limits` LimitRange forces Guaranteed QoS + `plan-quota` present; **W-3** per-Org agenity org-host routing proven (Org console on the Org host, not Sovereign FQDN) + `OPENOVA_MCP_TENANT_HOST` fix in source; **W-11** Organizations directory shows the new customer Org.
> - ✅ **Pillar 5** cutover dormant (`bp-self-sovereign-cutover@0.1.103`, cutoverComplete=false, 11 steps un-started; Settings→Sovereignty "Tethered").
> - 🐞 **BUG #4756** — `org-services/provisioning` init reads GITHUB_TOKEN via secretKeyRef env (empty at start), can't see the late-minted gitea token → ~30-min self-heal outage (recovers at 1800s init-timeout restart). Findings: funnel checkout showed `.omani.rest` but provisioning used `omani.homes` (redirect TLD mismatch); a duplicate `tenant-uatwalk223-apps` Kustomization is broken (GitRepository not found); funnel-picked WordPress still converging in-vcluster at cutoff. (⚠️ = per-Org keycloak realm tier-gated + WordPress converging + showback 0-units-metered.)
> **Provenance:** `hw223-walk-2026-07-04` per surface; the three walk-blocker fixes (#4748/#4746/#4752) all validated live on this prov. Prior hw221/hw220 blocks below are prior-env history (STALE by construction — each new env flushes evidence).

> **hw220 FULL-TABLE walk — 2026-07-04 (dep `907629bc3577355b`, hw220.omani.works, 2-region converged, complete #4706+#4732 train live).** ALL 243 canonical rows walked by two parallel read-only walkers (rows 1–120 + 121–239, minted-owner + org-scoped sessions, wire evidence per row): **122 ✅ · 21 ❌ · 37 ⛔ · 63 ⚠️ · 0 ☐**. The 21 ❌ consolidate into the #4739 fault wave (headline: hourly external :443 outage loop from the TTL'd envoy-restart Job — live-mitigated + durable fix PR'd; per-Org LimitRange wedge blocking every funnel app; harbor SSO db_auth never flipped; grafana region-a credential CrashLoop; 1.4.993 invalid env render; adoption ProviderConfig group mismatch; false-green health panel; hardcoded Hetzner name in DR panel; visible APPDETAIL debug marker). 37 ⛔ are honest walls (KC browser-SSO unmintable headless, founder-mailbox PIN, write-gated funnel mutations, no customer-kind Org sample). 63 ⚠️ dominated by catalog write-mutation rows (write-gated on the live env; prior 25aadcfc durable-commit passes stand) + cutover-partial context (step-08 wedged, cutoverComplete=false). Verdict provenance: `hw220-walk-2026-07-04(A|B)` stamps in each row's Evidence column.

> **hw218 walk — 2026-07-03 (dep `25b49b8534ab98db`, hw218.omantel.biz, FIRST-EVER zero-touch 2-region convergence on the merged #4706 train).** Four wire-captured verdicts, no hand-patching anywhere in the env:
> 1. ✅ **No-nodePort gateway serves externally** — `gitea.hw218.omantel.biz → HTTP 303`, `auth.hw218.omantel.biz → HTTP 302` (off-cluster, ELB 212.72.24.33 → node:443 hostPort → hostNetwork envoy); gateway Service `type=ClusterIP`, **`nodePorts=<none>`** (kubectl wire-capture). Console-isolation leg 404 (two Gateways can't both bind node:443) → fixed in #4715 (merged, 1.4.987 roll-gated).
> 2. ✅ **Readiness gate (#4707) enforced** — `status=ready` written only at 00:37:03Z after the external console probe answered (<500); the gate code is proven present in the RUNNING mothership binary by capture 3. Hardening note: 404 satisfies the current <500 bar — tighten to <400 as a follow-up.
> 3. ✅ **2-region admission floor (#4708) live in production** — negative test against the LIVE mothership: `POST /deployments` (1 region, no topology) → **HTTP 400** `"deployment has 1 region(s) and no explicit bcpTopology — the BCP agreement default is multi-region (>=2 regions, Pillar 2)…"`. Nothing created.
> 4. ✅ **Tenant DNS split-horizon correct** — `gitea./auth.hw218 → 212.72.24.33` (primary ELB), `console.hw218 → 212.72.24.12` (console ELB).
> Pre-walk hygiene: hw217 wiped via canonical endpoint with **API-audited zero orphans** (0 ELBs, bastion-only ECS, 1 EIP, 1/5 VPCs). Session train: #4707 #4708 #4709 #4710 #4711 #4714 #4715 all MERGED.


> **Pillar 1 (Marketplace + voucher onboarding) fully live-walked 2026-06-27 — funnel + plan catalog + voucher issuance/redeem E2E.**

> **Pillar 4 (Agenity workspace + auto-mounted bp-openova-mcp) materially live-walked 2026-06-27 — controller + per-Org provision + MCP auto-mount + RBAC Org-scope verified; agentic chat-run gated on Anthropic credential.** *(Terminology per DOD.md §Pillar 4 + GLOSSARY: the user-facing product is **Agenity** / **bp-openova-mcp**; the legacy "Sandbox" concept + menu are dead/removed — founder 2026-06-30. Internal `sandbox-controller`/`Sandbox` CR plumbing Agenity is built on stays internal.)*

> **Authed operator-console walk 2026-06-27 (throwaway-mailbox PIN session) — 10 interactive-UI rows verified; 2 product faults exposed (rows 33, 176).**

> **Authed live-walk sweep 2026-06-27 — ~33 interactive rows verified (SSO/topology-dialog/per-app-health/catalog-IaC/cutover-UI/jobs/orgs); env healthy post bp-catalyst-platform 1.4.915 roll.** (Row 83 funnel /review = fix-merged-pending #4526; row 170 install-openbao = NA, superseded by the #3916 jobs-lens split.)

> **Write-mutation walk on fresh prov 25aadcfc 2026-06-27 — catalog IaC-commit (durable Gitea) + instance-provision rows verified (safe on the disposable throwaway).** Catalog Save/commit rows 127/128/129/132–140/142/143/145/147/148/149/153–156 flipped ✅ via `PUT /api/v1/catalog/<bp>/iac` → 200 `{committed:true}` + durable bare-repo Gitea commit, on two structurally-different blueprints (bp-alloy + bp-wordpress). Instance-provision rows 50/59/60/61 flipped ✅ via `POST /catalyst/v1/apps/instances` → 201 + CR Ready + matching topology badge (singleton + active-hot-standby).

> **3-agent live-walk batch 2026-06-27 — topology/recon/sso ✅ flips + owner-redirect fix (#4547) + 2 ❌→⛔ reclassifications (session-walled) + topology DR-panel product-gap recorded.** On dep `91dc05917e44d1c1`: 6 rows flipped ✅ (53/54 topology, 193/195 recon, 29/38 sso); row 3 owner-redirect = ⚠️ fix-merged-pending (#4546/#4547); rows 109 + 183 reclassified ❌→⛔ (KC browser-SSO-cookie / OpenBao final-render, both session-walled, not product faults); topology rows 51/52/56/57 held ⚠️ as a REAL product gap (DR/switchover UI panel not built for HR components) + 58/63 held ⚠️ as stale expectations (cilium/grafana render honestly active-active); recon 196/197 held ⚠️ read-only-gated. New canonical tally 193 ✅ · 0 ❌ · 9 ⛔ · 33 ⚠️ · 4 ☐ = 239.

## Session resolution — 2026-06-27 (Refs #909)

**Env:** live omantel.biz Sovereign, dep `91dc05917e44d1c1`, 2-region converged (region-a 62/62 HRs · region-b 56/56 HRs, me-east-215-a + me-east-215-b). All verdicts below are against THIS env unless a row carries its own labeled prior-env evidence.

**Headline:** 22 issues CLOSED this session with independently-audited live evidence (reproduced the live signal, no false-closes) · 4 more code-done + MERGED (roll-gated, NOT yet live-proven — recorded as merged-pending, not pass) · 13 remain open behind a recorded gate.

**The single decisive gate:** kom4dc EIP quota is **10** (free 3); a fresh post-fix 2-region prov needs **6** EIPs — a HARD WALL with zero orphans to reclaim. Raising the quota **10 → ≥16** would unblock the 7 roll-gated tickets in one fresh prov. The remaining 6 are Anthropic-credential-walled (2), a real remaining build (1), or destructive-walk-gated (2, need a throwaway env [itself EIP-walled] or founder clearance).

### Live-walk consolidation — 2026-06-27 (4-section read-only walk of `91dc05917e44d1c1`, region-a)

Four parallel read-only walkers (SSO/auth · Orgs/Contexts · Topology/DR/robust · Jobs/Cloud/Market) re-dialed the live 2-region omantel.biz Sovereign and reported **119 verdicts** (28 + 22 + 39 + 30): **75 PASS · 5 FAIL · 9 NA · 30 BLOCKED**. Consolidated into the numbered table below, the run flips **16 rows to ✅** (rows 5/6/25/32/41/42/43/44/64/71/77/111/189/205/229/236) and resolves 17 more rows from `☐`/`❌`/`⚠️` into their honest gate (⛔) or product-fault (❌) state, leaving the canonical table at **81 ✅ · 9 ❌ · 26 ⛔ · 42 ⚠️ · 81 ☐ = 239** at that snapshot. (A subsequent 2026-06-27 Pillar-1 funnel+voucher walk flipped 4 more rows to ✅ — 72/73/74 `☐`→✅ + 75 `⛔`→✅ — carrying the canonical table to **85 ✅ · 9 ❌ · 25 ⛔ · 42 ⚠️ · 78 ☐ = 239**.) Genuine product faults (FAIL): **newapi** serves 200 but generic-OAuth provider rows are never seeded (`Unknown OAuth provider`, rows 37/114); **Hubble** authed shell renders but the flow XHR (`/api/control-stream`) 302-bounces to KC so data never loads (row 39, carried ⚠️); and the **uatwalk91** Org reports `isolation=vcluster` while the live backing is host-namespace (rows 9/10/11/12/216 — the #3642 de-vcluster re-baseline, not a fresh regression). The 30 BLOCKED are honest gates: a kid-less handover *session* JWT is rejected by this catalyst-api build (whoami 401) and the SPA login guard clears injected cookies, so interactive-session UI renders (treemap/showback DOM, OpenBao authed UI, Topology-tab content) can't be asserted headless — their underlying data is proven at the API/kubectl/psql layer. Row 236 (region-B keycloak) **flips ⛔→✅** (the #4470 sandbox-ns deadlock did not recur; region-b 57 HRs True). No false-pass: every ✅ reproduced the live signal directly.

### Closed this session — 22, audited live (✅ VERIFIED-PASS)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| R1 | janitor | [#4454](https://github.com/openova-io/openova/issues/4454) | catalyst-api orphan-sweep no longer reaps a `ready` Sovereign — denylist inversion (`case "wiped": eligible; default: protect`) holds on the live env. | hw228 | ✅ | ☐ ‖ hw228 LIVE 2026-07-06 (mothership catalyst-api): deployment ac35eda8 (ready) actively serviced (clustermesh secret-sync running 14:02) + [JANITOR] pass complete ×3 destructive:false reaped:0 orphanKubeconfigsDeleted:0 orphanTofuWorkdirsDeleted:0; in-use EVS 'skipped (in-use or active deployment)', only available/orphaned resources would-reap (log-only) → ready Sovereign NOT reaped; denylist inversion (#4454) confirmed live | — | hw227-reconfirm-2026-07-06: hw227 (dep 504e330d86aaefb4) console 200 + Sovereign ready after 5h — janitor did NOT reap the ready env; protect-side inversion holds. 
| R2 | network | [#4467](https://github.com/openova-io/openova/issues/4467) | Cilium VXLAN+WireGuard MTU set to **1370** in BOTH regions — cross-node pod TCP no longer DF-drops; full-size data packets traverse. | — | cilium hw224 | ❌ ‖ hw228 LIVE 2026-07-06 (kubeconfig): datapath now uses routing-mode=NATIVE (the #4656 native-routing fix — supersedes the #4467 MTU=1370/VXLAN mechanism this row asserts); cilium_host/cilium_net mtu=1420, cilium_wg0 mtu=1340, enable-wireguard=true. Row stays ❌ — MTU=1370/VXLAN premise superseded + 'both regions' single-region-gated (#4811, 6 region-a nodes / 0 region-b) ‖ hw228 LIVE 2026-07-06 (kubeconfig) — #4656 datapath PROVEN region-a: cilium-health 6/6 nodes reachable, Routing=Native/Host=BPF, KubeProxyReplacement Direct-Routing, masq BPF 10.42.0.0/15; acme vcluster apiserver Ready=True (the exact R2 DF-drop i/o-timeout canary — HEALTHY, not timing out), 64/67 HRs converged → cross-node pod TCP no DF-drops via native-routing. R2 stays ❌ ONLY because it asserts the superseded VXLAN+MTU=1370 mechanism AND 'both regions' (region-b absent #4811). GOAL achieved single-region | DISPROVEN — live cilium_host 1370 + VXLAN 50 = 1420 > cilium_wg0 1290 → still DF-drops cross-node (vcluster apiserver i/o-timeout). #4467 MTU=1370 insufficient; superseded by #4656 (native-routing fix designed). 
| R3 | sso | [#4448](https://github.com/openova-io/openova/issues/4448) | bp-plane-isolation openbao default-deny ADMITS sso-bridge ingress → sso-bridge reaches openbao:8200. | hw228 | ✅ | ☐ ‖ hw228 LIVE 2026-07-06 (kubeconfig): sso-bridge-reconciler 1/1 Running 6h41m 0-restarts + openbao-allow-apiserver-egress/gateway-ingress CNPs both Valid=True → reaches openbao:8200; grafana SSO 302→KC(kc_idp_hint=catalyst-pin) | sso-bridge-reconciler 1/1 Running 3h35m + grafana SSO works (302→KC kc_idp_hint) → it reaches openbao:8200; openbao CNPs (gateway-ingress/apiserver-egress) VALID=True admit the datapath. | hw227-reconfirm-2026-07-06: hw227 sso-bridge-reconciler 1/1 Running 4h57m 0-restarts + grafana SSO 302→KC → reaches openbao:8200. 
| R4 | sso | [#4458](https://github.com/openova-io/openova/issues/4458) | sso-bridge egress CNP permits openbao + keycloak — the reconciler's own egress is not blocked. | hw228 | ✅ | ☐ ‖ hw228 LIVE 2026-07-06 (kubeconfig): sso-bridge egress healthy 6h41m 0-restarts, tick-complete every 24s actively re-minting OIDC secrets (openbao+keycloak reachable — would CrashLoop if egress blocked) | sso-bridge egress reaches openbao+keycloak — reconciler healthy 3h35m, 0 restarts, actively re-minting OIDC secrets (would CrashLoop/error if egress blocked). | hw227-reconfirm-2026-07-06: hw227 sso-bridge egress healthy 4h57m 0-restarts, actively re-minting OIDC secrets (would crash if egress blocked). 
| R5 | sso | [#4437](https://github.com/openova-io/openova/issues/4437) | sso-bridge re-mints the grafana OIDC client secret each tick (KC_ADDR cache refreshed) — no stale-secret 401. | hw228 | ✅ | ☐ ‖ hw228 LIVE 2026-07-06: grafana /login/generic_oauth → HTTP 302 → auth.hw228.omantel.biz/realms/sovereign kc_idp_hint=catalyst-pin client_id=grafana (no stale-secret 401); sso-bridge log 'reconcile bp-grafana' each 24s tick re-mints client secret | grafana OIDC round-trips (walk: grafana 302→KC w/ kc_idp_hint=catalyst-pin, no stale-secret 401) → sso-bridge re-mints the client secret each tick. | hw227-reconfirm-2026-07-06: hw227 grafana /api/user=200×3 = emrah.baysal@openova.io, no stale-secret 401 → sso-bridge re-mints each tick (row 235). 
| R6 | postgres | [#4442](https://github.com/openova-io/openova/issues/4442) | bp-postgres NetworkPolicy admits the declared consumers (CNPG operator probe + app dials). | hw228 | ✅ | ☐ ‖ hw228 LIVE 2026-07-06 (kubeconfig): 3 shared-pg NetworkPolicies (shared-pg/-b/-c-allow-replication-to-primary) present + CNPG shared-pg/-b/-c all 3/3 healthy → declared consumers (CNPG probe + app dials) admitted | bp-postgres NetworkPolicies present (shared-pg/-b/-c allow-replication-to-primary) + CNPG clusters all 3/3 healthy → declared consumers (CNPG probe + app dials) admitted. | hw227-reconfirm-2026-07-06: hw227 4 shared-pg replication NetworkPolicies present + CNPG shared-pg/-b/-c all 3/3 healthy → declared consumers admitted. 
| R7 | plane-isolation | [#4444](https://github.com/openova-io/openova/issues/4444) | bp-plane-isolation admits gitea ingress for its declared consumers (org-services→gitea de-vcluster gap). | hw228 | ✅ | ☐ ‖ hw228 LIVE 2026-07-06 (kubeconfig): gitea-allow-apiserver-egress/gateway-ingress CNPs both Valid=True + 5 Flux GitRepositories from gitea Ready=True → plane-isolation admits gitea ingress for declared consumers | gitea CNPs (allow-gateway-ingress/apiserver-egress) VALID=True + Flux GitRepositories pull from gitea Ready → plane-isolation admits gitea ingress for its declared consumers. | hw227-reconfirm-2026-07-06: hw227 gitea reachable — Flux GitRepositories Ready + 20 apiserver-egress/gateway-ingress CNPs admit gitea ingress. 
| R8 | gitea | [#4447](https://github.com/openova-io/openova/issues/4447) | gitea-flux-auth secrets are seeded — Flux pulls from gitea with valid credentials. | hw228 | ✅ | ☐ ‖ hw228 LIVE 2026-07-06 (kubeconfig): all 5 Flux GitRepositories Ready=True artifact stored (catalyst-tenant-acme/-hw228/openova/catalog-sovereign/org-tenants) → gitea-flux-auth seeded valid | — | hw227-reconfirm-2026-07-06: hw227 Flux GitRepositories all Ready=True (catalyst-tenant-acme, catalyst-tenant-hw227-omantel-biz, openova, openova-catalog-sovereign, openova-org-tenants) → gitea-flux-auth valid. 
| R9 | sso | [#4446](https://github.com/openova-io/openova/issues/4446) | oidc-gate client_secret is seeded for powerdns-admin — full OIDC round-trip completes. | hw228 | ✅ | ☐ ‖ hw228 LIVE 2026-07-06 (kubeconfig): oidc-gate-powerdns-admin + oidc-gate-openova-flow both 1/1 Running 6h41m → client_secret seeded, OIDC round-trip serves | oidc-gate-powerdns-admin pod 1/1 Running 3h37m (+ oidc-gate-openova-flow) → client_secret seeded, OIDC round-trip serves. | hw227-reconfirm-2026-07-06: hw227 oidc-gate-powerdns-admin + oidc-gate-openova-flow both 1/1 Running 4h57m → client_secret seeded, OIDC serves. 
| R10 | orgs | [#4471](https://github.com/openova-io/openova/issues/4471) | org-controller RBAC carries update/patch — per-Org provisioning completes (was create-only, never reached Ready). | hw228 | ✅ | ⚠️ ‖ hw228 LIVE 2026-07-06 (kubeconfig): acme = Plan-M customer Org — Organization CR Ready=True + vcluster isolation CONFIRMED (HR acme/vcluster Ready 'Helm install succeeded chart vcluster@0.33.4' + vcluster-0 1/1 Running); org-controller ClusterRole carries update/patch (verbs get/list/patch/update/watch on organizations, #4471) → drove per-Org provisioning to Ready WITH vcluster isolation — the isolation=vcluster verdict hw227's namespace-tier acme couldn't prove (per-Org realm/IaC sub-conditions still reconciling) | org-controller provisioned customer Org `uatwp4758` to Active (kind=customer, isolation=vcluster) + console.uatwp4758.omani.homes→200 — provisioning reached Ready (hw224-verify). | hw227-reconfirm-2026-07-06: hw227 org-controller provisioned customer Org acme to ACTIVE (Ready=True) — BUT isolation=namespace not vcluster (funnel divergence #4813); reached-active proven, vcluster-isolation NOT. | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 
| R11 | gitea | [#4354](https://github.com/openova-io/openova/issues/4354) | gitea git-data is PRESERVED across the host-ns re-home (#4325 de-vcluster) — no empty-PVC data loss. | hw228 | ✅ | ☐ ‖ hw228 LIVE 2026-07-06 (kubeconfig): gitea 1/1 Running 6h41m + GitRepositories carry real content (rev main@sha1:f4081c0…, artifact stored) → git-data preserved post host-ns re-home (#4325), not empty-PVC | gitea pod 1/1 Running 3h35m + per-Org GitRepositories carry real content (revision sha1, stored artifact) → git-data present post host-ns re-home, not empty-PVC. | hw227-reconfirm-2026-07-06: hw227 Flux GitRepositories carry real content (Ready, artifact stored) → gitea git-data preserved post host-ns re-home. 
| R12 | postgres | [#4460](https://github.com/openova-io/openova/issues/4460) | shared-pg `-mesh-rw` resolves in BOTH regions + CNPG streaming replication is live. | — | — | ✅ | hw228 2026-07-07 ✅ (kubectl + cilium-dbg live): shared-pg-mesh-rw is service.cilium.io/global=true (ClusterMesh-EXPORTED → resolves cross-region), ClusterMesh 1/1 remote clusters ready, backend=10.42.4.145 (shared-pg-1 primary) present; cross-cluster NATIVE datapath verified this session (region-b→region-a, 5/6 nodes full HTTP, #4656/#4807). CNPG streaming replication LIVE on shared-pg-1: shared-pg-2 streaming/sync + shared-pg-3 streaming/potential (pg_stat_replication). Both assertion parts met — the -mesh-rw global service + healthy mesh + proven datapath give both-region resolution; replication directly verified. ‖ shared-pg / shared-pg-b / shared-pg-c all "Cluster in healthy state" 3/3 instances (CNPG streaming replication live). | hw227-reconfirm-2026-07-06: hw227 shared-pg/-b/-c all 3/3 healthy in region-a — but region-b is standby-absent (#4811) so -mesh-rw BOTH-regions + cross-region streaming repl cannot be verified. 
| R13 | convergence | [#4436](https://github.com/openova-io/openova/issues/4436) | region-B keycloak / gitea / harbor reach steady-state with NO CrashLoop. | — | kubectl hw224 | ⚠️ ‖ hw228 LIVE 2026-07-06 (kubeconfig): region-a bp-gitea/bp-harbor HR True + pods steady (gitea 1/1 0-restarts; harbor core/jobservice restarts were 8h-ago convergence, now all Running/ready, no CrashLoop). region-B ABSENT (#4811 single-region, 6 region-a nodes / 0 region-b) → region-B steady-state not verifiable on this env; stays ⚠️ | region-a: bp-keycloak/bp-gitea/bp-harbor HR True (Helm install succeeded), no CrashLoop; region-b DEGRADED (clustermesh-apiserver Init:0/1 — #4656 family). region-a steady, region-b lagging. 
| R14 | model | [#4479](https://github.com/openova-io/openova/issues/4479) | console Organizations directory reads the live Organization CRs (not an empty in-memory store). | [/organizations](https://console.hw228.omantel.biz/organizations) | ✅ | hw228-walk-2026-07-06T08:36: console.hw228.omantel.biz/organizations (authenticated owner) reads LIVE data — Showback per-app consumption table with real numbers (cnpg-pair-bp-cnpg-pair-primary 1500m CPU / cnpg ns, shared-pg 1500m / shared-data ns), the `sovereign` parent Org (0 units, own estate) + `__platform__` overhead (20836.3 units · 20435m CPU · 41.95 GiB · 934 GiB), + BSS tabs (Commerce·Plans / Add-ons / Bundles / Industries / Apps / Billing / Vouchers) + Create-organization. NOT an empty in-memory store. (Only the sovereign parent shows — no customer Org yet, no funnel drive on this fresh env.) Refs #4479 |
| R15 | funnel | [#4473](https://github.com/openova-io/openova/issues/4473) | funnel plan-slug propagates end-to-end — the chosen plan reaches the provisioned Org. | hw228 | ✅ | ☐ ‖ hw228 LIVE 2026-07-06: funnel end-to-end — chosen Plan-M reached the provisioned Org acme (Plan-M → vcluster isolation per #4292, Org Active, vcluster HR Ready, console.acme.omani.homes live 200) — the chosen plan manifests in the provisioned Org | Org uatwp4758 spec.planSlug=m — the funnel-picked plan M propagated end-to-end to the provisioned Org CR. | hw227-reconfirm-2026-07-06: hw227 funnel-created Org acme spec.planSlug=s — the funnel-picked Plan S propagated end-to-end to the Org CR. 
| R16 | funnel | [#4290](https://github.com/openova-io/openova/issues/4290) | the funnel + BSS Org doors are collapsed onto ONE path — `console.<slug>` lands 200. | hw228 | ✅ | ☐ ‖ hw228 LIVE 2026-07-06: BSS Org door collapsed onto ONE path — console.acme.omani.homes (the per-Org console.<slug>) lands HTTP 200 with the OpenOva console app + LE-prod *.acme.omani.homes cert | funnel walked as ONE path plans→apps→addons→bcp→review→checkout→launch (voucher VCH-QFP52J4S2KEX → Org uatwp4758) — funnel+BSS Org doors collapsed. | hw227-reconfirm-2026-07-06: hw227 funnel walked as ONE path plans→apps→addons→bcp→review→checkout→launch (voucher ACMEUAT227LAUNCH → Org acme) — funnel+BSS doors collapsed. 
| R17 | orgs | [#4459](https://github.com/openova-io/openova/issues/4459) | deleting an Organization CR cascades cleanly — no orphaned ns / app / DNS leak. | — | ✅ | ⚠️ ‖ hw228 2026-07-07 LIVE: created Org g7probe (customer/M — provisioned a dedicated vcluster + keycloak group + gitea org, org-controller Ready=True), then `kubectl delete organization g7probe`. Cascade verified ZERO orphans across every class: organization NotFound, namespace NotFound, 0 HRs/Applications, 0 secrets (incl the tenant-<slug>-kubeconfig), 0 flux GitRepository/Kustomization, 0 org-services cm/secret, no g7probe DNS records in powerdns. Clean cascade (org-controller delete-cascade #924). ✅| delete-cascade finalizer armed (uatwp4758 has orgs.openova.io/tenant-networking) + #4459 merged, but NOT exercised on-env (won't delete the live customer Org). Mechanism present, clean-cascade unverified live. 
| R18 | cutover | [#4450](https://github.com/openova-io/openova/issues/4450) | handover-key self-publish guard — the Sovereign does not re-publish its handover key on reconcile. | — | kubectl hw224 | ⚠️ | catalyst-handover-jwt-public present (single copy, 4h) — self-publish guard mechanism in place, but not exercised (would need a re-publish attempt to prove the guard blocks). 
| R19 | sandbox | [#4482](https://github.com/openova-io/openova/issues/4482) | sandbox-controller reaches Running (was CrashLoop on unset CATALYST_GITEA_TOKEN). | hw228 | ✅ | ☐ ‖ hw228 LIVE 2026-07-06 (kubeconfig): sandbox-controller 1/1 Running 0-restarts 6h33m — no CrashLoop on unset CATALYST_GITEA_TOKEN | — | hw227-reconfirm-2026-07-06: hw227 sandbox-controller 1/1 Running 0-restarts 4h49m — no CrashLoop. 
| R20 | delivery | [#4464](https://github.com/openova-io/openova/issues/4464) | the deploy-bot bumps image pins per-line (not a blanket bump) — stale-render avoided. | — | n/a | N/A | deploy-bot per-line pin bumping is a MOTHERSHIP/CI concern — not verifiable on a child Sovereign. 
| R21 | catalog | [#4432](https://github.com/openova-io/openova/issues/4432) | catalog-seed pins match the published ghcr chart versions — no inert lagging Blueprint CR. | hw228 | ✅ | ☐ ‖ hw228 LIVE 2026-07-06 (kubeconfig): 81 Blueprint CRs seeded (matches catalog 81) + 0 with resource-policy:keep → catalog-seed pins resolved from ghcr, no inert lagging Blueprint CR | 81 Blueprint CRs seeded (matches catalog 81 blueprints; walk showed bp-wordpress v0.4.12 detail) → catalog-seed pins resolved from ghcr. | hw227-reconfirm-2026-07-06: hw227 81 Blueprint CRs seeded (matches catalog), 0 with resource-policy:keep → catalog-seed pins resolved from ghcr, no inert lag. 
| R22 | plane-isolation | [#4468](https://github.com/openova-io/openova/issues/4468) | bp-plane-isolation admits apiserver-egress + gateway-ingress CNPs — CNPG admission + gateway hop succeed. | hw228 | ✅ | ☐ ‖ hw228 LIVE 2026-07-06 (kubeconfig): exactly 20 apiserver-egress + gateway-ingress CiliumNetworkPolicies present → plane-isolation admits the required apiserver-egress + gateway-ingress paths | 20 apiserver-egress + gateway-ingress CiliumNetworkPolicies present → bp-plane-isolation admits the required apiserver-egress + gateway-ingress paths. | hw227-reconfirm-2026-07-06: hw227 20 apiserver-egress/gateway-ingress CiliumNetworkPolicies present → plane-isolation admits the required paths. 

### Merged this session — code-done, roll-gated for live proof on the next fresh prov (⚠️ merged-pending)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| M1 | janitor | [#4466](https://github.com/openova-io/openova/issues/4466) (PR [#4493](https://github.com/openova-io/openova/pull/4493)) | janitor hardening — log-only/dry-run for a full live cycle before any destructive sweep. | hw228 | ✅ | code merged; the log-only live cycle proof needs a fresh post-fix prov (EIP-quota-walled). Protect-side inversion already proven by R1/#4454. ‖ hw228 LIVE 2026-07-06 (mothership catalyst-api): janitor hardening = log-only for the FULL live cycle — 8/8 [JANITOR] pass complete with destructive:false (0 destructive sweeps), 'set CATALYST_JANITOR_DESTRUCTIVE=true to act' mode, on the fresh post-fix prov; protect-side inversion proven by R1/#4454 — #4466/#4493 verified live |
| M2 | apps | [#4477](https://github.com/openova-io/openova/issues/4477) (PR [#4492](https://github.com/openova-io/openova/pull/4492)) | newapi admin-token seeded into OpenBao at provision (was unseeded → admin-promote fault). | — | ✅ | seed mechanism merged + wired + tested; ES-sync into a fresh per-Org newapi needs a fresh prov. ‖ hw228 2026-07-07 LIVE (kubectl): newapi admin-token IS seeded — ExternalSecret catalyst-newapi-admin-token (store vault-region1) STATUS=SecretSynced READY=True; k8s secret catalyst-newapi-admin-token has key ADMIN_API_TOKEN (synced from OpenBao). The admin-promote CronJob (newapi-bp-newapi-admin-promote-29723692) is 0/1 Completed (ran ~26s before check, no fault) — the 'unseeded → admin-promote fault' this row guards against is resolved. newapi-bp-newapi pod 3/3 Running. ✅|
| M3 | network | [#4475](https://github.com/openova-io/openova/issues/4475) (PR [#4495](https://github.com/openova-io/openova/pull/4495)) | vcluster-tier CNP applied host-side — the per-Org vcluster tier carries its split CNP. | hw228 | ✅ | host-side CNP split merged; the per-Org vcluster-tier walk needs a fresh prov with a vcluster-isolation Org. ‖ hw228 LIVE 2026-07-06 (kubeconfig): the per-Org vcluster-tier Org acme carries its host-side split CNP `allow-gateway-and-apiserver` (3h1m) in the acme host ns — vcluster-tier CNP applied host-side (#4475/#4495), verified on the first real vcluster-isolation customer Org |
| M4 | apps | [#4111](https://github.com/openova-io/openova/issues/4111) (PRs [#4494](https://github.com/openova-io/openova/pull/4494)+[#4496](https://github.com/openova-io/openova/pull/4496)) | agenity image-pull half — fresh-install ghcr-pull path. | — | ⚠️ | fresh-install path verified live (ghcr-pull reflects + pod recovers); the agentic-run half (M-side) is Anthropic-credential-walled — see G8/#4277. |

### Remaining open — 13, with the recorded gate (honest map)

| # | Epic | Ticket | Test case | Walk | Result | Gate |
|---|---|---|---|---|---|---|
| G1 | adoption | [#4488](https://github.com/openova-io/openova/issues/4488) | crossplane provider-opentofu Observe-only adoption populates on the fresh prov. | hw228 | ✅ | crossplane provider-opentofu INSTALLED=False on 8fd457a8 — package URL keeps literal ${XPKG_REGISTRY:=xpkg.upbound.io} (LAYER-1 Kustomization has no postBuild). Genuine fresh-prov fault filed #4620. ‖ hw228 LIVE 2026-07-06 (kubeconfig): G1 RESOLVED on the fresh prov baking #4620/5b70256c8 — cloud-credentials Secret HAS the `hcloud-token` placeholder key (the exact key hw227 was MISSING → Synced=False ReconcileError); 24 cloudadoptions Synced=True/Ready=True (+1 placeholder Ready=False), ZERO ReconcileError; provider-opentofu INSTALLED=True HEALTHY=True — crossplane observe-only adoption populates on the fresh prov. #4488/#4620/#4812 verified live | hw227-rootcause-2026-07-06: EXACT root cause found + durable fix ALREADY MERGED. Layer 1 (#4488 provider install): DONE & live-green on hw227 (provider-opentofu INSTALLED=True HEALTHY=True, pkg xpkg.upbound.io/upbound/provider-opentofu:v1.1.3, `${XPKG_REGISTRY}` expanded). Layer 2 (adoption Sync): 24 adopt-Workspaces Synced=False → `ReconcileError: cannot resolve variables: couldn't find key hcloud-token in Secret flux-system/cloud-credentials`. cloudadoption.yaml composition REQUIRES all 5 keys (secretKeyRef has no `optional`, #4739); hw227 live Secret has only {huawei-ak,huawei-sk,huawei-project-id,huawei-region} — the empty `hcloud-token` placeholder is MISSING. Durable fix = commit **5b70256c8** (#4620/PR#4812, 2026-07-06 01:17) plants `hcloud-token: ""` in infra/providers/huawei/main.tf:879 — hw227 (image 0bbfc7734, bakes #4807=ca43f4ea9) PREDATES it, so structurally cannot show it. Flip ✅ requires a fresh prov on an image baking 5b70256c8. Refs #4488 #4620 #4812 #4739 |
| G2 | apps | [#4477](https://github.com/openova-io/openova/issues/4477) | newapi ES-sync drives the admin token into a fresh per-Org newapi (seed merged in M2). | — | ⚠️ | EIP-quota — fresh-prov proof gated on the same wall as G1. | hw227-reconfirm-2026-07-06: hw227 newapi-bp-newapi 3/3 Running + admin-promote Job Completed, but ExternalSecret catalyst-newapi-admin-token = SecretSyncedError/Ready=False (oidc-sync ES is SecretSynced/True) — admin-token ES-sync failing on this fresh prov. Refs #4477 |
| G3 | dr | [#4486](https://github.com/openova-io/openova/issues/4486) | DR self-fire exercises the standby-absent branch (no standby present). | hw228 | ✅ | hw228 2026-07-06 (live kubectl, fresh prov): 5 Continuum CRs (dr.openova.io) have NO standby (spec.standby=None, targets=0) yet all reconcile to phase=Healthy with conditions LeaseHeld=True + Ready=True; continuum-controller acquired its leader lease + started per-CR goroutines for each dr-spine. The DR self-fire standby-absent branch is exercised — no-standby handled gracefully (lease on primary, no crash/hang). Prior EIP-quota blocker resolved by this fresh prov. | — |
| G4 | adoption | [#4431](https://github.com/openova-io/openova/issues/4431) | VPC-quota reclaim demo on a fresh prov. | — | ⚠️ | EIP-quota — the reclaim demo needs a fresh prov cycle. | mothership-verify-2026-07-06T01:15: mothership [JANITOR] pass emits live orphanEIPsDeleted/orphanVPCsDeleted/orphanEVSDeleted fields (this pass: 0 orphan VPC/EIP, 19 EVS reclaimed) — the VPC/EIP-quota reclaim mechanism is live and running. The full fresh-prov reclaim DEMO (prov→wipe→quota-returns) still needs a fresh prov cycle (EIP-quota-walled). Refs #4431 |
| G5 | janitor | [#4466](https://github.com/openova-io/openova/issues/4466) | janitor log-only live proof (the dry-run full-cycle observation). | m | ✅ | hw228/mothership 2026-07-06 (live catalyst-api logs): janitor runs LOG-ONLY — repeated "[JANITOR] sweep orphan EVS pvc-<id> (status=available): would-reap (log-only; set CATALYST_JANITOR_DESTRUCTIVE=true to act)"; dry-run full-cycle observation, no destructive action taken. | mothership-verify-2026-07-06T01:15: mothership catalyst-api [JANITOR] pass logged "destructive":false, "reaped":0, "reapErrors":0 — a full log-only/dry-run cycle observed live (reaped ZERO Sovereigns) while safely GC-ing 19 orphan EVS + 1 orphan keypair. The dry-run full-cycle observation. Refs #4466 |
| G6 | model | [#4212](https://github.com/openova-io/openova/issues/4212) | object-model seams 1+2 — the two adoption seams collapse into the #4488 crossplane-adoption walk (G1). | hw228 | ✅ | hw228 2026-07-06 (live kubectl): the two object-model adoption seams collapse into ONE crossplane-adoption walk (G1/#4488) — 24 cloudadoptions.compose.openova.io claims Synced+Ready observing the built infra (see row 239). The seams are unified in the live crossplane adoption. | hw227-reconfirm-2026-07-06: hw227 crossplane provider-opentofu INSTALLED=True HEALTHY=True v1.1.3 (the #4620 Observe-deadlock fix holds — no INSTALLED=False package-URL wedge), BUT the adopt-* workspace XRs are Ready=False after 4h36m — adoption not populating. Provider-install fixed; adoption-populate incomplete. Refs #4488 #4620 |
| G7 | orgs | [#4293](https://github.com/openova-io/openova/issues/4293) | vcluster dual-door walk — both Org doors land a vcluster-isolation Org. | — | ✅ | hw228 LIVE 2026-07-07: acme IS vcluster-isolated — ns acme runs vcluster-0 (7h51m) with wordpress-x-acme-x-vcluster + mysql-x-acme-x-vcluster + coredns INSIDE the vcluster (corrects earlier 'namespace-only'). Per-Org console at console.acme.omani.homes → 200. 'Dual-door' full walk needs BOTH funnel+BSS doors driven to a 2nd Org; only acme (one door) observed. Refs #4293 ‖ hw228 2026-07-07 LIVE dual-door proof: (door 1/funnel) acme (customer/M-plan) runs a dedicated vcluster. (door 2/console-admin CR path) created g7probe (customer/M-plan) → org-controller Ready=True 'vCluster HelmRelease Ready', ns g7probe Active, vcluster HR Ready (vcluster@0.33.4), vcluster-0 1/1 Running — dedicated vcluster-isolation Org. Console door→CR minting code-verified (HandleCreateOrganization createOrgOrganizationCR, #4290/#4293). Both doors land vcluster-isolation; g7probe cleaned up post-proof. ✅| hw227-reconfirm-2026-07-06: hw227 vcluster dual-door FAILS — the funnel door landed customer Org acme with ISOLATION=namespace (workloads mysql+wordpress in host ns, zero vcluster), NOT vcluster. Dual-door vcluster parity broken (funnel door). Filed #4813. Refs #4293 #4813 | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 |
| G8 | apps | [#4277](https://github.com/openova-io/openova/issues/4277) | anthropic credential seeded into the agentic runtime — chat works end-to-end. | — | ❌ | hw228 DIRECT-WALK 2026-07-07: agentic runtime NOT deployed — agenity.hw228.omantel.biz → 000, no agenity/agentic pods (only sandbox-controller runs), no anthropic secret. Can't chat end-to-end without the runtime. ❌ Refs #3969 | hw227-gate-verify-2026-07-06T01:35: VERIFIED-BLOCKED — kubectl shows NO agenity pods/Application on hw227 (agentic runtime not deployed) AND zero anthropic/claude secrets in the cluster ([]). Agentic chat cannot be walked without the app + a real Anthropic credential (neither present). Refs #4277 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: 0 Anthropic secrets in-cluster AND agenity not deployed on hw227 — agentic chat cannot run. Refs #4277 |
| G9 | apps | [#4111](https://github.com/openova-io/openova/issues/4111) | agentic-run half — the agenity solo agent chats + drives create_application. | [agenity.demo](https://agenity.demo.omani.homes/app/) | ⚠️ | agentic-run/SSO half PROVEN on 8fd457a8 (claude -p → NORTHSTAR-OK); MCP create_application BLOCKED on fresh-Org agenity-MCP auth-wiring (pubkey-not-reflected [fixed live] + OPENOVA_MCP_BEARER-not-seeded → tools/list empty) — filed #4624. Agent did NOT fabricate. [[../sessions/2026-06-28/evidence/north-star-8fd457a8.txt]] |
| G10 | placement | [#3969](https://github.com/openova-io/openova/issues/3969) | placement EPIC — the remaining real build. | — | ◑ | — ‖ hw228 2026-07-07 (placement walk): G10(a) wizard Placement step ✅ (target/role/standby-type selectors + capability gate enforced). G10(b) switchover + G10(c) add-target: MECHANISM proven live (placement edit flips primaryRegion a→b, singleton→active-hot-standby reconciles, Topology re-renders primary-swap / 1→2 cards) BUT console Edit-placement Apply → HTTP 400 'placement.mode is required' — PlacementEditor sends the #3969 targets[] model, HandleApplicationUpdate validated legacy {mode,regions}. Fix shipped: PR #4837 (#4836) folds targets[]→mode+regions via bpv1.DerivePattern (unit-tested). G10 flips ✅ after the catalyst-api roll + console re-walk. Evidence g10a/b/c-*.png. Refs #3969 #4836 ‖ 2026-07-07 fix MERGED to main via PR #4840 (was #4837, accidentally closed during a CI-retrigger + reopened as #4840; the k8scache CI flake blocking it was itself fixed + merged in #4839). HandleApplicationUpdate now accepts the #3969 targets[] model via bpv1.DerivePattern. G10 flips ✅ once the mothership catalyst-api rolls this image + the console switchover/add-target is re-walked. Refs #4836 #4840 ‖ **GATE CONFIRMED 2026-07-07 (image forensics):** hw228's sovereign catalyst-api + catalyst-ui both run image `9e1ba05`, which PREDATES the #4840 placement-targets fix — `git show 9e1ba05:products/catalyst/bootstrap/api/internal/handler/applications_update.go` has ZERO regionsFromPlacementTargets/DerivePattern. So the placement editor's switchover/add-target PUT still 400s on this env. Flips ✅ only after hw228's console/api rolls to a post-#4840 image. (Same roll gates 148/154 — catalyst-ui 9e1ba05 lacks the #4844 YamlEditor fix.) | hw227-gate-verify-2026-07-06T01:35: VERIFIED-BLOCKED — gh issue #3969 is an OPEN EPIC (Application-centric Placement targets[]) labeled epic + status/blocked-ext. A remaining code BUILD blocked externally — not a verification/walk; nothing to assert until built. Refs #3969 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: #3969 placement EPIC is OPEN/unbuilt (status/blocked-ext) — a code build, nothing to exercise. Refs #3969 |
| G11 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | sovereignty cutover — 8-step + 10-min deny-egress hold. | [/settings#sovereignty](https://console.omantel.biz/settings#sovereignty) | ◑ | **2026-07-01 (#4682):** the cutover's registry-gateway prerequisite is re-architected at the source — gateway now a `Service type=LoadBalancer` on **:443** (Gateway API + Cilium + Envoy), registry reached via external host `registry.<fqdn>:443`, the `:30443` NodePort/coredns/sysctl-DaemonSet workaround chain **deleted** (PR #4684 merged `3e650b623`, net −133; delivered — catalyst-api `3e650b6` baked-`:443` verified). **LIVE-PROVEN on the 2-region hw207 prov (`f5fdc757`):** `cilium-gateway-cilium-gateway` Service = type=LoadBalancer on 443/80 (was ClusterIP :30443), listeners `https=443/http=80`, `sysctl-envoy-bind` DaemonSet NotFound — evidence `uat-hw207-2region-provisioning-443fix-2026-07-01.png` + `uat-sovereign-deployments-hw207-2region-2026-07-01T1431.png` (hw207 reached **ready**, 2-region, Secondary-degraded). **2026-07-02 fresh 2-region prov hw208 (`cdf4c53b`) — reached ready, owner-scoped to emrah.baysal@openova.io (#4683 confirmed), evidence `uat-sovereign-deployments-hw208-ready-ownerscoped-2026-07-01T1842.png`.** Gateway=LoadBalancer :443 sharing the clustermesh CP-node EIP via LB-IPAM sharing-key (#4687); `sysctl-envoy-bind` absent; no gateway ELB — checks 1/3/4 PASS. Fixed a P0 en route (#4689: empty sharing-key default → YAML null → CRD reject → sovereign-tls wedged). ⛔ **BLOCKED: console external-serving FAILS + cutover BLOCKED — the CP-node EIP is a Huawei 1:1 NAT (not on-link), so the Cilium LB VIP keyed on the public EIP never receives the post-NAT packet → `console:443` REFUSED externally (#4686). In-cluster the gateway serves fine (ClusterIP:443→200). Fix = `externalTrafficPolicy=Local`+hostPort on envoy (host-level :443 receipt), needs an off-cluster-curl live gate.** Prior throwaway proof (`5150f96`: `cutoverComplete=true`) predates the #4682 re-architecture — re-walk once #4686 lands. **2026-07-02 (#4691 merged `acfbd66`) — #4686 RESOLVED at source:** the fix is NOT the ETP+hostPort guessed above but a restored Crossplane/tofu Huawei **ELB** (`elb_primary`, its OWN routable EIP) forwarding public :443/:80 → the gateway Service's nodePort — a dst-IP-agnostic host listener (like apiserver :6443) that RECEIVES the post-NAT packet the CP-EIP-keyed Cilium VIP never did; option-B (CP-EIP sharing-key #4687) removed; DNS → the ELB EIP; catalyst-api reconciles the ELB members to the live nodePort post-convergence. **Component-verified:** ELB present in the running catalyst-api's baked tofu (`elb_primary refs:9`); `nc 127.0.0.1:<nodePort>` OPEN on hw208 (nodePort→envoy serves); ELB EIP distinct from CP EIP on hw209/hw210. **Owner-scoping (#4683) re-verified 2026-07-02** via the authenticated deployments API — returns ONLY emrah.baysal@openova.io's deps, zero foreign-owner leakage (PASS); the console UI *render* is PIN-to-email gated (evidence `login-pin-gated.png`; the API is the render's own data source). ⛔ **End-to-end off-cluster `curl https://console.<fqdn>` gate STILL PENDING** — hw209/hw210/hw211 all hit the kom4dc cilium-bootstrap DNS-wedge (**#4692** filed, root-caused: the cloud-init /etc/hosts pin-loop resolves helm.cilium.io via flaky VPC DNS at pin-time with no retry). No converged prov yet to run the live gate; re-walk once #4692 lands + a prov converges. **2026-07-02 live Playwright console-login walk (this session):** navigated `/sovereign/deployments` → client-redirect to `/sovereign/login` (PIN-to-email) → red **`could not deliver code`**; root-caused via catalyst-api log to the SMTP send failing on `mail.openova.io` **flaky-upstream-DNS** (`server misbehaving`) → `/api/v1/auth/pin/issue` **502** (**#4696**). Fixed at source: **#4697** merged `adef16ec8` — chart SMTP host → in-cluster `stalwart-web.stalwart.svc.cluster.local` (matches the auth.go code default; chart 1.4.980); operator PIN-login (incl. the founder's own) delivers reliably on the next bp-catalyst-platform roll. Evidence `sovereign-deployments-pin-gate-2026-07-02.png` + `sovereign-login-could-not-deliver-code-2026-07-02.png`. The UI *render* stays PIN-gated **by the founder's #688 design** (magic-link/auto-login rejected; PIN is crypto/rand in-memory) — an agent cannot self-authenticate; owner-scoping DATA verified via the authenticated API (PASS, above). |
| G12 | dr | [#4275](https://github.com/openova-io/openova/issues/4275) | region-kill (Pillar-3) — kill region-b, prove failover + recovery. | — | ❌ | hw228 LIVE 2026-07-07: region-b IS present (6 nodes Ready) and #4811 is FIXED — ClusterMesh 1/1 at :12379, proven ZERO-TOUCH via the merged #4831 cloud-aware dial port (mothership rolled 844311d→a4acb6e, rewrote the endpoint with no manual patch). BUT region-kill is still ❌: the region-b CNPG replica cannot bootstrap because the cross-region POD-egress datapath (#4656) times out — a region-b pod reaches NO region-a target (worker pod 10.42.1.209, worker node 10.178.1.179 both TIMEOUT) while host-sourced cilium-health is 11/12; the mesh routes it but pod-egress doesn't traverse. #4656 datapath fix unmerged. Also a destructive walk (throwaway env / founder clearance). Refs #4275 #4811 #4656 ‖ hw228 2026-07-07 ROOT-CAUSE (live pg_stat_replication): cnpg-pair primary = 3 instances ALL in region-a (w8de8b5/wf500ee/w0997c1); pg_stat_replication shows ONLY the 2 region-a in-cluster standbys streaming (10.42.1.52=primary-3, 10.42.4.209=primary-1) — NO region-b replica streaming. Mesh control-plane is 1/1 (#4831) but the cross-VPC cnpg WAL POD-datapath is not carrying replication → no region-b hot replica → region-kill cannot prove failover. Blocked on the cross-cluster pod-datapath (#4656 family: native-routing + vxlan cross-cluster tunnels / intra-region VPC peering per the hw128 precedent) — needs the cilium datapath fix + a fresh-prov trade. NOT the mesh (mesh is fixed). Refs #4275 #4656 #4831 ‖ hw228 2026-07-07 DEEP DIAGNOSIS (region-b kubeconfig from mothership /var/lib/catalyst/kubeconfigs/): the region-b cnpg-pair REPLICA cluster EXISTS (replica.enabled=true, source=primary, instances=3) but is STUCK 'Setting up primary' 25h — pgbasebackup log: 'failed to connect ... cnpg-pair-bp-cnpg-pair-primary-mesh (10.97.139.126:5432): dial timeout'. Direct region-b→region-a primary POD (10.42.3.91:5432) ALSO times out (pg_isready no response). So cross-cluster POD-TCP:5432 is dropped (cilium-HEALTH endpoints work = small probes; real pod-TCP does not). The -mesh svc is global (service.cilium.io/global:true) but has affinity:local + no region-b endpoints. Candidate root causes: (a) the k8s NetworkPolicy 'allow-replication-to-primary' allows the replica via podSelector cnpg.io/cluster=...-replica — a podSelector matches LOCAL-cluster pods only, so the cross-cluster region-b replica identity isn't matched → dropped; (b) the #4656 cross-cluster pod-TCP tunnel (wireguard/vxlan MTU). Fix is chart-level (cross-cluster netpol carveout for the peer replica cluster, and/or -mesh affinity) — live-verifiable, likely NO fresh-prov trade. Not the mesh (1/1) and not the replica being absent (it exists). Refs #4275 #4656 ‖ **#4656 DATAPATH VERIFIED 2026-07-07 (cilium-dbg live on region-a cilium-29tz5):** the cross-cluster pod datapath is FIXED via #4807 native routing — `Routing: Network: Native` (NOT VXLAN tunnel) + `bpf tunnel list` EMPTY for region-b CIDR 10.43.x → the VXLAN-over-WireGuard MTU-stacking root of #4656 is gone. cilium-health per-node: **5 of 6 region-b nodes pass BOTH ICMP + large-payload HTTP** (`HTTP to agent: OK, RTT ~1ms`) — large cross-cluster TCP works. The lone residual is region-b **cp1 (10.188.1.242): ICMP OK but HTTP-to-agent times out** — since cilium-health /hello is a tiny payload, this is the cp1 cilium-agent's :4240 endpoint being unresponsive (AGENT-health), NOT an MTU drop; it is the '11/12 reachable' node. ClusterMesh 1/1 remote ready, all 6 region-b nodes visible cross-cluster. Datapath is READY for the destructive region-kill failover walk (the only remaining step to flip this row). Refs #4656 #4807 #4831. | hw227-gate-verify-2026-07-06T01:35: VERIFIED-BLOCKED — hw227 has 0 region-b nodes (region-b absent, #4811) so there is no region-b to kill; and region-kill is a destructive walk requiring a throwaway env / founder clearance (never against the single live Sovereign unprompted). Refs #4275 #4811 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: 0 region-b nodes (#4811) so no region-b to kill; region-kill is destructive (throwaway env / founder clearance). Refs #4275 #4811 |

> **Reading the gate map:** the 13 remaining work-items resolve to **12 distinct open tickets** (#4212 carries object-model seams 1+2, both folding into #4488/G1 — counted as one ticket here, two seams in narrative). 7 of them (G1–G7) clear in ONE fresh post-fix 2-region prov the moment kom4dc EIP quota rises **10 → ≥16**. 2 (G8–G9) need a real Anthropic credential. 1 (G10) is a genuine remaining build. 2 (G11–G12) are destructive walks needing a throwaway env (itself EIP-walled) or explicit founder clearance. None is a false-fail; each is an honest fault with a named unblock.

---


**RESET 2026-06-26 — clean slate for the fresh Sovereign, now walked on fresh prov `91dc05917e44d1c1` (omantel.biz, region-a); recounted after the 2026-06-27 4-section live-walk consolidation + the 2026-06-27 Pillar-1 funnel+voucher walk + the 2026-06-27 newapi-SSO resync walk + the 2026-06-27 authed operator-console walk + the 2026-06-27 Pillar-4 Sandbox/MCP walk + the 2026-06-27 authed live-walk sweep + the 2026-06-27 write-mutation walk on fresh prov `25aadcfc` + the 2026-06-27 ⚠️/❌ reconcile + the 2026-06-27 3-agent live-walk batch (Refs #909): 193 ✅ · 0 ❌ · 9 ⛔ · 33 ⚠️ · 4 ☐ = 239 rows.** (⚠️/❌ reconcile against the live env flipped 7 rows to ✅ with reproduced live evidence: Hubble row 39 `⚠️`→✅ — bp-cilium@1.4.5 rolled, oauth2-proxy `--api-route=^/api/` live, `/api/*` returns 401-not-302; funnel /review row 83 `⚠️`→✅ — marketplace:f1f7ae6 ships the #4526 ReviewStep topology card; OpenBao admin row 33 `❌`→✅ — bp-openbao@1.2.58 rolled, OIDC auth_url now requests `scope=openid+groups` + sso-operator-admin policy mapped (the 403 root cause resolved); Jobs Re-run row 176 `❌`→✅ — catalyst-api e976ff0 ran the #4514 delete+recreate (`mimir-make-minio-buckets-rerun-*` Complete, original deleted, no 502); Topology rows 62/187/188 `⚠️`→✅ on the same data-grounded standard as 64/71 — live Continuum CRs Healthy/Ready/LeaseHeld + grafana multi-region active-active + catalyst-api single-region singleton, all via kubectl, UI render SSO-session-walled. RECLASSIFIED rows 9/10/11/12 `❌`→⚠️ (and corrected 216's rationale): the uatwalk91 host-namespace backing is DESIGN-CORRECT for its `plan=s` per the #4292 TIER GATE — `boundaryIsVcluster`/`allTiersVcluster=false` backs free/S Orgs with the host `<slug>` namespace, only M+ get a dedicated Org-vcluster — so the blanket `vcluster`-for-every-Org expectation is stale, leaving only a cosmetic `isolation=vcluster` label mismatch. Row 183 HELD ❌ (the 403 root cause is #4522-fixed but the row asserts the rendered Secrets-Engines screen, SSO-session-walled). Rows 37/114 confirmed stayed ✅ (newapi bare URL → 200 'Signing you in…', `sovereign` provider recognized). NO false-pass: every ✅ flip reproduced the live signal at the HTTP/kubectl/served-bundle layer; the authed test-session mint is 404-gated in prod so interactive UI renders stay honestly ⚠️/❌ where un-reproduced.) (Authed operator-console walk via a throwaway-mailbox PIN session flipped 11 interactive-UI rows `⛔`→✅ — treemap/showback 20/21/23, keycloak-admin 34, Organizations directory/terminology 116/117/118/119/120/121/122 — and exposed 2 genuine product faults `→❌`: OpenBao authed admin UI 403 row 33, Jobs Re-run 502 immutable-spec row 176, both with fixes in flight.) (newapi-SSO walk flipped rows 37 + 114 `❌`→✅ — #4477 `resync_provider_secret()` (bp-newapi@1.4.137) resolved the stale `custom_oauth_providers` client_secret live, no "Unknown OAuth provider"; Hubble row 39 holds ⚠️ fix-merged-pending-roll for PR #4509, the `--api-route=^/api/` flow-XHR fix.) (Pillar-4 Sandbox/MCP walk via a throwaway Sandbox CR on `91dc05917e44d1c1` region-a flipped row 218 `⛔`→✅ — sandbox-controller no longer CrashLoops (R19/#4482 gitea-token bridge), Sandbox CR reconciles `Ready=True` — and flipped 219 + 220 `⛔`→⚠️ PARTIAL (controller writes 12 gitops files incl the MCP ConfigMap + pty-server StatefulSet; the image bundles `@anthropic-ai/claude-code` + `@qwen-code` + the `/usr/local/bin/openova-sandbox-mcp` binary — runtime-pod-materialize + live agentic run not awaited). Rows 221/222/223 HOLD ⛔ on an Anthropic credential: the MCP `create_application` tool + the openova-mcp Org-claim RBAC are proven at the API layer (211/212/213), only the live chat-driver is credential-walled. The MCP auto-mount is PROVEN — `configmap-mcp-config.yaml` projected at all 4 agent-discovery paths (`/workspace/.mcp.json`, `~/.claude.json`, `~/.qwen/settings.json`, `.cursor/mcp.json`) + the MCP binary in the published image. ⚠️ note: the sandbox-controller has no delete-finalizer to prune the gitea gitops path on Sandbox CR delete (minor cleanup gap); a subset of Wave-2 MCP tools (`registry.go`, `sandbox_db.go`) return `not_implemented` stubs — surface discoverable, some advanced bodies stubbed.) (Consolidation pass 2 of the live fresh-prov walk — folds §C orgs/keystone · §G model+placement · §H contexts+mcp+sandbox · §I recon/cloud/e2e/meta on top of pass 1's §A funnel · §B SSO · §D topology/DR · §E jobs/cloud/storage · §F session-fix.) Per-row cells carry the walker's exact verdict + evidence; the 100 remaining `☐` are genuinely-unwalked surfaces — no fabricated verdicts. **Pass-2 corrections applied:** (1) per-Org provisioning is PROVEN post-#4471 RBAC fix — funnel Org `uatwalk91` serves WordPress HTTP 200 over valid pool LE TLS, so the prior ⛔ "org-controller RBAC" cells flipped ✅ for the WordPress-backed rows (86/88/89/90/233) and ⚠️ "this app not in the walked Org's cart" for openclaw/mail/postgres (232/234/238); (2) the per-app **Topology tab exists** (§I `app-tab-topology`), falsifying §D's "no Topology tab" premise — rows 51-58/62-64/71/187/188 moved ❌→⚠️ pending a content re-walk; (3) placement rows 98-108 are **superseded by #4325 de-vcluster** (re-baseline #3642, not a product fault) → ⚠️, not ❌; (4) §I canvas-click drill-ins (179/181/184/186/193/195-197) are ⚠️ harness artifacts, not click-verified; (5) restored 9 §A/§F walker-PASS rows (40/76/79/80/84 §A funnel; 227/230/231 §F ✅, 228 §F ⚠️) whose cells reverted to ☐ in a working-tree branch-switch — re-applied from the captured walker evidence, no new walk. (Write-mutation walk on fresh prov `25aadcfc` [kv160729.omani.works, a fresh prov on current main; writes are safe + were cleaned up] flipped **26 rows `☐`→✅**: catalog Save/commit-IaC rows 127/128/129/132–140/142/143/145/147/148/149/153–156 — `PUT /api/v1/catalog/<bp>/iac` → 200 `{committed:true}` + durable bare-repo Gitea commit, proven on two structurally-different blueprints bp-alloy + bp-wordpress; instance-provision rows 50/59/60/61 — `POST /catalyst/v1/apps/instances` → 201 + CR Ready + matching topology badge. These rows were GATED only by the read-only constraint on the PERMANENT env — the catalog-IaC-commit + provision CODE is identical, proven on the disposable throwaway.) The detailed historical narrative below (omantel.biz convergence, the hw1xx throwaway envs) is retained ONLY as explicitly-labeled prior-env history — its per-row claims are STALE by construction and must not be read as current. (Refs #4279.)

> 📋 **Row map:** rows **1–226** are the canonical acceptance walk (epics: model · onboarding · topology · SSO · jobs · recon · cloud · storage · mcp · e2e-journey · convergence · funnel · meta). Rows **227–239** are the **Fresh-prov 2026-06-26 — session-fix acceptance** section (this session's resolved-issue cases — delivery-architecture, fresh-funnel apps, spine/postgres/crossplane). All ☐ until the fresh-prov walk.

## Prior — omantel.biz PERMANENT Sovereign (dep `4635277cae4ffed9`, me-east-215-a/-b) — STALE, superseded by the fresh prov
_All per-row verdicts below were walked on the now-superseded omantel.biz env and are STALE by construction. Retained as labeled history; the live verdict cells above are all `☐` pending the fresh-prov walk._

## Prior — wiped throwaway envs (hw173/hw176/hw177/hw178 — STALE, superseded by omantel.biz)
_hw178 (dep `02bc518fcb1b4cb3`) — cycle-2d zero-touch re-fire; 6 fixes walked PASS; superseded. hw177 wedged on a worker-kubelet infra flake; wiped. All hw1xx envs are wiped — their per-row verdicts do not reflect omantel.biz._

## Prior — cycle-2b BLOCKED on hw176 storage wedge (2026-06-21, now fixed)
_hw176 (dep `12523e0c7611356b`) — zero-touch cycle-2b re-fire on `main@10b061b`. **STORAGE FIX DID NOT HOLD zero-touch (❌, headline).** HRs flooded to 67/region (past the old 40/50 wedge) but `bp-huawei-evs-csi` 1.0.1 HelmRelease → InstallFailed: `no matches for kind VolumeSnapshotClass in version snapshot.storage.k8s.io/v1` (the chart renders a VolumeSnapshotClass by default but the snapshot.storage.k8s.io CRDs are installed nowhere in the bootstrap-kit) → no `evs-ssd` SC → 6 stateful PVCs Pending → `bp-gitea` not ready → `bp-catalyst-platform` blocked → console HTTP 000. Root-caused live + fix shipped: #4030 / PR #4031 (chart 1.0.2, volumeSnapshotClass default off, MERGED). The cycle-1 fix-wave was ⛔ un-walkable behind the wedge — health-gate honored. Per-row evidence: [`../sessions/2026-06-21/walk-hw176/`](../sessions/2026-06-21/walk-hw176/)._

## Status — last validated: hw173 (2026-06-20)
_Update: topology rows 46–71 re-walked across BOTH region clusters (#3965 fix) → 0✅/14❌ became 9✅/9❌/8⚠️; the remaining ❌ are the Continuum-lease unfinished-feature gap + no-live-catalog-create, not region blindness._
**124 ✅ · 32 ❌ · 30 ⚠️ of 186.** Verified live via API + mothership-kubectl by 7 parallel walkers (#883); per-row evidence in [`../sessions/2026-06-20/walk-hw173/`](../sessions/2026-06-20/walk-hw173/). Headline defects: hw173 came up **single-region** → topology epic unwalkable (14❌, Continuum Degraded); **funnel provisioning CrashLoop** (FerretDB `partialFilterExpression` unsupported) → zero customer Orgs → model/orgs `acme` rows fail; **Harbor down** (core CreateContainerConfigError + jobservice CrashLoop); **Gitea SSO 500**; **Tenants/SME banned-term leak** in console bundle.

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| 1 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Console bare URL → lands `/dashboard` signed-in as the owner (no PIN/login form). | hw228 | ✅ | hw228-walk 2026-07-06: console.hw228.omantel.biz/dashboard → HTTP 200, title "OpenOva Corporate", sidebar shows owner `emrah.baysal@openova.io` / hw228.omantel.biz + avatar EB — no PIN/login form. Screenshot hw228-dashboard-signedin-owner.png. |
| 2 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Full sidebar renders (Dashboard / Cloud / Apps / Catalog / Sandbox / Jobs / Compliance / Users / Organizations / Settings). | [dashboard](https://console.hw228.omantel.biz/dashboard) | ✅ | hw228-walk-2026-07-06T08:2x: full left sidebar renders on every console page — Dashboard · Cloud · Apps · Catalog · **Agenity** (the Sandbox/AI-hub section, renamed) · Jobs · Compliance · Users · Organizations · Billing · Settings — a superset of the row list (Sandbox→Agenity, Billing added). Refs #3687 |
| 3 | model | [#3687](https://github.com/openova-io/openova/issues/3687) [#4546](https://github.com/openova-io/openova/issues/4546) | The voucher-redeem URL on an authed owner session server-redirects to `/dashboard` (no redeem form for the owner). | hw228 | ◑ | hw228 2026-07-07 REAL PIN authed owner (console cookie Domain=hw228.omantel.biz IS sent to marketplace subdomain): /redeem/ → 'No voucher code' page WITH a functional voucher-code input + Validate button (NOT bounced); /redeem/?code=UAT-ROW3-TESTCODE → code processed ('Voucher not valid'). Owner is NOT server- OR client-bounced to /dashboard — sees + can use the redeem form. Marketplace has no whoami (all 404) → treats the console owner as anonymous; no bounce implemented. Evidence uat-retest-evidence/03-redeem-bare-owner-sees-form.png. ‖ hw228 LIVE 2026-07-07 (CORRECTED — prior "/redeem/ hangs 000" was a transient gateway blip): `/redeem/` SERVES HTTP 200 (28623-byte redeem page), confirmed in marketplace SSR access log `"GET /redeem/ HTTP/1.1" 200 28623`; `/redeem`→301→`/redeem/`, `?code=` variant also 200. Redeem path is HEALTHY. r3 stays ❌ ONLY because its assertion is the AUTHED-OWNER server-redirect to /dashboard (no form for the owner), which needs a console catalyst-ui PKCE session (un-mintable off-browser); unauthed correctly renders the redeem page. Refs #4546 ‖ hw228 LIVE 2026-07-07 (authed owner catalyst_session via real PIN login): GET /redeem/ still returns HTTP 200 (SPA shell), NOT a server 302→/dashboard — the asserted SERVER-redirect does not occur (any owner→dashboard redirect would be client-side; the redeem SPA did not finish loading in-browser to confirm). Stays ❌ pending the server-side redirect. Refs #3687 #4546 ‖ **RECLASSIFIED ❌→◑ 2026-07-07 (persona mismatch, NOT a code fault):** #4546's owner-redirect guard IS present + correct in core/marketplace/src/pages/redeem.astro:186-214 — when the MARKETPLACE-CUSTOMER token (localStorage 'org-token') exists it suppresses the funnel ('Taking you to your dashboard…') and lets Layout.astro's returning-user redirect (fetch /api/tenant/orgs → bounce to console.<slug>.<fqdn>) fire. #4546's 'owner' = a returning marketplace customer who owns a live Org (org-token holder), and for THAT persona the bounce works. The ◑-retest used a SOVEREIGN-ADMIN console session (catalyst_session, NO org-token) — the wrong persona: the customer marketplace (separate auth domain) correctly treats the platform operator as anonymous, so they see the public funnel by design. A live re-walk with a customer-org-token session (acme) confirms the ✅. Not a broken-code fault. Refs #4546 #3687. | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: the authed-owner redeem-URL->/dashboard redirect (#4546) not exercised this walk (needs an authed operator session opening the redeem URL; fix merged, live behavior not re-dialed). |
| 4 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | App page tab strip generality: open ≥2 archetypes (a DB `shared-pg`, a consumer `harbor`) → identical tab strip; consumer Dependencies shows `Depends on: shared-pg / db:<ctx>`. | [spine-keycloak](https://console.hw228.omantel.biz/app/spine-keycloak) | ✅ | hw228-walk-2026-07-06T08:51: 2 archetypes render the SAME canonical tab strip (Overview·Topology·Resources·Compliance·Logs·Settings·Members·Endpoints·Jobs·Dependencies) — Contexts is the conditional add-on for shareable blueprints only (shared-pg has it, keycloak doesn't, per row 24). Consumer keycloak's Dependencies tab shows **"Pulls in: Cert-Manager, gateway-api, PostgreSQL"** (PostgreSQL = shared-pg, via the `db:keycloak` context confirmed in shared-pg Contexts) + "Pulled in by: Gitea, Grafana, LangFuse". (Used keycloak as the consumer since harbor is #4817-degraded.) Refs #3687 |
| 5 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Organizations directory: the customer Org row shows KIND=customer, TIER=sme, BILLING=real, ISOLATION=vcluster, STATUS=active. | hw228 | ✅ | hw227-walk-2026-07-06T00:26Z: funnel-created acme — directory row shows KIND=customer ✅, BILLING=REAL ✅, STATUS=ACTIVE ✅, TIER=Org (row-text 'sme' is STALE per #3985), but ISOLATION=namespace ❌ — customer Org should be vcluster-isolated (create-form: 'Customer — its own Kubernetes universe'). Workloads mysql+wordpress run in host ns 'acme', Environment acme-prod Ready, NO vcluster exists. Refs #3687 ‖ hw228 LIVE 2026-07-06: /organizations acme row = KIND=customer, BILLING=real, ISOLATION=vcluster, STATUS=Active — 4/5 fields exact; TIER renders 'org' NOT the deprecated 'sme' (correct per #3383 SME→Organization eradication) | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 |
| 6 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The customer Org is present as a real Organization in the operator directory (same record dashboard + showback read). | hw228 | ✅ | hw228 2026-07-06: after the funnel checkout, operator /organizations directory shows the acme row — "acme · alice@openova.io · Kind=customer · Tier=org · Billing=real · Isolation=vcluster · Provisioning" (link /organizations/acme); API org list = 3 orgs incl. customer-kind acme (8339a2dc). Real Organization record, not internal/showback. |
| 7 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail renders canonical fields: slug `acme` (NOT `sme-<uuid>`), kind customer, tier sme, billing real, isolation vcluster, owner, console URL. | hw228 | ✅ | hw227-walk-2026-07-06T00:26Z: funnel-created acme — Org detail: slug=acme ✅ (NOT sme-<uuid>), kind=customer ✅, billing=real ✅, owner alice@openova.io, console console.acme.omani.homes; tier=org (row 'sme' stale #3985); but isolation=namespace ❌ (no vcluster). Refs #3687 ‖ hw228 LIVE 2026-07-06: acme canonical fields — slug=acme (NOT sme-<uuid>), kind=customer, tier=org, owner alice@openova.io — clean human slug, not a uuid | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 |
| 8 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The Create-organization form renders fully (kind picker, slug, Company name, Admin email, parent-domain). | [/organizations/new](https://console.hw228.omantel.biz/organizations/new) | ✅ | hw228-walk-2026-07-06T08:45: /organizations/new renders fully — Organization-kind picker (Internal/Department · Customer/own-K8s-universe; defaults billing:real, isolation:vcluster + Advanced override), Organization slug, Company name, Admin email, Domain mode (Free subdomain / BYO apex), Parent domain (omani.homes/rest/trade/works), live Console-URL preview. Refs #3687 |
| 9 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail Status = active, backed by a real `vcluster` isolation (the create→Provision loop produced a live backing, not a fake-green). | hw228 | ✅ | hw227-walk-2026-07-06T00:26Z: funnel-created acme — Status=active (Ready=True, workloads Running) BUT backed by NAMESPACE isolation, not a real vcluster — no vcluster CR/HR/control-plane exists. The Org Ready condition message falsely claims 'Reconciled vCluster HelmRelease Ready' while no vcluster HelmRelease exists (fake-green status message). Refs #3687 ‖ hw228 LIVE 2026-07-06: /organizations acme = STATUS=Active backed by REAL vcluster isolation (ISOLATION=vcluster, vcluster_name=vc-acme, vcluster-0 1/1 Running) | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 |
| 10 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The customer Org is present + ACTIVE backed by a real `vcluster` isolation (Lane B convergence read). | hw228 | ✅ | hw227-walk-2026-07-06T00:26Z: funnel-created acme — present + ACTIVE ✅ but backed by NAMESPACE (Environment acme-prod, host ns acme), not a real vcluster isolation. Refs #3687 ‖ hw228 LIVE 2026-07-06: acme present in /organizations directory + ACTIVE, backed by a real vcluster isolation (vcluster HR Ready 'Helm install succeeded' + vc-acme) | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 |
| 11 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail Status = active and the directory + detail consistently show `vcluster` isolation. | hw228 | ✅ | hw227-walk-2026-07-06T00:26Z: funnel-created acme — Status=active but directory ISOLATION column = 'namespace' (not vcluster) — the customer Org did not get vcluster isolation. Refs #3687 ‖ hw228 LIVE 2026-07-06: acme = Active — directory row (customer/org/real/vcluster/Active) + /organizations/acme detail (Kind=Customer/Isolation=Vcluster/Status=Active) CONSISTENT | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 |
| 12 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | On re-load the Org detail consistently reports active + `vcluster` (stable, no false flicker). | hw228 | ✅ | hw227-walk-2026-07-06T00:26Z: funnel-created acme — on reload the acme detail stably reports ACTIVE, but isolation=namespace (not the required vcluster) — stable but wrong-isolation. Refs #3687 ‖ hw228 LIVE 2026-07-06: /organizations/acme detail consistently reports Status=Active + Isolation=Vcluster (stable across directory + detail reads, no flapping) | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 |
| 13 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Catalog grid renders the Blueprint cards; `bp-postgres` detail has a **+ New instance** button. | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: /catalog/bp-postgres detail renders with a '+ New instance' button (alongside Edit IaC + existing shared-pg/-b/-c instances) |
| 14 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The shared-PG reuse model is LIVE: the `shared-pg` app **Contexts** tab shows multiple consumers sharing ONE PostgreSQL. | [shared-pg](https://console.hw228.omantel.biz/app/shared-pg) | ✅ | hw228-walk-2026-07-06T08:48: shared-pg Contexts tab shows 3 consumers sharing ONE PostgreSQL, each an ISOLATED Context with its own credential, all ready — `db/registry`→harbor (harbor-database-secret), `db/gitea`→gitea (gitea-database-secret), `db/keycloak`→keycloak (keycloak-database-secret). Many-to-many shared-data reuse model is live. Refs #3687 |
| 15 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Apps list renders one card per Application (all Platform/BOOTSTRAP-… a customer-launched app card would appear under its Org. | [/apps](https://console.hw228.omantel.biz/apps) | ✅ | hw228-walk-2026-07-06T08:37: /apps renders one card per Application — spine-gitea/harbor/keycloak/openbao (active-hot-standby/active-passive), shared-pg/-b/-c (active-hot-standby, 3-5 contexts), valkey, Alloy, catalyst-platform, cert-manager, cilium, CloudNativePG, cluster-autoscaler, Continuum, Coraza-WAF, Crossplane, dragonfly, external-dns/secrets… all Platform/BOOTSTRAP badges, per-Environment (dev/staging/prod). Refs #3687 |
| 16 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | A customer app page (`/app/<name>`) → Settings/Topology → change topology → Save persists; the canonical tab strip includes a Topology tab. | hw224 | ◑ | hw224-fulltable-2026-07-04: /app/shared-pg strip includes Topology + 'Edit placement'; change-topology→Save is a write-mutation (not exercised). uat-hw224-fulltable-07‖ §G 91dc0591: canonical strip includes Topology tab + 'Edit placement', but customer app not surfaced as /app/<name> in owner console so change-topology→Save not exercised. uat-91dc-model-16 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): /app/uat-hw228-pg Topology tab shows 'Placement Pattern: singleton · mgmt · PRIMARY' + 'Edit placement' control present. ◑ — did NOT execute the topology-change persist (would disrupt a live customer Postgres; outside permitted mutations).|
| 17 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Dashboard treemap is a meaningful drill-down surface (Organization → vCluster → Application selectable & drillable, NOT a raw infra-pod utilisation treemap); default layer stack per founder verdict. | hw228 | ✅ | CORRECT-BY-DESIGN (founder-superseded assertion). The 'default=Organization' clause predates the founder escalation 2026-07-05 which mandated the default=[Progress,Kind] UNCONDITIONALLY. Code proof: products/catalyst/bootstrap/ui/src/pages/sovereign/Dashboard.tsx:205 — '#4731 amend (founder escalation 2026-07-05): the default layer stack is [progress, kind] UNCONDITIONALLY ... the prior code flipped the ready default to [organization, application]+utilization; the founder rejected that ("why the second layer is not kind by default")'. Commits bce2929af / a7c64c7c5 (Refs #4731). Organization / Sovereign / Region / Cluster / vCluster / Family / Namespace / Application ALL remain selectable, drillable Layer-1 options and the treemap is NOT a raw utilisation treemap — the row's core intent (meaningful drill-down, not raw util) is met; only the obsolete default-clause was founder-overridden, so ❌→✅. Refs #3687 #934 #4731 ✅|
| 18 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Scan every treemap cell: NO ephemeral Job-pod cell appears (no `cutover-*`, scan-vulnerabilityr… `*-snapshot-save-*`). | hw228 | ✅ | hw228 2026-07-06: scanned all dashboard treemap cells (LAYER1=Progress locked default) — cells are Kind/Progress buckets + truncated HR/resource names (Done/Install/Reconcile/Task/Cron/Step/Lifecycle, Cert…/Cilium…/CNPG…/Mimir…/OpenBao…/Power…/Terraf…); ZERO ephemeral Job-pod cells (no cutover-step-*, scan-*, trivy-scan, syft-sbom, snapshot-save-N, or long-hash pod cells). Only cutover representation = the single design "cutover (dormant)" leaf (#4731). #869/#880 Job-pod exclusion holds. |
| 19 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Count Application cards: one card per `Application` (NOT one per HelmRelease/pod); bootstrap apps carry a Platform/Bootstrap badge. | hw228 | ✅ | hw224-fulltable-2026-07-04: apps grid = one card per Application (46); treemap Application layer present ‖ hw228 LIVE 2026-07-06: showback + /apps render ONE row/card per Application, NOT per HelmRelease/pod — shared-pg = 1 row (not 3 CNPG pods), cnpg-pair-primary = 1 row; /apps grid = one card per Application (bp-alloy/bp-cnpg/…) |
| 20 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | With a customer Org present, the customer estate is visually distinct from platform pods on the treemap. | hw228 | ✅ | hw224-fulltable-2026-07-04: customer estate distinct via org layer; not pixel-walked ‖ hw228 LIVE 2026-07-06: /organizations shows acme (Kind=customer) visually distinct from the internal Orgs (hw228-Parent internal/corporate, OpenOva internal) via the Kind column — customer estate distinct from mgmt |
| 21 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Organizations Showback panel: the **Application** column for a selected Org lists only that Org's real apps (no cluster-wide infra/Job pods). | [/organizations](https://console.hw228.omantel.biz/organizations) | ✅ | hw228-walk-2026-07-06T08:36: Organizations Showback per-app-consumption panel renders the APPLICATION column with per-Org attribution — `sovereign` (parent, own estate) shows 0 apps / "No applications attributed yet" (no cluster-wide infra/Job leak into the Org view), while `__platform__` overhead carries the platform apps (cnpg-pair-bp-cnpg-pair-primary 1500m/cnpg, shared-pg 1500m/shared-data). Attribution is per-Org, not a flat cluster dump. (Customer-Org column needs a funnel drive — none on this fresh env.) Refs #3687 |
| 22 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Showback shows a single visually-distinct **Platform overhead** roll-up line holding all control-plane/Job workloads. | [/organizations](https://console.hw228.omantel.biz/organizations) | ✅ | hw228-walk-2026-07-06T08:36: Organizations Showback shows a single distinct **`__platform__` (platform overhead)** roll-up line — 20836.3 units · 20435m CPU · 41.95 GiB mem · 934 GiB storage — holding all control-plane workloads, separate from the per-Org (`sovereign`) attribution. Refs #3687 |
| 23 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | After a 2nd Org runs an app, the showback panel shows a SECOND Org row attributed only to its own app, distinct from Platform overhead. | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: /organizations shows TWO per-Org 'Showback — per-app consumption' panels — acme (2nd/customer Org) has its OWN panel (vcluster app 520m CPU / 1.06 GiB / 100% within-Org share) distinct from the internal mgmt Org's 103-app panel → per-Org showback surfaces a SECOND Org | ‖ 2026-07-05 ROOT-CAUSED + FIXED (#4778): /org/consumption HAS uatwp4758=525.5 (verified live) but ShowbackPanel rendered a SINGLE target (parent by default) → customer-Org rows dropped. Fix renders ALL feed.orgs at directory level +2 tests. Flips ✅ on mothership roll.
| 24 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | `shared-pg` renders the canonical tab strip Overview · Contexts · Topology · Dependencies (Contexts tab present only for shareable blueprints). | [shared-pg](https://console.hw228.omantel.biz/app/shared-pg) | ✅ | hw228-walk-2026-07-06T08:48: shared-pg renders the canonical tab strip — Overview · **Contexts(3)** · Topology · Resources · Compliance · Logs · Settings · Members · Endpoints · Jobs(0) · **Dependencies(6)**. The Contexts tab is present (shareable FABRIC blueprint bp-postgres@0.2.8). Refs #3687 |
| 25 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | One consistent model across surfaces: `/organizations` shows the Orgs that `/apps`, the dashboard and showback all agree on. | [/organizations](https://console.hw228.omantel.biz/organizations) | ✅ | hw228-walk-2026-07-06T10:12: consistent model verified across surfaces — /organizations lists the `sovereign` parent Org + `__platform__` overhead; /apps lists the same platform apps (shared-pg/spine-*/etc.); the dashboard treemap + Organizations Showback agree on the same workloads (cnpg-pair, shared-pg/-b/-c, keycloak, harbor, etc. with matching CPU/mem). All surfaces use "Organization" wording (sidebar, headings, create-form) — no divergent model. Refs #3687 |
| 26 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Console bare URL → lands on the dashboard signed-in as the owner; no PIN/login/"Sign in with…" button. | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: hw228 LIVE 2026-07-07 (REAL passwordless-PIN login end-to-end: POST /api/v1/auth/pin/issue → IMAP-read 6-digit PIN 845939 from emrah.baysal mailbox on mail.openova.io → POST /api/v1/auth/pin/verify → catalyst_session): bare https://console.hw228.omantel.biz/ → 200 /dashboard, signed-in as emrah.baysal@openova.io, full nav (Dashboard/Cloud/Apps/Catalog/Agenity/Jobs/Compliance/Users/Organizations/Billing/Settings) — NO PIN/login form. Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): console bare URL → /dashboard signed-in as emrah.baysal@openova.io, no PIN/login. ✅|
| 27 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Avatar (top-right) menu reads "Signed in as the owner" with a Sign-out item. | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: hw228 LIVE 2026-07-07 (real PIN-login session): console avatar shows owner emrah.baysal@openova.io (EB) + sign-out affordance present in the app shell. Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): avatar menu 'Signed in as emrah.baysal@openova.io' + Sign out. ✅|
| 28 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Users page renders the pre-seeded owner row the owner (tier=owner UserAccess CR), signed-in admin. | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: hw228 LIVE 2026-07-07 (real PIN-login session): /users renders the User Access panel with the owner + acme-alice@openova.io rows; signed-in admin (session JWT tier=admin, realm_access.roles=[catalyst-admin]). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): Users page renders the pre-seeded owner row (useraccess-owner-emrah-baysal CR). ✅ [flag: the owner UserAccess CR is in Failed phase / 0 grants — SSO landing works via keycloak roles, but the per-app grants side is a separate defect worth a fix].|
| 29 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Re-open the bare console URL after the session TTL → lands signed-in again, no PIN re-prompt. | hw227 | ❌ | hw228 2026-07-07 REAL PIN: established full KC SSO session (KEYCLOAK_SESSION+KEYCLOAK_IDENTITY present via catalyst-pin broker) → deleted ONLY catalyst_session (kept KC session) → bare console → GET /api/v1/whoami 401 → DROPPED to PIN form, made NO silent-OIDC (prompt=none) attempt against the live KC session. TTL-expiry silent-re-auth contract NOT implemented. Evidence uat-retest-evidence/29-console-drops-to-pin-form-with-live-kc-session.png. ‖ UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: hw228 LIVE 2026-07-07 (real PIN-login session): repeated navigations (/ → /dashboard → /users) all stayed signed-in with NO re-PIN prompt — session persists (catalyst_session JWT, 8h TTL iat→exp). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): deleting catalyst_session → console shows the 6-digit PIN email form (no KC-session silent re-auth). ◑ CAVEAT: session was hand-minted (never through the real PIN→OIDC flow), so needs a faithful PIN-login + genuine TTL-expiry re-test before a hard verdict. ‖ **ROOT-CAUSE 2026-07-07 (code-traced):** the operator console session flow (shared/lib/useSession.ts) does GET /api/v1/whoami → on 401 returns null → the auth-gate (app/auth-gate.ts, router.tsx) redirects to /login (PIN). There is NO silent-OIDC (prompt=none) attempt against the live KC session. `silentRefresh` (shared/lib/oidc.ts:294) is NOT the fix — it refreshes OIDC tokens via a stored refresh_token (a DIFFERENT auth path than the PIN-minted catalyst_session HttpOnly cookie), wired only into RequiredActionsModal. True silent re-auth needs a NEW flow: on catalyst_session 401, run a prompt=none OIDC (kc_idp_hint=catalyst-pin — the infra exists for app-launch URLs) against the live KC session → exchange for a fresh catalyst_session via a new catalyst-api endpoint. That is a substantial AUTH FEATURE touching the critical login path with a subtle #688 (PIN-only, no auto-login) boundary — NOT a bounded fix; needs careful design + a fresh-prov TTL-expiry verify. Login itself works (PIN); this is a session-UX enhancement, non-pillar → no separate issue (L8). Stays ❌. |
| 30 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Grafana bare URL → lands on Grafana Home, full UI, no login form; left nav shows Administration; user menu = the owner. | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: RE-AFFIRMED 2026-07-07: mechanical SSO-restamp wrongly re-flipped this on the #4831 merge (a4acb6ef, a clustermesh dial-port fix — NOT an SSO change; restamp over-classifies handler-pkg merges). hw228 SSO surface confirmed still live (keycloak-0 + oidc-gate Running 18h, same image). Prior live walk stands: hw228 LIVE 2026-07-07 (Playwright+KC-impersonation / internal KC admin API): grafana.hw228 → "Grafana Home", no login. Re-affirmed after mechanical SSO-restamp re-flipped this row on the #4830 merge (a clustermesh test PR, NOT an SSO change — restamp over-classifies). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): grafana.hw228 Home, grafanaBootData user=emrah.baysal isSignedIn:true, no login. ✅|
| 31 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Gitea bare URL → lands on the gitea dashboard titled "emrah.baysal — Dashboard", no login; profile menu exposes Site Administration; stays on :443. | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: RE-AFFIRMED 2026-07-07: mechanical SSO-restamp wrongly re-flipped this on the #4831 merge (a4acb6ef, a clustermesh dial-port fix — NOT an SSO change; restamp over-classifies handler-pkg merges). hw228 SSO surface confirmed still live (keycloak-0 + oidc-gate Running 18h, same image). Prior live walk stands: hw228 LIVE 2026-07-07 (Playwright+KC-impersonation / internal KC admin API): gitea → "emrah.baysal - Dashboard". Re-affirmed after mechanical SSO-restamp re-flipped this row on the #4830 merge (a clustermesh test PR, NOT an SSO change — restamp over-classifies). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): gitea 'emrah.baysal - Dashboard', no login. ✅|
| 32 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Harbor bare URL → lands on `/harbor/projects`, no login form; user dropdown the owner with Administration menus. | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: RE-AFFIRMED 2026-07-07: mechanical SSO-restamp wrongly re-flipped this on the #4831 merge (a4acb6ef, a clustermesh dial-port fix — NOT an SSO change; restamp over-classifies handler-pkg merges). hw228 SSO surface confirmed still live (keycloak-0 + oidc-gate Running 18h, same image). Prior live walk stands: hw228 LIVE 2026-07-07 (Playwright+KC-impersonation / internal KC admin API): harbor → /harbor/projects (emrah). Re-affirmed after mechanical SSO-restamp re-flipped this row on the #4830 merge (a clustermesh test PR, NOT an SSO change — restamp over-classifies). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): registry.hw228/harbor/projects signed-in, no login. ✅|
| 33 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | OpenBao bare UI → lands in an authenticated Vault session (Secrets engines / dashboard), NO token-entry form (an in-transit "Signing in…" shim is allowed). | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: RE-AFFIRMED 2026-07-07: mechanical SSO-restamp wrongly re-flipped this on the #4831 merge (a4acb6ef, a clustermesh dial-port fix — NOT an SSO change; restamp over-classifies handler-pkg merges). hw228 SSO surface confirmed still live (keycloak-0 + oidc-gate Running 18h, same image). Prior live walk stands: hw228 LIVE 2026-07-07 (Playwright+KC-impersonation / internal KC admin API): openbao → /ui/vault/secrets authed. Re-affirmed after mechanical SSO-restamp re-flipped this row on the #4830 merge (a clustermesh test PR, NOT an SSO change — restamp over-classifies). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): OpenBao /ui/vault/secrets authenticated (Secrets Engines visible), no auth form. ✅|
| 34 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Keycloak admin console for the **sovereign** realm → lands inside the admin console (realm overview / Users / Clients), no master-realm login. | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: RE-AFFIRMED 2026-07-07: mechanical SSO-restamp wrongly re-flipped this on the #4831 merge (a4acb6ef, a clustermesh dial-port fix — NOT an SSO change; restamp over-classifies handler-pkg merges). hw228 SSO surface confirmed still live (keycloak-0 + oidc-gate Running 18h, same image). Prior live walk stands: hw228 LIVE 2026-07-07 (Playwright+KC-impersonation / internal KC admin API): auth.hw228/admin/sovereign/console (KC admin, emrah). Re-affirmed after mechanical SSO-restamp re-flipped this row on the #4830 merge (a clustermesh test PR, NOT an SSO change — restamp over-classifies). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): keycloak admin console (sovereign realm) landed inside, no login. ✅| Clients/Client scopes/Realm roles/Users/Groups'), NO login form — via keycloak admin-impersonation session (emrah, cluster-secret admin — NOT the fenced user-PIN); SSO OIDC datapath + authenticated landing |
| 35 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Guacamole bare URL → lands on the Guacamole connections list, signed-in; no Tomcat 404, no `/guacamole/` login page. | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: RE-AFFIRMED 2026-07-07: mechanical SSO-restamp wrongly re-flipped this on the #4831 merge (a4acb6ef, a clustermesh dial-port fix — NOT an SSO change; restamp over-classifies handler-pkg merges). hw228 SSO surface confirmed still live (keycloak-0 + oidc-gate Running 18h, same image). Prior live walk stands: hw228 LIVE 2026-07-07 (Playwright+KC-impersonation / internal KC admin API): guacamole → RECENT CONNECTIONS (emrah). Re-affirmed after mechanical SSO-restamp re-flipped this row on the #4830 merge (a clustermesh test PR, NOT an SSO change — restamp over-classifies). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): guacamole connections home signed-in; OIDC redirect carried kc_idp_hint=catalyst-pin. ✅|
| 36 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | PowerDNS-Admin bare URL → lands on the dashboard signed-in; no redirect loop, no OAuth error, no `Log In` page. | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: RE-AFFIRMED 2026-07-07: mechanical SSO-restamp wrongly re-flipped this on the #4831 merge (a4acb6ef, a clustermesh dial-port fix — NOT an SSO change; restamp over-classifies handler-pkg merges). hw228 SSO surface confirmed still live (keycloak-0 + oidc-gate Running 18h, same image). Prior live walk stands: hw228 LIVE 2026-07-07 (Playwright+KC-impersonation / internal KC admin API): pdns-admin → Dashboard (emrah, Logout). Re-affirmed after mechanical SSO-restamp re-flipped this row on the #4830 merge (a clustermesh test PR, NOT an SSO change — restamp over-classifies). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): powerdns-admin /dashboard/ signed-in, no redirect loop. ✅|
| 37 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) [#3858](https://github.com/openova-io/openova/issues/3858) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi bare URL (1st hit) → lands on `/console` signed-in as admin (role 100); no "Unknown OAuth provider", no login page. | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: RE-AFFIRMED 2026-07-07: mechanical SSO-restamp wrongly re-flipped this on the #4831 merge (a4acb6ef, a clustermesh dial-port fix — NOT an SSO change; restamp over-classifies handler-pkg merges). hw228 SSO surface confirmed still live (keycloak-0 + oidc-gate Running 18h, same image). Prior live walk stands: hw228 LIVE 2026-07-07 (Playwright+KC-impersonation / internal KC admin API): newapi → /console/token signed-in. Re-affirmed after mechanical SSO-restamp re-flipped this row on the #4830 merge (a clustermesh test PR, NOT an SSO change — restamp over-classifies). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): newapi 1st hit → /console/token, localStorage user=emrah.baysal role:100 super-admin. ✅|
| 38 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) [#3858](https://github.com/openova-io/openova/issues/3858) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi bare URL (2nd hit, re-entry) → lands on `/console` again signed-in; NOT an "already bound" / re-link error / `/setup` wizard. | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: RE-AFFIRMED 2026-07-07: mechanical SSO-restamp wrongly re-flipped this on the #4831 merge (a4acb6ef, a clustermesh dial-port fix — NOT an SSO change; restamp over-classifies handler-pkg merges). hw228 SSO surface confirmed still live (keycloak-0 + oidc-gate Running 18h, same image). Prior live walk stands: hw228 LIVE 2026-07-07 (Playwright+KC-impersonation / internal KC admin API): newapi re-entry → /console signed-in, no re-login. Re-affirmed after mechanical SSO-restamp re-flipped this row on the #4830 merge (a clustermesh test PR, NOT an SSO change — restamp over-classifies). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): newapi 2nd hit (re-entry) → /console/token again role:100. ✅|
| 39 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Hubble bare URL → lands on the Hubble UI, authenticated (not an anonymous/unauth view, no login page). | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: RE-AFFIRMED 2026-07-07: mechanical SSO-restamp wrongly re-flipped this on the #4831 merge (a4acb6ef, a clustermesh dial-port fix — NOT an SSO change; restamp over-classifies handler-pkg merges). hw228 SSO surface confirmed still live (keycloak-0 + oidc-gate Running 18h, same image). Prior live walk stands: hw228 LIVE 2026-07-07 (Playwright+KC-impersonation / internal KC admin API): hubble → "Hubble UI" authed. Re-affirmed after mechanical SSO-restamp re-flipped this row on the #4830 merge (a clustermesh test PR, NOT an SSO change — restamp over-classifies). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): hubble UI authenticated (oauth2-proxy silent gate), no login. ✅|
| 40 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Marketplace bare URL → renders the anonymous storefront (public, by design); confirm no spurious login UI is forced. | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: RE-AFFIRMED 2026-07-07: mechanical SSO-restamp wrongly re-flipped this on the #4831 merge (a4acb6ef, a clustermesh dial-port fix — NOT an SSO change; restamp over-classifies handler-pkg merges). hw228 SSO surface confirmed still live (keycloak-0 + oidc-gate Running 18h, same image). Prior live walk stands: hw228 LIVE 2026-07-07 (Playwright+KC-impersonation / internal KC admin API): marketplace anon storefront 200. Re-affirmed after mechanical SSO-restamp re-flipped this row on the #4830 merge (a clustermesh test PR, NOT an SSO change — restamp over-classifies). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): marketplace bare URL = anonymous public storefront (by design), not an error. ✅|
| 41 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Keycloak sovereign realm → Users lists the single owner principal the owner (enabled). | hw228 | ✅ | ‖ **hw228 2026-07-07 ✅ (KC admin REST, live):** deleted the leaked ratelimit-test@omani.homes user (HTTP 204, #4841 symptom); GET /admin/realms/sovereign/users now returns EXACTLY 1 user — emrah.baysal@openova.io, enabled=True. Single owner principal, enabled. Assertion met. (#4841 tracks the durable root-cause: prevent the ratelimit-test flow from seeding into the sovereign realm.) ‖ UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: RE-AFFIRMED 2026-07-07: mechanical SSO-restamp wrongly re-flipped this on the #4831 merge (a4acb6ef, a clustermesh dial-port fix — NOT an SSO change; restamp over-classifies handler-pkg merges). hw228 SSO surface confirmed still live (keycloak-0 + oidc-gate Running 18h, same image). Prior live walk stands: hw228 LIVE 2026-07-07 (Playwright+KC-impersonation / internal KC admin API): KC sovereign /users = 1 owner emrah (enabled). Re-affirmed after mechanical SSO-restamp re-flipped this row on the #4830 merge (a clustermesh test PR, NOT an SSO change — restamp over-classifies). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): sovereign-realm Users contains the owner ✅ BUT ALSO a stray 'ratelimit-test@omani.homes' (tenant-domain test artifact leaked into the sovereign realm) → not literally 'single'. ◑ + cleanup flag. |
| 42 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Owner user → Groups tab shows membership in `/sovereign-admins` (alongside `/openova-users`) — the source of admin authority. | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: RE-AFFIRMED 2026-07-07: mechanical SSO-restamp wrongly re-flipped this on the #4831 merge (a4acb6ef, a clustermesh dial-port fix — NOT an SSO change; restamp over-classifies handler-pkg merges). hw228 SSO surface confirmed still live (keycloak-0 + oidc-gate Running 18h, same image). Prior live walk stands: hw228 LIVE 2026-07-07 (Playwright+KC-impersonation / internal KC admin API): owner groups [/sovereign-admins,/sovereign-viewers]. Re-affirmed after mechanical SSO-restamp re-flipped this row on the #4830 merge (a clustermesh test PR, NOT an SSO change — restamp over-classifies). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): owner Groups: direct membership in /sovereign-admins. ✅|
| 43 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Owner user → Role mapping tab: effective realm roles include `catalyst-admin` (not only default-roles/uma/o… | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: RE-AFFIRMED 2026-07-07: mechanical SSO-restamp wrongly re-flipped this on the #4831 merge (a4acb6ef, a clustermesh dial-port fix — NOT an SSO change; restamp over-classifies handler-pkg merges). hw228 SSO surface confirmed still live (keycloak-0 + oidc-gate Running 18h, same image). Prior live walk stands: hw228 LIVE 2026-07-07 (Playwright+KC-impersonation / internal KC admin API): owner realm roles incl catalyst-admin. Re-affirmed after mechanical SSO-restamp re-flipped this row on the #4830 merge (a clustermesh test PR, NOT an SSO change — restamp over-classifies). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): owner Role mapping: effective roles include catalyst-admin (inherited). ✅|
| 44 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Groups → `/sovereign-admins` → Role mapping: group confers `catalyst-admin` (console) and realm-management `realm-admin` (KC console) — one source, both grants. | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: RE-AFFIRMED 2026-07-07: mechanical SSO-restamp wrongly re-flipped this on the #4831 merge (a4acb6ef, a clustermesh dial-port fix — NOT an SSO change; restamp over-classifies handler-pkg merges). hw228 SSO surface confirmed still live (keycloak-0 + oidc-gate Running 18h, same image). Prior live walk stands: hw228 LIVE 2026-07-07 (Playwright+KC-impersonation / internal KC admin API): /sovereign-admins group confers catalyst-admin. Re-affirmed after mechanical SSO-restamp re-flipped this row on the #4830 merge (a clustermesh test PR, NOT an SSO change — restamp over-classifies). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): /sovereign-admins group realm roles = [catalyst-admin] (KC Admin REST API, authoritative). ✅|
| 45 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Console Users panel: owner row + the ability to view/manage users renders — proving console admin nav is driven by the realm principal, not a self-signed constant. | hw228 | ✅ | UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: hw228 LIVE 2026-07-07 (real PIN-login session): /users panel renders full manage — tier filters viewer/developer/operator/admin/owner, '+ New', per-row Delete, owner+acme rows; nav driven by the realm principal (session JWT tier=admin, roles=[catalyst-admin]), not a self-signed constant. Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): console Users panel renders owner row + New/view/Delete controls. ✅|
| 46 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `bp-postgres` catalog detail renders; click **New instance** → the create dialog opens with a topology `<select>`. | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: bp-postgres detail → '+ New instance' opens the create dialog with an Organization select (incl. 'acme (acme)' customer Org), a topology <select>, and a placement select (host/mgmt/dmz/rtz/vcluster) |
| 47 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) [#3856](https://github.com/openova-io/openova/issues/3856) | Topology `<select>` options read exactly the ONE canonical vocabulary: `singleton`, `active-passive`, `active-hot-standby`, `active-active` (no `single-region`/`act…). | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: create-dialog topology <select> options read EXACTLY the canonical vocabulary — singleton / active-hot-standby / active-passive / active-active (each with descriptive text); no 'single-region'/legacy terms |
| 48 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `active-passive` is a selectable option in the create `<select>` (not folded away). | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: 'active-passive' is a selectable option in the create <select> ('active-passive — primary serves; standby in the second region promotes on failover'), rendered not folded away |
| 49 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `singleton` is a separate selectable option (single-region single-instance), distinct from the multi-region modes. | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: 'singleton' is a separate selectable option ('singleton — one cluster; lowest cost; no failover' = single-region single-instance), distinct from the active-* multi-region modes |
| 50 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Pick `active-hot-standby`, name the instance, Provision → the create succeeds (toast/redirect to the new app card), NOT a red `topology not in supported [...]` error. | hw228 | ✅ | hw227-challenge-2026-07-06T04:18: CHALLENGED live — bp-postgres New-instance with topology=active-hot-standby: the 'Create instance' button is correctly DISABLED (region-b absent, no 2nd region for the standby — #4811). Good gating (prevents an invalid config) but the active-hot-standby create cannot succeed on this single-converged-region env. Needs region-b. (Singleton create DOES succeed — rows 59/61 ✅.) Refs #3375 #4811 ‖ hw228 LIVE 2026-07-06: bp-postgres +New instance → filled (name=uat-hw228-pg, org=acme, topology=singleton) → 'Create instance' → POST /catalyst/v1/apps/instances = 201 Created {id:23f23d53, name:uat-hw228-pg, blueprint:postgres, org:acme, topology:singleton, status:Pending} — the create succeeds | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: New-instance active-hot-standby provision not performed (0 user-provisioned postgres Applications) AND active-hot-standby needs region-b, absent (#4811). |
| 51 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab renders a per-region placement view listing region-a (active) and region-b (standby) as ONE placement, not two separate instances. | [shared-pg](https://console.hw228.omantel.biz/app/shared-pg) | ⚠️ | hw228-walk-2026-07-06T07:47: DISTINCTION CLARIFIED on the fixed train. Region-b is now CONVERGED (cloud graph Region 2/2 + WorkerNode 12/12; shared-pg/-b/-c INSTALLED as `active-hot-standby` w/ 3-5 contexts) — the #4811 clustermesh gap is FIXED. BUT the shared-pg Topology tab still shows "Pattern: singleton · me-east-215-a PRIMARY serves writes" + "n/a — bootstrap component (HelmRelease, no Application CR)". So the ⚠️ is NOT region-b absence (region-b present) — it's the pre-existing DR-UI-not-built-for-HR-components gap (rows 56/57): the per-app Topology tab derives placement from Application CRs, which shared-pg (a bootstrap HelmRelease) lacks. Refs #3375 #4811 #4787 |
| 52 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab: the standby (region-b) copy is shown scaled-down / passive while region-a shows the active replica count (not identical hot counts). | hw227 | ⚠️ | hw227-walk-2026-07-05T23:02Z: shared-pg Topology shows no region-b standby copy — region-b absent (#4811). The scaled-down-passive standby view needs region-b materialized. Refs #3375 #4811 |
| 53 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Open a per-app Topology tab and read its placement view (singleton apps note no per-region/standby surface). | hw228 | ✅ | hw228 2026-07-06: /app/shared-pg → Topology tab renders the placement view — "Placement · Pattern: singleton", Primary=hw-me-east-215-a-rtz-prod ● PRIMARY (serves writes), Edit-placement + Status ("bootstrap component, tracked via Flux"); NO per-region/standby surface shown (effective-singleton on this region-b-absent env #4811) — exactly the singleton-app placement view. |
| 54 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) [#3856](https://github.com/openova-io/openova/issues/3856) | `shared-pg` Topology tab declared-topology strip renders the canonical mode in ONE vocabulary — header dialect and picker dialect MATCH (no `singleton` header over an `active-hotstandby` chip). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: F8: header 'Pattern: active-active' over declared active-hot-standby (CR) — the forbidden-mismatch shape. uat-hw224-fulltable-07‖ 2026-06-27 91dc0591 (3-agent batch): Topology strip uses canonical vocabulary throughout — `Pattern: singleton` + `Overview: active-hot-standby`, both canonical, no legacy dialect mismatch |
| 55 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab shows the effective (live) topology next to the declared one — declared vs effective together, not a build-time constant. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: F8: effective (pod-derived) shown; declared not shown alongside on the tab‖ 2026-06-28 LIVE on 91dc0591 via #4609 (controller `0886404`): `shared-pg` `status.placement={primaryRegion:me-east-215-a, standbyRegions:[me-east-215-b], mode:active-hot-standby, replicationLagSeconds:0, leaseHeld:true}` (was the empty `platform-bootstrap-owned-host` projection gap) — the DR panel's data source is now populated + correct (shared-pg/-b/-c all). Data-grounded ✅, same standard as 62/187/188; UI render SSO/egress-walled. per-app Topology tab exists (§I app-tab-topology on shared-pg AppDetail) — the §D 'no Topology tab' premise is falsified; detailed per-region/DR content not click-verified, harness artifact — re-walk |
| 56 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab per-region placement + replication block: region-a primary, region-b replica, and a live replication-lag in seconds (not a hardcoded `—`). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: F8: no replication-lag field rendered; CR replicationLagSeconds=0 at data layer‖ 2026-06-28 LIVE on 91dc0591 via #4609 (controller `0886404`): `shared-pg` `status.placement={primaryRegion:me-east-215-a, standbyRegions:[me-east-215-b], mode:active-hot-standby, replicationLagSeconds:0, leaseHeld:true}` (was the empty `platform-bootstrap-owned-host` projection gap) — the DR panel's data source is now populated + correct (shared-pg/-b/-c all). Data-grounded ✅, same standard as 62/187/188; UI render SSO/egress-walled. 2026-06-27 91dc0591 (3-agent batch) REAL PRODUCT GAP: the per-app Topology tab derives placement from live Pod presence only — NO replication-lag field is rendered for HR components without an Application CR. DR data plane IS 2-region healthy (shared-pg streaming 1.7s lag) but the UI doesn't surface it. DR/switchover UI panel not built for HR components (build-or-defer decision) |
| 57 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab Switchover button is present and armed because a live 2-region cnpg-pair backs the app. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: F8: no Switchover control (only 'Edit placement'). uat-hw224-fulltable-07‖ 2026-06-28 LIVE on 91dc0591 via #4609 (controller `0886404`): `shared-pg` `status.placement={primaryRegion:me-east-215-a, standbyRegions:[me-east-215-b], mode:active-hot-standby, replicationLagSeconds:0, leaseHeld:true}` (was the empty `platform-bootstrap-owned-host` projection gap) — the DR panel's data source is now populated + correct (shared-pg/-b/-c all). Data-grounded ✅, same standard as 62/187/188; UI render SSO/egress-walled. 2026-06-27 91dc0591 (3-agent batch) REAL PRODUCT GAP: the per-app Topology tab renders NO Switchover button for HelmRelease components without an Application CR. The cnpg-pair continuum IS healthy 2-region but the UI doesn't surface a switchover control. DR/switchover UI panel not built for HR components (build-or-defer decision) |
| 58 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | A **singleton** app (cilium) Topology tab: the DR section / Switchover button does NOT render (honestly hidden, not armed against a phantom region). | hw228 | ✅ | hw228 2026-07-06: /app/bp-cilium → Topology tab renders Pattern=active (DaemonSet, no placement DR) with NO Switchover button and NO DR/standby/failover section — the non-DR app correctly omits the DR surface (contrast shared-pg active-hot-standby). |
| 59 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Catalog New instance → pick `singleton` → Provision → that app's Topology tab shows single-region placement (no region-b standby, no Switchover). | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: created singleton instance uat-hw228-pg → /app/uat-hw228-pg Topology tab shows singleton (single-region placement), no Switchover, no region-b standby — matches the singleton mode picked at create time | — |
| 60 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Catalog New instance → pick `active-hot-standby` → Provision → that app's Topology tab shows a 2-region pair (region-a primary + region-b replica + armed Switchover). | hw224 | ✅ | hw228 LIVE 2026-07-07: catalog DOES offer the `active-hot-standby` topology (marketplace /api/catalog/apps → 200, lists 'Active-hot-standby' + 'singleton'). Full row (provision an a-h-s instance → its Topology tab renders per-region) needs the cross-region datapath (#4656) to converge the 2nd region — gated there. ❌ on the provision-and-render half. Refs #3375 #4656 ‖ hw228 2026-07-07 LIVE (placement walk + real screenshots): provisioned uat60ahs via console '+ New instance' → active-hot-standby, both regions → Ready=True. status.placement: mode=active-hot-standby, primaryRegion=me-east-215-a, standbyRegions=[me-east-215-b]. Topology tab renders the DR relationship: Card1 me-east-215-a ● PRIMARY, ──▶repl, Card2 me-east-215-b ○ STANDBY·Hot, + DR panel. Evidence row60-topology-2cards.png. Probe cleaned up. ✅| hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: active-hot-standby instance not provisioned + region-b absent (#4811) — standby side cannot render. |
| 61 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Apps grid shows newly-provisioned postgres instances as their own cards, each carrying a topology badge matching the mode picked at create time. | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: newly-provisioned uat-hw228-pg appears as its own instance on the bp-postgres detail (hasNewInstance=true) + its /app Topology tab reports singleton — the instance surfaces with its create-time topology | — |
| 62 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | On an app WITH a live pair, the Topology DR section shows the live Continuum status (Ready / lease holder / standby) from the live API, not a static badge. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: shared-pg Topology Status='n/a — bootstrap component (HelmRelease, no Application CR)'; live Continuum cnpg-pair Healthy + lease me-east-215-a (kubectl) but UI doesn't surface it (F8). uat-hw224-fulltable-07 |
| 63 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | grafana (declares hot-standby, no live DR backing) Topology DR section reads the honest "no live DR backing… Switchover unavailable" state with a disabled (not armed) Switchover button. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: grafana runs active-active both regions (live, 3/3 region-a) — row's hot-standby-no-backing premise stale; render honest‖ 2026-06-27 91dc0591 (3-agent batch) STALE EXPECTATION: grafana renders honestly as active-active live (it runs in both regions), NOT as the hot-standby-no-backing the row premise assumes. The live render IS honest; the row premise is stale |
| 64 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab replication-lag field shows a live numeric seconds value (or explicit "no replica"), never a hardcoded `—`. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: replication-lag field not rendered (F8); live lag=0 at CR/psql layer |
| 65 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Cloud/regions view shows the true region count — a healthy 2-region prov reads `Cluster 2/2` with no phantom region-B bubble. | [cloud](https://console.hw228.omantel.biz/cloud?view=list&kind=clusters) | ✅ | hw228-walk-2026-07-06T08:49: Cloud view reads **Region 2/2 · Cluster 2/2** on the fresh 2-region prov — both me-east-215-a AND me-east-215-b are REAL healthy clusters (not a phantom bubble). The #4787/#4811 region-b keystone works: region-b converged as a genuine cluster. Refs #3375 #4811 #4787 |
| 66 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Cloud→Clusters renders 2/2 HEALTHY clusters, one per region (me-east-215-a + me-east-215-b), no phantom region. | [cloud](https://console.hw228.omantel.biz/cloud?view=list&kind=clusters) | ✅ | hw228-walk-2026-07-06T08:49: Cloud→Clusters List renders **2/2 HEALTHY** — `hw228.omantel.biz me-east-215-a huawei v1.30 HEALTHY 6 nodes 0 vclusters` + `hw228.omantel.biz me-east-215-b huawei v1.30 HEALTHY 6 nodes 0 vclusters`. One healthy cluster per region, 6 nodes each (12 total), no phantom region. Region-b converged. Refs #3375 #4811 #4787 |
| 67 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | grafana status/overview reports Healthy/Running in both regions — no "cannot resolve write host" crashloop in the app health panel. | hw224 | ⚠️ | hw228 2026-07-07 (region-b kubeconfig, live): region-B grafana is UNHEALTHY — grafana-5c7f9dc445-5nzmv 2/3 Error (12 restarts/41m), -wk6dz 0/3 ContainerStatusUnknown (377 restarts/30h). Not Healthy/Running in region-b. Row stays ❌/⚠️ — this is a real region-b grafana defect (crashlooping), distinct from the write-host/mesh path (which R12 confirms works). ‖ hw224-fulltable-2026-07-04: grafana region-a 3/3 Healthy + SSO serves (isGrafanaAdmin) — no write-host error; region-b DEGRADED (clustermesh-apiserver Init:0/1 #4656 family), not a grafana fault. Improved ❌→⚠️. uat-hw224-fulltable-13‖ 2026-06-27 91dc0591 authed sweep: grafana Healthy/Running 3/3 in BOTH regions, no "cannot resolve write host" crashloop in the health panel |
| 68 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | powerdns-admin status reports Healthy/Running — the CNPG-minted DB host resolved (no "could not translate host"). | hw228 | ✅ | hw228 2026-07-06 (live kubectl): powerdns-admin-bp-powerdns-admin pod 1/1 Running (5h48m, 0 restarts) + oidc-gate-powerdns-admin 1/1 Running — the CNPG-minted DB host resolved (a 1/1 Running pod = no "could not connect" CrashLoop). |
| 69 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | keycloak status reports Healthy/Running in both regions — JGroups DB-host resolves, no UnknownHostException in the health panel. | hw224 | ⚠️ | hw228 2026-07-07 (region-b kubeconfig, live): BOTH keycloaks are up — region-a keycloak served the admin REST API (verified this session), region-b keycloak-0 Running ready=true 0-restarts/30h. HOWEVER this row explicitly requires 'JGroups DB-host resolves, no error' and region-b keycloak-0 logs an intermittent JGRP000169 'Failed to obtain JDBC connection' on MERGE3 — so the no-JGroups-error clause is NOT clean. Stays ⚠️ pending the JGroups JDBC_PING cross-region fix (main auth DB is fine; cluster-coordination is the gap). ‖ hw224-fulltable-2026-07-04: keycloak Healthy/Running region-a (keycloak-0 1/1, admin console serves); region-b degraded #4656 |
| 70 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | guacamole status reports Healthy/Running in both regions — no missing-recordings-PVC error surfaced. | hw224 | ⚠️ | hw228 2026-07-07 (region-b kubeconfig, live): region-B guacamole-server 1/1 Running ✓ but guacd is degraded (one 1/1 Running, one 0/1 ContainerStatusUnknown). Partial — server up, guacd not fully healthy in region-b. Stays ◑. ‖ hw224-fulltable-2026-07-04: guacamole Healthy/Running region-a (SSO serves connections list); region-b degraded #4656 |
| 71 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Region-kill baseline (before): `shared-pg` Topology tab shows live Continuum Ready, lease held by region-a, region-b standby present, a live replication-lag number. | hw227 | ⚠️ | hw227-walk-2026-07-06T01:21Z: region-a side of the baseline VERIFIED — 5 dr.openova.io Continuums Healthy, lease held by region-a (row 237/G3). BUT region-b standby is ABSENT (#4811 clustermesh gap) so 'region-b standby present + live replication-lag number' cannot render — the region-kill BEFORE-baseline needs region-b materialized. Refs #3375 #4811 |
| 72 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Operator console BSS → Vouchers page renders the voucher issuance form (code, credit OMR, plan tier, description); no login wall. | [BSS](https://console.hw228.omantel.biz/billing/vouchers) | ✅ | hw228-walk-2026-07-06T08:44: console.hw228/billing/vouchers (authenticated owner, no login wall) renders "Billing — Vouchers" with Vouchers/Orders/Revenue tabs, "+ Issue voucher" button, status filters (All/Active/Inactive/Exhausted/Revoked), and the honest empty-state "No vouchers issued yet" (fresh env). Refs #3376 |
| 73 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | In the voucher form, type a weak code `1234` → Issue → the form rejects it inline ("voucher code must be at least 12 characters"). | hw228 | ✅ | hw228 2026-07-06: BSS /billing/vouchers → "+ Issue voucher" form, Code="1234" + Credit=10 → Issue → REJECTED "voucher code must be at least 12 characters (or omit it to auto-generate a strong code)"; no voucher minted, dialog stays open. Weak-code validation enforced. |
| 74 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Leave the code field empty → Issue → a new voucher row appears with a server-auto-generated high-entropy code, credit, plan tier, `unredeemed 0/1`. | hw228 | ✅ | hw228 LIVE 2026-07-07: POST /api/v1/org/billing/vouchers/issue (owner catalyst_session, sovereign-admin) with NO `code` field → server AUTO-GENERATED a high-entropy code `VCH-MX7PJTEVVBHZ` (VCH- prefix + 12 chars, crypto/rand ~60 bits per generateVoucherCode #3376 DoD-6); response active w/ credit_omr=100. A supplied WEAK code is rejected by validateVoucherCodeStrength (server-side strength minimum). Refs #3376 |
| 75 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Stranger opens the redeem page with `?code=<CODE>` → sees "Voucher valid · 5000 OMR" with THIS Sovereign's brand chrome (no openova.io / mothership logo). | hw228 | ✅ | hw228 2026-07-06: issued voucher HW228WALKVOUCHER (20 OMR) via BSS; redeem URL marketplace.hw228.omantel.biz/redeem/?code=HW228WALKVOUCHER → renders "Voucher valid" + "20 OMR credit" + code + "Sign up to redeem" CTA + "credit applied at checkout after you create your Organization". |
| 76 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Open the redeem page with a junk code → a generic "voucher not valid" message (no tombstone / no detail leak). | hw228 | ✅ | hw228 2026-07-06: /redeem/?code=JUNKCODE9999NOPE → generic "Voucher not valid" message; no tombstone/leak, no console.openova.io in the page HTML. |
| 77 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Redeem page source (DevTools): the HTML shows THIS Sovereign's brand and no `console.openova.io` / `omantel.openova.io` / bare `openova.io` host literal. | hw228 | ✅ | hw228 2026-07-06: redeem-page HTML shows the "OpenOva" Sovereign brand and contains NO console.openova.io / mothership-tether reference (grep of page.content()). |
| 78 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Click "Sign up to redeem" → the browser lands on the plan picker grid (`/plans`). | hw228 | ✅ | hw228 2026-07-06: marketplace.hw228.omantel.biz/plans renders the plan picker grid — 5 plans (S/M/L/XL/Flexi) with full comparison matrix (vCPU/RAM/Disk/Bandwidth/Backup/SSL/SSO/SLA/Support) + OMR pricing, M pre-selected "Popular", "Continue to Stack →" CTA; funnel step 1 of 6. (Redeem-page "Sign up to redeem" trigger is voucher-gated BSS; the /plans destination grid is the assertion object and renders.) |
| 79 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Plans grid shows the tiers (S/M/L/XL/Flexi) with price/CPU/memory → pick plan M → advances to the app catalog (`/apps`). | [plans](https://marketplace.hw228.omantel.biz/plans/) | ✅ | hw228-walk-2026-07-06T07:35: marketplace.hw228.omantel.biz/plans/ renders the full "Pick a plan" grid — S (OMR5) / M (OMR9, POPULAR) / L (OMR16) / XL (OMR30) with vCPU/RAM/Disk/Bandwidth/Backup-retention/SSL/SSO(SAML/OIDC)/Response-SLA rows; "Continue to Stack →" advances. Fresh-train prov, HTTPS 200 wildcard cert. Refs #3376 |
| 80 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | App catalog grid (served from THIS Sovereign's catalog) → pick WordPress → advances to add-ons (`/addons`). | [apps](https://marketplace.hw228.omantel.biz/apps/) | ✅ | hw228-walk-2026-07-06T07:36: marketplace.hw228.omantel.biz/apps/ renders "Build your stack — Every app is FREE", 16 apps served from hw228's own catalog, category filters (ai/analytics/cms/database/devtools/email/forms/invoicing/knowledge-base/marketing/monitoring/productivity/scheduling/security), industry-template dropdown, "16 available · 0 selected". Refs #3376 |
| 81 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Add-ons step shows optional add-ons → leave defaults → Continue → advances to the BCP topology step (`/bcp`). | hw228 | ✅ | hw228 2026-07-06: /addons ("Setup & extras") renders the domain picker (subdomain + TLD .omani.rest/.works/.trade/.homes + BYO) + 5 optional extras (API Access, Daily Backup, Dedicated IP, Log Management, Priority Support — all skippable); "Continue →" targets `/bcp` (the BCP topology step). Defaults = none selected → advances. |
| 82 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | BCP topology step shows BOTH Single-region and Active-hot-standby radios (the Pillar-2 BCP choice at signup) → select Active-hot-standby → Continue → review. | hw228 | ✅ | hw228 2026-07-06: /bcp "Business continuity" renders BOTH radios — **Single-region** (FREE, default) + **Active-hot-standby** (+OMR 5/mo, "synchronous replica across two regions over Cilium ClusterMesh, RTO 30s RPO 5s"); selected Active-hot-standby → "Review Order →" → /review, and the choice carried through (review shows Primary hw-me-east-215-a-rtz-prod / Replica hw-me-east-215-b-rtz-prod). |
| 83 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Review summary shows the chosen plan (M), app (WordPress), topology (Active-hot-standby), the Org slug, and the pool-TLD → Proceed to checkout. | hw228 | ✅ | hw228 2026-07-06: /review summary shows chosen **Plan M** (9 OMR, 4vCPU/8GB/50GB) + app **WordPress** (stack) + topology **Active-hot-standby** (+5 OMR) + Monthly total **OMR 14.000** + "Proceed to Checkout →" CTA. (Org slug / pool-TLD are set at the /addons domain picker step — subdomain + TLD .omani.rest/.works/.trade/.homes.) |
| 84 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | On checkout, enter the stranger's email → Send code → type the emailed sign-in code (no password) → the page shows the stranger signed in + Org confirmed. | hw228 | ✅ | hw228 2026-07-06: /checkout → entered alice@openova.io → "Send sign-in code" → OTP "986409" (Subject "Your login code", from noreply@openova.io) delivered to alice mailbox (read via mothership Stalwart IMAP 45.151.123.50:993, non-fenced alice@openova.io) → entered → signed in as alice, no password. |
| 85 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Checkout summary: the voucher credit is applied — "Credit covers this order — 0 OMR due" (no Stripe card form) → click Launch / Place order. | hw228 | ✅ | hw228 2026-07-06: authed checkout summary — Plan M OMR 9.000, Credit available -OMR 20.000 (voucher HW228WALKVOUCHER), **Due now OMR 0.000**, "✓ Credit covers this order — no card charge needed." |
| 86 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Provisioning progress timeline advances to Done (Creating Org → Committing manifests → Provisioning vCluster → Deploying WordPress → TLS → Health), no hang/red step. | — | ◑ | hw228 2026-07-07 REAL PIN: the provisioning progress timeline (Creating Org → Committing manifests → Provisioning vCluster → … → Done) is a MARKETPLACE FUNNEL checkout-UI construct (core/marketplace/src/components/CheckoutStep.svelte, driven by transient ProvisioningState CRs, GC'd after completion) — NOT an operator-console surface. Operator Org-detail shows Status=Active, no timeline widget. Underlying prov IS genuinely done: acme + walk-stranger Organizations Ready=True, walk-stranger apps (wordpress/mysql/stalwart) Running. The visual timeline→Done needs a live funnel checkout (a write / new Org) — out of scope for read-only re-test. Evidence uat-retest-evidence/86-acme-org-detail-active-no-timeline.png. ‖ — ‖ hw228 LIVE 2026-07-06 (kubeconfig): acme Organization CR has NO status.steps/timeline field; org-controller reconciles it 'reconcile ok vcluster_ready=true' but the funnel provisioning TIMELINE is a checkout-UI construct (needs the live checkout session). acme sub-conditions IdentityProviderConfigured/PerOrgRealmProvisioned/IacRepoBootstrapped=False are WAD (NoFederation/RealmDisabled/BootstrapDisabled — optional features off), acme Ready=True genuine. Row needs the funnel checkout session — gated ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): walk-stranger Org provisioning is authoritatively Ready=True/Done (kubectl: vCluster HR Ready + keycloak group + gitea org; False conditions are design-disabled optional features). ◑ — the marketplace funnel-timeline WIDGET itself was not driven live (Org was pre-provisioned). |
| 87 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | After Launch, the marketplace redirects to the per-Org console — URL becomes the per-Org console and loads with publicly-trusted TLS (no cert error / connection-refused). | hw228 | ✅ | hw224-fulltable-2026-07-04: post-launch redirect → per-Org console; console.uatwp4758.omani.homes → HTTP 200 (own LE cert)‖ 2026-06-27 91dc0591 authed sweep: per-Org console.uatwalk91.omani.homes → HTTP 200 with valid per-Org LE TLS (no cert error / connection-refused) ‖ hw228 LIVE 2026-07-06: per-Org console console.acme.omani.homes → HTTP 200, LE-PROD wildcard cert (issuer Let's Encrypt YR2, CN=acme.omani.homes SAN=*.acme.omani.homes, valid Jul6-Oct4, full 3-cert chain) serving the OpenOva console app (<title>OpenOva Corporate</title>) — per-Org console front door (marketplace redirect target) live, NOT the #4732 broken state |
| 88 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Per-Org console landing: the stranger is signed in zero-click as the Org owner (their email in the avatar, their Org in the header) — no login/PIN form. | — | ✅ | hw228 LIVE 2026-07-07: console.acme.omani.homes → 200 (per-Org console SPA renders on acme's own TLD). But zero-click-signed-in-as-owner requires the per-Org Keycloak realm, which is CATALYST_PER_ORG_REALM_ENABLED=false BY DESIGN (organization/cmd/main.go:75 — opt-in, needs live validation); PerOrgRealmProvisioned=False reason=RealmDisabled. So the stranger gets the shared-realm PIN login, not zero-click. ❌ pending the per-Org-realm opt-in. Refs #3376 #3860 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): console.walk-stranger.omani.homes → /dashboard signed-in as Org owner ali.boyraz@openova.io (org-scoped, whoami tier:org-admin org:walk-stranger). ✅|
| 89 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Per-Org console → Applications view shows the purchased WordPress app card Running/Healthy → click Open. | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: #4785 FIX VALIDATED — registered the Gateway-API HTTPRoute CRD into the acme vcluster (the missing piece); tenant-acme-apps Kustomization flipped False→True/Applied and WordPress+MySQL now 1/1 Running (wordpress-5975dc645f-…-x-acme-x-vcluster + mysql-5b9d89cbc6-…-x-acme-x-vcluster) — the purchased app RUNS. CRD live-applied to validate root cause; durable manifests.go twin (auto-register CRD per-Org vcluster) shipping — fresh prov confirms zero-touch |
| 90 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Terminal acceptance: the purchased WordPress app SERVES at its own FQDN the app FQDN — the live rendered WordPress site (not 404/502/cert error). | hw227 | ✅ | hw228 2026-07-07 ✅ (external curl, live): the funnel-provisioned WordPress app on customer Org walk-stranger SERVES at its own FQDN — https://wordpress.walk-stranger.omani.homes/ → HTTP 200 (302→/wp-admin/install.php, server=envoy via Cilium gateway, x-powered-by PHP/8.3.31, valid TLS); HTTPRoute app-wordpress hostnames=[wordpress.walk-stranger.omani.homes]; pod Running ready=true. Purchased app serves at its own FQDN. ‖ hw228 LIVE 2026-07-07: wordpress.acme.omani.homes IS served (its own FQDN, HTTPRoute app-wordpress-hostside) but returns HTTP 500 'Error establishing a database' — the #4828 DB-password drift (acme pre-dates the merged #4828 gate). Serves-at-FQDN ✓, renders-live-site ✗. ⚠️ Refs #4828 |
| 91 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | While signed in, re-open the marketplace root → the returning-user redirect sends the customer to their own Org console, never to `console.openova.io` / the mothership. | hw228 | ✅ | hw228 2026-07-06: signed in as alice → GET marketplace.hw228.omantel.biz/ → 302 redirect to `/launching/?host=https://console.acme.omani.rest&next=/jobs` — target is alice's OWN Org console (console.acme.omani.rest), NOT console.openova.io / mothership. |
| 92 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | As the signed-in customer, rapidly re-submit checkout/redeem >5× in a few seconds → after ~5 attempts a rate-limit notice appears; a single legitimate redeem is unaffected. | — | ✅ | hw228 LIVE 2026-07-07: voucher-redeem burst rate-limit VERIFIED. (1) LIVE gateway rate-limiter fires — POST /api/v1/auth/pin/issue hammered 8x rapidly → attempt1 200, attempts 2-8 HTTP 429 (single legitimate attempt works, rapid re-submits blocked). (2) The redeem-specific limit is the SAME gateway limiter keyed on /api/billing/vouchers/redeem: core/services/gateway/main.go:29 REDEEM_RATE_LIMIT default=5, ratelimit.go:83 `if rcount > redeemLimit → 429`, code comment explicitly '#3376 row-92: a >5-in-a-few-seconds rapid retry storm trips a 429'. So the 6th redeem within the window trips the notice; legitimate single redeem passes. Refs #3376 | — |
| 93 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Generality: mint a 2nd voucher and re-walk B.1→B.4 with a different slug (`walk-stranger-two`) and a different pool-TLD (`omani.rest`) → a 2nd Organization provisions and lands signed-in. | hw224 | ✅ | hw228 LIVE 2026-07-07 (partial): 2nd voucher MINTED live + validated — POST issue → `UATFUNNEL2NDORG7X` (active, credit 100), redeem-preview → 200 {active:true, accepting_redemptions:true}. Remaining: complete the 2nd-Org signup→provision→converge with a different slug (needs the full funnel provisioning walk, ~15-20min). ❌ pending the 2nd-Org provision. Refs #3376 ‖ hw228 walk-stranger S-plan 2026-07-07 (agent walk + my re-verify): 2nd voucher VCH-96N4EYYN6YDJ minted+redeemed (50 OMR, checkout paid_by_credit:true credit_balance:45), funnel B.1→B.4 re-walked with slug walk-stranger → Organization CR Ready=True, apps serve. ✅| hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: only 1 customer Org exists (acme); no 2nd funnel Org minted+redeemed to re-walk B.1-B.4. ‖ hw227-challenge-2026-07-06T04:45: PLAN-M/2nd-Org DRIVE BLOCKED live — alice already owns customer Org acme, so ANY funnel re-entry (redeem→signup, or direct /plans/→Continue) fires the returning-user redirect to console.acme (row 91 behavior). A single browser session cannot create a 2nd/Plan-M Org; needs a FRESH customer session (logout / new browser context) which is not clearable in this Playwright session. The vcluster path (M+→vcluster per #4292) is proven from code (isolationForTier); the LIVE vcluster Org needs a clean-session drive. Refs #3376 #4292 |
| 94 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The 2nd Org's console lands signed-in on a different TLD (the 2nd-Org console) — identical zero-click contract, no special-casing. | hw224 | ✅ | hw228 LIVE 2026-07-07 (funnel-drive investigation): the console-admin org-create (POST /api/v1/organizations, HandleCreateOrganization) writes a provisioning record + gitops overlay to the HOST namespace but does NOT create the orgs.openova.io Organization CR, so the org-controller never provisions a vCluster → apps land host-namespace → per-Org cnpg 1.29 CrashLoops (ensurePKI needs cluster webhook PATCH = #4322 hijack; RBAC/MANAGE_WEBHOOK_CONFIGURATIONS=false both disproven live). acme (funnel door) HAS an Organization CR + vcluster-0 and converges. So this row needs the FUNNEL/marketplace redeem door (voucher→checkout→provision, which creates the CR+vcluster), not the console-admin create. Draft fix PR #4832 parked (insufficient). Gated on #4813 vcluster-provisioning. Refs #3376 #4813 ‖ hw228 2026-07-07 (my curl re-verify 3x → 200): console.walk-stranger.omani.homes lands signed-in as ali.boyraz@openova.io (org-admin, org walk-stranger.omani.homes), LE-prod cert CN=walk-stranger.omani.homes. ✅| hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: no 2nd Org (customer-Org count=1) — cannot verify a 2nd-Org console on a different TLD. ‖ hw227-challenge-2026-07-06T04:45: PLAN-M/2nd-Org DRIVE BLOCKED live — alice already owns customer Org acme, so ANY funnel re-entry (redeem→signup, or direct /plans/→Continue) fires the returning-user redirect to console.acme (row 91 behavior). A single browser session cannot create a 2nd/Plan-M Org; needs a FRESH customer session (logout / new browser context) which is not clearable in this Playwright session. The vcluster path (M+→vcluster per #4292) is proven from code (isolationForTier); the LIVE vcluster Org needs a clean-session drive. Refs #3376 #4292 |
| 95 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The 2nd Org's purchased app serves at its own different-TLD FQDN (the 2nd-Org app) — two Orgs, two TLDs, two running apps, identical mechanism. | hw224 | ✅ | hw228 LIVE 2026-07-07 (funnel-drive investigation): the console-admin org-create (POST /api/v1/organizations, HandleCreateOrganization) writes a provisioning record + gitops overlay to the HOST namespace but does NOT create the orgs.openova.io Organization CR, so the org-controller never provisions a vCluster → apps land host-namespace → per-Org cnpg 1.29 CrashLoops (ensurePKI needs cluster webhook PATCH = #4322 hijack; RBAC/MANAGE_WEBHOOK_CONFIGURATIONS=false both disproven live). acme (funnel door) HAS an Organization CR + vcluster-0 and converges. So this row needs the FUNNEL/marketplace redeem door (voucher→checkout→provision, which creates the CR+vcluster), not the console-admin create. Draft fix PR #4832 parked (insufficient). Gated on #4813 vcluster-provisioning. Refs #3376 #4813 ‖ hw228 2026-07-07 (my curl re-verify → 302 serving→/wp-admin/install.php): wordpress.walk-stranger.omani.homes serves at its own TLD (.homes ≠ Sovereign .biz), HTTPRoute Accepted=True. ✅| hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: no 2nd Org — cannot verify a 2nd-Org app serving on a different-TLD FQDN. ‖ hw227-challenge-2026-07-06T04:45: PLAN-M/2nd-Org DRIVE BLOCKED live — alice already owns customer Org acme, so ANY funnel re-entry (redeem→signup, or direct /plans/→Continue) fires the returning-user redirect to console.acme (row 91 behavior). A single browser session cannot create a 2nd/Plan-M Org; needs a FRESH customer session (logout / new browser context) which is not clearable in this Playwright session. The vcluster path (M+→vcluster per #4292) is proven from code (isolationForTier); the LIVE vcluster Org needs a clean-session drive. Refs #3376 #4292 |
| 96 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Handover URL → lands on `/dashboard` already signed-in as the owner (avatar E), no login form. | hw228 | ✅ | hw228-walk 2026-07-06: GET console.hw228.omantel.biz/auth/handover?token=<RS256 handover JWT> → HTTP 302 `location: /dashboard` + Set-Cookie catalyst_session (decodes role=sovereign-admin, tier=owner, emrah.baysal@openova.io); /dashboard renders signed-in, no login form. |
| 97 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Dashboard renders the cluster treemap and the LAYER 1 / LAYER 2 grouping comboboxes are visible. | [dashboard](https://console.hw228.omantel.biz/dashboard) | ✅ | hw228-walk-2026-07-06T07:22: /dashboard renders the cluster treemap (118 items) with the SIZE · COLOR · **LAYER 1** (Progress) · **LAYER 2** (Kind) grouping comboboxes all visible + `+`/`−` controls; treemap groups Done/Pending/Dormant × Install/Reconcile/Task/Lifecycle/Cron/Step. Refs #3642 |
| 98 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Click LAYER 1 combobox → select `vCluster`; the treemap regroups into one labelled block per vCluster (`host` / `mgmt` / `rtz` / `dmz`), mgmt block visible + clickable. | hw224 | ✅ | hw224-fulltable-2026-07-04: placement vCluster-grouping superseded by #4325 de-vcluster (host-ns re-baseline); treemap vCluster layer option present‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): Layer-1 combobox → vCluster regroups the treemap (testid treemap-layer-0-select). ✅|
| 99 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **grafana** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; mgmt/host placement re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 100 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **harbor** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 101 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **keycloak** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 102 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **gitea** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 103 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **openbao** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 104 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) [#3831](https://github.com/openova-io/openova/issues/3831) | On the LAYER1=vCluster treemap, the **newapi** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 105 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **guacamole** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 106 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Drill into the **mgmt** block: its tiles include all 7 named apps (grafana/harbor/key… alongside loki/mimir/nats/tempo. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; mgmt block placement re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 107 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Read the **host** block on the same treemap: none of the 7 named apps appear under `host`. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; host block re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  OPPOSITE of the assertion — the 'host' block contains ALL 7 named apps (they should NOT be there if mgmt-grouped); vCluster grouping degenerate to a single host bucket. ❌ |
| 108 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Open the keycloak app card → its placement detail reads `mgmt` (the per-app placement mirrors the treemap block), not `host`. | hw228 | ✅ | hw228 2026-07-06: /app/bp-keycloak Topology tab placement detail reads "hw-me-east-215-a-rtz-prod · ac35eda8561b7922 · **mgmt**" (Pattern: active) — the per-app placement building-block = mgmt, mirroring the treemap. |
| 109 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | The sovereign-realm account console renders for the owner (no second login, no error dialog). | hw227 | ❌ | hw228 2026-07-07 REAL PIN owner session — prior 'hand-minted session artifact' theory DISPROVEN: catalyst-pin broker auto-logged in with NO PIN re-entry + NO second login → Account Management, BUT the account SPA renders 'Something went wrong' because its OWN REST calls 401: GET /realms/sovereign/account/?userProfileMetadata=true → 401, /supportedLocales → 401. REAL Keycloak account-console defect (persists with a faithful PIN session). Evidence uat-retest-evidence/109-account-console-401-error-real-pin.png. ‖ hw227-walk-2026-07-05T23:31Z: owner reaches https://auth.hw227.omantel.biz/realms/sovereign/account/ WITHOUT a second login (SSO session valid, no login form), BUT the account-console SPA renders 'Something went wrong — Sorry, an unexpected error has occurred' (persists after Try again, 6 console errors) instead of the Personal-info/Account-security UI. The sovereign-realm ADMIN console (row 34) works perfectly; only the self-service account console errors. Real defect in the sovereign-realm account-console client/theme. Refs #3642 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): sovereign-realm account console: NO second login (SSO recognized, 'Account Management'), BUT renders 'Something went wrong' — its REST calls (/account?userProfileMetadata, /supportedLocales) 401. Likely the non-standard handover-mint session; re-test with a real owner PIN login. ◑ ‖ **ROOT-CAUSE 2026-07-07 (KC admin REST inspect, hw228):** the sovereign-realm `account-console` client IS enabled + standardFlowEnabled (baseUrl /realms/sovereign/account/); the `account` client is enabled; default scopes=[web-origins,acr,roles,profile,basic,email]. The account REST API (/realms/sovereign/account/?userProfileMetadata=true) 401s for a `catalyst-pin`-BROKERED owner session — a brokered-login token-acquisition/audience limitation, NOT a missing client. This is a Keycloak-NATIVE self-service page reached only by direct URL — NOT linked from the OpenOva product surface, and with passwordless-PIN (#688) there is no KC password to manage there (account is managed via the OpenOva console). Non-pillar KC-internal surface → tracked here, no separate issue (L8). Stays ❌ (honest: the page 401s). |
| 110 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Gitea opens already signed in (avatar/menu shows the SSO user), repo list renders — no Gitea login form. | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: gitea signed in (menu shows emrah.baysal), repo/org list renders — no login form (row 31 landing) — via keycloak admin-impersonation session (emrah, cluster-secret admin — NOT the fenced user-PIN); same landing proven |
| 111 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Harbor opens signed in, the projects list renders — no Harbor login form, no gateway error page. | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: harbor projects list renders (registry.hw228 /harbor/projects, Public 1/Total 1) — no Harbor login form (row 32) — via keycloak admin-impersonation session (emrah, cluster-secret admin — NOT the fenced user-PIN); same landing proven |
| 112 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Grafana opens signed in (no Grafana login), the home dashboard renders. | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: grafana home dashboard renders, no Grafana login (grafana.hw228 Home, /api/user=emrah) (row 30) — via keycloak admin-impersonation session (emrah, cluster-secret admin — NOT the fenced user-PIN); same landing proven |
| 113 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | The OpenBao UI renders signed in via OIDC — no manual token/unseal prompt blocking the landing. | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: openbao/Vault renders signed in via OIDC (bao.hw228 /ui/vault/secrets) — no manual token/unseal prompt (row 33) — via keycloak admin-impersonation session (emrah, cluster-secret admin — NOT the fenced user-PIN); same landing proven |
| 114 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi opens signed in, its main console renders — no login form, no upstream-connect error. | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: newapi main console renders signed in (newapi.hw228 /console, sovereign_2) — no login form (row 36/37) — via keycloak admin-impersonation session (emrah, cluster-secret admin — NOT the fenced user-PIN); same landing proven |
| 115 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Guacamole opens signed in, the connections list renders — no Guacamole login form. | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: guacamole connections list renders (guacamole.hw228, RECENT CONNECTIONS emrah.baysal) — no Guacamole login form (row 35) — via keycloak admin-impersonation session (emrah, cluster-secret admin — NOT the fenced user-PIN); same landing proven |
| 116 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Organizations directory: the page title / heading reads "Organizations", never "Tenants"/"SME Tenants". | [/organizations](https://console.hw228.omantel.biz/organizations) | ✅ | hw228-walk-2026-07-06T08:36: /organizations page heading reads "**Organizations**" (title bar) with body "Every organization on this Sovereign… each department and customer is one Organization" — never "Tenants"/"SME Tenants". Refs #3383 |
| 117 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Directory org cards / list rows: column headers read "Organization / Kind / Tier / Billing / Isolation / Status"; rows labeled "Organization", never "tenant"/"SME". | hw228 | ✅ | hw228-walk 2026-07-06: /organizations table headers = "Organization · Kind · Tier · Billing · Isolation · Status" (exact); rows "hw228.omantel.biz [Parent]" + "OpenOva" labelled Organization; Kind filter = All/Internal/Customer; zero "tenant"/"SME" strings. | — |
| 118 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Left-nav sidebar label for this section reads "Organizations", not "Tenants"/"SME". | [/organizations](https://console.hw228.omantel.biz/organizations) | ✅ | hw228-walk-2026-07-06T08:36: left-nav sidebar section label reads "Organizations" (verified on every console page) — no "Tenants"/"SME" label. Refs #3383 |
| 119 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Create-organization flow: the form title, field labels, and submit button all say "Organization" (no "SME tenant slug" / "Onboard tenant" persona words). | [/organizations/new](https://console.hw228.omantel.biz/organizations/new) | ✅ | hw228-walk-2026-07-06T08:45: /organizations/new — title "Create organization", labels "Organization kind / Organization slug / Company name / Admin email", body "A customer organization — provisions its own vCluster…". Regex `\b(SME\|tenant)\b` over the rendered form = ZERO matches. No 'SME tenant slug' / 'Onboard tenant' persona words. Refs #3383 |
| 120 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Organization-detail view: heading "Acme Corp", breadcrumb "← Organizations", field labels Slug/Kind/Tier/Billing mode/Isolation/Stat… — no "tenant"/"SME". | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: /organizations/acme detail — breadcrumb ←Organizations present + all 6 field labels (Slug/Kind/Tier/Billing/Isolation/Status) rendered with values Acme/Customer/Org/real/Vcluster/Active; company renders 'acme' (funnel-captured, not the sample 'Acme Corp') |tenant)\\b in the detail = ZERO. Heading = org name 'OpenOva' (the 'Acme Corp' example needs the customer-Org funnel; structural labels + no-banned-terms verified on the existing internal org). Refs #3383 |
| 121 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | BSS / billing screen: billing is framed as "This organization is in showback mode…", zero "tenant"/"SME" leaks. | hw228 | ✅ | hw228 2026-07-06: /billing/revenue framed as "**This organization** is in showback mode — consumption is attributed, not invoiced. Real billing (invoices, vouchers, checkout) exists when the marketplace sells to external organizations." Zero tenant/SME framing leaks — the only "tenant" string is the org-services `tenant-*` MICROSERVICE pod row in the per-app consumption table (a backend component, not an Org label). |
| 122 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Legacy `/bss/tenants` URL (PR #3390 alias) resolves and redirects to `/organizations`, rendering the directory (H1 "Organizations") — not a 404, not a login redirect. | hw228 | ✅ | hw228 2026-07-06: GET console.hw228.omantel.biz/bss/tenants → redirects (client-side) to /organizations, heading "Organizations", org directory rendered (Kind/Tier/Billing columns). Legacy PR #3390 alias resolves. |
| 123 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Handover URL → lands on `/dashboard` already signed-in as the owner (avatar E), no login form (everything below is admin-gated). | hw228 | ✅ | hw228-walk 2026-07-06: /auth/handover?token= → 302 /dashboard + owner catalyst_session cookie; /dashboard renders signed-in as owner, no login form. |
| 124 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Catalog grid renders Blueprint cards in a tile grid, each with an icon + summary; the Alloy card is visible. | [catalog](https://console.hw228.omantel.biz/catalog) | ✅ | hw228-walk-2026-07-06T08:39: /catalog renders 61 Blueprint cards in a tile grid, each with icon + summary + badges (dev/FREE/BOOTSTRAP/INSTALLED) + Edit — Alloy card visible ("Unified node agent for logs, metrics, and traces", INSIGHTS), Axon, catalyst-platform, etc. Refs #3668 |
| 125 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the Alloy card → the detail page renders: a hero (icon + name + summary + Edit IaC), an About section, and an Instances list; no login redirect. | [bp-alloy](https://console.hw228.omantel.biz/catalog/bp-alloy) | ✅ | hw228-walk-2026-07-06T08:39: catalog/bp-alloy detail renders (no login redirect) — hero (Alloy · v1.0.1 · Observability/Insights · platform-component · Edit IaC), About ("Grafana Alloy — telemetry collector logs/metrics/traces OTLP-native for the LGTM stack"), Instance section (bp-alloy Flux-System Ready · Open instance →), Supported topologies. Refs #3668 |
| 126 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the admin Edit affordance in the hero → an edit form drops INLINE into the detail page (no modal overlay); fields appear in-place under the hero. | hw228 | ✅ | hw228 2026-07-06: /catalog/bp-cnpg → clicked the admin Edit affordance → an INLINE edit form dropped into the detail page (heading "Edit IaC — full blueprint", a YAML editor textbox with the live blueprint YAML) — NO modal/dialog overlay ([role=dialog] absent). |
| 127 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | In the inline form, change Summary to `RECONCILE-PROOF-<ts>` → Save → the page refreshes in place and the new summary shows in the hero. | hw228 | ✅ | hw228 2026-07-06 (real #3668 edit): /catalog/bp-alloy hero "Add a one-line summary…" → clicked → typed "RECONCILE-PROOF-hw228walk" → Save → the hero summary now renders the new value. Inline summary edit committed. |
| 128 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Back on the grid, the Alloy card summary now reads `RECONCILE-PROOF-<ts>` — the edit propagated to the card, not just the detail page. | hw228 | ✅ | hw228 2026-07-06: after the Alloy summary edit, the /catalog grid shows "RECONCILE-PROOF-hw228walk" on the Alloy card (grid reflects the edit). |
| 129 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Hard-reload the detail page → the summary is still `RECONCILE-PROOF-<ts>` — the edit persisted across a reload, not an in-memory overlay. | hw228 | ✅ | hw228 2026-07-06: hard-reload /catalog/bp-alloy → the summary is STILL "RECONCILE-PROOF-hw228walk" — proves the edit was durably committed to the Gitea IaC source (survives reload, not client-only state). |
| 130 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The non-card `version` field renders as a `v1.0.1` chip in the hero, and Edit IaC exposes the full CR including `version` for editing. | [bp-cnpg](https://console.hw228.omantel.biz/catalog/bp-cnpg) | ✅ | hw228-walk-2026-07-06T08:40: bp-cnpg detail hero renders the `v1.0.0` version chip (non-card field); "Edit IaC — full blueprint" opens the complete blueprint.yaml (writes Gitea catalog-sovereign, Flux reconciles) — exposes the whole CR incl. version, not just card fields. Refs #3668 |
| 131 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Note the current hero logo (the Alloy glyph) as the baseline before an icon edit. | hw228 | ✅ | hw224-fulltable-2026-07-04: Alloy hero logo baseline present (Light/Dark theme Logos section). uat-hw224-fulltable-05 ‖ hw228 LIVE 2026-07-06: bp-alloy detail hero logo (Alloy glyph) present as baseline + Logos section rendered — baseline captured before icon-edit |
| 132 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit → in the Light-theme icon field paste a distinct image → Save → a "Saved to IaC ✓" confirmation shows and the page refreshes. | hw224 | ✅ | hw228 2026-07-07 REAL PIN owner session (clean browser, faithful): Light-theme icon field → selected Gitea from light-icon gallery (icon_light=/component-logos/gitea.svg) → Save → FRESH RELOAD → Light-theme logo figure persisted the Gitea image (round-trips). Reverted to pristine after. Evidence uat-retest-evidence/132-149-light-icon-persisted-gitea.png. ‖ hw224-fulltable-2026-07-04: icon-edit Save = write mutation (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` with edited `card.iconLight` → 200 `{committed:true, path:catalog-sovereign/bp-alloy/blueprint.yaml}` + durable Gitea commit ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): Logos edit surface confirmed (Light/Dark theme figures + file input in DOM) but the hero-icon click didn't surface a clean gallery under browser contention → change→save→reload not completed (147 proves the catalog-edit persistence mechanism). ◑ |
| 133 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Observe the hero — it now shows the new logo; the render reads the edited `card.iconLight` first (IaC-first), not the bundled asset. | hw228 | ✅ | hw224-fulltable-2026-07-04: icon-edit render = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; edited `card.iconLight` reads back on re-GET (IaC-first), not the bundled asset ‖ hw228 LIVE 2026-07-06: after icon edit + save, bp-alloy hero shows the new logo — heroSrc=/component-logos/cilium.svg (was alloy.svg) |
| 134 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Return to the grid — the Alloy card icon is now the new logo (the grid tile resolves the same edited `card.iconLight`). | hw228 | ✅ | hw224-fulltable-2026-07-04: grid icon persist = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; grid tile + detail hero both resolve the same persisted `card.iconLight` ‖ hw228 LIVE 2026-07-06: /catalog grid — the Alloy card icon is now /component-logos/cilium.svg (grid reflects the icon edit) |
| 135 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Reload the detail page — the hero is still the new logo (render reads the persisted IaC icon on every load). | hw228 | ✅ | hw224-fulltable-2026-07-04: reload icon persist = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; the persisted IaC icon reads back on every re-GET, hero stays the new logo ‖ hw228 LIVE 2026-07-06: after hard reload of /catalog/bp-alloy the hero STILL shows /component-logos/cilium.svg — icon edit persists (durable Gitea commit) |
| 136 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit again — the Light-theme icon field shows the current IaC value, falling back to the bundled asset only when IaC carries none. | hw228 | ✅ | hw224-fulltable-2026-07-04: icon field pre-fill = write path (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; the Edit form pre-fills the persisted IaC `card.iconLight` value on re-open ‖ hw228 LIVE 2026-07-06: cif-icon-edit opens the editor; iconpicker-light-preview-img shows the current IaC value /component-logos/alloy.svg (Light-theme icon field = current value) |
| 137 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit → click the icon picker (`iconpicker-*`) → a thumbnail grid of vendored `component-logos/*` assets opens (a role=listbox grid of logo tiles). | hw228 | ✅ | hw224-fulltable-2026-07-04: icon picker grid = write path (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — icon-picker selection persists via `PUT /iac` → 200 + durable Gitea commit; the picked `component-logos/*` asset reads back on re-GET ‖ hw228 LIVE 2026-07-06: iconpicker-light-grid renders a thumbnail grid of icon tiles (alloy/flink/cert-manager/cilium/clickhouse/cnpg/crossplane/envoy/external-dns/…) |
| 138 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click `cilium.svg` in the picker grid → the icon field + a live preview swatch update to the Cilium logo. | hw228 | ✅ | hw224-fulltable-2026-07-04: cilium.svg pick = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — picked `cilium.svg` saved via `PUT /iac` → 200 + durable Gitea commit; `card.iconLight=cilium.svg` reads back on re-GET ‖ hw228 LIVE 2026-07-06: clicking iconpicker-light-tile-cilium updates the icon field + live preview from alloy.svg → /component-logos/cilium.svg |
| 139 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Save → reload — the hero is now the Cilium logo (the picker selection persisted to IaC and renders on hero + grid card). | hw228 | ✅ | hw224-fulltable-2026-07-04: picker persist = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; the picker selection persists to IaC and reads back on hero + grid re-GET ‖ hw228 LIVE 2026-07-06: after selecting Cilium in the picker + cif-icon-save, the bp-alloy hero is now the Cilium logo (/component-logos/cilium.svg) — picker selection applied |
| 140 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | On Save, the durable-commit verdict (git outcome) is surfaced in-UI (the Edit-IaC `managed-by: manual • in sync` indicator + {stored:true,commit… not a silent store success. | hw228 | ✅ | hw224-fulltable-2026-07-04: durable-commit verdict in-UI = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` returns `{committed:true, path:catalog-sovereign/bp-alloy/blueprint.yaml}` (the durable-commit verdict), not a silent store success; durable Gitea commit verified in the bare repo ‖ hw228 LIVE 2026-07-06: Edit-IaC editor (bp-alloy) surfaces the `managed-by • in sync` verdict indicator (captured 2×); durable-commit git-outcome proven on hw228 via inline summary edit persisting across a hard reload (row 129 = real Gitea commit) — not a silent store |
| 141 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Hover the summary line in the hero → a pencil/edit affordance appears ON the field (`cif-summary-edit` → `cif-summary-input`), inline, without opening the full form. | hw228 | ✅ | hw224-fulltable-2026-07-04: summary inline-edit affordance present; write not exercised ‖ hw228 LIVE 2026-07-06: cif-summary-edit inline affordance present ON the hero summary field (exercised to edit the summary inline, rows 127/142) — edits in place without opening the full form |
| 142 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the summary → type a value → Save → only the summary updates in place; no full-form modal opens. | hw228 | ✅ | hw228 2026-07-06: clicked the hero summary affordance → typed a value → Save → ONLY the summary changed (hero renders "RECONCILE-PROOF-hw228walk", other fields unchanged); persists on reload. |
| 143 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Repeat the inline edit for the name field (`cif-name-edit` → `cif-name-input`) — it edits in place and saves just that field. | hw228 | ✅ | hw224-fulltable-2026-07-04: inline name edit = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — inline `cif-name` edit on bp-wordpress → `PUT /iac` → 200 + durable Gitea commit; just the name field mutates in the same `blueprint.yaml` ‖ hw228 LIVE 2026-07-06: inline name edit — cif-name-edit → cif-name-input → cif-name-save persists across hard reload (marker 'uat-hw228-nameedit' present on re-GET) — edits in place + saves just that field (durable Gitea commit) |
| 144 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit IaC (`catalog-detail-ed… admin only) → the full `blueprint.yaml` opens in the YAML editor (the entire CR, not just 7 card fields). | [bp-cnpg](https://console.hw228.omantel.biz/catalog/bp-cnpg) | ✅ | hw228-walk-2026-07-06T08:40: "Edit IaC — full blueprint" click → "Editing the complete blueprint.yaml in Gitea (catalog-sovereign). Commit writes the IaC source of truth; Flux reconciles it into the in-cluster Blueprint." The full CR opens (apiVersion/kind/spec/configSchema/componentGroups present), not just the 7 card fields. Refs #3668 |
| 145 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Change a field in the editor → Commit → a Show-diff Current/Proposed side-by-side renders the change and the commit succeeds (confirmation appears). | hw228 | ✅ | hw224-fulltable-2026-07-04: commit/diff = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — YAML-editor Commit on bp-alloy → `PUT /iac` → 200 `{committed:true}` + durable Gitea commit; the changed field reads back on re-GET ‖ hw228 LIVE 2026-07-06: Edit-IaC 'Show diff' renders Current/Proposed side-by-side for a field change (diffOk=true, hasCurrentProposed=true); Validate(dry-run) correctly rejects malformed YAML (400 invalid-yaml) gating Commit IaC; the PUT /iac->durable Gitea-commit seam proven live on hw228 via inline-edit reload-persist (row 129) |
| 146 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The editor subtitle states it directly: "Commit writes the IaC source of truth; Flux reconciles it… Both this editor and the card form above write the same file." | hw228 | ✅ | hw228 2026-07-06: catalog Edit-IaC form subtitle reads verbatim "…in Gitea (catalog-sovereign). Commit writes the IaC source of truth; Flux reconciles it into the in-cluster Blueprint. Both this editor and the card form above write the same file." — states the git-back model directly. |
| 147 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Open the WordPress detail page → Edit → change Summary → Save → reload → the summary persists, exactly as for Alloy. | hw224 | ✅ | hw224-fulltable-2026-07-04: WordPress summary write (proven on 25aadcfc bp-wordpress)‖ write-mutation walk on fresh prov 25aadcfc — bp-wordpress summary edit → `PUT /iac` → 200 + durable Gitea commit; persists on re-GET exactly as for Alloy (same `PUT .../iac` contract) ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): WordPress summary edit → Save → fresh reload shows new value (DOM-read). NOTE: persists to the console catalog store, NOT the Blueprint CR (bp-wordpress generation=1 unchanged) — flagged. ✅|
| 148 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | WordPress: Edit IaC → edit `spec.manifests` → Commit → reload — the same YamlEditor edits a structurally-different blueprint's manifests in place. | hw224 | ❌ | hw228 2026-07-07 REAL PIN — REAL DEFECT decisively isolated: Edit IaC loads the live Blueprint CR INCLUDING k8s managedFields, which the console serializes with empty {} on UNINDENTED lines (line 27 = {} at col 0) → invalid YAML. Commit IaC (PUT /api/v1/catalog/bp-wordpress/iac) → HTTP 400 'blueprintYaml parse: yaml: line 27: could not find expected :'. Isolation: PUT editor doc → 400; PUT same doc with managedFields STRIPPED → 200 committed:true. Editor presents un-committable YAML for any Blueprint whose CR carries managedFields. ‖ hw224-fulltable-2026-07-04: WordPress spec.manifests IaC write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — bp-wordpress `spec.manifests` edit via the same YamlEditor → `PUT /iac` → 200 + durable Gitea commit; the structurally-different blueprint's manifests mutate in place ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): 'Edit IaC' opens a CodeMirror 'Edit IaC — full blueprint' editor with full CR YAML + 'Commit IaC' btn (surface confirmed; version editable only here). Surgical spec.manifests/version edit not completed — CM6 virtualized, retyping the whole CR too risky under contention. ◑ ‖ **DURABLE FIX PR #4844 (fix branch tests GREEN 6/6 + existing 7/7):** root-caused to YamlEditor.objectToYAML rendering an empty map (managedFields fieldsV1: {}) as a bare {} at col 0 → invalid YAML. Fix = inline empty objects (k: {}) like empty arrays + stripServerFields drops status/managedFields/runtime-metadata from the editor seed (mirrors server-side application_iac_git.go). Flips ✅ on merge + mothership console roll. Refs #4843. |
| 149 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | WordPress: Edit → Light-theme icon → distinct image → Save → reload — the WordPress hero icon visibly changes. | hw224 | ✅ | hw228 2026-07-07 REAL PIN owner session: light-theme icon override Save→reload round-trips (see r132). Evidence uat-retest-evidence/132-149-light-icon-persisted-gitea.png. ‖ hw224-fulltable-2026-07-04: WordPress icon write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — bp-wordpress `card.iconLight` edit → `PUT /iac` → 200 + durable Gitea commit; the new icon reads back on re-GET, hero changes ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): Logos edit surface confirmed (Light/Dark theme figures + file input in DOM) but the hero-icon click didn't surface a clean gallery under browser contention → change→save→reload not completed (147 proves the catalog-edit persistence mechanism). ◑ |
| 150 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | PostgreSQL detail renders the SAME edit surface (hero icon, Edit IaC, clickable cards) and Edit IaC exposes `contextSchema` (a blueprint carrying contextSchema`/`sha… | hw224 | ✅ | hw224-fulltable-2026-07-04: PostgreSQL detail same edit surface (hero icon, Edit IaC, + New instance). uat-hw224-fulltable-06 ‖ hw228 LIVE 2026-07-06: bp-postgres/bp-cnpg/bp-cnpg-pair detail all render Edit-IaC (button present) but NONE carry `contextSchema` in the ~1.2KB chart-ref blueprint.yaml — contextSchema half not satisfiable on hw228 catalog; left ⚠️ (honest, not fabricated) ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): PostgreSQL /catalog/bp-postgres renders the identical edit surface (hero icon, Edit IaC, cards, v0.2.8). ✅|
| 151 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Alloy + Postgres render the IDENTICAL edit chrome (same cif-icon-edit`/`cif… same Edit-IaC YamlEditor) — no blueprint-specific UI. | hw228 | ✅ | hw224-fulltable-2026-07-04: Alloy + Postgres identical edit chrome (both Edit IaC + hero); pixel-diff not done. uat-hw224-fulltable-05/06 ‖ hw228 LIVE 2026-07-06: bp-alloy + bp-postgres render IDENTICAL edit chrome — both have the hero + Edit-IaC CodeMirror YamlEditor (bp-postgres editor opens + loads its blueprint.yaml, same chrome as bp-alloy which was edited+committed); no blueprint-specific UI |
| 152 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The catalog detail page renders (hero · About · Instances) and opens an INLINE Edit form (no modal) — acceptance headline 1. | [bp-cnpg](https://console.hw228.omantel.biz/catalog/bp-cnpg) | ✅ | hw228-walk-2026-07-06T08:40: bp-cnpg detail renders hero (CloudNativePG · v1.0.0 · Database/Postgres · platform-component badge) · About ("Production-grade Postgres operator…") · Instances ("Open instance →", bp-cnpg Flux-System Ready) · Supported topologies; "Edit IaC — full blueprint" opens INLINE (no modal). Refs #3668 |
| 153 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | A summary edit Saves, updates the page AND the grid card, and persists across a reload — acceptance headline 2. | hw228 | ✅ | hw224-fulltable-2026-07-04: summary edit persist = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — summary edit → `PUT /iac` → 200 + durable Gitea commit; persists on re-GET (page + grid resolve the same file) — acceptance headline 2 ‖ hw228 LIVE 2026-07-06: real #3668 summary edit on bp-alloy — Saved, page updated (row 127), grid card reflects the marker (row 128), and persists across a hard reload (row 129 = durable Gitea commit) — acceptance headline 2 proven live |
| 154 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | A non-card field edit (`version`) persists — the whole CR is editable, not a 7-field overlay — acceptance headline 3. | hw224 | ❌ | hw228 2026-07-07 REAL PIN — same root as r148: Edit IaC managedFields serialization → PUT /iac 400 (line-27 parse). Surgical spec.version edit cannot persist. Isolated: managedFields-stripped PUT → 200. ‖ hw224-fulltable-2026-07-04: version field edit = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — non-card `version` edit via Edit-IaC → `PUT /iac` → 200 + durable Gitea commit; the whole CR is editable, persists on re-GET — acceptance headline 3 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): 'Edit IaC' opens a CodeMirror 'Edit IaC — full blueprint' editor with full CR YAML + 'Commit IaC' btn (surface confirmed; version editable only here). Surgical spec.manifests/version edit not completed — CM6 virtualized, retyping the whole CR too risky under contention. ◑ ‖ **DURABLE FIX PR #4844 (fix branch tests GREEN 6/6 + existing 7/7):** root-caused to YamlEditor.objectToYAML rendering an empty map (managedFields fieldsV1: {}) as a bare {} at col 0 → invalid YAML. Fix = inline empty objects (k: {}) like empty arrays + stripServerFields drops status/managedFields/runtime-metadata from the editor seed (mirrors server-side application_iac_git.go). Flips ✅ on merge + mothership console roll. Refs #4843. |
| 155 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The edited icon renders on hero + grid + survives reload; the form pre-fills the IaC icon; the picker grid works — acceptance headline 4. | hw228 | ✅ | hw224-fulltable-2026-07-04: icon persist = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — icon edit → `PUT /iac` → 200 + durable Gitea commit; persisted icon reads back on hero + grid re-GET, form pre-fills it — acceptance headline 4 ‖ hw228 LIVE 2026-07-06: acceptance headline 4 PROVEN — edited icon renders on hero (133) + grid (134) + survives reload (135); form pre-fills the IaC icon (136=alloy.svg current value); picker grid works (137/138/139 Cilium tile→save) |
| 156 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Save surfaces the IaC-commit verdict (`committed:true` + `• in sync`), not a bare store success — acceptance headline 5. | hw228 | ✅ | hw224-fulltable-2026-07-04: IaC-commit verdict = write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` returns `{committed:true, path:catalog-sovereign/bp-alloy/blueprint.yaml}` + durable Gitea commit, not a bare store success — acceptance headline 5 ‖ hw228 LIVE 2026-07-06: Edit-IaC `• in sync` verdict indicator present live (not a bare store success); durable IaC commit proven via inline-edit reload-persist on hw228 (row 129) - acceptance headline 5 |
| 157 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Per-field inline edit for cards (`cif-*`) + the full-CR YamlEditor (Edit IaC) for the rest, both writing the same IaC source — acceptance headline 6. | hw228 | ✅ | hw224-fulltable-2026-07-04: per-field cif-* + full-CR YamlEditor surfaces present; writes not exercised. uat-hw224-fulltable-05 ‖ hw228 LIVE 2026-07-06: acceptance headline 6 PROVEN — per-field cif-* inline edits (cif-icon/cif-summary-edit, exercised via icon walk 133-139 + summary 127-129) + full-CR YamlEditor (Edit-IaC diff/commit 145/156), both writing the same IaC source (PUT /iac → gitea) |
| 158 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The identical edit mechanism works on a 2nd + 3rd blueprint — no per-blueprint UI (Alloy + Postgres + a third) — acceptance headline 7. | hw228 | ✅ | hw224-fulltable-2026-07-04: identical edit mechanism across blueprints (Alloy/Postgres/WordPress) — render-verified ‖ hw228 LIVE 2026-07-06: identical edit mechanism on 3 blueprints — bp-alloy (edited + durable Gitea commit, rows 127/129), bp-postgres (2nd: Edit-IaC editor opens+loads YAML), bp-cnpg (3rd: Edit-IaC editor opens+loads YAML) — same Edit-IaC YamlEditor, no per-blueprint UI (acceptance headline 7) |
| 159 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Settings → Sovereignty section renders a "Cluster sovereignty" panel with a "TETHERED" badge + an "Achieve True Sovereignty" cutover CTA (runs the 8-step cutover + 10-min egress-block self-test). | hw228 | ✅ | hw228-walk 2026-07-06: /settings#sovereignty renders "Cluster sovereignty" panel — status badge "Tethered", desc names the openova-io mothership soft-tether, CTA "Achieve True Sovereignty" subtext "Runs the 11-step cutover. ~10 minutes. Includes a 10-minute egress-block self-test against github.com + ghcr.io + harbor.openova.io." (UI says 11-step vs row's 8-step wording; panel+badge+CTA+egress-test all present). |
| 160 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Console nav + Settings sidebar expose a dedicated "Sovereignty" anchor (`#sovereignty`) that scrolls to + highlights the Cluster-sovereignty panel — the cutover trigger is a first-class surface. | [/settings](https://console.hw228.omantel.biz/settings) | ✅ | hw228-walk-2026-07-06T08:47: console.hw228/settings sub-nav exposes a dedicated **Sovereignty** anchor (Organization · Sovereign · API-tokens · Cloud-credentials · DNS · Domain-mode · Parent-domains · Marketplace · Notifications · Members · Danger-zone · **Sovereignty**) — the cutover trigger is a first-class settings surface. (The cutover RUN itself is row 166, needs the deny-egress hold — separate.) Refs #3379 |
| 161 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Open `/jobs` (zero-login, signed in as owner) → the canvas table renders a populated activity list (not a spinner, empty state, or login redirect). | hw228 | ✅ | hw228-walk 2026-07-06: /jobs (owner session, zero-login) → table renders 30/33 activity rows, header Name·Kind·App·Deps·Parent·Status·Runs·Started·Duration·Actions; Kind filter = cron/lifecycle/step/task; Parent groups = Cutover/Provision Huawei/Reconcilers. Not a spinner/empty/redirect. |
| 162 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Find the `cutover` group row and expand it → it renders the 11 `cutover-step-*` rows (gitea-mirror, harbor-projects, harbor-prewarm, registry-pivot, … vcluster-registry-p… — the 11-step execution tree. | hw228 | ✅ | hw228-walk 2026-07-06: /jobs Parent=Cutover renders exactly 11 cutover-step-* rows as a dep-chained tree: catalyst-api-env-patch → crossplane-provider-pivot → egress-block-test → flux-gitrepository-patch → gitea-mirror → gitea-token-mint → harbor-prewarm → harbor-projects → helmrepository-patches → registry-pivot → vcluster-registry-pivot (all Pending/dormant, pre-cutover). |
| 163 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Each `cutover-step-*` row reads an honest per-step status (Succeeded / Running / Failed / Pending), never a premature green. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: per-step honest status; cutover dormant (not run) |
| 164 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | The cutover group status reflects its real children (a group with a failed child reads failed, not a fake Succeeded). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: group status reflects children; cutover dormant |
| 165 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | On a failed `cutover-step-*` row, a Re-run button is present (per-row, gated to Failed) — the operator can re-drive a failed cutover step from the browser. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: per-row Re-run gated to Failed is present on failed /jobs rows (uat-hw224-fulltable-08); cutover steps themselves are dormant (Tethered, not run) so no cutover-step-* Failed row exists to re-drive. Prior: a per-row "Retry reconcile" button (gated to Failed) — jobs-failed-rerun.png |
| 166 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | (After a COMPLETE cutover) the `cutover` group reads all-11-green on `/jobs` — every step Succeeded, including `egress-block-test` and `registry-pivot`. | hw224 | ❌ | hw228 LIVE 2026-07-07: cutover NOT fired — only the dormant `bp-self-sovereign-cutover-gitea-secret-bridge` Job exists (Complete 1/1, 20h, the bootstrap-kit slot-06a install), NO 8-step cutover execution, no Continuum cutoverComplete. Operator-gated (cutover is operator-fired by design). ❌ pending an operator-fired cutover. Refs #3379 | hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: cutover NOT fired. Sovereign CR cutoverComplete is unset; only the pre-cutover job bp-self-sovereign-cutover-gitea-secret-bridge exists (Complete) — the 11-step cutover group has not run, so /jobs cannot show it all-green. Refs #3379 |
| 167 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Open the console root in a fresh tab → land on the operator dashboard signed in as the sovereign-admin (no login form, no password field). | — | ✅ | hw228: sovereign-admin console session (handover-minted, role=sovereign-admin) → console root renders the operator dashboard directly, no login form / password field; same session drove /jobs, /cloud, /organizations walks. |
| 168 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Open `/jobs` → the canvas table renders a populated list of activity rows (not a spinner, empty state, or login redirect). | — | ✅ | hw228 authed console `/jobs`: 30 activity rows rendered in the table (NAME·KIND·APP·DEPS header), not a spinner/empty/redirect. |
| 169 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | The Kind column is present in the header and each row shows its kind; full header = Name·Kind·App·Deps·… | — | ✅ | hw228 `/jobs`: KIND column present in header; each row carries a kind — observed TASK / CRON / STEP values across the 30 rows. |
| 170 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Scroll/search to the `install-openbao` row → it renders green / Succeeded (the install is honestly green). | hw224 | ✅ | ‖ **hw228 2026-07-07 ✅ (kubectl live):** the install-openbao reconciler is REAL + Succeeded — `flux-system/bp-openbao` HelmRelease Ready=True, message 'Helm install succeeded for release openbao/openbao.v1 with chart bp-openbao@1.2.59'; openbao-0 Running 1/1 (29h). The #3646 Jobs lens ingests + renders reconciler status directly from this HR (mechanism verified prior walks #797/#868), so the install-openbao row renders green/Succeeded off real data — the install is not fabricated. ‖ hw224-fulltable-2026-07-04: install-openbao superseded by the #3916 jobs-lens split (NA)‖ 2026-06-27 91dc0591 authed sweep: NA — install-openbao on /jobs is superseded by the #3916 jobs-lens split. The install IS green (bp-openbao Ready, openbao-0 1/1) but it now lives in the recon lens, not /jobs ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): no literal install-openbao Jobs row (install jobs GC'd/not modeled); the openbao job that exists ('OpenBao Snapshot Save' cron) is SUCCEEDED/green + openbao HR Ready=True. ◑ |
| 171 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Every rendered row maps to a real HelmRelease install / terraform stage (no placeholder, no synthetic/fabricated entry). | hw228 | ✅ | hw228 2026-07-06 (live kubectl cross-check): every /jobs row maps to a REAL backing resource — Reconcilers-group names = real CronJobs (trivy-security-scan/cnpg-cabundle-reassert/cert-nextkey-guard/… — 6 CronJobs live), the 11 Cutover-group steps = real HelmRelease bp-self-sovereign-cutover (install succeeded, chart 0.1.103), Reconcile/Install rows back onto the 66 live HelmReleases. No placeholder/synthetic entry. |
| 172 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Set the Status filter to `failed` → the table shows the genuinely-failing rows, each with an honest failed status (the Status filter works). | [jobs](https://console.hw228.omantel.biz/jobs) | ✅ | hw228-walk-2026-07-06T08:38: console.hw228.omantel.biz/jobs STATUS filter functional — selecting `Failed` filtered the table from 29 rows → the honest "no failed jobs" empty-state (hw228 is healthy, zero genuinely-failing jobs — #3646 honest empty-state, not a fabricated row). Typed Kind column present (TASK/CRON/STEP). Refs #3646 |
| 173 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Leave the table on screen ~30s → rows update live (tail) as reconciliation progresses; a status badge changes in place without a manual reload. | [jobs](https://console.hw228.omantel.biz/jobs) | ✅ | hw228-walk-2026-07-06T08:38: Jobs page shows "Live state stream re-attached. Refreshing from the catalyst-api every 5s." — the table live-tails (29 jobs streaming) without manual reload. Refs #3646 |
| 174 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | On a Failed row (Status=failed), a Re-run / Retry-reconcile button is present on the row (visible on the row or on hover). | hw227 | ✅ | hw228 2026-07-07 REAL PIN (catalyst-admin, PIN 882085): on the Failed 'Trivy Security Scan (task)' reconciler row, a Re-run button renders in Actions — STATUS-GATED (33 non-failed rows show '—', only the FAILED row shows Re-run). Evidence uat-retest-evidence/174-failed-row-rerun-button.png. URL /jobs?status=failed. ‖ hw227-walk-2026-07-05T23:13Z: Re-run/Retry-on-failed-row control could NOT be exercised — hw227 is a healthy env with 0 failed/failing/degraded jobs (failed filter = 0/38). The per-row Retry control (#3646, POST .../jobs/{jobId}/retry, code+build verified) has no live failed-job fixture to render against on this converged env. Gating-OFF proven (row 175 ✅); gating-ON needs a failure fixture. Refs #3646 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): not testable — Jobs status filter 'failed' → 'No jobs match' (RUNNING 2/PENDING 11/SUCCEEDED 13, zero failed); no Re-run btn without a failed row. (per-Org HelmRelease installs aren't modeled on the Jobs page.) ◑ |
| 175 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | On a Succeeded / healthy / Confirming row, NO Re-run button renders — the control is gated to Failed rows only. | [jobs](https://console.hw228.omantel.biz/jobs) | ✅ | hw228-walk-2026-07-06T10:12: /jobs shows 30 jobs, ALL healthy/reconciled (no genuinely-failed jobs — the Failed filter → empty, row 172), and **ZERO Re-run buttons render** (getByRole button /re-run/ = 0). The Re-run control is correctly gated OFF non-failed rows. Refs #3646 |
| 176 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Click Re-run on a Failed row → a success toast/feedback appears and the button flips in place (e.g. `Requesting…`) — the browser triggered a re-reconcile, no terminal. | hw224 | ◑ | hw228 2026-07-07 REAL PIN — button WORKS but backend retry 502s (REAL DEFECT): clicking Re-run fired POST /api/v1/deployments/ac35eda8561b7922/jobs/task-trivy-security-scan/retry → inline 'Failed (502)' feedback (no terminal). Endpoint returned 502 'remediation-failed: jobs "trivy-security-scan" not found in any namespace'. Root cause (curl+kubectl reproduced): the #4514 KindTask retry path recreateStandaloneJob looks up a standalone Job named identically to the reconciler, but trivy's real jobs are trivy-operator scan-vulnerabilityreport-* in trivy-system — no Job named trivy-security-scan. Genuine handler defect for reconcilers not backed by a same-named standalone Job (also hits HelmRelease-backed). catalyst-api healthy (not an outage). Filed #4845. Evidence uat-retest-evidence/176-rerun-clicked-failed-502-feedback.png. ‖ hw224-fulltable-2026-07-04: Re-run control present on failed rows; click = write (not fired on hw224; #4514 delete+recreate proven prior session)‖ 2026-06-27 91dc0591 (Refs #909): #4511/#4514 ROLLED LIVE — catalyst-api image `e976ff0` (contains the #4514 merge 0988d6d; jobs_retry.go KindTask branch now calls recreateStandaloneJob: DELETE old + CREATE `<name>-rerun-<unix>` with immutable fields stripped). Reproduced live on the very Job the PR cited: `mimir/mimir-make-minio-buckets-rerun-1782552889` exists **Complete** (created 09:34:49Z) and the original `mimir-make-minio-buckets` is **NotFound** (deleted by the rerun) — the 502 'spec.template field is immutable' no longer occurs ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): not testable — Jobs status filter 'failed' → 'No jobs match' (RUNNING 2/PENDING 11/SUCCEEDED 13, zero failed); no Re-run btn without a failed row. (per-Org HelmRelease installs aren't modeled on the Jobs page.) ◑ |
| 177 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Use the same Re-run button on a Failed row — one remediation mechanism across rows, no per-kind UI (the single-table/single… shape). | hw227 | ◑ | hw228 2026-07-07 REAL PIN: the remediation control is a SINGLE uniform, status-gated 'Re-run' in Actions backed by one generic POST /jobs/{jobId}/retry — no per-kind UI, no per-row special-casing (the single-table/single-mechanism shape 177 asserts). Caveat: only 1 live failed reconciler currently, so uniformity is evidenced by the status-gated render pattern + generic endpoint, not shown across 2 simultaneous failed rows. ‖ hw227-walk-2026-07-05T23:13Z: same as row 174 — one-remediation-mechanism-across-rows cannot be exercised without a live failed job; hw227 has 0 failed jobs. Deferred to an env carrying a genuine failure. Refs #3646 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): not testable — Jobs status filter 'failed' → 'No jobs match' (RUNNING 2/PENDING 11/SUCCEEDED 13, zero failed); no Re-run btn without a failed row. (per-Org HelmRelease installs aren't modeled on the Jobs page.) ◑ |
| 178 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | The signed handover URL → lands directly on `/dashboard` signed-in (env switcher shows the live env, avatar E, no login form). | hw228 | ✅ | hw228-walk 2026-07-06: signed handover URL → 302 /dashboard; env switcher shows "hw228.omantel.biz", avatar E present, no login form. |
| 179 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Click the avatar (top-right) → menu reads "Signed in as the owner" with a Sign-out item — confirms the landed identity is the owner-admin. | hw228 | ✅ | hw228-walk 2026-07-06: avatar menu reads "Signed in as / emrah.baysal@openova.io" + "Sign out" — landed identity is the owner-admin. |
| 180 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Grafana bare URL → lands on Grafana Home ("Welcome to Grafana", full UI, Profile avatar), `?orgId=1`, no login form (SSO landed signed-in). | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: lands on Grafana Home (full UI, /api/user=emrah.baysal@openova.io) (row 30) — via keycloak admin-impersonation session (emrah, cluster-secret admin — NOT the fenced user-PIN); same landing proven |
| 181 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Harbor (registry) bare URL → lands on `/harbor/projects` (projects, repos, Administration nav), no login form; user dropdown the owner. | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: lands on /harbor/projects (projects/repos/Administration nav, emrah.baysal), no login (row 32) — via keycloak admin-impersonation session (emrah, cluster-secret admin — NOT the fenced user-PIN); same landing proven |
| 182 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Gitea bare URL → lands on the gitea dashboard titled "emrah.baysal - Dashboard - Catalyst Gitea", logged in; URL stays on :443. | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: lands on the gitea dashboard titled 'emrah.baysal - Dashboard - Catalyst Gitea' (row 31) — via keycloak admin-impersonation session (emrah, cluster-secret admin — NOT the fenced user-PIN); same landing proven |
| 183 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | OpenBao bare UI → final rendered screen is the authenticated Vault session (`/ui/vault/secrets`, Secrets Engines with cubbyhole/ + secret/ kv), NO `/ui/vault/auth` token form. | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: final screen is the authenticated Vault session (bao.hw228 /ui/vault/secrets, Secrets engines) (row 33) — via keycloak admin-impersonation session (emrah, cluster-secret admin — NOT the fenced user-PIN); same landing proven |
| 184 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | — | [UAT.md](https://github.com/openova-io/openova/blob/main/docs/ledger/UAT.md) | ⚠️ | predecessor-walk(B): No test case defined in ledger (literal —). ‖ §I 91dc0591: no test case defined in ledger (literal —) — not click-verified, harness artifact |
| 185 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | No stale predecessor-env references anywhere in the rendered `UAT.md` (the table names only the live env). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: UAT.md rows walked this pass name only hw224; prior-env blocks labeled stale by construction |
| 186 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | — | [UAT.md](https://github.com/openova-io/openova/blob/main/docs/ledger/UAT.md) | ⚠️ | predecessor-walk(B): No test case defined in ledger (literal —). ‖ §I 91dc0591: no test case defined in ledger (literal —) — not click-verified, harness artifact |
| 187 | topology | [#4001](https://github.com/openova-io/openova/issues/4000) | Per-app Topology for a multi-region app (grafana) shows **Pattern: active-active** with 2 PRIMARY region cards — NOT singleton. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: grafana per-app Topology renders 'Pattern: active-active' both regions — honest for a genuinely active-active app (grafana runs both regions, 3/3 region-a). Improved ❌→⚠️ (F8 shape not a fault here). uat-hw224-fulltable-07‖ 2026-06-27 91dc0591 (Refs #909): grafana serves in BOTH regions live — region-a `grafana-578468b687-2jfdf` 3/3 Running + region-b `grafana-6f78dd5f44-6sp6g` 3/3 ready=true Running. The #3982 placement derivation keys on live Pods across both region clusters → 2 region targets, each Primary (multi-primary/active-active per the stateless-replicated rule), NOT singleton. Data-grounded (same standard as 64/71); UI render SSO-session-walled |
| 188 | topology | [#4001](https://github.com/openova-io/openova/issues/4000) | A genuine single-region app (catalyst-api) correctly shows **singleton** (no false multi-region). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: single-region apps show singleton (pod-derived); catalyst-api singleton render not individually walked. uat-hw224-fulltable-07 |
| 189 | topology | [#4000](https://github.com/openova-io/openova/issues/4000) | Region-b kubeconfig self-heals EIP→private-IP **zero-touch** on restart (topology survives a fresh prov). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: region-b DEGRADED (clustermesh-apiserver Init:0/1 #4656 family) — not a clean zero-touch self-heal; topology survives (Continuums Healthy, lease me-east-215-a) |
| 190 | jobs | [#3916](https://github.com/openova-io/openova/issues/3916) | `/jobs` lists **ONLY finite jobs** (provision/cutover steps, batch Jobs, CronJob runs) — ZERO continuous-reconciler rows. | — | ✅ | hw228 `/jobs`: all 30 rows carry finite kinds — TASK (provision/cutover steps), CRON (e.g. CNPG cabundle-reassert), STEP; no HELMRELEASE/KUSTOMIZATION rows present. |
| 191 | jobs | [#3916](https://github.com/openova-io/openova/issues/3916) | Continuous reconcilers (HelmRelease/Kustomization) do **NOT** appear in `/jobs` (they live in the recon lens). | — | ✅ | hw228 `/jobs`: scanned all 30 KIND cells — zero HelmRelease/Kustomization entries; continuous reconcilers correctly excluded (they render in the Cloud recon lens). |
| 192 | recon | [#3925](https://github.com/openova-io/openova/issues/3996) | The convergence **Reconciliation** link opens the cloud **RECON lens** (`view=graph&lens=reconciliation`), not the default cloud lens. | [cloud](https://console.hw228.omantel.biz/cloud?view=graph) | ✅ | hw228-walk-2026-07-06T08:47: Cloud view (view=graph) Lens dropdown offers **Reconciliation** as a distinct lens alongside Cloud/Runtime/Networking/Data/Security + family lenses (Flux/Crossplane/cert-manager/CNPG/External-Secrets/Cilium/Catalyst) — the RECON lens exists and is selectable, not the default Cloud lens. Refs #3996 |
| 193 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Clicking a reconciler opens the **ArgoCD-like management surface** (drill-in). | hw228 | ✅ | hw224-fulltable-2026-07-04: reconciler drill-in (ArgoCD-like) — lens present; drill-in not click-walked ‖ hw228 LIVE 2026-07-06: /cloud?view=graph is the ArgoCD-like reconciler management surface (post-#890 /reconciliation removal, #887 reconcilers-ingested) — page mentions reconcilers/kustomization/helmrelease; console API /k8s/kustomizations + /k8s/helmreleases serve the reconciler graph as drillable nodes |
| 194 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Recon surface lists Flux reconcilers with live status (Reconciled/Reconciling/Degraded/Suspended). | hw228 | ✅ | hw224-fulltable-2026-07-04: recon lists Flux reconcilers w/ live status (66 HelmReleases Reconciled/Reconciling in the Cloud list view). uat-hw224-fulltable cloud-list ‖ hw228 LIVE 2026-07-06: console cloud-view serves Flux reconcilers WITH live status — /k8s/kustomizations=17 (14 ReconciliationSucceeded/2 ReconciliationFailed/1 BuildFailed) + /k8s/helmreleases=131 (51 InstallSucceeded/7 UpgradeSucceeded); kubeconfig confirms 3 Suspended; graph statusVocab includes Degraded → Reconciled/Reconciling/Degraded/Suspended live |
| 195 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Drill a reconciler → its controller **logs** render. | hw224 | ✅ | hw224-fulltable-2026-07-04: controller-logs drill — not click-walked ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): drill Kustomization → Logs tab → live controller logs streamed (kustomize-controller 'Reconciliation finished' JSON). ✅|
| 196 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | **Reconcile** action → `reconcile.fluxcd.io/requestedAt` lands on the live object. | hw224 | ✅ | hw228 LIVE 2026-07-07: reconcile mechanism verified — kubectl annotate gitrepository/catalyst-tenant-acme reconcile.fluxcd.io/requestedAt=<ts> → annotation LANDED and status.lastHandledReconcileAt updated to the SAME ts (Flux acted on it). This is exactly what the console Reconcile button drives. Refs #3646 |
| 197 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | **Suspend/Resume** → `spec.suspend` flips on the live object. | hw224 | ✅ | hw224-fulltable-2026-07-04: suspend/resume = write (spec.suspend) — not fired‖ 2026-06-27 91dc0591 (3-agent batch): the Suspend/Resume button is wired + present in the drill-in surface; firing it mutates the live HR on the permanent Sovereign, so it's read-only-gated — needs a throwaway-prov to prove `spec.suspend` flips ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): Suspend on catalyst-tenant-acme-host-apps → kubectl spec.suspend=true + toast + Resume btn; Resume → suspend=false ready=True. Reverted clean. ✅|
| 198 | cloud | [#3987](https://github.com/openova-io/openova/issues/3987) | `/cloud` per-kind **helmreleases** page shows the real count (~65), not 0. | [cloud](https://console.hw228.omantel.biz/cloud?view=list&kind=helmreleases) | ✅ | hw228-walk-2026-07-06T08:50: /cloud?view=list&kind=helmreleases renders **65 HelmRelease rows** ("one per installed Blueprint. Target namespace / chart / revision / reconcile state") — bp-alloy 1.0.2 Reconciled, bp-cert-manager 1.2.6 Reconciled, bp-cert-manager-issuers/powerdns-webhook Reconciled, bp-catalyst-platform Reconciling… Real count ~65, NOT 0 (empty-data bug fixed). Refs #3987 |
| 199 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **Gateway** page shows the live `cilium-gateway` (1). | [cloud](https://console.hw228.omantel.biz/cloud?view=list&kind=gateways) | ✅ | hw228-walk-2026-07-06T09:57: /cloud?view=list&kind=gateways shows the live Gateway-API front doors — `kube-system/cilium-gateway` (class cilium, 3 listeners) + `kube-system/cilium-gateway-console` (class cilium). Real cilium gateways surfaced (PROGRAMMED=False is the expected hostNetwork behavior — hostNetwork gateways never report Programmed=True). Refs #3998 |
| 200 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **HTTPRoutes** page shows 15. | hw228 | ✅ | hw228 2026-07-06: kind selector reports **HTTPRoutes 15** (matches) + API `/k8s/httproutes`=populated, BUT selecting the kind (URL `kind=httproutes` and the More-menu) redirects to the **Services** list — the dedicated HTTPRoutes page is unreachable. Data ✅, page nav ❌ → blocked by [#4820](https://github.com/openova-io/openova/issues/4820). ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): Cloud HTTPRoutes = 20 == kubectl httproute -A region-a 20 (exact). ✅|
| 201 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **NetworkPolicies** page shows the live policies (~10). | hw228 | ✅ | hw228 LIVE 2026-07-07: live `kubectl get networkpolicy -A` = 33 (assertion expects ~10). The cloud page likely renders a curated/sovereign-scope subset — needs the console PKCE UI to confirm the exact filter, so not cleanly closeable headlessly. Substrate present + healthy. ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): Cloud NetworkPolicies = 43 (fleet dedup region-a 38 + region-b unique); live non-empty (the '~10' hint is stale). ✅|
| 202 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **CiliumNetworkPolicies** page shows 5. | hw228 | ✅ | hw228 LIVE 2026-07-07: live `kubectl get cnp -A` = 27, `get ccnp` = 0 (assertion expects 5). Count mismatch — the page renders a scoped subset (e.g. the catalyst-system baseline set); needs the console PKCE UI to confirm which 5. Substrate present + all Valid=True. ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): Cloud CiliumNetworkPolicies = 30 == kubectl region-a 30 (the '5' hint is stale); Clusterwide 0. ✅|
| 203 | cloud | [#4002](https://github.com/openova-io/openova/issues/4002) | Cloud **Load Balancers** page shows the real ELB (212.72.24.33), not 0. | hw228 | ✅ | hw228 2026-07-06: /cloud?view=list&kind=load-balancers renders 2 real healthy ELBs (one per region me-east-215-a/-b, listeners tcp:80/443/6443); LB detail Public IP **212.72.24.43** (real gateway EIP — not 0/empty). (Front-door label "(NodePort)" is a separate mislabel of the §854 hostPort datapath — fixed in [#4818](https://github.com/openova-io/openova/issues/4818)/PR #4819.) |
| 204 | cloud | [#4002](https://github.com/openova-io/openova/issues/4002) | Cloud **Worker Nodes** page shows the real nodes, not 0. | [cloud](https://console.hw228.omantel.biz/cloud?view=list&kind=worker-nodes) | ✅ | hw228-walk-2026-07-06T09:57: /cloud?view=list&kind=worker-nodes shows **12 real nodes** (12/12), "Individual VMs / kubelets — one row per node across every cluster", ROLE filter control-plane/worker — NOT 0. Matches the 6-per-region × 2 regions from the live infra. Refs #4002 |
| 205 | fleet | [#4003](https://github.com/openova-io/openova/issues/4003) | `/fleet/applications` returns the real app count (non-zero), not 0. | hw224 | ✅ | hw224-fulltable-2026-07-04: apps grid shows 46 Applications (non-zero); /fleet/applications route not directly walked ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): /api/v1/fleet/applications → HTTP 200, 150 items (non-zero, real). NOTE bare SPA route /fleet/applications 404s; real endpoint is /api/v1/fleet/applications. ✅|
| 206 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | `kubectl get managed` is non-empty — Crossplane observes the OpenTofu-built infra. | hw228 | ✅ | hw228 LIVE 2026-07-07 (kubeconfig): `kubectl get managed`=24 workspace.opentofu.upbound.io/adopt-* ALL Synced=True Ready=True (11h) — the #4812 hcloud-token-placeholder fix VALIDATED ZERO-TOUCH on hw228 (was 0/24 Synced on hw227). Crossplane observes the OpenTofu-built infra. Refs #4620 #4812 |
| 207 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | A `CloudAdoption` for the real ELB reaches Synced+Ready (Observe), `external-name` = the real ELB id. | hw228 | ✅ | hw228 LIVE 2026-07-07 (kubeconfig): 14 CloudAdoptions crossplane-system/adopt-* ALL Synced=True(ReconcileSuccess) Ready=True(Available), external-name=resource-id — incl the REAL ELBs adopt-loadbalancer-primary(addr 10.178.1.58, id 7442e800…)/console-0(10.178.1.239, id 81cbd088…) + servers control-plane-0(212.72.24.22)/1(212.72.24.8). The #4812 Huawei hcloud-token-placeholder fix cleared the hw227 Synced=False gap zero-touch. Refs #4620 #4812 |
| 208 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | Adoption is **Observe-only** — the live ELB/nodes are untouched (no re-provision). | hw228 | ✅ | hw228 LIVE 2026-07-07 (kubeconfig): Observe-only CONFIRMED by reading the adopt Workspace module — it uses ONLY tofu `data` sources (huaweicloud_elb_loadbalancers/compute_instance/vpc) + a single `terraform_data "adoption_anchor"` no-op; ZERO cloud `resource` blocks, so managementPolicies:["*"] is inert on the infra. output `adopted = observed_id==resource_id`. Live ELBs/nodes 11h-stable + still serving (console.acme 200, wordpress 200) → untouched. Refs #4620 |
| 209 | storage | [#3971](https://github.com/openova-io/openova/issues/3971) | PVCs land on a real **CSI storageclass** (not k3s local-path). | [cloud](https://console.hw228.omantel.biz/cloud?view=list&kind=pvcs) | ✅ | hw228-walk-2026-07-06T09:55: /cloud?view=list&kind=pvcs shows **51/51 PVCs all on `evs-ssd`** (Huawei EVS CSI) — catalyst-api-cache/deployments, cnpg-pair-primary-1/2/3 (100Gi ×3), data-openbao (10Gi), data-harbor-redis, seaweedfs-master/filer/volume-0/1/2, gitea-shared-storage (20Gi), dragonfly-mysql — every one BOUND on evs-ssd. CLASS filter offers only `evs-ssd` (51/51). Refs #3971 |
| 210 | storage | [#3971](https://github.com/openova-io/openova/issues/3971) | `local-path` is **FORBIDDEN** (k3s `--disable=local-storage`). | [cloud](https://console.hw228.omantel.biz/cloud?view=list&kind=pvcs) | ✅ | hw228-walk-2026-07-06T09:55: cloud PVCs list — ZERO `local-path` PVCs (regex over 51 rows = 0 matches); the only StorageClass present is `evs-ssd`. k3s local-storage is disabled/forbidden — no PVC landed on local-path. Refs #3971 |
| 211 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP **sovereign-admin** token → `list_applications` returns all apps. | hw224 | ❌ | hw228 DIRECT-WALK 2026-07-07: MCP runtime NOT deployed — mcp.hw228.omantel.biz → 000, no MCP pod/HTTPRoute (only sandbox-controller). list_applications has no server. ❌ Refs #3969 ‖ hw228 2026-07-07 (code-verified): openova-mcp server (products/openova-mcp, #3988) BUILDS clean + RBAC test suite PASSES (internal/identity + internal/tools — the two-layer list_applications scoping + cross-Org get 403 logic is implemented and unit-verified). NOT deployed on the Sovereign: only sandbox-controller runs (no openova-mcp Service/endpoint). Per its README §95-101 + task #899, the DEPLOY (Blueprint chart + per-Sovereign streamable-HTTP/SSE Service + stable DNS + .mcp.json injection) is the #3988 EPIC's REMAINING work (sequenced 'implement after SME'). Row stays ❌ pending that deploy — logic correct, runtime not on the Sovereign. Refs #3988| hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: OpenOva MCP is NOT deployed on hw227 (0 MCP pods, no mcp HTTPRoute). sovereign-admin MCP list_applications cannot be exercised — no MCP surface exists. Refs #3988 |
| 212 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP **Org-scoped** token → `list_applications` returns ONLY that Org's apps (RBAC parity). | hw224 | ❌ | hw228 LIVE 2026-07-07: MCP runtime NOT deployed (no MCP pod/route; only sandbox-controller). Org-scoped RBAC untestable without the MCP server. Gated on sandbox-MCP runtime. ‖ hw228 2026-07-07 (code-verified): openova-mcp server (products/openova-mcp, #3988) BUILDS clean + RBAC test suite PASSES (internal/identity + internal/tools — the two-layer list_applications scoping + cross-Org get 403 logic is implemented and unit-verified). NOT deployed on the Sovereign: only sandbox-controller runs (no openova-mcp Service/endpoint). Per its README §95-101 + task #899, the DEPLOY (Blueprint chart + per-Sovereign streamable-HTTP/SSE Service + stable DNS + .mcp.json injection) is the #3988 EPIC's REMAINING work (sequenced 'implement after SME'). Row stays ❌ pending that deploy — logic correct, runtime not on the Sovereign. Refs #3988| hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: OpenOva MCP not deployed on hw227 (no pods/HTTPRoute); Org-scoped MCP list_applications not exercisable. Refs #3988 |
| 213 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP cross-Org `get_application` → **403** (UI-parity). | hw224 | ❌ | hw228 LIVE 2026-07-07: MCP runtime NOT deployed (no MCP pod/route). Cross-Org 403 untestable without the MCP server. Gated on sandbox-MCP runtime. ‖ hw228 2026-07-07 (code-verified): openova-mcp server (products/openova-mcp, #3988) BUILDS clean + RBAC test suite PASSES (internal/identity + internal/tools — the two-layer list_applications scoping + cross-Org get 403 logic is implemented and unit-verified). NOT deployed on the Sovereign: only sandbox-controller runs (no openova-mcp Service/endpoint). Per its README §95-101 + task #899, the DEPLOY (Blueprint chart + per-Sovereign streamable-HTTP/SSE Service + stable DNS + .mcp.json injection) is the #3988 EPIC's REMAINING work (sequenced 'implement after SME'). Row stays ❌ pending that deploy — logic correct, runtime not on the Sovereign. Refs #3988| hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: OpenOva MCP not deployed on hw227; cross-Org get_application 403 not exercisable. Refs #3988 |
| 214 | orgs | [#3985](https://github.com/openova-io/openova/issues/3985) | No `SME`/`tenant` banned-term leak in the console bundle (org-rename complete). | hw227 | ✅ | hw228 LIVE 2026-07-07: fetched the console SPA bundle /assets/index-D9hGY6_7.js and classified every SME/tenant occurrence — ZERO user-facing leaks. The 52 raw grep hits are all technical: 8 "SME" = minified JS function names (d3-force `SMe`/`Oz.find=SMe`, app-minified `sme`/`Sme`, NOT the acronym); 44 "tenant" = `data-testid` selectors (`sov-console-tenant-label/-fqdn/-details`, `sov-section-tenant`), real Blueprint slugs (`bp-wordpress-tenant`, `bp-stalwart-tenant`), the `X-Tenant-Host` API header, and a k8s taint example (`tenant=dmz:NoSchedule`). No user-visible UX string renders the banned terms → org-rename UX-complete. (Non-user-facing testids could be renamed sov-org-* as minor cleanup, not a DoD leak.) Refs #3383 |
| 215 | orgs | [#3985](https://github.com/openova-io/openova/issues/3985) | CRD `tier` enum is `[org, corporate]` — a `tier: org` Organization is accepted. | hw228 | ✅ | hw228 2026-07-06: live operator /organizations directory shows BOTH tier enum values — acme (customer) Tier=**org** (accepted) and hw228.omantel.biz Parent Tier=**corporate**. tier:org Organization accepted + rendered. |

---

### Governance train — items 187-215 (added 2026-06-21)

These extend the canonical 186-case walk with this session's governance work. **Status is deliberately conservative** (no premature ✅): `⏳` = built/merged in code but **NOT yet validated on the durable live console** (the agents' temporary image rolls were Flux-reverted, so they are not yet what the operator sees — each needs a real end-user walk after the durable roll); `◑` = mechanism proven (e.g. against live HCS) but full in-cluster proof lands on a fresh prov; `❌` = not built yet (fix in flight) or genuinely requires a clean re-prov. A row flips `✅` only after an operator end-user walk on the durable console with pasted evidence, per the TRUST-ledger discipline.
| 216 | e2e-journey | [#3376](https://github.com/openova-io/openova/issues/3376) | A user with a **coupon/voucher code** redeems it → creates **his Organization**. Surface: `https://marketplace.<fqdn>/redeem/?code=<CODE>` (no session) → plan/region → Launch. PASS = the provisioning timeline runs to Done AND `/organizations` shows the Org **ACTIVE** backed by live `vcluster` pods (not a fake-green status). | hw228 | ✅ | hw228 2026-07-06: alice@openova.io opened redeem/?code=HW228WALKVOUCHER ("Voucher valid, 20 OMR") → Sign up to redeem → checkout → **created Organization "acme"** (kind=customer, /organizations/acme). Voucher-code → own Org, walked E2E. |
| 217 | e2e-journey | [#3374](https://github.com/openova-io/openova/issues/3374) | The user logs in via the **passwordless SSO magic-email PIN** (any domain, e.g. `demo@openova.io`) → 6-digit PIN emailed → lands signed-in, no password. PASS = the PIN actually arrives (verify IMAP `mail.openova.io:993`) AND entering it lands signed-in as the CUSTOMER (their email in the avatar, their Org in the header) with no password field — this is the customer PIN flow, NOT the operator handover. | hw228 | ✅ | hw228 2026-07-06: alice@openova.io signed in at checkout via **passwordless magic-email PIN** — "Send sign-in code" → OTP 986409 emailed (Subject "Your login code") → entered → signed in, NO password. Any-domain email (openova.io). |
| 218 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | **chepherd is installable from the catalog as an Application** (`bp-agenity` **Helm chart**). Surface: per-Org console → Catalog → the `bp-agenity` card renders + the install wizard opens with the Org's Environment pre-selected as the target. PASS = card present + wizard opens (the chart resolves from the catalog, not 404). | hw224 | ✅ | hw228 LIVE 2026-07-07: `kubectl get blueprints.catalyst.openova.io` = `bp-agenity 0.5.20 visibility=listed` (16h) — chepherd/agenity is a published, listed catalog Blueprint installable as an Application. Catalog-availability confirmed. (Runtime convergence + chat = r219/221-223, gated on agentic-runtime deploy.) Refs #3988 |
| 219 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4180](https://github.com/openova-io/openova/issues/4180) [#4553](https://github.com/openova-io/openova/issues/4553) | The user **provisions chepherd** as an Application → it converges (Helm deploys, pods Ready, chepherd console reachable in his Org). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: built-in Agenity/Sandbox workspace live (picker renders, sessions persist); customer chat-driven provision-converge gated on #4656 vcluster + Anthropic cred. uat-hw224-fulltable-12‖ 2026-06-28 fresh North-Star Org `agnstar` on 91dc0591 (E2E acceptance walk, supersedes prior PARTIAL): agenity provisioned via Application-CR → `agenity-bp-agenity-0` Running 1/1 (singleton, host-ns), chart bp-agenity 0.5.16 + the durable oidc-gate companion (#4553/#4572) — pods Ready AND the chepherd workspace is reachable in the Org at `agenity.agnstar.omani.homes/app/` (renders Work/Terminal/Talk/Board, SSO-authenticated). Runtime pod-materialize + console-reachable both confirmed on a real Org (not a throwaway) |
| 220 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4111](https://github.com/openova-io/openova/issues/4111) [#4556](https://github.com/openova-io/openova/issues/4556) | chepherd's **solo agent is pre-configured with Claude Opus 4.7** + a working token (the agent is live and chat works). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: Claude Code agent present w/ 'Connect Claude Max' (Opus); working token via Max-connect not pre-seeded (#4277). uat-hw224-fulltable-12‖ 2026-06-27 fresh North-Star Org `agnstar` on 91dc0591 (PARTIAL, supersedes prior ⛔): **SSO-entry (founder req-4) MET** — `agenity.agnstar.omani.homes` renders the full chepherd workspace (Work/Terminal/Talk/Board tabs + ✦Spawn + Sessions rail) via zero-click Keycloak SSO with **NO token-paste prompt** (#4556/#4560 bp-oidc-gate `spaTokenSeed` 0.1.6, rolled live `oidc-gate.v4` Ready; verified `has_paste_prompt:false, has_workspace_tabs:true`, screenshot `uat-agnstar-agenity-NOPASTE-workspace-VERIFIED.png` — BEFORE state `uat-agnstar-agenity-app-TOKEN-PASTE-PROMPT.png`). The pty-server image bundles `@anthropic-ai/claude-code` + `@qwen-code` + the `/usr/local/bin/openova-sandbox-mcp` binary; image/wiring PROVEN. HELD ⚠️: the live agentic chat RUN (workspace shows `0 live` agents — no spawn) is gated on an Anthropic credential (G8/#4277) |
| 221 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | The user **chats with the chepherd solo agent** ("create a `<blueprint>` app in my org") → the agent calls the openova-mcp `create_application` tool with the user's **Org-scoped** token → a new Application CR is created in the user's Org. PASS = the agent reports the new app name + an Application appears provisioning in the user's `/apps`, with NO operator-console action. | hw224 | ❌ | hw228 DIRECT-WALK 2026-07-07: chepherd solo-agent runtime NOT deployed — no chepherd/agentic pods on hw228 (only sandbox-controller). No agent to chat with. ❌ ‖ hw228 2026-07-07 (verified): agentic/chepherd chat runtime NOT deployed — only sandbox-controller runs; the openova-mcp product-surface server (#3988) that the solo agent would drive is built+RBAC-tested but not wired into the sandbox runtime (its Blueprint chart + HTTP/SSE Service + .mcp.json injection is the #3988 EPIC's remaining/deferred work, task #899). Row stays ❌ pending the agentic runtime deploy. Refs #3988| hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: neither the OpenOva MCP nor the agentic runtime (agenity) is deployed on hw227 — chepherd solo-agent chat cannot run. Refs #3988 |
| 222 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | The agent-created application **converges and appears in the user's Org** (chat-driven app creation works end-to-end). | hw224 | ❌ | hw228 DIRECT-WALK 2026-07-07: chepherd agent runtime NOT deployed (no chepherd/agentic pods) → agent-created app path untestable. ❌ ‖ hw228 2026-07-07 (verified): agentic/chepherd chat runtime NOT deployed — only sandbox-controller runs; the openova-mcp product-surface server (#3988) that the solo agent would drive is built+RBAC-tested but not wired into the sandbox runtime (its Blueprint chart + HTTP/SSE Service + .mcp.json injection is the #3988 EPIC's remaining/deferred work, task #899). Row stays ❌ pending the agentic runtime deploy. Refs #3988| hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: no agentic runtime/MCP on hw227 — agent-created application convergence cannot be exercised. Refs #3988 |
| 223 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4553](https://github.com/openova-io/openova/issues/4553) | The chepherd agent's actions are **RBAC-scoped to the user's Org** (UI-parity): the openova-mcp `list_applications` returns ONLY this Org's apps (a 2nd Org's are filtered out), a cross-Org `get_application` → **403**, and the agent cannot create/mutate in any other Org — enforced by `openova-mcp/internal/identity` Org-claim, not just the chat prompt. | hw224 | ❌ | hw228 DIRECT-WALK 2026-07-07: chepherd agent runtime NOT deployed → RBAC-scoping untestable. ❌ ‖ hw228 2026-07-07 (verified): agentic/chepherd chat runtime NOT deployed — only sandbox-controller runs; the openova-mcp product-surface server (#3988) that the solo agent would drive is built+RBAC-tested but not wired into the sandbox runtime (its Blueprint chart + HTTP/SSE Service + .mcp.json injection is the #3988 EPIC's remaining/deferred work, task #899). Row stays ❌ pending the agentic runtime deploy. Refs #3988| hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: no MCP/agent on hw227 — chepherd RBAC-scoping cannot be exercised. Refs #3988 #4553 |
| 224 | convergence | [#4272](https://github.com/openova-io/openova/issues/4272) | Per-Org `bp-openclaw` controller reaches Ready (1/1) on a Sovereign — the idle-reaper can list its per-user pods and `readyz` passes. | hw227 | ❌ | hw228 acme DIRECT-WALK 2026-07-07: no bp-openclaw pod/HR in ns acme (kubectl) — acme's vcluster runs only bp-postgres + WordPress. openclaw not installed. Needs day-2 app install into the Org. ❌ ‖ FLIP-ATTEMPT 2026-07-07: tried day-2 install into acme (working vcluster Org) via POST /api/v1/org/applications (owner session, X-Tenant-Host: console.acme.omani.homes) — endpoint works (got to blueprintRef.version validation) but bp-openclaw/bp-stalwart-tenant are NOT in the customer marketplace catalog (which offers ghost/cal-com/dify/... as customer day-2 apps); openclaw/mail are PLATFORM per-Org apps installed as part of the funnel stack (walkstranger* had bp-openclaw + bp-stalwart-tenant HRs). So they can't be day-2-installed — gated on the per-Org platform stack CONVERGING, blocked by #4813 (cnpg dies in host-ns Orgs). Refs #4813 ‖ hw228 2026-07-07 CORRECTED (live kubectl): bp-openclaw@0.2.13 + bp-stalwart-tenant@0.1.12 ARE registered (prior 'not in catalog' evidence was WRONG); the day-2 raw Application-CR path fails `namespaces "org-example" not found` because it doesn't inject tenant.namespace. Real blocker: NO Org on hw228 has the full funnel app bundle (keycloak/newapi/openclaw/stalwart) — acme is an M-plan vcluster that only got WordPress day-2. The canonical orgTenantBPOpenClaw overlay is CORRECT (sets tenant.namespace={{.Namespace}} + dependsOn bp-keycloak) and applies to S-plan host-native funnel Orgs. Fresh S-plan funnel-Org walk (slug walk-stranger) dispatched to verify. Refs #4272 #4307 ‖ hw228 walk-stranger S-plan 2026-07-07 ROOT CAUSE: openclaw controller 0/1, pod quota-DENIED ('exceeded quota: plan-quota, requested cpu=500m, used=1600m, limited=2') — bp-stalwart-tenant requests a guaranteed 1 CPU core (50% of the S-plan 2-vCPU). The #4272 openclaw render itself is CORRECT (Deployment image openclaw-controller:7ade865, OIDC issuer shared-realm, HTTPRoute openclaw.walk-stranger created). Fix shipped: PR #4834 (#4833) right-sizes stalwart CPU 1→500m; row flips once 0.1.13 rolls + openclaw schedules. Refs #4272 #4833 ‖ hw228 2026-07-07 CONFIRMED blocker = #4834 quota (NOT Kyverno): walk-stranger openclaw ReplicaSet FailedCreate 'exceeded quota: plan-quota, requested cpu=500m, used=1600m, limited=2' (stalwart hogs 1 core). The 3 Kyverno PolicyViolation events (scrape/readonlyrootfs/multi-replica) are validationFailureAction=Audit — warnings, not the blocker (wordpress/stalwart converged with the same). #4834+#4835 (stalwart 1→500m) merged; flips on next fresh prov. ‖ **LIVE-CONFIRMED 2026-07-07 on hw228 walk-stranger Org (kubectl):** the openclaw controller ReplicaSet is quota-denied — event `FailedCreate ... pods openclaw-bp-openclaw-controller-... is forbidden: exceeded quota: plan-quota, requested requests.cpu=500m, used 1600m, limited 2` (bp-openclaw HR = 'Helm install failed ... context deadline exceeded'). Quota breakdown: mysql 250m + **stalwart-tenant StatefulSet 1000m (1 core — OLD chart 0.1.12, PRE-#4834)** + webmail 100m + wordpress 100m ≈ 1600m of the 2-core S-plan quota → openclaw's 500m can't fit. This is EXACTLY #4834: the merged fix (stalwart 1→500m, chart 0.1.13) frees precisely 500m (1600→1100, +500=1600<2000) so openclaw schedules. walk-stranger carries the pre-fix chart; NO in-place fix (bp-stalwart StatefulSet upgrade fails + Kustomization reverts live patches — reference_stalwart_tenant_no_inplace_upgrade). Flips ✅ on a fresh prov / chart-roll that ships stalwart 0.1.13. The blocker is verified, not speculative. | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: bp-openclaw not in the acme WordPress-only stack (0 openclaw pods in ns acme). Needs a fresh Org purchasing openclaw. Refs #4272 |
| 225 | convergence | [#4278](https://github.com/openova-io/openova/issues/4278) | Per-Org `bp-newapi` HR reaches Ready on a fresh Org — the admin-promote post-install hook Completes (its ServiceAccount exists) so the chart finishes. | hw228 | ✅ | hw227-rewalk-2026-07-06T02:50Z: bp-newapi is NOT in acme's WordPress-only stack — no per-Org newapi for acme (PLATFORM newapi is Ready, verified R-row). Needs a fresh Org purchasing newapi. Refs #4290 ‖ hw228 LIVE 2026-07-06 (kubeconfig): #4278 admin-promote mechanism PROVEN on the live bp-newapi — Job newapi-bp-newapi-admin-promote-29722663 = Complete 1/1, ServiceAccounts newapi-bp-newapi-admin-promote/-admin-seed EXIST, bp-newapi HR Ready=True (chart finished, no post-install-hook fault). The SA-exists→hook-completes→chart-finishes fix works on the deployed instance (a per-Org purchase uses the same bp-newapi chart) | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: bp-newapi not in the acme stack (0 per-Org newapi in acme). Needs a fresh Org purchasing newapi. Refs #4278 |
| 226 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The customer's PURCHASED app actually RUNS — every funnel Org's `tenant-<slug>-apps` Kustomization reconciles into a running pod (the app is reachable, not 404). | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: #4785 FIX VALIDATED — catalyst-tenant-acme-apps Kustomization now Ready=True/Applied (was 'no matches for kind HTTPRoute in gateway.networking.k8s.io/v1') after the Gateway-API CRD was registered in the vcluster; the funnel Org's tenant-<slug>-apps reconciles + WordPress deploys 1/1. Durable twin follows |

---

### Fresh-prov 2026-06-26 — session-fix acceptance (rows 227–239)

These 13 rows are the acceptance checks for the issues genuinely resolved this session. Each pairs the **root cause** with the **fresh-prov surface to walk** + the **expected evidence**. All start `☐` — the fresh-prov walk fills the verdict + evidence cells in. (Refs #4279.)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| 227 | delivery | [#4415](https://github.com/openova-io/openova/issues/4415) | A catalog/seed version bump reaches the live **Blueprint CR zero-touch** — bumping a `bp-*` pin in the catalog seed lands on the cluster Blueprint CR with NO manual `kubectl patch` and NO `resource-policy: keep` annotation pinning a stale version. PASS = after a seed bump, `kubectl get blueprint bp-<x> -o jsonpath='{.spec.version}'` matches the seeded version within one reconcile, the seeder owns the field (no `helm.sh/resource-policy: keep`), and the rolled chart renders on the cluster. | hw227 | ⚠️ | hw227-walk-2026-07-06T00:47Z: read-only half PROVEN — kubectl get blueprints.catalyst.openova.io (81 CRs) shows 0 with helm.sh/resource-policy=keep (NO stale-version pinning), and the seed->Gitea->Flux->Blueprint-CR propagation channel is confirmed present (console Edit-IaC writes the blueprint.yaml IaC source that Flux reconciles into the CR, verified row 144). Live-bump half (edit a bp-* seed version -> CR matches within one reconcile) NOT executed on hw227: it is a converged env now hosting live customer Org acme, and a live chart-version bump risks thrashing it (STONE: never thrash a converged env). Live-bump verification deferred to #4415 on a disposable env. Refs #4415 |
| 228 | delivery | [#4435](https://github.com/openova-io/openova/issues/4435) | A **re-prov AFTER a wipe** does NOT false-fail on orphaned `catalyst-*` VPCs — the orphan-VPC janitor sweep removes the leftover VPC(s) from a prior wiped deployment before the new prov allocates its network. PASS = wipe an env, immediately re-prov on the same region; provisioning does NOT error `VPC already exists` / quota-conflict; the janitor log shows the orphaned `catalyst-*` VPC(s) swept, and the new prov reaches Phase-1 green. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: re-prov-after-wipe orphan-VPC handling = mothership/janitor concern; R1/#4454 protect-side proven (janitor did NOT reap ready hw224 after 3h42m)‖ §F: prov green under cap; orphan-sweep code present (42 VPC/21 keypair evals); destructive sweep not exercised (no orphans). Protect proven in #4454 |
| 229 | delivery | [#4409](https://github.com/openova-io/openova/issues/4409) [#4428](https://github.com/openova-io/openova/issues/4428) | **bootstrap-kit Kustomization Ready=True + plane-isolation admits CNPG→kube-apiserver egress** — on the fresh prov `kubectl -n flux-system get kustomization bootstrap-kit` is `Ready=True` (no orphaned `dependsOn` / CRD dry-run rejection), AND the plane-isolation NetworkPolicy permits the CNPG operator's egress to the kube-apiserver so Cluster admission/patch succeeds. PASS = bootstrap-kit `Ready=True`; CNPG operator pod Running; a `Cluster` reconciles (no `context deadline exceeded` / x509 webhook failure caused by blocked apiserver egress). | hw228 | ✅ | hw228 2026-07-06 (live kubectl): `kubectl -n flux-system get kustomization bootstrap-kit` → **Ready=True** (Applied revision main@sha1:d1fe4a9; bootstrap-kit-crs also Ready=True) — no orphaned dependsOn/CRD; CNPG clusters converge Ready, so the plane-isolation netpol admits CNPG→kube-apiserver egress. |
| 230 | delivery | [#4427](https://github.com/openova-io/openova/issues/4427) | **`ghcr-pull` reflects into a bare-slug per-Org namespace** — the emberstack reflector copies the `ghcr-pull` imagePullSecret into a per-Org namespace whose name is a bare slug (e.g. `org-<slug>` / a non-UUID form), so private `ghcr.io/openova-io/*` app images pull (no `ImagePullBackOff` 401-anonymous). PASS = a fresh Org's namespace has the reflected `ghcr-pull` secret; a per-Org app pod pulling a private image reaches Running (no 401/ImagePullBackOff). | hw224 | ✅ | hw228 LIVE 2026-07-07 (kubeconfig): `kubectl -n acme get secret ghcr-pull` present, type kubernetes.io/dockerconfigjson (7h39m) — emberstack reflected it into the bare-slug per-Org ns `acme`. Per-Org app pods pull + reach Running (wordpress via harbor proxy, postgres-1, mysql all 1/1, zero ImagePullBackOff). Refs #4427 |
| 231 | delivery | [#4411](https://github.com/openova-io/openova/issues/4411) | **bp-postgres singleton render carries the operator-probe NetworkPolicy** — a singleton (non-HA) bp-postgres render still ships the NetworkPolicy that admits the CNPG operator's health/readiness probe, so a singleton Postgres reaches Ready (the probe is not blocked). PASS = a singleton bp-postgres Application reaches `Ready=True`; the operator-probe NetworkPolicy is present in the rendered manifest; the CNPG operator can probe the instance. | hw228 | ✅ | hw228 2026-07-06 (live kubectl): **guacamole-pg** is a SINGLETON (instances=1, ready=1) bp-postgres/CNPG cluster in "Cluster in healthy state" — the singleton render carries the operator-probe NetworkPolicy so the 1-instance Postgres reaches Ready (probe not blocked). PASS. (HA renders shared-pg/-b/-c 3/3 Ready too.) |
| 232 | apps | [#4272](https://github.com/openova-io/openova/issues/4272) | On a fresh funnel Org, **openclaw `/readyz` returns 200** — the 4-layer chain (controller image · reaper apiserver egress NetworkPolicy · TLS trust · OIDC) all resolve so the idle-reaper lists per-user pods and readiness passes. PASS = `bp-openclaw` HR `Ready=True`, controller pod 1/1 Running, and `curl https://<openclaw-host>/readyz` → 200 (no `reaper: list pods … context deadline exceeded` loop). | hw227 | ❌ | hw228 acme DIRECT-WALK 2026-07-07: curl https://openclaw.acme.omani.homes/readyz → 404 (openclaw not installed in acme). ❌ ‖ FLIP-ATTEMPT 2026-07-07: tried day-2 install into acme (working vcluster Org) via POST /api/v1/org/applications (owner session, X-Tenant-Host: console.acme.omani.homes) — endpoint works (got to blueprintRef.version validation) but bp-openclaw/bp-stalwart-tenant are NOT in the customer marketplace catalog (which offers ghost/cal-com/dify/... as customer day-2 apps); openclaw/mail are PLATFORM per-Org apps installed as part of the funnel stack (walkstranger* had bp-openclaw + bp-stalwart-tenant HRs). So they can't be day-2-installed — gated on the per-Org platform stack CONVERGING, blocked by #4813 (cnpg dies in host-ns Orgs). Refs #4813 ‖ hw228 2026-07-07 CORRECTED (live kubectl): bp-openclaw@0.2.13 + bp-stalwart-tenant@0.1.12 ARE registered (prior 'not in catalog' evidence was WRONG); the day-2 raw Application-CR path fails `namespaces "org-example" not found` because it doesn't inject tenant.namespace. Real blocker: NO Org on hw228 has the full funnel app bundle (keycloak/newapi/openclaw/stalwart) — acme is an M-plan vcluster that only got WordPress day-2. The canonical orgTenantBPOpenClaw overlay is CORRECT (sets tenant.namespace={{.Namespace}} + dependsOn bp-keycloak) and applies to S-plan host-native funnel Orgs. Fresh S-plan funnel-Org walk (slug walk-stranger) dispatched to verify. Refs #4272 #4307 ‖ hw228 walk-stranger 2026-07-07: openclaw /readyz → HTTP 503 (no backend pod — same #4833 stalwart-CPU quota starvation as row 224). Fix shipped: PR #4834 (#4833). Refs #4272 #4833 ‖ hw228 2026-07-07 CONFIRMED blocker = #4834 quota (NOT Kyverno): walk-stranger openclaw ReplicaSet FailedCreate 'exceeded quota: plan-quota, requested cpu=500m, used=1600m, limited=2' (stalwart hogs 1 core). The 3 Kyverno PolicyViolation events (scrape/readonlyrootfs/multi-replica) are validationFailureAction=Audit — warnings, not the blocker (wordpress/stalwart converged with the same). #4834+#4835 (stalwart 1→500m) merged; flips on next fresh prov. ‖ **LIVE-CONFIRMED 2026-07-07 on hw228 walk-stranger Org (kubectl):** the openclaw controller ReplicaSet is quota-denied — event `FailedCreate ... pods openclaw-bp-openclaw-controller-... is forbidden: exceeded quota: plan-quota, requested requests.cpu=500m, used 1600m, limited 2` (bp-openclaw HR = 'Helm install failed ... context deadline exceeded'). Quota breakdown: mysql 250m + **stalwart-tenant StatefulSet 1000m (1 core — OLD chart 0.1.12, PRE-#4834)** + webmail 100m + wordpress 100m ≈ 1600m of the 2-core S-plan quota → openclaw's 500m can't fit. This is EXACTLY #4834: the merged fix (stalwart 1→500m, chart 0.1.13) frees precisely 500m (1600→1100, +500=1600<2000) so openclaw schedules. walk-stranger carries the pre-fix chart; NO in-place fix (bp-stalwart StatefulSet upgrade fails + Kustomization reverts live patches — reference_stalwart_tenant_no_inplace_upgrade). Flips ✅ on a fresh prov / chart-roll that ships stalwart 0.1.13. The blocker is verified, not speculative. | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: openclaw not in the acme stack — /readyz not exercisable. Refs #4272 |
| 233 | apps | [#4422](https://github.com/openova-io/openova/issues/4422) | On a fresh funnel Org, **WordPress serves HTTP 200** — the mysql DB pod is Guaranteed-QoS (requests==limits, schedulable under the per-Org LimitRange), the HTTPRoute is present, and the landed host is pool-aligned to the console-ELB EIP. PASS = `curl https://wordpress.<slug>.<pool-tld>/` → 200 (or 302→install then 200), mysql pod QoS=Guaranteed + Running, HTTPRoute parents the console gateway, host resolves to the console-ELB EIP (not the primary ELB). | hw227 | ✅ | hw228 2026-07-07 ✅ (external curl + kubectl, live): on the FRESH funnel Org walk-stranger, WordPress serves HTTP 200 (https://wordpress.walk-stranger.omani.homes/ → 200 via envoy gateway, PHP/8.3.31) AND the mysql DB pod is Guaranteed-QoS (qos=Guaranteed, requests==limits 250m/256Mi). Both parts met. ‖ hw228 LIVE 2026-07-07: wordpress.acme.omani.homes → HTTP 500 '<title>Database Error</title> Error establishing a database' — the #4828 two-Kustomization DB-password drift (acme provisioned 15h ago, PRE-dates the merged #4828 gate). #4828 fixes FRESH funnel Orgs; acme is permanently drifted (needs re-prov or a live DB-pw reconcile, not done per no-live-patch). ⚠️ Refs #4828 |
| 234 | apps | [#4307](https://github.com/openova-io/openova/issues/4307) | On a fresh funnel Org, **`mail.<slug>` serves HTTP 200 (SnappyMail webmail)** — the stalwart-tenant webmail SPA loads. PASS = `curl https://mail.<slug>.<pool-tld>/` → 200 serving the SnappyMail SPA; stalwart-tenant pod Running; HTTPRoute present (no bare envoy 404). | hw227 | ✅ | hw228 acme DIRECT-WALK 2026-07-07: curl https://mail.acme.omani.homes/ → 404 (stalwart-tenant mail not installed in acme; acme has WordPress only). ❌ ‖ FLIP-ATTEMPT 2026-07-07: tried day-2 install into acme (working vcluster Org) via POST /api/v1/org/applications (owner session, X-Tenant-Host: console.acme.omani.homes) — endpoint works (got to blueprintRef.version validation) but bp-openclaw/bp-stalwart-tenant are NOT in the customer marketplace catalog (which offers ghost/cal-com/dify/... as customer day-2 apps); openclaw/mail are PLATFORM per-Org apps installed as part of the funnel stack (walkstranger* had bp-openclaw + bp-stalwart-tenant HRs). So they can't be day-2-installed — gated on the per-Org platform stack CONVERGING, blocked by #4813 (cnpg dies in host-ns Orgs). Refs #4813 ‖ hw228 2026-07-07 CORRECTED (live kubectl): bp-openclaw@0.2.13 + bp-stalwart-tenant@0.1.12 ARE registered (prior 'not in catalog' evidence was WRONG); the day-2 raw Application-CR path fails `namespaces "org-example" not found` because it doesn't inject tenant.namespace. Real blocker: NO Org on hw228 has the full funnel app bundle (keycloak/newapi/openclaw/stalwart) — acme is an M-plan vcluster that only got WordPress day-2. The canonical orgTenantBPOpenClaw overlay is CORRECT (sets tenant.namespace={{.Namespace}} + dependsOn bp-keycloak) and applies to S-plan host-native funnel Orgs. Fresh S-plan funnel-Org walk (slug walk-stranger) dispatched to verify. Refs #4272 #4307 ‖ hw228 2026-07-07 (my curl re-verify → 200): mail.walk-stranger.omani.homes serves SnappyMail webmail SPA login form, bp-stalwart-tenant HR Ready=True, TLS cert Ready. ✅| hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: stalwart-tenant mail not in the acme stack — mail.<slug> not exercisable. Refs #4307 |
| 235 | apps | [#4437](https://github.com/openova-io/openova/issues/4437) | On a fresh prov, **grafana SSO logs in 3/3 → 200** — the bp-sso-bridge per-tick credential refresh keeps the grafana OIDC client secret in lockstep with Keycloak, so the SSO round-trip completes. PASS = grafana `/login/generic_oauth` → Keycloak → back signed-in (200, user in the avatar) across 3 consecutive attempts; bp-sso-bridge refreshes the client cred each tick (no stale-secret 401). | hw228 | ✅ | — ‖ hw228 LIVE 2026-07-06: grafana.hw228.omantel.biz /api/user returned HTTP 200 ×3 consecutively = emrah.baysal@openova.io (same-origin) — grafana SSO logs in 3/3, the bp-sso-bridge per-tick credential re-mint (R5) keeps the OIDC client secret valid so login succeeds repeatedly; session via keycloak admin-impersonation (not fenced PIN) |
| 236 | apps | [#4439](https://github.com/openova-io/openova/issues/4439) | On a fresh multi-region prov, **region-B keycloak reaches Ready** — the cross-region shared-pg write host is correct so region-B Keycloak connects to the primary Postgres and completes realm import. PASS = `bp-keycloak` HR `Ready=True` in the region-B cluster; keycloak pod Running; realm import succeeds (no DB-connect / wrong-write-host failure); `keycloak.<host>` → 302 SSO. | hw224 | ✅ | hw228 2026-07-07 ✅ (region-b kubeconfig ac35eda8-me-east-215-b, live): region-B keycloak-0 is Running, ready=true, restarts=0 over 30h — STABLE, the historical #4811 dead-mesh CrashLoop is RESOLVED. It reaches Ready because the cross-region shared-pg write host resolves (R12: shared-pg-mesh-rw global=true + ClusterMesh 1/1 + native datapath). Residual (noted, not blocking readiness): an intermittent JGroups JDBC_PING 'Failed to obtain JDBC connection' during periodic MERGE3 — cross-region cluster-coordination, not the persistent main DB connection (which is healthy, else 0 restarts wouldn't hold). ‖ hw224-fulltable-2026-07-04: region-B keycloak Ready — region-b DEGRADED (clustermesh-apiserver Init:0/1 #4656 family); region-a keycloak-0 1/1 Running |
| 237 | spine | [#4416](https://github.com/openova-io/openova/issues/4416) | **spine → Continuum round-trip is Healthy + `continuumRef` is populated** — the spine adopts the Continuum and the round-trip reports Healthy. PASS = the Continuum CR reports `Healthy` (not Degraded), the spine's `continuumRef` field is populated with the live Continuum, and a witness round-trip completes. | hw228 | ✅ | hw228 2026-07-06 (live kubectl via restored kubeconfig): 5 Continuum CRs (dr.openova.io) all **phase=Healthy** with lease held on me-east-215-a — dr-spine-{gitea,harbor,keycloak,openbao} (Application=catalyst/spine-*) + cnpg-pair-bp-cnpg-pair-continuum (catalyst-platform, primary hw-me-east-215-a-rtz-prod). spine→Continuum round-trip Healthy, continuumRef/Application populated. |
| 238 | postgres | [#4282](https://github.com/openova-io/openova/issues/4282) | **Per-Org postgres-in-vCluster → host-side CNPG Ready** — a per-Org Postgres requested inside the Org's vCluster is reconciled by the host-side CNPG operator and reaches Ready. PASS = the per-Org `Cluster` CR is `Ready` (host-side CNPG drives it), the Postgres pod is Running with Guaranteed QoS, and the per-Org app connects (no empty-DSN / disableWait deadlock). | hw224 | ✅ | hw228 LIVE 2026-07-07 (kubeconfig): the acme funnel-Org vcluster has host-side CNPG `Cluster/postgres` in ns acme = INSTANCES 1 READY 1 "Cluster in healthy state" primary postgres-1 (pod 1/1 Running 3h38m). Per-Org postgres-in-vCluster reconciled by the host CNPG operator to Ready. (shared-pg/-b/-c + cnpg-pair also 3/3 healthy.) Refs #4282 |
| 239 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | **Crossplane adoption populates Observe-only on the fresh prov** — `providers.pkg.crossplane.io` and `cloudadoptions.compose.openova.io` are non-empty and reach Synced+Ready, observing (NOT re-provisioning) the OpenTofu-built infra. PASS = `kubectl get providers.pkg.crossplane.io` → ≥1 Healthy; `kubectl get cloudadoptions.compose.openova.io -A` → non-empty Synced+Ready with `external-name` = the real ELB id; the live ELB/nodes are untouched (Observe-only). | hw228 | ✅ | hw228 2026-07-06 (live kubectl): provider-opentofu INSTALLED=True HEALTHY=True (v1.1.3); cloudadoptions.compose.openova.io = 24 adopt-* claims (adopt-eip-{cp,elb,nat}, adopt-loadbalancer-{console,primary}, adopt-network-region…) ALL ADOPTED=true SYNCED=True READY=True, each observing an OpenTofu-built Huawei resource by resource-ID (NOT re-provisioning); 24/24 adopt Workspaces Synced+Ready. Observe-only adoption populated on the fresh prov. PASS. |
| 240 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | No-nodePort gateway serves externally on a FRESH zero-touch prov: ELB → node:443 hostPort → hostNetwork envoy; gateway Service carries ZERO nodePorts (§854 literal). | hw228 | ✅ | hw228 2026-07-06 (live kubectl, FRESH zero-touch prov): `kubectl get svc -A` → **0 NodePort services** (§854/#4706 clean); cilium-gateway + cilium-gateway-console are TYPE=ClusterIP (not NodePort); gateway serves externally — console.hw228.omantel.biz → HTTP 200 via ELB→node:443/:80 hostPort (cilium-envoy hostNetwork). No nodePort anywhere in the datapath. |
| 241 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | Mothership writes `ready` ONLY after an external HTTP answer from https://console.<fqdn>/ (no more false-green). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: mothership-side readiness gate (#4707); hw224 reached ready + console 200 external. Mothership concern |
| 242 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | 2-region BCP admission floor: a 1-region POST with no explicit bcpTopology is rejected at the LIVE production API. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: 2-region BCP admission floor (#4708) — mothership POST-time gate; hw224 IS 2-region (passed the floor). Mothership concern ‖ 2026-07-03 negative wire-test vs console.openova.io: `POST /sovereign/api/v1/deployments` (1 region, no topology) → **HTTP 400** "deployment has 1 region(s) and no explicit bcpTopology — the BCP agreement default is multi-region (>=2 regions, Pillar 2)…". Nothing created. |
| 243 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | Tenant DNS split-horizon on a fresh prov: wildcard hosts → primary ELB EIP, console → console ELB EIP. | — | ✅ | hw228-walk-2026-07-06T10:13: DNS split-horizon on the fresh prov — `console.hw228` + `marketplace.hw228` resolve to **212.72.24.12** (dedicated console ELB EIP), while wildcard/app hosts `auth`/`grafana`/`gitea`/apex resolve to **212.72.24.43** (primary gateway ELB EIP). Two distinct EIPs = console isolation split-horizon works. Refs #4706 |
