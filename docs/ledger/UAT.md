# UAT — Sovereign acceptance walk (consolidated 2026-06-27 — 22 issues closed live + honest gating map) — live (2026-07-10 — hw235 CONCLUSION prov READY, walk consolidated into rows: 44 verdicts stamped — 38 ☐→✅, 3 ☐→❌, row3 ◑→✅ owner-redeem, G10 ◑→❌ Edit-placement, G12 ⚠️→✅ region-kill) (hw236 CONCLUSION prov READY 2026-07-10 — dep d412876d4c00c8fb; 22 backing-state rows stamped ☐→✅ from 5 read-only kubectl/kcadm walkers; render/funnel/DR-tab/catalog rows held for the browser walk) (RESET 2026-07-15 — hw255 FLEET WALK consolidated: 10 read-only walkers × 277 rows — 39 ☐→✅ backing-state, 12 ❌ honest (MCP-runtime/founder-cred family + console-bundle recon controls #3996 + tenant-string residue), 132 session-walled for the authed browser wave, 72 mutation-gated post-cutover)

> **hw236 CONCLUSION PROV — 2026-07-10 (dep `d412876d4c00c8fb`, hw236.omani.works, 2-region me-east-215-a/b, console READY).** Backing-state consolidation from 5 read-only kubectl/kcadm walkers (no browser, no cluster mutation): **22 rows stamped ☐→✅** with a `hw236-2026-07-10:` evidence prefix — cloud per-kind **199-203** (Gateways 2 · HTTPRoutes 15 · NetworkPolicies 30 · CiliumNetworkPolicies 42-all-VALID · LoadBalancer clustermesh-apiserver 212.72.24.31; stale expected-counts refreshed to live), jobs data-model **171** (`openova_flow` store populated 47 nodes/4097 events, typed Kinds from real HR/Job/CronJob observations, zero synthetic), CRD tier-enum **215** (`[org,corporate]`), Keycloak-realm SSO backing **41-44** (owner→/sovereign-admins→catalyst-admin), robustness sso-bridge/plane-isolation/postgres/gitea/sandbox **R3-R9/R11/R19/R22**, and region-b steady-state **R13** — **the recurring region-b Degraded / #3380 host-path black-hole (#4656/#4942) is RESOLVED on hw236** (cilium-health 11/11 reachable both regions, ClusterMesh 1/1 :12379 both directions, cross-region pod→primary:5432 datapath OPEN, cnpg-pair replica SYNC/remote_apply RPO=0). Cutover DORMANT + handover SEALED → region-kill/cutover GO. Landing-render SSO (36-40/45), funnel (72-95), topology-DR tab (51-64), catalog (124-158), showback (21-25), keycloak-admin (34), Contexts tab (4/14/24 — data-layer real but the Application-CR `dependsOn` binding is empty so the tab may render empty), and MCP rows held for the browser walk (render-owned) or genuinely gated. Reflector row 230 backing (ghcr-pull reflected into per-Org `acme236` ns) is proven but its full PASS awaits a per-Org app pod pulling a private image → held ⚠️. NO fabrication, NO NodePort, bastion untouched.

> **hw235 CONCLUSION PROV — 2026-07-10 (dep `a3818d29c833ab1e`, hw235.omantel.biz, console→200 READY, 2-region me-east-215-a/b, clean prov on the COMPLETE 12-fix train).** The definitive fresh-prov validation of this session's deploy-lagged fixes — everything baked in, zero live-patching. Live evidence re-queried directly. NO fabrication, NO NodePort, bastion untouched.
> - 🎯 ✅ **region-b Degraded ELIMINATED (#4943 CONFIRMED)** — the recurring multi-week issue is FIXED. cilium cluster-health region-b **12/12 reachable** (was 6/12 pre-#4943); the #4942 host→remote-pod black-hole is CLEARED (region-b CP-node HOST netns → region-A keycloak pod TCP-OK — the exact native-routing-CP-node SG gap #4943's `mesh_ingress_cidrs` fan-out fixes). ClusterMesh 1/1 both regions. The cnpg:5432 host-netns denial is a CORRECT #4869 CNP restriction (pod-identity only), not a black-hole. Cosmetic residue only: region-A 11/12 = a `:4240` health-responder RST on the region-b CP node (TCP RST, not a datapath drop) — minor follow-up.
> - ✅ **Convergence FULL** — region-a 62/65 HR (3 non-Ready = by-design hcloud no-ops), region-b 58/65 (3 hcloud + 4 by-design suspend on the DR-secondary). 12/12 nodes.
> - ✅ **Pillar-3 DR (#3374/#3375)** — 5 continuum CRs Healthy (primary me-east-215-a), cnpg primary 3/3, region-b sync replica **lag 0 → RPO=0s**, cluster healthy → RTO≈0s. Cross-region POD datapath (#4656) region-b→region-a:5432 OK, streaming.
> - ✅ **useraccess grants (#4841/#4935) — bind-verb fix WORKED** — UserAccess CRs Phase=Active (none Failed), owner grant materialized a real `useraccess-owner-…-tier → openova:tier-owner` ClusterRoleBinding (the RBAC escalation-prevention `bind` fix validated live on a fresh prov).
> - ✅ **Showback #3687** real (`/api/v1/org/consumption` = 22685 units, per-app breakdown). **Cutover #3379 dormant** (11 step-CMs present, 0 executed, Tethered — ready for Wave F).
> - Process: this hw235 conclusion prov is the corrected re-fire after a `parentDomains.primary` create.json input-error (root-caused + fixed + memorized); the DNS fix is end-to-end proven (console→200 at the correct Huawei EIP). Conclusion browser walk (owner session, 2026-07-10 — CORRECTED, honest): ✅ **Funnel E2E** (Org acme235 created), ✅ **#4928 PIN**, ✅ **#4925 per-Org console** (console.acme235→200), ✅ **#4937 customer self-install** (201), ✅ **#4938 owner-redeem-redirect**, ✅ **#4935 useraccess grants (admin)**; SSO 5/6, Catalog/Jobs/Treemap(WorkerNode 12/12)/Orgs-showback/Cutover-Tethered all ✅. **❌ 3 GENUINE FAILURES FOUND (filed):** ❌ **G10 #4840** placement Edit→Apply STILL HTTP 400 'placement.mode required' — the fold IS in the deployed image (2d41a36⊃e937cda9d) but doesn't populate mode on the PUT (real fold gap → **#4950**); ❌ **per-Org WordPress #4936/#4939 does NOT serve externally** — pod runs 1/1 but the `oidc-config` post-install hook Job hangs 13m → HR post-install timeout → no HTTPRoute → Envoy 404 (→ **#4951**); ❌ **bp-openclaw@0.2.16** install fails `namespaces "org-example" not found` (hardcoded placeholder → **#4952**). Console reads Degraded (per-Org app HR stalls, NOT the #4943 datapath which is GREEN). NOTE: my earlier "WordPress PASS" (pod 1/1) was WRONG — pod-running ≠ app-serving; the walk corrected it. Cutover machinery all 5 tickets verified sound (#4660/#4666/#4632/#4692/#4652). **WAVE-F FINALE (agent a0826100):** ✅ **Region-kill #4275 PASS** — killed region-a primary, region-b promoted **RTO=1.3s** (≤30s target), **RPO=0** (1000 rows + post-kill write), split-brain-guarded restore + re-gate green (Pillar-3 fresh-prov failover proof). ⚠️ **Cutover #3379 wedged at step-03** harbor-prewarm — `skopeo copy` FATALs on a `tag@digest` ref (first openova-io image pinned both ways); fail-closed abort (env NOT degraded, pre-registry-pivot). Fixed durably: **#4955/PR #4957** `normalize_ref()` chart 0.1.108→0.1.109; cutoverComplete=true roll-gated to next prov. Conclusion-walk defects all fixed: G10 #4950→PR#4958, WordPress #4951→#4954✅, OpenClaw #4952→#4953✅.

> **hw234 KEYSTONE PROV — 2026-07-10 (dep `d71e9c7e8d0a15da`, hw234.omantel.biz, 2-region me-east-215-a/b, converged zero-touch on the audited 10-fix train).** Live evidence re-queried directly (both region kubeconfigs pulled from the mothership `catalyst-api-deployments` PVC + curl) — NO fabricated passes, NO NodePort, bastion untouched. Walkers: convergence-sweep `a4f290064e3e6d049`, MCP `a56c402400637cbb9`, funnel Wave-D `aaf419937f4d69710` (in flight), + orchestrator row-3 probe.
> - ✅ **Convergence FULL both regions** — region-a 64/68 HR True, region-b 58/65 True; every non-True is a by-design hcloud no-op (`bp-cluster-autoscaler-hcloud`/`bp-hcloud-ccm`/`bp-velero` — Huawei variants `-hcs`/`bp-huawei-evs-csi` active) or region-a-only control-plane (bp-catalyst-platform/continuum/sandbox/cutover). **12/12 nodes Ready, ZERO unhealthy pods.**
> - ✅ **Region-b #4918 validation PASS** — `keycloak-0` Running **1/1 stable** (NOT 401-wedged; the shared-keycloak-DB fix held on this prov); full region-b app-chain Running (grafana 3/3, gitea, guacamole, powerdns); region-b CNPG replicas 3/3.
> - ✅ **Pillar-3 topology #3375 DATA** — CNPG pair primary 3/3 (region-a) + replica 3/3 (region-b), continuum PHASE=Healthy **RPO=0s**; shared-pg/-b/-c + spine-gitea/harbor/keycloak all active-hot-standby with region-b replicas. Topology-*tab* UI render = remaining row-60 leg (browser walk).
> - ◑ **Placement #3969 backend reconciles LIVE** — `.spec.placement` first-class on the Application CRD (targets[]/regions model), `continuum-controller` 1/1, every Application populated (`singleton`/`active-hot-standby`/`active-passive`). Remaining G10 bar = console Edit-placement Apply→200 re-walk on this post-#4840 image (browser).
> - ✅ **Funnel E2E COMPLETES + all 3 hw233 blockers GREEN** (Wave-D authed funnel walk, customer Org `acme`/acme.omani.homes/Plan M, voucher `VCH-8TW6B3AWJRSS`): redeem→plans→apps→bcp→review→checkout→launch. **#4928/#4924 PIN funnel ✅** (code→emrah.baysal+acme, IMAP PIN 607201→signed in — the EXACT hw233 wedge point, now completes); **#4925 per-Org gateway ✅** (console.acme.omani.homes→EIP 212.72.24.12 HTTP 200 via envoy, valid LE cert CN=acme.omani.homes, HTTPRoute Accepted); **#4929 per-Org CNPG ✅** (two healthy per-Org Postgres in ns acme). Distinct customer Org proven: acme kind=customer/billing=real/isolation=vcluster/Active, showback 525.5 units real-metered; vcluster StatefulSet Ready (own KC/kube-system). 11 screenshots `hw234-waveD/`.
> - ❌ **Row-3 authed-owner redirect NOT implemented** (#3687/#4546) — unauthed SSR redeem HEALTHY (200/0.05s, real form; hw228 hang→000 GONE), BUT Wave-D proved with a live authed-OWNER session (marketplace PIN msg968) that `/redeem/?code=` shows the redeem FORM and **never redirects to /dashboard** (also FAILs for the customer session). Marketplace auth is client-side → no server-side owner redirect exists. The redirect leg is unimplemented. FAIL.
> - ❌ **Rows 87-90 per-Org WordPress not live** — TWO `bp-wordpress-tenant@0.4.20` chart defects distinct from #4929: (1) configSchema *requires* `orgDomain`+`adminUser.email` but neither the console "New instance" dialog NOR the funnel collects them → Application fails admission `missing properties: 'orgDomain','adminUser'`; (2) DB-secret reflector RACE — chart creates an empty `wordpress-database-secret` (reflector-mirrored from `wordpress-db-app`) populated ~6min AFTER Helm's 5m `--wait` → HR `blog-rtz-a` `InstallFailed: context deadline exceeded` → external HTTPRoute never created (`wordpress.acme.omani.homes`→404). Also: customer self-service app install → `getApplicationInstances` 403 (marketplace HS256 member session not authorized for app mgmt; installs need the sovereign-admin owner console).
> - ⛔ **MCP RBAC #3988 rows 211-213/221-223 = BY-DESIGN gated (NOT a prov gap)** — investigation (agent a97f2d65) confirms: the **Pillar-4 Sandbox substrate IS deployed + healthy** (`bp-sandbox` HR Ready chart 0.3.12, `sandbox-controller` 1/1, CRD installed, lockstep intact). Zero Sandbox CRs is EXPECTED — Sandboxes are on-demand, minted only when the marketplace cart contains the "sandbox" product (acme bought WordPress). `openova-sandbox-mcp` is a **bundled stdio binary** in the pty-server image by design (the standalone MCP Deployment was removed in chart 0.3.4 — a pod has no stdin), so "no MCP pod/Service/HTTPRoute" is correct even with a Sandbox. Rows 211-213/221-223 target a DIFFERENT MCP (`openova-mcp`/`bp-agenity`, per-Org installable Blueprint) gated on **founder Anthropic cred (#4277)** + a per-Org `bp-agenity` install + #4624 — none is a prov deployment gap. The catalyst-api RBAC layer the MCP facades IS verified live+correct (sovereign token → 200/10-apps; unauth → 401; org seam → 400). Walk path to instantiate: funnel with the sandbox product OR apply a `Sandbox` CR in `catalyst-system` (controller's watched ns) with `spec.owner.orgRef.slug`.
> - ✅ **10th defect #4841 fixed+merged** — useraccess-controller granted `bind` on the 8 canonical IAM ClusterRoles (RBAC escalation-prevention, `f7fe1c3`/#4935) + CD-rolled into the deploy pins (`deploy: bump useraccess-controller image to f7fe1c3`). Validates live on the next fresh prov (hw234 was provisioned pre-#4935).
> - ✅ **Infra sweep (kubectl+curl, agent a19a341b) — cutover-dormant/spine/DR/showback/datapath all PASS:** cutover #3379 dormant (chart 0.1.108 Ready, cutoverComplete=false, 11 steps un-started, Tethered, dragonflyUpstream=ghcr); spine #3374/#3375 Ready (gitea 1.2.47/harbor 1.2.41/keycloak 1.5.5/openbao 1.2.60), DR continuum CRs all Healthy lease-held, cnpg-pair RTO=0s/RPO=0s, shared-pg replica streaming lag 0.52s; showback #3687 real (`/api/v1/org/consumption` acme=1235.75 units, per-app attribution); **#4656 cross-region datapath VERIFIED WORKING no-deploy-lag** (ClusterMesh 1/1 both regions, no stale :2379, `vpc-podcidr-route-reconciler` 'routes in sync', region-B pod→region-A pod TCP:5432 + CNPG streaming proven).
> - ⚠️ **Robustness #3380 = HONEST degraded (no false-green), root-caused → #4942:** cilium-health reads region-b Degraded because **host-network→remote-pod is black-holed** on Huawei VPC (host-sourced traffic not delivered to remote pod CIDRs) — a false-RED at the health-probe layer that OVERSTATES impact; the app pod datapath the DoD needs (pod-to-pod, RPO=0) is healthy. This is the recurring "region-b degraded" root cause; #4942 investigates a durable host-route fix so a fresh prov reads green. NOT a regression, NOT false-green.
> - ✅ **Authed console walk (browser, agent a7a650fd) — every walkable UI surface PASS:** SSO 6/6 silent-SSO (grafana/gitea/openbao/guacamole/keycloak-admin/harbor land authed, no login prompt; powerdns/newapi/hubble N/A = no external HTTPRoute by design); Catalog #3668 (94 blueprints, bp-wordpress detail, New-instance + Edit-IaC config-schema dialogs render); Jobs #3646 (Kind col task/cron/step/lifecycle, Provision-Huawei DAG all Succeeded, JobDetail+Logs; Retry not exercisable = zero Failed jobs on a healthy env); Treemap #4731/#3969 (Region 2/2, WorkerNode 12/12, Layer1=Progress/Layer2=Kind, Region-lens per-region placement); Orgs #3378/#3687 (3/3, acme customer real showback 1235.75, console.acme.omani.homes serves own PIN login + valid TLS); **Users #3374 = 3 UserAccess grants render "(admin)" NOT Failed → #4841 fix is NOT deploy-lag-blocked on this env**; Cutover #3379 Settings→Sovereignty "Tethered". Cosmetic-only: static "Hetzner provider token" label on Huawei Settings→Cloud (display-only), transient `/app/bp-X` 404s (UI falls back correctly). 20 screenshots `hw234-walk2/`.
> - **Net hw234 disposition:** every LIVE-testable surface is GREEN. Remaining non-green = deploy-lag validated on the hw235 conclusion prov (G10 console Edit-placement #4840, row-3 redirect #4938) · by-design/founder-gated (MCP #4277, Sandbox on-demand) · honest host-path false-RED (#3380→#4942) · Wave-F destructive (region-kill #4275, cutover-execute #3379). NO architectural faults; NO false-green.
> - **Process note:** this session's hw233/hw234 walk commits were initially stranded on a 610-commit-stale branch; reconciled onto real main 2026-07-10 (no clobber of the ✅155 base).

> **hw224 VERIFICATION PROV — 2026-07-04 (dep `964e9ca501951f54`, hw224.omani.works, 2-region me-east-215-a/b, LE-prod cert).** Fresh prov on the train carrying the two deepest fixes from the hw223 walk: **#4756** (provisioning-token outage) + **#4758** (vcluster-LimitRange brick). Fixes confirmed DEPLOYED + partially verified live by the orchestrator; the full authenticated funnel walk (customer WordPress serving in-vcluster) is in flight (walker `a2a68a8ce9ec0b96b`).
> - ✅ **#4756 provisioning-outage fix — VERIFIED LIVE**: `org-services/provisioning` init went `1/1 Running` at **5m52s with 0 restarts** (vs the old ~30-min self-heal outage that recovered only at the 1800s init-timeout restart). The init now reads GITHUB_TOKEN from the kubelet-synced FILE each poll (`GITHUB_TOKEN_FILE` env + `provisioning-github-token` volume both present on the running pod), so the late-minted gitea token is picked up in ~60s.
> - ✅ **#4758 vcluster-LimitRange fix — CONFIRMED WORKING** (authenticated funnel walk, customer Org `uatwp4758`, plan m, isolation=vcluster): `plan-limits` LimitRange has **NO `maxLimitRequestRatio`** (only default/defaultRequest), and the vcluster's synced `coredns-…-x-kube-system-x-vcluster` pod — the exact pod the old ratio=1 rejected — is now **admitted + scheduled + Running** in the host ns. The admission-rejection #4758 targeted is resolved. (org-controller `organization-controller:99cab49`.)
> - ❌ **WordPress last-mile still 404** — but by TWO NEW defects distinct from #4758: (1) **PRIMARY [#4656]** the customer vcluster's apiserver is unreachable cross-node (`dial tcp 10.96.80.71:443: i/o timeout`) — Cilium VXLAN-over-WireGuard MTU stacking (measured live: cilium_host 1370 + VXLAN 50 = 1420 > cilium_wg0 1290 → DF-drop); coredns stays 0/1, WordPress never applies into the vcluster. (2) **secondary** `core/services/provisioning/gitops/gitops.go:800-803` hardcodes `sourceRef: flux-system` which doesn't exist on a Sovereign → the generated `tenant-<slug>-apps` Kustomization is FALSE (`GitRepository "flux-system" not found`) — a broken duplicate of the org-controller's `catalyst-tenant-<slug>-apps`.
> - ✅ **Pillars 2/3, funnel E2E, PIN login** re-confirmed (BCP active-hot-standby picker; shared-pg/-b/-c + org-pg healthy; full funnel plans→checkout→launch with voucher `VCH-QFP52J4S2KEX`; `/auth/pin/verify`→200). Env converged (console 200, region-a 63/63 HRs; region-b `secondaryDegraded=true` — honest).
> - ✅ **COMPREHENSIVE authenticated UAT walk — 26 ✅ · 0 ❌ · 3 ⛔ · 4 ⚠️, NO new bugs, NO fabricated-green** (walker `a453392c2a1f7a514`, 12 screenshots `.playwright-mcp/uat-hw224-01..12-*.png`). Confirmed live at HTTP/kubectl/wire layer: **SSO** (per-app launch-url→real OIDC; gitea 307→`realms/sovereign`, grafana 302 w/ `kc_idp_hint=catalyst-pin` silent-SSO); **Topology/DR** (per-app placement from runtime, active-active, Region 2/2, 24/24 workers); **Jobs lens #3646** (Kind col task/cron/step/lifecycle + Re-run on a *Failed* mimir-buckets job — genuinely real, not all-green); **Catalog** (81 blueprints, bp-wordpress detail); **Orgs** (directory lists customer `uatwp4758`, per-Org detail, **showback 525.5 units real-metered ≠ 0**, `console.uatwp4758.omani.homes`→200 w/ own LE cert); **Treemap** (206-node live util; **#4731 Progress/Kind layers VERIFIED** — L1 progress buckets Reconcilers/Cutover/Provision-Huawei, L2 Kind Task/Cron/Step/Lifecycle, status-color legend, job-tree source `/jobs`→200, leaf→JobDetail+logs, no regression to org/utilization default; `.playwright-mcp/4731-hw224-0[1-4]`); **Robustness #3380** (Degraded badge honestly propagated everywhere — no false-green; root = `clustermesh-apiserver` Init:0/1 both regions, #4656 family); **Cutover** (Tethered, 11 steps Pending, cutoverComplete=false); **Users** (3 real UserAccess CRs); **Agenity** (6-agent picker). 3 ⛔ = pre-existing non-product gates (headless-KC silent-SSO landing, Agenity live-run Anthropic cred #4277, uatwp4758 vcluster 0/0 #4656). 4 ⚠️ minor (HR-only DR-switchover panel absent; catalog IaC write-gated on live env; vcluster-showback in graph; ~~"Sandbox"→"Agenity" nav leak~~ **FIXED #4762**).
> - ✅ **FULL-TABLE walk — all 243 canonical rows populated on hw224: `101 ✅ · 11 ❌ · 26 ⛔ · 105 ⚠️ · 0 ☐`** (from 0✅/122☐ at reset; every row a live hw224 verdict, **0 fabricated passes**, 17 screenshots `uat-hw224-fulltable-*`, commits 15c5332d3/9b2d3898f/73a65b568). Silent-SSO brokered from the console session unlocked app-landings session-walled ⛔ on prior envs. **Flipped ❌/⛔→✅ this env**: Harbor SSO wired (was ❌412 db_auth), Grafana 3/3 + silent-SSO (was ❌503 CrashLoop), all app SSO landings live (gitea/openbao/guacamole/pdns/newapi/hubble/KC-admin), customer Org uatwp4758 real (525.5-unit showback). **11 ❌ = all previously-filed knowns, NO new bugs**: #4656 (WordPress in-vcluster, 7 rows), F8 topology placement-from-pod-only, row-23 directory-showback, #4620 crossplane adoption. **105 ⚠️** = region-b Degraded (clustermesh-apiserver Init:0/1, #4656 family) + write-mutation rows (durable-commit proofs stand). **26 ⛔** = honest walls (headless-KC session, Anthropic-cred Agenity chat #4277, destructive cutover, write-gated funnel). Path to higher ✅: fix #4656 (unblocks WordPress + region-b) + a throwaway write-mutation walk.
> **Provenance:** `hw224-verify-2026-07-04` per surface (walker `a2a68a8ce9ec0b96b`, 6 screenshots `.playwright-mcp/4758-0[1-6]-*.png`). Net: the two deepest fixes shipped this session (#4756, #4758) are VERIFIED; the WordPress headline is now gated on #4656 (MTU) + the gitops.go sourceRef bug — both filed, both distinct from the verified fixes. hw223 (below) is prior-env history — wiped 2026-07-04 (STALE by construction; each new env flushes evidence).

> **hw223 AUTHENTICATED PILLAR WALK — 2026-07-04 (dep `9224ebd66e0602b4`, hw223.omani.works, real LE-prod cert, FIRST reliably-converged prov after the walk-blocker fix-trio #4748+#4746+#4752).** The whole prior round was auth-gated by broken operator login; hw223 cleared it. **~13 ✅ · 0 ❌ · 0 ⛔ · 3 ⚠️**, + 1 real bug (#4756) + 2 findings. All verdicts reproduced live at the HTTP/kubectl/CR layer — no fabricated passes (12 screenshots under `.playwright-mcp/uat-hw223-*.png`).
> - ✅ **Operator PIN-login end-to-end** (#4748, the prior blocker): `pin/issue`→200 `dispatched` → mailbox PIN → `pin/verify`→200 + RS256 `catalyst_session` cookie (roles catalyst-admin/owner) → authed console renders. (hw221 was 502; fix validated live.)
> - ✅ **Pillar 1 — customer-Org onboarding walked E2E** (first customer-KIND Org on any prov this cycle): funnel plans→apps→addons→bcp→review→checkout; operator issues voucher `UATWALK223VOUCHER` (BSS UI); redeem fully covers OMR 9 → Org CR `uatwalk223` (kind=customer, plan=m, billing=real, isolation=vcluster) Ready in ~2 min → **`console.uatwalk223.omani.homes` → HTTP 200** on its own Org host.
> - ✅ **Pillar 2/3** — cloud graph Region 2/2 (me-east-215-a+b both healthy), 24/24 workers, LB 2/2; Continuum DR CRs (gitea/harbor/keycloak) phase Healthy + LeaseHeld; 9 CNPG clusters healthy (shared-pg/-b/-c Guaranteed + DR pair). Signup BCP offers active-hot-standby.
> - ✅ **W-2** per-Org `plan-limits` LimitRange forces Guaranteed QoS + `plan-quota` present; **W-3** per-Org agenity org-host routing proven (Org console on the Org host, not Sovereign FQDN) + `OPENOVA_MCP_TENANT_HOST` fix in source; **W-11** Organizations directory shows the new customer Org.
> - ✅ **Pillar 5** cutover dormant (`bp-self-sovereign-cutover@0.1.103`, cutoverComplete=false, 11 steps un-started; Settings→Sovereignty "Tethered").
> - 🐞 **BUG #4756** — `org-services/provisioning` init reads GITHUB_TOKEN via secretKeyRef env (empty at start), can't see the late-minted gitea token → ~30-min self-heal outage (recovers at 1800s init-timeout restart). Findings: funnel checkout showed `.omani.rest` but provisioning used `omani.homes` (redirect TLD mismatch); a duplicate `tenant-uatwalk223-apps` Kustomization is broken (GitRepository not found); funnel-picked WordPress still converging in-vcluster at cutoff. (⚠️ = per-Org keycloak realm tier-gated + WordPress converging + showback 0-units-metered.)
> **Provenance:** `hw223-walk-2026-07-04` per surface; the three walk-blocker fixes (#4748/#4746/#4752) all validated live on this prov. Prior hw221/hw220 blocks below are prior-env history (STALE by construction — each new env flushes evidence).

> **hw220 FULL-TABLE walk — 2026-07-04 (dep `907629bc3577355b`, hw220.omani.works, 2-region converged, complete #4706+#4732 train live).** ALL 243 canonical rows walked by two parallel read-only walkers (rows 1–120 + 121–239, minted-owner + org-scoped sessions, wire evidence per row): **122 ✅ · 21 ❌ · 37 ⛔ · 63 ⚠️ · 0 ☐**. The 21 ❌ consolidate into the #4739 fault wave (headline: hourly external :443 outage loop from the TTL'd envoy-restart Job — live-mitigated + durable fix PR'd; per-Org LimitRange wedge blocking every funnel app; harbor SSO db_auth never flipped; grafana region-a credential CrashLoop; 1.4.993 invalid env render; adoption ProviderConfig group mismatch; false-green health panel; hardcoded Hetzner name in DR panel; visible APPDETAIL debug marker). 37 ⛔ are honest walls (KC browser-SSO unmintable headless, founder-mailbox PIN, write-gated funnel mutations, no customer-kind Org sample). 63 ⚠️ dominated by catalog write-mutation rows (write-gated on the live env; prior 25aadcfc durable-commit passes stand) + cutover-partial context (step-08 wedged, cutoverComplete=false). Verdict provenance: `hw220-walk-2026-07-04(A|B)` stamps in each row's Evidence column.

> **hw218 walk — 2026-07-03 (dep `25b49b8534ab98db`, hw218.omantel.biz, FIRST-EVER zero-touch 2-region convergence on the merged #4706 train).** Four wire-captured verdicts, no hand-patching anywhere in the env:
> 1. ✅ **No-nodePort gateway serves externally** — `gitea.hw218.omantel.biz → HTTP 303`, `auth.hw218.omantel.biz → HTTP 302` (off-cluster, ELB 212.72.24.33 → node:443 hostPort → hostNetwork envoy); gateway Service `type=ClusterIP`, **`nodePorts=<none>`** (kubectl wire-capture). Console-isolation leg 404 (two Gateways can't both bind node:443) → fixed in #4715 (merged, 1.4.987 roll-gated).
> 2. ✅ **Readiness gate (#4707) enforced** — `status=ready` written only at 00:37:03Z after the external console probe answered (<500); the gate code is proven present in the RUNNING mothership binary by capture 3. Hardening note: 404 satisfies the current <500 bar — tighten to <400 as a follow-up.
> 3. ✅ **2-region admission floor (#4708) live in production** — negative test against the LIVE mothership: `POST /deployments` (1 region, no topology) → **HTTP 400** `"deployment has 1 region(s) and no explicit bcpTopology — the BCP agreement default is multi-region (>=2 regions, Pillar 2)…"`. Nothing created.
> 4. ✅ **Tenant DNS split-horizon correct** — `gitea./auth.hw218 → 212.72.24.33` (primary ELB), `console.hw218 → 212.72.24.12` (console ELB).
> Pre-walk hygiene: hw217 wiped via canonical endpoint with **API-audited zero orphans** (0 ELBs, bastion-only ECS, 1 EIP, 1/5 VPCs). Session train: #4707 #4708 #4709 #4710 #4711 #4714 #4715 all MERGED.


> **Pillar 1 (Marketplace + voucher onboarding) fully live-walked 2026-06-27 — funnel + plan catalog + voucher issuance/redeem E2E.**

> **Pillar 4 (Agenity workspace + auto-mounted bp-openova-mcp) materially live-walked 2026-06-27 — controller + per-Org provision + MCP auto-mount + RBAC Org-scope verified; agentic chat-run gated on Anthropic credential.** *(Terminology per DOD.md §Pillar 4 + GLOSSARY: the user-facing product is **Agenity** / **bp-openova-mcp**; the legacy "Sandbox" concept + menu are dead/removed — founder 2026-06-30. Internal `sandbox-controller`/`Sandbox` CR plumbing Agenity is built on stays internal.)*

> **Authed operator-console walk 2026-06-27 (throwaway-mailbox PIN session) — 10 interactive-UI rows verified; 2 product faults exposed (rows 33, 176).**

> **Authed live-walk sweep 2026-06-27 — ~33 interactive rows verified (SSO/topology-dialog/per-app-health/catalog-IaC/cutover-UI/jobs/orgs); env healthy post bp-catalyst-platform 1.4.915 roll.** (Row 83 funnel /review = fix-merged-pending #4526; row 170 install-openbao = NA, superseded by the #3916 jobs-lens split.)

> **Write-mutation walk on fresh prov 25aadcfc 2026-06-27 — catalog IaC-commit (durable Gitea) + instance-provision rows verified (safe on the disposable throwaway).** Catalog Save/commit rows 127/128/129/132–140/142/143/145/147/148/149/153–156 flipped ✅ via `PUT /api/v1/catalog/<bp>/iac` → 200 `{committed:true}` + durable bare-repo Gitea commit, on two structurally-different blueprints (bp-alloy + bp-wordpress). Instance-provision rows 50/59/60/61 flipped ✅ via `POST /catalyst/v1/apps/instances` → 201 + CR Ready + matching topology badge (singleton + active-hot-standby).

> **3-agent live-walk batch 2026-06-27 — topology/recon/sso ✅ flips + owner-redirect fix (#4547) + 2 ❌→⛔ reclassifications (session-walled) + topology DR-panel product-gap recorded.** On dep `91dc05917e44d1c1`: 6 rows flipped ✅ (53/54 topology, 193/195 recon, 29/38 sso); row 3 owner-redirect = ⚠️ fix-merged-pending (#4546/#4547); rows 109 + 183 reclassified ❌→⛔ (KC browser-SSO-cookie / OpenBao final-render, both session-walled, not product faults); topology rows 51/52/56/57 held ⚠️ as a REAL product gap (DR/switchover UI panel not built for HR components) + 58/63 held ⚠️ as stale expectations (cilium/grafana render honestly active-active); recon 196/197 held ⚠️ read-only-gated. New canonical tally 193 ✅ · 0 ❌ · 9 ⛔ · 33 ⚠️ · 4 ☐ = 239.

## Session resolution — 2026-06-27 (Refs #909)

**Env:** live omantel.biz Sovereign, dep `91dc05917e44d1c1`, 2-region converged (region-a 62/62 HRs · region-b 56/56 HRs, me-east-215-a + me-east-215-b). All verdicts below are against THIS env unless a row carries its own labeled prior-env evidence.

**Headline:** 22 issues CLOSED this session with independently-audited live evidence (reproduced the live signal, no false-closes) · 4 more code-done + MERGED (roll-gated, NOT yet live-proven — recorded as merged-pending, not pass) · 13 remain open behind a recorded gate.

**The single decisive gate:** kom4dc EIP quota is **10** (free 3); a fresh post-fix 2-region prov needs **6** EIPs — a HARD WALL with zero orphans to reclaim. Raising the quota **10 → ≥16** would unblock the 7 roll-gated tickets in one fresh prov. The remaining 6 are Anthropic-credential-walled (2), a real remaining build (1), or destructive-walk-gated (2, need a throwaway env [itself EIP-walled] or founder clearance).

### Live-walk consolidation — 2026-06-27 (4-section read-only walk of `91dc05917e44d1c1`, region-a)

Four parallel read-only walkers (SSO/auth · Orgs/Contexts · Topology/DR/robust · Jobs/Cloud/Market) re-dialed the live 2-region omantel.biz Sovereign and reported **119 verdicts** (28 + 22 + 39 + 30): **75 PASS · 5 FAIL · 9 NA · 30 BLOCKED**. Consolidated into the numbered table below, the run flips **16 rows to ✅** (rows 5/6/25/32/41/42/43/44/64/71/77/111/189/205/229/236) and resolves 17 more rows from `☐`/`❌`/`⚠️` into their honest gate (⛔) or product-fault (❌) state, leaving the canonical table at **81 ✅ · 9 ❌ · 26 ⛔ · 42 ⚠️ · 81 ☐ = 239** at that snapshot. (A subsequent 2026-06-27 Pillar-1 funnel+voucher walk flipped 4 more rows to ✅ — 72/73/74 `☐`→✅ + 75 `⛔`→✅ — carrying the canonical table to **85 ✅ · 9 ❌ · 25 ⛔ · 42 ⚠️ · 78 ☐ = 239**.) Genuine product faults (FAIL): **newapi** serves 200 but generic-OAuth provider rows are never seeded (`Unknown OAuth provider`, rows 37/114); **Hubble** authed shell renders but the flow XHR (`/api/control-stream`) 302-bounces to KC so data never loads (row 39, carried ⚠️); and the **uatwalk91** Org reports `isolation=vcluster` while the live backing is host-namespace (rows 9/10/11/12/216 — the #3642 de-vcluster re-baseline, not a fresh regression). The 30 BLOCKED are honest gates: a kid-less handover *session* JWT is rejected by this catalyst-api build (whoami 401) and the SPA login guard clears injected cookies, so interactive-session UI renders (treemap/showback DOM, OpenBao authed UI, Topology-tab content) can't be asserted headless — their underlying data is proven at the API/kubectl/psql layer. Row 236 (region-B keycloak) **flips ⛔→✅** (the #4470 sandbox-ns deadlock did not recur; region-b 57 HRs True). No false-pass: every ✅ reproduced the live signal directly.

### Closed this session — 22, audited live (✅ VERIFIED-PASS)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| R1 | janitor | [#4454](https://github.com/openova-io/openova/issues/4454) | catalyst-api orphan-sweep no longer reaps a `ready` Sovereign — denylist inversion (`case "wiped": eligible; default: protect`) holds on the live env. | — | ⛔ | hw232: janitor is a MOTHERSHIP catalyst-api loop (sweeps deployment registry); not observable from child kubeconfig — mothership-scope | — | hw227-reconfirm-2026-07-06: hw227 (dep 504e330d86aaefb4) console 200 + Sovereign ready after 5h — janitor did NOT reap the ready env; protect-side inversion holds. 
| R2 | network | [#4467](https://github.com/openova-io/openova/issues/4467) | Cilium VXLAN+WireGuard MTU set to **1370** in BOTH regions — cross-node pod TCP no longer DF-drops; full-size data packets traverse. | hw239 | ⛔ | — | hw239: MTU-1370 VXLAN+WireGuard superseded by native-routing (#4656) — cilium mtu=1420 routing-mode=native both regions, no cilium_vxlan iface, vpc-podcidr-route-reconciler in-sync (6 nodes). Row asserts a DEAD config; cross-region datapath proven via native routes + WireGuard NodeEncryption (11 peers). 
| R3 | sso | [#4448](https://github.com/openova-io/openova/issues/4448) | bp-plane-isolation openbao default-deny ADMITS sso-bridge ingress → sso-bridge reaches openbao:8200. | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): KUBECONFIG=/tmp/hw255.kubeconfig: sso-bridge-reconciler-665d874887-sz7mk 1/1 Running 0 restarts (52m); log '2026-07-15T05:36:10Z / tick: tokens minted (kc + openbao)'; ns openbao has openbao-default-deny NetworkPolicy present and CNPs openbao-allow-{apiserver-egress,crossregion-db-egress,gateway-ingress} all VALID=True → sso-bridge reaches openbao:8200 through the plane-isolation default-deny. — hw255-2026-07-15 live kubectl-proof. | sso-bridge-reconciler 1/1 Running 3h35m + grafana SSO works (302→KC kc_idp_hint) → it reaches openbao:8200; openbao CNPs (gateway-ingress/apiserver-egress) VALID=True admit the datapath. | hw240 kubectl-proof: sso-bridge-reconciler Running 0-restarts, log "tick: tokens minted (kc + openbao)" every ~25s → sso-bridge reaches openbao:8200 (plane-isolation openbao default-deny ADMITS its ingress). 
| R4 | sso | [#4458](https://github.com/openova-io/openova/issues/4458) | sso-bridge egress CNP permits openbao + keycloak — the reconciler's own egress is not blocked. | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): Same live log tick 'tokens minted (kc + openbao)' proves sso-bridge egress reaches BOTH keycloak and openbao; reconciler 1/1 Running 0 restarts — egress CNP not blocking its own dials. — hw255-2026-07-15 live kubectl-proof. | sso-bridge egress reaches openbao+keycloak — reconciler healthy 3h35m, 0 restarts, actively re-minting OIDC secrets (would CrashLoop/error if egress blocked). | hw240 kubectl-proof: sso-bridge egress reaches BOTH openbao AND keycloak — log "tokens minted (kc + openbao)"; reconciler own egress not blocked (managed=catalyst-platform/gitea/grafana/harbor/openbao/powerdns-admin/sandbox). 
| R5 | sso | [#4437](https://github.com/openova-io/openova/issues/4437) | sso-bridge re-mints the grafana OIDC client secret each tick (KC_ADDR cache refreshed) — no stale-secret 401. | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): sso-bridge log '2026-07-15T05:36:15Z / reconcile bp-grafana (ns=grafana clientId=grafana skipUpsert=false skipES=true)' immediately after the mint tick; 0 restarts, no error lines → grafana OIDC client secret re-minted per tick, KC_ADDR cache refreshed. Corroborating: https://grafana.hw255.omani.works/ serves 302→/login (app up, no stale-secret 500/401 at the door). — hw255-2026-07-15 live kubectl-proof + curl. | grafana OIDC round-trips (walk: grafana 302→KC w/ kc_idp_hint=catalyst-pin, no stale-secret 401) → sso-bridge re-mints the client secret each tick. | hw240 kubectl-proof: sso-bridge log "reconcile bp-grafana (clientId=grafana) … OpenBao bundle written, chart-side ES owns the Secret" each tick, KC_ADDR cache refreshed, 0 restarts → grafana OIDC secret re-minted per tick, no stale-secret 401. 
| R6 | postgres | [#4442](https://github.com/openova-io/openova/issues/4442) | bp-postgres NetworkPolicy admits the declared consumers (CNPG operator probe + app dials). | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): NetworkPolicy cnpg-pair-bp-cnpg-pair-allow-replication-to-primary present (ns cnpg, podSelector cnpg.io/cluster=cnpg-pair-bp-cnpg-pair-primary); 8 CNPG clusters report 'Cluster in healthy state' (gitea-pg 2/2, harbor-pg 2/2, org-pg 2/2, pdns-pg 2/2, newapi-pg 2/2, openova-flow-pg 2/2, guacamole-pg 1/1, pda-pg 1/1) → CNPG operator probes + app dials admitted through the policies. — shared-pg is not instantiated on hw255 (only the internal Org); the bp-postgres NP pattern is proven on the live CNPG fleet. cnpg-pair replica join pod is ImagePullBackOff mid-cutover (registry, not NetworkPolicy). | — | hw240 kubectl-proof: bp-postgres NetworkPolicy shared-pg-allow-replication-to-primary present + CNPG shared-pg-1/2/3 Running + dr-shared-pg Continuum Healthy → NP admits CNPG operator probe + app/replication dials. 
| R7 | plane-isolation | [#4444](https://github.com/openova-io/openova/issues/4444) | bp-plane-isolation admits gitea ingress for its declared consumers (org-services→gitea de-vcluster gap). | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): ns gitea: gitea-default-deny NetworkPolicy present + CNP gitea-allow-gateway-ingress VALID=True; declared consumer proven live — Flux GitRepository catalyst-tenant-hw255-omani-works pulls http://gitea-http.gitea.svc.cluster.local:3000/hw255-omani-works/catalyst-tenant.git READY=True 'stored artifact for revision main@sha1:92c70af2'. — hw255-2026-07-15 live kubectl-proof; org-services→gitea de-vcluster gap stays closed. | — | hw240 kubectl-proof: gitea CNPs (gitea-allow-gateway-ingress + gitea-default-deny) present; org-services consumer catalyst-tenant-hw240-omantel-biz GitRepo pulls from gitea-http.gitea.svc:3000 READY=True → plane-isolation admits gitea ingress for declared consumers (org-services→gitea gap closed). 
| R8 | gitea | [#4447](https://github.com/openova-io/openova/issues/4447) | gitea-flux-auth secrets are seeded — Flux pulls from gitea with valid credentials. | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): 3 gitea-backed Flux GitRepositories READY=True with 'stored artifact for revision': catalyst-tenant-hw255-omani-works (main@92c70af2), openova-catalog-sovereign (catalog-sovereign@d5975c9c), openova-org-tenants (org-tenants@2ef3fb8b) → gitea-flux-auth credentials seeded and working. — hw255-2026-07-15 live kubectl-proof. | — | hw240 kubectl-proof: 3 gitea-backed Flux GitRepos READY=True (catalyst-tenant-hw240-omantel-biz, openova-catalog-sovereign, openova-org-tenants) all "stored artifact for revision" → Flux pulls from gitea with valid gitea-flux-auth credentials. 
| R9 | sso | [#4446](https://github.com/openova-io/openova/issues/4446) | oidc-gate client_secret is seeded for powerdns-admin — full OIDC round-trip completes. | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): Secrets seeded: pda-sso-oidc-credentials (7 keys) + powerdns-admin-sso-oidc-credentials (7 keys) + oidc-gate-powerdns-admin-oidc; oidc-gate-powerdns-admin pod 1/1 Running 0 restarts (52m). Live wire-proof: https://pdns-admin.hw255.omani.works/oauth2/start → 302 to https://auth.hw255.omani.works/realms/sovereign/protocol/openid-connect/auth?...client_id=powerdns-admin&kc_idp_hint=catalyst-pin — well-formed authorize l… — Authed completion of the round-trip is session-walled (no credentials for a read-only walker); the #4446 mechanism (client_secret seeded + KC leg) is proven live. | oidc-gate-powerdns-admin pod 1/1 Running 3h37m (+ oidc-gate-openova-flow) → client_secret seeded, OIDC round-trip serves. | hw240 kubectl-proof: oidc-gate-powerdns-admin-oidc + pda-sso-oidc-credentials (7 keys) secrets seeded; agentA SSO row36 confirmed powerdns-admin /dashboard/ signed-in, Administration menus, NO OAuth error → full OIDC round-trip completes. 
| R10 | orgs | [#4471](https://github.com/openova-io/openova/issues/4471) | org-controller RBAC carries update/patch — per-Org provisioning completes (was create-only, never reached Ready). | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): ClusterRole catalyst-organization-controller rules: organizations [get,list,watch,update,patch], organizations/status [get,update,patch], organizations/finalizers [update]; catalyst-organization-controller pod 1/1 Running; Organization CR hw255-omani-works exists (kind=internal, billing=showback, sovereign=hw255.omani.works). — update+patch present — no longer create-only; internal Org reached materialized state on this env. | — | — | hw240 kubectl-proof: ClusterRole catalyst-organization-controller organization verbs=[get,list,patch,update,watch] — carries update+patch (was create-only) → per-Org provisioning completes. 
| R11 | gitea | [#4354](https://github.com/openova-io/openova/issues/4354) | gitea git-data is PRESERVED across the host-ns re-home (#4325 de-vcluster) — no empty-PVC data loss. | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): PVC gitea-shared-storage Bound 20Gi RWO evs-ssd (ns gitea); Flux pulls real repo content from gitea (catalyst-tenant.git + openova.git GitRepos READY=True with sha1 revisions) → git-data present, not an empty PVC. — hw255-2026-07-15 live kubectl-proof; fresh-prov env so this proves the seeded-data path, matching the hw240 stamp pattern. | gitea pod 1/1 Running 3h35m + per-Org GitRepositories carry real content (revision sha1, stored artifact) → git-data present post host-ns re-home, not empty-PVC. | hw240 kubectl-proof: gitea PVC gitea-shared-storage Bound 20Gi + Flux pulls catalyst-tenant.git & openova.git from gitea successfully (GitRepos READY=True) → git-data PRESERVED across #4325 host-ns re-home, no empty-PVC data loss. 
| R12 | postgres | [#4460](https://github.com/openova-io/openova/issues/4460) | shared-pg `-mesh-rw` resolves in BOTH regions + CNPG streaming replication is live. | — | — | ☐ ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): kubectl: svc cnpg/cnpg-pair-bp-cnpg-pair-primary-mesh has service.cilium.io/global=true (ClusterMesh-global, resolvable from the peer cluster hw255-me-east-b); Continuum cnpg-pair-bp-cnpg-pair-continuum phase=Healthy primary=hw-me-east-215-a-rtz-prod; CNPG cluster 3/3 healthy. GET .../continuum/cnpg-pair-bp-cnpg-pair-continuum/replication-status -> 200 replicas[{region hw-me-ea… — Cannot honestly stamp pass read-only: replication health is explicitly 'unverified' by the CR, and the in-region-b DNS resolution probe needs pod exec (forbidden for this walker) or region-b kubeconfi… | — | hw240 kubectl-proof: dr-shared-pg/-b/-c Continuum phase=Healthy, primary=me-east-215-a standby=me-east-215-b, agentB console lag 0.0s async → -mesh-rw resolves in BOTH regions + CNPG streaming replication live. 
| R13 | convergence | [#4436](https://github.com/openova-io/openova/issues/4436) | region-B keycloak / gitea / harbor reach steady-state with NO CrashLoop. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 wire via console k8s API, cluster 5762118f63abef96-me-east-215-b-1): region-B keycloak-0 + keycloak-postgresql-0 Running 0 restarts; gitea + gitea-pg-1/2 + gitea-sso-configure Running 0 restarts; harbor-core/nginx/portal/registry/redis/pg-1/pg-2/sso-configure Running 0 restarts; zero containers in CrashLoopBackOff across all three namespaces (waiting.reas… — Region-B has no direct kubeconfig on this walker; state read through the authed console aggregation API (GET /api/v1/sovereigns/{id}/k8s/pods, 192 region-B pods). | hw240 kubectl-proof: region-B keycloak-0 / harbor-core / harbor-registry all Running 0-restarts (steady-state, no CrashLoop). NOTE: unrelated region-B pods hubble-ui-oauth2-proxy(9)/mimir-querier(6)/openova-flow-server(6) show early convergence restarts but are Running — not R13 targets. 
| R14 | model | [#4479](https://github.com/openova-io/openova/issues/4479) | console Organizations directory reads the live Organization CRs (not an empty in-memory store). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): GET /api/v1/organizations (authed) -> 200 returning exactly the live Organization CRs — proven DYNAMIC: at 08:39Z API=1 item (hw255-omani-works) matching kubectl get organizations -A (1 CR); after a peer walker created acme255 (~08:59Z), re-fetch -> 2 items (acme255 kind=customer state=bp_charts_installed + hw255-omani-works kind=internal state=done) matching kubectl (acme255, … — Directory reads live CRs, not an empty in-memory store — it tracked a CR created mid-walk within minutes. |
| R15 | funnel | [#4473](https://github.com/openova-io/openova/issues/4473) | funnel plan-slug propagates end-to-end — the chosen plan reaches the provisioned Org. | — | ⛔ | hw232: no funnel/customer Org (only internal Org) — needs funnel walk on a converged re-prov | Org uatwp4758 spec.planSlug=m — the funnel-picked plan M propagated end-to-end to the provisioned Org CR. | hw227-reconfirm-2026-07-06: hw227 funnel-created Org acme spec.planSlug=s — the funnel-picked Plan S propagated end-to-end to the Org CR. 
| R16 | funnel | [#4290](https://github.com/openova-io/openova/issues/4290) | the funnel + BSS Org doors are collapsed onto ONE path — `console.<slug>` lands 200. | — | ⛔ | hw232: no customer Org — funnel+BSS door collapse needs a customer-Org walk | funnel walked as ONE path plans→apps→addons→bcp→review→checkout→launch (voucher VCH-QFP52J4S2KEX → Org uatwp4758) — funnel+BSS Org doors collapsed. | hw227-reconfirm-2026-07-06: hw227 funnel walked as ONE path plans→apps→addons→bcp→review→checkout→launch (voucher ACMEUAT227LAUNCH → Org acme) — funnel+BSS doors collapsed. 
| R17 | orgs | [#4459](https://github.com/openova-io/openova/issues/4459) | deleting an Organization CR cascades cleanly — no orphaned ns / app / DNS leak. | — | ⛔ | hw232: destructive (org-delete cascade) + no customer Org to exercise the tenant-networking finalizer — re-prov + funnel | delete-cascade finalizer armed (uatwp4758 has orgs.openova.io/tenant-networking) + #4459 merged, but NOT exercised on-env (won't delete the live customer Org). Mechanism present, clean-cascade unverified live. 
| R18 | cutover | [#4450](https://github.com/openova-io/openova/issues/4450) | handover-key self-publish guard — the Sovereign does not re-publish its handover key on reconcile. | hw255 | ✅ | ⚠️ ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): Direct live proof of the guard firing: catalyst-api (catalyst-system, 1/1 Running) log 2026-07-15T04:53:39Z 'handoverjwt: preserving mothership-injected handover pubkey; not publishing local signer key (#4450)' secret=catalyst-system/catalyst-handover-jwt-public; secret exists as a single copy, age 80m (never re-created across reconciles). — Upgrades the prior hw224 ⚠️ 'mechanism in place but not exercised' — on hw255 the guard demonstrably executed on reconcile and declined to re-publish. | catalyst-handover-jwt-public present (single copy, 4h) — self-publish guard mechanism in place, but not exercised (would need a re-publish attempt to prove the guard blocks). 
| R19 | sandbox | [#4482](https://github.com/openova-io/openova/issues/4482) | sandbox-controller reaches Running (was CrashLoop on unset CATALYST_GITEA_TOKEN). | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): sandbox-controller-75cf97bbd5-8hnvs 1/1 Running 0 restarts (45m); log tail: 'Starting workers controller=sandbox controllerKind=Sandbox worker count:1' + idle-scaler started; zero GITEA_TOKEN/fatal/panic lines in log. — hw255-2026-07-15 live kubectl-proof — no CrashLoop on CATALYST_GITEA_TOKEN. | — | hw240 kubectl-proof: sandbox-controller Running 0-restarts (was CrashLoop on unset CATALYST_GITEA_TOKEN) — token now seeded. 
| R20 | delivery | [#4464](https://github.com/openova-io/openova/issues/4464) | the deploy-bot bumps image pins per-line (not a blanket bump) — stale-render avoided. | — | n/a | N/A | deploy-bot per-line pin bumping is a MOTHERSHIP/CI concern — not verifiable on a child Sovereign. 
| R21 | catalog | [#4432](https://github.com/openova-io/openova/issues/4432) | catalog-seed pins match the published ghcr chart versions — no inert lagging Blueprint CR. | hw255 | ✅ | 81 Blueprint CRs seeded pinned versions none lagging or errored ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): 81 Blueprint CRs seeded (kubectl get blueprints -A = 82 lines incl. header); sample pin parity: bp-postgres spec.version=0.2.12 == ghcr latest chart tag 0.2.12 (gh api orgs/openova-io/packages/container/bp-postgres/versions → '0.2.12'); bp-wordpress spec.version=0.4.12 matches the ledger's known-current; zero Blueprints in an error phase (status phase unset on all 81, none failed). — The hw240 cosmetic display-label lag (spec.version 0.2.10 vs delivered 0.2.12) is resolved on hw255 — spec.version now reads 0.2.12. | 81 Blueprint CRs seeded (matches catalog 81 blueprints; walk showed bp-wordpress v0.4.12 detail) → catalog-seed pins resolved from ghcr. | hw240 kubectl-proof: catalog-seed bp-postgres delivery source.version=0.2.12 = published ghcr latest (0.2.12) → delivered chart is current, not inert/lagging; wizard-created instances get the #4987 continuum.yaml. NOTE: the Blueprint spec.version display-label still reads 0.2.10 (cosmetic lag) — bump-to-match shipped as follow-up. 
| R22 | plane-isolation | [#4468](https://github.com/openova-io/openova/issues/4468) | bp-plane-isolation admits apiserver-egress + gateway-ingress CNPs — CNPG admission + gateway hop succeed. | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): CNP fleet live: 12 *-allow-apiserver-egress + 7 *-allow-gateway-ingress CiliumNetworkPolicies, zero with VALID=False. CNPG admission works: 8 CNPG clusters created and 'Cluster in healthy state'. Gateway hop works live: curl -k console.hw255.omani.works → 200, marketplace → 200 (distinct titles), grafana → 302 /login, pdns-admin → 302 KC. — Gateways report PROGRAMMED=False/no ADDRESS yet the HTTPS data path serves — the hop itself is demonstrably live (direct §854 path); the Gateway status condition is a separate anomaly already owned by the parallel RCA ag… | — | hw240 kubectl-proof: plane-isolation apiserver-egress + gateway-ingress CNPs present per app (gitea/keycloak/harbor/grafana/guacamole/newapi/…-allow-apiserver-egress + -allow-gateway-ingress); CNPG cluster healthy (apiserver admission works) + console 200 (gateway hop works). 

### Merged this session — code-done, roll-gated for live proof on the next fresh prov (⚠️ merged-pending)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| M1 | janitor | [#4466](https://github.com/openova-io/openova/issues/4466) (PR [#4493](https://github.com/openova-io/openova/pull/4493)) | janitor hardening — log-only/dry-run for a full live cycle before any destructive sweep. | — | ⛔ | hw232: mothership catalyst-api janitor dry-run — not observable on child |
| M2 | apps | [#4477](https://github.com/openova-io/openova/issues/4477) (PR [#4492](https://github.com/openova-io/openova/pull/4492)) | newapi admin-token seeded into OpenBao at provision (was unseeded → admin-promote fault). | — | ☐ | — |
| M3 | network | [#4475](https://github.com/openova-io/openova/issues/4475) (PR [#4495](https://github.com/openova-io/openova/pull/4495)) | vcluster-tier CNP applied host-side — the per-Org vcluster tier carries its split CNP. | — | ⚠️ | hw232: vcluster internal Org ns host-side cnp allow-gateway-and-apiserver VALID=True + default-deny-all + allow-same-org (mechanism proven; no customer M+ vcluster Org) |
| M4 | apps | [#4111](https://github.com/openova-io/openova/issues/4111) (PRs [#4494](https://github.com/openova-io/openova/pull/4494)+[#4496](https://github.com/openova-io/openova/pull/4496)) | agenity image-pull half — fresh-install ghcr-pull path. | — | ⚠️ | fresh-install path verified live (ghcr-pull reflects + pod recovers); the agentic-run half (M-side) is Anthropic-credential-walled — see G8/#4277. |

### Remaining open — 13, with the recorded gate (honest map)

| # | Epic | Ticket | Test case | Walk | Result | Gate |
|---|---|---|---|---|---|---|
| G1 | adoption | [#4488](https://github.com/openova-io/openova/issues/4488) | crossplane provider-opentofu Observe-only adoption populates on the fresh prov. | hw255 | ✅ | hw236: crossplane adoption POPULATED - 25 cloudadoptions Synced=True real external-names, provider-opentofu Healthy; 8/25 Ready (eip+subnet), 17 server/lb/network Synced-not-Ready = known #4620 gap ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): KUBECONFIG=/tmp/hw255.kubeconfig kubectl get cloudadoptions -A → 25/25 real adoptions SYNCED=True READY=True with real Huawei resource-IDs (e.g. adopt-eip-cp-me-east-215-a 2f703d8e-db1b-4da6-891b-ca3bcd527d2f, adopt-server-control-plane-0 5916b96f... ADDRESS 212.72.24.36, ADOPTED=true), covering eip/loadbalancer/network/server/subnet across both regions; kubectl get providers.pkg.crossplane.io → provider-opentofu INS… — Improvement over hw236 (was 8/25 Ready): the #4620 server/lb/network Synced-not-Ready gap is resolved live on hw255 — gh shows #4620 CLOSED and #4488 CLOSED. Fresh-prov adoption population fully green. | — |
| G2 | apps | [#4477](https://github.com/openova-io/openova/issues/4477) | newapi ES-sync drives the admin token into a fresh per-Org newapi (seed merged in M2). | hw237 | ⚠️ | seed merged newapi admin-promoted; per-Org ES-sync needs customer funnel Org | — |
| G3 | dr | [#4486](https://github.com/openova-io/openova/issues/4486) | DR self-fire exercises the standby-absent branch (no standby present). | hw255 | ⛔ | standby-absent branch not exercisable on 2-region prov (region-b replica present + sync) ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Standby-absent branch NOT exercisable on hw255: all 5 Continuums have a hot standby present — kubectl get continuums -A: dr-spine-{gitea,harbor,keycloak,openbao} primary=me-east-215-a hotStandbyRegions=[me-east-215-b] and cnpg/cnpg-pair-bp-cnpg-pair-continuum hotStandbyRegions=[hw-me-east-215-b-rtz-prod], all phase=Healthy (cnpg-pair: LeaseHeld=True, replicationLagSeconds=0). — Same verdict class as hw239 stamp: needs a standby-absent topology, not a defect; DR self-fire would additionally be a mutation (forbidden for this walker) | hw239: DR standby-absent branch NOT exercisable on this 2-region prov (standby present: all 5 Continuums standbyRegions=[me-east-215-b], phase=Healthy). Needs a standby-absent topology; not a defect. |
| G4 | adoption | [#4431](https://github.com/openova-io/openova/issues/4431) | VPC-quota reclaim demo on a fresh prov. | — | ⚠️ | EIP-quota — the reclaim demo needs a fresh prov cycle. | mothership-verify-2026-07-06T01:15: mothership [JANITOR] pass emits live orphanEIPsDeleted/orphanVPCsDeleted/orphanEVSDeleted fields (this pass: 0 orphan VPC/EIP, 19 EVS reclaimed) — the VPC/EIP-quota reclaim mechanism is live and running. The full fresh-prov reclaim DEMO (prov→wipe→quota-returns) still needs a fresh prov cycle (EIP-quota-walled). Refs #4431 |
| G5 | janitor | [#4466](https://github.com/openova-io/openova/issues/4466) | janitor log-only live proof (the dry-run full-cycle observation). | m | ⛔ | hw232: mothership catalyst-api janitor log-only — not observable on child | mothership-verify-2026-07-06T01:15: mothership catalyst-api [JANITOR] pass logged "destructive":false, "reaped":0, "reapErrors":0 — a full log-only/dry-run cycle observed live (reaped ZERO Sovereigns) while safely GC-ing 19 orphan EVS + 1 orphan keypair. The dry-run full-cycle observation. Refs #4466 |
| G6 | model | [#4212](https://github.com/openova-io/openova/issues/4212) | object-model seams 1+2 — the two adoption seams collapse into the #4488 crossplane-adoption walk (G1). | hw255 | ✅ | hw236: collapses into #4488/G1 adoption walk - 25 CRs populated + provider healthy; fully-Ready half per #4620 ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): Row is defined as collapsing into the #4488/G1 crossplane-adoption walk. G1 evidence on hw255: 25/25 cloudadoptions Synced=True Ready=True with real external resource-IDs + provider-opentofu Installed=True Healthy=True. The previous caveat ('fully-Ready half per #4620') is resolved — all real CRs Ready=True and gh shows #4620 CLOSED. — Umbrella issue #4212 is still OPEN on GitHub (close-out is the orchestrator's call), but the row's own assertion — the two adoption seams proven via the G1 walk — is live-green on hw255. | — |
| G7 | orgs | [#4293](https://github.com/openova-io/openova/issues/4293) | vcluster dual-door walk — both Org doors land a vcluster-isolation Org. | hw236 | ⚠️ | hw236: customer vcluster Org acme236 plan=m billing=real, vcluster HR Ready=True (vcluster@0.33.4) - isolation backing present; dual-door funnel+BSS browser walk deferred | — | — |
| G8 | apps | [#4277](https://github.com/openova-io/openova/issues/4277) | anthropic credential seeded into the agentic runtime — chat works end-to-end. | hw255 | ❌ | hw228 DIRECT-WALK 2026-07-07: agentic runtime NOT deployed — agenity.hw228.omantel.biz → 000, no agenity/agentic pods (only sandbox-controller runs), no anthropic secret. Can't chat end-to-end without the runtime. ❌ Refs #3969 ‖ hw228 2026-07-09 LIVE: the sandbox PLATFORM is deployed (sandbox-controller 1/1 Running, bp-sandbox HR Ready chart 0.3.11) but NO anthropic/claude credential secret exists in any namespace → the agentic runtime has no Claude key → chat cannot run. Gated on the agentic-runtime cred-provisioning + a live sandbox session (Pillar-4 sandbox completion, adjacent to the post-SME MCP build #3988/GOV#899). Stays ❌ — not a quick fix. ‖ 2026-07-09 (G8 fix-agent, orchestrator-verified): the Anthropic-cred CHAIN is 100% built (#4277/#4111: ExternalSecret openbao catalyst/anthropic/token → agenity ANTHROPIC_API_KEY; seedAnthropicToken at Org-provision; api-deployment env wiring — all verified live). Gap = NO anthropic-api-key/credentials-json key in catalyst-openova-kc-credentials on hw228 OR the mothership (no key ever placed → seedAnthropicToken skips by design). FOUNDER ACTION: place a claude-code credentials.json into the mothership secret (chain resolves it) + decide on cloud-init propagation to Sovereigns. NOT agent-fixable (founder-supplied cred + security decision + agenity not installed on hw228). Stays as-is. ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): kubectl get secrets -A / grep -iE 'anthropic/claude' → EMPTY (zero Anthropic/Claude credential secrets cluster-wide on hw255); kubectl get pods -A / grep -iE 'agenity/sandbox' → only sandbox-controller-75cf97bbd5 1/1 Running, NO agenity/agentic runtime pods. The row's assertion 'anthropic credential seeded into the agentic runtime — chat works end-to-end' is live-false. — Same founder-gated state as hw228: the cred chain is built but no credentials.json was ever placed in the mothership secret (seedAnthropicToken skips by design) and bp-agenity is not installed. gh: #4277 OPEN. Not agent-… | hw227-gate-verify-2026-07-06T01:35: VERIFIED-BLOCKED — kubectl shows NO agenity pods/Application on hw227 (agentic runtime not deployed) AND zero anthropic/claude secrets in the cluster ([]). Agentic chat cannot be walked without the app + a real Anthropic credential (neither present). Refs #4277 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: 0 Anthropic secrets in-cluster AND agenity not deployed on hw227 — agentic chat cannot run. Refs #4277 ‖ **GATE CONFIRMED 2026-07-07 (repo inspection):** #3988 built the openova-MCP SERVER (products/openova-mcp/ — Go cmd/internal/{identity,tools,catalystapi}) but NOT its deployment layer: NO Chart.yaml, NO Dockerfile, NO blueprint.yaml, NOT in the bootstrap-kit or catalog. So the RBAC-scoped MCP facade cannot be deployed on ANY Sovereign (confirmed absent on hw228 — only sandbox-controller runs). These rows are gated on PACKAGING the server (image build workflow + Helm chart + blueprint.yaml + bootstrap-kit slot + per-Org KC identity wiring), a substantial build cycle, not a simple deploy. Refs #3988. ‖ **2026-07-09 LEDGER CORRECTION (repo re-inspection, evidence-backed):** the earlier 'NO Chart.yaml/blueprint.yaml/bootstrap-kit/catalog' gate note is FACTUALLY WRONG — it inspected `products/openova-mcp/` only. By design (`docs/INTEGRATION-OPENOVA-MCP.md` §1) the MCP server is baked as a subprocess binary inside the **`bp-agenity`** runtime image, and that deployment layer IS fully built: `products/agenity/blueprint.yaml` (kind:Blueprint, bp-agenity v0.5.20, visibility:listed), `products/agenity/chart/` (statefulset/httproute/oidc-gate/externalsecret-anthropic/externalsecret-mcp-bearer/CNPs), `products/agenity/Containerfile` (bakes `/usr/local/bin/openova-mcp`), catalog-seed `blueprints.yaml:5981`, bootstrap-kit `13-bp-catalyst-platform.yaml`, CI `agenity-build.yaml`. RBAC (list/get/create_application Org-scoping, cross-Org 403) is implemented + unit-tested in `products/openova-mcp/internal/{identity,tools}`. REAL blockers (NOT 'unbuilt'): (a) founder places a live Anthropic credential (`CATALYST_ANTHROPIC_CREDENTIALS_JSON`) → `sovereign_anthropic_seed.go:140` stops loud-skipping — FOUNDER action; (b) install `bp-agenity` into a live Org + walk; (c) close fresh-Org bearer-wiring defect **#4624**. Refs #3988 #4277 #4276 #4624 |
| G9 | apps | [#4111](https://github.com/openova-io/openova/issues/4111) | agentic-run half — the agenity solo agent chats + drives create_application. | [agenity.demo](https://agenity.demo.omani.homes/app/) | ⚠️ | agentic-run/SSO half PROVEN on 8fd457a8 (claude -p → NORTHSTAR-OK); MCP create_application BLOCKED on fresh-Org agenity-MCP auth-wiring (pubkey-not-reflected [fixed live] + OPENOVA_MCP_BEARER-not-seeded → tools/list empty) — filed #4624. Agent did NOT fabricate. [[../sessions/2026-06-28/evidence/north-star-8fd457a8.txt]] ‖ hw228 2026-07-09 LIVE: no active sandbox session + no anthropic cred (see G8) → the agenity solo agent isn't live to chat/drive create_application. Gated on sandbox/agentic-runtime completion (Pillar 4, post-SME-adjacent). Stays ⚠️. ‖ 2026-07-09 (G8 fix-agent, orchestrator-verified): the Anthropic-cred CHAIN is 100% built (#4277/#4111: ExternalSecret openbao catalyst/anthropic/token → agenity ANTHROPIC_API_KEY; seedAnthropicToken at Org-provision; api-deployment env wiring — all verified live). Gap = NO anthropic-api-key/credentials-json key in catalyst-openova-kc-credentials on hw228 OR the mothership (no key ever placed → seedAnthropicToken skips by design). FOUNDER ACTION: place a claude-code credentials.json into the mothership secret (chain resolves it) + decide on cloud-init propagation to Sovereigns. NOT agent-fixable (founder-supplied cred + security decision + agenity not installed on hw228). Stays as-is. ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255 backing state (read-only kubectl): secret catalyst-system/catalyst-openova-kc-credentials contains ONLY [kc-addr, kc-audience, kc-realm, kc-sa-client-id, kc-sa-client-secret, smtp-*] — NO anthropic-api-key/credentials-json; no agenity HelmRelease or Application anywhere (grep over hr/applications -A empty); zero Sandbox CRs; sandbox-controller 1/1 Running (ns sandbox). — The 'agenity solo agent chats' half is founder-cred-gated (#4277: no Anthropic credential ever placed; seedAnthropicToken skips by design) and bp-agenity is not installed per-Org on hw255; the 'drives… |
| G10 | placement | [#3969](https://github.com/openova-io/openova/issues/3969) | placement EPIC — the remaining real build. | hw237 | ⚠️ | Application CRs carry placement active-hot-standby singleton active-passive; Apply authed gap 4950 | — | — |
| G11 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | sovereignty cutover — 8-step + 10-min deny-egress hold. | hw255 | ✅ | **2026-07-01 (#4682):** the cutover's registry-gateway prerequisite is re-architected at the source — gateway now a `Service type=LoadBalancer` on **:443** (Gateway API + Cilium + Envoy), registry reached via external host `registry.<fqdn>:443`, the `:30443` NodePort/coredns/sysctl-DaemonSet workaround chain **deleted** (PR #4684 merged `3e650b623`, net −133; delivered — catalyst-api `3e650b6` baked-`:443` verified). **LIVE-PROVEN on the 2-region hw207 prov (`f5fdc757`):** `cilium-gateway-cilium-gateway` Service = type=LoadBalancer on 443/80 (was ClusterIP :30443), listeners `https=443/http=80`, `sysctl-envoy-bind` DaemonSet NotFound — evidence `uat-hw207-2region-provisioning-443fix-2026-07-01.png` + `uat-sovereign-deployments-hw207-2region-2026-07-01T1431.png` (hw207 reached **ready**, 2-region, Secondary-degraded). **2026-07-02 fresh 2-region prov hw208 (`cdf4c53b`) — reached ready, owner-scoped to emrah.baysal@openova.io (#4683 confirmed), evidence `uat-sovereign-deployments-hw208-ready-ownerscoped-2026-07-01T1842.png`.** Gateway=LoadBalancer :443 sharing the clustermesh CP-node EIP via LB-IPAM sharing-key (#4687); `sysctl-envoy-bind` absent; no gateway ELB — checks 1/3/4 PASS. Fixed a P0 en route (#4689: empty sharing-key default → YAML null → CRD reject → sovereign-tls wedged). ⛔ **BLOCKED: console external-serving FAILS + cutover BLOCKED — the CP-node EIP is a Huawei 1:1 NAT (not on-link), so the Cilium LB VIP keyed on the public EIP never receives the post-NAT packet → `console:443` REFUSED externally (#4686). In-cluster the gateway serves fine (ClusterIP:443→200). Fix = `externalTrafficPolicy=Local`+hostPort on envoy (host-level :443 receipt), needs an off-cluster-curl live gate.** Prior throwaway proof (`5150f96`: `cutoverComplete=true`) predates the #4682 re-architecture — re-walk once #4686 lands. **2026-07-02 (#4691 merged `acfbd66`) — #4686 RESOLVED at source:** the fix is NOT the ETP+hostPort guessed above but a restored Crossplane/tofu Huawei **ELB** (`elb_primary`, its OWN routable EIP) forwarding public :443/:80 → the gateway Service's nodePort — a dst-IP-agnostic host listener (like apiserver :6443) that RECEIVES the post-NAT packet the CP-EIP-keyed Cilium VIP never did; option-B (CP-EIP sharing-key #4687) removed; DNS → the ELB EIP; catalyst-api reconciles the ELB members to the live nodePort post-convergence. **Component-verified:** ELB present in the running catalyst-api's baked tofu (`elb_primary refs:9`); `nc 127.0.0.1:<nodePort>` OPEN on hw208 (nodePort→envoy serves); ELB EIP distinct from CP EIP on hw209/hw210. **Owner-scoping (#4683) re-verified 2026-07-02** via the authenticated deployments API — returns ONLY emrah.baysal@openova.io's deps, zero foreign-owner leakage (PASS); the console UI *render* is PIN-to-email gated (evidence `login-pin-gated.png`; the API is the render's own data source). ⛔ **End-to-end off-cluster `curl https://console.<fqdn>` gate STILL PENDING** — hw209/hw210/hw211 all hit the kom4dc cilium-bootstrap DNS-wedge (**#4692** filed, root-caused: the cloud-init /etc/hosts pin-loop resolves helm.cilium.io via flaky VPC DNS at pin-time with no retry). No converged prov yet to run the live gate; re-walk once #4692 lands + a prov converges. **2026-07-02 live Playwright console-login walk (this session):** navigated `/sovereign/deployments` → client-redirect to `/sovereign/login` (PIN-to-email) → red **`could not deliver code`**; root-caused via catalyst-api log to the SMTP send failing on `mail.openova.io` **flaky-upstream-DNS** (`server misbehaving`) → `/api/v1/auth/pin/issue` **502** (**#4696**). Fixed at source: **#4697** merged `adef16ec8` — chart SMTP host → in-cluster `stalwart-web.stalwart.svc.cluster.local` (matches the auth.go code default; chart 1.4.980); operator PIN-login (incl. the founder's own) delivers reliably on the next bp-catalyst-platform roll. Evidence `sovereign-deployments-pin-gate-2026-07-02.png` + `sovereign-login-could-not-deliver-code-2026-07-02.png`. The UI *render* stays PIN-gated **by the founder's #688 design** (magic-link/auto-login rejected; PIN is crypto/rand in-memory) — an agent cannot self-authenticate; owner-scoping DATA verified via the authenticated API (PASS, above). ‖ **2026-07-09 CUTOVER FIRED on hw231 (fresh env, dep ab1355c25f9798c3):** operator-fired via the canonical runner-SA in-cluster trigger; `CATALYST_CUTOVER_ACK_TOLERANCE=1` (#4870 fix) baked. Steps PROVEN sequentially: 01 gitea-mirror ✅, 02 harbor-projects ✅, 03 harbor-prewarm ✅ (Phase A push_ok=30/0-fail, full image mirror to registry.hw231.omantel.biz), **04 registry-pivot ✅ — the #4870-gated step: registry-pivot DaemonSet 6/6 READY, ack-gate cleared** (this is the EXACT step that FAILED on hw228 with tolerance=0 + a dead-dfdaemon node; tolerance=1 let 6/6 ack cleanly), 05 flux-gitrepository-patch ✅, 06 helmrepository-patches (running). Remaining: 07 catalyst-api-env-patch, 08 egress-block-test (10-min deny-egress hold), 09 gitea-token-mint, 10 vcluster-registry-pivot, 11 crossplane-provider-pivot. cutoverComplete pending final steps. Refs #3379 #4870 ‖ **UPDATE 2026-07-09 — cutover WEDGED at step 6 on a NEW real defect (#4873), fix shipped PR #4874:** steps 01-05 ✅ incl. **registry-pivot #4870 ack-gate PASSED 6/6** (the hw228-failing step — #4870 VALIDATED). Step 06 helmrepository-patches retry-loops: `bp-catalyst-platform:1.4.1038 not pullable` — harbor-prewarm mirrored 1.4.1039 (a deploybot bump the gitea-mirror step pulled in raced `.spec.chart.spec.version`) while step-06 checks the settled deployed 1.4.1038. Both steps read the mutable desired version → skew. FIX #4874: read immutable `.status.history[0].chartVersion` in both. Full cutoverComplete=true (steps 06-11 + 10-min deny-egress) previously PROVEN on hw148 (task #847) — this env hit the version-skew race; the clean seal needs a fresh prov on cutover-chart 0.1.104. Registry-pivot half of Pillar-5 proven; final seal pends #4874 + re-prov. Refs #3379 #4870 #4873 #4874 ‖ **2026-07-15 hw253 (dep 8a0f810a, clean re-fire after the hw252 mid-cutover-merge version-drift):** §854 PASS on the fresh prov — kubectl wire-capture: **0 `type=NodePort` Services, 0 LB-allocated-nodePorts**; gateway = `cilium-gateway ClusterIP` 80/443 (hostPort/LB path, §854-compliant, no nodePort). (Mothership carries 4 legacy app/API NodePorts — separate pre-existing gap, filed #5088 + Kyverno audit-guard PR #5089.) Cutover FIRED 00:45:16Z via runner-SA (operator-gated #4061); `drive-cutover.sh` driving the 11-step chain → step-08 minimal-roll-set crux (#5081, clears the hw251 CSI-desync); **merges HELD** so no prewarm version-drift (the hw252 step-06 wedge). cutoverComplete PENDING. Refs #3379 #5081 #5088 ‖ hw255-cc-2026-07-15T07:21Z: cutoverComplete=TRUE zero-touch (cutover 0.1.135, 05:03→07:21Z, all 11 steps success; step-08: ref-host lint PASS + mothership black-holed #3678 + 15/15 minimal roll-set PASS under deny-egress (CSI excluded #5081, gitea RWO-EVS excluded #5091) + 600s hold zero regressions; registriesYaml v2 on 6/6 nodes; dragonflyUpstream=local; prewarm transit-flap FATAL recovered by designed re-run #5030). #3379 CLOSED. ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): kubectl -n catalyst CM self-sovereign-cutover-status: cutoverComplete=true, cutoverStartedAt 2026-07-15T05:03:23Z, cutoverFinishedAt 07:21:07Z, 11/11 step.*.result=success, egress-block-test 06:54:33->07:20:31Z (incl. 600s deny-egress hold), registriesYaml=v2 on all 6 nodes, dragonflyUpstream=local, progressPercent=100. Authed GET /api/v1/sovereign/cutover/status -> 200 state=s… — Re-verifies the existing hw255-cc-2026-07-15T07:21Z ✅ stamp from live state; zero-touch keystone intact (no mutations performed by this walker). |
| G12 | dr | [#4275](https://github.com/openova-io/openova/issues/4275) | region-kill (Pillar-3) — kill region-b, prove failover + recovery. | — | ☐ | — |1000, new primary accepts INSERT 1001) -> demote-before-uncordon restore, no split-brain (region-a healthy 3/3, region-b replica healthy). Pillar-3 #4275 | — | — | streaming | sync | — | streaming | — |

> **Reading the gate map:** the 13 remaining work-items resolve to **12 distinct open tickets** (#4212 carries object-model seams 1+2, both folding into #4488/G1 — counted as one ticket here, two seams in narrative). 7 of them (G1–G7) clear in ONE fresh post-fix 2-region prov the moment kom4dc EIP quota rises **10 → ≥16**. 2 (G8–G9) need a real Anthropic credential. 1 (G10) is a genuine remaining build. 2 (G11–G12) are destructive walks needing a throwaway env (itself EIP-walled) or explicit founder clearance. None is a false-fail; each is an honest fault with a named unblock.

---


**RESET 2026-06-26 — clean slate for the fresh Sovereign, now walked on fresh prov `91dc05917e44d1c1` (omantel.biz, region-a); recounted after the 2026-06-27 4-section live-walk consolidation + the 2026-06-27 Pillar-1 funnel+voucher walk + the 2026-06-27 newapi-SSO resync walk + the 2026-06-27 authed operator-console walk + the 2026-06-27 Pillar-4 Sandbox/MCP walk + the 2026-06-27 authed live-walk sweep + the 2026-06-27 write-mutation walk on fresh prov `25aadcfc` + the 2026-06-27 ⚠️/❌ reconcile + the 2026-06-27 3-agent live-walk batch (Refs #909): 193 ✅ · 0 ❌ · 9 ⛔ · 33 ⚠️ · 4 ☐ = 239 rows.** (⚠️/❌ reconcile against the live env flipped 7 rows to ✅ with reproduced live evidence: Hubble row 39 `⚠️`→✅ — bp-cilium@1.4.5 rolled, oauth2-proxy `--api-route=^/api/` live, `/api/*` returns 401-not-302; funnel /review row 83 `⚠️`→✅ — marketplace:f1f7ae6 ships the #4526 ReviewStep topology card; OpenBao admin row 33 `❌`→✅ — bp-openbao@1.2.58 rolled, OIDC auth_url now requests `scope=openid+groups` + sso-operator-admin policy mapped (the 403 root cause resolved); Jobs Re-run row 176 `❌`→✅ — catalyst-api e976ff0 ran the #4514 delete+recreate (`mimir-make-minio-buckets-rerun-*` Complete, original deleted, no 502); Topology rows 62/187/188 `⚠️`→✅ on the same data-grounded standard as 64/71 — live Continuum CRs Healthy/Ready/LeaseHeld + grafana multi-region active-active + catalyst-api single-region singleton, all via kubectl, UI render SSO-session-walled. RECLASSIFIED rows 9/10/11/12 `❌`→⚠️ (and corrected 216's rationale): the uatwalk91 host-namespace backing is DESIGN-CORRECT for its `plan=s` per the #4292 TIER GATE — `boundaryIsVcluster`/`allTiersVcluster=false` backs free/S Orgs with the host `<slug>` namespace, only M+ get a dedicated Org-vcluster — so the blanket `vcluster`-for-every-Org expectation is stale, leaving only a cosmetic `isolation=vcluster` label mismatch. Row 183 HELD ❌ (the 403 root cause is #4522-fixed but the row asserts the rendered Secrets-Engines screen, SSO-session-walled). Rows 37/114 confirmed stayed ✅ (newapi bare URL → 200 'Signing you in…', `sovereign` provider recognized). NO false-pass: every ✅ flip reproduced the live signal at the HTTP/kubectl/served-bundle layer; the authed test-session mint is 404-gated in prod so interactive UI renders stay honestly ⚠️/❌ where un-reproduced.) (Authed operator-console walk via a throwaway-mailbox PIN session flipped 11 interactive-UI rows `⛔`→✅ — treemap/showback 20/21/23, keycloak-admin 34, Organizations directory/terminology 116/117/118/119/120/121/122 — and exposed 2 genuine product faults `→❌`: OpenBao authed admin UI 403 row 33, Jobs Re-run 502 immutable-spec row 176, both with fixes in flight.) (newapi-SSO walk flipped rows 37 + 114 `❌`→✅ — #4477 `resync_provider_secret()` (bp-newapi@1.4.137) resolved the stale `custom_oauth_providers` client_secret live, no "Unknown OAuth provider"; Hubble row 39 holds ⚠️ fix-merged-pending-roll for PR #4509, the `--api-route=^/api/` flow-XHR fix.) (Pillar-4 Sandbox/MCP walk via a throwaway Sandbox CR on `91dc05917e44d1c1` region-a flipped row 218 `⛔`→✅ — sandbox-controller no longer CrashLoops (R19/#4482 gitea-token bridge), Sandbox CR reconciles `Ready=True` — and flipped 219 + 220 `⛔`→⚠️ PARTIAL (controller writes 12 gitops files incl the MCP ConfigMap + pty-server StatefulSet; the image bundles `@anthropic-ai/claude-code` + `@qwen-code` + the `/usr/local/bin/openova-sandbox-mcp` binary — runtime-pod-materialize + live agentic run not awaited). Rows 221/222/223 HOLD ⛔ on an Anthropic credential: the MCP `create_application` tool + the openova-mcp Org-claim RBAC are proven at the API layer (211/212/213), only the live chat-driver is credential-walled. The MCP auto-mount is PROVEN — `configmap-mcp-config.yaml` projected at all 4 agent-discovery paths (`/workspace/.mcp.json`, `~/.claude.json`, `~/.qwen/settings.json`, `.cursor/mcp.json`) + the MCP binary in the published image. ⚠️ note: the sandbox-controller has no delete-finalizer to prune the gitea gitops path on Sandbox CR delete (minor cleanup gap); a subset of Wave-2 MCP tools (`registry.go`, `sandbox_db.go`) return `not_implemented` stubs — surface discoverable, some advanced bodies stubbed.) (Consolidation pass 2 of the live fresh-prov walk — folds §C orgs/keystone · §G model+placement · §H contexts+mcp+sandbox · §I recon/cloud/e2e/meta on top of pass 1's §A funnel · §B SSO · §D topology/DR · §E jobs/cloud/storage · §F session-fix.) Per-row cells carry the walker's exact verdict + evidence; the 100 remaining `☐` are genuinely-unwalked surfaces — no fabricated verdicts. **Pass-2 corrections applied:** (1) per-Org provisioning is PROVEN post-#4471 RBAC fix — funnel Org `uatwalk91` serves WordPress HTTP 200 over valid pool LE TLS, so the prior ⛔ "org-controller RBAC" cells flipped ✅ for the WordPress-backed rows (86/88/89/90/233) and ⚠️ "this app not in the walked Org's cart" for openclaw/mail/postgres (232/234/238); (2) the per-app **Topology tab exists** (§I `app-tab-topology`), falsifying §D's "no Topology tab" premise — rows 51-58/62-64/71/187/188 moved ❌→⚠️ pending a content re-walk; (3) placement rows 98-108 are **superseded by #4325 de-vcluster** (re-baseline #3642, not a product fault) → ⚠️, not ❌; (4) §I canvas-click drill-ins (179/181/184/186/193/195-197) are ⚠️ harness artifacts, not click-verified; (5) restored 9 §A/§F walker-PASS rows (40/76/79/80/84 §A funnel; 227/230/231 §F ✅, 228 §F ⚠️) whose cells reverted to ☐ in a working-tree branch-switch — re-applied from the captured walker evidence, no new walk. (Write-mutation walk on fresh prov `25aadcfc` [kv160729.omani.works, a fresh prov on current main; writes are safe + were cleaned up] flipped **26 rows `☐`→✅**: catalog Save/commit-IaC rows 127/128/129/132–140/142/143/145/147/148/149/153–156 — `PUT /api/v1/catalog/<bp>/iac` → 200 `{committed:true}` + durable bare-repo Gitea commit, proven on two structurally-different blueprints bp-alloy + bp-wordpress; instance-provision rows 50/59/60/61 — `POST /catalyst/v1/apps/instances` → 201 + CR Ready + matching topology badge. These rows were GATED only by the read-only constraint on the PERMANENT env — the catalog-IaC-commit + provision CODE is identical, proven on the disposable throwaway.) The detailed historical narrative below (omantel.biz convergence, the hw1xx throwaway envs) is retained ONLY as explicitly-labeled prior-env history — its per-row claims are STALE by construction and must not be read as current. (Refs #4279.)

> 📋 **Row map:** rows **1–226** are the canonical acceptance walk (epics: model · onboarding · topology · SSO · jobs · recon · cloud · storage · mcp · e2e-journey · convergence · funnel · meta). Rows **227–239** are the **Fresh-prov 2026-06-26 — session-fix acceptance** section (this session's resolved-issue cases — delivery-architecture, fresh-funnel apps, spine/postgres/crossplane). All ☐ until the fresh-prov walk.

## Prior — omantel.biz PERMANENT Sovereign (dep `4635277cae4ffed9`, me-east-215-a/-b) — STALE, superseded by the fresh prov
_All per-row verdicts below were walked on the now-superseded omantel.biz env and are STALE by construction. Retained as labeled history; the live verdict cells above are all `☐` pending the fresh-prov walk._

## Prior — wiped throwaway envs (hw173/hw176/hw177/hw178 — STALE, superseded by omantel.biz)
_hw178 (dep `02bc518fcb1b4cb3`) — cycle-2d zero-touch re-fire; 6 fixes walked PASS; superseded. hw177 wedged on a worker-kubelet infra flake; wiped. All hw1xx envs are wiped — their per-row verdicts do not reflect omantel.biz._

## Prior — cycle-2b BLOCKED on hw176 storage wedge (2026-06-21, now fixed)
_hw176 (dep `12523e0c7611356b`) — zero-touch cycle-2b re-fire on `main@10b061b`. **STORAGE FIX DID NOT HOLD zero-touch (❌, headline).** HRs flooded to 67/region (past the old 40/50 wedge) but `bp-huawei-evs-csi` 1.0.1 HelmRelease → InstallFailed: `no matches for kind VolumeSnapshotClass in version snapshot.storage.k8s.io/v1` (the chart renders a VolumeSnapshotClass by default but the snapshot.storage.k8s.io CRDs are installed nowhere in the bootstrap-kit) → no `evs-ssd` SC → 6 stateful PVCs Pending → `bp-gitea` not ready → `bp-catalyst-platform` blocked → console HTTP 000. Root-caused live + fix shipped: #4030 / PR #4031 (chart 1.0.2, volumeSnapshotClass default off, MERGED). The cycle-1 fix-wave was ⛔ un-walkable behind the wedge — health-gate honored. Per-row evidence: [`../sessions/2026-06-21/walk-hw176/`](../sessions/2026-06-21/walk-hw176/)._

## Status — last validated: hw173 (2026-06-20)
_Update: topology rows 46–71 re-walked across BOTH region clusters (#3965 fix) → 0✅/14❌ became 9✅/9❌/8⚠️; the remaining ❌ are the Continuum-lease unfinished-feature gap + no-live-catalog-create, not region blindness._
**124 ✅ · 32 ❌ · 30 ⚠️ of 186.** Verified live via API + mothership-kubectl by 7 parallel walkers (#883); per-row evidence in [`../sessions/2026-06-20/walk-hw173/`](../sessions/2026-06-20/walk-hw173/). Headline defects: hw173 came up **single-region** → topology epic unwalkable (14❌, Continuum Degraded); **funnel provisioning CrashLoop** (FerretDB `partialFilterExpression` unsupported) → zero customer Orgs → model/orgs `acme` rows fail; **Harbor down** (core CreateContainerConfigError + jobservice CrashLoop); **Gitea SSO 500**; **Tenants/SME banned-term leak** in console bundle.

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| 1 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Console bare URL → lands `/dashboard` signed-in as the owner (no PIN/login form). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Owner session (GET /api/v1/whoami -> HTTP 200 tier=owner, roles catalyst-admin/catalyst-owner) + browser: bare https://console.hw255.omani.works/ landed on /dashboard signed-in as emrah.baysal@openova.io (EB avatar, full sidebar), no PIN/login form. Unauthed control: bare URL -> /login?next=%2Fdashboard and /api/v1/whoami -> 401. Screenshot docs/sessions/2026-07-15/hw255-wave2/… — Walked with the injected wave-2 owner session per briefing; treemap defaults Layer1=Progress/Layer2=Kind render live. |
| 2 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Full sidebar renders (Dashboard / Cloud / Apps / Catalog / Sandbox / Jobs / Compliance / Users / Organizations / Settings). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Authed browser walk console.hw255.omani.works/dashboard: sidebar renders Dashboard(/dashboard), Cloud(/cloud), Apps(/apps), Catalog(/catalog), Agenity(/sandbox), Jobs(/jobs), Compliance(/sre/compliance), Users(/users), Organizations(/organizations), Billing(/billing), Settings(/settings). Screenshot row2-sidebar-w22.png. — Row text lists 'Sandbox' — live sidebar shows 'Agenity' (href /sandbox); Sandbox concept superseded by Agenity per founder 2026-06-30. All other named items present, plus Billing. |
| 3 | model | [#3687](https://github.com/openova-io/openova/issues/3687) [#4546](https://github.com/openova-io/openova/issues/4546) | The voucher-redeem URL on an authed owner session server-redirects to `/dashboard` (no redeem form for the owner). | hw237 | ⚠️ | authed owner redeem redirect not unauth-testable historically 3687 | — |
| 4 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | App page tab strip generality: open ≥2 archetypes (a DB `shared-pg`, a consumer `harbor`) → identical tab strip; consumer Dependencies shows `Depends on: shared-pg / db:<ctx>`. | hw255 | ❌ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): /app/shared-pg tab strip Overview·Contexts(3)·Topology·Resources·Compliance·Logs·Settings·Members·Endpoints·Jobs·Dependencies(8); /app/spine-harbor identical strip minus Contexts (by-design per row 24). FAIL leg: shared-pg Contexts tab proves harbor occupies context db/registry (row 'db/registry harbor → harbor-database-secret ready') yet… — FE mechanism exists (AppDetail.tsx:783 renders 'Depends on:' from appDependsOn) but derives no consumer-side edge on hw255 — producer Contexts shows the binding, consumer Dependencies omits it. |
| 5 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Organizations directory: the customer Org row shows KIND=customer, TIER=sme, BILLING=real, ISOLATION=vcluster, STATUS=active. | — | ☐ | — | — |
| 6 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The customer Org is present as a real Organization in the operator directory (same record dashboard + showback read). | — | ☐ | — |
| 7 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail renders canonical fields: slug `acme` (NOT `sme-<uuid>`), kind customer, tier sme, billing real, isolation vcluster, owner, console URL. | — | ☐ | no customer Org acme; only internal Org | — |
| 8 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The Create-organization form renders fully (kind picker, slug, Company name, Admin email, parent-domain). | hw255 | ✅ | Create-organization form render authed; internal Org present ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255 authed browser 2026-07-15: /organizations/new renders full form — Organization kind picker (Internal/Customer), Organization slug, Company name, Admin email, Domain mode + Parent domain select (omani.homes/rest/trade/works), Console URL preview, Create organization submit. Screenshot row8-create-org-form.png. — Form opened read-only, never submitted. |
| 9 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail Status = active, backed by a real `vcluster` isolation (the create→Provision loop produced a live backing, not a fake-green). | — | ☐ | no customer Org present; vcluster-backed Org needs funnel walk | — |
| 10 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The customer Org is present + ACTIVE backed by a real `vcluster` isolation (Lane B convergence read). | — | ☐ | no customer Org present; needs funnel walk | — |
| 11 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail Status = active and the directory + detail consistently show `vcluster` isolation. | — | ⚠️ | hw232: acme 0 running pods (7 tenant HRs queued, chart-pull blocked). hw232 ⚠️ env-transient: mechanism works, blocked by stuck cutover deny-egress hold (egress-block-test Running 45m vs 10m design; #4885 defect-3 12 non-pivoted ghcr.io workloads never reconcile → hold never completes → starves chart/image pulls). Resolves green on clean re-prov post-#4885. | — |
| 12 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | On re-load the Org detail consistently reports active + `vcluster` (stable, no false flicker). | — | ☐ | — | — |
| 13 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Catalog grid renders the Blueprint cards; `bp-postgres` detail has a **+ New instance** button. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Authed browser: GET https://console.hw255.omani.works/catalog -> 200, grid renders 81 Blueprint cards (Agenity, Alloy, Axon, ... per GET /api/v1/catalog -> 200 items=81). /catalog/bp-postgres detail renders with exactly one '+ New instance' button (buttons scan: ['+ New instance']). Screenshot row13-bppostgres-newinstance.png — hw255-2026-07-15 W2-5 authed walk (owner session) |
| 14 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The shared-PG reuse model is LIVE: the `shared-pg` app **Contexts** tab shows multiple consumers sharing ONE PostgreSQL. | hw255 | ❌ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): /app/shared-pg -> 'App not found — The component shared-pg is not part of this deployment.' /app/bp-postgres renders tab 'Contexts 0'; panel text: 'No Contexts declared on this instance yet. Consumers occupy a Context when they are provisioned against this instance…'. Screenshot row14-sharedpg-contexts.png — No multi-consumer sharing rendered. Matches hw236 note: data-layer real but Application-CR dependsOn binding empty -> tab renders empty. Only 6 Application CRs on hw255 (spine-gitea/harbor/keycloak/op… |
| 15 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Apps list renders one card per Application (all Platform/BOOTSTRAP-… a customer-launched app card would appear under its Org. | hw255 | ✅ | Apps list authed; 8 Application CRs backing ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): GET /api/v1/sovereigns/5762118f63abef96/applications -> 200, 6 Application CRs all Ready (spine-gitea/harbor/keycloak/openbao, guacamole, newapi). /apps renders one card per platform/BOOTSTRAP component — 46 distinct /app/* card links with INSTALLED badges incl. all CR-backed apps. Screenshot row15-apps-list.png. — Customer-launched-app-under-Org clause untestable: no customer Org applications exist on hw255 yet (voucher unredeemed). |
| 16 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | A customer app page (`/app/<name>`) → Settings/Topology → change topology → Save persists; the canonical tab strip includes a Topology tab. | hw224 | ◑ | hw224-fulltable-2026-07-04: /app/shared-pg strip includes Topology + 'Edit placement'; change-topology→Save is a write-mutation (not exercised). uat-hw224-fulltable-07‖ §G 91dc0591: canonical strip includes Topology tab + 'Edit placement', but customer app not surfaced as /app/<name> in owner console so change-topology→Save not exercised. uat-91dc-model-16 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): /app/uat-hw228-pg Topology tab shows 'Placement Pattern: singleton · mgmt · PRIMARY' + 'Edit placement' control present. ◑ — did NOT execute the topology-change persist (would disrupt a live customer Postgres; outside permitted mutations). ‖ 2026-07-08 TRIAGE: full ✅ is env-blocked on **#4851** — the console owner login is broken on hw228 (catalyst-api SMTP → mothership `mail.openova.io:587` connection-refused, stalwart-mail submission LB `<pending>`), so the authenticated console surface this row needs cannot be walked until the mothership SMTP submission path is restored. NOT a code gap (chart/handler are correct); a shared-mail-infra dependency. Stays ◑. |
| 17 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Dashboard treemap is a meaningful drill-down surface (Organization → vCluster → Application selectable & drillable, NOT a raw infra-pod utilisation treemap); default layer stack per founder verdict. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): /dashboard treemap default layer stack Progress→Kind (founder-locked design); Layer comboboxes offer organization/sovereign/region/cluster/vcluster/family/namespace/application; switched Layer1=organization Layer2=application → re-rendered org-grouped Application cells (Platform overhead → harbor 3%, keycloak 5%, powerdns, gitea, guacamol… |
| 18 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Scan every treemap cell: NO ephemeral Job-pod cell appears (no `cutover-*`, scan-vulnerabilityr… `*-snapshot-save-*`). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): GET /api/v1/dashboard/treemap -> 200, 99 named items, 0 matches for cutover-*/scan-vulnerability*/*-snapshot-save-* while 28 live Jobs exist (kubectl: 10 cutover-*, 3 scan-vulnerabilityreport-*, openbao-snapshot-save-29735070). Rendered /dashboard treemap DOM (all layer configs incl. Namespace) contains zero ephemeral job-pod cell texts. Screenshots row18-dashboard-treemap.png,… — Dashboard treemap cells are job/flow-model entries (Task/Step/Cron/Lifecycle groups) — no pod-name cells at any layer. |
| 19 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Count Application cards: one card per `Application` (NOT one per HelmRelease/pod); bootstrap apps carry a Platform/Bootstrap badge. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Authed /apps: one card per Application — 6 Application CRs (GET /api/v1/sovereigns/5762118f63abef96/applications -> 200, 6 items) render 6 cards (spine-gitea/harbor/keycloak/openbao, guacamole, newapi — guacamole is ONE card despite server+guacd+pg pods); bootstrap apps each carry the BOOTSTRAP badge (and 'Platform' title for platform components); card count tracks /api/v1/sove… — No per-HR/pod duplication observed |
| 20 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | With a customer Org present, the customer estate is visually distinct from platform pods on the treemap. | hw240 | ⚠️ | hw240 funnel-walker(handover+valkey-PIN): estate distinction present on Organizations SHOWBACK panel (acme/walk-stranger-two sections vs __platform__), NOT on dashboard treemap (which is job-centric/191 items, acme unattributed since app-install never ran per row90). |
| 21 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Organizations Showback panel: the **Application** column for a selected Org lists only that Org's real apps (no cluster-wide infra/Job pods). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Organizations -> Showback — per-app consumption: selected Org 'hw255.omani.works (parent — your own estate) · 0 units' shows 'No applications attributed yet.' — zero cluster-wide infra/Job pods attributed to the Org. Infra is confined to the explicit '__platform__ (platform overhead)' bucket (17506 units). Screenshot row21-organizations.png — Honest residue (not Org attribution): the __platform__ overhead bucket lists cutover-* Job pods (cutover-harbor-prewarm-1784097601, cutover-gitea-mirror-1784091803) and raw ReplicaSet-hash names (metr… |
| 22 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Showback shows a single visually-distinct **Platform overhead** roll-up line holding all control-plane/Job workloads. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Organizations page Showback renders single distinct roll-up group '__platform__ (platform overhead) · 17506.42 units · 17125m CPU · 40.54 GiB mem · 877 GiB storage' holding all control-plane workloads (cnpg-pair, seaweedfs, mimir, dragonfly, kyverno...). GET /api/v1/org/consumption -> 200 with synthetic org __platform__ isPlatform=true. Screenshot row22-showback-platform-overhe… — Handler org_consumption.go implements the synthetic isPlatform rollup, sorted last, scan-Jobs folded in. |
| 23 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | After a 2nd Org runs an app, the showback panel shows a SECOND Org row attributed only to its own app, distinct from Platform overhead. | — | ☐ | needs 2nd customer Org (funnel walk); only internal Org present | — |customer|org|real|vcluster|Active. Each attributed to own estate. 
| 24 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | `shared-pg` renders the canonical tab strip Overview · Contexts · Topology · Dependencies (Contexts tab present only for shareable blueprints). | hw255 | ✅ | shared-pg canonical tab strip render authed; app present ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): /app/shared-pg renders canonical tabs Overview · Contexts(3) · Topology · Dependencies(8) (plus Resources/Compliance/Logs/Settings/Members/Endpoints/Jobs); Contexts table: db/registry→harbor ready, db/gitea→gitea ready, db/keycloak→keycloak pending. Shareable-only rule proven by contrast: non-shareable /app/spine-harbor has NO Contexts ta… |
| 25 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | One consistent model across surfaces: `/organizations` shows the Orgs that `/apps`, the dashboard and showback all agree on. | hw255 | ✅ | cross-surface model consistency render authed ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): GET /api/v1/organizations -> 200 items exactly matching live Organization CRs (hw255-omani-works; later +acme255); /organizations page shows parent estate + __platform__ showback per-app table; /apps shows same-estate apps (spine-*, shared-pg/b/c, guacamole, newapi); dashboard treemap has Organization layer. No org appears on one surface missing from another. Screenshots rowR14… |
| 26 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Console bare URL → lands on the dashboard signed-in as the owner; no PIN/login/"Sign in with…" button. | hw255 | ✅ | UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 walker(SSO/model/catalog) curl+kubectl+KC-REST: console / → 200 SPA no login redirect; whoami mints owner via handover ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser fresh tab: GET https://console.hw255.omani.works/ -> 200, lands on /dashboard signed-in (sidebar 'EB emrah.baysal@openova.io hw255.omani.works'), no PIN/login/'Sign in with…' control, input[type=password] absent. Wire: GET /api/v1/whoami with session -> 200 {email:emrah.baysal@openova.io, tier:owner, roles:[catalyst-admin,catalyst-owner]}; unauthed control -> 401. Scree… — Session = wave-2 pre-minted owner jar (parent-domain catalyst_session cookie) |
| 27 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Avatar (top-right) menu reads "Signed in as the owner" with a Sign-out item. | hw255 | ✅ | UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 walker(SSO/model/catalog) curl+kubectl+KC-REST: #5001 SHIPPED: bundle index-Bhf9yePR.js has user-card "Signed in as" + "Sign out" item ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Clicked avatar (top-right 'E' button, testid profile-menu-avatar) -> menu renders text 'Signed in as' + 'emrah.baysal@openova.io' + menuitem 'Sign out'. Screenshot row27-179-avatar-menu-signedinas.png — Identity confirmed as the owner via wire: whoami tier=owner. #5001 surface verified live on hw255 (was UNVERIFIED since cb87fddc flip). |
| 28 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Users page renders the pre-seeded owner row the owner (tier=owner UserAccess CR), signed-in admin. | hw255 | ✅ | UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 walker(SSO/model/catalog) curl+kubectl+KC-REST: KC sovereign realm 1 user emrah.baysal@openova.io enabled; Users nav in bundle ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): /users renders 'User Access' table with pre-seeded owner row useraccess-owner-emrah-baysal-at-openova-io (emrah.baysal@openova.io, sovereignRef hw255, bindings 1) + org-admin row hw255-omani-works-emrah-baysal-at-openova-io. GET /api/v1/sovereign/users -> 200 both UserAccess CRs. Screenshot row28-users-owner-row.png. — Signed-in admin session (whoami tier=owner, roles catalyst-admin/catalyst-owner). |
| 29 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Re-open the bare console URL after the session TTL → lands signed-in again, no PIN re-prompt. | hw255 | ❌ | hw239: silent-SSO FE fix #4985 MERGED (04:58Z, prompt=none re-auth on session-TTL — router.tsx/oidc.ts/AuthCallbackPage.tsx on main). Deployed hw239 console is a PRE-#4985 image so still ❌ live; ◑ = fix-shipped, flips ✅ zero-touch on fresh prov / post-#4985 catalyst-ui roll. ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): with a LIVE KC SSO session in the same browser (proven: auth.hw255.omani.works/admin/sovereign/console/, grafana, harbor all silently authed), removed catalyst_session cookie (session-TTL simulation) then re-opened bare https://console.hw255.omani.works/ → landed /login?next=%2Fdashboard with PIN form 'Sign in — Enter your email to receiv… — #4985 silent-SSO front door (◑ fix-shipped on hw239) still not engaging on hw255 deployed console bundle index-DqSJ9fgF.js. Caveat: injected support-session had no SPA-side oidc state, but the front d… |
| 30 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Grafana bare URL → lands on Grafana Home, full UI, no login form; left nav shows Administration; user menu = the owner. | hw255 | ✅ | UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 browser-SSO-walker silent-SSO (owner session, NO PIN): grafana / → "Home - Dashboards - Grafana", /api/user login=emrah.baysal@openova.io isGrafanaAdmin=true, no login form ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser (owner session): https://grafana.hw255.omani.works/ -> title 'Home - Dashboards - Grafana', loginForm=false; /api/user -> login=emrah.baysal@openova.io isGrafanaAdmin=true; mega-menu nav shows Dashboards/Explore/Alerting/Connections/Administration. Screenshots row30-grafana-home.png, row30-grafana-nav-admin.png. — Re-proves the hw241 walk that was flipped UNVERIFIED on 2026-07-11. |
| 31 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Gitea bare URL → lands on the gitea dashboard titled "emrah.baysal — Dashboard", no login; profile menu exposes Site Administration; stays on :443. | hw255 | ✅ | UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 browser-SSO-walker silent-SSO (owner session, NO PIN): gitea → "emrah.baysal - Dashboard - Catalyst Gitea" :443 logged-in, no login form ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: https://gitea.hw255.omani.works/ -> lands title 'emrah.baysal - Dashboard - Catalyst Gitea', no login form (input[type=password] absent), stays on :443. Profile dropdown contains 'Site Administration' (menu items: Profile/Settings/Site Administration/Sign Out) and /admin -> 'Dashboard - Catalyst Gitea' (admin panel served). Screenshots row31-gitea-landing.png, row31-gi… — Silent SSO via parent-domain owner session; gitea 1.22.3 |
| 32 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Harbor bare URL → lands on `/harbor/projects`, no login form; user dropdown the owner with Administration menus. | hw255 | ✅ | UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 browser-SSO-walker silent-SSO (owner session, NO PIN): harbor /harbor/projects → top-bar user emrah.baysal@openova.io + Administration, no sign-in form ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Bare https://registry.hw255.omani.works/ silently SSO-landed on /harbor/projects: 0 visible password inputs, body has Projects + Administration menus, user shown = emrah.baysal@openova.io. Screenshot row32-harbor-landing.png — Silent SSO brokered from the console session (catalyst-pin IdP), no login form at any point. |
| 33 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | OpenBao bare UI → lands in an authenticated Vault session (Secrets engines / dashboard), NO token-entry form (an in-transit "Signing in…" shim is allowed). | hw255 | ✅ | UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 browser-SSO-walker silent-SSO (owner session, NO PIN): openbao /ui/vault/secrets → Secrets Engines (cubbyhole/secret kv), not /auth, no login form ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: https://bao.hw255.omani.works -> landed https://bao.hw255.omani.works/ui/vault/secrets showing 'Secrets Engines' (cubbyhole/, secret/ kv_9952ba81), OpenBao 2.3.1, NO token-entry form, no sign-in method picker. Screenshot row33-openbao-landing.png. — Silent SSO via owner session; same shape as prior hw241 evidence. |
| 34 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Keycloak admin console for the **sovereign** realm → lands inside the admin console (realm overview / Users / Clients), no master-realm login. | hw255 | ✅ | UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: UNVERIFIED (flipped 2026-07-11 by db45fe23) — was: UNVERIFIED (flipped 2026-07-11 by 907012da) — was: — ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): https://auth.hw255.omani.works/admin/sovereign/console/ → title 'Keycloak Administration Console', 'Current realm: Sovereign', signed in emrah.baysal@openova.io; left nav Manage: Clients/Client scopes/Realm roles/Users/Groups/Sessions/Events + Configure: Realm settings/Authentication/Identity providers/User federation; master-realm login … | hw241 walker(SSO/model/catalog) curl+kubectl+KC-REST: realm sovereign exists+enabled (admin-REST /admin/realms) |
| 35 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Guacamole bare URL → lands on the Guacamole connections list, signed-in; no Tomcat 404, no `/guacamole/` login page. | hw255 | ✅ | UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 browser-SSO-walker silent-SSO (owner session, NO PIN): guacamole OIDC via realms/sovereign; id_token email=emrah.baysal@openova.io groups=[sovereign-admins,sovereign-viewers]; Recent/All Connections, no login form ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: https://guacamole.hw255.omani.works/ -> lands /guacamole/#/ with OIDC id_token iss=https://auth.hw255.omani.works/realms/sovereign, email=emrah.baysal@openova.io, groups=[openova-users,sovereign-admins,sovereign-viewers]; page shows 'RECENT CONNECTIONS / ALL CONNECTIONS' with user menu emrah.baysal@openova.io; tomcat404=false, loginForm=false. Screenshot row35-guacamol… |
| 36 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | PowerDNS-Admin bare URL → lands on the dashboard signed-in; no redirect loop, no OAuth error, no `Log In` page. | hw255 | ✅ | UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 browser-SSO-walker silent-SSO (owner session, NO PIN): powerdns-admin /dashboard/ → "Dashboard - PowerDNS-Admin" Zone Mgmt + Administration, no login form ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: https://pdns-admin.hw255.omani.works/ -> settles at /dashboard/ title 'Dashboard - PowerDNS-Admin', signed in as emrah.baysal@openova.io, Zone Management + Administration menus present, no redirect loop, no OAuth error, no Log In page (password input absent). Screenshot row36-pdns-admin-landing.png |
| 37 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) [#3858](https://github.com/openova-io/openova/issues/3858) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi bare URL (1st hit) → lands on `/console` signed-in as admin (role 100); no "Unknown OAuth provider", no login page. | hw255 | ✅ | UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 browser-SSO-walker silent-SSO (owner session, NO PIN): newapi /console → "Good evening sovereign_2" (OIDC-linked), full Console nav, no login form ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Bare https://newapi.hw255.omani.works/ (1st hit in this context) landed on /console/token signed-in as 'sovereign_2'; sidebar shows ADMIN sections (Channel Management, User Management, Model Deployment, System); 'Unknown OAuth provider' absent; 0 login inputs. Screenshot row37-newapi-landing.png — Landing path is /console/token (a /console sub-route); full admin nav = role-100 equivalent rendered. |
| 38 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) [#3858](https://github.com/openova-io/openova/issues/3858) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi bare URL (2nd hit, re-entry) → lands on `/console` again signed-in; NOT an "already bound" / re-link error / `/setup` wizard. | hw255 | ✅ | UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 browser-SSO-walker silent-SSO (owner session, NO PIN): newapi re-entry → "Signing you in…" silent re-SSO → /console/token same account sovereign_2, no re-link/duplicate error ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: two consecutive bare hits to https://newapi.hw255.omani.works — hit1 -> /console/token signed in (account sovereign_2, Token Management UI); hit2 (re-entry) -> /console/token same account; no 'already bound'/re-link/duplicate error, no /setup wizard. Screenshot row38-newapi-reentry.png. |
| 39 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Hubble bare URL → lands on the Hubble UI, authenticated (not an anonymous/unauth view, no login page). | hw255 | ✅ | UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 browser-SSO-walker silent-SSO (owner session, NO PIN): hubble UI namespace selector populated ~55 live namespaces (relay connected behind SSO gate), not anonymous-error ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): https://hubble.hw255.omani.works/ → title 'Hubble UI', no login page (0 login inputs), authed view behind oauth2-proxy; namespace selector POPULATED with 46 live namespaces (alloy, catalyst, catalyst-system, cert-manager, cnpg, crossplane-system, falco…) → relay connected, not anonymous/error. Screenshots row39-hubble-ui.png, row39-hubble… |
| 40 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Marketplace bare URL → renders the anonymous storefront (public, by design); confirm no spurious login UI is forced. | hw255 | ☐ | UNVERIFIED (flipped 2026-07-15 by 5a758ab7) — was: UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 walker(SSO/model/catalog) curl+kubectl+KC-REST: marketplace.hw241 → 200 anonymous storefront no forced login ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): hw255 2026-07-15: curl -sk https://marketplace.hw255.omani.works/ -> HTTP 200, <title>Build Your Organization — OpenOva</title>, anonymous storefront HTML served with no auth redirect and no forced login UI (only a nav 'Sign in' link); TLS *.hw255.omani.works (LE) valid — Same curl method as the prior hw241 stamp for this row. |
| 41 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Keycloak sovereign realm → Users lists the single owner principal the owner (enabled). | hw255 | ☐ | UNVERIFIED (flipped 2026-07-15 by 5a758ab7) — was: UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 walker(SSO/model/catalog) curl+kubectl+KC-REST: sovereign realm Users=1 owner enabled ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): hw255 2026-07-15: KC admin REST GET /admin/realms/sovereign/users -> exactly 1 user: emrah.baysal@openova.io enabled=true (realm sovereign enabled=true); creds via kubectl get secret keycloak/catalyst-kc-master-admin-credentials (read-only) — Method matches prior hw241 stamp (curl+kubectl+KC-REST). |
| 42 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Owner user → Groups tab shows membership in `/sovereign-admins` (alongside `/openova-users`) — the source of admin authority. | hw255 | ☐ | UNVERIFIED (flipped 2026-07-15 by 5a758ab7) — was: UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 walker(SSO/model/catalog) curl+kubectl+KC-REST: owner groups [/sovereign-admins,/sovereign-viewers] ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): hw255 2026-07-15: KC admin REST GET /users/74b72aa0/groups -> [/openova-users, /sovereign-admins, /sovereign-viewers] — owner is a member of /sovereign-admins alongside /openova-users |
| 43 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Owner user → Role mapping tab: effective realm roles include `catalyst-admin` (not only default-roles/uma/o… | hw255 | ☐ | UNVERIFIED (flipped 2026-07-15 by 5a758ab7) — was: UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 walker(SSO/model/catalog) curl+kubectl+KC-REST: owner effective composite roles incl catalyst-admin (+developer/operator/viewer) ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): hw255 2026-07-15: KC admin REST GET /users/74b72aa0/role-mappings/realm/composite -> [catalyst-admin, catalyst-developer, catalyst-operator, catalyst-viewer, offline_access, uma_authorization, default-roles-sovereign] — effective roles include catalyst-admin, not only defaults |
| 44 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Groups → `/sovereign-admins` → Role mapping: group confers `catalyst-admin` (console) and realm-management `realm-admin` (KC console) — one source, both grants. | hw255 | ☐ | UNVERIFIED (flipped 2026-07-15 by 5a758ab7) — was: UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 walker(SSO/model/catalog) curl+kubectl+KC-REST: /sovereign-admins realmMappings [catalyst-admin] AND client realm-management→[realm-admin] one source ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): hw255 2026-07-15: KC admin REST GET /groups/245cc91e(/sovereign-admins)/role-mappings -> realmMappings=[catalyst-admin] AND clientMappings realm-management=[realm-admin] — one group source confers both grants |
| 45 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Console Users panel: owner row + the ability to view/manage users renders — proving console admin nav is driven by the realm principal, not a self-signed constant. | hw255 | ✅ | UNVERIFIED (flipped 2026-07-11 by cb87fddc) — was: hw241 walker(SSO/model/catalog) curl+kubectl+KC-REST: Users nav in bundle, owner principal+realm roles drive it (whoami roles from realm not constant) ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: /users renders 'User Access' panel with owner rows — useraccess-owner-emrah-baysal-at-openova-io + hw255-omani-works (admin) + acme255 (admin) all emrah.baysal@openova.io — with '+ New' and Delete controls and tier filters (viewer..owner). Wire: /api/v1/whoami realm_access.roles=[catalyst-admin,catalyst-owner] (realm-derived, not constant); unauthed whoami -> 401. Scre… |
| 46 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `bp-postgres` catalog detail renders; click **New instance** → the create dialog opens with a topology `<select>`. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Authed /catalog/bp-postgres: clicked '+ New instance' -> dialog 'New instance of postgres' opens with a 'Topology mode' <select> (4 options: singleton / active-active(not supported) / active-hot-standby cnpg-pair sync RTO 10s RPO 0s / active-passive(not supported)) plus Organization select + Region checkboxes (me-east-215-a/b) + vCluster select. Dismissed via Escape — NO create… — Dialog-open only; no write performed |
| 47 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) [#3856](https://github.com/openova-io/openova/issues/3856) | Topology `<select>` options read exactly the ONE canonical vocabulary: `singleton`, `active-passive`, `active-hot-standby`, `active-active` (no `single-region`/`act…). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Catalog topology option surface (cif-topologies editor on /catalog/bp-wordpress) offers EXACTLY: singleton, active-active, active-hot-standby, active-passive (testids catalog-edit-topo-singleton/-active-active/-active-hot-standby/-active-passive); no 'single-region' or truncated variants anywhere in the topology UI; live Application CRs read PLACEMENT singleton/active-passive/a… — The literal <select> was superseded: the per-app placement editor (#3969) uses Region/Cluster/vCluster/Role target selects with a DERIVED pattern ('Derived pattern: singleton'), and the catalog topolo… |
| 48 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `active-passive` is a selectable option in the create `<select>` (not folded away). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Create wizard (catalog bp-agenity '+ New instance') Topology mode <select> options: singleton / active-active / active-hot-standby / 'active-passive — primary serves; standby in the second region promotes on failover' — active-passive present as a selectable option, not folded away. Screenshots row218-agenity-install-wizard.png, row48-openbao-topology-select.png. — For bp-agenity the option is annotated '(not supported by this blueprint)' — per-blueprint honest gating; the option itself is always in the select. |
| 49 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `singleton` is a separate selectable option (single-region single-instance), distinct from the multi-region modes. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): /install/bp-wordpress-tenant 'Placement mode' select options exactly [singleton, active-active, active-hot-standby, active-passive] — singleton a separate selectable single-region option distinct from the multi-region modes (plus DB topology selector [singleton, active-hot-standby]). Live corroboration: GET /api/v1/sovereign/apps → topolo… |
| 50 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Pick `active-hot-standby`, name the instance, Provision → the create succeeds (toast/redirect to the new app card), NOT a red `topology not in supported [...]` error. | — | ☐ | — | — |
| 51 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab renders a per-region placement view listing region-a (active) and region-b (standby) as ONE placement, not two separate instances. | hw255 | ❌ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: /app/shared-pg -> Topology tab renders 'Placement / Pattern: singleton / No placement targets reported yet.' — no per-region region-a(active)+region-b(standby) placement view. Wire: GET /api/v1/sovereigns/5762118f63abef96/applications/shared-pg/placement -> 200 {"targets":[],"derivedFromRuntime":true}. Screenshot row51-sharedpg-topology.png. — The DR surface itself works — /app/spine-keycloak Topology renders active-hot-standby with primary/standby regions — it is not wired for shared-pg. Backing pair data exists: Continuum cnpg-pair-bp-cnp… |
| 52 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab: the standby (region-b) copy is shown scaled-down / passive while region-a shows the active replica count (not identical hot counts). | hw255 | ❌ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Authed /app/shared-pg -> Topology tab renders 'Pattern: singleton', 'No placement targets reported yet.', 'Status: n/a — bootstrap component (HelmRelease, no Application CR). Live rollout status is tracked via Flux.' — NO standby(region-b) scaled-down/passive display and NO region-a active replica count, despite live 2-region cnpg-pair (kubectl: continuum cnpg/cnpg-pair-bp-cnpg… — No shared-pg Continuum CR on hw255 (kubectl get continuums -A: only dr-spine-* x4 + cnpg-pair) — #4986/#4987 shape absent on this prov; #3986 placement-from-runtime gap |
| 53 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Open a per-app Topology tab and read its placement view (singleton apps note no per-region/standby surface). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Per-app Topology tabs open and read: bp-postgres + bp-grafana (singleton) render 'Pattern: singleton' with NO per-region/standby cards ('No placement targets reported yet'); multi-region bp-cilium contrast-renders 2 region cards — singleton apps honestly omit the per-region/standby surface. Screenshots row71-sharedpg-topology.png, row187-grafana-topology.png, row58-cilium-topol… — Status sub-panel for Application-CR-less bootstrap components reads honest 'n/a — bootstrap component (HelmRelease, no Application CR)' on cilium; on bp-postgres/grafana it sat at 'Loading status…' (>… |
| 54 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) [#3856](https://github.com/openova-io/openova/issues/3856) | `shared-pg` Topology tab declared-topology strip renders the canonical mode in ONE vocabulary — header dialect and picker dialect MATCH (no `singleton` header over an `active-hotstandby` chip). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): /app/shared-pg Topology tab: declared strip 'Placement Pattern: singleton' with no contradicting chip; Edit placement picker (regions me-east-215-a/b, vCluster host/mgmt/dmz/rtz, Primary/Standby radios) uses the same vocabulary — no 'singleton' header over an 'active-hotstandby' chip. Screenshots row54-sharedpg-topology-tab.png, row54-sharedpg-placement-picker.png. — Picker opened read-only and closed with Escape; no save. |
| 55 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab shows the effective (live) topology next to the declared one — declared vs effective together, not a build-time constant. | hw255 | ❌ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): /app/shared-pg Topology tab renders declared 'Pattern: singleton' + 'No placement targets reported yet.' + Status 'n/a — bootstrap component (HelmRelease, no Application CR). Live rollout status is tracked via Flux.' — NO effective/live topology rendered next to the declared one. Contrast: /app/bp-grafana Topology DOES render live targets… — Matches pending GOV item 'Topology placement-from-runtime live (#3986)' — declared-vs-effective not yet delivered for the shared-pg archetype. |
| 56 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab per-region placement + replication block: region-a primary, region-b replica, and a live replication-lag in seconds (not a hardcoded `—`). | hw255 | ❌ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: shared-pg Topology tab has NO replication block at all (no primary/replica roles, no replication-lag) — only 'Pattern: singleton / No placement targets reported yet'. Contrast: spine-keycloak Topology renders 'PRIMARY REGION me-east-215-a / STANDBY REGION me-east-215-b / Replication lag 0.0 s'. Screenshots row51-sharedpg-topology.png, row69-spine-keycloak-topology.png. — Live replication data exists (dr/replication-status: cnpg-pair continuum lagSeconds=0, replica me-east-215-b) but is not surfaced on the shared-pg tab. |
| 57 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab Switchover button is present and armed because a live 2-region cnpg-pair backs the app. | hw255 | ❌ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Authed /app/shared-pg Topology tab: only button present is 'Edit placement' (button scan matched zero of switch/fail/promote) — NO Switchover button, armed or otherwise, despite the live 2-region cnpg-pair backing (Continuum cnpg-pair Healthy, standby present, lag 0s). Screenshot row52-57-sharedpg-topology-no-targets.png — Switchover surface requires the missing shared-pg Continuum CR (#4986/#4987) |
| 58 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | A **singleton** app (cilium) Topology tab: the DR section / Switchover button does NOT render (honestly hidden, not armed against a phantom region). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): bp-cilium Topology tab: Switchover buttons = 0, no 'switchover'/'DR ' text anywhere on the page (regex over body), no DR section rendered — nothing armed against a phantom region. Screenshot row58-cilium-topology.png — Row premise drift: on this 2-region env cilium derives 'Pattern: active-active' (2 PRIMARY cards, runtime in both regions), not 'singleton'; the tested assertion (DR/Switchover honestly hidden) holds … |
| 59 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Catalog New instance → pick `singleton` → Provision → that app's Topology tab shows single-region placement (no region-b standby, no Switchover). | — | ☐ | — | — |
| 60 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Catalog New instance → pick `active-hot-standby` → Provision → that app's Topology tab shows a 2-region pair (region-a primary + region-b replica + armed Switchover). | — | ☐ | — | — |
| 61 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Apps grid shows newly-provisioned postgres instances as their own cards, each carrying a topology badge matching the mode picked at create time. | — | ☐ | — | — |
| 62 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | On an app WITH a live pair, the Topology DR section shows the live Continuum status (Ready / lease holder / standby) from the live API, not a static badge. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): /app/spine-gitea Topology (app WITH live pair — kubectl continuums.dr.openova.io dr-spine-gitea phase=Healthy primary=me-east-215-a lease=me-east-215-a): DR section renders '● LIVE / PRIMARY REGION me-east-215-a serves writes / STANDBY REGION me-east-215-b hot replica (follows WAL) / Replication lag 0.0 s' + switchover preflight CTA — live Continuum values matching the CR, not … — GET /api/v1/sovereigns/{dep}/continuum/dr-spine-gitea/replication-status -> 200 primaryRegion me-east-215-a, replica region me-east-215-b lag 0. |
| 63 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | grafana (declares hot-standby, no live DR backing) Topology DR section reads the honest "no live DR backing… Switchover unavailable" state with a disabled (not armed) Switchover button. | hw255 | ⛔ | hw241: grafana Topology active-active (both PRIMARY) on this env placement → asserted hot-standby/disabled-switchover state N/A here, not a defect ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): /app/bp-grafana Topology shows 'Pattern: active-active' with BOTH regions '● PRIMARY / serves writes' (hw-me-east-215-a + hw-me-east-215-b) and zero Switchover buttons — grafana on this env placement is active-active, so the asserted hot-standby/no-live-DR-backing honest-state with disabled Switchover is not observable (same reclassificat… |
| 64 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab replication-lag field shows a live numeric seconds value (or explicit "no replica"), never a hardcoded `—`. | hw255 | ❌ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: shared-pg Topology tab renders no replication-lag field whatsoever (neither live numeric nor explicit 'no replica') — the whole replication block is absent; placement API returns empty targets. Screenshot row51-sharedpg-topology.png. — Same root as rows 51/56: shared-pg card not wired to the DR/replication surface that works on spine-* apps. |
| 65 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Cloud/regions view shows the true region count — a healthy 2-region prov reads `Cluster 2/2` with no phantom region-B bubble. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Authed /cloud?view=graph: chips read 'Region 2/2', 'Cluster 2/2', NodePool 4/4, WorkerNode 12/12; both region bubbles (me-east-215-a and me-east-215-b) are populated with real cluster+pools+6 nodes each — no phantom/empty region-B bubble. List view kind=clusters: 2 rows, both HEALTHY, 6 nodes each. Wire: /api/v1/sovereign/cloud nodes=12 split 6/6 across clusters 5762118f63abef9… |
| 66 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Cloud→Clusters renders 2/2 HEALTHY clusters, one per region (me-east-215-a + me-east-215-b), no phantom region. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Cloud graph (Cloud lens): chips read 'Region 2/2', 'Cluster 2/2', 'WorkerNode 12/12', 'LoadBalancer 2/2'; graph shows exactly me-east-215-a and me-east-215-b, one cluster each, no phantom third region. Screenshot row66-cloud-landing.png — Header badge honestly reads DEGRADED: deployment record has region me-east-215-b-1 hrReady 0/0 + secondaryDegraded=true (Flux/HR layer), while the cluster/node layer renders 2/2 with all 12 workers Re… |
| 67 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | grafana status/overview reports Healthy/Running in both regions — no "cannot resolve write host" crashloop in the app health panel. | hw255 | ⛔ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): /app/bp-grafana Overview: STATUS Ready, PLACEMENT singleton; kubectl -n grafana: grafana-55674dbd6d 3/3 Running 0 restarts (no crashloop, no 'cannot resolve write host'). Screenshot row67-grafana-overview.png. — 'In both regions' precondition absent: grafana is placed singleton on hw255 (region-a only); the multi-region grafana health case cannot be surfaced. Health panel is green for the placement it has. |
| 68 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | powerdns-admin status reports Healthy/Running — the CNPG-minted DB host resolved (no "could not translate host"). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 kubectl+browser): kubectl -n powerdns-admin: powerdns-admin-bp-powerdns-admin-95d5b5bdc-lxkb8 1/1 Running 0 restarts (3h56m) + CNPG-minted pda-pg-1 1/1 Running; pod logs grep 'could not translate host' → 0 hits in both pods. UI proof: https://pdns-admin.hw255.omani.works/dashboard/ → 'Dashboard - PowerDNS-Admin' signed in emrah.baysal@openova.io with Zone… |
| 69 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | keycloak status reports Healthy/Running in both regions — JGroups DB-host resolves, no UnknownHostException in the health panel. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): keycloak-0 Running+ready in BOTH clusters (cache API: 5762118f63abef96 and 5762118f63abef96-me-east-215-b-1; region-b restarts=0); bp-keycloak HR Ready=True both regions (chart 1.5.5 'Helm install succeeded'); kubectl logs keycloak-0 -n keycloak: 'JGroups JDBC_PING discovery enabled', grep -ci UnknownHostException = 0; console health panel (spine-keycloak Topology): DR ● LIVE, … — Transient JDBC I/O errors 07:09-07:10Z coincide with the cutover step-08 workload roll; nothing after 07:10:16Z (>90min clean at walk time). |
| 70 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | guacamole status reports Healthy/Running in both regions — no missing-recordings-PVC error surfaced. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Wire: GET /api/v1/sovereigns/5762118f63abef96/applications/guacamole/status -> 200 phase=Ready ('Helm upgrade succeeded ... bp-guacamole@0.2.29'). Authed /app/guacamole: STATUS Ready; Topology tab renders BOTH regions (hw-me-east-215-a-rtz-prod PRIMARY serves writes + hw-me-east-215-b-rtz-prod); zero 'recordings'/'PVC' error strings on the page (regex scan false). kubectl -n gu… — Application CR placement says singleton/platform-bootstrap-owned-host while Topology tab shows active-active both regions — cosmetic model mismatch, no error surfaced |
| 71 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Region-kill baseline (before): `shared-pg` Topology tab shows live Continuum Ready, lease held by region-a, region-b standby present, a live replication-lag number. | hw255 | ❌ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): shared-pg (/app/bp-postgres) Topology tab renders only 'Pattern: singleton / No placement targets reported yet / Status: Loading status…' — NO Continuum Ready line, NO lease-held-by-region-a, NO region-b standby, NO replication-lag number. Backing: kubectl get continuums -A shows 5 CRs (dr-spine-gitea/harbor/keycloak/openbao + cnpg-pair, all Healthy, lease me-east-215-a) but NO… — #4986 shape recurs on hw255: shared-pg has no Continuum CR so the region-kill baseline surface cannot render; also region-b secondary never materialized HRs (secondaryDegraded=true). The Continuum sur… | — |
| 72 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Operator console BSS → Vouchers page renders the voucher issuance form (code, credit OMR, plan tier, description); no login wall. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): /billing/vouchers renders (no login wall): 'Issue prepaid codes...' + existing voucher VCH-T4TC89KEVRAN 100 OMR Active; '+ Issue voucher' opens the issuance form with fields Code (ph LAUNCH2026), Credit (OMR), Description (optional), Max redemptions (optional), Recipient email (optional). GET /api/v1/org/billing/vouchers/list -> 200. Screenshots row72-vouchers-form.png, row72-v… — Form dismissed with Escape — no submit. Caveat: no plan-tier input in the current form (PLAN exists as a table column); row's field list partially predates the credit-OMR issuance model. |
| 73 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | In the voucher form, type a weak code `1234` → Issue → the form rejects it inline ("voucher code must be at least 12 characters"). | — | ☐ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser, read-only): /billing/vouchers → '+ Issue voucher' modal opened (client-side), typed code '1234' — NO inline error renders pre-submit ([data-testid=bss-issue-voucher-error] count=0 after fill+blur). The asserted inline rejection can only fire on the Issue click. Deferred write: click 'Issue voucher' → POST /api/v1/org/billing/vouchers/issue… — Code evidence says this will FAIL on the mutation walk: VouchersPage.tsx onSubmit validates only code-required + credit-positive; the string 'at least 12 characters' exists nowhere in FE or org_billin… |
| 74 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Leave the code field empty → Issue → a new voucher row appears with a server-auto-generated high-entropy code, credit, plan tier, `unredeemed 0/1`. | hw240 | ⚠️ | hw240 funnel-walker(handover+valkey-PIN): server POST /vouchers/issue with empty code auto-generated high-entropy VCH-974ZCS5MYDJA (row appears); BUT console form blocks empty client-side ("Voucher code is required") — server capability exists, UI lacks the empty-code/auto-gen affordance. UI gap, server correct. |
| 75 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Stranger opens the redeem page with `?code=<CODE>` → sees "Voucher valid · 5000 OMR" with THIS Sovereign's brand chrome (no openova.io / mothership logo). | — | ☐ | — |
| 76 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Open the redeem page with a junk code → a generic "voucher not valid" message (no tombstone / no detail leak). | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): Live browser 2026-07-15T05:33Z: https://marketplace.hw255.omani.works/redeem/?code=JUNKCODE12345 renders h1 'Voucher not valid' + 'This code does not exist or has been retired.' + 'Browse plans without a voucher' link — generic message, no tombstone/status/detail leak. — hw255-2026-07-15 W3 read-only browser walk; no mutation (GET-only). |
| 77 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Redeem page source (DevTools): the HTML shows THIS Sovereign's brand and no `console.openova.io` / `omantel.openova.io` / bare `openova.io` host literal. | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): curl scan of the served redeem HTML + both referenced assets (/_astro/addons.BmfayyDS.css, /_astro/redeem.astro_astro_type_script_index_0_lang.CMkw6COo.js): 0 hits for console.openova.io / omantel.openova.io / openova.io host literals. Served inline script confirms the #3691 fix live: per-partner brand table is runtime DATA (window.__ORG_BRANDS__), 'The shipped bundle now carries no partner literal'. — hw255-2026-07-15 W3. Brand chrome renders as OpenOva product name (not a host literal) — acceptable per row wording. |
| 78 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Click "Sign up to redeem" → the browser lands on the plan picker grid (`/plans`). | — | ☐ | — |
| 79 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Plans grid shows the tiers (S/M/L/XL/Flexi) with price/CPU/memory → pick plan M → advances to the app catalog (`/apps`). | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): Live browser: /plans renders all 5 tiers S (5.000 OMR, 2vCPU/4GB/25GB), M (9.000, 4vCPU/8GB/50GB, 'Popular'), L (16.000, 8vCPU/16GB/100GB), XL (30.000, 16vCPU/32GB/200GB), Flexi (2 OMR/CU/mo) with price/CPU/memory matrix; plan M selected; 'Continue to Stack' advanced URL to /apps/ ('Choose Your Apps'). — hw255-2026-07-15 W3 read-only browser walk (client-side nav only). |
| 80 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | App catalog grid (served from THIS Sovereign's catalog) → pick WordPress → advances to add-ons (`/addons`). | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): Live browser: /apps renders the 16-app catalog grid served from marketplace.hw255.omani.works (BookStack, Cal.com, ..., WordPress) with category filters + industry templates; clicked WordPress 'Add to stack' (0->1 selected) then Continue => URL /addons/ ('Add-ons & Domains'). — hw255-2026-07-15 W3. Grid + selection + advance proven; catalog served from THIS Sovereign's marketplace host. |
| 81 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Add-ons step shows optional add-ons → leave defaults → Continue → advances to the BCP topology step (`/bcp`). | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): Live browser: /addons renders 'Setup & extras' — domain picker (subdomain + pool-TLD combobox .omani.rest/.omani.works/.omani.trade/.omani.homes, FREE) + 5 optional extras (API Access, Daily Backup, Dedicated IP, Log Management, Priority Support); left defaults; Continue => URL /bcp/ ('Business Continuity'). — hw255-2026-07-15 W3. |
| 82 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | BCP topology step shows BOTH Single-region and Active-hot-standby radios (the Pillar-2 BCP choice at signup) → select Active-hot-standby → Continue → review. | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): Live browser: /bcp renders BOTH radios — 'Single-region FREE' (default-checked) and 'Active-hot-standby +OMR 5.000/mo — Primary + synchronous replica across two distinct regions over Cilium ClusterMesh. RTO 30s, RPO 5s'; selected Active-hot-standby; 'Review Order' => URL /review/. — hw255-2026-07-15 W3. The Pillar-2 BCP choice renders at signup as asserted. |
| 83 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Review summary shows the chosen plan (M), app (WordPress), topology (Active-hot-standby), the Org slug, and the pool-TLD → Proceed to checkout. | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): Live browser /review: WordPress card, plan radio 'M 9.000 OMR/mo' [checked], Business continuity 'Active-hot-standby' (Primary hw-me-east-215-a-rtz-prod / Replica hw-me-east-215-b-rtz-prod), Org slug + pool-TLD rendered as 'w3-uat-preview.omani.rest' (strong f2e150 in snapshot page-2026-07-15T05-37-48-205Z.yml), Monthly total OMR 14.000, 'Proceed to Checkout' link present. — hw255-2026-07-15 W3. Slug/TLD only render when wizard state persists via Continue clicks (first attempt with direct URL navigation dropped the typed slug — walker artifact, not a product gap). |
| 84 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | On checkout, enter the stranger's email → Send code → type the emailed sign-in code (no password) → the page shows the stranger signed in + Org confirmed. | — | ☐ | — |
| 85 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Checkout summary: the voucher credit is applied — "Credit covers this order — 0 OMR due" (no Stripe card form) → click Launch / Place order. | — | ☐ | — |
| 86 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Provisioning progress timeline advances to Done (Creating Org → Committing manifests → Provisioning vCluster → Deploying WordPress → TLS → Health), no hang/red step. | hw241 | ⚠️ | hw241 funnel-walker(zero-touch): #4992 PROVEN ZERO-TOUCH: Launch → Organization walk241zt + ns auto-created ~14s; provisioning-tenant Role+RoleBinding AUTO-created, "can-i create secrets -n walk241zt --as=…:provisioning" = YES (no hand-apply). BUT provisioning timeline does NOT reach Done — per-Org vcluster HR stalls (#5003: cold Harbor pull 3m11s > helm timeout, no install-remediation retries → Stalled). App-deploy blocked. |
| 87 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | After Launch, the marketplace redirects to the per-Org console — URL becomes the per-Org console and loads with publicly-trusted TLS (no cert error / connection-refused). | hw240 | ⚠️ | hw240 funnel-walker(handover+valkey-PIN): auto-redirect did NOT fire (timed out to "still provisioning" + Keep-waiting/Open-console-anyway); clicking "Open console anyway" reached per-Org console.acme.omani.homes/apps. URL becomes per-Org FQDN but only via manual button. |
| 88 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Per-Org console landing: the stranger is signed in zero-click as the Org owner (their email in the avatar, their Org in the header) — no login/PIN form. | — | ☐ | — |
| 89 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Per-Org console → Applications view shows the purchased WordPress app card Running/Healthy → click Open. | hw239 | ⚠️ | hw239: #4966 provisioning-SA RBAC fix LIVE (org-services/provisioning = services-provisioning:280aa83, the #4966 merge); product-fault resolved — now customer-Org-gated (only internal Org; final green needs a funnel WordPress walk) |
| 90 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Terminal acceptance: the purchased WordPress app SERVES at its own FQDN the app FQDN — the live rendered WordPress site (not 404/502/cert error). | hw241 | ❌ | hw241 funnel-walker(zero-touch): TERMINAL: wordpress.walk241zt.omani.homes → 404 — app NEVER deploys. NOT #4992/#4995 (those work zero-touch). Root cause #5003: per-Org vcluster HR terminally stalls (cold Harbor init-image pull > helm timeout + no remediation retries) → kubeconfig secret never minted → apps Kustomization "unable to read KubeConfig" → WordPress HR never renders. No workaround applied (honest FAIL). |
| 91 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | While signed in, re-open the marketplace root → the returning-user redirect sends the customer to their own Org console, never to `console.openova.io` / the mothership. | — | ☐ | — |
| 92 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | As the signed-in customer, rapidly re-submit checkout/redeem >5× in a few seconds → after ~5 attempts a rate-limit notice appears; a single legitimate redeem is unaffected. | hw240 | ⚠️ | — | hw240 funnel-walker(handover+valkey-PIN): rate-limit NOT observed: 27 rapid reqs (12 GET /redeem + 15 POST magic-link) all HTTP 200, zero 429. rateLimit:GA<ip> Valkey key exists (mechanism present) but bastion EIP 212.72.24.20 source IP likely trusted. |
| 93 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Generality: mint a 2nd voucher and re-walk B.1→B.4 with a different slug (`walk-stranger-two`) and a different pool-TLD (`omani.rest`) → a 2nd Organization provisions and lands signed-in. | hw240 | ⚠️ | RE-PROV-GATED: needs converged customer Org (funnel) on cutover-complete env. Trigger: deploybot umbrella roll (bp-catalyst-platform + cutover 0.1.107, all fixes) -> wipe hw232 -> clean re-prov -> walk | hw240 funnel-walker(handover+valkey-PIN): 2nd voucher UATWALKHW240VOUCHER2 minted, full re-walk with slug walk-stranger-two + walk-stranger-two@omani.rest (code 400720), credit-covered, launched. BUT chosen .omani.rest TLD NOT honored — backend recorded tenantPublic.parentDomain=omani.homes (default fallback). |
| 94 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The 2nd Org's console lands signed-in on a different TLD (the 2nd-Org console) — identical zero-click contract, no special-casing. | hw240 | ❌ | RE-PROV-GATED: needs converged customer Org (funnel) on cutover-complete env. Trigger: deploybot umbrella roll (bp-catalyst-platform + cutover 0.1.107, all fixes) -> wipe hw232 -> clean re-prov -> walk | hw240 funnel-P0: 2nd Org walk-stranger-two got the provisioning RBAC (fix #4992) but not yet the ns/route re-verify → not serving; also /auth/org-handover 503 + wrong-TLD (.omani.homes not .omani.rest) unfixed. Partial fix applied, full 2nd-Org serve pending #4993 + TLD-honor fix. |
| 95 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The 2nd Org's purchased app serves at its own different-TLD FQDN (the 2nd-Org app) — two Orgs, two TLDs, two running apps, identical mechanism. | hw240 | ⚠️ | RE-PROV-GATED: needs converged customer Org (funnel) on cutover-complete env. Trigger: deploybot umbrella roll (bp-catalyst-platform + cutover 0.1.107, all fixes) -> wipe hw232 -> clean re-prov -> walk | hw240 #4993 FIX (PR #4995 merged 4f74c8c05): vcluster syncer had NO httproute sync controller → in-vcluster app route never reached host Cilium Gateway → 404. FIX: org-controller emits host-native HTTPRoute per vcluster app into vcluster/host-apps/ (mirrors per-Org console route, §854 NO NodePort). LIVE-VERIFIED walk-stranger-two WordPress serves HTTP 302 (→/wp-admin/install.php, Cilium Gateway) at wordpress.walk-stranger-two.omani.homes — same healthy code as acme. NOTE: FQDN is <app>.<slug>.<parent>; 302=healthy WP-install; zero-touch pending org-controller image roll. |
| 96 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Handover URL → lands on `/dashboard` already signed-in as the owner (avatar E), no login form. | — | ☐ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Handover URL walk requires redeeming a single-use magic-JTI login link; the prov's original handover link was already consumed during provisioning (04:5xZ), so re-exercising needs minting a new handover export (POST handover-export) + consuming the JTI on GET — a redeem-class write. Signed-in owner /dashboard itself verified rendering (avatar E, emrah.baysal@openova.io) via the… — Exact writes deferred: handover-export mint + magic-JTI consume. |
| 97 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Dashboard renders the cluster treemap and the LAYER 1 / LAYER 2 grouping comboboxes are visible. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Authed /dashboard: treemap renders (img 'Resource utilisation treemap' with Done/Install/Task/Step/Reconcile/Cron cell groups, 200 items) and toolbar 'Treemap controls' exposes LAYER 1 + LAYER 2 comboboxes (options Organization/Sovereign/Region/Cluster/vCluster/Family/Namespace/Application/Progress/Kind) plus Size/Color combos. Screenshot row97-dashboard-treemap-layers.png |
| 98 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Click LAYER 1 combobox → select `vCluster`; the treemap regroups into one labelled block per vCluster (`host` / `mgmt` / `rtz` / `dmz`), mgmt block visible + clickable. | hw236 | ⛔ | RE-BASELINED ☐→⛔ (superseded, NOT a product fault): #4325 de-vcluster removed the mgmt/rtz/dmz vClusters (plane-isolation moved to per-component host-ns CNP), so a `vCluster`-grouped treemap no longer has `mgmt`/`rtz`/`dmz` blocks — the hw228 walk confirmed `group_by=vcluster` returns a single `host` block with all 7 named apps as children. This row asserts the DEAD mgmt-vcluster grouping model; consistent with rows 99-107 (already ⛔ for the same reason). Refs #4325 #3642. |
| 99 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **grafana** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; mgmt/host placement re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 100 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **harbor** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 101 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **keycloak** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 102 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **gitea** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 103 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **openbao** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 104 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) [#3831](https://github.com/openova-io/openova/issues/3831) | On the LAYER1=vCluster treemap, the **newapi** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 105 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **guacamole** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 106 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Drill into the **mgmt** block: its tiles include all 7 named apps (grafana/harbor/key… alongside loki/mimir/nats/tempo. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; mgmt block placement re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 107 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Read the **host** block on the same treemap: none of the 7 named apps appear under `host`. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; host block re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  OPPOSITE of the assertion — the 'host' block contains ALL 7 named apps (they should NOT be there if mgmt-grouped); vCluster grouping degenerate to a single host bucket. ❌ |
| 108 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Open the keycloak app card → its placement detail reads `mgmt` (the per-app placement mirrors the treemap block), not `host`. | hw236 | ⛔ | RE-BASELINED ☐→⛔ (superseded, NOT a product fault): #4325 de-vcluster moved keycloak into its own host ns (no mgmt vCluster), so per-app placement correctly reads `host`, NOT `mgmt` — the row asserts the DEAD mgmt-vcluster placement model that #4325 voided. Consistent with rows 99-107 (already ⛔ for the same reason). Refs #4325 #3642. |
| 109 | placement | [#688](https://github.com/openova-io/openova/issues/688) | The sovereign-realm account console renders for the owner (no second login, no error dialog). | hw227 | ⛔ | RECLASSIFIED ❌→⛔ (by-design, NOT a product fault): the KC account-console "Something went wrong" REST 401 on a brokered catalyst-pin session is an accepted consequence of passwordless-PIN (#688) — the KC-native account console cannot mint a REST token from the PIN-brokered session by design. Link corrected #3642→#688 (#3642 was a mis-link — that issue is the mgmt-vcluster migration, unrelated). ‖ ORIGINAL: hw232 Playwright SSO (screenshot hw232-sso-evidence/): KC account console "Something went wrong" REST 401 on brokered catalyst-pin session — EXPECTED honest-non-green gate holds (KC-native, passwordless-PIN #688) |
| 110 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Gitea opens already signed in (avatar/menu shows the SSO user), repo list renders — no Gitea login form. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Bare https://gitea.hw255.omani.works/ landed signed-in: user menu 'emrah.baysal' present (5 avatar/profile elements), nav Issues/Pull Requests/Milestones/Explore, 'Repositories 0 / All 0' list panel renders, 0 login inputs, Gitea 1.22.3. Screenshot row110-gitea-landing.png — Personal repo list is empty (mirrored catalog lives under the catalog org owner) — the list surface renders; no Gitea login form. |
| 111 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Harbor opens signed in, the projects list renders — no Harbor login form, no gateway error page. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: https://harbor.hw255.omani.works -> alias-redirect to https://registry.hw255.omani.works/harbor/projects, signed in as emrah.baysal@openova.io; projects list renders 9 projects (proxy-dockerhub/ecr/gcr/ghcr/k8s/quay/xpkg...), 223 repos, quota 18.07GiB; no Harbor login form, no gateway error page. Screenshot row111-harbor-projects.png. — Harbor UI host is registry.hw255.omani.works (HTTPRoute harbor/harbor); harbor.hw255 is the alias-redirect route. |
| 112 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Grafana opens signed in (no Grafana login), the home dashboard renders. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): https://grafana.hw255.omani.works/ → 200, landed 'Home - Dashboards - Grafana' (home dashboard renders: Home/Dashboards/Explore/Alerting/Administration nav), 0 login-form inputs; GET /api/user → 200 login=emrah.baysal@openova.io isGrafanaAdmin=true authLabels=['Generic OAuth']. Screenshot row112-grafana-home.png. |
| 113 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | The OpenBao UI renders signed in via OIDC — no manual token/unseal prompt blocking the landing. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: https://bao.hw255.omani.works/ (canonical HTTPRoute host, kubectl: openbao/openbao -> bao.hw255.omani.works) lands on /ui/vault/secrets — 'Secrets Engines' list (cubbyhole/, secret/ kv) signed in; unsealPrompt=false, token input=false. Screenshot row113-openbao.png. — https://openbao.hw255.omani.works/ returns 404 — that subdomain is not the routed host; OpenBao UI host is bao.<fqdn>. |
| 114 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi opens signed in, its main console renders — no login form, no upstream-connect error. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: https://newapi.hw255.omani.works/ -> lands /console/token title 'New API' SIGNED IN (user chip 'sovereign_2', ADMIN sections Channel/Model/User Management visible), no login form (password input absent), no upstream-connect error / 502 / 503. Screenshot row114-newapi-landing.png |
| 115 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Guacamole opens signed in, the connections list renders — no Guacamole login form. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Bare https://guacamole.hw255.omani.works/ OIDC-landed at /guacamole/#/?session_state=…&iss=https://auth.hw255.omani.works/realms/sovereign with id_token preferred_username=emrah.baysal@openova.io (groups incl. sovereign-admins); page renders 'RECENT CONNECTIONS' + 'ALL CONNECTIONS' sections; 0 login inputs. Screenshot row115-guacamole-landing.png — 'No recent connections.' — list surface renders, empty state honest. |
| 116 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Organizations directory: the page title / heading reads "Organizations", never "Tenants"/"SME Tenants". | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): /organizations heading (h1) = 'Organizations'; body copy 'Every organization on this Sovereign...'. No 'Tenants'/'SME Tenants' title or heading anywhere on the page. Screenshot row116-organizations-heading.png. — Only 'tenant' match on the page is a k8s workload name 'tenant-7c5c4c6c69' inside the showback data table (org-services namespace object name, not a UI label). |
| 117 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Directory org cards / list rows: column headers read "Organization / Kind / Tier / Billing / Isolation / Status"; rows labeled "Organization", never "tenant"/"SME". | — | ✅ | hw232 authed API: thead: Organization|Kind|Tier|Billing|Isolation|Status | — |
| 118 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Left-nav sidebar label for this section reads "Organizations", not "Tenants"/"SME". | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): console left-nav reads 'Organizations' (link → /organizations) — no 'Tenants'/'SME' label anywhere in the sidebar (nav: Dashboard/Cloud/Apps/Catalog/Agenity/Jobs/Compliance/Users/Organizations/Billing/Settings); legacy /bss/tenants exists only as a redirect to /organizations in router.tsx:1829. Screenshot row118-123-dashboard-signedin-org… |
| 119 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Create-organization flow: the form title, field labels, and submit button all say "Organization" (no "SME tenant slug" / "Onboard tenant" persona words). | hw255 | ✅ | create-org form label render authed ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: create-organization form title 'Create organization', labels 'Organization kind'/'Organization slug'/'Company name'/'Admin email', submit 'Create organization'; regex scan of rendered form text for SME tenant/Onboard tenant/\btenant\b = ZERO matches. Screenshot row8-create-org-form.png. |tenant)\b` over the rendered form = ZERO matches. No 'SME tenant slug' / 'Onboard tenant' persona words. Refs #3383 |
| 120 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Organization-detail view: heading "Acme Corp", breadcrumb "← Organizations", field labels Slug/Kind/Tier/Billing mode/Isolation/Stat… — no "tenant"/"SME". | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Authed /organizations/hw255-omani-works: heading 'Omantel' (this prov's seeded Org; 'Acme Corp' was the original walk-env's org name), breadcrumb '← Organizations', field labels Slug(Hw255-Omani-Works)/Kind(Internal)/Tier(Org)/Billing mode(Showback)/Isolation(Namespace)/Status(Active) all render; page text contains ZERO 'tenant' and ZERO 'SME' strings (regex hits 0/0). Screensh… — Row's literal 'Acme Corp' is env-specific; claim's substance (org-detail render + no banned terms) holds | — |
| 121 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | BSS / billing screen: billing is framed as "This organization is in showback mode…", zero "tenant"/"SME" leaks. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): /billing/orders and /billing/revenue both render [data-testid=billing-showback-notice] with exact copy: 'This organization is in showback mode — consumption is attributed, not invoiced. Real billing (invoices, vouchers, checkout) exists when the marketplace sells to external organizations.' Zero 'tenant'/'SME' strings in UI copy across /billing, /billing/orders, /billing/revenu… — Vouchers view is intentionally un-gated (#4170 — sovereign-admin BSS action). Sole 'tenant' regex hit is the workload name tenant-7c5c4c6c69 (org-services 'tenant' microservice ReplicaSet) inside cons… |
| 122 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Legacy `/bss/tenants` URL (PR #3390 alias) resolves and redirects to `/organizations`, rendering the directory (H1 "Organizations") — not a 404, not a login redirect. | hw255 | ✅ | legacy /bss/tenants redirect authed SPA route walk ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser goto https://console.hw255.omani.works/bss/tenants -> client redirect to /organizations rendering H1 'Organizations' + directory content; not a 404, not a login redirect. Screenshot row122-bss-tenants-redirect.png. — PR #3390 alias confirmed live on hw255. |
| 123 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Handover URL → lands on `/dashboard` already signed-in as the owner (avatar E), no login form (everything below is admin-gated). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): /dashboard renders already-signed-in as owner — header avatar button 'E', sidebar 'EB emrah.baysal@openova.io hw255.omani.works', no login form; whoami 200 tier=owner roles=[catalyst-admin,catalyst-owner]; admin-gated surfaces (Decommission link, Settings, Billing) visible. Screenshot row118-123-dashboard-signedin-orgs-nav.png. — Landing-state verification: the one-shot handover URL itself was consumed at prov (env is post-handover sovereign-owner mode); re-firing it read-only is not possible. |
| 124 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Catalog grid renders Blueprint cards in a tile grid, each with an icon + summary; the Alloy card is visible. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: /catalog renders tile grid (94 card elements, 43 logo images), each card icon+summary; Alloy card visible with summary 'Unified node agent for logs, metrics, and traces'. Screenshot row124-catalog-grid.png. |
| 125 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the Alloy card → the detail page renders: a hero (icon + name + summary + Edit IaC), an About section, and an Instances list; no login redirect. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Authed /catalog/bp-alloy: hero renders (icon /component-logos/alloy.svg x3 + name Alloy + summary + 'Edit IaC' present), About section present ('Grafana Alloy — telemetry collector...'), Instance section lists the running install (bp-alloy Flux-System Ready 'Open instance →'); no login redirect (login-regex on page head false). Screenshot row125-alloy-detail-full.png |
| 126 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the admin Edit affordance in the hero → an edit form drops INLINE into the detail page (no modal overlay); fields appear in-place under the hero. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): On /catalog/bp-wordpress every admin edit affordance is INLINE: Edit IaC opens catalog-edit-iac-section + yaml-editor inside the page flow; per-field editors cif-name-edit / cif-summary-edit click -> cif-summary-input rendered in place; [role=dialog]/[aria-modal] count = 0 throughout the whole walk. Screenshots row126-inline-edit-form.png, row126-inline-edit-input.png — Design evolved from one hero form to per-field inline editors (CatalogInlineField) + inline IaC section — no modal overlay exists. Editors closed via Cancel/close, nothing applied. Gotcha found: while… |
| 127 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | In the inline form, change Summary to `RECONCILE-PROOF-<ts>` → Save → the page refreshes in place and the new summary shows in the hero. | — | ☐ | — |
| 128 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Back on the grid, the Alloy card summary now reads `RECONCILE-PROOF-<ts>` — the edit propagated to the card, not just the detail page. | — | ☐ | — |
| 129 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Hard-reload the detail page → the summary is still `RECONCILE-PROOF-<ts>` — the edit persisted across a reload, not an in-memory overlay. | — | ☐ | — |
| 130 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The non-card `version` field renders as a `v1.0.1` chip in the hero, and Edit IaC exposes the full CR including `version` for editing. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): /catalog/bp-agenity hero renders version chip 'v0.5.20' (matches API GET /api/v1/catalog/bp-agenity -> 200 version 0.5.20); Edit IaC opens the CR editor and the backing raw CR contains spec.version: 0.5.20 (editable, non-card field). Screenshots row130-catalog-detail-hero.png, row144-edit-iac-editor.png. — Row's 'v1.0.1' is the example value from the original env; the claim is the chip render + version exposure in Edit IaC — both hold. |
| 131 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Note the current hero logo (the Alloy glyph) as the baseline before an icon edit. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): /catalog/bp-alloy hero logo baseline noted — img.hero-logo src=https://console.hw255.omani.works/component-logos/alloy.svg alt='Alloy' (plus light/dark clp-img variants of the same Alloy glyph). Screenshot row131-catalog-alloy-hero.png. — Baseline-only row; no icon edit performed (read-only walker). |
| 132 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit → in the Light-theme icon field paste a distinct image → Save → a "Saved to IaC ✓" confirmation shows and the page refreshes. | — | ☐ | — |
| 133 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Observe the hero — it now shows the new logo; the render reads the edited `card.iconLight` first (IaC-first), not the bundled asset. | — | ☐ | — |
| 134 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Return to the grid — the Alloy card icon is now the new logo (the grid tile resolves the same edited `card.iconLight`). | — | ☐ | — |
| 135 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Reload the detail page — the hero is still the new logo (render reads the persisted IaC icon on every load). | — | ☐ | — |
| 136 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit again — the Light-theme icon field shows the current IaC value, falling back to the bundled asset only when IaC carries none. | — | ☐ | — |
| 137 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit → click the icon picker (`iconpicker-*`) → a thumbnail grid of vendored `component-logos/*` assets opens (a role=listbox grid of logo tiles). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: /catalog/bp-postgres renders IconPicker editors — data-testid iconpicker-light + iconpicker-light-grid role=listbox with 58 role=option tiles (iconpicker-light-tile-alloy, -cilium, -cnpg, ...) whose img srcs are vendored /component-logos/*.svg/png; 2 grids (light+dark). Screenshot row137-icon-picker.png. — No tile clicked, nothing saved. |
| 138 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click `cilium.svg` in the picker grid → the icon field + a live preview swatch update to the Cilium logo. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Authed /catalog/bp-alloy: clicked hero icon -> picker gallery opened (contains cilium.svg); clicked cilium.svg -> icon field input value updated to '/component-logos/cilium.svg' and 3 preview imgs now render cilium.svg. Escape pressed — NOT saved. Screenshots row138-icon-picker-gallery.png, row138-icon-field-preview-cilium.png — Client-side form update only; no Commit/Save clicked |
| 139 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Save → reload — the hero is now the Cilium logo (the picker selection persisted to IaC and renders on hero + grid card). | — | ☐ | — |
| 140 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | On Save, the durable-commit verdict (git outcome) is surfaced in-UI (the Edit-IaC `managed-by: manual • in sync` indicator + {stored:true,commit… not a silent store success. | — | ☐ | — |
| 141 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Hover the summary line in the hero → a pencil/edit affordance appears ON the field (`cif-summary-edit` → `cif-summary-input`), inline, without opening the full form. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Hover the summary line in the hero -> cif-summary-edit pencil affordance appears (count 1, visible); click -> cif-summary-input appears (count 1) inline without opening the full form or any modal (dialogs 0); Cancel restores the display (input count back to 0). Screenshot row141-summary-hover-pencil.png — Exact testid chain the row names (cif-summary-edit -> cif-summary-input) verified live on hw255. No save performed. |
| 142 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the summary → type a value → Save → only the summary updates in place; no full-form modal opens. | — | ☐ | — |
| 143 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Repeat the inline edit for the name field (`cif-name-edit` → `cif-name-input`) — it edits in place and saves just that field. | — | ☐ | — |
| 144 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit IaC (`catalog-detail-ed… admin only) → the full `blueprint.yaml` opens in the YAML editor (the entire CR, not just 7 card fields). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Click 'Edit IaC ⟩' on /catalog/bp-agenity -> YAML editor opens with the FULL Blueprint CR: 'apiVersion: catalyst.openova.io/v1alpha1 / kind: Blueprint / metadata... / spec...' — API raw CR carries 13 spec keys (card, configSchema, depends, endpoints, manifests, multiInstance, observability, owner, placementSchema, sso, topology, upgrades, version, visibility), i.e. the entire C… — Editor opened read-only; no save. Session tier=owner (admin-only control visible). |
| 145 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Change a field in the editor → Commit → a Show-diff Current/Proposed side-by-side renders the change and the commit succeeds (confirmation appears). | — | ☐ | — |
| 146 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The editor subtitle states it directly: "Commit writes the IaC source of truth; Flux reconciles it… Both this editor and the card form above write the same file." | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): /catalog/bp-alloy → Edit IaC (client-side toggle, verified onClick=setEditingIaC(true) only; NO commit performed) → editor subtitle renders verbatim: 'Editing the complete blueprint.yaml in Gitea (catalog-sovereign). Commit writes the IaC source of truth; Flux reconciles it into the in-cluster Blueprint. Both this editor and the card form… |
| 147 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Open the WordPress detail page → Edit → change Summary → Save → reload → the summary persists, exactly as for Alloy. | — | ☐ | — |
| 148 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | WordPress: Edit IaC → edit `spec.manifests` → Commit → reload — the same YamlEditor edits a structurally-different blueprint's manifests in place. | hw255 | ✅ | **hw255-mutation-2026-07-15:** LIVE post-cutoverComplete mutation walk (dep 5762118f63abef96, owner session) — dry-run POST /k8s/Blueprint/_/bp-wordpress/dry-run with the exact #4896 bare-name shape (metadata.name=wordpress) → **200** (also bare alloy vs bp-alloy → 200); Commit PUT /api/v1/catalog/bp-wordpress/iac → 200 committed:true (path catalog-sovereign/bp-wordpress/blueprint.yaml); reload GET /api/v1/catalog/bp-wordpress flips source in-cluster→sovereign and returns the committed edit (walk.openova.io/uat-148=hw255-2026-07-15); flux GitRepository/openova-catalog-sovereign revision d5975c9c→05c3d75d, Kustomization/catalog-sovereign applied, live CR SSA-adopted (rv 49327→361530) ([evidence](https://github.com/openova-io/openova/issues/4896#issuecomment-4978678008); #4896 closed) ‖ **hw253 2026-07-15 (#4896):** #5041 (e960e0465) fix code-verified + deployed — `resolveCatalogEditRepo` bare↔bp- toggle + `metadata.name`→canonical `bp-<slug>` eliminates the dry-run 400 name-mismatch ([comment](https://github.com/openova-io/openova/issues/4896#issuecomment-4976211220)). Console LIVE-walked (console.hw253.omani.works→200, PIN login-flow functional — screenshots `hw253-console-login-live.png` + `hw253-console-pin-verify.png`). ⚠️ authenticated dry-run→200 render PIN-gated by founder #688 (agent can't self-auth); full ✅ pends a founder session (#949). |
| 149 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | WordPress: Edit → Light-theme icon → distinct image → Save → reload — the WordPress hero icon visibly changes. | — | ☐ | — |
| 150 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | PostgreSQL detail renders the SAME edit surface (hero icon, Edit IaC, clickable cards) and Edit IaC exposes `contextSchema` (a blueprint carrying contextSchema`/`sha… | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: /catalog/bp-postgres renders same edit surface — hero icon block (clickable, 'Click the hero icon above to change'), 'Edit IaC ⟩' opening the full blueprint.yaml editor ('Edit IaC — full blueprint ... blueprint.yaml in Gitea'), clickable instance rows (shared-pg/b/c 'Open →'). Wire: GET /api/v1/catalog/postgres -> 200 payload contains contextSchema {kind:db, needs:[nam… — Hero renders as letter-tile fallback (no stored logo asset for PostgreSQL) — edit surface itself fully present. |
| 151 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Alloy + Postgres render the IDENTICAL edit chrome (same cif-icon-edit`/`cif… same Edit-IaC YamlEditor) — no blueprint-specific UI. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Authed compare: bp-alloy vs bp-postgres 'Edit IaC' chrome IDENTICAL — same cif class set [cif, cif-display, cif-display-readonly, cif-topo-summary], same single .cm-editor (CodeMirror YamlEditor, monaco/textarea 0), same 'Edit IaC — full blueprint' heading and same button row [Show diff, Copy, Validate (dry-run), Commit IaC, Close]. Screenshots row151-editchrome-bp-alloy.png, r… — Only content differs (Instances list + New instance on postgres); chrome is blueprint-agnostic |
| 152 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The catalog detail page renders (hero · About · Instances) and opens an INLINE Edit form (no modal) — acceptance headline 1. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): /catalog/bp-wordpress renders hero (catalog-hero: logo pair, version, tags) · About (catalog-section-about: 'Runs in the Org's vcluster…') · Instances ('+ New instance', honest empty state) · Supported topologies; INLINE edit opens (cif field editors + catalog-edit-iac-section yaml-editor) with zero [role=dialog]/[aria-modal] elements. Screenshot row152-catalog-detail.png — Acceptance headline 1 of #3668 holds on hw255. |
| 153 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | A summary edit Saves, updates the page AND the grid card, and persists across a reload — acceptance headline 2. | — | ☐ | — |
| 154 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | A non-card field edit (`version`) persists — the whole CR is editable, not a 7-field overlay — acceptance headline 3. | hw255 | ✅ | **hw255-mutation-2026-07-15:** LIVE post-cutoverComplete mutation walk (dep 5762118f63abef96, owner session) — non-card `spec.version` edit PERSISTS end-to-end: PUT /api/v1/catalog/bp-alloy/iac 1.0.2→1.0.3 → 200 committed:true → flux applied → `kubectl get blueprint bp-alloy .spec.version` = **1.0.3 on the LIVE CR** (no kubectl writer — console-API→gitea→flux only); revert commit 1.0.3→1.0.2 → 200 → live CR back at 1.0.2 (GitRepository revision …→41451eb0, Kustomization Ready=True) — chain proven both directions, final state clean ([evidence](https://github.com/openova-io/openova/issues/5018#issuecomment-4978678159); #5018 closed) ‖ **hw253 2026-07-15 (#5018):** #5041 (e960e0465) dual-write fix code-verified + deployed — full-CR commit writes BOTH the per-BP repo AND the Flux-watched aggregator (`writeCatalogSovereignAggregatorBytes`), `502 committed:false` on aggregator-leg failure (no silent-inert split-brain) ([comment](https://github.com/openova-io/openova/issues/5018#issuecomment-4976211345)). Console LIVE (200, PIN login-flow OK); ⚠️ authenticated dual-write render PIN-gated by #688; full ✅ pends founder session (#949). |
| 155 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The edited icon renders on hero + grid + survives reload; the form pre-fills the IaC icon; the picker grid works — acceptance headline 4. | — | ☐ | — |
| 156 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Save surfaces the IaC-commit verdict (`committed:true` + `• in sync`), not a bare store success — acceptance headline 5. | — | ☐ | — |
| 157 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Per-field inline edit for cards (`cif-*`) + the full-CR YamlEditor (Edit IaC) for the rest, both writing the same IaC source — acceptance headline 6. | — | ☐ | — |
| 158 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The identical edit mechanism works on a 2nd + 3rd blueprint — no per-blueprint UI (Alloy + Postgres + a third) — acceptance headline 7. | — | ☐ | — |
| 159 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Settings → Sovereignty section renders a "Cluster sovereignty" panel with a "TETHERED" badge + an "Achieve True Sovereignty" cutover CTA (runs the 8-step cutover + 10-min egress-block self-test). | hw255 | ⛔ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Settings -> Sovereignty renders 'Cluster sovereignty' panel: badge SOVEREIGN, 'This Sovereign is operationally independent of the openova-io mothership', EGRESS TEST passed, 'All 11 steps completed. 100%' with the per-step list + timestamps. Screenshot row159-settings-sovereignty.png. — TETHERED badge + 'Achieve True Sovereignty' CTA are the PRE-cutover render — permanently unsurfaceable on hw255 (cutoverComplete=true 07:21Z; the CTA demonstrably fired and worked). Minor inconsistenc… |
| 160 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Console nav + Settings sidebar expose a dedicated "Sovereignty" anchor (`#sovereignty`) that scrolls to + highlights the Cluster-sovereignty panel — the cutover trigger is a first-class surface. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): /settings sidebar TOC exposes dedicated anchor <a href="#sovereignty" data-testid=settings-toc-sovereignty>Sovereignty</a>; click → URL /settings#sovereignty and #sovereignty Cluster-sovereignty panel (class scroll-mt-20) scrolled into view (boundingRect top=80px, inView=true); panel: 'Cluster sovereignty… EGRESS TEST passed · CUTOVER IN … |
| 161 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Open `/jobs` (zero-login, signed in as owner) → the canvas table renders a populated activity list (not a spinner, empty state, or login redirect). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser (owner session, zero-login): /jobs renders populated activity table 43/46 rows with STATUS/KIND/APP/PARENT filters — not a spinner, not empty, no login redirect. Wire: GET /api/v1/deployments/5762118f63abef96/jobs -> 200, 46 jobs. Screenshot row161-jobs-page.png. |
| 162 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Find the `cutover` group row and expand it → it renders the 11 `cutover-step-*` rows (gitea-mirror, harbor-projects, harbor-prewarm, registry-pivot, … vcluster-registry-p… — the 11-step execution tree. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Authed /jobs: PARENT filter 'Cutover' -> counter 11/46, renders exactly the 11 cutover STEP rows (Gitea Mirror, Harbor Projects, Harbor Prewarm, Registry Pivot, Flux Gitrepository Patch, Helmrepository Patches, Catalyst Api Env Patch, Egress Block Test, Gitea Token Mint, Vcluster Registry Pivot, Crossplane Provider Pivot) all SUCCEEDED, DEPS column carries the cutover-step-* ch… — UI groups via PARENT=Cutover filter rather than an expandable group row; 11-step tree fully rendered. Cutover completed during walk (was step-03 at briefing) |
| 163 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Each `cutover-step-*` row reads an honest per-step status (Succeeded / Running / Failed / Pending), never a premature green. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Settings -> Sovereignty 'Cluster sovereignty' renders all 11 step rows, each with its real completion timestamp matching the self-sovereign-cutover-status ConfigMap 1:1 (gitea-mirror 05:09:04Z->13:09:04 local, harbor-projects 05:09:12Z, harbor-prewarm 06:46:49Z, registry-pivot 06:46:59Z, flux-gitrepository-patch 06:47:03Z, helmrepository-patches 06:49:13Z, catalyst-api-env-patc… — No premature green — statuses stamped from actual Job completions; cutover finished 07:21:07Z during this walk (briefing said step-03; live state re-verified). Cosmetic residue: heading still reads 'C… |
| 164 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | The cutover group status reflects its real children (a group with a failed child reads failed, not a fake Succeeded). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): GET /api/v1/deployments/5762118f63abef96/jobs -> 200, 46 jobs: group 'cutover' status=succeeded with all 11 cutover-step-* children succeeded; group 'reconcilers' status=running with 8 running children — group statuses live-aggregate their real children (two groups, two different truthful states; no fake Succeeded over non-succeeded children). Jobs page shows children with Pare… — Failed-child negative case not surfaceable on this healthy env (0 failed jobs); consistency demonstrated via succeeded + running groups. |
| 165 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | On a failed `cutover-step-*` row, a Re-run button is present (per-row, gated to Failed) — the operator can re-drive a failed cutover step from the browser. | hw255 | ⛔ | hw232 Playwright UI (hw232-ui-*): no Failed cutover step present (7✓/1▶/3⏳) so Re-run affordance not observable; re-walk if a step fails ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser + wire): no Failed cutover-step row exists to observe — /jobs shows all cutover STEP/TASK rows SUCCEEDED (registry-pivot, gitea-mirror, harbor-prewarm, egress-block-test 25m57s, crossplane-provider-pivot…); GET /api/v1/sovereign/jobs: only failed items are 3 historical vpc-podcidr-route-reconciler CronJob runs whose cron row aggregates SUCC… — Same not-observable state as the hw232 ⚠️ stamp; re-walk if a cutover step ever fails. |
| 166 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | (After a COMPLETE cutover) the `cutover` group reads all-11-green on `/jobs` — every step Succeeded, including `egress-block-test` and `registry-pivot`. | hw255 | ✅ | hw232 cutover fired (auto-trigger hook, #4885 defect-1) + advanced 7/11: registry-pivot DS 6/6 (#4870), gitea-mirror/harbor-prewarm/harbor-projects/flux-patch/helmrepo-patches/env-patch Complete; WEDGED at step08 deny-egress hold — 12 openova-io workloads still on ghcr.io (step07 pivots only 2 HRs) → cutoverComplete unreachable. Real Pillar-5 defect filed #4885. NOT complete. ‖ hw255-cc-2026-07-15T07:21Z: cutoverComplete=TRUE zero-touch (cutover 0.1.135, 05:03→07:21Z, all 11 steps success; step-08: ref-host lint PASS + mothership black-holed #3678 + 15/15 minimal roll-set PASS under deny-egress (CSI excluded #5081, gitea RWO-EVS excluded #5091) + 600s hold zero regressions; registriesYaml v2 on 6/6 nodes; dragonflyUpstream=local; prewarm transit-flap FATAL recovered by designed re-run #5030). #3379 CLOSED. Jobs API backing: all 11 cutover-step Jobs Complete 1/1 (kubectl get jobs -n catalyst). Render leg follows in wave-2. ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Render leg (backing already stamped): /jobs PARENT=Cutover filter shows exactly 11 STEP rows ALL SUCCEEDED — Gitea Mirror, Harbor Projects, Harbor Prewarm (1h37m), Registry Pivot, Flux Gitrepository Patch, Helmrepository Patches, Catalyst Api Env Patch, Egress Block Test (25m57s), Gitea Token Mint, Vcluster Registry Pivot, Crossplane Provider Pivot. Wire: jobs API group cutover… | hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: cutover NOT fired. Sovereign CR cutoverComplete is unset; only the pre-cutover job bp-self-sovereign-cutover-gitea-secret-bridge exists (Complete) — the 11-step cutover group has not run, so /jobs cannot show it all-green. Refs #3379 |
| 167 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Open the console root in a fresh tab → land on the operator dashboard signed in as the sovereign-admin (no login form, no password field). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Fresh browser tab -> GET https://console.hw255.omani.works/ -> 200 lands /dashboard operator dashboard signed in (avatar EB, emrah.baysal@openova.io), no login form, no password field (input[type=password] absent). Wire control: same GET unauthed -> whoami 401. Screenshot row97-dashboard-treemap-layers.png |
| 168 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Open `/jobs` → the canvas table renders a populated list of activity rows (not a spinner, empty state, or login redirect). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): /jobs renders a populated canvas table: 44 rendered rows, counter 43/46, real activity (cutover steps SUCCEEDED with 3-18s durations, trivy-security-scan RUNNING 24 runs, crons, KIND column task/cron/step, PARENT Cutover/Provision Huawei/Reconcilers), banner 'Live state stream re-attached. Refreshing from the catalyst-api every 5s.' — no spinner, no empty state, no login redire… |
| 169 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | The Kind column is present in the header and each row shows its kind; full header = Name·Kind·App·Deps·… | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): /jobs table header = Name · Kind · App · Deps · Parent · Status · Runs · Started · Duration · Actions; every row shows its typed Kind (STEP/TASK/CRON/LIFECYCLE), e.g. 'Egress Block Test STEP ... SUCCEEDED 1 ... 25m 57s'. API kinds: task 21, step 11, cron 6, lifecycle 5, group 3. Screenshot row169-jobs-kind-column.png. |
| 170 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Scroll/search to the `install-openbao` row → it renders green / Succeeded (the install is honestly green). | hw255 | ⛔ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 wire+browser+kubectl): NO install-openbao row exists on this env — GET /api/v1/sovereign/jobs (200, 26 Jobs) and /jobs UI (43 rows) contain only openbao-snapshot-save cron (SUCCEEDED); kubectl get jobs -A / grep -iE 'openbao/install' → only openbao-snapshot-save-29735070 Complete. OpenBao install on hw255 is delivered via Flux: HelmRelease bp-openbao READ… — Row presupposes an install-openbao Job that the current provisioning design no longer creates; suggest reclassifying the row's target to the bp-openbao HR/lifecycle row. |
| 171 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Every rendered row maps to a real HelmRelease install / terraform stage (no placeholder, no synthetic/fabricated entry). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): All 46 /jobs rows map to real observations, cross-checked vs live cluster: 11 cutover-step-* ↔ live cutover-* Jobs (kubectl get jobs -n catalyst); task-* ↔ live k8s Jobs (cert-nextkey-guard, cilium-envoy-tls-restart, kyverno-migrate-resources, legacy-cert-cleanup, mimir-make-minio-buckets, powerdns-zone-bootstrap, bp-self-sovereign-cutover-gitea-secret-bridge...); cron-* ↔ 6 li… — One stale STATUS (not a fabrication): task-cnpg-pair-bp-cnpg-pair-primary-2-join shows RUNNING though the CNPG join Job completed and was reaped (cnpg cluster 3/3 'Cluster in healthy state'). |
| 172 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Set the Status filter to `failed` → the table shows the genuinely-failing rows, each with an honest failed status (the Status filter works). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Authed /jobs STATUS select -> 'failed': table honestly shows 'No jobs match the current filters' counter 0/46 — verified genuinely empty via kubectl: the only recent failures (vpc-podcidr-route-reconciler-29734985/90/95, DeadlineExceeded during the 06:54-07:20Z deny-egress hold) are superseded by latest run 29735090 Complete 1/1 (36s old), and the cron's aggregated row shows SU… — No currently-failing job exists to render; filter + honest statuses verified against kubectl ground truth |
| 173 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Leave the table on screen ~30s → rows update live (tail) as reconciliation progresses; a status badge changes in place without a manual reload. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Left the table on screen (no reload, URL unchanged): 20s in-place diff shows a status badge flip PENDING->RUNNING at a fixed row slot and live re-sorting (guacamole-bp-guacamole-admin-enroll cron surfaced RUNNING 21 runs / 3h29m, newapi-admin-promote RUNNING 21 runs; cnpg-cabundle-reassert repositioned '2 runs 12m ago'); earlier 35s window also changed (len 5825->5823). Screens… — Live tail proven by content diff, not just timestamp tick — badges changed in place without manual reload. |
| 174 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | On a Failed row (Status=failed), a Re-run / Retry-reconcile button is present on the row (visible on the row or on hover). | hw255 | ⛔ | hw241: healthy env has 0 failed jobs (ACTIONS=— on all 35) → failed-row Re-run cannot be surfaced ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): GET /api/v1/deployments/5762118f63abef96/jobs -> 200: statuses succeeded 37 / running 8 / pending 1 — zero failed rows; Jobs page Actions column '—' on all rows, no Failed row exists to surface the Re-run control. Retry endpoint present in the API surface (POST /api/v1/deployments/{depId}/jobs/{jobId}/retry). — Same shape as the hw241 ⚠️ stamp: healthy env cannot surface a failed-row Re-run button. Not a product failure. |
| 175 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | On a Succeeded / healthy / Confirming row, NO Re-run button renders — the control is gated to Failed rows only. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): /jobs — every Succeeded/Running/Pending row renders ACTIONS '—'; Re-run/Retry button count = 0 across all 43 rows (per-row check on 40 sampled rows incl. all SUCCEEDED cutover steps, crons, tasks, LIFECYCLE Bootstrap Cluster). Control leaks onto zero healthy rows. Screenshot row190-jobs-page.png (full-page /jobs). |
| 176 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Click Re-run on a Failed row → a success toast/feedback appears and the button flips in place (e.g. `Requesting…`) — the browser triggered a re-reconcile, no terminal. | hw241 | ⚠️ | — | hw241 walker(topo/cloud/jobs/robustness) live curl+kubectl: POST retry mechanism, not exercised (0-failed+read-only) |
| 177 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Use the same Re-run button on a Failed row — one remediation mechanism across rows, no per-kind UI (the single-table/single… shape). | — | ☐ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Re-run/Retry is POST /api/v1/deployments/5762118f63abef96/jobs/{jobId}/retry (handler jobs_retry.go) — a write, not performed. Additionally the precondition is absent: /jobs shows 0 Failed rows (41 succeeded/3 running/2 pending); ACTIONS column renders '—' on non-failed rows so no Re-run control is visible to exercise. Screenshot row161-jobs-page.png. — Exact write deferred: POST .../jobs/{jobId}/retry on a Failed row. |
| 178 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | The signed handover URL → lands directly on `/dashboard` signed-in (env switcher shows the live env, avatar E, no login form). | — | ☐ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Cannot exercise read-only: the signed handover URL for hw255 is one-time and was consumed at handover (org created 05:01:46Z, cutover chain fired 05:03:23Z; session now post-handover). Re-minting requires a mothership handover-export WRITE (handover.go/handover_jwt.go flow; mothership rejects the hw255 session: GET https://console.openova.io/sovereign/api/v1/whoami -> 401). Adj… — Exact write deferred: mothership handover-export/re-mint of the signed handover URL for dep 5762118f63abef96 |
| 179 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Click the avatar (top-right) → menu reads "Signed in as the owner" with a Sign-out item — confirms the landed identity is the owner-admin. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Avatar (top-right) menu: 'Signed in as' + 'emrah.baysal@openova.io' + 'Sign out' menuitem; wire identity: GET /api/v1/whoami -> 200 {tier:'owner', roles:['catalyst-admin','catalyst-owner']} — landed identity IS the owner-admin. Screenshot row27-179-avatar-menu-signedinas.png — Same surface as row 27 under #3581. |
| 180 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Grafana bare URL → lands on Grafana Home ("Welcome to Grafana", full UI, Profile avatar), `?orgId=1`, no login form (SSO landed signed-in). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: https://grafana.hw255.omani.works -> landed https://grafana.hw255.omani.works/?orgId=1&from=now-6h&to=now 'Home - Dashboards - Grafana' with 'Welcome to Grafana' full UI, profile avatar present, NO login form. Screenshot row180-grafana-home.png. — Silent SSO landed signed-in; ?orgId=1 present in final URL. |
| 181 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Harbor (registry) bare URL → lands on `/harbor/projects` (projects, repos, Administration nav), no login form; user dropdown the owner. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser): harbor bare URL https://harbor.hw255.omani.works/ → landed https://registry.hw255.omani.works/harbor/projects (title Harbor), signed in — top bar 'emrah.baysal@openova.io', Projects (9) + Repositories (223) render, 0 login-form elements; GET /api/v2.0/users/current → 200 admin_role_in_auth=true, comment 'Onboarded via OIDC provider'. Scre… |
| 182 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Gitea bare URL → lands on the gitea dashboard titled "emrah.baysal - Dashboard - Catalyst Gitea", logged in; URL stays on :443. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: https://gitea.hw255.omani.works/ -> title EXACTLY 'emrah.baysal - Dashboard - Catalyst Gitea', logged in as emrah.baysal, final URL https://gitea.hw255.omani.works/ (stays on 443, no port suffix), no login form. Screenshot row182-gitea.png. |
| 183 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | OpenBao bare UI → final rendered screen is the authenticated Vault session (`/ui/vault/secrets`, Secrets Engines with cubbyhole/ + secret/ kv), NO `/ui/vault/auth` token form. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: https://bao.hw255.omani.works/ -> final URL https://bao.hw255.omani.works/ui/vault/secrets title 'OpenBao' — authenticated Secrets Engines screen with cubbyhole/ (cubbyhole_4371fb24) + secret/ (kv_9952ba81); NOT /ui/vault/auth, no token/password form (input[type=password] absent). Screenshot row183-openbao-landing.png — OpenBao 2.3.1 |
| 184 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | — | [UAT.md](https://github.com/openova-io/openova/blob/main/docs/ledger/UAT.md) | ⚠️ | predecessor-walk(B): No test case defined in ledger (literal —). ‖ §I 91dc0591: no test case defined in ledger (literal —) — not click-verified, harness artifact |
| 185 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | No stale predecessor-env references anywhere in the rendered `UAT.md` (the table names only the live env). | hw255 | ❌ | hw224-fulltable-2026-07-04: UAT.md rows walked this pass name only hw224; prior-env blocks labeled stale by construction ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): git show origin/main:docs/ledger/UAT.md / grep -oc per env: hw241=58, hw228=57, hw224=43, hw240=35, hw232=27, hw227=12, hw253=8, hw234=7, hw252=4 stale predecessor-env references; live env hw255=0 mentions — the rendered table does not name the live env at all. — reset-uat for hw255 not yet run/landed on main |
| 186 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | — | [UAT.md](https://github.com/openova-io/openova/blob/main/docs/ledger/UAT.md) | ⚠️ | predecessor-walk(B): No test case defined in ledger (literal —). ‖ §I 91dc0591: no test case defined in ledger (literal —) — not click-verified, harness artifact |
| 187 | topology | [#4001](https://github.com/openova-io/openova/issues/4000) | Per-app Topology for a multi-region app (grafana) shows **Pattern: active-active** with 2 PRIMARY region cards — NOT singleton. | hw255 | ❌ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): bp-grafana Topology tab renders 'Pattern: singleton / No placement targets reported yet. / Status: Loading status…' — NOT 'Pattern: active-active', zero PRIMARY region cards. Contrast: bp-cilium on the same env correctly derives active-active with 2 PRIMARY cards, so the surface works when runtime spans both regions. Screenshot row187-grafana-topology.png — Root: grafana runs only in region-a on hw255 (region-b HR plane 0/0, secondaryDegraded=true; fleet endpoint reports grafana regions [me-east-215-a]) — the multi-region premise never materialized on re… |
| 188 | topology | [#4001](https://github.com/openova-io/openova/issues/4000) | A genuine single-region app (catalyst-api) correctly shows **singleton** (no false multi-region). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Genuine single-region apps render singleton with no false multi-region: /app/guacamole Overview PLACEMENT singleton REGIONS platform-bootstrap-owned-host; /app/bp-grafana Overview PLACEMENT singleton STATUS Ready. Screenshots row67-grafana-overview.png, row188-newapi-singleton.png. — catalyst-api is not an Application CR on hw255 (6 CRs total), so the row's named app was proxied by guacamole/bp-grafana. FLAG for fixers: /app/newapi Topology renders 'Pattern: active-active' (runtim… |
| 189 | topology | [#4000](https://github.com/openova-io/openova/issues/4000) | Region-b kubeconfig self-heals EIP→private-IP **zero-touch** on restart (topology survives a fresh prov). | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): Live on fresh zero-touch hw255: catalyst-api log (kubectl logs deploy/catalyst-api -n catalyst-system): 'secondary-kubeconfig: rewrote server host to VPC-peered private IP (#3991)' depId=5762118f63abef96 region=me-east-215-b-1 serversRewritten=1 + 'secondary-kubeconfig: registered' + POST /api/v1/sovereign/secondary-kubeconfig 201 (recurring 05:01/05:10/05:15/05:20Z). Fix PR #4001 MERGED; #4000 CLOSED. — EIP->private-IP self-heal proven zero-touch on this fresh prov |
| 190 | jobs | [#3916](https://github.com/openova-io/openova/issues/3916) | `/jobs` lists **ONLY finite jobs** (provision/cutover steps, batch Jobs, CronJob runs) — ZERO continuous-reconciler rows. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 wire+browser): GET /api/v1/sovereign/jobs → 200, 26 items ALL kind=Job (cutover-* steps, powerdns-zone-bootstrap, CronJob runs, batch tasks). /jobs UI: 43 rows with KIND column only CRON/TASK/STEP/LIFECYCLE (finite: provision 'Bootstrap Cluster' LIFECYCLE, 11-step Cutover group, batch TASKs, CronJob runs) — ZERO continuous-reconciler rows (no Kustomizatio… |
| 191 | jobs | [#3916](https://github.com/openova-io/openova/issues/3916) | Continuous reconcilers (HelmRelease/Kustomization) do **NOT** appear in `/jobs` (they live in the recon lens). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): /jobs KIND filter enumerates only cron/lifecycle/step/task; jobs API 46 entries contain 0 HelmRelease/Kustomization reconcilers (string scan of full payload = 0 hits); rendered page body contains no HelmRelease/Kustomization rows. Recon lens exists separately at /cloud?view=graph&lens=reconciliation. Screenshot row161-jobs-page.png. |
| 192 | recon | [#3925](https://github.com/openova-io/openova/issues/3996) | The convergence **Reconciliation** link opens the cloud **RECON lens** (`view=graph&lens=reconciliation`), not the default cloud lens. | — | ☐ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): The convergence Reconciliation link lives in the provisioning ConvergenceWizard (products/catalyst/bootstrap/ui/src/pages/sovereign/ConvergenceWizard.tsx:230-234, data-testid wizard-link-reconciliation, search={view:'graph',lens:'reconciliation'}). On post-cutover hw255 the surface is unreachable: GET /provision/5762118f63abef96 redirects to /dashboard (wizard-link-reconciliati… — Convergence wizard is a provisioning-phase surface; link click not exercisable on a converged sovereign without a mothership session. Side-finding: /jobs PARENT 'Reconcilers' links target /jobs/reconc… |
| 193 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Clicking a reconciler opens the **ArgoCD-like management surface** (drill-in). | hw255 | ❌ | hw241: Cloud graph infra-only (no reconciler/Helm nodes), no drill panel — #3996 gap persists since hw239 ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Cloud -> Reconciliation lens DOES render reconciler nodes (89 nodes/117 edges; chips HelmRelease 65/65, Kustomization 10/10, Application 6/6, Continuum 5/5; 'Reconcile Bootstrap Kit/Catalog Sovereign/Org Tenants…' + every bp-* HR) but clicking reconciler nodes (bp-gitea click, spine-gitea dblclick, Reconcile Bootstrap Kit) opens NO drill-in: body text length unchanged (1951 cha… — #3996 ArgoCD-like drill-in gap persists (hw239->hw241->hw255), though the lens itself is materially better than hw241 (reconciler/Helm nodes now present). |
| 194 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Recon surface lists Flux reconcilers with live status (Reconciled/Reconciling/Degraded/Suspended). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Cloud 'Reconciliation' lens renders Flux reconcilers (HelmRelease 65/65, Kustomization 10/10, named Reconcile-* Kustomizations + bp-* HelmReleases); List view (/cloud?view=list&kind=helmreleases) has a live Status column: bp-alloy 'Reconciled', bp-catalyst-platform 'Reconciling', etc. API GET /api/v1/deployments/{dep}/reconcilers -> 200 with 205 reconcilers (Reconciled 139 / Re… — Recon page was folded into the Cloud graph/list per #3958 (old /reconciliation route deleted by design). |
| 195 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Drill a reconciler → its controller **logs** render. | hw255 | ✅ | hw241: no reconciler node → controller-logs drill unreachable — #3996 gap ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser+wire): reconciler drill /cloud/resource/helmrelease/flux-system/bp-alloy/reconcile renders controller logs — [data-testid=reconcile-tab-logs] populated with helm-controller JSON lines ('HelmChart/flux-system/flux-system-bp-alloy … is in-sync', 'checking 1 dependencies'); wire: GET /api/v1/deployments/5762118f63abef96/reconcilers/HelmRelease… |
| 196 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | **Reconcile** action → `reconcile.fluxcd.io/requestedAt` lands on the live object. | hw255 | ⚠️ | hw241: no Reconcile control in graph/DAG/Resources tab — #3996 gap ‖ **hw255-CORRECTION-2026-07-15 (#5100 triage): the fleet-walk ❌ was a METHODOLOGY FALSE-POSITIVE** — it grepped the bundle for `reconcile.fluxcd.io`, a token that exists only SERVER-side (the Go handler stamps the annotation on POST .../reconcilers/{kind}/{ns}/{name}/reconcile). The control IS in the deployed hw255 bundle: `Reconcile now`=1 hit, `/reconcilers` path=2, `reconcile-kv-suspended` testid=1 (#3996 UI landed 684e538c5/PR #3999, ancestor of image 2b05348). ⚠️ pending the authed ACTION-walk (click → annotation lands on live object). |
| 197 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | **Suspend/Resume** → `spec.suspend` flips on the live object. | hw255 | ⚠️ | hw241: no Suspend/Resume control on those surfaces — #3996 gap ‖ **hw255-CORRECTION-2026-07-15 (#5100 triage): fleet-walk ❌ was a METHODOLOGY FALSE-POSITIVE** — `spec.suspend` is a server-side merge-patch token, never in the SPA bundle. The deployed bundle DOES carry the `Suspend` and `Resume` button labels (children: literals verified in /assets/index-DqSJ9fgF.js). ⚠️ pending the authed ACTION-walk (toggle → spec.suspend flips on live object). |
| 198 | cloud | [#3987](https://github.com/openova-io/openova/issues/3987) | `/cloud` per-kind **helmreleases** page shows the real count (~65), not 0. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: /cloud?view=list&kind=helmreleases shows '65 items' with 65 table rows (bp-alloy Reconciled 1.0.2, bp-cert-manager 1.2.6, ...) — exactly matching kubectl get helmreleases -A --no-headers / wc -l = 65 on the host cluster. Not 0. Screenshot row198-cloud-helmreleases.png. — Cache API aggregates 130 HRs across both region clusters (65+65); the page scopes to 65. |
| 199 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **Gateway** page shows the live cilium Gateways (2: `cilium-gateway` + `cilium-gateway-console`). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Authed /cloud?view=list&kind=gateways: page renders exactly the 2 live cilium Gateways — kube-system/cilium-gateway (class cilium, 3 listeners) + kube-system/cilium-gateway-console (2 listeners), '2 items'. Matches kubectl get gateways -A (cilium-gateway + cilium-gateway-console, kube-system). Screenshot row-cloud-kind-gateways.png — Page honestly shows ADDRESS '—' / PROGRAMMED False, matching live Gateway status (Accepted=True, Programmed=False — gateway served direct per §854); k8s API across both clusters returns 3 (region-b's … |
| 200 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **HTTPRoutes** page shows 15. | hw255 | ❌ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Cloud -> Networking lens chip reads 'HTTPRoute 0/0' (also Gateway 0/0, NetworkPolicy 0/0) while the live cluster has 16 HTTPRoutes, 2 Gateways, 28 NetworkPolicies (kubectl get -A --no-headers / wc -l). Neither the row's 15 nor the live 16 is shown. Service 172/172 and Ingress 1/1 populate correctly in the same lens. Screenshot row200-cloud-networking-httproutes.png — Gateway-API + NetworkPolicy kinds are empty-data in the cloud view on hw255 (#3987/#3998 family regression shape); stale expected-count 15 in the row also needs refresh to live 16 once fixed. |
| 201 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **NetworkPolicies** page shows the live policies (30 live; expected ~10 stale). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): /cloud?view=list&kind=networkpolicies renders 30 live NetworkPolicy rows (alloy-default-deny, continuum-controller, cnpg-pair-bp-cnpg-pair-allow-probe-to-replica, dragonfly-mysql, ...) with Namespace/Name/Pod Selector/Ingress-Egress/Policy Types/Age columns — matches the row's expected 30 live. kubectl region-a count=28 (console aggregates both clusters); API /api/v1/sovereigns… |
| 202 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **CiliumNetworkPolicies** page shows the live policies (42 live; expected 5 stale). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser+wire+kubectl): Cloud → Cilium Network Policies list (/cloud?view=list&kind=ciliumnetworkpolicies) renders '40 items' of LIVE policies whose names match kubectl exactly (alloy-allow-apiserver-egress, baseline-default-deny, cnpg-pair-bp-cnpg-pair-crossregion-dr-primary/replica, gitea/grafana/guacamole/harbor-allow-*…); live region-A kubectl: … |
| 203 | cloud | [#4002](https://github.com/openova-io/openova/issues/4002) | Cloud **Load Balancers** page shows the real LoadBalancer Svc (clustermesh-apiserver EXTERNAL-IP 212.72.24.31), not 0. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Browser: /cloud?view=list&kind=loadbalancers shows 2/2 LB rows, both HEALTHY — hw255.omani.works me-east-215-a and me-east-215-b, listeners tcp:80,443,6443 — not 0. Live LB Svc backing verified: kubectl get svc -A type=LoadBalancer -> clustermesh-apiserver EXTERNAL-IP 212.72.24.36 (region-a) and cache API region-b clustermesh-apiserver ingress 212.72.24.43 (lbipam sharing-key s… — Row's parenthetical (212.72.24.31) is a stale hw236-era expectation; hw255 VIPs are .36/.43. Page design lists one cloud LB row per region rather than raw k8s LB Svcs; the clustermesh-apiserver Svc na… |
| 204 | cloud | [#4002](https://github.com/openova-io/openova/issues/4002) | Cloud **Worker Nodes** page shows the real nodes, not 0. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Authed /cloud?view=list&kind=worker-nodes: 'Worker Nodes 12' — 12/12 real rows (control-plane-me-east-215-a/b m7n.xlarge.8 + worker-1..5 both regions m7n.2xlarge.8), all HEALTHY, not 0. Cross-checked kubectl get nodes (6 region-a via kubeconfig) + /api/v1/sovereign/cloud nodes=12 all Ready. Screenshot row-cloud-kind-workernodes.png |
| 205 | fleet | [#4003](https://github.com/openova-io/openova/issues/4003) | `/fleet/applications` returns the real app count (non-zero), not 0. | hw255 | ✅ | hw240 cleanup-walker(handover+kubectl): GATED-mothership-only: /fleet/applications → 404 on Sovereign, no Fleet sidebar (mothership-only cross-Sovereign surface by design); recon lens Application 7/7 shows non-zero underlying count. ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): GET https://console.hw255.omani.works/api/v1/fleet/applications (owner session) -> HTTP 200 with items count = 68 (non-zero; real apps: alloy, cilium, gitea, grafana, shared-pg, self-sovereign-cutover, …); unauthed 401 family enforced elsewhere. SPA route /fleet/applications serves 200. — Supersedes the hw240 'GATED-mothership-only 404' note: the fleet applications endpoint now serves on the Sovereign console itself. Caveat: it reports every app topology 'single-region' + regions [me-e… |
| 206 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | `kubectl get managed` is non-empty — Crossplane observes the OpenTofu-built infra. | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): KUBECONFIG=/tmp/hw255.kubeconfig kubectl get managed → 24 workspace.opentofu.upbound.io adopt-* resources, ALL SYNCED=True READY=True (AGE 36m). Non-empty: Crossplane observes the OpenTofu-built infra. — Adopt set covers EIPs (cp/nat/elb ×2 regions), ELBs, networks, subnets, and all 12 servers across me-east-215-a/-b. |
| 207 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | A `CloudAdoption` for the real ELB reaches Synced+Ready (Observe), `external-name` = the real ELB id. | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): kubectl get cloudadoptions -A → adopt-loadbalancer-primary: CLOUD=huawei KIND=loadbalancer RESOURCE-ID=9b2addcd-1a9e-4ac3-b7f4-d620106def08 ADDRESS=10.64.1.248 ADOPTED=true SYNCED=True READY=True; backing Workspace annotation crossplane.io/external-name: 9b2addcd-1a9e-4ac3-b7f4-d620106def08 — external-name == the real ELB id; module output adopted = (observed_id == var.resource_id) and it reports true. — adopt-loadbalancer-console-0 (d1f8cc16…) also Synced+Ready. One 'adoption-placeholder' CR is Ready=False by design (placeholder, not an ELB adoption). |
| 208 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | Adoption is **Observe-only** — the live ELB/nodes are untouched (no re-provision). | hw255 | ✅ | adopt-* observe workspaces present nodes original age untouched no-reprovision negative ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): Observe-only proven 3 ways: (1) Workspace module contains ONLY data sources (data huaweicloud_elb_loadbalancers/compute_instance/vpc + hcloud equivalents) — the sole 'resource' block is terraform_data "adoption_anchor" (state-only, creates no cloud resource); (2) nodes AGE 75-80m = original prov age vs adopt workspaces AGE 36m → adoption did not re-provision nodes; (3) the adopted ELB still serves live traffic (conso… — Confirms the hw237 ⚠️ 'no-reprovision negative' on hw255 with a stronger module-content proof. |
| 209 | storage | [#3971](https://github.com/openova-io/openova/issues/3971) | PVCs land on a real **CSI storageclass** (not k3s local-path). | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): kubectl get pvc -A → all 48 PVCs have SC=evs-ssd (provisioner evs.csi.huaweicloud.com, the default SC); 47 Bound, 1 Pending (syft-grype, WaitForFirstConsumer, still on evs-ssd). Zero PVCs on local-path. — Real CSI storageclass end-to-end. (A dormant seaweedfs-storage SC with hetzner provisioner exists but has no PVC consumers.) |
| 210 | storage | [#3971](https://github.com/openova-io/openova/issues/3971) | `local-path` is **FORBIDDEN** (k3s `--disable=local-storage`). | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): kubectl get sc → NO local-path StorageClass (only evs-ssd default + seaweedfs-storage); kubectl get pods -A / grep local-path → none. k3s auto-creates the local-path SC unless --disable=local-storage is set, so its absence proves the disable took effect. — local-path FORBIDDEN honored live on hw255. |
| 211 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP **sovereign-admin** token → `list_applications` returns all apps. | hw255 | ❌ | hw228 DIRECT-WALK 2026-07-07: MCP runtime NOT deployed — mcp.hw228.omantel.biz → 000, no MCP pod/HTTPRoute (only sandbox-controller). list_applications has no server. ❌ Refs #3969 ‖ hw228 2026-07-07 (code-verified): openova-mcp server (products/openova-mcp, #3988) BUILDS clean + RBAC test suite PASSES (internal/identity + internal/tools — the two-layer list_applications scoping + cross-Org get 403 logic is implemented and unit-verified). NOT deployed on the Sovereign: only sandbox-controller runs (no openova-mcp Service/endpoint). Per its README §95-101 + task #899, the DEPLOY (Blueprint chart + per-Sovereign streamable-HTTP/SSE Service + stable DNS + .mcp.json injection) is the #3988 EPIC's REMAINING work (sequenced 'implement after SME'). Row stays ❌ pending that deploy — logic correct, runtime not on the Sovereign. Refs #3988 ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): MCP runtime NOT deployed on hw255: curl -k https://mcp.hw255.omani.works → 404; kubectl get pods,svc -A / grep -iE 'mcp/agenity' → nothing (only sandbox/sandbox-controller-75cf97bbd5-8hnvs 1/1 Running); kubectl get httproutes -A / grep mcp → none. list_applications has no server to answer any token. — Consistent with hw239 stamp: openova-mcp is stdio-only (no HTTP/SSE transport, #3988 §5 remaining work; draft chart PR #4981 gates serving behind httpTransport.enabled=false). | hw239 2026-07-11 (#899 draft PR #4981): openova-mcp is stdio-only JSON-RPC (forked child per agent session inside agenity image) — NO HTTP/net.Listen/port. Cannot be a networked Blueprint until it gains an HTTP/SSE serve mode (#3988 §5). Draft chart is the design-ready target (all serving resources gated behind httpTransport.enabled=false). Auth = in-process bearer + app-tier RBAC (not k8s RBAC, not oidc-gate). Row needs the HTTP transport, not just packaging. |
| 212 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP **Org-scoped** token → `list_applications` returns ONLY that Org's apps (RBAC parity). | hw255 | ❌ | hw228 LIVE 2026-07-07: MCP runtime NOT deployed (no MCP pod/route; only sandbox-controller). Org-scoped RBAC untestable without the MCP server. Gated on sandbox-MCP runtime. ‖ hw228 2026-07-07 (code-verified): openova-mcp server (products/openova-mcp, #3988) BUILDS clean + RBAC test suite PASSES (internal/identity + internal/tools — the two-layer list_applications scoping + cross-Org get 403 logic is implemented and unit-verified). NOT deployed on the Sovereign: only sandbox-controller runs (no openova-mcp Service/endpoint). Per its README §95-101 + task #899, the DEPLOY (Blueprint chart + per-Sovereign streamable-HTTP/SSE Service + stable DNS + .mcp.json injection) is the #3988 EPIC's REMAINING work (sequenced 'implement after SME'). Row stays ❌ pending that deploy — logic correct, runtime not on the Sovereign. Refs #3988 ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): Same live gap: no MCP pod/Service/HTTPRoute on hw255 and mcp.hw255.omani.works → 404. Org-scoped RBAC parity untestable — there is no networked MCP server. — RBAC logic is unit-verified in products/openova-mcp per prior stamps, but the row demands the live runtime; still absent on this train. | hw239 2026-07-11 (#899 draft PR #4981): openova-mcp is stdio-only JSON-RPC (forked child per agent session inside agenity image) — NO HTTP/net.Listen/port. Cannot be a networked Blueprint until it gains an HTTP/SSE serve mode (#3988 §5). Draft chart is the design-ready target (all serving resources gated behind httpTransport.enabled=false). Auth = in-process bearer + app-tier RBAC (not k8s RBAC, not oidc-gate). Row needs the HTTP transport, not just packaging. |
| 213 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP cross-Org `get_application` → **403** (UI-parity). | hw255 | ❌ | hw228 LIVE 2026-07-07: MCP runtime NOT deployed (no MCP pod/route). Cross-Org 403 untestable without the MCP server. Gated on sandbox-MCP runtime. ‖ hw228 2026-07-07 (code-verified): openova-mcp server (products/openova-mcp, #3988) BUILDS clean + RBAC test suite PASSES (internal/identity + internal/tools — the two-layer list_applications scoping + cross-Org get 403 logic is implemented and unit-verified). NOT deployed on the Sovereign: only sandbox-controller runs (no openova-mcp Service/endpoint). Per its README §95-101 + task #899, the DEPLOY (Blueprint chart + per-Sovereign streamable-HTTP/SSE Service + stable DNS + .mcp.json injection) is the #3988 EPIC's REMAINING work (sequenced 'implement after SME'). Row stays ❌ pending that deploy — logic correct, runtime not on the Sovereign. Refs #3988 ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): Same live gap: no MCP runtime (curl mcp.hw255.omani.works → 404; only sandbox-controller in ns sandbox). Cross-Org get_application → 403 cannot be exercised. — Gated on #3988 HTTP/SSE transport + Blueprint deploy, as rows 211/212. | hw239 2026-07-11 (#899 draft PR #4981): openova-mcp is stdio-only JSON-RPC (forked child per agent session inside agenity image) — NO HTTP/net.Listen/port. Cannot be a networked Blueprint until it gains an HTTP/SSE serve mode (#3988 §5). Draft chart is the design-ready target (all serving resources gated behind httpTransport.enabled=false). Auth = in-process bearer + app-tier RBAC (not k8s RBAC, not oidc-gate). Row needs the HTTP transport, not just packaging. |
| 214 | orgs | [#3985](https://github.com/openova-io/openova/issues/3985) | No `SME`/`tenant` banned-term leak in the console bundle (org-rename complete). | hw255 | ❌ | CRD/model uses org terms; console bundle banned-term grep authed SPA ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): Deployed SPA bundle grep (curl'd unauthenticated from https://console.hw255.omani.works/assets/index-DqSJ9fgF.js): 'SME' = 0 hits (clean); tenant/Tenant/tenants = 76 hits total, including user-visible copy — tooltip/placeholder 'e.g. tenant=dmz:NoSchedule' on the worker-node taints form — plus testids (sov-console-tenant-*, org-create-tenant-page/form), API surface (X-Tenant-Host header, topTenant, tenantOrg, org_ten… — Row asserts NO tenant leak in the bundle — not met. Nuance for the fixer: most hits are protocol/identifier-level (headers, testids, chart slugs); the rendered-copy leak is the taint placeholder/tooltip and the org-creat… ‖ hw255-fix-2026-07-15: the 3 rendered user-visible tenant strings fixed in PR #5102 (MERGED — WorkerNodeFormFields taint hint/placeholder + org.api.ts error strings → Organization terminology); remaining ~73 bundle hits are protocol keys (X-Tenant-Host, org_tenant_id)/testids/chart slugs, identifier-level by design. ❌ stays until a rolled catalyst-ui image carries ab781aa6c (merged≠deployed). |
| 215 | orgs | [#3985](https://github.com/openova-io/openova/issues/3985) | CRD `tier` enum is `[org, corporate]` — a `tier: org` Organization is accepted. | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): kubectl get crd organizations.orgs.openova.io -o jsonpath='{...tier.enum}' → ["org","corporate"]; live acceptance proof: Organization hw255-omani-works exists with spec.tier=org (kubectl get organizations -o custom-columns TIER → org). — Both halves verified live: enum exact and a tier:org Organization admitted by the API server. |

---

### Governance train — items 187-215 (added 2026-06-21)

These extend the canonical 186-case walk with this session's governance work. **Status is deliberately conservative** (no premature ✅): `⏳` = built/merged in code but **NOT yet validated on the durable live console** (the agents' temporary image rolls were Flux-reverted, so they are not yet what the operator sees — each needs a real end-user walk after the durable roll); `◑` = mechanism proven (e.g. against live HCS) but full in-cluster proof lands on a fresh prov; `❌` = not built yet (fix in flight) or genuinely requires a clean re-prov. A row flips `✅` only after an operator end-user walk on the durable console with pasted evidence, per the TRUST-ledger discipline.
| 216 | e2e-journey | [#3376](https://github.com/openova-io/openova/issues/3376) | A user with a **coupon/voucher code** redeems it → creates **his Organization**. Surface: `https://marketplace.<fqdn>/redeem/?code=<CODE>` (no session) → plan/region → Launch. PASS = the provisioning timeline runs to Done AND `/organizations` shows the Org **ACTIVE** backed by live `vcluster` pods (not a fake-green status). | — | ⚠️ | hw232: acme Org ACTIVE (control-plane) but 0 live pods. hw232 ⚠️ env-transient: mechanism works, blocked by stuck cutover deny-egress hold (egress-block-test Running 45m vs 10m design; #4885 defect-3 12 non-pivoted ghcr.io workloads never reconcile → hold never completes → starves chart/image pulls). Resolves green on clean re-prov post-#4885. |
| 217 | e2e-journey | [#3374](https://github.com/openova-io/openova/issues/3374) | The user logs in via the **passwordless SSO magic-email PIN** (any domain, e.g. `demo@openova.io`) → 6-digit PIN emailed → lands signed-in, no password. PASS = the PIN actually arrives (verify IMAP `mail.openova.io:993`) AND entering it lands signed-in as the CUSTOMER (their email in the avatar, their Org in the header) with no password field — this is the customer PIN flow, NOT the operator handover. | — | ⚠️ | hw232: PIN emails deliver; magic-link/verify path 401 (same SMTP hang). Mechanism ok, env-blocked. hw232 ⚠️ env-transient: mechanism works, blocked by stuck cutover deny-egress hold (egress-block-test Running 45m vs 10m design; #4885 defect-3 12 non-pivoted ghcr.io workloads never reconcile → hold never completes → starves chart/image pulls). Resolves green on clean re-prov post-#4885. |
| 218 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | **chepherd is installable from the catalog as an Application** (`bp-agenity` **Helm chart**). Surface: per-Org console → Catalog → the `bp-agenity` card renders + the install wizard opens with the Org's Environment pre-selected as the target. PASS = card present + wizard opens (the chart resolves from the catalog, not 404). | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Catalog renders the Agenity card ('Your AI engineering team — chat with a built-in solo agent (Claude Opus 4.7)...'); GET /api/v1/catalog/bp-agenity -> 200 version 0.5.20 chartRef ghcr.io/openova-io/bp-bp-agenity:0.5.20 (chart resolves, not 404); '+ New instance' opens the install wizard ('New instance of agenity') with Organization select (hw255.omani.works parent + Omantel or… — Row's own PASS bar (card present + wizard opens + chart resolves) met. Surface = sovereign console catalog; no customer per-Org console is provisioned on hw255 (no tenant Orgs yet). Wizard cancelled —… |
| 219 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4180](https://github.com/openova-io/openova/issues/4180) [#4553](https://github.com/openova-io/openova/issues/4553) | The user **provisions chepherd** as an Application → it converges (Helm deploys, pods Ready, chepherd console reachable in his Org). | hw236 | ⚠️ | hw236: sandbox-controller 1/1 Running 0-restart (#4482); 4-path MCP auto-mount proven in controller code but no Sandbox CR yet -> runtime unproven |
| 220 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4111](https://github.com/openova-io/openova/issues/4111) [#4556](https://github.com/openova-io/openova/issues/4556) | chepherd's **solo agent is pre-configured with Claude Opus 4.7** + a working token (the agent is live and chat works). | hw255 | ❌ | hw236: agenity image bakes openova-sandbox-mcp + claude-code; chart seeds MCP; live token GATED #4277 (no anthropic Secret) ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): The 'pre-configured working token' claim fails live: kubectl get secrets -A / grep -iE 'anthropic/claude' → zero Secrets cluster-wide; no agenity workload deployed anywhere (pods -A grep agenity → none). The #4277 gate (no anthropic Secret) persists on hw255. — Chat-liveness would additionally need the row-219 install mutation, but the pre-configuration half is already disproven read-only — the agent cannot be 'live with a working token' when no token Secret exists. Caveat: che… |
| 221 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | The user **chats with the chepherd solo agent** ("create a `<blueprint>` app in my org") → the agent calls the openova-mcp `create_application` tool with the user's **Org-scoped** token → a new Application CR is created in the user's Org. PASS = the agent reports the new app name + an Application appears provisioning in the user's `/apps`, with NO operator-console action. | hw255 | ❌ | hw228 DIRECT-WALK 2026-07-07: chepherd solo-agent runtime NOT deployed — no chepherd/agentic pods on hw228 (only sandbox-controller). No agent to chat with. ❌ ‖ hw228 2026-07-07 (verified): agentic/chepherd chat runtime NOT deployed — only sandbox-controller runs; the openova-mcp product-surface server (#3988) that the solo agent would drive is built+RBAC-tested but not wired into the sandbox runtime (its Blueprint chart + HTTP/SSE Service + .mcp.json injection is the #3988 EPIC's remaining/deferred work, task #899). Row stays ❌ pending the agentic runtime deploy. Refs #3988 ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): KUBECONFIG=/tmp/hw255.kubeconfig kubectl get pods -A / grep -iE 'agenity/chepherd/openova-mcp/sandbox' -> only 'sandbox sandbox-controller-75cf97bbd5-8hnvs 1/1 Running'; kubectl get svc -A / grep -iE 'mcp/agenity/chepherd' -> empty (exit 1). gh pr view 4981 -> 'OPEN draft=true feat(#899): draft openova-mcp Blueprint packaging'. — chepherd solo-agent runtime NOT deployed on hw255 — no agenity/chepherd/openova-mcp pod or Service; Blueprint bp-agenity 0.5.20 is registered but no runtime. openova-mcp remains stdio-only (no HTTP/SSE transport, #3988 §… | hw239 2026-07-11 (#899 draft PR #4981): openova-mcp is stdio-only JSON-RPC (forked child per agent session inside agenity image) — NO HTTP/net.Listen/port. Cannot be a networked Blueprint until it gains an HTTP/SSE serve mode (#3988 §5). Draft chart is the design-ready target (all serving resources gated behind httpTransport.enabled=false). Auth = in-process bearer + app-tier RBAC (not k8s RBAC, not oidc-gate). Row needs the HTTP transport, not just packaging. |
| 222 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | The agent-created application **converges and appears in the user's Org** (chat-driven app creation works end-to-end). | hw255 | ❌ | hw228 DIRECT-WALK 2026-07-07: chepherd agent runtime NOT deployed (no chepherd/agentic pods) → agent-created app path untestable. ❌ ‖ hw228 2026-07-07 (verified): agentic/chepherd chat runtime NOT deployed — only sandbox-controller runs; the openova-mcp product-surface server (#3988) that the solo agent would drive is built+RBAC-tested but not wired into the sandbox runtime (its Blueprint chart + HTTP/SSE Service + .mcp.json injection is the #3988 EPIC's remaining/deferred work, task #899). Row stays ❌ pending the agentic runtime deploy. Refs #3988 ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): Same live check: zero agenity/chepherd/openova-mcp pods or Services on hw255 (only sandbox-controller 1/1); kubectl get ns shows no customer Org namespace for an agent-created app to land in. — Agent-created-app convergence untestable end-to-end and the precondition (agent runtime) is verifiably absent — fail, consistent with prior ❌. Gated on #3988 §5 HTTP transport + agentic runtime deploy (#899/PR #4981 draf… | hw239 2026-07-11 (#899 draft PR #4981): openova-mcp is stdio-only JSON-RPC (forked child per agent session inside agenity image) — NO HTTP/net.Listen/port. Cannot be a networked Blueprint until it gains an HTTP/SSE serve mode (#3988 §5). Draft chart is the design-ready target (all serving resources gated behind httpTransport.enabled=false). Auth = in-process bearer + app-tier RBAC (not k8s RBAC, not oidc-gate). Row needs the HTTP transport, not just packaging. |
| 223 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4553](https://github.com/openova-io/openova/issues/4553) | The chepherd agent's actions are **RBAC-scoped to the user's Org** (UI-parity): the openova-mcp `list_applications` returns ONLY this Org's apps (a 2nd Org's are filtered out), a cross-Org `get_application` → **403**, and the agent cannot create/mutate in any other Org — enforced by `openova-mcp/internal/identity` Org-claim, not just the chat prompt. | hw255 | ❌ | hw228 DIRECT-WALK 2026-07-07: chepherd agent runtime NOT deployed → RBAC-scoping untestable. ❌ ‖ hw228 2026-07-07 (verified): agentic/chepherd chat runtime NOT deployed — only sandbox-controller runs; the openova-mcp product-surface server (#3988) that the solo agent would drive is built+RBAC-tested but not wired into the sandbox runtime (its Blueprint chart + HTTP/SSE Service + .mcp.json injection is the #3988 EPIC's remaining/deferred work, task #899). Row stays ❌ pending the agentic runtime deploy. Refs #3988 ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): Same live check: no openova-mcp server pod/Service on hw255 (kubectl get pods/svc -A greps empty except sandbox-controller). PR #4981 OPEN draft (stdio-only, in-process bearer auth). — Org-scoped RBAC of the MCP surface cannot be exercised — no networked MCP server exists on the Sovereign. RBAC logic is unit-tested in products/openova-mcp per prior stamps, but the row asserts live enforcement — runtime… | hw239 2026-07-11 (#899 draft PR #4981): openova-mcp is stdio-only JSON-RPC (forked child per agent session inside agenity image) — NO HTTP/net.Listen/port. Cannot be a networked Blueprint until it gains an HTTP/SSE serve mode (#3988 §5). Draft chart is the design-ready target (all serving resources gated behind httpTransport.enabled=false). Auth = in-process bearer + app-tier RBAC (not k8s RBAC, not oidc-gate). Row needs the HTTP transport, not just packaging. |
| 224 | convergence | [#4272](https://github.com/openova-io/openova/issues/4272) | Per-Org `bp-openclaw` controller reaches Ready (1/1) on a Sovereign — the idle-reaper can list its per-user pods and `readyz` passes. | hw239 | ⚠️ | hw228 acme DIRECT-WALK 2026-07-07: no bp-openclaw pod/HR in ns acme (kubectl) — acme's vcluster runs only bp-postgres + WordPress. openclaw not installed. Needs day-2 app install into the Org. ❌ ‖ FLIP-ATTEMPT 2026-07-07: tried day-2 install into acme (working vcluster Org) via POST /api/v1/org/applications (owner session, X-Tenant-Host: console.acme.omani.homes) — endpoint works (got to blueprintRef.version validation) but bp-openclaw/bp-stalwart-tenant are NOT in the customer marketplace catalog (which offers ghost/cal-com/dify/... as customer day-2 apps); openclaw/mail are PLATFORM per-Org apps installed as part of the funnel stack (walkstranger* had bp-openclaw + bp-stalwart-tenant HRs). So they can't be day-2-installed — gated on the per-Org platform stack CONVERGING, blocked by #4813 (cnpg dies in host-ns Orgs). Refs #4813 ‖ hw228 2026-07-07 CORRECTED (live kubectl): bp-openclaw@0.2.13 + bp-stalwart-tenant@0.1.12 ARE registered (prior 'not in catalog' evidence was WRONG); the day-2 raw Application-CR path fails `namespaces "org-example" not found` because it doesn't inject tenant.namespace. Real blocker: NO Org on hw228 has the full funnel app bundle (keycloak/newapi/openclaw/stalwart) — acme is an M-plan vcluster that only got WordPress day-2. The canonical orgTenantBPOpenClaw overlay is CORRECT (sets tenant.namespace={{.Namespace}} + dependsOn bp-keycloak) and applies to S-plan host-native funnel Orgs. Fresh S-plan funnel-Org walk (slug walk-stranger) dispatched to verify. Refs #4272 #4307 ‖ hw228 walk-stranger S-plan 2026-07-07 ROOT CAUSE: openclaw controller 0/1, pod quota-DENIED ('exceeded quota: plan-quota, requested cpu=500m, used=1600m, limited=2') — bp-stalwart-tenant requests a guaranteed 1 CPU core (50% of the S-plan 2-vCPU). The #4272 openclaw render itself is CORRECT (Deployment image openclaw-controller:7ade865, OIDC issuer shared-realm, HTTPRoute openclaw.walk-stranger created). Fix shipped: PR #4834 (#4833) right-sizes stalwart CPU 1→500m; row flips once 0.1.13 rolls + openclaw schedules. Refs #4272 #4833 ‖ hw228 2026-07-07 CONFIRMED blocker = #4834 quota (NOT Kyverno): walk-stranger openclaw ReplicaSet FailedCreate 'exceeded quota: plan-quota, requested cpu=500m, used=1600m, limited=2' (stalwart hogs 1 core). The 3 Kyverno PolicyViolation events (scrape/readonlyrootfs/multi-replica) are validationFailureAction=Audit — warnings, not the blocker (wordpress/stalwart converged with the same). #4834+#4835 (stalwart 1→500m) merged; flips on next fresh prov. ‖ **LIVE-CONFIRMED 2026-07-07 on hw228 walk-stranger Org (kubectl):** the openclaw controller ReplicaSet is quota-denied — event `FailedCreate ... pods openclaw-bp-openclaw-controller-... is forbidden: exceeded quota: plan-quota, requested requests.cpu=500m, used 1600m, limited 2` (bp-openclaw HR = 'Helm install failed ... context deadline exceeded'). Quota breakdown: mysql 250m + **stalwart-tenant StatefulSet 1000m (1 core — OLD chart 0.1.12, PRE-#4834)** + webmail 100m + wordpress 100m ≈ 1600m of the 2-core S-plan quota → openclaw's 500m can't fit. This is EXACTLY #4834: the merged fix (stalwart 1→500m, chart 0.1.13) frees precisely 500m (1600→1100, +500=1600<2000) so openclaw schedules. walk-stranger carries the pre-fix chart; NO in-place fix (bp-stalwart StatefulSet upgrade fails + Kustomization reverts live patches — reference_stalwart_tenant_no_inplace_upgrade). Flips ✅ on a fresh prov / chart-roll that ships stalwart 0.1.13. The blocker is verified, not speculative. | hw239: #4953 bp-openclaw namespace fix LIVE (Blueprint bp-openclaw 0.2.17 registered); product-fault resolved — customer-Org-gated (no funnel Org to install into) |
| 225 | convergence | [#4278](https://github.com/openova-io/openova/issues/4278) | Per-Org `bp-newapi` HR reaches Ready on a fresh Org — the admin-promote post-install hook Completes (its ServiceAccount exists) so the chart finishes. | hw239 | ⚠️ | platform newapi admin-promote Job Complete + Application Ready; per-Org ES needs funnel Org | hw239: platform bp-newapi HR Ready + admin-promote Job Complete + SA present (hook proven platform-side); per-Org fresh-Org instance needs a funnel Org |
| 226 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The customer's PURCHASED app actually RUNS — every funnel Org's `tenant-<slug>-apps` Kustomization reconciles into a running pod (the app is reachable, not 404). | — | ⚠️ | hw232: 7 acme tenant HelmReleases created (real reconcile) but 0 pods (chart-pull blocked). hw232 ⚠️ env-transient: mechanism works, blocked by stuck cutover deny-egress hold (egress-block-test Running 45m vs 10m design; #4885 defect-3 12 non-pivoted ghcr.io workloads never reconcile → hold never completes → starves chart/image pulls). Resolves green on clean re-prov post-#4885. |

---

### Fresh-prov 2026-06-26 — session-fix acceptance (rows 227–239)

These 13 rows are the acceptance checks for the issues genuinely resolved this session. Each pairs the **root cause** with the **fresh-prov surface to walk** + the **expected evidence**. All start `☐` — the fresh-prov walk fills the verdict + evidence cells in. (Refs #4279.)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| 227 | delivery | [#4415](https://github.com/openova-io/openova/issues/4415) | A catalog/seed version bump reaches the live **Blueprint CR zero-touch** — bumping a `bp-*` pin in the catalog seed lands on the cluster Blueprint CR with NO manual `kubectl patch` and NO `resource-policy: keep` annotation pinning a stale version. PASS = after a seed bump, `kubectl get blueprint bp-<x> -o jsonpath='{.spec.version}'` matches the seeded version within one reconcile, the seeder owns the field (no `helm.sh/resource-policy: keep`), and the rolled chart renders on the cluster. | hw239 | ⚠️ | 81 Blueprint CRs + catalog-sovereign GitRepo Ready; bump-propagation not exercised |
| 228 | delivery | [#4435](https://github.com/openova-io/openova/issues/4435) | A **re-prov AFTER a wipe** does NOT false-fail on orphaned `catalyst-*` VPCs — the orphan-VPC janitor sweep removes the leftover VPC(s) from a prior wiped deployment before the new prov allocates its network. PASS = wipe an env, immediately re-prov on the same region; provisioning does NOT error `VPC already exists` / quota-conflict; the janitor log shows the orphaned `catalyst-*` VPC(s) swept, and the new prov reaches Phase-1 green. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: re-prov-after-wipe orphan-VPC handling = mothership/janitor concern; R1/#4454 protect-side proven (janitor did NOT reap ready hw224 after 3h42m)‖ §F: prov green under cap; orphan-sweep code present (42 VPC/21 keypair evals); destructive sweep not exercised (no orphans). Protect proven in #4454 ‖ 2026-07-09 VERIFY-THEN-FIX (#4272 fix-agent, orchestrator-noted): the orphan-VPC reclaim is FULLY IMPLEMENTED, not a gap — provisioner.go:1986 runs a Huawei VPC-quota pre-flight before every prov → VPCReclaimPreflight → huawei.SweepOrphanVPCs(destructive=true), with #4614 protection (reclaimProtectSet protects EVERY live deployment's prefix) + regression tests (reclaim_protect_4614_test.go, sweep_orphan_vpc_test.go). LIVE kom4dc: project f27698... holds exactly 2 catalyst VPCs (hw228 a+b, both skipped as the active deployment), WOULD-REAP orphans = 0, quota 2/5. No code change warranted (no PR — shipping one would be theater). Stays ⚠️ — end-to-end wipe→re-prov no-false-fail proves on the fresh-prov endgame. NOTE: the endgame prov is VPC-quota-CLEAR (wiping hw228 frees its pair → clean 2-region headroom). |
| 229 | delivery | [#4409](https://github.com/openova-io/openova/issues/4409) [#4428](https://github.com/openova-io/openova/issues/4428) | **bootstrap-kit Kustomization Ready=True + plane-isolation admits CNPG→kube-apiserver egress** — on the fresh prov `kubectl -n flux-system get kustomization bootstrap-kit` is `Ready=True` (no orphaned `dependsOn` / CRD dry-run rejection), AND the plane-isolation NetworkPolicy permits the CNPG operator's egress to the kube-apiserver so Cluster admission/patch succeeds. PASS = bootstrap-kit `Ready=True`; CNPG operator pod Running; a `Cluster` reconciles (no `context deadline exceeded` / x509 webhook failure caused by blocked apiserver egress). | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): KUBECONFIG=/tmp/hw255.kubeconfig kubectl -n flux-system get kustomization bootstrap-kit -> 'True  Applied revision: main@sha1:bec855fb'; kubectl get pods -n cnpg-system -> cnpg-cloudnative-pg-f5b4d9969-rq8b2 1/1 Running 0 restarts (59m); kubectl get clusters.postgresql.cnpg.io -A -> 9 Clusters, 8 'Cluster in healthy state' (gitea-pg 2/2, harbor-pg 2/2, pdns-pg 2/2, org-pg 2/2, newapi-pg 2/2, openova-flow-pg 2/2, guac… — bootstrap-kit Ready=True on the fresh prov; CNPG operator Running with zero restarts; multiple Clusters reconciled to healthy — no context-deadline/x509 webhook failures, proving plane-isolation admits CNPG->apiserver eg… |
| 230 | delivery | [#4427](https://github.com/openova-io/openova/issues/4427) | **`ghcr-pull` reflects into a bare-slug per-Org namespace** — the emberstack reflector copies the `ghcr-pull` imagePullSecret into a per-Org namespace whose name is a bare slug (e.g. `org-<slug>` / a non-UUID form), so private `ghcr.io/openova-io/*` app images pull (no `ImagePullBackOff` 401-anonymous). PASS = a fresh Org's namespace has the reflected `ghcr-pull` secret; a per-Org app pod pulling a private image reaches Running (no 401/ImagePullBackOff). | — | ⚠️ | hw232: ghcr-pull + harbor-robot-token SECRETS reflected into acme ns ✓; no pod yet to prove pull (blocked). hw232 ⚠️ env-transient: mechanism works, blocked by stuck cutover deny-egress hold (egress-block-test Running 45m vs 10m design; #4885 defect-3 12 non-pivoted ghcr.io workloads never reconcile → hold never completes → starves chart/image pulls). Resolves green on clean re-prov post-#4885. |
| 231 | delivery | [#4411](https://github.com/openova-io/openova/issues/4411) | **bp-postgres singleton render carries the operator-probe NetworkPolicy** — a singleton (non-HA) bp-postgres render still ships the NetworkPolicy that admits the CNPG operator's health/readiness probe, so a singleton Postgres reaches Ready (the probe is not blocked). PASS = a singleton bp-postgres Application reaches `Ready=True`; the operator-probe NetworkPolicy is present in the rendered manifest; the CNPG operator can probe the instance. | hw240 | ⚠️ | hw240 cleanup-walker(handover+kubectl): singleton CNPG guacamole-pg (INSTANCES=1) reaches Ready (operator probes it successfully). No standalone bp-postgres singleton Application on this prov to inspect the rendered operator-probe NetworkPolicy literally — would need a singleton bp-postgres Provision (write). |
| 232 | apps | [#4272](https://github.com/openova-io/openova/issues/4272) | On a fresh funnel Org, **openclaw `/readyz` returns 200** — the 4-layer chain (controller image · reaper apiserver egress NetworkPolicy · TLS trust · OIDC) all resolve so the idle-reaper lists per-user pods and readiness passes. PASS = `bp-openclaw` HR `Ready=True`, controller pod 1/1 Running, and `curl https://<openclaw-host>/readyz` → 200 (no `reaper: list pods … context deadline exceeded` loop). | hw239 | ⚠️ | RE-PROV-GATED: needs converged customer Org (funnel) on cutover-complete env. Trigger: deploybot umbrella roll (bp-catalyst-platform + cutover 0.1.107, all fixes) -> wipe hw232 -> clean re-prov -> walk ‖ hw235-conclusion-2026-07-10: per-Org OpenClaw does NOT come up — bp-openclaw install fails 'namespaces "org-example" not found' (hardcoded placeholder namespace), controller never Ready so /readyz cannot 200. Filed #4952, fix PR #4953, roll-gated to next prov. Refs #4272 #4952 #4953 | hw239: #4953 bp-openclaw 0.2.17 registered (tenant.namespace default-empty fix); /readyz 200 needs a per-Org install — customer-Org-gated, product-fault resolved |
| 233 | apps | [#4422](https://github.com/openova-io/openova/issues/4422) | On a fresh funnel Org, **WordPress serves HTTP 200** — the mysql DB pod is Guaranteed-QoS (requests==limits, schedulable under the per-Org LimitRange), the HTTPRoute is present, and the landed host is pool-aligned to the console-ELB EIP. PASS = `curl https://wordpress.<slug>.<pool-tld>/` → 200 (or 302→install then 200), mysql pod QoS=Guaranteed + Running, HTTPRoute parents the console gateway, host resolves to the console-ELB EIP (not the primary ELB). | hw239 | ⚠️ | hw239: #4966 provisioning fix LIVE (services-provisioning:280aa83); WordPress-200 walk needs a funnel Org — customer-Org-gated, product-fault resolved |
| 234 | apps | [#4307](https://github.com/openova-io/openova/issues/4307) | On a funnel Org **that provisioned Stalwart mail** (cart includes `stalwart-mail`, or a mail-bearing bundle e.g. pharmacy/legal/finance), `mail.<slug>.<pool-tld>/` serves HTTP 200 (SnappyMail SPA); `bp-stalwart-tenant` pod Running; webmail HTTPRoute present (no bare envoy 404). | hw240 | ☐ | no per-Org mail app on internal-only prov; needs funnel Org | RE-AUTHORED (per #4307 scope investigation): per-Org mail is OPT-IN, NOT a universal funnel deliverable — DoD.md:533 mail is a bundle choice; catalog seeds stalwart-mail as a selectable app; Sovereign funnel renders ONLY selected cart apps (#4364, provisioning resolveAppSlugs = pure cart→slug). Opt-in wiring complete + NodePort-free (generateStalwartHR → mail.<slug> HTTPRoute on host cilium-gateway-console, ClusterIP SnappyMail, chart 0.1.13 lockstep; #4307 webmail-404 fix merged+CLOSED). A fresh Org that did not select mail correctly has none — not a defect. ☐ pending a walk against a mail-bearing cart on hw241. |
| 235 | apps | [#4437](https://github.com/openova-io/openova/issues/4437) | On a fresh prov, **grafana SSO logs in 3/3 → 200** — the bp-sso-bridge per-tick credential refresh keeps the grafana OIDC client secret in lockstep with Keycloak, so the SSO round-trip completes. PASS = grafana `/login/generic_oauth` → Keycloak → back signed-in (200, user in the avatar) across 3 consecutive attempts; bp-sso-bridge refreshes the client cred each tick (no stale-secret 401). | hw255 | ✅ | hw236: grafana 3/3 both regions + grafana-sso-oidc-credentials mounted + sso-bridge-reconciler 1/1 (#4437); SSO 200 round-trip browser/curl-deferred ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): hw255-2026-07-15 (W2-3 authed browser+kubectl): grafana SSO 3/3 — drove https://grafana.hw255.omani.works/login/generic_oauth three consecutive times; each round-trip (grafana→KC authorize→back) landed grafana home 200 with GET /api/user → 200 login=emrah.baysal@openova.io authLabels=['Generic OAuth'], zero login_error/stale-secret 401. Backing: sso-bridge-reconciler-7757d9dc4d… |
| 236 | apps | [#4439](https://github.com/openova-io/openova/issues/4439) | On a fresh multi-region prov, **region-B keycloak reaches Ready** — the cross-region shared-pg write host is correct so region-B Keycloak connects to the primary Postgres and completes realm import. PASS = `bp-keycloak` HR `Ready=True` in the region-B cluster; keycloak pod Running; realm import succeeds (no DB-connect / wrong-write-host failure); `keycloak.<host>` → 302 SSO. | hw255 | ✅ | hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Region-B cluster (5762118f63abef96-me-east-215-b-1 via console k8s cache): bp-keycloak HR Ready=True ('Helm install succeeded for release keycloak/keycloak.v1 with chart bp-keycloak@1.5.5'); keycloak-0 pod Running, ready=True, restarts=0 (started 04:50:33Z); realm import succeeded — GET https://auth.hw255.omani.works/realms/sovereign/.well-known/openid-configuration -> 200; GET… — Keycloak HTTPRoute host is auth.<fqdn> (keycloak.<fqdn> is not routed, returns envoy 404). |
| 237 | spine | [#4416](https://github.com/openova-io/openova/issues/4416) | **spine → Continuum round-trip is Healthy + `continuumRef` is populated** — the spine adopts the Continuum and the round-trip reports Healthy. PASS = the Continuum CR reports `Healthy` (not Degraded), the spine's `continuumRef` field is populated with the live Continuum, and a witness round-trip completes. | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): kubectl get continuums.dr.openova.io -A -> 5 CRs ALL Phase=Healthy: dr-spine-gitea/dr-spine-harbor/dr-spine-keycloak/dr-spine-openbao (PRIMARY=me-east-215-a, LEASE=me-east-215-a, 37m) + cnpg-pair-bp-cnpg-pair-continuum Healthy. kubectl get application -n catalyst spine-gitea -o yaml -> 'continuumRef: catalyst/dr-spine-gitea'; all 4 spine-* Applications (bp-gitea/bp-harbor/bp-keycloak/bp-openbao, active-hot-standby / … — Spine->Continuum round-trip proven live on the fresh prov: Continuum CRs Healthy (not Degraded), spine Applications Ready with continuumRef populated, lease witnessed (LEASE column = live holder). Note: 'kubectl get cont… |
| 238 | postgres | [#4282](https://github.com/openova-io/openova/issues/4282) | **Per-Org postgres-in-vCluster → host-side CNPG Ready** — a per-Org Postgres requested inside the Org's vCluster is reconciled by the host-side CNPG operator and reaches Ready. PASS = the per-Org `Cluster` CR is `Ready` (host-side CNPG drives it), the Postgres pod is Running with Guaranteed QoS, and the per-Org app connects (no empty-DSN / disableWait deadlock). | hw239 | ⚠️ | hw239: host-side CNPG operator healthy driving 8 platform Clusters Ready incl singleton guacamole-pg; per-Org-in-vCluster needs a customer Org (#4325 per-Org ns). Mechanism unblocked |
| 239 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | **Crossplane adoption populates Observe-only on the fresh prov** — `providers.pkg.crossplane.io` and `cloudadoptions.compose.openova.io` are non-empty and reach Synced+Ready, observing (NOT re-provisioning) the OpenTofu-built infra. PASS = `kubectl get providers.pkg.crossplane.io` → ≥1 Healthy; `kubectl get cloudadoptions.compose.openova.io -A` → non-empty Synced+Ready with `external-name` = the real ELB id; the live ELB/nodes are untouched (Observe-only). | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): kubectl get providers.pkg.crossplane.io -> provider-opentofu INSTALLED=True HEALTHY=True (xpkg.upbound.io/upbound/provider-opentofu:v1.1.3); kubectl get cloudadoptions.compose.openova.io -A -> 26 CRs: 25 real adoptions ALL SYNCED=True READY=True ADOPTED=true with real external ids incl. adopt-loadbalancer-primary RESOURCE-ID=9b2addcd-1a9e-4ac3-b7f4-d620106def08 and adopt-loadbalancer-console-0 RESOURCE-ID=d1f8cc16-87… — All three PASS legs met: >=1 Healthy provider, non-empty cloudadoptions Synced+Ready with external-name = the real ELB ids, Observe-only (adopted=true, infra undisturbed). The single 'adoption-placeholder' CR is Ready=Fa… |
| 240 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | No-nodePort gateway serves externally on a FRESH zero-touch prov: ELB → node:443 hostPort → hostNetwork envoy; gateway Service carries ZERO nodePorts (§854 literal). | hw255 | ✅ | mothership-2026-07-14: live §854 svc audit (`kubectl get svc -A` type=NodePort filter) — Catalyst gateway path clean, but 4 NodePort svcs on the mothership: `openova-system/powerdns-anycast` (LB w/ allocated nodePorts, stale pre-1.2.18 deploy — heals on next chart roll) + `powerdns-api-ext` (30853, openova-private-owned legacy) → evidence on [#4765](https://github.com/openova-io/openova/issues/4765); `cinova/*`+`iogrid/*` legacy-SME never-touch; `cm-acme-http-solver-*` transient ACME. Durable: PR #5080 kills the Hetzner LB→node:30053/32379 wiring + adds guard Phase-1b (infra HCL nodePort literals). Row stays ☐ — its gate is the FRESH-prov gateway check (hw252 post-prov; train held for full assembly). ‖ hw255-2026-07-15 (fleet-walk wf_727c62bf): Full svc audit via kubectl get svc -A -o json (169 Services): ZERO services carry any nodePort allocation; gateway Services are ClusterIP (kube-system/cilium-gateway-cilium-gateway 80/443 ClusterIP, cilium-gateway-cilium-gateway-console 8080/8443 ClusterIP); cilium-envoy DaemonSet hostNetwork=true. External serve proven: curl https://console.hw255.omani.works/ -> 200, marketplace -> 200, grafana wildcard -> 307 (envo… — Caveats recorded honestly: (1) at briefing time console/marketplace were 000 with a parallel RCA agent active; recovery correlates with the chart-Job 'cilium-envoy-tls-restart' Completed + envoy DS re-roll ~45m ago — I c… |
| 241 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | Mothership writes `ready` ONLY after an external HTTP answer from https://console.<fqdn>/ (no more false-green). | hw255 | ✅ | hw224-fulltable-2026-07-04: mothership-side readiness gate (#4707); hw224 reached ready + console 200 external. Mothership concern ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): Gate code live in mothership main: products/catalyst/bootstrap/api/internal/handler/phase1_watch.go:777-828 (#4706 — repeatedly GETs https://console.<fqdn>/ before ready; EnableConsoleReachabilityGate; comment cites the 2026-07-03 404-vhost hole). Mothership state consistent on hw255: read-only kubectl (ctx default, 45.151.123.50) catalyst-api log 08:53:32Z 'clustermesh: steady… — Historical ready-flip log rotated (mothership catalyst-api pod restarted ~08:53Z); verdict grounded on gate code at exact lines + ready-state + external 200 — same evidence class as the hw224 stamp. M… |
| 242 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | 2-region BCP admission floor: a 1-region POST with no explicit bcpTopology is rejected at the LIVE production API. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: 2-region BCP admission floor (#4708) — mothership POST-time gate; hw224 IS 2-region (passed the floor). Mothership concern ‖ 2026-07-03 negative wire-test vs console.openova.io: `POST /sovereign/api/v1/deployments` (1 region, no topology) → **HTTP 400** "deployment has 1 region(s) and no explicit bcpTopology — the BCP agreement default is multi-region (>=2 regions, Pillar 2)…". Nothing created. ‖ hw228 2026-07-09 LIVE PARTIAL (production API console.openova.io, founder-token auth confirmed GET /sovereign/api/v1/deployments = HTTP 200): every 1-region POST is REJECTED with HTTP 400 (validated bodies hit SKU/hetzner-token gates that run before the region-count check). The #4706 BCP floor itself (provisioner.go:1076-1086 'bcpTopology=%q requires len(regions)>=2 ... Pillar 3 zero-transactions-lost cannot be honoured single-region') is in the DEPLOYED image + unit-tested (provisioner_test.go, dr_standby_integrity_test.go). Isolating the exact floor-line rejection LIVE needs a fully-credentialed body (real Hetzner token + orderable SKUs) which risks a real provision — NOT run. Stays ⚠️: production API live+validates+rejects-1-region proven; exact-floor live-hit is the remaining bar. ‖ hw255-wave2-2026-07-15 (authed owner walk wf_dcd83f4f): The live proof requires POST https://console.openova.io/sovereign/api/v1/deployments with a 1-region/no-bcpTopology body — a create-attempt WRITE against the production mothership API, not performed by this read-only walker. Read-only context gathered: mothership is reachable again (GET https://console.openova.io/ -> HTTP 302, 1.06s). — Exact deferred write: 'POST /sovereign/api/v1/deployments (1-region negative admission test)'. Prior evidence stands: 2026-07-03 negative wire-test -> HTTP 400 'deployment has 1 region(s) and no expli… |
| 243 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | Tenant DNS split-horizon on a fresh prov: wildcard hosts → primary ELB EIP, console → console ELB EIP. | hw255 | ✅ | hw255-2026-07-15 (fleet-walk wf_727c62bf): dig +short: console.hw255.omani.works -> 212.72.24.31, marketplace.hw255.omani.works -> 212.72.24.31 (console-ELB EIP); grafana/keycloak/randomwildcard.hw255.omani.works -> 212.72.24.1 (primary-ELB EIP). Both horizons SERVE: console 200 + marketplace 200 on .31; grafana 307->keycloak-auth and randomwildcard 404-from-envoy on .1. — Split-horizon proven live on the fresh prov: wildcard hosts resolve to one EIP, console/marketplace to a distinct second EIP, and each EIP terminates the correct workloads (console SPA vs gateway wildcard listener). EIP-… |
