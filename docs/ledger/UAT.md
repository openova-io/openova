# UAT — Sovereign acceptance walk (consolidated 2026-06-27 — 22 issues closed live + honest gating map) — live (RESET 2026-07-09 — pending **hw231** dep `ab1355c25f9798c3`, converging: region-a 31/63 + region-b 30/63 HRs on train 1.4.1038 w/ #4869 row-67 + #4870 cutover-ack; prior hw228 wiped, hw229 failed [OBS-quota #4872], hw230 failed [CIDR-overflow, fixed PR #4871]. Prior test provs hw220/hw224/hw228 are all DEAD — curl HTTP 000 + Playwright 60s timeout, 2026-07-09T01:36Z — their historical evidence cells below are stale prior-env history, not current.)

> **hw224 VERIFICATION PROV — 2026-07-04 (dep `964e9ca501951f54`, hw224.omani.works, 2-region me-east-215-a/b, LE-prod cert).** Fresh prov on the train carrying the two deepest fixes from the hw223 walk: **#4756** (provisioning-token outage) + **#4758** (vcluster-LimitRange brick). Fixes confirmed DEPLOYED + partially verified live by the orchestrator; the full authenticated funnel walk (customer WordPress serving in-vcluster) is in flight (walker `a2a68a8ce9ec0b96b`).
> - ✅ **#4756 provisioning-outage fix — VERIFIED LIVE**: `org-services/provisioning` init went `1/1 Running` at **5m52s with 0 restarts** (vs the old ~30-min self-heal outage that recovered only at the 1800s init-timeout restart). The init now reads GITHUB_TOKEN from the kubelet-synced FILE each poll (`GITHUB_TOKEN_FILE` env + `provisioning-github-token` volume both present on the running pod), so the late-minted gitea token is picked up in ~60s.
> - ✅ **#4758 vcluster-LimitRange fix — CONFIRMED WORKING** (authenticated funnel walk, customer Org `uatwp4758`, plan m, isolation=vcluster): `plan-limits` LimitRange has **NO `maxLimitRequestRatio`** (only default/defaultRequest), and the vcluster's synced `coredns-…-x-kube-system-x-vcluster` pod — the exact pod the old ratio=1 rejected — is now **admitted + scheduled + Running** in the host ns. The admission-rejection #4758 targeted is resolved. (org-controller `organization-controller:99cab49`.)
> - ❌ **WordPress last-mile still 404** — but by TWO NEW defects distinct from #4758: (1) **PRIMARY [#4656]** the customer vcluster's apiserver is unreachable cross-node (`dial tcp 10.96.80.71:443: i/o timeout`) — Cilium VXLAN-over-WireGuard MTU stacking (measured live: cilium_host 1370 + VXLAN 50 = 1420 > cilium_wg0 1290 → DF-drop); coredns stays 0/1, WordPress never applies into the vcluster. (2) **secondary** `core/services/provisioning/gitops/gitops.go:800-803` hardcodes `sourceRef: flux-system` which doesn't exist on a Sovereign → the generated `tenant-<slug>-apps` Kustomization is FALSE (`GitRepository "flux-system" not found`) — a broken duplicate of the org-controller's `catalyst-tenant-<slug>-apps`.
> - ✅ **Pillars 2/3, funnel E2E, PIN login** re-confirmed (BCP active-hot-standby picker; shared-pg/-b/-c + org-pg healthy; full funnel plans→checkout→launch with voucher `VCH-QFP52J4S2KEX`; `/auth/pin/verify`→200). Env converged (console 200, region-a 63/63 HRs; region-b `secondaryDegraded=true` — honest).
> - ✅ **COMPREHENSIVE authenticated UAT walk — 26 ✅ · 0 ❌ · 3 ⛔ · 4 ⚠️, NO new bugs, NO fabricated-green** (walker `a453392c2a1f7a514`, 12 screenshots `.playwright-mcp/uat-hw224-01..12-*.png`). Confirmed live at HTTP/kubectl/wire layer: **SSO** (per-app launch-url→real OIDC; gitea 307→`realms/sovereign`, grafana 302 w/ `kc_idp_hint=catalyst-pin` silent-SSO); **Topology/DR** (per-app placement from runtime, active-active, Region 2/2, 24/24 workers); **Jobs lens #3646** (Kind col task/cron/step/lifecycle + Re-run on a *Failed* mimir-buckets job — genuinely real, not all-green); **Catalog** (81 blueprints, bp-wordpress detail); **Orgs** (directory lists customer `uatwp4758`, per-Org detail, **showback 525.5 units real-metered ≠ 0**, `console.uatwp4758.omani.homes`→200 w/ own LE cert); **Treemap** (206-node live util; **#4731 Progress/Kind layers VERIFIED** — L1 progress buckets Reconcilers/Cutover/Provision-Huawei, L2 Kind Task/Cron/Step/Lifecycle, status-color legend, job-tree source `/jobs`→200, leaf→JobDetail+logs, no regression to org/utilization default; `.playwright-mcp/4731-hw224-0[1-4]`); **Robustness #3380** (Degraded badge honestly propagated everywhere — no false-green; root = `clustermesh-apiserver` Init:0/1 both regions, #4656 family); **Cutover** (Tethered, 11 steps Pending, cutoverComplete=false); **Users** (3 real UserAccess CRs); **Agenity** (6-agent picker). 3 ⛔ = pre-existing non-product gates (headless-KC silent-SSO landing, Agenity live-run Anthropic cred #4277, uatwp4758 vcluster 0/0 #4656). 4 ⚠️ minor (HR-only DR-switchover panel absent; catalog IaC write-gated on live env; vcluster-showback in graph; ~~"Sandbox"→"Agenity" nav leak~~ **FIXED #4762**).
> - ✅ **FULL-TABLE walk — all 243 canonical rows populated on hw224: `101 ✅ · 11 ❌ · 26 ⛔ · 105 ⚠️ · 0 ☐`** (from 0✅/122☐ at reset; every row a live hw224 verdict, **0 fabricated passes**, 17 screenshots `uat-hw224-fulltable-*`, commits 15c5332d3/9b2d3898f/73a65b568). Silent-SSO brokered from the console session unlocked app-landings session-walled ⛔ on prior envs. **Flipped ❌/⛔→✅ this env**: Harbor SSO wired (was ❌412 db_auth), Grafana 3/3 + silent-SSO (was ❌503 CrashLoop), all app SSO landings live (gitea/openbao/guacamole/pdns/newapi/hubble/KC-admin), customer Org uatwp4758 real (525.5-unit showback). **11 ❌ = all previously-filed knowns, NO new bugs**: #4656 (WordPress in-vcluster, 7 rows), F8 topology placement-from-pod-only, row-23 directory-showback, #4620 crossplane adoption. **105 ⚠️** = region-b Degraded (clustermesh-apiserver Init:0/1, #4656 family) + write-mutation rows (durable-commit proofs stand). **26 ⛔** = honest walls (headless-KC session, Anthropic-cred Agenity chat #4277, destructive cutover, write-gated funnel). Path to higher ✅: fix #4656 (unblocks WordPress + region-b) + a throwaway write-mutation walk.
> **Provenance:** `hw224-verify-2026-07-04` per surface (walker `a2a68a8ce9ec0b96b`, 6 screenshots `.playwright-mcp/4758-0[1-6]-*.png`). Net: the two deepest fixes shipped this session (#4756, #4758) are VERIFIED; the WordPress headline is now gated on #4656 (MTU) + the gitops.go sourceRef bug — both filed, both distinct from the verified fixes. hw223 (below) is prior-env history — wiped 2026-07-04 (STALE by construction; each new env flushes evidence).

> **hw223 AUTHENTICATED PILLAR WALK — 2026-07-04 (dep `9224ebd66e0602b4`, hw223.omani.works, real LE-prod cert, FIRST reliably-converged prov after the walk-blocker fix-trio #4748+#4746+#4752).** The whole prior round was auth-gated by broken operator login; hw223 cleared it. **~13 ✅ · 0 ❌ · 0 ⛔ · 3 ⚠️**, + 1 real bug (#4756) + 2 findings. All verdicts reproduced live at the HTTP/kubectl/CR layer — no fabricated passes (12 screenshots under `.playwright-mcp/uat-hw223-*.png`).
> - ✅ **Operator PIN-login end-to-end** (#4748, the prior blocker): `pin/issue`→200 `dispatched` → mailbox PIN → `pin/verify`→200 + RS256 `catalyst_session` cookie (roles catalyst-admin/owner) → authed console renders. (hw221 was 502; fix validated live.)
> - ✅ **Pillar 1 — customer-Org onboarding walked E2E** (first customer-KIND Org on any prov this cycle): funnel plans→apps→addons→bcp→review→checkout; operator issues voucher `UATWALK223VOUCHER` (BSS UI); redeem fully covers OMR 9 → Org CR `uatwalk223` (kind=customer, plan=m, billing=real, isolation=vcluster) Ready in ~2 min → **`console.uatwalk223.omani.homes` → HTTP 200** on its own Org host.
> - ✅ **Pillar 2/3** — cloud graph Region 2/2 (me-east-215-a+b both healthy), 24/24 workers, LB 2/2; Continuum DR CRs (gitea/harbor/keycloak) phase Healthy + LeaseHeld; 9 CNPG clusters healthy (shared-pg/-b/-c Guaranteed + DR pair). Signup BCP offers active-hot-standby.
> - ✅ **W-2** per-Org `plan-limits` LimitRange forces Guaranteed QoS + `plan-quota` present; **W-3** per-Org agenity org-host routing proven (Org console on the Org host, not Sovereign FQDN) + `OPENOVA_MCP_TENANT_HOST` fix in source; **W-11** Organizations directory shows the new customer Org.
> - ✅ **Pillar 5** cutover dormant (`bp-self-sovereign-cutover@0.1.103`, cutoverComplete=false, 11 steps un-started; Settings→Sovereignty "Tethered").
> - 🐞 **BUG #4756** — `org-services/provisioning` init reads GITHUB_TOKEN via secretKeyRef env (empty at start), can't see the late-minted gitea token → ~30-min self-heal outage (recovers at 1800s init-timeout restart). Findings: funnel checkout showed `.omani.rest` but provisioning used `omani.homes` (redirect TLD mismatch); a duplicate `tenant-uatwalk223-apps` Kustomization is broken (GitRepository not found); funnel-picked WordPress still converging in-vcluster at cutoff. (⚠️ = per-Org keycloak realm tier-gated + WordPress converging + showback 0-units-metered.)
> **Provenance:** `hw223-walk-2026-07-04` per surface; the three walk-blocker fixes (#4748/#4746/#4752) all validated live on this prov. Prior hw221/hw220 blocks below are prior-env history (STALE by construction — each new env flushes evidence).

> **hw220 FULL-TABLE walk — 2026-07-04 (dep `907629bc3577355b`, hw220.omani.works, 2-region converged, complete #4706+#4732 train live).** ALL 243 canonical rows walked by two parallel read-only walkers (rows 1–120 + 121–239, minted-owner + org-scoped sessions, wire evidence per row): **122 ✅ · 21 ❌ · 37 ⛔ · 63 ⚠️ · 0 ☐**. The 21 ❌ consolidate into the #4739 fault wave (headline: hourly external :443 outage loop from the TTL'd envoy-restart Job — live-mitigated + durable fix PR'd; per-Org LimitRange wedge blocking every funnel app; harbor SSO db_auth never flipped; grafana region-a credential CrashLoop; 1.4.993 invalid env render; adoption ProviderConfig group mismatch; false-green health panel; hardcoded Hetzner name in DR panel; visible APPDETAIL debug marker). 37 ⛔ are honest walls (KC browser-SSO unmintable headless, founder-mailbox PIN, write-gated funnel mutations, no customer-kind Org sample). 63 ⚠️ dominated by catalog write-mutation rows (write-gated on the live env; prior 25aadcfc durable-commit passes stand) + cutover-partial context (step-08 wedged, cutoverComplete=false). Verdict provenance: `hw220-walk-2026-07-04(A|B)` stamps in each row's Evidence column.

> **hw218 walk — 2026-07-03 (dep `25b49b8534ab98db`, hw218.omantel.biz, FIRST-EVER zero-touch 2-region convergence on the merged #4706 train).** Four wire-captured verdicts, no hand-patching anywhere in the env:
> 1. ✅ **No-nodePort gateway serves externally** — `gitea.hw218.omantel.biz → HTTP 303`, `auth.hw218.omantel.biz → HTTP 302` (off-cluster, ELB 212.72.24.33 → node:443 hostPort → hostNetwork envoy); gateway Service `type=ClusterIP`, **`nodePorts=<none>`** (kubectl wire-capture). Console-isolation leg 404 (two Gateways can't both bind node:443) → fixed in #4715 (merged, 1.4.987 roll-gated).
> 2. ✅ **Readiness gate (#4707) enforced** — `status=ready` written only at 00:37:03Z after the external console probe answered (<500); the gate code is proven present in the RUNNING mothership binary by capture 3. Hardening note: 404 satisfies the current <500 bar — tighten to <400 as a follow-up.
> 3. ✅ **2-region admission floor (#4708) live in production** — negative test against the LIVE mothership: `POST /deployments` (1 region, no topology) → **HTTP 400** `"deployment has 1 region(s) and no explicit bcpTopology — the BCP agreement default is multi-region (>=2 regions, Pillar 2)…"`. Nothing created.
> 4. ✅ **Tenant DNS split-horizon correct** — `gitea./auth.hw218 → 212.72.24.33` (primary ELB), `console.hw218 → 212.72.24.12` (console ELB).
> Pre-walk hygiene: hw217 wiped via canonical endpoint with **API-audited zero orphans** (0 ELBs, bastion-only ECS, 1 EIP, 1/5 VPCs). Session train: #4707 #4708 #4709 #4710 #4711 #4714 #4715 all MERGED.


> **Pillar 1 (Marketplace + voucher onboarding) fully live-walked 2026-06-27 — funnel + plan catalog + voucher issuance/redeem E2E.**

> **Pillar 4 (Agenity workspace + auto-mounted bp-openova-mcp) materially live-walked 2026-06-27 — controller + per-Org provision + MCP auto-mount + RBAC Org-scope verified; agentic chat-run gated on Anthropic credential.** *(Terminology per DOD.md §Pillar 4 + GLOSSARY: the user-facing product is **Agenity** / **bp-openova-mcp**; the legacy "Sandbox" concept + menu are dead/removed — founder 2026-06-30. Internal `sandbox-controller`/`Sandbox` CR plumbing Agenity is built on stays internal.)*

> **Authed operator-console walk 2026-06-27 (throwaway-mailbox PIN session) — 10 interactive-UI rows verified; 2 product faults exposed (rows 33, 176).**

> **Authed live-walk sweep 2026-06-27 — ~33 interactive rows verified (SSO/topology-dialog/per-app-health/catalog-IaC/cutover-UI/jobs/orgs); env healthy post bp-catalyst-platform 1.4.915 roll.** (Row 83 funnel /review = fix-merged-pending #4526; row 170 install-openbao = NA, superseded by the #3916 jobs-lens split.)

> **Write-mutation walk on fresh prov 25aadcfc 2026-06-27 — catalog IaC-commit (durable Gitea) + instance-provision rows verified (safe on the disposable throwaway).** Catalog Save/commit rows 127/128/129/132–140/142/143/145/147/148/149/153–156 flipped ✅ via `PUT /api/v1/catalog/<bp>/iac` → 200 `{committed:true}` + durable bare-repo Gitea commit, on two structurally-different blueprints (bp-alloy + bp-wordpress). Instance-provision rows 50/59/60/61 flipped ✅ via `POST /catalyst/v1/apps/instances` → 201 + CR Ready + matching topology badge (singleton + active-hot-standby).

> **3-agent live-walk batch 2026-06-27 — topology/recon/sso ✅ flips + owner-redirect fix (#4547) + 2 ❌→⛔ reclassifications (session-walled) + topology DR-panel product-gap recorded.** On dep `91dc05917e44d1c1`: 6 rows flipped ✅ (53/54 topology, 193/195 recon, 29/38 sso); row 3 owner-redirect = ⚠️ fix-merged-pending (#4546/#4547); rows 109 + 183 reclassified ❌→⛔ (KC browser-SSO-cookie / OpenBao final-render, both session-walled, not product faults); topology rows 51/52/56/57 held ⚠️ as a REAL product gap (DR/switchover UI panel not built for HR components) + 58/63 held ⚠️ as stale expectations (cilium/grafana render honestly active-active); recon 196/197 held ⚠️ read-only-gated. New canonical tally 193 ✅ · 0 ❌ · 9 ⛔ · 33 ⚠️ · 4 ☐ = 239.

## Session resolution — 2026-06-27 (Refs #909)

**Env:** live omantel.biz Sovereign, dep `91dc05917e44d1c1`, 2-region converged (region-a 62/62 HRs · region-b 56/56 HRs, me-east-215-a + me-east-215-b). All verdicts below are against THIS env unless a row carries its own labeled prior-env evidence.

**Headline:** 22 issues CLOSED this session with independently-audited live evidence (reproduced the live signal, no false-closes) · 4 more code-done + MERGED (roll-gated, NOT yet live-proven — recorded as merged-pending, not pass) · 13 remain open behind a recorded gate.

**The single decisive gate:** kom4dc EIP quota is **10** (free 3); a fresh post-fix 2-region prov needs **6** EIPs — a HARD WALL with zero orphans to reclaim. Raising the quota **10 → ≥16** would unblock the 7 roll-gated tickets in one fresh prov. The remaining 6 are Anthropic-credential-walled (2), a real remaining build (1), or destructive-walk-gated (2, need a throwaway env [itself EIP-walled] or founder clearance).

### Live-walk consolidation — 2026-06-27 (4-section read-only walk of `91dc05917e44d1c1`, region-a)

Four parallel read-only walkers (SSO/auth · Orgs/Contexts · Topology/DR/robust · Jobs/Cloud/Market) re-dialed the live 2-region omantel.biz Sovereign and reported **119 verdicts** (28 + 22 + 39 + 30): **75 PASS · 5 FAIL · 9 NA · 30 BLOCKED**. Consolidated into the numbered table below, the run flips **16 rows to ✅** (rows 5/6/25/32/41/42/43/44/64/71/77/111/189/205/229/236) and resolves 17 more rows from `☐`/`❌`/`⚠️` into their honest gate (⛔) or product-fault (❌) state, leaving the canonical table at **81 ✅ · 9 ❌ · 26 ⛔ · 42 ⚠️ · 81 ☐ = 239** at that snapshot. (A subsequent 2026-06-27 Pillar-1 funnel+voucher walk flipped 4 more rows to ✅ — 72/73/74 `☐`→✅ + 75 `⛔`→✅ — carrying the canonical table to **85 ✅ · 9 ❌ · 25 ⛔ · 42 ⚠️ · 78 ☐ = 239**.) Genuine product faults (FAIL): **newapi** serves 200 but generic-OAuth provider rows are never seeded (`Unknown OAuth provider`, rows 37/114); **Hubble** authed shell renders but the flow XHR (`/api/control-stream`) 302-bounces to KC so data never loads (row 39, carried ⚠️); and the **uatwalk91** Org reports `isolation=vcluster` while the live backing is host-namespace (rows 9/10/11/12/216 — the #3642 de-vcluster re-baseline, not a fresh regression). The 30 BLOCKED are honest gates: a kid-less handover *session* JWT is rejected by this catalyst-api build (whoami 401) and the SPA login guard clears injected cookies, so interactive-session UI renders (treemap/showback DOM, OpenBao authed UI, Topology-tab content) can't be asserted headless — their underlying data is proven at the API/kubectl/psql layer. Row 236 (region-B keycloak) **flips ⛔→✅** (the #4470 sandbox-ns deadlock did not recur; region-b 57 HRs True). No false-pass: every ✅ reproduced the live signal directly.

### Closed this session — 22, audited live (✅ VERIFIED-PASS)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| R1 | janitor | [#4454](https://github.com/openova-io/openova/issues/4454) | catalyst-api orphan-sweep no longer reaps a `ready` Sovereign — denylist inversion (`case "wiped": eligible; default: protect`) holds on the live env. | — | ☐ | — | — | hw227-reconfirm-2026-07-06: hw227 (dep 504e330d86aaefb4) console 200 + Sovereign ready after 5h — janitor did NOT reap the ready env; protect-side inversion holds. 
| R2 | network | [#4467](https://github.com/openova-io/openova/issues/4467) | Cilium VXLAN+WireGuard MTU set to **1370** in BOTH regions — cross-node pod TCP no longer DF-drops; full-size data packets traverse. | — | cilium hw224 | ❌ ‖ hw228 LIVE 2026-07-06 (kubeconfig): datapath now uses routing-mode=NATIVE (the #4656 native-routing fix — supersedes the #4467 MTU=1370/VXLAN mechanism this row asserts); cilium_host/cilium_net mtu=1420, cilium_wg0 mtu=1340, enable-wireguard=true. Row stays ❌ — MTU=1370/VXLAN premise superseded + 'both regions' single-region-gated (#4811, 6 region-a nodes / 0 region-b) ‖ hw228 LIVE 2026-07-06 (kubeconfig) — #4656 datapath PROVEN region-a: cilium-health 6/6 nodes reachable, Routing=Native/Host=BPF, KubeProxyReplacement Direct-Routing, masq BPF 10.42.0.0/15; acme vcluster apiserver Ready=True (the exact R2 DF-drop i/o-timeout canary — HEALTHY, not timing out), 64/67 HRs converged → cross-node pod TCP no DF-drops via native-routing. R2 stays ❌ ONLY because it asserts the superseded VXLAN+MTU=1370 mechanism AND 'both regions' (region-b absent #4811). GOAL achieved single-region | DISPROVEN — live cilium_host 1370 + VXLAN 50 = 1420 > cilium_wg0 1290 → still DF-drops cross-node (vcluster apiserver i/o-timeout). #4467 MTU=1370 insufficient; superseded by #4656 (native-routing fix designed). 
| R3 | sso | [#4448](https://github.com/openova-io/openova/issues/4448) | bp-plane-isolation openbao default-deny ADMITS sso-bridge ingress → sso-bridge reaches openbao:8200. | — | ☐ | — | sso-bridge-reconciler 1/1 Running 3h35m + grafana SSO works (302→KC kc_idp_hint) → it reaches openbao:8200; openbao CNPs (gateway-ingress/apiserver-egress) VALID=True admit the datapath. | hw227-reconfirm-2026-07-06: hw227 sso-bridge-reconciler 1/1 Running 4h57m 0-restarts + grafana SSO 302→KC → reaches openbao:8200. 
| R4 | sso | [#4458](https://github.com/openova-io/openova/issues/4458) | sso-bridge egress CNP permits openbao + keycloak — the reconciler's own egress is not blocked. | — | ☐ | — | sso-bridge egress reaches openbao+keycloak — reconciler healthy 3h35m, 0 restarts, actively re-minting OIDC secrets (would CrashLoop/error if egress blocked). | hw227-reconfirm-2026-07-06: hw227 sso-bridge egress healthy 4h57m 0-restarts, actively re-minting OIDC secrets (would crash if egress blocked). 
| R5 | sso | [#4437](https://github.com/openova-io/openova/issues/4437) | sso-bridge re-mints the grafana OIDC client secret each tick (KC_ADDR cache refreshed) — no stale-secret 401. | — | ☐ | — | grafana OIDC round-trips (walk: grafana 302→KC w/ kc_idp_hint=catalyst-pin, no stale-secret 401) → sso-bridge re-mints the client secret each tick. | hw227-reconfirm-2026-07-06: hw227 grafana /api/user=200×3 = emrah.baysal@openova.io, no stale-secret 401 → sso-bridge re-mints each tick (row 235). 
| R6 | postgres | [#4442](https://github.com/openova-io/openova/issues/4442) | bp-postgres NetworkPolicy admits the declared consumers (CNPG operator probe + app dials). | — | ✅ | — | bp-postgres NetworkPolicies present (shared-pg/-b/-c allow-replication-to-primary) + CNPG clusters all 3/3 healthy → declared consumers (CNPG probe + app dials) admitted. ‖ **2026-07-09 ✅ LIVE hw231 (kubectl):** bp-postgres NP/CNP admits consumers — shared-pg + shared-pg-b + shared-pg-c all **3/3 ready** (Cluster healthy), the crossregion-dr-primary CNPs present in shared-data ns, and consumers (grafana/keycloak/guacamole) connect + run 0-crashloop (rows 67-70). Refs #4442 | hw227-reconfirm-2026-07-06: hw227 4 shared-pg replication NetworkPolicies present + CNPG shared-pg/-b/-c all 3/3 healthy → declared consumers admitted. 
| R7 | plane-isolation | [#4444](https://github.com/openova-io/openova/issues/4444) | bp-plane-isolation admits gitea ingress for its declared consumers (org-services→gitea de-vcluster gap). | — | ✅ | — | gitea CNPs (allow-gateway-ingress/apiserver-egress) VALID=True + Flux GitRepositories pull from gitea Ready → plane-isolation admits gitea ingress for its declared consumers. ‖ **2026-07-09 ✅ LIVE hw231 (kubectl):** bp-plane-isolation admits gitea ingress — gitea CNPs present (allow-apiserver-egress, allow-crossregion-db-egress, allow-gateway-ingress); gitea 1/1 Running; org-services console 1/1 Running + org-pg 1/1 Running (org-services→gitea path healthy). Refs #4444 | hw227-reconfirm-2026-07-06: hw227 gitea reachable — Flux GitRepositories Ready + 20 apiserver-egress/gateway-ingress CNPs admit gitea ingress. 
| R8 | gitea | [#4447](https://github.com/openova-io/openova/issues/4447) | gitea-flux-auth secrets are seeded — Flux pulls from gitea with valid credentials. | — | ☐ | — | — | hw227-reconfirm-2026-07-06: hw227 Flux GitRepositories all Ready=True (catalyst-tenant-acme, catalyst-tenant-hw227-omantel-biz, openova, openova-catalog-sovereign, openova-org-tenants) → gitea-flux-auth valid. 
| R9 | sso | [#4446](https://github.com/openova-io/openova/issues/4446) | oidc-gate client_secret is seeded for powerdns-admin — full OIDC round-trip completes. | — | ☐ | — | oidc-gate-powerdns-admin pod 1/1 Running 3h37m (+ oidc-gate-openova-flow) → client_secret seeded, OIDC round-trip serves. | hw227-reconfirm-2026-07-06: hw227 oidc-gate-powerdns-admin + oidc-gate-openova-flow both 1/1 Running 4h57m → client_secret seeded, OIDC serves. 
| R10 | orgs | [#4471](https://github.com/openova-io/openova/issues/4471) | org-controller RBAC carries update/patch — per-Org provisioning completes (was create-only, never reached Ready). | — | ☐ | — | — | — | hw227-correction-2026-07-06T03:30: CORRECTION — acme was Plan S; per the #4292 tier gate (isolationForTier: free/S->namespace, M+->vcluster) namespace is CORRECT, NOT the #4813 'defect' (retracted, WAD). The ISOLATION=vcluster verdict needs a customer Org on Plan M+ (a separate funnel walk). Refs #4292 
| R11 | gitea | [#4354](https://github.com/openova-io/openova/issues/4354) | gitea git-data is PRESERVED across the host-ns re-home (#4325 de-vcluster) — no empty-PVC data loss. | — | ☐ | — | gitea pod 1/1 Running 3h35m + per-Org GitRepositories carry real content (revision sha1, stored artifact) → git-data present post host-ns re-home, not empty-PVC. | hw227-reconfirm-2026-07-06: hw227 Flux GitRepositories carry real content (Ready, artifact stored) → gitea git-data preserved post host-ns re-home. 
| R12 | postgres | [#4460](https://github.com/openova-io/openova/issues/4460) | shared-pg `-mesh-rw` resolves in BOTH regions + CNPG streaming replication is live. | — | — | ☐ | — | hw227-reconfirm-2026-07-06: hw227 shared-pg/-b/-c all 3/3 healthy in region-a — but region-b is standby-absent (#4811) so -mesh-rw BOTH-regions + cross-region streaming repl cannot be verified. 
| R13 | convergence | [#4436](https://github.com/openova-io/openova/issues/4436) | region-B keycloak / gitea / harbor reach steady-state with NO CrashLoop. | — | kubectl hw224 | ⚠️ ‖ hw228 LIVE 2026-07-06 (kubeconfig): region-a bp-gitea/bp-harbor HR True + pods steady (gitea 1/1 0-restarts; harbor core/jobservice restarts were 8h-ago convergence, now all Running/ready, no CrashLoop). region-B ABSENT (#4811 single-region, 6 region-a nodes / 0 region-b) → region-B steady-state not verifiable on this env; stays ⚠️ | region-a: bp-keycloak/bp-gitea/bp-harbor HR True (Helm install succeeded), no CrashLoop; region-b DEGRADED (clustermesh-apiserver Init:0/1 — #4656 family). region-a steady, region-b lagging. 
| R14 | model | [#4479](https://github.com/openova-io/openova/issues/4479) | console Organizations directory reads the live Organization CRs (not an empty in-memory store). | — | ☐ | — |
| R15 | funnel | [#4473](https://github.com/openova-io/openova/issues/4473) | funnel plan-slug propagates end-to-end — the chosen plan reaches the provisioned Org. | — | ☐ | — | Org uatwp4758 spec.planSlug=m — the funnel-picked plan M propagated end-to-end to the provisioned Org CR. | hw227-reconfirm-2026-07-06: hw227 funnel-created Org acme spec.planSlug=s — the funnel-picked Plan S propagated end-to-end to the Org CR. 
| R16 | funnel | [#4290](https://github.com/openova-io/openova/issues/4290) | the funnel + BSS Org doors are collapsed onto ONE path — `console.<slug>` lands 200. | — | ☐ | — | funnel walked as ONE path plans→apps→addons→bcp→review→checkout→launch (voucher VCH-QFP52J4S2KEX → Org uatwp4758) — funnel+BSS Org doors collapsed. | hw227-reconfirm-2026-07-06: hw227 funnel walked as ONE path plans→apps→addons→bcp→review→checkout→launch (voucher ACMEUAT227LAUNCH → Org acme) — funnel+BSS doors collapsed. 
| R17 | orgs | [#4459](https://github.com/openova-io/openova/issues/4459) | deleting an Organization CR cascades cleanly — no orphaned ns / app / DNS leak. | — | ☐ | — | delete-cascade finalizer armed (uatwp4758 has orgs.openova.io/tenant-networking) + #4459 merged, but NOT exercised on-env (won't delete the live customer Org). Mechanism present, clean-cascade unverified live. 
| R18 | cutover | [#4450](https://github.com/openova-io/openova/issues/4450) | handover-key self-publish guard — the Sovereign does not re-publish its handover key on reconcile. | — | kubectl hw224 | ⚠️ | catalyst-handover-jwt-public present (single copy, 4h) — self-publish guard mechanism in place, but not exercised (would need a re-publish attempt to prove the guard blocks). 
| R19 | sandbox | [#4482](https://github.com/openova-io/openova/issues/4482) | sandbox-controller reaches Running (was CrashLoop on unset CATALYST_GITEA_TOKEN). | — | ☐ | — | — | hw227-reconfirm-2026-07-06: hw227 sandbox-controller 1/1 Running 0-restarts 4h49m — no CrashLoop. 
| R20 | delivery | [#4464](https://github.com/openova-io/openova/issues/4464) | the deploy-bot bumps image pins per-line (not a blanket bump) — stale-render avoided. | — | n/a | N/A | deploy-bot per-line pin bumping is a MOTHERSHIP/CI concern — not verifiable on a child Sovereign. 
| R21 | catalog | [#4432](https://github.com/openova-io/openova/issues/4432) | catalog-seed pins match the published ghcr chart versions — no inert lagging Blueprint CR. | — | ☐ | — | 81 Blueprint CRs seeded (matches catalog 81 blueprints; walk showed bp-wordpress v0.4.12 detail) → catalog-seed pins resolved from ghcr. | hw227-reconfirm-2026-07-06: hw227 81 Blueprint CRs seeded (matches catalog), 0 with resource-policy:keep → catalog-seed pins resolved from ghcr, no inert lag. 
| R22 | plane-isolation | [#4468](https://github.com/openova-io/openova/issues/4468) | bp-plane-isolation admits apiserver-egress + gateway-ingress CNPs — CNPG admission + gateway hop succeed. | — | ✅ | — | 20 apiserver-egress + gateway-ingress CiliumNetworkPolicies present → bp-plane-isolation admits the required apiserver-egress + gateway-ingress paths. ‖ **2026-07-09 ✅ LIVE hw231 (kubectl):** bp-plane-isolation apiserver-egress + gateway-ingress CNPs present (26 apiserver/gateway CNPs cluster-wide, incl. alloy/gitea/external-dns); CNPG admission works (shared-pg clusters reach 3/3 Ready → the cnpg webhook is reachable via the apiserver-egress carve-out). Refs #4442 #4444 | hw227-reconfirm-2026-07-06: hw227 20 apiserver-egress/gateway-ingress CiliumNetworkPolicies present → plane-isolation admits the required paths. 

### Merged this session — code-done, roll-gated for live proof on the next fresh prov (⚠️ merged-pending)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| M1 | janitor | [#4466](https://github.com/openova-io/openova/issues/4466) (PR [#4493](https://github.com/openova-io/openova/pull/4493)) | janitor hardening — log-only/dry-run for a full live cycle before any destructive sweep. | — | ☐ | — |
| M2 | apps | [#4477](https://github.com/openova-io/openova/issues/4477) (PR [#4492](https://github.com/openova-io/openova/pull/4492)) | newapi admin-token seeded into OpenBao at provision (was unseeded → admin-promote fault). | — | ☐ | — |
| M3 | network | [#4475](https://github.com/openova-io/openova/issues/4475) (PR [#4495](https://github.com/openova-io/openova/pull/4495)) | vcluster-tier CNP applied host-side — the per-Org vcluster tier carries its split CNP. | — | ☐ | — |
| M4 | apps | [#4111](https://github.com/openova-io/openova/issues/4111) (PRs [#4494](https://github.com/openova-io/openova/pull/4494)+[#4496](https://github.com/openova-io/openova/pull/4496)) | agenity image-pull half — fresh-install ghcr-pull path. | — | ⚠️ | fresh-install path verified live (ghcr-pull reflects + pod recovers); the agentic-run half (M-side) is Anthropic-credential-walled — see G8/#4277. |

### Remaining open — 13, with the recorded gate (honest map)

| # | Epic | Ticket | Test case | Walk | Result | Gate |
|---|---|---|---|---|---|---|
| G1 | adoption | [#4488](https://github.com/openova-io/openova/issues/4488) | crossplane provider-opentofu Observe-only adoption populates on the fresh prov. | — | ☐ | — | — |
| G2 | apps | [#4477](https://github.com/openova-io/openova/issues/4477) | newapi ES-sync drives the admin token into a fresh per-Org newapi (seed merged in M2). | — | ⚠️ | EIP-quota — fresh-prov proof gated on the same wall as G1. | hw227-reconfirm-2026-07-06: hw227 newapi-bp-newapi 3/3 Running + admin-promote Job Completed, but ExternalSecret catalyst-newapi-admin-token = SecretSyncedError/Ready=False (oidc-sync ES is SecretSynced/True) — admin-token ES-sync failing on this fresh prov. Refs #4477 |
| G3 | dr | [#4486](https://github.com/openova-io/openova/issues/4486) | DR self-fire exercises the standby-absent branch (no standby present). | — | ☐ | — | — |
| G4 | adoption | [#4431](https://github.com/openova-io/openova/issues/4431) | VPC-quota reclaim demo on a fresh prov. | — | ⚠️ | EIP-quota — the reclaim demo needs a fresh prov cycle. | mothership-verify-2026-07-06T01:15: mothership [JANITOR] pass emits live orphanEIPsDeleted/orphanVPCsDeleted/orphanEVSDeleted fields (this pass: 0 orphan VPC/EIP, 19 EVS reclaimed) — the VPC/EIP-quota reclaim mechanism is live and running. The full fresh-prov reclaim DEMO (prov→wipe→quota-returns) still needs a fresh prov cycle (EIP-quota-walled). Refs #4431 |
| G5 | janitor | [#4466](https://github.com/openova-io/openova/issues/4466) | janitor log-only live proof (the dry-run full-cycle observation). | m | ☐ | — | mothership-verify-2026-07-06T01:15: mothership catalyst-api [JANITOR] pass logged "destructive":false, "reaped":0, "reapErrors":0 — a full log-only/dry-run cycle observed live (reaped ZERO Sovereigns) while safely GC-ing 19 orphan EVS + 1 orphan keypair. The dry-run full-cycle observation. Refs #4466 |
| G6 | model | [#4212](https://github.com/openova-io/openova/issues/4212) | object-model seams 1+2 — the two adoption seams collapse into the #4488 crossplane-adoption walk (G1). | — | ☐ | — | — |
| G7 | orgs | [#4293](https://github.com/openova-io/openova/issues/4293) | vcluster dual-door walk — both Org doors land a vcluster-isolation Org. | — | ☐ | — | — | — |
| G8 | apps | [#4277](https://github.com/openova-io/openova/issues/4277) | anthropic credential seeded into the agentic runtime — chat works end-to-end. | — | ❌ | hw228 DIRECT-WALK 2026-07-07: agentic runtime NOT deployed — agenity.hw228.omantel.biz → 000, no agenity/agentic pods (only sandbox-controller runs), no anthropic secret. Can't chat end-to-end without the runtime. ❌ Refs #3969 ‖ hw228 2026-07-09 LIVE: the sandbox PLATFORM is deployed (sandbox-controller 1/1 Running, bp-sandbox HR Ready chart 0.3.11) but NO anthropic/claude credential secret exists in any namespace → the agentic runtime has no Claude key → chat cannot run. Gated on the agentic-runtime cred-provisioning + a live sandbox session (Pillar-4 sandbox completion, adjacent to the post-SME MCP build #3988/GOV#899). Stays ❌ — not a quick fix. ‖ 2026-07-09 (G8 fix-agent, orchestrator-verified): the Anthropic-cred CHAIN is 100% built (#4277/#4111: ExternalSecret openbao catalyst/anthropic/token → agenity ANTHROPIC_API_KEY; seedAnthropicToken at Org-provision; api-deployment env wiring — all verified live). Gap = NO anthropic-api-key/credentials-json key in catalyst-openova-kc-credentials on hw228 OR the mothership (no key ever placed → seedAnthropicToken skips by design). FOUNDER ACTION: place a claude-code credentials.json into the mothership secret (chain resolves it) + decide on cloud-init propagation to Sovereigns. NOT agent-fixable (founder-supplied cred + security decision + agenity not installed on hw228). Stays as-is. | hw227-gate-verify-2026-07-06T01:35: VERIFIED-BLOCKED — kubectl shows NO agenity pods/Application on hw227 (agentic runtime not deployed) AND zero anthropic/claude secrets in the cluster ([]). Agentic chat cannot be walked without the app + a real Anthropic credential (neither present). Refs #4277 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: 0 Anthropic secrets in-cluster AND agenity not deployed on hw227 — agentic chat cannot run. Refs #4277 ‖ **GATE CONFIRMED 2026-07-07 (repo inspection):** #3988 built the openova-MCP SERVER (products/openova-mcp/ — Go cmd/internal/{identity,tools,catalystapi}) but NOT its deployment layer: NO Chart.yaml, NO Dockerfile, NO blueprint.yaml, NOT in the bootstrap-kit or catalog. So the RBAC-scoped MCP facade cannot be deployed on ANY Sovereign (confirmed absent on hw228 — only sandbox-controller runs). These rows are gated on PACKAGING the server (image build workflow + Helm chart + blueprint.yaml + bootstrap-kit slot + per-Org KC identity wiring), a substantial build cycle, not a simple deploy. Refs #3988. |
| G9 | apps | [#4111](https://github.com/openova-io/openova/issues/4111) | agentic-run half — the agenity solo agent chats + drives create_application. | [agenity.demo](https://agenity.demo.omani.homes/app/) | ⚠️ | agentic-run/SSO half PROVEN on 8fd457a8 (claude -p → NORTHSTAR-OK); MCP create_application BLOCKED on fresh-Org agenity-MCP auth-wiring (pubkey-not-reflected [fixed live] + OPENOVA_MCP_BEARER-not-seeded → tools/list empty) — filed #4624. Agent did NOT fabricate. [[../sessions/2026-06-28/evidence/north-star-8fd457a8.txt]] ‖ hw228 2026-07-09 LIVE: no active sandbox session + no anthropic cred (see G8) → the agenity solo agent isn't live to chat/drive create_application. Gated on sandbox/agentic-runtime completion (Pillar 4, post-SME-adjacent). Stays ⚠️. ‖ 2026-07-09 (G8 fix-agent, orchestrator-verified): the Anthropic-cred CHAIN is 100% built (#4277/#4111: ExternalSecret openbao catalyst/anthropic/token → agenity ANTHROPIC_API_KEY; seedAnthropicToken at Org-provision; api-deployment env wiring — all verified live). Gap = NO anthropic-api-key/credentials-json key in catalyst-openova-kc-credentials on hw228 OR the mothership (no key ever placed → seedAnthropicToken skips by design). FOUNDER ACTION: place a claude-code credentials.json into the mothership secret (chain resolves it) + decide on cloud-init propagation to Sovereigns. NOT agent-fixable (founder-supplied cred + security decision + agenity not installed on hw228). Stays as-is. |
| G10 | placement | [#3969](https://github.com/openova-io/openova/issues/3969) | placement EPIC — the remaining real build. | — | ◑ | — ‖ hw228 2026-07-07 (placement walk): G10(a) wizard Placement step ✅ (target/role/standby-type selectors + capability gate enforced). G10(b) switchover + G10(c) add-target: MECHANISM proven live (placement edit flips primaryRegion a→b, singleton→active-hot-standby reconciles, Topology re-renders primary-swap / 1→2 cards) BUT console Edit-placement Apply → HTTP 400 'placement.mode is required' — PlacementEditor sends the #3969 targets[] model, HandleApplicationUpdate validated legacy {mode,regions}. Fix shipped: PR #4837 (#4836) folds targets[]→mode+regions via bpv1.DerivePattern (unit-tested). G10 flips ✅ after the catalyst-api roll + console re-walk. Evidence g10a/b/c-*.png. Refs #3969 #4836 ‖ 2026-07-07 fix MERGED to main via PR #4840 (was #4837, accidentally closed during a CI-retrigger + reopened as #4840; the k8scache CI flake blocking it was itself fixed + merged in #4839). HandleApplicationUpdate now accepts the #3969 targets[] model via bpv1.DerivePattern. G10 flips ✅ once the mothership catalyst-api rolls this image + the console switchover/add-target is re-walked. Refs #4836 #4840 ‖ **GATE CONFIRMED 2026-07-07 (image forensics):** hw228's sovereign catalyst-api + catalyst-ui both run image `9e1ba05`, which PREDATES the #4840 placement-targets fix — `git show 9e1ba05:products/catalyst/bootstrap/api/internal/handler/applications_update.go` has ZERO regionsFromPlacementTargets/DerivePattern. So the placement editor's switchover/add-target PUT still 400s on this env. Flips ✅ only after hw228's console/api rolls to a post-#4840 image. (Same roll gates 148/154 — catalyst-ui 9e1ba05 lacks the #4844 YamlEditor fix.) | hw227-gate-verify-2026-07-06T01:35: VERIFIED-BLOCKED — gh issue #3969 is an OPEN EPIC (Application-centric Placement targets[]) labeled epic + status/blocked-ext. A remaining code BUILD blocked externally — not a verification/walk; nothing to assert until built. Refs #3969 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: #3969 placement EPIC is OPEN/unbuilt (status/blocked-ext) — a code build, nothing to exercise. Refs #3969 |
| G11 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | sovereignty cutover — 8-step + 10-min deny-egress hold. | [/settings#sovereignty](https://console.omantel.biz/settings#sovereignty) | ◑ | **2026-07-01 (#4682):** the cutover's registry-gateway prerequisite is re-architected at the source — gateway now a `Service type=LoadBalancer` on **:443** (Gateway API + Cilium + Envoy), registry reached via external host `registry.<fqdn>:443`, the `:30443` NodePort/coredns/sysctl-DaemonSet workaround chain **deleted** (PR #4684 merged `3e650b623`, net −133; delivered — catalyst-api `3e650b6` baked-`:443` verified). **LIVE-PROVEN on the 2-region hw207 prov (`f5fdc757`):** `cilium-gateway-cilium-gateway` Service = type=LoadBalancer on 443/80 (was ClusterIP :30443), listeners `https=443/http=80`, `sysctl-envoy-bind` DaemonSet NotFound — evidence `uat-hw207-2region-provisioning-443fix-2026-07-01.png` + `uat-sovereign-deployments-hw207-2region-2026-07-01T1431.png` (hw207 reached **ready**, 2-region, Secondary-degraded). **2026-07-02 fresh 2-region prov hw208 (`cdf4c53b`) — reached ready, owner-scoped to emrah.baysal@openova.io (#4683 confirmed), evidence `uat-sovereign-deployments-hw208-ready-ownerscoped-2026-07-01T1842.png`.** Gateway=LoadBalancer :443 sharing the clustermesh CP-node EIP via LB-IPAM sharing-key (#4687); `sysctl-envoy-bind` absent; no gateway ELB — checks 1/3/4 PASS. Fixed a P0 en route (#4689: empty sharing-key default → YAML null → CRD reject → sovereign-tls wedged). ⛔ **BLOCKED: console external-serving FAILS + cutover BLOCKED — the CP-node EIP is a Huawei 1:1 NAT (not on-link), so the Cilium LB VIP keyed on the public EIP never receives the post-NAT packet → `console:443` REFUSED externally (#4686). In-cluster the gateway serves fine (ClusterIP:443→200). Fix = `externalTrafficPolicy=Local`+hostPort on envoy (host-level :443 receipt), needs an off-cluster-curl live gate.** Prior throwaway proof (`5150f96`: `cutoverComplete=true`) predates the #4682 re-architecture — re-walk once #4686 lands. **2026-07-02 (#4691 merged `acfbd66`) — #4686 RESOLVED at source:** the fix is NOT the ETP+hostPort guessed above but a restored Crossplane/tofu Huawei **ELB** (`elb_primary`, its OWN routable EIP) forwarding public :443/:80 → the gateway Service's nodePort — a dst-IP-agnostic host listener (like apiserver :6443) that RECEIVES the post-NAT packet the CP-EIP-keyed Cilium VIP never did; option-B (CP-EIP sharing-key #4687) removed; DNS → the ELB EIP; catalyst-api reconciles the ELB members to the live nodePort post-convergence. **Component-verified:** ELB present in the running catalyst-api's baked tofu (`elb_primary refs:9`); `nc 127.0.0.1:<nodePort>` OPEN on hw208 (nodePort→envoy serves); ELB EIP distinct from CP EIP on hw209/hw210. **Owner-scoping (#4683) re-verified 2026-07-02** via the authenticated deployments API — returns ONLY emrah.baysal@openova.io's deps, zero foreign-owner leakage (PASS); the console UI *render* is PIN-to-email gated (evidence `login-pin-gated.png`; the API is the render's own data source). ⛔ **End-to-end off-cluster `curl https://console.<fqdn>` gate STILL PENDING** — hw209/hw210/hw211 all hit the kom4dc cilium-bootstrap DNS-wedge (**#4692** filed, root-caused: the cloud-init /etc/hosts pin-loop resolves helm.cilium.io via flaky VPC DNS at pin-time with no retry). No converged prov yet to run the live gate; re-walk once #4692 lands + a prov converges. **2026-07-02 live Playwright console-login walk (this session):** navigated `/sovereign/deployments` → client-redirect to `/sovereign/login` (PIN-to-email) → red **`could not deliver code`**; root-caused via catalyst-api log to the SMTP send failing on `mail.openova.io` **flaky-upstream-DNS** (`server misbehaving`) → `/api/v1/auth/pin/issue` **502** (**#4696**). Fixed at source: **#4697** merged `adef16ec8` — chart SMTP host → in-cluster `stalwart-web.stalwart.svc.cluster.local` (matches the auth.go code default; chart 1.4.980); operator PIN-login (incl. the founder's own) delivers reliably on the next bp-catalyst-platform roll. Evidence `sovereign-deployments-pin-gate-2026-07-02.png` + `sovereign-login-could-not-deliver-code-2026-07-02.png`. The UI *render* stays PIN-gated **by the founder's #688 design** (magic-link/auto-login rejected; PIN is crypto/rand in-memory) — an agent cannot self-authenticate; owner-scoping DATA verified via the authenticated API (PASS, above). ‖ **2026-07-09 CUTOVER FIRED on hw231 (fresh env, dep ab1355c25f9798c3):** operator-fired via the canonical runner-SA in-cluster trigger; `CATALYST_CUTOVER_ACK_TOLERANCE=1` (#4870 fix) baked. Steps PROVEN sequentially: 01 gitea-mirror ✅, 02 harbor-projects ✅, 03 harbor-prewarm ✅ (Phase A push_ok=30/0-fail, full image mirror to registry.hw231.omantel.biz), **04 registry-pivot ✅ — the #4870-gated step: registry-pivot DaemonSet 6/6 READY, ack-gate cleared** (this is the EXACT step that FAILED on hw228 with tolerance=0 + a dead-dfdaemon node; tolerance=1 let 6/6 ack cleanly), 05 flux-gitrepository-patch ✅, 06 helmrepository-patches (running). Remaining: 07 catalyst-api-env-patch, 08 egress-block-test (10-min deny-egress hold), 09 gitea-token-mint, 10 vcluster-registry-pivot, 11 crossplane-provider-pivot. cutoverComplete pending final steps. Refs #3379 #4870 ‖ **UPDATE 2026-07-09 — cutover WEDGED at step 6 on a NEW real defect (#4873), fix shipped PR #4874:** steps 01-05 ✅ incl. **registry-pivot #4870 ack-gate PASSED 6/6** (the hw228-failing step — #4870 VALIDATED). Step 06 helmrepository-patches retry-loops: `bp-catalyst-platform:1.4.1038 not pullable` — harbor-prewarm mirrored 1.4.1039 (a deploybot bump the gitea-mirror step pulled in raced `.spec.chart.spec.version`) while step-06 checks the settled deployed 1.4.1038. Both steps read the mutable desired version → skew. FIX #4874: read immutable `.status.history[0].chartVersion` in both. Full cutoverComplete=true (steps 06-11 + 10-min deny-egress) previously PROVEN on hw148 (task #847) — this env hit the version-skew race; the clean seal needs a fresh prov on cutover-chart 0.1.104. Registry-pivot half of Pillar-5 proven; final seal pends #4874 + re-prov. Refs #3379 #4870 #4873 #4874 |
| G12 | dr | [#4275](https://github.com/openova-io/openova/issues/4275) | region-kill (Pillar-3) — kill region-b, prove failover + recovery. | — | ✅ | — | — | — ‖ **2026-07-09 REGION-KILL DRILL on hw231 (dep ab1355c25f9798c3, FRESH prov, live kubectl):** baseline write on region-a primary (`cnpg-pair-...-primary-1`, LSN 0/9031860) → confirmed replicated to region-b replica in **sync** state (RPO=0 precondition: `pg_stat_replication` = `...replica | streaming | sync | 0/9031898`). KILL: cordoned all 3 region-a cnpg nodes (simulate region-a down, prevent timeline fork) → operator promotion `kubectl patch cluster ...replica {enabled:false}` on region-b. **RTO = 1.0s** (region-b writable at poll attempt 2; target ≤30s ✅). **RPO = 0** (baseline row `hw231-preKill-baseline` present on the promoted primary + post-promote writes accepted). RECOVERY: uncordoned region-a; region-b showed expected post-promotion `ConsistentSystemID=False` divergence → canonical clean rebuild (delete replica Cluster CR → bp-cnpg-pair HR reconcile → fresh pg_basebackup from region-a, consistent system-ID) — bootstrapping now, streaming-sync re-establishment pending. Failover metric PROVEN; ⏳ pending recovery-complete confirm. Refs #4275 **RECOVERY CONFIRMED 2026-07-09:** region-b replica rebuilt fresh (join Jobs Completed, 3/3 Running, consistent system-ID) and region-a `pg_stat_replication` shows `cnpg-pair-...-replica | streaming | sync` RE-ESTABLISHED — full failover+recovery cycle PROVEN live on hw231. G12 ✅. |

> **Reading the gate map:** the 13 remaining work-items resolve to **12 distinct open tickets** (#4212 carries object-model seams 1+2, both folding into #4488/G1 — counted as one ticket here, two seams in narrative). 7 of them (G1–G7) clear in ONE fresh post-fix 2-region prov the moment kom4dc EIP quota rises **10 → ≥16**. 2 (G8–G9) need a real Anthropic credential. 1 (G10) is a genuine remaining build. 2 (G11–G12) are destructive walks needing a throwaway env (itself EIP-walled) or explicit founder clearance. None is a false-fail; each is an honest fault with a named unblock.

---


**RESET 2026-06-26 — clean slate for the fresh Sovereign, now walked on fresh prov `91dc05917e44d1c1` (omantel.biz, region-a); recounted after the 2026-06-27 4-section live-walk consolidation + the 2026-06-27 Pillar-1 funnel+voucher walk + the 2026-06-27 newapi-SSO resync walk + the 2026-06-27 authed operator-console walk + the 2026-06-27 Pillar-4 Sandbox/MCP walk + the 2026-06-27 authed live-walk sweep + the 2026-06-27 write-mutation walk on fresh prov `25aadcfc` + the 2026-06-27 ⚠️/❌ reconcile + the 2026-06-27 3-agent live-walk batch (Refs #909): 193 ✅ · 0 ❌ · 9 ⛔ · 33 ⚠️ · 4 ☐ = 239 rows.** (⚠️/❌ reconcile against the live env flipped 7 rows to ✅ with reproduced live evidence: Hubble row 39 `⚠️`→✅ — bp-cilium@1.4.5 rolled, oauth2-proxy `--api-route=^/api/` live, `/api/*` returns 401-not-302; funnel /review row 83 `⚠️`→✅ — marketplace:f1f7ae6 ships the #4526 ReviewStep topology card; OpenBao admin row 33 `❌`→✅ — bp-openbao@1.2.58 rolled, OIDC auth_url now requests `scope=openid+groups` + sso-operator-admin policy mapped (the 403 root cause resolved); Jobs Re-run row 176 `❌`→✅ — catalyst-api e976ff0 ran the #4514 delete+recreate (`mimir-make-minio-buckets-rerun-*` Complete, original deleted, no 502); Topology rows 62/187/188 `⚠️`→✅ on the same data-grounded standard as 64/71 — live Continuum CRs Healthy/Ready/LeaseHeld + grafana multi-region active-active + catalyst-api single-region singleton, all via kubectl, UI render SSO-session-walled. RECLASSIFIED rows 9/10/11/12 `❌`→⚠️ (and corrected 216's rationale): the uatwalk91 host-namespace backing is DESIGN-CORRECT for its `plan=s` per the #4292 TIER GATE — `boundaryIsVcluster`/`allTiersVcluster=false` backs free/S Orgs with the host `<slug>` namespace, only M+ get a dedicated Org-vcluster — so the blanket `vcluster`-for-every-Org expectation is stale, leaving only a cosmetic `isolation=vcluster` label mismatch. Row 183 HELD ❌ (the 403 root cause is #4522-fixed but the row asserts the rendered Secrets-Engines screen, SSO-session-walled). Rows 37/114 confirmed stayed ✅ (newapi bare URL → 200 'Signing you in…', `sovereign` provider recognized). NO false-pass: every ✅ flip reproduced the live signal at the HTTP/kubectl/served-bundle layer; the authed test-session mint is 404-gated in prod so interactive UI renders stay honestly ⚠️/❌ where un-reproduced.) (Authed operator-console walk via a throwaway-mailbox PIN session flipped 11 interactive-UI rows `⛔`→✅ — treemap/showback 20/21/23, keycloak-admin 34, Organizations directory/terminology 116/117/118/119/120/121/122 — and exposed 2 genuine product faults `→❌`: OpenBao authed admin UI 403 row 33, Jobs Re-run 502 immutable-spec row 176, both with fixes in flight.) (newapi-SSO walk flipped rows 37 + 114 `❌`→✅ — #4477 `resync_provider_secret()` (bp-newapi@1.4.137) resolved the stale `custom_oauth_providers` client_secret live, no "Unknown OAuth provider"; Hubble row 39 holds ⚠️ fix-merged-pending-roll for PR #4509, the `--api-route=^/api/` flow-XHR fix.) (Pillar-4 Sandbox/MCP walk via a throwaway Sandbox CR on `91dc05917e44d1c1` region-a flipped row 218 `⛔`→✅ — sandbox-controller no longer CrashLoops (R19/#4482 gitea-token bridge), Sandbox CR reconciles `Ready=True` — and flipped 219 + 220 `⛔`→⚠️ PARTIAL (controller writes 12 gitops files incl the MCP ConfigMap + pty-server StatefulSet; the image bundles `@anthropic-ai/claude-code` + `@qwen-code` + the `/usr/local/bin/openova-sandbox-mcp` binary — runtime-pod-materialize + live agentic run not awaited). Rows 221/222/223 HOLD ⛔ on an Anthropic credential: the MCP `create_application` tool + the openova-mcp Org-claim RBAC are proven at the API layer (211/212/213), only the live chat-driver is credential-walled. The MCP auto-mount is PROVEN — `configmap-mcp-config.yaml` projected at all 4 agent-discovery paths (`/workspace/.mcp.json`, `~/.claude.json`, `~/.qwen/settings.json`, `.cursor/mcp.json`) + the MCP binary in the published image. ⚠️ note: the sandbox-controller has no delete-finalizer to prune the gitea gitops path on Sandbox CR delete (minor cleanup gap); a subset of Wave-2 MCP tools (`registry.go`, `sandbox_db.go`) return `not_implemented` stubs — surface discoverable, some advanced bodies stubbed.) (Consolidation pass 2 of the live fresh-prov walk — folds §C orgs/keystone · §G model+placement · §H contexts+mcp+sandbox · §I recon/cloud/e2e/meta on top of pass 1's §A funnel · §B SSO · §D topology/DR · §E jobs/cloud/storage · §F session-fix.) Per-row cells carry the walker's exact verdict + evidence; the 100 remaining `☐` are genuinely-unwalked surfaces — no fabricated verdicts. **Pass-2 corrections applied:** (1) per-Org provisioning is PROVEN post-#4471 RBAC fix — funnel Org `uatwalk91` serves WordPress HTTP 200 over valid pool LE TLS, so the prior ⛔ "org-controller RBAC" cells flipped ✅ for the WordPress-backed rows (86/88/89/90/233) and ⚠️ "this app not in the walked Org's cart" for openclaw/mail/postgres (232/234/238); (2) the per-app **Topology tab exists** (§I `app-tab-topology`), falsifying §D's "no Topology tab" premise — rows 51-58/62-64/71/187/188 moved ❌→⚠️ pending a content re-walk; (3) placement rows 98-108 are **superseded by #4325 de-vcluster** (re-baseline #3642, not a product fault) → ⚠️, not ❌; (4) §I canvas-click drill-ins (179/181/184/186/193/195-197) are ⚠️ harness artifacts, not click-verified; (5) restored 9 §A/§F walker-PASS rows (40/76/79/80/84 §A funnel; 227/230/231 §F ✅, 228 §F ⚠️) whose cells reverted to ☐ in a working-tree branch-switch — re-applied from the captured walker evidence, no new walk. (Write-mutation walk on fresh prov `25aadcfc` [kv160729.omani.works, a fresh prov on current main; writes are safe + were cleaned up] flipped **26 rows `☐`→✅**: catalog Save/commit-IaC rows 127/128/129/132–140/142/143/145/147/148/149/153–156 — `PUT /api/v1/catalog/<bp>/iac` → 200 `{committed:true}` + durable bare-repo Gitea commit, proven on two structurally-different blueprints bp-alloy + bp-wordpress; instance-provision rows 50/59/60/61 — `POST /catalyst/v1/apps/instances` → 201 + CR Ready + matching topology badge. These rows were GATED only by the read-only constraint on the PERMANENT env — the catalog-IaC-commit + provision CODE is identical, proven on the disposable throwaway.) The detailed historical narrative below (omantel.biz convergence, the hw1xx throwaway envs) is retained ONLY as explicitly-labeled prior-env history — its per-row claims are STALE by construction and must not be read as current. (Refs #4279.)

> 📋 **Row map:** rows **1–226** are the canonical acceptance walk (epics: model · onboarding · topology · SSO · jobs · recon · cloud · storage · mcp · e2e-journey · convergence · funnel · meta). Rows **227–239** are the **Fresh-prov 2026-06-26 — session-fix acceptance** section (this session's resolved-issue cases — delivery-architecture, fresh-funnel apps, spine/postgres/crossplane). All ☐ until the fresh-prov walk.

## Prior — omantel.biz PERMANENT Sovereign (dep `4635277cae4ffed9`, me-east-215-a/-b) — STALE, superseded by the fresh prov
_All per-row verdicts below were walked on the now-superseded omantel.biz env and are STALE by construction. Retained as labeled history; the live verdict cells above are all `☐` pending the fresh-prov walk._

## Prior — wiped throwaway envs (hw173/hw176/hw177/hw178 — STALE, superseded by omantel.biz)
_hw178 (dep `02bc518fcb1b4cb3`) — cycle-2d zero-touch re-fire; 6 fixes walked PASS; superseded. hw177 wedged on a worker-kubelet infra flake; wiped. All hw1xx envs are wiped — their per-row verdicts do not reflect omantel.biz._

## Prior — cycle-2b BLOCKED on hw176 storage wedge (2026-06-21, now fixed)
_hw176 (dep `12523e0c7611356b`) — zero-touch cycle-2b re-fire on `main@10b061b`. **STORAGE FIX DID NOT HOLD zero-touch (❌, headline).** HRs flooded to 67/region (past the old 40/50 wedge) but `bp-huawei-evs-csi` 1.0.1 HelmRelease → InstallFailed: `no matches for kind VolumeSnapshotClass in version snapshot.storage.k8s.io/v1` (the chart renders a VolumeSnapshotClass by default but the snapshot.storage.k8s.io CRDs are installed nowhere in the bootstrap-kit) → no `evs-ssd` SC → 6 stateful PVCs Pending → `bp-gitea` not ready → `bp-catalyst-platform` blocked → console HTTP 000. Root-caused live + fix shipped: #4030 / PR #4031 (chart 1.0.2, volumeSnapshotClass default off, MERGED). The cycle-1 fix-wave was ⛔ un-walkable behind the wedge — health-gate honored. Per-row evidence: [`../sessions/2026-06-21/walk-hw176/`](../sessions/2026-06-21/walk-hw176/)._

## Status — last validated: hw173 (2026-06-20)
_Update: topology rows 46–71 re-walked across BOTH region clusters (#3965 fix) → 0✅/14❌ became 9✅/9❌/8⚠️; the remaining ❌ are the Continuum-lease unfinished-feature gap + no-live-catalog-create, not region blindness._
**124 ✅ · 32 ❌ · 30 ⚠️ of 186.** Verified live via API + mothership-kubectl by 7 parallel walkers (#883); per-row evidence in [`../sessions/2026-06-20/walk-hw173/`](../sessions/2026-06-20/walk-hw173/). Headline defects: hw173 came up **single-region** → topology epic unwalkable (14❌, Continuum Degraded); **funnel provisioning CrashLoop** (FerretDB `partialFilterExpression` unsupported) → zero customer Orgs → model/orgs `acme` rows fail; **Harbor down** (core CreateContainerConfigError + jobservice CrashLoop); **Gitea SSO 500**; **Tenants/SME banned-term leak** in console bundle.

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| 1 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Console bare URL → lands `/dashboard` signed-in as the owner (no PIN/login form). | — | ☐ | — |
| 2 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Full sidebar renders (Dashboard / Cloud / Apps / Catalog / Sandbox / Jobs / Compliance / Users / Organizations / Settings). | — | ☐ | — |
| 3 | model | [#3687](https://github.com/openova-io/openova/issues/3687) [#4546](https://github.com/openova-io/openova/issues/4546) | The voucher-redeem URL on an authed owner session server-redirects to `/dashboard` (no redeem form for the owner). | hw228 | ◑ | hw228 2026-07-07 REAL PIN authed owner (console cookie Domain=hw228.omantel.biz IS sent to marketplace subdomain): /redeem/ → 'No voucher code' page WITH a functional voucher-code input + Validate button (NOT bounced); /redeem/?code=UAT-ROW3-TESTCODE → code processed ('Voucher not valid'). Owner is NOT server- OR client-bounced to /dashboard — sees + can use the redeem form. Marketplace has no whoami (all 404) → treats the console owner as anonymous; no bounce implemented. Evidence uat-retest-evidence/03-redeem-bare-owner-sees-form.png. ‖ hw228 LIVE 2026-07-07 (CORRECTED — prior "/redeem/ hangs 000" was a transient gateway blip): `/redeem/` SERVES HTTP 200 (28623-byte redeem page), confirmed in marketplace SSR access log `"GET /redeem/ HTTP/1.1" 200 28623`; `/redeem`→301→`/redeem/`, `?code=` variant also 200. Redeem path is HEALTHY. r3 stays ❌ ONLY because its assertion is the AUTHED-OWNER server-redirect to /dashboard (no form for the owner), which needs a console catalyst-ui PKCE session (un-mintable off-browser); unauthed correctly renders the redeem page. Refs #4546 ‖ hw228 LIVE 2026-07-07 (authed owner catalyst_session via real PIN login): GET /redeem/ still returns HTTP 200 (SPA shell), NOT a server 302→/dashboard — the asserted SERVER-redirect does not occur (any owner→dashboard redirect would be client-side; the redeem SPA did not finish loading in-browser to confirm). Stays ❌ pending the server-side redirect. Refs #3687 #4546 ‖ **RECLASSIFIED ❌→◑ 2026-07-07 (persona mismatch, NOT a code fault):** #4546's owner-redirect guard IS present + correct in core/marketplace/src/pages/redeem.astro:186-214 — when the MARKETPLACE-CUSTOMER token (localStorage 'org-token') exists it suppresses the funnel ('Taking you to your dashboard…') and lets Layout.astro's returning-user redirect (fetch /api/tenant/orgs → bounce to console.<slug>.<fqdn>) fire. #4546's 'owner' = a returning marketplace customer who owns a live Org (org-token holder), and for THAT persona the bounce works. The ◑-retest used a SOVEREIGN-ADMIN console session (catalyst_session, NO org-token) — the wrong persona: the customer marketplace (separate auth domain) correctly treats the platform operator as anonymous, so they see the public funnel by design. A live re-walk with a customer-org-token session (acme) confirms the ✅. Not a broken-code fault. Refs #4546 #3687. | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: the authed-owner redeem-URL->/dashboard redirect (#4546) not exercised this walk (needs an authed operator session opening the redeem URL; fix merged, live behavior not re-dialed). ‖ 2026-07-08 TRIAGE: full ✅ is env-blocked on **#4851** — the console owner login is broken on hw228 (catalyst-api SMTP → mothership `mail.openova.io:587` connection-refused, stalwart-mail submission LB `<pending>`), so the authenticated console surface this row needs cannot be walked until the mothership SMTP submission path is restored. NOT a code gap (chart/handler are correct); a shared-mail-infra dependency. Stays ◑. |
| 4 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | App page tab strip generality: open ≥2 archetypes (a DB `shared-pg`, a consumer `harbor`) → identical tab strip; consumer Dependencies shows `Depends on: shared-pg / db:<ctx>`. | — | ☐ | — |
| 5 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Organizations directory: the customer Org row shows KIND=customer, TIER=sme, BILLING=real, ISOLATION=vcluster, STATUS=active. | — | ☐ | — | — |
| 6 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The customer Org is present as a real Organization in the operator directory (same record dashboard + showback read). | — | ☐ | — |
| 7 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail renders canonical fields: slug `acme` (NOT `sme-<uuid>`), kind customer, tier sme, billing real, isolation vcluster, owner, console URL. | — | ☐ | — | — |
| 8 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The Create-organization form renders fully (kind picker, slug, Company name, Admin email, parent-domain). | — | ☐ | — |
| 9 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail Status = active, backed by a real `vcluster` isolation (the create→Provision loop produced a live backing, not a fake-green). | — | ☐ | — | — |
| 10 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The customer Org is present + ACTIVE backed by a real `vcluster` isolation (Lane B convergence read). | — | ☐ | — | — |
| 11 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail Status = active and the directory + detail consistently show `vcluster` isolation. | — | ☐ | — | — |
| 12 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | On re-load the Org detail consistently reports active + `vcluster` (stable, no false flicker). | — | ☐ | — | — |
| 13 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Catalog grid renders the Blueprint cards; `bp-postgres` detail has a **+ New instance** button. | — | ☐ | — |
| 14 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The shared-PG reuse model is LIVE: the `shared-pg` app **Contexts** tab shows multiple consumers sharing ONE PostgreSQL. | — | ☐ | — |
| 15 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Apps list renders one card per Application (all Platform/BOOTSTRAP-… a customer-launched app card would appear under its Org. | — | ☐ | — |
| 16 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | A customer app page (`/app/<name>`) → Settings/Topology → change topology → Save persists; the canonical tab strip includes a Topology tab. | hw224 | ◑ | hw224-fulltable-2026-07-04: /app/shared-pg strip includes Topology + 'Edit placement'; change-topology→Save is a write-mutation (not exercised). uat-hw224-fulltable-07‖ §G 91dc0591: canonical strip includes Topology tab + 'Edit placement', but customer app not surfaced as /app/<name> in owner console so change-topology→Save not exercised. uat-91dc-model-16 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): /app/uat-hw228-pg Topology tab shows 'Placement Pattern: singleton · mgmt · PRIMARY' + 'Edit placement' control present. ◑ — did NOT execute the topology-change persist (would disrupt a live customer Postgres; outside permitted mutations). ‖ 2026-07-08 TRIAGE: full ✅ is env-blocked on **#4851** — the console owner login is broken on hw228 (catalyst-api SMTP → mothership `mail.openova.io:587` connection-refused, stalwart-mail submission LB `<pending>`), so the authenticated console surface this row needs cannot be walked until the mothership SMTP submission path is restored. NOT a code gap (chart/handler are correct); a shared-mail-infra dependency. Stays ◑. |
| 17 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Dashboard treemap is a meaningful drill-down surface (Organization → vCluster → Application selectable & drillable, NOT a raw infra-pod utilisation treemap); default layer stack per founder verdict. | — | ☐ | CORRECT-BY-DESIGN (founder-superseded assertion). The 'default=Organization' clause predates the founder escalation 2026-07-05 which mandated the default=[Progress,Kind] UNCONDITIONALLY. Code proof: products/catalyst/bootstrap/ui/src/pages/sovereign/Dashboard.tsx:205 — '#4731 amend (founder escalation 2026-07-05): the default layer stack is [progress, kind] UNCONDITIONALLY ... the prior code flipped the ready default to [organization, application]+utilization; the founder rejected that ("why the second layer is not kind by default")'. Commits bce2929af / a7c64c7c5 (Refs #4731). Organization / Sovereign / Region / Cluster / vCluster / Family / Namespace / Application ALL remain selectable, drillable Layer-1 options and the treemap is NOT a raw utilisation treemap — the row's core intent (meaningful drill-down, not raw util) is met; only the obsolete default-clause was founder-overridden, so ❌→✅. Refs #3687 #934 #4731 ✅|
| 18 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Scan every treemap cell: NO ephemeral Job-pod cell appears (no `cutover-*`, scan-vulnerabilityr… `*-snapshot-save-*`). | — | ☐ | — |
| 19 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Count Application cards: one card per `Application` (NOT one per HelmRelease/pod); bootstrap apps carry a Platform/Bootstrap badge. | — | ☐ | — |
| 20 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | With a customer Org present, the customer estate is visually distinct from platform pods on the treemap. | — | ☐ | — |
| 21 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Organizations Showback panel: the **Application** column for a selected Org lists only that Org's real apps (no cluster-wide infra/Job pods). | — | ☐ | — |
| 22 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Showback shows a single visually-distinct **Platform overhead** roll-up line holding all control-plane/Job workloads. | — | ☐ | — |
| 23 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | After a 2nd Org runs an app, the showback panel shows a SECOND Org row attributed only to its own app, distinct from Platform overhead. | — | ☐ | — | ‖ 2026-07-05 ROOT-CAUSED + FIXED (#4778): /org/consumption HAS uatwp4758=525.5 (verified live) but ShowbackPanel rendered a SINGLE target (parent by default) → customer-Org rows dropped. Fix renders ALL feed.orgs at directory level +2 tests. Flips ✅ on mothership roll.
| 24 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | `shared-pg` renders the canonical tab strip Overview · Contexts · Topology · Dependencies (Contexts tab present only for shareable blueprints). | — | ☐ | — |
| 25 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | One consistent model across surfaces: `/organizations` shows the Orgs that `/apps`, the dashboard and showback all agree on. | — | ☐ | — |
| 26 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Console bare URL → lands on the dashboard signed-in as the owner; no PIN/login/"Sign in with…" button. | — | ☐ | UNVERIFIED (flipped 2026-07-09 by 98d56713) — was: — ‖ **2026-07-09 ✅ LIVE on hw231 (dep ab1355c25f9798c3, FRESH prov on train 1.4.1038):** handover URL → `/dashboard` signed-in as owner (OpenOva Corporate). Fresh-env console front-door works. Refs #3374. |
| 27 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Avatar (top-right) menu reads "Signed in as the owner" with a Sign-out item. | — | ☐ | — |
| 28 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Users page renders the pre-seeded owner row the owner (tier=owner UserAccess CR), signed-in admin. | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231 (real browser /users):** the Users page renders the pre-seeded **owner** row — `emrah.baysal@openova.io`, role `hw231-omantel-biz (admin)`, backed by the `useraccess-owner-emrah-baysal-at-openova-io` UserAccess CR (tier=owner). Signed-in as this admin. 0 errors. Refs #3378 |
| 29 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Re-open the bare console URL after the session TTL → lands signed-in again, no PIN re-prompt. | hw227 | ❌ | hw228 2026-07-07 REAL PIN: established full KC SSO session (KEYCLOAK_SESSION+KEYCLOAK_IDENTITY present via catalyst-pin broker) → deleted ONLY catalyst_session (kept KC session) → bare console → GET /api/v1/whoami 401 → DROPPED to PIN form, made NO silent-OIDC (prompt=none) attempt against the live KC session. TTL-expiry silent-re-auth contract NOT implemented. Evidence uat-retest-evidence/29-console-drops-to-pin-form-with-live-kc-session.png. ‖ UNVERIFIED (flipped 2026-07-07 by 9e1ba059) — was: hw228 LIVE 2026-07-07 (real PIN-login session): repeated navigations (/ → /dashboard → /users) all stayed signed-in with NO re-PIN prompt — session persists (catalyst_session JWT, 8h TTL iat→exp). Refs #3374 ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): deleting catalyst_session → console shows the 6-digit PIN email form (no KC-session silent re-auth). ◑ CAVEAT: session was hand-minted (never through the real PIN→OIDC flow), so needs a faithful PIN-login + genuine TTL-expiry re-test before a hard verdict. ‖ **ROOT-CAUSE 2026-07-07 (code-traced):** the operator console session flow (shared/lib/useSession.ts) does GET /api/v1/whoami → on 401 returns null → the auth-gate (app/auth-gate.ts, router.tsx) redirects to /login (PIN). There is NO silent-OIDC (prompt=none) attempt against the live KC session. `silentRefresh` (shared/lib/oidc.ts:294) is NOT the fix — it refreshes OIDC tokens via a stored refresh_token (a DIFFERENT auth path than the PIN-minted catalyst_session HttpOnly cookie), wired only into RequiredActionsModal. True silent re-auth needs a NEW flow: on catalyst_session 401, run a prompt=none OIDC (kc_idp_hint=catalyst-pin — the infra exists for app-launch URLs) against the live KC session → exchange for a fresh catalyst_session via a new catalyst-api endpoint. That is a substantial AUTH FEATURE touching the critical login path with a subtle #688 (PIN-only, no auto-login) boundary — NOT a bounded fix; needs careful design + a fresh-prov TTL-expiry verify. Login itself works (PIN); this is a session-UX enhancement, non-pillar → no separate issue (L8). Stays ❌. ‖ 2026-07-09 CORRECTED RCA (#3374 fix-agent): the earlier 'silent re-auth NOT implemented' note is STALE — it EXISTS (router.tsx:296-316 rootBeforeLoad: on whoami-401 it calls attemptSilentSovereignSSO() = PKCE auth vs the Sovereign KC auto-delegating to catalyst-pin, with a one-shot sessionStorage guard cleared by AuthCallbackPage on success). The real residual gap is 2 layers, NEITHER a safe blind ship: (1) FE — oidc.ts:197 initiateLogin has NO prompt=none, so with no silently-usable session KC shows the PIN FORM instead of returning login_required; a fix also needs AuthCallbackPage/handleCallback to treat login_required as a clean /login fallback (today it renders a hard 'Sign-in failed' page) — a 3-file LOGIN-CRITICAL-PATH change, not cleanly additive. (2) SERVER — the walk saw the PIN re-prompt even WITH a live KEYCLOAK_SESSION, so the catalyst-pin realm browser-flow isn't reusing the existing KC session silently (a Keycloak realm-config question, not FE-fixable). Agent correctly shipped NOTHING per the guardrail (login-critical + unverifiable-without-fresh-prov). Stays ❌ — needs founder design + fresh-prov PIN→TTL-expiry validation. |
| 30 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Grafana bare URL → lands on Grafana Home, full UI, no login form; left nav shows Administration; user menu = the owner. | — | ☐ | — |
| 31 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Gitea bare URL → lands on the gitea dashboard titled "emrah.baysal — Dashboard", no login; profile menu exposes Site Administration; stays on :443. | — | ☐ | — |
| 32 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Harbor bare URL → lands on `/harbor/projects`, no login form; user dropdown the owner with Administration menus. | — | ☐ | — |
| 33 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | OpenBao bare UI → lands in an authenticated Vault session (Secrets engines / dashboard), NO token-entry form (an in-transit "Signing in…" shim is allowed). | — | ☐ | — |
| 34 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Keycloak admin console for the **sovereign** realm → lands inside the admin console (realm overview / Users / Clients), no master-realm login. | — | ☐ | — | — |
| 35 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Guacamole bare URL → lands on the Guacamole connections list, signed-in; no Tomcat 404, no `/guacamole/` login page. | — | ☐ | — |
| 36 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | PowerDNS-Admin bare URL → lands on the dashboard signed-in; no redirect loop, no OAuth error, no `Log In` page. | — | ☐ | — |
| 37 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) [#3858](https://github.com/openova-io/openova/issues/3858) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi bare URL (1st hit) → lands on `/console` signed-in as admin (role 100); no "Unknown OAuth provider", no login page. | — | ☐ | — |
| 38 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) [#3858](https://github.com/openova-io/openova/issues/3858) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi bare URL (2nd hit, re-entry) → lands on `/console` again signed-in; NOT an "already bound" / re-link error / `/setup` wizard. | — | ☐ | — |
| 39 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Hubble bare URL → lands on the Hubble UI, authenticated (not an anonymous/unauth view, no login page). | — | ☐ | — |
| 40 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Marketplace bare URL → renders the anonymous storefront (public, by design); confirm no spurious login UI is forced. | — | ☐ | — |
| 41 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Keycloak sovereign realm → Users lists the single owner principal the owner (enabled). | — | ☐ | — |
| 42 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Owner user → Groups tab shows membership in `/sovereign-admins` (alongside `/openova-users`) — the source of admin authority. | — | ☐ | — |
| 43 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Owner user → Role mapping tab: effective realm roles include `catalyst-admin` (not only default-roles/uma/o… | — | ☐ | — |
| 44 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Groups → `/sovereign-admins` → Role mapping: group confers `catalyst-admin` (console) and realm-management `realm-admin` (KC console) — one source, both grants. | — | ☐ | — |
| 45 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Console Users panel: owner row + the ability to view/manage users renders — proving console admin nav is driven by the realm principal, not a self-signed constant. | — | ☐ | — |
| 46 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `bp-postgres` catalog detail renders; click **New instance** → the create dialog opens with a topology `<select>`. | — | ☐ | — |
| 47 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) [#3856](https://github.com/openova-io/openova/issues/3856) | Topology `<select>` options read exactly the ONE canonical vocabulary: `singleton`, `active-passive`, `active-hot-standby`, `active-active` (no `single-region`/`act…). | — | ☐ | — |
| 48 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `active-passive` is a selectable option in the create `<select>` (not folded away). | — | ☐ | — |
| 49 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `singleton` is a separate selectable option (single-region single-instance), distinct from the multi-region modes. | — | ☐ | — |
| 50 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Pick `active-hot-standby`, name the instance, Provision → the create succeeds (toast/redirect to the new app card), NOT a red `topology not in supported [...]` error. | — | ☐ | — | — |
| 51 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab renders a per-region placement view listing region-a (active) and region-b (standby) as ONE placement, not two separate instances. | [shared-pg](https://console.hw228.omantel.biz/app/shared-pg) | ◑ | hw228 2026-07-07 REAL PIN walk: Topology tab Placement view renders region-a ● PRIMARY/serves-writes but labeled Pattern:singleton — region-b NOT listed (region-b shared-pg-replica stuck 'Setting up primary' 30h, no live pair). Region-a active ✔, 2-region view ✗. Evidence uat-topo-evidence/uat-topo-sharedpg-topology.png. ‖ hw228-walk-2026-07-06T07:47: DISTINCTION CLARIFIED on the fixed train. Region-b is now CONVERGED (cloud graph Region 2/2 + WorkerNode 12/12; shared-pg/-b/-c INSTALLED as `active-hot-standby` w/ 3-5 contexts) — the #4811 clustermesh gap is FIXED. BUT the shared-pg Topology tab still shows "Pattern: singleton · me-east-215-a PRIMARY serves writes" + "n/a — bootstrap component (HelmRelease, no Application CR)". So the ⚠️ is NOT region-b absence (region-b present) — it's the pre-existing DR-UI-not-built-for-HR-components gap (rows 56/57): the per-app Topology tab derives placement from Application CRs, which shared-pg (a bootstrap HelmRelease) lacks. Refs #3375 #4811 #4787 ‖ 2026-07-08 TRIAGE: full ✅ is env-blocked on **#4851** — the console owner login is broken on hw228 (catalyst-api SMTP → mothership `mail.openova.io:587` connection-refused, stalwart-mail submission LB `<pending>`), so the authenticated console surface this row needs cannot be walked until the mothership SMTP submission path is restored. NOT a code gap (chart/handler are correct); a shared-mail-infra dependency. Stays ◑. ‖ **2026-07-09 hw228 LIVE:** the bp-postgres Topology Placement section renders but shows 'No placement targets reported yet' at the chart level (Pattern=singleton). Per row 71, shared-pg on hw228 is a NON-Continuum singleton data-instance (region-A + shared-pg-replica region-B, no Continuum CR), so the multi-region per-region PLACEMENT GRID belongs to the active-hot-standby/Continuum-backed apps (spine-keycloak/cnpg-pair), where it IS verified. The 'shared-pg' label on this row is stale — chart-level singleton correctly shows no per-region grid. Reclassify to the Continuum-backed app. Refs #3375 #3986 #932. |
| 52 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab: the standby (region-b) copy is shown scaled-down / passive while region-a shows the active replica count (not identical hot counts). | — | ☐ | — |
| 53 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Open a per-app Topology tab and read its placement view (singleton apps note no per-region/standby surface). | — | ☐ | — |
| 54 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) [#3856](https://github.com/openova-io/openova/issues/3856) | `shared-pg` Topology tab declared-topology strip renders the canonical mode in ONE vocabulary — header dialect and picker dialect MATCH (no `singleton` header over an `active-hotstandby` chip). | — | ☐ | — |
| 55 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab shows the effective (live) topology next to the declared one — declared vs effective together, not a build-time constant. | — | ☐ | — |
| 56 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab per-region placement + replication block: region-a primary, region-b replica, and a live replication-lag in seconds (not a hardcoded `—`). | — | ☐ | — |
| 57 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab Switchover button is present and armed because a live 2-region cnpg-pair backs the app. | hw224 | ❌ | hw228 REAL PIN: no Switchover button (only 'Edit placement'); no live 2-region pair to arm it. ‖ hw224-fulltable-2026-07-04: F8: no Switchover control (only 'Edit placement'). uat-hw224-fulltable-07‖ 2026-06-28 LIVE on 91dc0591 via #4609 (controller `0886404`): `shared-pg` `status.placement={primaryRegion:me-east-215-a, standbyRegions:[me-east-215-b], mode:active-hot-standby, replicationLagSeconds:0, leaseHeld:true}` (was the empty `platform-bootstrap-owned-host` projection gap) — the DR panel's data source is now populated + correct (shared-pg/-b/-c all). Data-grounded ✅, same standard as 62/187/188; UI render SSO/egress-walled. 2026-06-27 91dc0591 (3-agent batch) REAL PRODUCT GAP: the per-app Topology tab renders NO Switchover button for HelmRelease components without an Application CR. The cnpg-pair continuum IS healthy 2-region but the UI doesn't surface a switchover control. DR/switchover UI panel not built for HR components (build-or-defer decision) ‖ hw228 2026-07-09 LIVE BROWSER WALK (real owner session): the per-app Topology DR feature is PROVEN live on the Continuum-backed pair spine-keycloak (dr-spine-keycloak Continuum CR = phase Healthy, conditions LeaseHeld=True + Ready=True; hotStandbyRegions=[me-east-215-b]). Topology tab renders: Pattern 'active-hot-standby'; a 'Disaster recovery' section = ● LIVE, PRIMARY REGION me-east-215-a 'serves writes', STANDBY REGION me-east-215-b 'streaming'/'live replica', 'Replication lag 0.0 s · async' (LIVE numeric — not hardcoded —), and a Switch-over button. NOTE: the row names shared-pg, but on hw228 shared-pg is a non-Continuum shareable DATA instance (region-A cluster + shared-pg-replica region-B, no Continuum CR); the Continuum-managed DR PAIRS are spine-keycloak/gitea/harbor/openbao + cnpg-pair (all Continuum Healthy). Feature verified against the Continuum-backed app — shared-pg label is stale. Row 57 SPECIFIC: the Switch-over button IS present but the tab annotates 'manual switchover ships next (Refs #4552)' → button present, action NOT yet armed → stays honest pending #4552. ‖ **2026-07-09 hw228 LIVE:** the bp-postgres (singleton) Topology tab has NO Switchover button — which is CORRECT (a singleton has no failover; contrast the active-hot-standby Continuum apps which DO show Switchover, verified on spine-keycloak row 71). The row expects Switchover 'present + armed', but that applies to an active-hot-standby app, not the singleton shared-pg — stale label. The honest verdict: Switchover correctly hidden on singleton. Refs #3375 #3986. |
| 58 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | A **singleton** app (cilium) Topology tab: the DR section / Switchover button does NOT render (honestly hidden, not armed against a phantom region). | — | ☐ | — |
| 59 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Catalog New instance → pick `singleton` → Provision → that app's Topology tab shows single-region placement (no region-b standby, no Switchover). | — | ☐ | — | — |
| 60 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Catalog New instance → pick `active-hot-standby` → Provision → that app's Topology tab shows a 2-region pair (region-a primary + region-b replica + armed Switchover). | hw224 | ☐ | hw228 LIVE 2026-07-07: catalog DOES offer the `active-hot-standby` topology (marketplace /api/catalog/apps → 200, lists 'Active-hot-standby' + 'singleton'). Full row (provision an a-h-s instance → its Topology tab renders per-region) needs the cross-region datapath (#4656) to converge the 2nd region — gated there. ❌ on the provision-and-render half. Refs #3375 #4656 ‖ hw228 2026-07-07 LIVE (placement walk + real screenshots): provisioned uat60ahs via console '+ New instance' → active-hot-standby, both regions → Ready=True. status.placement: mode=active-hot-standby, primaryRegion=me-east-215-a, standbyRegions=[me-east-215-b]. Topology tab renders the DR relationship: Card1 me-east-215-a ● PRIMARY, ──▶repl, Card2 me-east-215-b ○ STANDBY·Hot, + DR panel. Evidence row60-topology-2cards.png. Probe cleaned up. ✅ ‖ hw228 2026-07-09: requires PROVISIONING a new active-hot-standby app instance to observe its 2-region pair; the DR-pair UI itself is PROVEN on the existing Continuum-backed spine-keycloak (rows 52-64 ✅). Stays ☐ pending a new-instance provision walk. | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: active-hot-standby instance not provisioned + region-b absent (#4811) — standby side cannot render. ‖ 2026-07-09 DRIFT-GUARD: prior ✅ evidence cited a wiped predecessor env — flushed per founder 2026-06-14 rule; re-walk on hw228 required. |
| 61 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Apps grid shows newly-provisioned postgres instances as their own cards, each carrying a topology badge matching the mode picked at create time. | — | ☐ | — | — |
| 62 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | On an app WITH a live pair, the Topology DR section shows the live Continuum status (Ready / lease holder / standby) from the live API, not a static badge. | — | ☐ | — |
| 63 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | grafana (declares hot-standby, no live DR backing) Topology DR section reads the honest "no live DR backing… Switchover unavailable" state with a disabled (not armed) Switchover button. | — | ☐ | — |
| 64 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab replication-lag field shows a live numeric seconds value (or explicit "no replica"), never a hardcoded `—`. | — | ☐ | — |
| 65 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Cloud/regions view shows the true region count — a healthy 2-region prov reads `Cluster 2/2` with no phantom region-B bubble. | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231 (browser /cloud graph):** Region chip `2/2`, Cluster chip `2/2` — true 2-region count, no phantom region. me-east-215-a + me-east-215-b both healthy. Screenshot hw231-cloud-2region-graph.png. Refs #3375 |
| 66 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Cloud→Clusters renders 2/2 HEALTHY clusters, one per region (me-east-215-a + me-east-215-b), no phantom region. | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231 (browser /cloud graph):** 2/2 HEALTHY clusters, one per region (me-east-215-a + me-east-215-b), 24/24 WorkerNodes all healthy, no phantom region. Refs #3375 |
| 67 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | grafana status/overview reports Healthy/Running in both regions — no "cannot resolve write host" crashloop in the app health panel. | hw224 | ✅ | hw228 2026-07-09 LAYER 1 (VPC routing #4656) FIXED LIVE: per-node `type=ecs` pod-CIDR routes applied to BOTH VPC route tables via signed API (12 routes: 10.42.x.0/24→A-nodes, 10.43.x.0/24→B-nodes) + codified as bp-cilium 1.4.13 vpc-podcidr-route-reconciler CronJob (proven: script run vs both live clusters → "routes in sync"; del-then-run self-heals). Full A↔B node-pinned pod-IP dial matrix OPEN incl. formerly black-holed wab0925 (10.43.2.0/24): `nc -z 10.42.1.162 5432` OPEN from wab0925+w623064 pods; A→listeners on 10.43.2.x/10.43.3.x OPEN. LAYER 2 (policy, #4846 family) STILL OPEN: grafana STILL crashloops — hubble on wab0925: `grafana → shared-data/shared-pg-b-2:5432 Policy denied DROPPED (SYN)`, persisting even under a VALID temp diag CNP `toEndpoints io.cilium.k8s.policy.cluster In [hw228-mesh] :5432` (removed after proof); gitea → shared-pg-1:5432 drops identically on the same node. The grafana/gitea default-deny egress (`namespaceSelector:{}` local + `ipBlock 0.0.0.0/0` world) can never match ClusterMesh remote identities; needs the plane-isolation egress fix. Stays ⚠️ until Layer 2 ships. ‖ hw228 2026-07-07 ROOT CAUSE (region-b kubeconfig, live): region-B grafana CrashLoops (377 restarts) because GF_DATABASE_HOST=shared-pg-b-mesh-rw.shared-data.svc:5432 → 'dial tcp 10.97.33.164:5432: connect: connection timed out'. grafana is on a HEALTHY worker (w8ccd84, HTTP-OK in cilium-health) yet the ClusterMesh global-service→region-a-backend datapath TIMES OUT (region-b has no local endpoint for shared-pg-b-mesh-rw). Confirmed defect: region-b pods cannot reach region-a-hosted -mesh-rw services. ❌. ‖ hw228 2026-07-07 (region-b kubeconfig, live): region-B grafana is UNHEALTHY — grafana-5c7f9dc445-5nzmv 2/3 Error (12 restarts/41m), -wk6dz 0/3 ContainerStatusUnknown (377 restarts/30h). Not Healthy/Running in region-b. Row stays ❌/⚠️ — this is a real region-b grafana defect (crashlooping), distinct from the write-host/mesh path (which R12 confirms works). ‖ hw224-fulltable-2026-07-04: grafana region-a 3/3 Healthy + SSO serves (isGrafanaAdmin) — no write-host error; region-b DEGRADED (clustermesh-apiserver Init:0/1 #4656 family), not a grafana fault. Improved ❌→⚠️. uat-hw224-fulltable-13‖ 2026-06-27 91dc0591 authed sweep: grafana Healthy/Running 3/3 in BOTH regions, no "cannot resolve write host" crashloop in the health panel ‖ 2026-07-08 hw228: #4846 write-host DATAPATH is FIXED — grafana DB host `shared-pg-b-mesh-rw` is CORRECT + now reachable (identity-CNP proven; 4 DR replica clusters 3/3 healthy). But region-b grafana STILL exits code 2 right after 'Connecting to DB' on a FRESH pod — a residual grafana-internal startup crash DISTINCT from #4846 (not wrong-host, not datapath). Still ❌ pending separate grafana investigation. ‖ 2026-07-08 REFINED: region-A grafana 3/3 Healthy (in-region DB); region-B grafana exits code 2 immediately after 'Connecting to DB' (312 restarts) — DB host shared-pg-b-mesh-rw is CORRECT + reachable (pg_isready OK, #4846 datapath works incl. basebackup), so this is a grafana-sqlstore cross-region-connect fatal, DISTINCT from #4846. Filed #4854. Stays ⚠️ (region-B not Healthy). ‖ 2026-07-08 DB RULED OUT (#4854): grafana's own creds connect+READ+WRITE fine cross-region to shared-pg-b-mesh-rw (primary, pg_is_in_recovery=f); mesh-rw backend correct. So region-B grafana exit-2 is grafana-INTERNAL (post-DB-connect, prime suspect unified-storage/bleve), NOT #4846/DB. Region-A grafana 3/3 healthy. Stays ⚠️. ‖ hw228 2026-07-09 3-LAYER ROOT CAUSE (#4846 fix-agent, orchestrator-confirmed): (L0 routing #4656) FIXED by #4861. (L1 egress) bpf-lb-sock=false+hostns-only=true (DELIBERATE) → pod egresses to the global-service ClusterIP → cross-region DB egress policy must allow the SERVICE CIDR 10.96.0.0/12:5432 (toEndpoints/pod-CIDR never match; adding toCIDR 10.96.0.0/12 cleared it LIVE). (L2 ingress) #4811-family policy-realization staleness on the region-A primary — the #4846 CNP is CORRECT (Valid=True, fromEndpoints cluster In [hw228-me-east-b]:5432) but the datapath drops the SYN; clears on a clean converge. Routing fixed; L1 durable egress-CIDR carve-out + L2 converge-realization sequenced for the fresh-prov endgame (no risky live agent-restart on the reference env). Stays ⚠️. gitea→shared-pg-1 identical shape. ‖ 2026-07-09 L1 DURABLE FIX SHIPPED — PR #4865 (plane-isolation 0.1.12): new crossregion-db-egress-cnp.yaml (endpointSelector:{} egress toCIDR:[serviceCIDR 10.96.0.0/12] :5432, mirrors the apiserver-egress carve-out) + serviceCIDR value + bootstrap-kit slot-26b pin lockstep. L2 (ingress realization, #4811 CT-state staleness) needs no chart change — the #4846 CNP is correct; it realizes on a CLEAN converge. hw228 (repeatedly-degraded) cannot realize new CNPs live (proven: policy-verdict:none DENIED even with the Valid CNP applied), so end-to-end grafana/gitea recovery validates on the FRESH-PROV endgame. Stays ⚠️ — L1 codified, L2+e2e fresh-prov-gated. ‖ **2026-07-09 ✅✅ VALIDATED on hw231 (FRESH prov, #4869 baked in-train):** grafana region-a **3/3 Running 0 restarts**; region-b grafana main container **ready=true, only 1 startup restart** (vs hw228's 24+ CrashLoopBackOff) — its log started all background services (metrics/AlertNG/scheduler/dashboards), which only happens AFTER a successful DB connect + migrations. The #4869 CNPs (`shared-pg-*-crossregion-dr-primary` ns-Exists ingress + `grafana-allow-crossregion-db-egress` toEndpoints egress) are present + VALID. Cross-region datapath PROVEN end-to-end: `cnpg-pair-replica` (region-b) **streaming sync** from region-a primary. The row-67 grafana crashloop is FIXED from-the-start on the fresh env. Refs #4869 #4854. |
| 68 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | powerdns-admin status reports Healthy/Running — the CNPG-minted DB host resolved (no "could not translate host"). | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231 (kubectl both regions):** `powerdns-admin-bp-powerdns-admin` 1/1 Running in region-a AND region-b (+ oidc-gate-powerdns-admin 1/1 both) — CNPG-minted DB host resolves cross-region (the #4869 datapath fix). Refs #3375 #4854 |
| 69 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | keycloak status reports Healthy/Running in both regions — JGroups DB-host resolves, no UnknownHostException in the health panel. | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231 (kubectl both regions):** `keycloak-0` 1/1 Running in region-a AND region-b, **region-b restarts=0** — JGroups/DB-host resolves cross-region, no UnknownHostException crashloop (contrast pre-#4869 grafana crashloop). The #4869 CNP fix proven for keycloak too. Refs #3375 #4854 |
| 70 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | guacamole status reports Healthy/Running in both regions — no missing-recordings-PVC error surfaced. | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231 (kubectl both regions):** `guacamole-server` + `guacamole-pg` 1/1 Running in region-a AND region-b — no missing-recordings-PVC error, cross-region DB reachable. Refs #3375 #4854 |
| 71 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Region-kill baseline (before): `shared-pg` Topology tab shows live Continuum Ready, lease held by region-a, region-b standby present, a live replication-lag number. | hw227 | ⚠️ | hw227-walk-2026-07-06T01:21Z: region-a side of the baseline VERIFIED — 5 dr.openova.io Continuums Healthy, lease held by region-a (row 237/G3). BUT region-b standby is ABSENT (#4811 clustermesh gap) so 'region-b standby present + live replication-lag number' cannot render — the region-kill BEFORE-baseline needs region-b materialized. Refs #3375 #4811 ‖ hw228 2026-07-09 LIVE BROWSER WALK (real owner session): the per-app Topology DR feature is PROVEN live on the Continuum-backed pair spine-keycloak (dr-spine-keycloak Continuum CR = phase Healthy, conditions LeaseHeld=True + Ready=True; hotStandbyRegions=[me-east-215-b]). Topology tab renders: Pattern 'active-hot-standby'; a 'Disaster recovery' section = ● LIVE, PRIMARY REGION me-east-215-a 'serves writes', STANDBY REGION me-east-215-b 'streaming'/'live replica', 'Replication lag 0.0 s · async' (LIVE numeric — not hardcoded —), and a Switch-over button. NOTE: the row names shared-pg, but on hw228 shared-pg is a non-Continuum shareable DATA instance (region-A cluster + shared-pg-replica region-B, no Continuum CR); the Continuum-managed DR PAIRS are spine-keycloak/gitea/harbor/openbao + cnpg-pair (all Continuum Healthy). Feature verified against the Continuum-backed app — shared-pg label is stale. Row 71 baseline (Continuum Ready, lease me-east-215-a, standby me-east-215-b present, live replication-lag 0.0s) is PROVEN on spine-keycloak; the region-kill 'after' still needs a destructive kill on a throwaway env (not run on the live reference). |
| 72 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Operator console BSS → Vouchers page renders the voucher issuance form (code, credit OMR, plan tier, description); no login wall. | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231 (real browser, operator console BSS → /billing/vouchers):** '+ Issue voucher' opens the voucher issuance form — heading 'Issue voucher' with **Code** textbox, **Credit (OMR)** spinbutton, Description, Max-redemptions, Recipient-email (fires one-shot email) fields + Issue/Cancel. 6 inputs, 0 errors. Refs #3376 |
| 73 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | In the voucher form, type a weak code `1234` → Issue → the form rejects it inline ("voucher code must be at least 12 characters"). | — | ☐ | — |
| 74 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Leave the code field empty → Issue → a new voucher row appears with a server-auto-generated high-entropy code, credit, plan tier, `unredeemed 0/1`. | — | ☐ | — |
| 75 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Stranger opens the redeem page with `?code=<CODE>` → sees "Voucher valid · 5000 OMR" with THIS Sovereign's brand chrome (no openova.io / mothership logo). | — | ☐ | — |
| 76 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Open the redeem page with a junk code → a generic "voucher not valid" message (no tombstone / no detail leak). | — | ☐ | — |
| 77 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Redeem page source (DevTools): the HTML shows THIS Sovereign's brand and no `console.openova.io` / `omantel.openova.io` / bare `openova.io` host literal. | — | ☐ | — |
| 78 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Click "Sign up to redeem" → the browser lands on the plan picker grid (`/plans`). | — | ☐ | — |
| 79 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Plans grid shows the tiers (S/M/L/XL/Flexi) with price/CPU/memory → pick plan M → advances to the app catalog (`/apps`). | — | ☐ | — |
| 80 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | App catalog grid (served from THIS Sovereign's catalog) → pick WordPress → advances to add-ons (`/addons`). | — | ☐ | — |
| 81 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Add-ons step shows optional add-ons → leave defaults → Continue → advances to the BCP topology step (`/bcp`). | — | ☐ | — |
| 82 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | BCP topology step shows BOTH Single-region and Active-hot-standby radios (the Pillar-2 BCP choice at signup) → select Active-hot-standby → Continue → review. | — | ☐ | — |
| 83 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Review summary shows the chosen plan (M), app (WordPress), topology (Active-hot-standby), the Org slug, and the pool-TLD → Proceed to checkout. | — | ☐ | — |
| 84 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | On checkout, enter the stranger's email → Send code → type the emailed sign-in code (no password) → the page shows the stranger signed in + Org confirmed. | — | ☐ | — |
| 85 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Checkout summary: the voucher credit is applied — "Credit covers this order — 0 OMR due" (no Stripe card form) → click Launch / Place order. | — | ☐ | — |
| 86 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Provisioning progress timeline advances to Done (Creating Org → Committing manifests → Provisioning vCluster → Deploying WordPress → TLS → Health), no hang/red step. | — | ◑ | hw228 2026-07-07 REAL PIN: the provisioning progress timeline (Creating Org → Committing manifests → Provisioning vCluster → … → Done) is a MARKETPLACE FUNNEL checkout-UI construct (core/marketplace/src/components/CheckoutStep.svelte, driven by transient ProvisioningState CRs, GC'd after completion) — NOT an operator-console surface. Operator Org-detail shows Status=Active, no timeline widget. Underlying prov IS genuinely done: acme + walk-stranger Organizations Ready=True, walk-stranger apps (wordpress/mysql/stalwart) Running. The visual timeline→Done needs a live funnel checkout (a write / new Org) — out of scope for read-only re-test. Evidence uat-retest-evidence/86-acme-org-detail-active-no-timeline.png. ‖ — ‖ hw228 LIVE 2026-07-06 (kubeconfig): acme Organization CR has NO status.steps/timeline field; org-controller reconciles it 'reconcile ok vcluster_ready=true' but the funnel provisioning TIMELINE is a checkout-UI construct (needs the live checkout session). acme sub-conditions IdentityProviderConfigured/PerOrgRealmProvisioned/IacRepoBootstrapped=False are WAD (NoFederation/RealmDisabled/BootstrapDisabled — optional features off), acme Ready=True genuine. Row needs the funnel checkout session — gated ‖ hw228 2026-07-07 SSO WALK (clean-slate cookie wipe, owner catalyst_session via handover-key, real screenshots): walk-stranger Org provisioning is authoritatively Ready=True/Done (kubectl: vCluster HR Ready + keycloak group + gitea org; False conditions are design-disabled optional features). ◑ — the marketplace funnel-timeline WIDGET itself was not driven live (Org was pre-provisioned). ‖ 2026-07-08 TRIAGE: full ✅ is env-blocked on **#4851** — the console owner login is broken on hw228 (catalyst-api SMTP → mothership `mail.openova.io:587` connection-refused, stalwart-mail submission LB `<pending>`), so the authenticated console surface this row needs cannot be walked until the mothership SMTP submission path is restored. NOT a code gap (chart/handler are correct); a shared-mail-infra dependency. Stays ◑. |
| 87 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | After Launch, the marketplace redirects to the per-Org console — URL becomes the per-Org console and loads with publicly-trusted TLS (no cert error / connection-refused). | — | ☐ | — |
| 88 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Per-Org console landing: the stranger is signed in zero-click as the Org owner (their email in the avatar, their Org in the header) — no login/PIN form. | — | ☐ | — |
| 89 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Per-Org console → Applications view shows the purchased WordPress app card Running/Healthy → click Open. | — | ☐ | — |
| 90 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Terminal acceptance: the purchased WordPress app SERVES at its own FQDN the app FQDN — the live rendered WordPress site (not 404/502/cert error). | — | ☐ | — |
| 91 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | While signed in, re-open the marketplace root → the returning-user redirect sends the customer to their own Org console, never to `console.openova.io` / the mothership. | — | ☐ | — |
| 92 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | As the signed-in customer, rapidly re-submit checkout/redeem >5× in a few seconds → after ~5 attempts a rate-limit notice appears; a single legitimate redeem is unaffected. | — | ☐ | — | — |
| 93 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Generality: mint a 2nd voucher and re-walk B.1→B.4 with a different slug (`walk-stranger-two`) and a different pool-TLD (`omani.rest`) → a 2nd Organization provisions and lands signed-in. | — | ☐ | — | — |
| 94 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The 2nd Org's console lands signed-in on a different TLD (the 2nd-Org console) — identical zero-click contract, no special-casing. | — | ☐ | — | — |
| 95 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The 2nd Org's purchased app serves at its own different-TLD FQDN (the 2nd-Org app) — two Orgs, two TLDs, two running apps, identical mechanism. | — | ☐ | — | — |
| 96 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Handover URL → lands on `/dashboard` already signed-in as the owner (avatar E), no login form. | — | ☐ | — |
| 97 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Dashboard renders the cluster treemap and the LAYER 1 / LAYER 2 grouping comboboxes are visible. | — | ☐ | — |
| 98 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Click LAYER 1 combobox → select `vCluster`; the treemap regroups into one labelled block per vCluster (`host` / `mgmt` / `rtz` / `dmz`), mgmt block visible + clickable. | — | ☐ | — |
| 99 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **grafana** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; mgmt/host placement re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 100 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **harbor** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 101 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **keycloak** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 102 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **gitea** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 103 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **openbao** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 104 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) [#3831](https://github.com/openova-io/openova/issues/3831) | On the LAYER1=vCluster treemap, the **newapi** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 105 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **guacamole** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 106 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Drill into the **mgmt** block: its tiles include all 7 named apps (grafana/harbor/key… alongside loki/mimir/nats/tempo. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; mgmt block placement re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 107 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Read the **host** block on the same treemap: none of the 7 named apps appear under `host`. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; host block re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  OPPOSITE of the assertion — the 'host' block contains ALL 7 named apps (they should NOT be there if mgmt-grouped); vCluster grouping degenerate to a single host bucket. ❌ |
| 108 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Open the keycloak app card → its placement detail reads `mgmt` (the per-app placement mirrors the treemap block), not `host`. | — | ☐ | — |
| 109 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | The sovereign-realm account console renders for the owner (no second login, no error dialog). | hw227 | ❌ | hw228 2026-07-07 REAL PIN owner session — prior 'hand-minted session artifact' theory DISPROVEN: catalyst-pin broker auto-logged in with NO PIN re-entry + NO second login → Account Management, BUT the account SPA renders 'Something went wrong' because its OWN REST calls 401: GET /realms/sovereign/account/?userProfileMetadata=true → 401, /supportedLocales → 401. REAL Keycloak account-console defect (persists with a faithful PIN session). Evidence uat-retest-evidence/109-account-console-401-error-real-pin.png. ‖ hw227-walk-2026-07-05T23:31Z: owner reaches https://auth.hw227.omantel.biz/realms/sovereign/account/ WITHOUT a second login (SSO session valid, no login form), BUT the account-console SPA renders 'Something went wrong — Sorry, an unexpected error has occurred' (persists after Try again, 6 console errors) instead of the Personal-info/Account-security UI. The sovereign-realm ADMIN console (row 34) works perfectly; only the self-service account console errors. Real defect in the sovereign-realm account-console client/theme. Refs #3642 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): sovereign-realm account console: NO second login (SSO recognized, 'Account Management'), BUT renders 'Something went wrong' — its REST calls (/account?userProfileMetadata, /supportedLocales) 401. Likely the non-standard handover-mint session; re-test with a real owner PIN login. ◑ ‖ **ROOT-CAUSE 2026-07-07 (KC admin REST inspect, hw228):** the sovereign-realm `account-console` client IS enabled + standardFlowEnabled (baseUrl /realms/sovereign/account/); the `account` client is enabled; default scopes=[web-origins,acr,roles,profile,basic,email]. The account REST API (/realms/sovereign/account/?userProfileMetadata=true) 401s for a `catalyst-pin`-BROKERED owner session — a brokered-login token-acquisition/audience limitation, NOT a missing client. This is a Keycloak-NATIVE self-service page reached only by direct URL — NOT linked from the OpenOva product surface, and with passwordless-PIN (#688) there is no KC password to manage there (account is managed via the OpenOva console). Non-pillar KC-internal surface → tracked here, no separate issue (L8). Stays ❌ (honest: the page 401s). ‖ **ROOT-CAUSE DEEPENED 2026-07-07 (KC admin REST, two hypotheses RULED OUT):** (1) NOT a missing-role issue — the owner emrah.baysal HAS the account client roles [manage-account, view-profile, manage-account-links] via default-roles-sovereign (which composites them), plus catalyst-admin/operator/developer/viewer. (2) NOT a webOrigins/CORS issue — the sovereign account-console client webOrigins=[] but the MASTER realm's account-console is ALSO webOrigins=[] (standard KC default), so that's not the cause. account-console client is enabled + standardFlow + pkce=S256 + rootUrl=${authBaseUrl} (all standard). So the account REST API 401 is a BROKERED-SESSION (catalyst-pin IdP) token-claim nuance specific to the account REST endpoint — the catalyst-pin identity-broker session's exchanged token isn't accepted by the account API. Deep KC IdP-mapper area, on a NON-PILLAR KC-native surface (users manage account via the OpenOva console, not this page; passwordless-PIN #688 = no KC password to manage). Stays ❌; not a role/CORS fix. Refs #3642. ‖ hw228 2026-07-09 LIVE (real owner session): auth.hw228/realms/sovereign/account/ → renders a 'Something went wrong / an unexpected error has occurred' danger-alert, NOT the account UI. Browser console: HTTP 401 Unauthorized on GET /realms/sovereign/account/?userProfileMetadata=true + /account/supportedLocales → React-Router error boundary. Stays ❌ — the account-console client cannot mint a token from the current session (either an account-console client misconfig or an injected-session artifact; a genuine PIN-login re-test would disambiguate). Non-pillar operator surface. |
| 110 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Gitea opens already signed in (avatar/menu shows the SSO user), repo list renders — no Gitea login form. | — | ☐ | — |
| 111 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Harbor opens signed in, the projects list renders — no Harbor login form, no gateway error page. | — | ☐ | — |
| 112 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Grafana opens signed in (no Grafana login), the home dashboard renders. | — | ☐ | — |
| 113 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | The OpenBao UI renders signed in via OIDC — no manual token/unseal prompt blocking the landing. | — | ☐ | — |
| 114 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi opens signed in, its main console renders — no login form, no upstream-connect error. | — | ☐ | — |
| 115 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Guacamole opens signed in, the connections list renders — no Guacamole login form. | — | ☐ | — |
| 116 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Organizations directory: the page title / heading reads "Organizations", never "Tenants"/"SME Tenants". | — | ☐ | — |
| 117 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Directory org cards / list rows: column headers read "Organization / Kind / Tier / Billing / Isolation / Status"; rows labeled "Organization", never "tenant"/"SME". | — | ☐ | — | — |
| 118 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Left-nav sidebar label for this section reads "Organizations", not "Tenants"/"SME". | — | ☐ | — |
| 119 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Create-organization flow: the form title, field labels, and submit button all say "Organization" (no "SME tenant slug" / "Onboard tenant" persona words). | — | ☐ | — |tenant)\b` over the rendered form = ZERO matches. No 'SME tenant slug' / 'Onboard tenant' persona words. Refs #3383 |
| 120 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Organization-detail view: heading "Acme Corp", breadcrumb "← Organizations", field labels Slug/Kind/Tier/Billing mode/Isolation/Stat… — no "tenant"/"SME". | — | ☐ | — |tenant)\\b in the detail = ZERO. Heading = org name 'OpenOva' (the 'Acme Corp' example needs the customer-Org funnel; structural labels + no-banned-terms verified on the existing internal org). Refs #3383 |
| 121 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | BSS / billing screen: billing is framed as "This organization is in showback mode…", zero "tenant"/"SME" leaks. | — | ☐ | — |
| 122 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Legacy `/bss/tenants` URL (PR #3390 alias) resolves and redirects to `/organizations`, rendering the directory (H1 "Organizations") — not a 404, not a login redirect. | — | ☐ | — |
| 123 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Handover URL → lands on `/dashboard` already signed-in as the owner (avatar E), no login form (everything below is admin-gated). | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231 (fresh handover session, real browser):** handover URL → `/dashboard` signed-in as owner (sidebar EB / emrah.baysal@openova.io / hw231.omantel.biz, avatar E), NO login form. Screenshot hw231-row123-167-dashboard-treemap.png. Refs #3374 |
| 124 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Catalog grid renders Blueprint cards in a tile grid, each with an icon + summary; the Alloy card is visible. | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231 (real browser /catalog):** grid renders **30 Blueprint cards** in a tile grid, each with icon + summary, grouped by category (Platform / AI / observability / data); 50 launch/New-instance affordances; 0 error/empty-state. Cards incl. postgres, keycloak, gitea, harbor, cilium, grafana, wordpress, agenity, axon, cortex, redis, valkey, seaweedfs, nats, openbao, powerdns, guacamole. Screenshot hw231-catalog-grid.png. Refs #3646 |
| 125 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the Alloy card → the detail page renders: a hero (icon + name + summary + Edit IaC), an About section, and an Instances list; no login redirect. | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231 (real browser /catalog/alloy):** the Alloy (Grafana Alloy) detail page renders — hero with icon + name 'Alloy' + summary + **Edit IaC** affordance; **About** section; **Instance(s)** section; Supported topologies; Logos. 0 errors. Refs #3646 |
| 126 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the admin Edit affordance in the hero → an edit form drops INLINE into the detail page (no modal overlay); fields appear in-place under the hero. | — | ☐ | — |
| 127 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | In the inline form, change Summary to `RECONCILE-PROOF-<ts>` → Save → the page refreshes in place and the new summary shows in the hero. | — | ☐ | — |
| 128 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Back on the grid, the Alloy card summary now reads `RECONCILE-PROOF-<ts>` — the edit propagated to the card, not just the detail page. | — | ☐ | — |
| 129 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Hard-reload the detail page → the summary is still `RECONCILE-PROOF-<ts>` — the edit persisted across a reload, not an in-memory overlay. | — | ☐ | — |
| 130 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The non-card `version` field renders as a `v1.0.1` chip in the hero, and Edit IaC exposes the full CR including `version` for editing. | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231:** the non-card `version` field renders as a **`v1.0.2` chip** in the Alloy hero. Refs #3646 |
| 131 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Note the current hero logo (the Alloy glyph) as the baseline before an icon edit. | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231:** the Alloy hero logo/glyph is present (baseline before any icon edit) — 22 img/icon refs on the detail page. Refs #3646 |
| 132 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit → in the Light-theme icon field paste a distinct image → Save → a "Saved to IaC ✓" confirmation shows and the page refreshes. | hw224 | ☐ | hw228 2026-07-07 REAL PIN owner session (clean browser, faithful): Light-theme icon field → selected Gitea from light-icon gallery (icon_light=/component-logos/gitea.svg) → Save → FRESH RELOAD → Light-theme logo figure persisted the Gitea image (round-trips). Reverted to pristine after. Evidence uat-retest-evidence/132-149-light-icon-persisted-gitea.png. ‖ hw224-fulltable-2026-07-04: icon-edit Save = write mutation (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` with edited `card.iconLight` → 200 `{committed:true, path:catalog-sovereign/bp-alloy/blueprint.yaml}` + durable Gitea commit ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): Logos edit surface confirmed (Light/Dark theme figures + file input in DOM) but the hero-icon click didn't surface a clean gallery under browser contention → change→save→reload not completed (147 proves the catalog-edit persistence mechanism). ◑ ‖ 2026-07-09 DRIFT-GUARD: prior ✅ evidence cited a wiped predecessor env — flushed per founder 2026-06-14 rule; re-walk on hw228 required. ‖ hw228 2026-07-09: the icon/logo Save writes the Blueprint IaC via the same catalyst-api path as rows 148/154 — gated on the #4862 (RBAC live-applied) + dry-run resourceVersion fix reaching hw228 (chart 1.4.1031 roll). Stays ☐ pending that roll + an image-paste walk. ‖ hw228 2026-07-09: the icon/logo Save uses the SAME Edit-IaC→Gitea path, now UNBLOCKED by #4862 (dry-run accepted live). A real icon-image Save (which pushes a durable commit to the catalog Gitea) was not performed to avoid a junk change to the live reference catalog. Stays ☐ — mechanism unblocked, specific icon-save persist walk pending a throwaway env / fresh prov. |
| 133 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Observe the hero — it now shows the new logo; the render reads the edited `card.iconLight` first (IaC-first), not the bundled asset. | — | ☐ | — |
| 134 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Return to the grid — the Alloy card icon is now the new logo (the grid tile resolves the same edited `card.iconLight`). | — | ☐ | — |
| 135 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Reload the detail page — the hero is still the new logo (render reads the persisted IaC icon on every load). | — | ☐ | — |
| 136 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit again — the Light-theme icon field shows the current IaC value, falling back to the bundled asset only when IaC carries none. | — | ☐ | — |
| 137 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit → click the icon picker (`iconpicker-*`) → a thumbnail grid of vendored `component-logos/*` assets opens (a role=listbox grid of logo tiles). | — | ☐ | — |
| 138 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click `cilium.svg` in the picker grid → the icon field + a live preview swatch update to the Cilium logo. | — | ☐ | — |
| 139 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Save → reload — the hero is now the Cilium logo (the picker selection persisted to IaC and renders on hero + grid card). | — | ☐ | — |
| 140 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | On Save, the durable-commit verdict (git outcome) is surfaced in-UI (the Edit-IaC `managed-by: manual • in sync` indicator + {stored:true,commit… not a silent store success. | — | ☐ | — |
| 141 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Hover the summary line in the hero → a pencil/edit affordance appears ON the field (`cif-summary-edit` → `cif-summary-input`), inline, without opening the full form. | — | ☐ | — |
| 142 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the summary → type a value → Save → only the summary updates in place; no full-form modal opens. | — | ☐ | — |
| 143 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Repeat the inline edit for the name field (`cif-name-edit` → `cif-name-input`) — it edits in place and saves just that field. | — | ☐ | — |
| 144 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit IaC (`catalog-detail-ed… admin only) → the full `blueprint.yaml` opens in the YAML editor (the entire CR, not just 7 card fields). | — | ☐ | — |
| 145 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Change a field in the editor → Commit → a Show-diff Current/Proposed side-by-side renders the change and the commit succeeds (confirmation appears). | — | ☐ | — |
| 146 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The editor subtitle states it directly: "Commit writes the IaC source of truth; Flux reconciles it… Both this editor and the card form above write the same file." | — | ☐ | — |
| 147 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Open the WordPress detail page → Edit → change Summary → Save → reload → the summary persists, exactly as for Alloy. | — | ☐ | — |
| 148 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | WordPress: Edit IaC → edit `spec.manifests` → Commit → reload — the same YamlEditor edits a structurally-different blueprint's manifests in place. | — | ☐ | — |
| 149 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | WordPress: Edit → Light-theme icon → distinct image → Save → reload — the WordPress hero icon visibly changes. | hw224 | ☐ | hw228 2026-07-07 REAL PIN owner session: light-theme icon override Save→reload round-trips (see r132). Evidence uat-retest-evidence/132-149-light-icon-persisted-gitea.png. ‖ hw224-fulltable-2026-07-04: WordPress icon write (proven on 25aadcfc)‖ write-mutation walk on fresh prov 25aadcfc — bp-wordpress `card.iconLight` edit → `PUT /iac` → 200 + durable Gitea commit; the new icon reads back on re-GET, hero changes ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): Logos edit surface confirmed (Light/Dark theme figures + file input in DOM) but the hero-icon click didn't surface a clean gallery under browser contention → change→save→reload not completed (147 proves the catalog-edit persistence mechanism). ◑ ‖ 2026-07-09 DRIFT-GUARD: prior ✅ evidence cited a wiped predecessor env — flushed per founder 2026-06-14 rule; re-walk on hw228 required. ‖ hw228 2026-07-09: WordPress hero-icon Save = same Blueprint-IaC write path as 132/148/154 — gated on the #4862 roll to hw228. Stays ☐. ‖ hw228 2026-07-09: WordPress hero-icon Save = same Gitea-write path as 132, unblocked by #4862; not pushed live (would durably commit to the catalog Gitea). Stays ☐. |
| 150 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | PostgreSQL detail renders the SAME edit surface (hero icon, Edit IaC, clickable cards) and Edit IaC exposes `contextSchema` (a blueprint carrying contextSchema`/`sha… | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231 (real browser /catalog/postgres):** PostgreSQL detail renders the SAME edit surface as Alloy — hero (name 'PostgreSQL' + version chip **v0.2.11**), **Edit IaC** affordance, **About**, **Instances**, **Supported topologies** strip (singleton + active-hot-standby, canonical vocab), New-instance button. Same chrome on a 2nd blueprint. Refs #3646 |
| 151 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Alloy + Postgres render the IDENTICAL edit chrome (same cif-icon-edit`/`cif… same Edit-IaC YamlEditor) — no blueprint-specific UI. | — | ☐ | — |
| 152 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The catalog detail page renders (hero · About · Instances) and opens an INLINE Edit form (no modal) — acceptance headline 1. | — | ☐ | — |
| 153 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | A summary edit Saves, updates the page AND the grid card, and persists across a reload — acceptance headline 2. | — | ☐ | — |
| 154 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | A non-card field edit (`version`) persists — the whole CR is editable, not a 7-field overlay — acceptance headline 3. | — | ☐ | — | ‖ hw228 2026-07-09 CORRECTED: see row 148 — Edit-IaC editor delivery-gap (fix #4844 stranded 2 days by the dead pipeline, now unblocked by #4857 / 1406c76). Stays ❌ pending hw228 console roll to ≥1406c76 + browser walk. 
| 155 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The edited icon renders on hero + grid + survives reload; the form pre-fills the IaC icon; the picker grid works — acceptance headline 4. | — | ☐ | — |
| 156 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Save surfaces the IaC-commit verdict (`committed:true` + `• in sync`), not a bare store success — acceptance headline 5. | — | ☐ | — |
| 157 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Per-field inline edit for cards (`cif-*`) + the full-CR YamlEditor (Edit IaC) for the rest, both writing the same IaC source — acceptance headline 6. | — | ☐ | — |
| 158 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The identical edit mechanism works on a 2nd + 3rd blueprint — no per-blueprint UI (Alloy + Postgres + a third) — acceptance headline 7. | — | ☐ | — |
| 159 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Settings → Sovereignty section renders a "Cluster sovereignty" panel with a "TETHERED" badge + an "Achieve True Sovereignty" cutover CTA (runs the 8-step cutover + 10-min egress-block self-test). | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231 (real browser /settings):** Sovereignty section renders a **'Cluster sovereignty' panel** with a **'Tethered' badge** + 'Sovereignty status'. Panel also live-shows 'Sovereignty cutover progress / Cutover in progress' (a cutover is actively running) — proves the panel reads live state, not a static badge. Screenshot in .playwright-mcp. Refs #3379 |
| 160 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Console nav + Settings sidebar expose a dedicated "Sovereignty" anchor (`#sovereignty`) that scrolls to + highlights the Cluster-sovereignty panel — the cutover trigger is a first-class surface. | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231 (real browser /settings):** a dedicated **'Sovereignty' anchor (`#sovereignty`)** is present + reachable from the Settings nav. Refs #3379 |
| 161 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Open `/jobs` (zero-login, signed in as owner) → the canvas table renders a populated activity list (not a spinner, empty state, or login redirect). | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231 (real browser /jobs, signed-in owner):** the canvas renders a **populated activity list** (695 activity refs — cutover/reconciler/cron rows), not a spinner or empty state. Refs #3646 |
| 162 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Find the `cutover` group row and expand it → it renders the 11 `cutover-step-*` rows (gitea-mirror, harbor-projects, harbor-prewarm, registry-pivot, … vcluster-registry-p… — the 11-step execution tree. | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231:** the `cutover` group renders its `cutover-step-*` rows (helmrepository-patches, registry-pivot, harbor-projects, harbor-prewarm, gitea-mirror, flux-gitrepository-patch, gitea-token-mint, …) — the 11-step chain. Refs #3646 |
| 163 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Each `cutover-step-*` row reads an honest per-step status (Succeeded / Running / Failed / Pending), never a premature green. | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231 (fixture = the live wedged cutover):** each `cutover-step-*` reads an **honest per-step status** — steps 01-05 Succeeded, step-06 helmrepository-patches **Failed**, steps 07-11 Pending (page shows Succeeded/Failed/Running/Pending). No fabricated all-green. Refs #3646 #4873 |
| 164 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | The cutover group status reflects its real children (a group with a failed child reads failed, not a fake Succeeded). | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231:** the cutover group status reflects its real children — with the helmrepository-patches child Failed, the group reads **Failed/Degraded** (not a false green over a failed child). Refs #3646 |
| 165 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | On a failed `cutover-step-*` row, a Re-run button is present (per-row, gated to Failed) — the operator can re-drive a failed cutover step from the browser. | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231:** the Failed `cutover-step-*` rows carry a **Re-run** button (per-row, gated to Failed) — 3 Re-run affordances present on the failed steps. Refs #3646 |
| 166 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | (After a COMPLETE cutover) the `cutover` group reads all-11-green on `/jobs` — every step Succeeded, including `egress-block-test` and `registry-pivot`. | hw224 | ❌ | hw228 LIVE 2026-07-07: cutover NOT fired — only the dormant `bp-self-sovereign-cutover-gitea-secret-bridge` Job exists (Complete 1/1, 20h, the bootstrap-kit slot-06a install), NO 8-step cutover execution, no Continuum cutoverComplete. Operator-gated (cutover is operator-fired by design). ❌ pending an operator-fired cutover. Refs #3379 | hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: cutover NOT fired. Sovereign CR cutoverComplete is unset; only the pre-cutover job bp-self-sovereign-cutover-gitea-secret-bridge exists (Complete) — the 11-step cutover group has not run, so /jobs cannot show it all-green. Refs #3379 |
| 167 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Open the console root in a fresh tab → land on the operator dashboard signed in as the sovereign-admin (no login form, no password field). | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231:** console root → operator Dashboard signed in as sovereign-admin; header shows 197 items + Decommission link; treemap populated (Layer1=Progress, Layer2=Kind; cells Done/Install/Task/Reconcile/Running/Pending). Console correctly surfaces the running-cutover banner ('operation in progress — finite cutover/switchover; convergence unaffected'). Refs #3646 |
| 168 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Open `/jobs` → the canvas table renders a populated list of activity rows (not a spinner, empty state, or login redirect). | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231 (real browser /jobs):** the canvas table renders a **populated list** (grouped activity rows — Cutover steps, Reconcilers, Crons — 10 columnheaders), not a spinner/empty. Refs #3646 |
| 169 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | The Kind column is present in the header and each row shows its kind; full header = Name·Kind·App·Deps·… | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231:** the **Kind column** is present in the header (Name·Kind·…·Status) and rows carry their kind (Cutover steps show Kind=Cutover; the cron reconciler shows '(cron)'). Refs #3646 |
| 170 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Scroll/search to the `install-openbao` row → it renders green / Succeeded (the install is honestly green). | — | ☐ | — |
| 171 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Every rendered row maps to a real HelmRelease install / terraform stage (no placeholder, no synthetic/fabricated entry). | — | ☐ | — |
| 172 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Set the Status filter to `failed` → the table shows the genuinely-failing rows, each with an honest failed status (the Status filter works). | — | ☐ | — |
| 173 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Leave the table on screen ~30s → rows update live (tail) as reconciliation progresses; a status badge changes in place without a manual reload. | — | ☐ | — |
| 174 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | On a Failed row (Status=failed), a Re-run / Retry-reconcile button is present on the row (visible on the row or on hover). | — | ☐ | — |
| 175 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | On a Succeeded / healthy / Confirming row, NO Re-run button renders — the control is gated to Failed rows only. | — | ☐ | — |
| 176 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Click Re-run on a Failed row → a success toast/feedback appears and the button flips in place (e.g. `Requesting…`) — the browser triggered a re-reconcile, no terminal. | hw224 | ◑ | hw228 2026-07-07 REAL PIN — button WORKS but backend retry 502s (REAL DEFECT): clicking Re-run fired POST /api/v1/deployments/ac35eda8561b7922/jobs/task-trivy-security-scan/retry → inline 'Failed (502)' feedback (no terminal). Endpoint returned 502 'remediation-failed: jobs "trivy-security-scan" not found in any namespace'. Root cause (curl+kubectl reproduced): the #4514 KindTask retry path recreateStandaloneJob looks up a standalone Job named identically to the reconciler, but trivy's real jobs are trivy-operator scan-vulnerabilityreport-* in trivy-system — no Job named trivy-security-scan. Genuine handler defect for reconcilers not backed by a same-named standalone Job (also hits HelmRelease-backed). catalyst-api healthy (not an outage). Filed #4845. Evidence uat-retest-evidence/176-rerun-clicked-failed-502-feedback.png. ‖ hw224-fulltable-2026-07-04: Re-run control present on failed rows; click = write (not fired on hw224; #4514 delete+recreate proven prior session)‖ 2026-06-27 91dc0591 (Refs #909): #4511/#4514 ROLLED LIVE — catalyst-api image `e976ff0` (contains the #4514 merge 0988d6d; jobs_retry.go KindTask branch now calls recreateStandaloneJob: DELETE old + CREATE `<name>-rerun-<unix>` with immutable fields stripped). Reproduced live on the very Job the PR cited: `mimir/mimir-make-minio-buckets-rerun-1782552889` exists **Complete** (created 09:34:49Z) and the original `mimir-make-minio-buckets` is **NotFound** (deleted by the rerun) — the 502 'spec.template field is immutable' no longer occurs ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): not testable — Jobs status filter 'failed' → 'No jobs match' (RUNNING 2/PENDING 11/SUCCEEDED 13, zero failed); no Re-run btn without a failed row. (per-Org HelmRelease installs aren't modeled on the Jobs page.) ◑ ‖ 2026-07-08 TRIAGE: full ✅ is env-blocked on **#4851** — the console owner login is broken on hw228 (catalyst-api SMTP → mothership `mail.openova.io:587` connection-refused, stalwart-mail submission LB `<pending>`), so the authenticated console surface this row needs cannot be walked until the mothership SMTP submission path is restored. NOT a code gap (chart/handler are correct); a shared-mail-infra dependency. Stays ◑. ‖ 2026-07-09 DURABLE FIX SHIPPED — PR #4866 (#4845 fix-agent, orchestrator-verified diff+CI): the raw 502 was RetryJob mapping EVERY dispatchRetry error to 502 — including AGGREGATE reconcilers (e.g. 'Trivy Security Scan') that have NO backing standalone Job (confirmed live: kubectl get jobs -A ‖ 2026-07-09 FIX MERGED — #4866 (sha 0c64d9e20): the Jobs Re-run graceful-422 (not raw 502 for aggregate reconcilers) is now on main. Live 422/200 confirmation is roll-gated (catalyst-api image rolls to hw228 next). ◑ pending the roll. | grep trivy-security-scan = 0 hits; real jobs are scan-vulnerabilityreport-* in trivy-system). Fix (jobs_retry.go): errNotDirectlyRetryable sentinel wraps the 3 not-retryable branches → RetryJob returns graceful 422 'not-directly-retryable' instead of 5xx; directly-retryable jobs still 200 + re-run. Unit test TestRetryJob_Task_AggregateNoBackingJob_422 green. Stays ◑ — durable fix + unit-proven; live 422/200 confirmation is roll-gated (catalyst-api image pending hw228 roll). FE follow-up (a `retryable` model flag to hide Re-run on aggregate rows) noted on #4845. ‖ **2026-07-09 hw228 LIVE:** clicked Re-run on the Failed 'Trivy Security Scan' (aggregate reconciler, no backing Job) → `POST .../jobs/task-trivy-security-scan/retry` returned **HTTP 502** = the OLD behavior. #4866 (graceful 422 `not-directly-retryable` for aggregate reconcilers) is MERGED but NOT deployed to hw228's catalyst-api (hw228 predates the #4866 roll — merged≠deployed). Flips on a fresh prov / catalyst-api roll carrying #4866. Refs #4866 #3646. |
| 177 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Use the same Re-run button on a Failed row — one remediation mechanism across rows, no per-kind UI (the single-table/single… shape). | hw227 | ◑ | hw228 2026-07-07 REAL PIN: the remediation control is a SINGLE uniform, status-gated 'Re-run' in Actions backed by one generic POST /jobs/{jobId}/retry — no per-kind UI, no per-row special-casing (the single-table/single-mechanism shape 177 asserts). Caveat: only 1 live failed reconciler currently, so uniformity is evidenced by the status-gated render pattern + generic endpoint, not shown across 2 simultaneous failed rows. ‖ hw227-walk-2026-07-05T23:13Z: same as row 174 — one-remediation-mechanism-across-rows cannot be exercised without a live failed job; hw227 has 0 failed jobs. Deferred to an env carrying a genuine failure. Refs #3646 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): not testable — Jobs status filter 'failed' → 'No jobs match' (RUNNING 2/PENDING 11/SUCCEEDED 13, zero failed); no Re-run btn without a failed row. (per-Org HelmRelease installs aren't modeled on the Jobs page.) ◑ ‖ 2026-07-08 TRIAGE: full ✅ is env-blocked on **#4851** — the console owner login is broken on hw228 (catalyst-api SMTP → mothership `mail.openova.io:587` connection-refused, stalwart-mail submission LB `<pending>`), so the authenticated console surface this row needs cannot be walked until the mothership SMTP submission path is restored. NOT a code gap (chart/handler are correct); a shared-mail-infra dependency. Stays ◑. ‖ **2026-07-09 hw228 LIVE:** clicked Re-run on the Failed 'Trivy Security Scan' (aggregate reconciler, no backing Job) → `POST .../jobs/task-trivy-security-scan/retry` returned **HTTP 502** = the OLD behavior. #4866 (graceful 422 `not-directly-retryable` for aggregate reconcilers) is MERGED but NOT deployed to hw228's catalyst-api (hw228 predates the #4866 roll — merged≠deployed). Flips on a fresh prov / catalyst-api roll carrying #4866. Refs #4866 #3646. |
| 178 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | The signed handover URL → lands directly on `/dashboard` signed-in (env switcher shows the live env, avatar E, no login form). | — | ☐ | — |
| 179 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Click the avatar (top-right) → menu reads "Signed in as the owner" with a Sign-out item — confirms the landed identity is the owner-admin. | — | ☐ | — |
| 180 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Grafana bare URL → lands on Grafana Home ("Welcome to Grafana", full UI, Profile avatar), `?orgId=1`, no login form (SSO landed signed-in). | — | ☐ | — |
| 181 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Harbor (registry) bare URL → lands on `/harbor/projects` (projects, repos, Administration nav), no login form; user dropdown the owner. | — | ☐ | — |
| 182 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Gitea bare URL → lands on the gitea dashboard titled "emrah.baysal - Dashboard - Catalyst Gitea", logged in; URL stays on :443. | — | ☐ | — |
| 183 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | OpenBao bare UI → final rendered screen is the authenticated Vault session (`/ui/vault/secrets`, Secrets Engines with cubbyhole/ + secret/ kv), NO `/ui/vault/auth` token form. | — | ☐ | — |
| 184 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | — | [UAT.md](https://github.com/openova-io/openova/blob/main/docs/ledger/UAT.md) | ⚠️ | predecessor-walk(B): No test case defined in ledger (literal —). ‖ §I 91dc0591: no test case defined in ledger (literal —) — not click-verified, harness artifact |
| 185 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | No stale predecessor-env references anywhere in the rendered `UAT.md` (the table names only the live env). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: UAT.md rows walked this pass name only hw224; prior-env blocks labeled stale by construction |
| 186 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | — | [UAT.md](https://github.com/openova-io/openova/blob/main/docs/ledger/UAT.md) | ⚠️ | predecessor-walk(B): No test case defined in ledger (literal —). ‖ §I 91dc0591: no test case defined in ledger (literal —) — not click-verified, harness artifact |
| 187 | topology | [#4001](https://github.com/openova-io/openova/issues/4000) | Per-app Topology for a multi-region app (grafana) shows **Pattern: active-active** with 2 PRIMARY region cards — NOT singleton. | — | ☐ | — |
| 188 | topology | [#4001](https://github.com/openova-io/openova/issues/4000) | A genuine single-region app (catalyst-api) correctly shows **singleton** (no false multi-region). | — | ☐ | — |
| 189 | topology | [#4000](https://github.com/openova-io/openova/issues/4000) | Region-b kubeconfig self-heals EIP→private-IP **zero-touch** on restart (topology survives a fresh prov). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: region-b DEGRADED (clustermesh-apiserver Init:0/1 #4656 family) — not a clean zero-touch self-heal; topology survives (Continuums Healthy, lease me-east-215-a) |
| 190 | jobs | [#3916](https://github.com/openova-io/openova/issues/3916) | `/jobs` lists **ONLY finite jobs** (provision/cutover steps, batch Jobs, CronJob runs) — ZERO continuous-reconciler rows. | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231:** `/jobs` lists **ONLY finite jobs** — Cutover steps (11), batch Jobs, and CronJob runs (the 'Reconcilers' group = the finite `vpc-podcidr-route-reconciler (cron)` #4861 CronJob). No continuous reconcile-loop rows. Refs #3646 #3958 |
| 191 | jobs | [#3916](https://github.com/openova-io/openova/issues/3916) | Continuous reconcilers (HelmRelease/Kustomization) do **NOT** appear in `/jobs` (they live in the recon lens). | — | ✅ | — ‖ **2026-07-09 ✅ LIVE hw231:** continuous reconcilers (HelmRelease/Kustomization) do **NOT** appear as `/jobs` rows — ZERO `HelmRelease`/`Kustomization` Kind cells in the snapshot; only finite Cutover/Cron/Job kinds. They live in the Cloud graph, not /jobs. Refs #3646 #3958 |
| 192 | recon | [#3925](https://github.com/openova-io/openova/issues/3996) | The convergence **Reconciliation** link opens the cloud **RECON lens** (`view=graph&lens=reconciliation`), not the default cloud lens. | — | ☐ | — |
| 193 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Clicking a reconciler opens the **ArgoCD-like management surface** (drill-in). | — | ☐ | — |
| 194 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Recon surface lists Flux reconcilers with live status (Reconciled/Reconciling/Degraded/Suspended). | — | ☐ | — |
| 195 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Drill a reconciler → its controller **logs** render. | — | ☐ | — |
| 196 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | **Reconcile** action → `reconcile.fluxcd.io/requestedAt` lands on the live object. | — | ☐ | — |
| 197 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | **Suspend/Resume** → `spec.suspend` flips on the live object. | — | ☐ | — |
| 198 | cloud | [#3987](https://github.com/openova-io/openova/issues/3987) | `/cloud` per-kind **helmreleases** page shows the real count (~65), not 0. | — | ☐ | — |
| 199 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **Gateway** page shows the live `cilium-gateway` (1). | — | ☐ | — |
| 200 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **HTTPRoutes** page shows 15. | — | ☐ | — |
| 201 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **NetworkPolicies** page shows the live policies (~10). | — | ☐ | — |
| 202 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **CiliumNetworkPolicies** page shows 5. | — | ☐ | — |
| 203 | cloud | [#4002](https://github.com/openova-io/openova/issues/4002) | Cloud **Load Balancers** page shows the real ELB (212.72.24.33), not 0. | — | ☐ | — |
| 204 | cloud | [#4002](https://github.com/openova-io/openova/issues/4002) | Cloud **Worker Nodes** page shows the real nodes, not 0. | — | ☐ | — |
| 205 | fleet | [#4003](https://github.com/openova-io/openova/issues/4003) | `/fleet/applications` returns the real app count (non-zero), not 0. | — | ☐ | — |
| 206 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | `kubectl get managed` is non-empty — Crossplane observes the OpenTofu-built infra. | — | ☐ | — |
| 207 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | A `CloudAdoption` for the real ELB reaches Synced+Ready (Observe), `external-name` = the real ELB id. | — | ☐ | — |
| 208 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | Adoption is **Observe-only** — the live ELB/nodes are untouched (no re-provision). | — | ☐ | — |
| 209 | storage | [#3971](https://github.com/openova-io/openova/issues/3971) | PVCs land on a real **CSI storageclass** (not k3s local-path). | — | ☐ | — |
| 210 | storage | [#3971](https://github.com/openova-io/openova/issues/3971) | `local-path` is **FORBIDDEN** (k3s `--disable=local-storage`). | — | ☐ | — |
| 211 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP **sovereign-admin** token → `list_applications` returns all apps. | hw224 | ❌ | hw228 DIRECT-WALK 2026-07-07: MCP runtime NOT deployed — mcp.hw228.omantel.biz → 000, no MCP pod/HTTPRoute (only sandbox-controller). list_applications has no server. ❌ Refs #3969 ‖ hw228 2026-07-07 (code-verified): openova-mcp server (products/openova-mcp, #3988) BUILDS clean + RBAC test suite PASSES (internal/identity + internal/tools — the two-layer list_applications scoping + cross-Org get 403 logic is implemented and unit-verified). NOT deployed on the Sovereign: only sandbox-controller runs (no openova-mcp Service/endpoint). Per its README §95-101 + task #899, the DEPLOY (Blueprint chart + per-Sovereign streamable-HTTP/SSE Service + stable DNS + .mcp.json injection) is the #3988 EPIC's REMAINING work (sequenced 'implement after SME'). Row stays ❌ pending that deploy — logic correct, runtime not on the Sovereign. Refs #3988| hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: OpenOva MCP is NOT deployed on hw227 (0 MCP pods, no mcp HTTPRoute). sovereign-admin MCP list_applications cannot be exercised — no MCP surface exists. Refs #3988 ‖ **GATE CONFIRMED 2026-07-07 (repo inspection):** #3988 built the openova-MCP SERVER (products/openova-mcp/ — Go cmd/internal/{identity,tools,catalystapi}) but NOT its deployment layer: NO Chart.yaml, NO Dockerfile, NO blueprint.yaml, NOT in the bootstrap-kit or catalog. So the RBAC-scoped MCP facade cannot be deployed on ANY Sovereign (confirmed absent on hw228 — only sandbox-controller runs). These rows are gated on PACKAGING the server (image build workflow + Helm chart + blueprint.yaml + bootstrap-kit slot + per-Org KC identity wiring), a substantial build cycle, not a simple deploy. Refs #3988. |
| 212 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP **Org-scoped** token → `list_applications` returns ONLY that Org's apps (RBAC parity). | hw224 | ❌ | hw228 LIVE 2026-07-07: MCP runtime NOT deployed (no MCP pod/route; only sandbox-controller). Org-scoped RBAC untestable without the MCP server. Gated on sandbox-MCP runtime. ‖ hw228 2026-07-07 (code-verified): openova-mcp server (products/openova-mcp, #3988) BUILDS clean + RBAC test suite PASSES (internal/identity + internal/tools — the two-layer list_applications scoping + cross-Org get 403 logic is implemented and unit-verified). NOT deployed on the Sovereign: only sandbox-controller runs (no openova-mcp Service/endpoint). Per its README §95-101 + task #899, the DEPLOY (Blueprint chart + per-Sovereign streamable-HTTP/SSE Service + stable DNS + .mcp.json injection) is the #3988 EPIC's REMAINING work (sequenced 'implement after SME'). Row stays ❌ pending that deploy — logic correct, runtime not on the Sovereign. Refs #3988| hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: OpenOva MCP not deployed on hw227 (no pods/HTTPRoute); Org-scoped MCP list_applications not exercisable. Refs #3988 ‖ **GATE CONFIRMED 2026-07-07 (repo inspection):** #3988 built the openova-MCP SERVER (products/openova-mcp/ — Go cmd/internal/{identity,tools,catalystapi}) but NOT its deployment layer: NO Chart.yaml, NO Dockerfile, NO blueprint.yaml, NOT in the bootstrap-kit or catalog. So the RBAC-scoped MCP facade cannot be deployed on ANY Sovereign (confirmed absent on hw228 — only sandbox-controller runs). These rows are gated on PACKAGING the server (image build workflow + Helm chart + blueprint.yaml + bootstrap-kit slot + per-Org KC identity wiring), a substantial build cycle, not a simple deploy. Refs #3988. |
| 213 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP cross-Org `get_application` → **403** (UI-parity). | hw224 | ❌ | hw228 LIVE 2026-07-07: MCP runtime NOT deployed (no MCP pod/route). Cross-Org 403 untestable without the MCP server. Gated on sandbox-MCP runtime. ‖ hw228 2026-07-07 (code-verified): openova-mcp server (products/openova-mcp, #3988) BUILDS clean + RBAC test suite PASSES (internal/identity + internal/tools — the two-layer list_applications scoping + cross-Org get 403 logic is implemented and unit-verified). NOT deployed on the Sovereign: only sandbox-controller runs (no openova-mcp Service/endpoint). Per its README §95-101 + task #899, the DEPLOY (Blueprint chart + per-Sovereign streamable-HTTP/SSE Service + stable DNS + .mcp.json injection) is the #3988 EPIC's REMAINING work (sequenced 'implement after SME'). Row stays ❌ pending that deploy — logic correct, runtime not on the Sovereign. Refs #3988| hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: OpenOva MCP not deployed on hw227; cross-Org get_application 403 not exercisable. Refs #3988 ‖ **GATE CONFIRMED 2026-07-07 (repo inspection):** #3988 built the openova-MCP SERVER (products/openova-mcp/ — Go cmd/internal/{identity,tools,catalystapi}) but NOT its deployment layer: NO Chart.yaml, NO Dockerfile, NO blueprint.yaml, NOT in the bootstrap-kit or catalog. So the RBAC-scoped MCP facade cannot be deployed on ANY Sovereign (confirmed absent on hw228 — only sandbox-controller runs). These rows are gated on PACKAGING the server (image build workflow + Helm chart + blueprint.yaml + bootstrap-kit slot + per-Org KC identity wiring), a substantial build cycle, not a simple deploy. Refs #3988. |
| 214 | orgs | [#3985](https://github.com/openova-io/openova/issues/3985) | No `SME`/`tenant` banned-term leak in the console bundle (org-rename complete). | — | ☐ | — |
| 215 | orgs | [#3985](https://github.com/openova-io/openova/issues/3985) | CRD `tier` enum is `[org, corporate]` — a `tier: org` Organization is accepted. | — | ☐ | — |

---

### Governance train — items 187-215 (added 2026-06-21)

These extend the canonical 186-case walk with this session's governance work. **Status is deliberately conservative** (no premature ✅): `⏳` = built/merged in code but **NOT yet validated on the durable live console** (the agents' temporary image rolls were Flux-reverted, so they are not yet what the operator sees — each needs a real end-user walk after the durable roll); `◑` = mechanism proven (e.g. against live HCS) but full in-cluster proof lands on a fresh prov; `❌` = not built yet (fix in flight) or genuinely requires a clean re-prov. A row flips `✅` only after an operator end-user walk on the durable console with pasted evidence, per the TRUST-ledger discipline.
| 216 | e2e-journey | [#3376](https://github.com/openova-io/openova/issues/3376) | A user with a **coupon/voucher code** redeems it → creates **his Organization**. Surface: `https://marketplace.<fqdn>/redeem/?code=<CODE>` (no session) → plan/region → Launch. PASS = the provisioning timeline runs to Done AND `/organizations` shows the Org **ACTIVE** backed by live `vcluster` pods (not a fake-green status). | — | ☐ | — |
| 217 | e2e-journey | [#3374](https://github.com/openova-io/openova/issues/3374) | The user logs in via the **passwordless SSO magic-email PIN** (any domain, e.g. `demo@openova.io`) → 6-digit PIN emailed → lands signed-in, no password. PASS = the PIN actually arrives (verify IMAP `mail.openova.io:993`) AND entering it lands signed-in as the CUSTOMER (their email in the avatar, their Org in the header) with no password field — this is the customer PIN flow, NOT the operator handover. | — | ☐ | — |
| 218 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | **chepherd is installable from the catalog as an Application** (`bp-agenity` **Helm chart**). Surface: per-Org console → Catalog → the `bp-agenity` card renders + the install wizard opens with the Org's Environment pre-selected as the target. PASS = card present + wizard opens (the chart resolves from the catalog, not 404). | — | ☐ | — |
| 219 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4180](https://github.com/openova-io/openova/issues/4180) [#4553](https://github.com/openova-io/openova/issues/4553) | The user **provisions chepherd** as an Application → it converges (Helm deploys, pods Ready, chepherd console reachable in his Org). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: built-in Agenity/Sandbox workspace live (picker renders, sessions persist); customer chat-driven provision-converge gated on #4656 vcluster + Anthropic cred. uat-hw224-fulltable-12‖ 2026-06-28 fresh North-Star Org `agnstar` on 91dc0591 (E2E acceptance walk, supersedes prior PARTIAL): agenity provisioned via Application-CR → `agenity-bp-agenity-0` Running 1/1 (singleton, host-ns), chart bp-agenity 0.5.16 + the durable oidc-gate companion (#4553/#4572) — pods Ready AND the chepherd workspace is reachable in the Org at `agenity.agnstar.omani.homes/app/` (renders Work/Terminal/Talk/Board, SSO-authenticated). Runtime pod-materialize + console-reachable both confirmed on a real Org (not a throwaway) |
| 220 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4111](https://github.com/openova-io/openova/issues/4111) [#4556](https://github.com/openova-io/openova/issues/4556) | chepherd's **solo agent is pre-configured with Claude Opus 4.7** + a working token (the agent is live and chat works). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: Claude Code agent present w/ 'Connect Claude Max' (Opus); working token via Max-connect not pre-seeded (#4277). uat-hw224-fulltable-12‖ 2026-06-27 fresh North-Star Org `agnstar` on 91dc0591 (PARTIAL, supersedes prior ⛔): **SSO-entry (founder req-4) MET** — `agenity.agnstar.omani.homes` renders the full chepherd workspace (Work/Terminal/Talk/Board tabs + ✦Spawn + Sessions rail) via zero-click Keycloak SSO with **NO token-paste prompt** (#4556/#4560 bp-oidc-gate `spaTokenSeed` 0.1.6, rolled live `oidc-gate.v4` Ready; verified `has_paste_prompt:false, has_workspace_tabs:true`, screenshot `uat-agnstar-agenity-NOPASTE-workspace-VERIFIED.png` — BEFORE state `uat-agnstar-agenity-app-TOKEN-PASTE-PROMPT.png`). The pty-server image bundles `@anthropic-ai/claude-code` + `@qwen-code` + the `/usr/local/bin/openova-sandbox-mcp` binary; image/wiring PROVEN. HELD ⚠️: the live agentic chat RUN (workspace shows `0 live` agents — no spawn) is gated on an Anthropic credential (G8/#4277) |
| 221 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | The user **chats with the chepherd solo agent** ("create a `<blueprint>` app in my org") → the agent calls the openova-mcp `create_application` tool with the user's **Org-scoped** token → a new Application CR is created in the user's Org. PASS = the agent reports the new app name + an Application appears provisioning in the user's `/apps`, with NO operator-console action. | hw224 | ❌ | hw228 DIRECT-WALK 2026-07-07: chepherd solo-agent runtime NOT deployed — no chepherd/agentic pods on hw228 (only sandbox-controller). No agent to chat with. ❌ ‖ hw228 2026-07-07 (verified): agentic/chepherd chat runtime NOT deployed — only sandbox-controller runs; the openova-mcp product-surface server (#3988) that the solo agent would drive is built+RBAC-tested but not wired into the sandbox runtime (its Blueprint chart + HTTP/SSE Service + .mcp.json injection is the #3988 EPIC's remaining/deferred work, task #899). Row stays ❌ pending the agentic runtime deploy. Refs #3988| hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: neither the OpenOva MCP nor the agentic runtime (agenity) is deployed on hw227 — chepherd solo-agent chat cannot run. Refs #3988 ‖ **GATE CONFIRMED 2026-07-07 (repo inspection):** #3988 built the openova-MCP SERVER (products/openova-mcp/ — Go cmd/internal/{identity,tools,catalystapi}) but NOT its deployment layer: NO Chart.yaml, NO Dockerfile, NO blueprint.yaml, NOT in the bootstrap-kit or catalog. So the RBAC-scoped MCP facade cannot be deployed on ANY Sovereign (confirmed absent on hw228 — only sandbox-controller runs). These rows are gated on PACKAGING the server (image build workflow + Helm chart + blueprint.yaml + bootstrap-kit slot + per-Org KC identity wiring), a substantial build cycle, not a simple deploy. Refs #3988. |
| 222 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | The agent-created application **converges and appears in the user's Org** (chat-driven app creation works end-to-end). | hw224 | ❌ | hw228 DIRECT-WALK 2026-07-07: chepherd agent runtime NOT deployed (no chepherd/agentic pods) → agent-created app path untestable. ❌ ‖ hw228 2026-07-07 (verified): agentic/chepherd chat runtime NOT deployed — only sandbox-controller runs; the openova-mcp product-surface server (#3988) that the solo agent would drive is built+RBAC-tested but not wired into the sandbox runtime (its Blueprint chart + HTTP/SSE Service + .mcp.json injection is the #3988 EPIC's remaining/deferred work, task #899). Row stays ❌ pending the agentic runtime deploy. Refs #3988| hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: no agentic runtime/MCP on hw227 — agent-created application convergence cannot be exercised. Refs #3988 ‖ **GATE CONFIRMED 2026-07-07 (repo inspection):** #3988 built the openova-MCP SERVER (products/openova-mcp/ — Go cmd/internal/{identity,tools,catalystapi}) but NOT its deployment layer: NO Chart.yaml, NO Dockerfile, NO blueprint.yaml, NOT in the bootstrap-kit or catalog. So the RBAC-scoped MCP facade cannot be deployed on ANY Sovereign (confirmed absent on hw228 — only sandbox-controller runs). These rows are gated on PACKAGING the server (image build workflow + Helm chart + blueprint.yaml + bootstrap-kit slot + per-Org KC identity wiring), a substantial build cycle, not a simple deploy. Refs #3988. |
| 223 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4553](https://github.com/openova-io/openova/issues/4553) | The chepherd agent's actions are **RBAC-scoped to the user's Org** (UI-parity): the openova-mcp `list_applications` returns ONLY this Org's apps (a 2nd Org's are filtered out), a cross-Org `get_application` → **403**, and the agent cannot create/mutate in any other Org — enforced by `openova-mcp/internal/identity` Org-claim, not just the chat prompt. | hw224 | ❌ | hw228 DIRECT-WALK 2026-07-07: chepherd agent runtime NOT deployed → RBAC-scoping untestable. ❌ ‖ hw228 2026-07-07 (verified): agentic/chepherd chat runtime NOT deployed — only sandbox-controller runs; the openova-mcp product-surface server (#3988) that the solo agent would drive is built+RBAC-tested but not wired into the sandbox runtime (its Blueprint chart + HTTP/SSE Service + .mcp.json injection is the #3988 EPIC's remaining/deferred work, task #899). Row stays ❌ pending the agentic runtime deploy. Refs #3988| hw227-challenge-2026-07-06T03:20: CHALLENGED — missing precondition: no MCP/agent on hw227 — chepherd RBAC-scoping cannot be exercised. Refs #3988 #4553 ‖ **GATE CONFIRMED 2026-07-07 (repo inspection):** #3988 built the openova-MCP SERVER (products/openova-mcp/ — Go cmd/internal/{identity,tools,catalystapi}) but NOT its deployment layer: NO Chart.yaml, NO Dockerfile, NO blueprint.yaml, NOT in the bootstrap-kit or catalog. So the RBAC-scoped MCP facade cannot be deployed on ANY Sovereign (confirmed absent on hw228 — only sandbox-controller runs). These rows are gated on PACKAGING the server (image build workflow + Helm chart + blueprint.yaml + bootstrap-kit slot + per-Org KC identity wiring), a substantial build cycle, not a simple deploy. Refs #3988. |
| 224 | convergence | [#4272](https://github.com/openova-io/openova/issues/4272) | Per-Org `bp-openclaw` controller reaches Ready (1/1) on a Sovereign — the idle-reaper can list its per-user pods and `readyz` passes. | hw227 | ❌ | hw228 acme DIRECT-WALK 2026-07-07: no bp-openclaw pod/HR in ns acme (kubectl) — acme's vcluster runs only bp-postgres + WordPress. openclaw not installed. Needs day-2 app install into the Org. ❌ ‖ FLIP-ATTEMPT 2026-07-07: tried day-2 install into acme (working vcluster Org) via POST /api/v1/org/applications (owner session, X-Tenant-Host: console.acme.omani.homes) — endpoint works (got to blueprintRef.version validation) but bp-openclaw/bp-stalwart-tenant are NOT in the customer marketplace catalog (which offers ghost/cal-com/dify/... as customer day-2 apps); openclaw/mail are PLATFORM per-Org apps installed as part of the funnel stack (walkstranger* had bp-openclaw + bp-stalwart-tenant HRs). So they can't be day-2-installed — gated on the per-Org platform stack CONVERGING, blocked by #4813 (cnpg dies in host-ns Orgs). Refs #4813 ‖ hw228 2026-07-07 CORRECTED (live kubectl): bp-openclaw@0.2.13 + bp-stalwart-tenant@0.1.12 ARE registered (prior 'not in catalog' evidence was WRONG); the day-2 raw Application-CR path fails `namespaces "org-example" not found` because it doesn't inject tenant.namespace. Real blocker: NO Org on hw228 has the full funnel app bundle (keycloak/newapi/openclaw/stalwart) — acme is an M-plan vcluster that only got WordPress day-2. The canonical orgTenantBPOpenClaw overlay is CORRECT (sets tenant.namespace={{.Namespace}} + dependsOn bp-keycloak) and applies to S-plan host-native funnel Orgs. Fresh S-plan funnel-Org walk (slug walk-stranger) dispatched to verify. Refs #4272 #4307 ‖ hw228 walk-stranger S-plan 2026-07-07 ROOT CAUSE: openclaw controller 0/1, pod quota-DENIED ('exceeded quota: plan-quota, requested cpu=500m, used=1600m, limited=2') — bp-stalwart-tenant requests a guaranteed 1 CPU core (50% of the S-plan 2-vCPU). The #4272 openclaw render itself is CORRECT (Deployment image openclaw-controller:7ade865, OIDC issuer shared-realm, HTTPRoute openclaw.walk-stranger created). Fix shipped: PR #4834 (#4833) right-sizes stalwart CPU 1→500m; row flips once 0.1.13 rolls + openclaw schedules. Refs #4272 #4833 ‖ hw228 2026-07-07 CONFIRMED blocker = #4834 quota (NOT Kyverno): walk-stranger openclaw ReplicaSet FailedCreate 'exceeded quota: plan-quota, requested cpu=500m, used=1600m, limited=2' (stalwart hogs 1 core). The 3 Kyverno PolicyViolation events (scrape/readonlyrootfs/multi-replica) are validationFailureAction=Audit — warnings, not the blocker (wordpress/stalwart converged with the same). #4834+#4835 (stalwart 1→500m) merged; flips on next fresh prov. ‖ **LIVE-CONFIRMED 2026-07-07 on hw228 walk-stranger Org (kubectl):** the openclaw controller ReplicaSet is quota-denied — event `FailedCreate ... pods openclaw-bp-openclaw-controller-... is forbidden: exceeded quota: plan-quota, requested requests.cpu=500m, used 1600m, limited 2` (bp-openclaw HR = 'Helm install failed ... context deadline exceeded'). Quota breakdown: mysql 250m + **stalwart-tenant StatefulSet 1000m (1 core — OLD chart 0.1.12, PRE-#4834)** + webmail 100m + wordpress 100m ≈ 1600m of the 2-core S-plan quota → openclaw's 500m can't fit. This is EXACTLY #4834: the merged fix (stalwart 1→500m, chart 0.1.13) frees precisely 500m (1600→1100, +500=1600<2000) so openclaw schedules. walk-stranger carries the pre-fix chart; NO in-place fix (bp-stalwart StatefulSet upgrade fails + Kustomization reverts live patches — reference_stalwart_tenant_no_inplace_upgrade). Flips ✅ on a fresh prov / chart-roll that ships stalwart 0.1.13. The blocker is verified, not speculative. | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: bp-openclaw not in the acme WordPress-only stack (0 openclaw pods in ns acme). Needs a fresh Org purchasing openclaw. Refs #4272 ‖ 2026-07-08 ROOT-CAUSE (live kubectl, non-#4851): openclaw controller Deployment is 0/1 — ReplicaSet `FailedCreate: exceeded quota plan-quota, requested cpu=500m, used=1600m, limited=2`. Cause: hw228's LIVE `bp-stalwart-tenant@0.1.12` still requests **1000m** CPU, leaving <500m free in the S-plan 2vCPU. The fix — **bp-stalwart-tenant 0.1.13 right-sizes CPU 1→500m (#4834/#4833, commit cb8c17248, MERGED on main)** — frees the quota so openclaw's 500m fits. Not reached on this env: stalwart-tenant is a StatefulSet (no in-place resource upgrade), so it flips on a **fresh prov** (which builds the 0.1.13 StatefulSet at 500m). ❌ pin-lag, not a code gap. |
| 225 | convergence | [#4278](https://github.com/openova-io/openova/issues/4278) | Per-Org `bp-newapi` HR reaches Ready on a fresh Org — the admin-promote post-install hook Completes (its ServiceAccount exists) so the chart finishes. | — | ☐ | — | — |
| 226 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The customer's PURCHASED app actually RUNS — every funnel Org's `tenant-<slug>-apps` Kustomization reconciles into a running pod (the app is reachable, not 404). | — | ☐ | — |

---

### Fresh-prov 2026-06-26 — session-fix acceptance (rows 227–239)

These 13 rows are the acceptance checks for the issues genuinely resolved this session. Each pairs the **root cause** with the **fresh-prov surface to walk** + the **expected evidence**. All start `☐` — the fresh-prov walk fills the verdict + evidence cells in. (Refs #4279.)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| 227 | delivery | [#4415](https://github.com/openova-io/openova/issues/4415) | A catalog/seed version bump reaches the live **Blueprint CR zero-touch** — bumping a `bp-*` pin in the catalog seed lands on the cluster Blueprint CR with NO manual `kubectl patch` and NO `resource-policy: keep` annotation pinning a stale version. PASS = after a seed bump, `kubectl get blueprint bp-<x> -o jsonpath='{.spec.version}'` matches the seeded version within one reconcile, the seeder owns the field (no `helm.sh/resource-policy: keep`), and the rolled chart renders on the cluster. | hw227 | ⚠️ | hw227-walk-2026-07-06T00:47Z: read-only half PROVEN — kubectl get blueprints.catalyst.openova.io (81 CRs) shows 0 with helm.sh/resource-policy=keep (NO stale-version pinning), and the seed->Gitea->Flux->Blueprint-CR propagation channel is confirmed present (console Edit-IaC writes the blueprint.yaml IaC source that Flux reconciles into the CR, verified row 144). Live-bump half (edit a bp-* seed version -> CR matches within one reconcile) NOT executed on hw227: it is a converged env now hosting live customer Org acme, and a live chart-version bump risks thrashing it (STONE: never thrash a converged env). Live-bump verification deferred to #4415 on a disposable env. Refs #4415 ‖ hw228 2026-07-09 (#4415 fix-agent, orchestrator-verified + MERGED PR #4864): the seed→catalog-CR→HR propagation mechanism is already correct (resource-policy:keep removed); the REAL defect was systemic seed-pin LAG — 38 of 81 catalog-seed delivery pins had drifted BEHIND their published component charts (e.g. openclaw seed 0.2.13 vs #4863's 0.2.16, keycloak-src 1.0.0 vs 1.5.5), so the catalog served STALE charts. PR #4864 (merged, chart 1.4.1033) syncs all 38 to their published versions. Layer-3 (existing-Org tenant-repo freeze per #4384 seed-once) is a separate version-reconciler, scoped not attempted. Recommended CI guard: fail when any seed source.version < component Chart.yaml. Stays ⚠️ — durable delivery fix shipped, end-to-end zero-touch proof lands on the fresh-prov endgame. ‖ 2026-07-09 catalog pin-sync COMPLETE + guarded: #4867 MERGED (5 more stale pins #4864 missed — bp-rtz-vcluster/sandbox/dmz-vcluster/mgmt-vcluster/wordpress-tenant — all synced) + a NEW CI guard TestCatalogSeed_DeliveryPinsNotBehindComponentCharts that fails on any future seed-pin drift (semver-aware). The catalog now carries current charts; the recurrence is permanently gated. Stays ⚠️ — end-to-end zero-touch propagation to the live Blueprint CR proves on the fresh-prov endgame. |
| 228 | delivery | [#4435](https://github.com/openova-io/openova/issues/4435) | A **re-prov AFTER a wipe** does NOT false-fail on orphaned `catalyst-*` VPCs — the orphan-VPC janitor sweep removes the leftover VPC(s) from a prior wiped deployment before the new prov allocates its network. PASS = wipe an env, immediately re-prov on the same region; provisioning does NOT error `VPC already exists` / quota-conflict; the janitor log shows the orphaned `catalyst-*` VPC(s) swept, and the new prov reaches Phase-1 green. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: re-prov-after-wipe orphan-VPC handling = mothership/janitor concern; R1/#4454 protect-side proven (janitor did NOT reap ready hw224 after 3h42m)‖ §F: prov green under cap; orphan-sweep code present (42 VPC/21 keypair evals); destructive sweep not exercised (no orphans). Protect proven in #4454 ‖ 2026-07-09 VERIFY-THEN-FIX (#4272 fix-agent, orchestrator-noted): the orphan-VPC reclaim is FULLY IMPLEMENTED, not a gap — provisioner.go:1986 runs a Huawei VPC-quota pre-flight before every prov → VPCReclaimPreflight → huawei.SweepOrphanVPCs(destructive=true), with #4614 protection (reclaimProtectSet protects EVERY live deployment's prefix) + regression tests (reclaim_protect_4614_test.go, sweep_orphan_vpc_test.go). LIVE kom4dc: project f27698... holds exactly 2 catalyst VPCs (hw228 a+b, both skipped as the active deployment), WOULD-REAP orphans = 0, quota 2/5. No code change warranted (no PR — shipping one would be theater). Stays ⚠️ — end-to-end wipe→re-prov no-false-fail proves on the fresh-prov endgame. NOTE: the endgame prov is VPC-quota-CLEAR (wiping hw228 frees its pair → clean 2-region headroom). |
| 229 | delivery | [#4409](https://github.com/openova-io/openova/issues/4409) [#4428](https://github.com/openova-io/openova/issues/4428) | **bootstrap-kit Kustomization Ready=True + plane-isolation admits CNPG→kube-apiserver egress** — on the fresh prov `kubectl -n flux-system get kustomization bootstrap-kit` is `Ready=True` (no orphaned `dependsOn` / CRD dry-run rejection), AND the plane-isolation NetworkPolicy permits the CNPG operator's egress to the kube-apiserver so Cluster admission/patch succeeds. PASS = bootstrap-kit `Ready=True`; CNPG operator pod Running; a `Cluster` reconciles (no `context deadline exceeded` / x509 webhook failure caused by blocked apiserver egress). | — | ☐ | — |
| 230 | delivery | [#4427](https://github.com/openova-io/openova/issues/4427) | **`ghcr-pull` reflects into a bare-slug per-Org namespace** — the emberstack reflector copies the `ghcr-pull` imagePullSecret into a per-Org namespace whose name is a bare slug (e.g. `org-<slug>` / a non-UUID form), so private `ghcr.io/openova-io/*` app images pull (no `ImagePullBackOff` 401-anonymous). PASS = a fresh Org's namespace has the reflected `ghcr-pull` secret; a per-Org app pod pulling a private image reaches Running (no 401/ImagePullBackOff). | — | ☐ | — |
| 231 | delivery | [#4411](https://github.com/openova-io/openova/issues/4411) | **bp-postgres singleton render carries the operator-probe NetworkPolicy** — a singleton (non-HA) bp-postgres render still ships the NetworkPolicy that admits the CNPG operator's health/readiness probe, so a singleton Postgres reaches Ready (the probe is not blocked). PASS = a singleton bp-postgres Application reaches `Ready=True`; the operator-probe NetworkPolicy is present in the rendered manifest; the CNPG operator can probe the instance. | — | ☐ | — |
| 232 | apps | [#4272](https://github.com/openova-io/openova/issues/4272) | On a fresh funnel Org, **openclaw `/readyz` returns 200** — the 4-layer chain (controller image · reaper apiserver egress NetworkPolicy · TLS trust · OIDC) all resolve so the idle-reaper lists per-user pods and readiness passes. PASS = `bp-openclaw` HR `Ready=True`, controller pod 1/1 Running, and `curl https://<openclaw-host>/readyz` → 200 (no `reaper: list pods … context deadline exceeded` loop). | — | ☐ | — | — |
| 233 | apps | [#4422](https://github.com/openova-io/openova/issues/4422) | On a fresh funnel Org, **WordPress serves HTTP 200** — the mysql DB pod is Guaranteed-QoS (requests==limits, schedulable under the per-Org LimitRange), the HTTPRoute is present, and the landed host is pool-aligned to the console-ELB EIP. PASS = `curl https://wordpress.<slug>.<pool-tld>/` → 200 (or 302→install then 200), mysql pod QoS=Guaranteed + Running, HTTPRoute parents the console gateway, host resolves to the console-ELB EIP (not the primary ELB). | — | ☐ | — |
| 234 | apps | [#4307](https://github.com/openova-io/openova/issues/4307) | On a fresh funnel Org, **`mail.<slug>` serves HTTP 200 (SnappyMail webmail)** — the stalwart-tenant webmail SPA loads. PASS = `curl https://mail.<slug>.<pool-tld>/` → 200 serving the SnappyMail SPA; stalwart-tenant pod Running; HTTPRoute present (no bare envoy 404). | — | ☐ | — | — |
| 235 | apps | [#4437](https://github.com/openova-io/openova/issues/4437) | On a fresh prov, **grafana SSO logs in 3/3 → 200** — the bp-sso-bridge per-tick credential refresh keeps the grafana OIDC client secret in lockstep with Keycloak, so the SSO round-trip completes. PASS = grafana `/login/generic_oauth` → Keycloak → back signed-in (200, user in the avatar) across 3 consecutive attempts; bp-sso-bridge refreshes the client cred each tick (no stale-secret 401). | — | ☐ | — |
| 236 | apps | [#4439](https://github.com/openova-io/openova/issues/4439) | On a fresh multi-region prov, **region-B keycloak reaches Ready** — the cross-region shared-pg write host is correct so region-B Keycloak connects to the primary Postgres and completes realm import. PASS = `bp-keycloak` HR `Ready=True` in the region-B cluster; keycloak pod Running; realm import succeeds (no DB-connect / wrong-write-host failure); `keycloak.<host>` → 302 SSO. | — | ☐ | — |
| 237 | spine | [#4416](https://github.com/openova-io/openova/issues/4416) | **spine → Continuum round-trip is Healthy + `continuumRef` is populated** — the spine adopts the Continuum and the round-trip reports Healthy. PASS = the Continuum CR reports `Healthy` (not Degraded), the spine's `continuumRef` field is populated with the live Continuum, and a witness round-trip completes. | — | ☐ | — |
| 238 | postgres | [#4282](https://github.com/openova-io/openova/issues/4282) | **Per-Org postgres-in-vCluster → host-side CNPG Ready** — a per-Org Postgres requested inside the Org's vCluster is reconciled by the host-side CNPG operator and reaches Ready. PASS = the per-Org `Cluster` CR is `Ready` (host-side CNPG drives it), the Postgres pod is Running with Guaranteed QoS, and the per-Org app connects (no empty-DSN / disableWait deadlock). | — | ☐ | — |
| 239 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | **Crossplane adoption populates Observe-only on the fresh prov** — `providers.pkg.crossplane.io` and `cloudadoptions.compose.openova.io` are non-empty and reach Synced+Ready, observing (NOT re-provisioning) the OpenTofu-built infra. PASS = `kubectl get providers.pkg.crossplane.io` → ≥1 Healthy; `kubectl get cloudadoptions.compose.openova.io -A` → non-empty Synced+Ready with `external-name` = the real ELB id; the live ELB/nodes are untouched (Observe-only). | — | ☐ | — |
| 240 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | No-nodePort gateway serves externally on a FRESH zero-touch prov: ELB → node:443 hostPort → hostNetwork envoy; gateway Service carries ZERO nodePorts (§854 literal). | — | ☐ | — |
| 241 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | Mothership writes `ready` ONLY after an external HTTP answer from https://console.<fqdn>/ (no more false-green). | hw224 | ⚠️ | hw224-fulltable-2026-07-04: mothership-side readiness gate (#4707); hw224 reached ready + console 200 external. Mothership concern |
| 242 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | 2-region BCP admission floor: a 1-region POST with no explicit bcpTopology is rejected at the LIVE production API. | hw224 | ⚠️ | hw224-fulltable-2026-07-04: 2-region BCP admission floor (#4708) — mothership POST-time gate; hw224 IS 2-region (passed the floor). Mothership concern ‖ 2026-07-03 negative wire-test vs console.openova.io: `POST /sovereign/api/v1/deployments` (1 region, no topology) → **HTTP 400** "deployment has 1 region(s) and no explicit bcpTopology — the BCP agreement default is multi-region (>=2 regions, Pillar 2)…". Nothing created. ‖ hw228 2026-07-09 LIVE PARTIAL (production API console.openova.io, founder-token auth confirmed GET /sovereign/api/v1/deployments = HTTP 200): every 1-region POST is REJECTED with HTTP 400 (validated bodies hit SKU/hetzner-token gates that run before the region-count check). The #4706 BCP floor itself (provisioner.go:1076-1086 'bcpTopology=%q requires len(regions)>=2 ... Pillar 3 zero-transactions-lost cannot be honoured single-region') is in the DEPLOYED image + unit-tested (provisioner_test.go, dr_standby_integrity_test.go). Isolating the exact floor-line rejection LIVE needs a fully-credentialed body (real Hetzner token + orderable SKUs) which risks a real provision — NOT run. Stays ⚠️: production API live+validates+rejects-1-region proven; exact-floor live-hit is the remaining bar. |
| 243 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | Tenant DNS split-horizon on a fresh prov: wildcard hosts → primary ELB EIP, console → console ELB EIP. | — | ☐ | — |
