# UAT — Sovereign acceptance walk (hw286 live-walk 2026-07-23 (dep 8252169e5cd7a2dc, hw286.omani.works, cc=true 11/11, G12 failover RPO=0)) (RESET 2026-07-26 — pending hw289)

> **hw269 AUTHED CONSOLE + FUNNEL + MCP WALK — 2026-07-18 (dep `a47b82b25fbd5baf`, hw269.omani.works, 2-region me-east-215-a/b, cutover mid-flight at step-03 harbor-prewarm/cutoverComplete=false — driven separately, NOT claimed here).** Owner session minted via the real passwordless-PIN flow, browser-driven this time end-to-end (POST /auth/pin/issue triggered from the actual login form → IMAP PIN over mothership Stalwart → PIN typed into the actual verify form → RS256 catalyst_session cookie set by the real browser flow; whoami→200 tier=owner). 19 screenshots `docs/sessions/2026-07-18/hw269-authed-console/`. No cluster mutation beyond the funnel Org provisioning itself (the walk); NO NodePort (row 240 already ✅ pre-stamped, re-confirmed 0 nodePorts both regions); bastion untouched. **Dashboard** (167 ✅) owner treemap 193 items + identity chip, no login form. **Cloud** graph live 2-region (Cloud 1/1·Region 2/2·Cluster 2/2·NodePool 4/4·WorkerNode 12/12·LoadBalancer 2/2) → rows 65/66/199-204 ✅ (Gateway Programmed=False is expected §854 DIRECT-serving cosmetic, not a fault; HTTPRoutes/NetworkPolicies/CiliumNetworkPolicies live counts 17/30/44 cross-checked against kubectl). **Jobs** (#3646) full typed table 167 rows → 168/169/170/171/172/173/175 ✅ (Status=failed filter honestly empty-state, 0 genuine Failed rows this env — cutover healthy); 174 ⚠️ (no Failed row to exercise Re-run, same as hw268). **Catalog** (#3668) 124/125 ✅ (Alloy card + detail hero/About/Instance). **SSO** (#3374) 30/31/32/34/37 ✅ (grafana /api/user 200 isGrafanaAdmin=true; gitea "emrah.baysal - Dashboard"; harbor /harbor/projects; **Keycloak sovereign-realm admin console landed with NO master-realm login** — a first-time clean flip vs prior envs' session-walled ⛔; newapi "Login successful!" account sovereign_2). **Orgs/Showback** (#3383/#3687) 116/117/118/25 ✅, **21/22/23 ✅ — row 23 FLIPPED ⛔→✅**: the funnel-provisioned customer Org `hw269uatwalk` produced the first-ever 2nd-Org showback row this cycle (353.25 units, mysql+wordpress apps, distinct from `__platform__` and `sovereign (parent)`). **Funnel E2E (#5104/#5123, marquee pair)**: owner BSS mint voucher HW269UATWALK (20 OMR) → stranger redeem (brand-clean, no openova.io leak) → plans/apps/addons/bcp/review/checkout (rows 72/75/77-85 ✅, row 83 ⚠️ slug/TLD shown one step later than "review", row 86 ⚠️ generic spinner not a labelled step-timeline) → PIN-less emailed-code sign-in (no password) → credit covers 100% (0 OMR due) → Org `hw269uatwalk` provisioned (customer/plan=s/real billing) → per-Org console landed zero-click (rows 87/88 ✅) → **row 89 FLIPPED ❌→✅**: Applications view shows WordPress card INSTALLED, live `GET /api/v1/org/applications` wire-proof (finding: `generatedAt` field is an identical hardcoded literal across hw256/hw269, cosmetic not correctness) → **row 90 ✅**: `kubectl -n hw269uatwalk get deploy,pods` wordpress+mysql both 1/1 (mysql image-pull stalled ~16min on the pre-cutover harbor.openova.io/proxy-dockerhub path, same root cause as harbor-prewarm's own logged #5095 note — resolved on its own, not an architectural fault) → `https://wordpress.hw269uatwalk.omani.rest/` → HTTP 302→wp-admin/install.php→200, live WordPress Installation page rendered. #5123-B confirmed: member session still fires owner-scoped `/api/v1/deployments/{id}` + `/console-ui/sidebar-entries` → 403 on every page load (known cosmetic frontend gap, recorded not fixed). **MCP north-star (#5175/#5167)**: row 211 FLIPPED ⚠️→✅ — real owner RS256 bearer → `tools/list` full 7-tool surface + `list_applications` → 200 real 8-app JSON (spine-gitea/harbor/keycloak/openbao, newapi, shared-pg trio) — the whoami-fallback data round-trip the prior hw266 stamp said "pends the real PIN-login session token" is now proven end-to-end; rows 212/213 stay ⚠️ (Org-scoped/cross-Org RBAC checks need a distinct RS256 Org-member bearer this session could not extract — the httpOnly cookie can't be read client-side and the marketplace HS256 token is correctly rejected by MCP). NO fabrication.

> **hw258 AUTHED CONSOLE WALK — 2026-07-16 (dep `4a0390c4eb0380ca`, hw258.omani.works, 2-region me-east-215-a/b, region-a 63/63 healthy; cutover FAILED #5114, env to be wiped after #5128 — evidence banked now, no cutover-gated surfaces claimed).** Owner session minted via the real passwordless-PIN flow (POST /auth/pin/issue → IMAP PIN over mothership Stalwart → /auth/pin/verify → RS256 catalyst_session injected into Playwright; whoami→200 tier=owner). Browser-driven, no cluster mutation, no NodePort, bastion untouched. 9 screenshots `docs/sessions/2026-07-15/hw258-authed-console/`. **Dashboard** (167 ✅) owner treemap 190 items + identity chip. **Cloud** graph live 2-region (Cloud 1/1·Region 2/2·Cluster 2/2·NodePool 4/4·WorkerNode 8/8·LoadBalancer 2/2, 18 nodes/10 edges) → rows 65/66/199/200/203/204 ✅ (200 ❌→✅ flip: Networking lens empty-data regression #3987/#3998 absent here), 201/202 ⚠️ (NP+CNP folded into one "NetworkPolicy 35/35" chip, no split-by-kind page). **Jobs** (#3646) full typed table 167 rows → 168/169/171/173 ✅; **174/175 ✅ FIRST-EVER exercise of the Re-run/Retry control on genuine Failed rows** (cutover-harbor-prewarm→"Re-run", cutover-step-gitea-token-mint→"Retry reconcile"; all prior envs 0-failed ⛔); 172 ⚠️ (Status=failed filter returned 0 while a churning cutover had just healed the 2 Failed rows — ambiguous filter-scope-vs-live-heal). **Orgs/Showback** (#3383/#3687) 116/117/118/21/22/25 ✅ (directory 2/2 internal Orgs, __platform__ 20475.91-unit overhead roll-up + real per-app attribution), 23 ⛔ (no customer Org sample). **Catalog** (#3668) 124/125 ✅ (94 cards, Alloy detail hero+v1.0.2). **SSO** (#3374) 30/31 ✅ (grafana /api/user 200 isGrafanaAdmin=true; gitea "emrah.baysal - Dashboard" :443, both 0-password-input). NO fabrication.

> **hw236 CONCLUSION PROV — 2026-07-10 (dep `d412876d4c00c8fb`, hw236.omani.works, 2-region me-east-215-a/b, console READY).** Backing-state consolidation from 5 read-only kubectl/kcadm walkers (no browser, no cluster mutation): **22 rows stamped ☐→✅** with a `hw236-2026-07-10:` evidence prefix — cloud per-kind **199-203** (Gateways 2 · HTTPRoutes 15 · NetworkPolicies 30 · CiliumNetworkPolicies 42-all-VALID · LoadBalancer clustermesh-apiserver 212.72.24.31; stale expected-counts refreshed to live), jobs data-model **171** (`openova_flow` store populated 47 nodes/4097 events, typed Kinds from real HR/Job/CronJob observations, zero synthetic), CRD tier-enum **215** (`[org,corporate]`), Keycloak-realm SSO backing **41-44** (owner→/sovereign-admins→catalyst-admin), robustness sso-bridge/plane-isolation/postgres/gitea/sandbox **R3-R9/R11/R19/R22**, and region-b steady-state **R13** — **the recurring region-b Degraded / #3380 host-path black-hole (#4656/#4942) is RESOLVED on hw236** (cilium-health 11/11 reachable both regions, ClusterMesh 1/1 :12379 both directions, cross-region pod→primary:5432 datapath OPEN, cnpg-pair replica SYNC/remote_apply RPO=0). Cutover DORMANT + handover SEALED → region-kill/cutover GO. Landing-render SSO (36-40/45), funnel (72-95), topology-DR tab (51-64), catalog (124-158), showback (21-25), keycloak-admin (34), Contexts tab (4/14/24 — data-layer real but the Application-CR `dependsOn` binding is empty so the tab may render empty), and MCP rows held for the browser walk (render-owned) or genuinely gated. Reflector row 230 backing (ghcr-pull reflected into per-Org `acme236` ns) is proven but its full PASS awaits a per-Org app pod pulling a private image → held ⚠️. NO fabrication, NO NodePort, bastion untouched.

> **hw235 CONCLUSION PROV — 2026-07-10 (dep `a3818d29c833ab1e`, hw235.omantel.biz, console→200 READY, 2-region me-east-215-a/b, clean prov on the COMPLETE 12-fix train).** The definitive fresh-prov validation of this session's deploy-lagged fixes — everything baked in, zero live-patching. Live evidence re-queried directly. NO fabrication, NO NodePort, bastion untouched.
> - 🎯 ✅ **region-b Degraded ELIMINATED (#4943 CONFIRMED)** — the recurring multi-week issue is FIXED. cilium cluster-health region-b **12/12 reachable** (was 6/12 pre-#4943); the #4942 host→remote-pod black-hole is CLEARED (region-b CP-node HOST netns → region-A keycloak pod TCP-OK — the exact native-routing-CP-node SG gap #4943's `mesh_ingress_cidrs` fan-out fixes). ClusterMesh 1/1 both regions. The cnpg:5432 host-netns denial is a CORRECT #4869 CNP restriction (pod-identity only), not a black-hole. Cosmetic residue only: region-A 11/12 = a `:4240` health-responder RST on the region-b CP node (TCP RST, not a datapath drop) — minor follow-up.
> - ✅ **Convergence FULL** — region-a 62/65 HR (3 non-Ready = by-design hcloud no-ops), region-b 58/65 (3 hcloud + 4 by-design suspend on the DR-secondary). 12/12 nodes.
> - ✅ **Pillar-3 DR (#3374/#3375)** — 5 continuum CRs Healthy (primary me-east-215-a), cnpg primary 3/3, region-b sync replica **lag 0 → RPO=0s**, cluster healthy → RTO≈0s. Cross-region POD datapath (#4656) region-b→region-a:5432 OK, streaming.
> - ✅ **useraccess grants (#4841/#4935) — bind-verb fix WORKED** — UserAccess CRs Phase=Active (none Failed), owner grant materialized a real `useraccess-owner-…-tier → openova:tier-owner` ClusterRoleBinding (the RBAC escalation-prevention `bind` fix validated live on a fresh prov).
> - ✅ **Showback #3687** real (`/api/v1/org/consumption` = 22685 units, per-app breakdown). **Cutover #3379 dormant** (11 step-CMs present, 0 executed, Tethered — ready for Wave F).
> - Process: this hw235 conclusion prov is the corrected re-fire after a `parentDomains.primary` create.json input-error (root-caused + fixed + memorized); the DNS fix is end-to-end proven (console→200 at the correct Huawei EIP). Conclusion browser walk (owner session, 2026-07-10 — CORRECTED, honest): ✅ **Funnel E2E** (Org acme235 created), ✅ **#4928 PIN**, ✅ **#4925 per-Org console** (console.acme235→200), ✅ **#4937 customer self-install** (201), ✅ **#4938 owner-redeem-redirect**, ✅ **#4935 useraccess grants (admin)**; SSO 5/6, Catalog/Jobs/Treemap(WorkerNode 12/12)/Orgs-showback/Cutover-Tethered all ✅. **❌ 3 GENUINE FAILURES FOUND (filed):** ❌ **G10 #4840** placement Edit→Apply STILL HTTP 400 'placement.mode required' — the fold IS in the deployed image (2d41a36⊃e937cda9d) but doesn't populate mode on the PUT (real fold gap → **#4950**); ❌ **per-Org WordPress #4936/#4939 does NOT serve externally** — pod runs 1/1 but the `oidc-config` post-install hook Job hangs 13m → HR post-install timeout → no HTTPRoute → Envoy 404 (→ **#4951**); ❌ **bp-openclaw@0.2.16** install fails `namespaces "org-example" not found` (hardcoded placeholder → **#4952**). Console reads Degraded (per-Org app HR stalls, NOT the #4943 datapath which is GREEN). NOTE: my earlier "WordPress PASS" (pod 1/1) was WRONG — pod-running ≠ app-serving; the walk corrected it. Cutover machinery all 5 tickets verified sound (#4660/#4666/#4632/#4692/#4652). **WAVE-F FINALE (agent a0826100):** ✅ **Region-kill #4275 PASS** — killed region-a primary, region-b promoted **RTO=1.3s** (≤30s target), **RPO=0** (1000 rows + post-kill write), split-brain-guarded restore + re-gate green (Pillar-3 fresh-prov failover proof). ⚠️ **Cutover #3379 wedged at step-03** harbor-prewarm — `skopeo copy` FATALs on a `tag@digest` ref (first openova-io image pinned both ways); fail-closed abort (env NOT degraded, pre-registry-pivot). Fixed durably: **#4955/PR #4957** `normalize_ref()` chart 0.1.108→0.1.109; cutoverComplete=true roll-gated to next prov. Conclusion-walk defects all fixed: G10 #4950→PR#4958, WordPress #4951→#4954✅, OpenClaw #4952→#4953✅.

> **hw234 KEYSTONE PROV — 2026-07-10 (dep `d71e9c7e8d0a15da`, hw234.omantel.biz, 2-region me-east-215-a/b, converged zero-touch on the audited 10-fix train).** Live evidence re-queried directly (both region kubeconfigs pulled from the mothership `catalyst-api-deployments` PVC + curl) — NO fabricated passes, NO NodePort, bastion untouched. Walkers: convergence-sweep `a4f290064e3e6d049`, MCP `a56c402400637cbb9`, funnel Wave-D `aaf419937f4d69710` (in flight), + orchestrator row-3 probe.
> - ✅ **Convergence FULL both regions** — region-a 64/68 HR True, region-b 58/65 True; every non-True is a by-design hcloud no-op (`bp-cluster-autoscaler-hcloud`/`bp-hcloud-ccm`/`bp-velero` — Huawei variants `-hcs`/`bp-huawei-evs-csi` active) or region-a-only control-plane (bp-catalyst-platform/continuum/sandbox/cutover). **12/12 nodes Ready, ZERO unhealthy pods.**
> - ✅ **Region-b #4918 validation PASS** — `keycloak-0` Running **1/1 stable** (NOT 401-wedged; the shared-keycloak-DB fix held on this prov); full region-b app-chain Running (grafana 3/3, gitea, guacamole, powerdns); region-b CNPG replicas 3/3.
> - ✅ **Pillar-3 topology #3375 DATA** — CNPG pair primary 3/3 (region-a) + replica 3/3 (region-b), continuum PHASE=Healthy **RPO=0s**; shared-pg/-b/-c + spine-gitea/harbor/keycloak all active-hot-standby with region-b replicas. Topology-*tab* UI render = remaining row-60 leg (browser walk).
> - ◑ **Placement #3969 backend reconciles LIVE** — `.spec.placement` first-class on the Application CRD (targets[]/regions model), `continuum-controller` 1/1, every Application populated (`singleton`/`active-hot-standby`/`active-passive`). Remaining G10 bar = console Edit-placement Apply→200 re-walk on this post-#4840 image (browser).
> - ✅ **Funnel E2E COMPLETES + all 3 hw233 blockers GREEN** (Wave-D authed funnel walk, customer Org `acme`/acme.omani.homes/Plan M, voucher `VCH-8TW6B3AWJRSS`): redeem→plans→apps→bcp→review→checkout→launch. **#4928/#4924 PIN funnel ✅** (code→emrah.baysal+acme, IMAP PIN 607201→signed in — the EXACT hw233 wedge point, now completes); **#4925 per-Org gateway ✅** (console.acme.omani.homes→EIP 212.72.24.12 HTTP 200 via envoy, valid LE cert CN=acme.omani.homes, HTTPRoute Accepted); **#4929 per-Org CNPG ✅** (two healthy per-Org Postgres in ns acme). Distinct customer Org proven: acme kind=customer/billing=real/isolation=vcluster/Active, showback 525.5 units real-metered; vcluster StatefulSet Ready (own KC/kube-system). 11 screenshots `hw234-waveD/`.
> - ❌ **Row-3 authed-owner redirect NOT implemented** (#3687/#4546) — unauthed SSR redeem HEALTHY (200/0.05s, real form; hw228 hang→000 GONE), BUT Wave-D proved with a live authed-OWNER session (marketplace PIN msg968) that `/redeem/?code=` shows the redeem FORM and **never redirects to /dashboard** (also FAILs for the customer session). Marketplace auth is client-side → no server-side owner redirect exists. The redirect leg is unimplemented. FAIL.
> - ❌ **Rows 87-90 per-Org WordPress not live** — TWO `bp-wordpress-tenant@0.4.20` chart defects distinct from #4929: (1) configSchema *requires* `orgDomain`+`adminUser.email` but neither the console "New instance" dialog NOR the funnel collects them → Application fails admission `missing properties: 'orgDomain','adminUser'`; (2) DB-secret reflector RACE — chart creates an empty `wordpress-database-secret` (reflector-mirrored from `wordpress-db-app`) populated ~6min AFTER Helm's 5m `--wait` → HR `blog-rtz-a` `InstallFailed: context deadline exceeded` → external HTTPRoute never created (`wordpress.acme.omani.homes`→404). Also: customer self-service app install → `getApplicationInstances` 403 (marketplace HS256 member session not authorized for app mgmt; installs need the sovereign-admin owner console).
> - ⛔ **MCP RBAC #3988 rows 211-213/221-223 = BY-DESIGN gated (NOT a prov gap)** — investigation (agent a97f2d65) confirms: the **Pillar-4 Sandbox substrate IS deployed + healthy** (`bp-sandbox` HR Ready chart 0.3.12, `sandbox-controller` 1/1, CRD installed, lockstep intact). Zero Sandbox CRs is EXPECTED — Sandboxes are on-demand, minted only when the marketplace cart contains the "sandbox" product (acme bought WordPress). `openova-sandbox-mcp` is a **bundled stdio binary** in the pty-server image by design (the standalone MCP Deployment was removed in chart 0.3.4 — a pod has no stdin), so "no MCP pod/Service/HTTPRoute" is correct even with a Sandbox. Rows 211-213/221-223 target a DIFFERENT MCP (`openova-mcp`/`bp-agenity`, per-Org installable Blueprint) gated on **founder Anthropic cred (#4277)** + a per-Org `bp-agenity` install + #4624 — none is a prov deployment gap. The catalyst-api RBAC layer the MCP facades IS verified live+correct (sovereign token → 200/10-apps; unauth → 401; org seam → 400). Walk path to instantiate: funnel with the sandbox product OR apply a `Sandbox` CR in `catalyst-system` (controller's watched ns) with `spec.owner.orgRef.slug`.
> - ✅ **10th defect #4841 fixed+merged** — useraccess-controller granted `bind` on the 8 canonical IAM ClusterRoles (RBAC escalation-prevention, `f7fe1c3`/#4935) + CD-rolled into the deploy pins (`deploy: bump useraccess-controller image to f7fe1c3`). Validates live on the next fresh prov (hw234 was provisioned pre-#4935).
> - ✅ **Infra sweep (kubectl+curl, agent a19a341b) — cutover-dormant/spine/DR/showback/datapath all PASS:** cutover #3379 dormant (chart 0.1.108 Ready, cutoverComplete=false, 11 steps un-started, Tethered, dragonflyUpstream=ghcr); spine #3374/#3375 Ready (gitea 1.2.47/harbor 1.2.41/keycloak 1.5.5/openbao 1.2.60), DR continuum CRs all Healthy lease-held, cnpg-pair RTO=0s/RPO=0s, shared-pg replica streaming lag 0.52s; showback #3687 real (`/api/v1/org/consumption` acme=1235.75 units, per-app attribution); **#4656 cross-region datapath VERIFIED WORKING no-deploy-lag** (ClusterMesh 1/1 both regions, no stale :2379, `vpc-podcidr-route-reconciler` 'routes in sync', region-B pod→region-A pod TCP:5432 + CNPG streaming proven).
> - ⚠️ **Robustness #3380 = HONEST degraded (no false-green), root-caused → #4942:** cilium-health reads region-b Degraded because **host-network→remote-pod is black-holed** on Huawei VPC (host-sourced traffic not delivered to remote pod CIDRs) — a false-RED at the health-probe layer that OVERSTATES impact; the app pod datapath the DoD needs (pod-to-pod, RPO=0) is healthy. This is the recurring "region-b degraded" root cause; #4942 investigates a durable host-route fix so a fresh prov reads green. NOT a regression, NOT false-green.
> - ✅ **Authed console walk (browser, agent a7a650fd) — every walkable UI surface PASS:** SSO 6/6 silent-SSO (grafana/gitea/openbao/guacamole/keycloak-admin/harbor land authed, no login prompt; powerdns/newapi/hubble N/A = no external HTTPRoute by design); Catalog #3668 (94 blueprints, bp-wordpress detail, New-instance + Edit-IaC config-schema dialogs render); Jobs #3646 (Kind col task/cron/step/lifecycle, Provision-Huawei DAG all Succeeded, JobDetail+Logs; Retry not exercisable = zero Failed jobs on a healthy env); Treemap #4731/#3969 (Region 2/2, WorkerNode 12/12, Layer1=Progress/Layer2=Kind, Region-lens per-region placement); Orgs #3378/#3687 (3/3, acme customer real showback 1235.75, console.acme.omani.homes serves own PIN login + valid TLS); **Users #3374 = 3 UserAccess grants render "(admin)" NOT Failed → #4841 fix is NOT deploy-lag-blocked on this env**; Cutover #3379 Settings→Sovereignty "Tethered". Cosmetic-only: static "Hetzner provider token" label on Huawei Settings→Cloud (display-only), transient `/app/bp-X` 404s (UI falls back correctly). 20 screenshots `hw234-walk2/`.
> - **Net hw234 disposition:** every LIVE-testable surface is GREEN. Remaining non-green = deploy-lag validated on the hw235 conclusion prov (G10 console Edit-placement #4840, row-3 redirect #4938) · by-design/founder-gated (MCP #4277, Sandbox on-demand) · honest host-path false-RED (#3380→#4942) · Wave-F destructive (region-kill #4275, cutover-execute #3379). NO architectural faults; NO false-green.
> - **Process note:** this session's hw233/hw234 walk commits were initially stranded on a 610-commit-stale branch; reconciled onto real main 2026-07-10 (no clobber of the ✅155 base).

> **hw224 VERIFICATION PROV — 2026-07-04 (dep `964e9ca501951f54`, hw224.omani.works, 2-region me-east-215-a/b, LE-prod cert).** Fresh prov on the train carrying the two deepest fixes from the hw223 walk: **#4756** (provisioning-token outage) + **#4758** (vcluster-LimitRange brick). Fixes confirmed DEPLOYED + partially verified live by the orchestrator; the full authenticated funnel walk (customer WordPress serving in-vcluster) is in flight (walker `a2a68a8ce9ec0b96b`).
> - ✅ **#4756 provisioning-outage fix — VERIFIED LIVE**: `org-services/provisioning` init went `1/1 Running` at **5m52s with 0 restarts** (vs the old ~30-min self-heal outage that recovered only at the 1800s init-timeout restart). The init now reads GITHUB_TOKEN from the kubelet-synced FILE each poll (`GITHUB_TOKEN_FILE` env + `provisioning-github-token` volume both present on the running pod), so the late-minted gitea token is picked up in ~60s.
> - ✅ **#4758 vcluster-LimitRange fix — CONFIRMED WORKING** (authenticated funnel walk, customer Org `uatwp4758`, plan m, isolation=vcluster): `plan-limits` LimitRange has **NO `maxLimitRequestRatio`** (only default/defaultRequest), and the vcluster's synced `coredns-…-x-kube-system-x-vcluster` pod — the exact pod the old ratio=1 rejected — is now **admitted + scheduled + Running** in the host ns. The admission-rejection #4758 targeted is resolved. (org-controller `organization-controller:99cab49`.)
> - ❌ **WordPress last-mile still 404** — but by TWO NEW defects distinct from #4758: (1) **PRIMARY [#4656]** the customer vcluster's apiserver is unreachable cross-node (`dial tcp 10.96.80.71:443: i/o timeout`) — Cilium VXLAN-over-WireGuard MTU stacking (measured live: cilium_host 1370 + VXLAN 50 = 1420 > cilium_wg0 1290 → DF-drop); coredns stays 0/1, WordPress never applies into the vcluster. (2) **secondary** `core/services/provisioning/gitops/gitops.go:800-803` hardcodes `sourceRef: flux-system` which doesn't exist on a Sovereign → the generated `tenant-<slug>-apps` Kustomization is FALSE (`GitRepository "flux-system" not found`) — a broken duplicate of the org-controller's `catalyst-tenant-<slug>-apps`.
> - ✅ **Pillars 2/3, funnel E2E, PIN login** re-confirmed (BCP active-hot-standby picker; shared-pg/-b/-c + org-pg healthy; full funnel plans→checkout→launch with voucher `VCH-QFP52J4S2KEX`; `/auth/pin/verify`→200). Env converged (console 200, region-a 63/63 HRs; region-b `secondaryDegraded=true` — honest).
> - ✅ **COMPREHENSIVE authenticated UAT walk — 26 ✅ · 0 ❌ · 3 ⛔ · 4 ⚠️, NO new bugs, NO fabricated-green** (walker `a453392c2a1f7a514`, 12 screenshots `.playwright-mcp/uat-hw224-01..12-*.png`). Confirmed live at HTTP/kubectl/wire layer: **SSO** (per-app launch-url→real OIDC; gitea 307→`realms/sovereign`, grafana 302 w/ `kc_idp_hint=catalyst-pin` silent-SSO); **Topology/DR** (per-app placement from runtime, active-active, Region 2/2, 24/24 workers); **Jobs lens #3646** (Kind col task/cron/step/lifecycle + Re-run on a *Failed* mimir-buckets job — genuinely real, not all-green); **Catalog** (81 blueprints, bp-wordpress detail); **Orgs** (directory lists customer `uatwp4758`, per-Org detail, **showback 525.5 units real-metered ≠ 0**, `console.uatwp4758.omani.homes`→200 w/ own LE cert); **Treemap** (206-node live util; **#4731 Progress/Kind layers VERIFIED** — L1 progress buckets Reconcilers/Cutover/Provision-Huawei, L2 Kind Task/Cron/Step/Lifecycle, status-color legend, job-tree source `/jobs`→200, leaf→JobDetail+logs, no regression to org/utilization default; `.playwright-mcp/4731-hw224-0[1-4]`); **Robustness #3380** (Degraded badge honestly propagated everywhere — no false-green; root = `clustermesh-apiserver` Init:0/1 both regions, #4656 family); **Cutover** (Tethered, 11 steps Pending, cutoverComplete=false); **Users** (3 real UserAccess CRs); **Agenity** (6-agent picker). 3 ⛔ = pre-existing non-product gates (headless-KC silent-SSO landing, Agenity live-run Anthropic cred #4277, uatwp4758 vcluster 0/0 #4656). 4 ⚠️ minor (HR-only DR-switchover panel absent; catalog IaC write-gated on live env; vcluster-showback in graph; ~~"Sandbox"→"Agenity" nav leak~~ **FIXED #4762**).
> - ✅ **FULL-TABLE walk — all 243 canonical rows populated on hw224: `101 ✅ · 11 ❌ · 26 ⛔ · 105 ⚠️ · 0 ☐`** (from 0✅/122☐ at reset; every row a live hw224 verdict, **0 fabricated passes**, 17 screenshots `uat-hw224-fulltable-*`, commits 15c5332d3/9b2d3898f/73a65b568). Silent-SSO brokered from the console session unlocked app-landings session-walled ⛔ on prior envs. **Flipped ❌/⛔→✅ this env**: Harbor SSO wired (was ❌412 db_auth), Grafana 3/3 + silent-SSO (was ❌503 CrashLoop), all app SSO landings live (gitea/openbao/guacamole/pdns/newapi/hubble/KC-admin), customer Org uatwp4758 real (525.5-unit showback). **11 ❌ = all previously-filed knowns, NO new bugs**: #4656 (WordPress in-vcluster, 7 rows), F8 topology placement-from-pod-only, row-23 directory-showback, #4620 crossplane adoption. **105 ⚠️** = region-b Degraded (clustermesh-apiserver Init:0/1, #4656 family) + write-mutation rows (durable-commit proofs stand). **26 ⛔** = honest walls (headless-KC session, Anthropic-cred Agenity chat #4277, destructive cutover, write-gated funnel). Path to higher ✅: fix #4656 (unblocks WordPress + region-b) + a throwaway write-mutation walk.
> **Provenance:** `hw224-verify-2026-07-04` per surface (walker `a2a68a8ce9ec0b96b`, 6 screenshots `.playwright-mcp/4758-0[1-6]-*.png`). Net: the two deepest fixes shipped this session (#4756, #4758) are VERIFIED; the WordPress headline is now gated on #4656 (MTU) + the gitops.go sourceRef bug — both filed, both distinct from the verified fixes. hw223 (below) is prior-env history — wiped 2026-07-04 (STALE by construction; each new env flushes evidence).

> **hw223 AUTHENTICATED PILLAR WALK — 2026-07-04 (dep `9224ebd66e0602b4`, hw223.omani.works, real LE-prod cert, FIRST reliably-converged prov after the walk-blocker fix-trio #4748+#4746+#4752).** The whole prior round was auth-gated by broken operator login; hw223 cleared it. **~13 ✅ · 0 ❌ · 0 ⛔ · 3 ⚠️**, + 1 real bug (#4756) + 2 findings. All verdicts reproduced live at the HTTP/kubectl/CR layer — no fabricated passes (12 screenshots under `.playwright-mcp/uat-hw223-*.png`).
> - ✅ **Operator PIN-login end-to-end** (#4748, the prior blocker): `pin/issue`→200 `dispatched` → mailbox PIN → `pin/verify`→200 + RS256 `catalyst_session` cookie (roles catalyst-admin/owner) → authed console renders. (hw221 was 502; fix validated live.)
> - ✅ **Pillar 1 — customer-Org onboarding walked E2E** (first customer-KIND Org on any prov this cycle): funnel plans→apps→addons→bcp→review→checkout; operator issues voucher `UATWALK223VOUCHER` (BSS UI); redeem fully covers OMR 9 → Org CR `uatwalk223` (kind=customer, plan=m, billing=real, isolation=vcluster) Ready in ~2 min → **`console.uatwalk223.omani.homes` → HTTP 200** on its own Org host.
> - ✅ **Pillar 2/3** — cloud graph Region 2/2 (me-east-215-a+b both healthy), 24/24 workers, LB 2/2; Continuum DR CRs (gitea/harbor/keycloak) phase Healthy + LeaseHeld; 9 CNPG clusters healthy (shared-pg/-b/-c Guaranteed + DR pair). Signup BCP offers active-hot-standby.
> - ✅ **W-2** per-Org `plan-limits` LimitRange forces Guaranteed QoS + `plan-quota` present; **W-3** per-Org agenity org-host routing proven (Org console on the Org host, not Sovereign FQDN) + `OPENOVA_MCP_TENANT_HOST` fix in source; **W-11** Organizations directory shows the new customer Org.
> - ✅ **Pillar 5** cutover dormant (`bp-self-sovereign-cutover@0.1.103`, cutoverComplete=false, 11 steps un-started; Settings→Sovereignty "Tethered").
> - 🐞 **BUG #4756** — `org-services/provisioning` init reads GITHUB_TOKEN via secretKeyRef env (empty at start), can't see the late-minted gitea token → ~30-min self-heal outage (recovers at 1800s init-timeout restart). Findings: funnel checkout showed `.omani.rest` but provisioning used `omani.homes` (redirect TLD mismatch); a duplicate `tenant-uatwalk223-apps` Kustomization is broken (GitRepository not found); funnel-picked WordPress still converging in-vcluster at cutoff. (⚠️ = per-Org keycloak realm tier-gated + WordPress converging + showback 0-units-metered.)
> **Provenance:** `hw223-walk-2026-07-04` per surface; the three walk-blocker fixes (#4748/#4746/#4752) all validated live on this prov. Prior hw221/hw220 blocks below are prior-env history (STALE by construction — each new env flushes evidence).

> **hw220 FULL-TABLE walk — 2026-07-04 (dep `907629bc3577355b`, hw220.omani.works, 2-region converged, complete #4706+#4732 train live).** ALL 243 canonical rows walked by two parallel read-only walkers (rows 1–120 + 121–239, minted-owner + org-scoped sessions, wire evidence per row): **122 ✅ · 21 ❌ · 37 ⛔ · 63 ⚠️ · 0 ☐**. The 21 ❌ consolidate into the #4739 fault wave (headline: hourly external :443 outage loop from the TTL'd envoy-restart Job — live-mitigated + durable fix PR'd; per-Org LimitRange wedge blocking every funnel app; harbor SSO db_auth never flipped; grafana region-a credential CrashLoop; 1.4.993 invalid env render; adoption ProviderConfig group mismatch; false-green health panel; hardcoded Hetzner name in DR panel; visible APPDETAIL debug marker). 37 ⛔ are honest walls (KC browser-SSO unmintable headless, founder-mailbox PIN, write-gated funnel mutations, no customer-kind Org sample). 63 ⚠️ dominated by catalog write-mutation rows (write-gated on the live env; prior 25aadcfc durable-commit passes stand) + cutover-partial context (step-08 wedged, cutoverComplete=false). Verdict provenance: `hw220-walk-2026-07-04(A|B)` stamps in each row's Evidence column.

> **hw218 walk — 2026-07-03 (dep `25b49b8534ab98db`, hw218.omantel.biz, FIRST-EVER zero-touch 2-region convergence on the merged #4706 train).** Four wire-captured verdicts, no hand-patching anywhere in the env:
> 1. ✅ **No-nodePort gateway serves externally** — `gitea.hw218.omantel.biz → HTTP 303`, `auth.hw218.omantel.biz → HTTP 302` (off-cluster, ELB 212.72.24.33 → node:443 hostPort → hostNetwork envoy); gateway Service `type=ClusterIP`, **`nodePorts=<none>`** (kubectl wire-capture). Console-isolation leg 404 (two Gateways can't both bind node:443) → fixed in #4715 (merged, 1.4.987 roll-gated).
> 2. ✅ **Readiness gate (#4707) enforced** — `status=ready` written only at 00:37:03Z after the external console probe answered (<500); the gate code is proven present in the RUNNING mothership binary by capture 3. Hardening note: 404 satisfies the current <500 bar — tighten to <400 as a follow-up.
> 3. ✅ **2-region admission floor (#4708) live in production** — negative test against the LIVE mothership: `POST /deployments` (1 region, no topology) → **HTTP 400** `"deployment has 1 region(s) and no explicit bcpTopology — the BCP agreement default is multi-region (>=2 regions, Pillar 2)…"`. Nothing created.
> 4. ✅ **Tenant DNS split-horizon correct** — `gitea./auth.hw218 → 212.72.24.33` (primary ELB), `console.hw218 → 212.72.24.12` (console ELB).
> Pre-walk hygiene: hw217 wiped via canonical endpoint with **API-audited zero orphans** (0 ELBs, bastion-only ECS, 1 EIP, 1/5 VPCs). Session train: #4707 #4708 #4709 #4710 #4711 #4714 #4715 all MERGED.


> **Pillar 1 (Marketplace + voucher onboarding) fully live-walked 2026-06-27 — funnel + plan catalog + voucher issuance/redeem E2E.**

> **Pillar 4 (Agenity workspace + auto-mounted bp-openova-mcp) materially live-walked 2026-06-27 — controller + per-Org provision + MCP auto-mount + RBAC Org-scope verified; agentic chat-run gated on Anthropic credential.** *(Terminology per DOD.md §Pillar 4 + GLOSSARY: the user-facing product is **Agenity** / **bp-openova-mcp**; the legacy "Sandbox" concept + menu are dead/removed — founder 2026-06-30. Internal `sandbox-controller`/`Sandbox` CR plumbing Agenity is built on stays internal.)*

> **Authed operator-console walk 2026-06-27 (throwaway-mailbox PIN session) — 10 interactive-UI rows verified; 2 product faults exposed (rows 33, 176).**

> **Authed live-walk sweep 2026-06-27 — ~33 interactive rows verified (SSO/topology-dialog/per-app-health/catalog-IaC/cutover-UI/jobs/orgs); env healthy post bp-catalyst-platform 1.4.915 roll.** (Row 83 funnel /review = fix-merged-pending #4526; row 170 install-openbao = NA, superseded by the #3916 jobs-lens split.)

> **Write-mutation walk on fresh prov 25aadcfc 2026-06-27 — catalog IaC-commit (durable Gitea) + instance-provision rows verified (safe on the disposable throwaway).** Catalog Save/commit rows 127/128/129/132–140/142/143/145/147/148/149/153–156 flipped ✅ via `PUT /api/v1/catalog/<bp>/iac` → 200 `{committed:true}` + durable bare-repo Gitea commit, on two structurally-different blueprints (bp-alloy + bp-wordpress). Instance-provision rows 50/59/60/61 flipped ✅ via `POST /catalyst/v1/apps/instances` → 201 + CR Ready + matching topology badge (singleton + active-hot-standby).

> **3-agent live-walk batch 2026-06-27 — topology/recon/sso ✅ flips + owner-redirect fix (#4547) + 2 ❌→⛔ reclassifications (session-walled) + topology DR-panel product-gap recorded.** On dep `91dc05917e44d1c1`: 6 rows flipped ✅ (53/54 topology, 193/195 recon, 29/38 sso); row 3 owner-redirect = ⚠️ fix-merged-pending (#4546/#4547); rows 109 + 183 reclassified ❌→⛔ (KC browser-SSO-cookie / OpenBao final-render, both session-walled, not product faults); topology rows 51/52/56/57 held ⚠️ as a REAL product gap (DR/switchover UI panel not built for HR components) + 58/63 held ⚠️ as stale expectations (cilium/grafana render honestly active-active); recon 196/197 held ⚠️ read-only-gated. New canonical tally 193 ✅ · 0 ❌ · 9 ⛔ · 33 ⚠️ · 4 ☐ = 239.

## Session resolution — 2026-06-27 (Refs #909)

**Env:** live omantel.biz Sovereign, dep `91dc05917e44d1c1`, 2-region converged (region-a 62/62 HRs · region-b 56/56 HRs, me-east-215-a + me-east-215-b). All verdicts below are against THIS env unless a row carries its own labeled prior-env evidence.

**Headline:** 22 issues CLOSED this session with independently-audited live evidence (reproduced the live signal, no false-closes) · 4 more code-done + MERGED (roll-gated, NOT yet live-proven — recorded as merged-pending, not pass) · 13 remain open behind a recorded gate.

**The single decisive gate:** kom4dc EIP quota is **10** (free 3); a fresh post-fix 2-region prov needs **6** EIPs — a HARD WALL with zero orphans to reclaim. Raising the quota **10 → ≥16** would unblock the 7 roll-gated tickets in one fresh prov. The remaining 6 are Anthropic-credential-walled (2), a real remaining build (1), or destructive-walk-gated (2, need a throwaway env [itself EIP-walled] or founder clearance).

### Live-walk consolidation — 2026-06-27 (4-section read-only walk of `91dc05917e44d1c1`, region-a)

Four parallel read-only walkers (SSO/auth · Orgs/Contexts · Topology/DR/robust · Jobs/Cloud/Market) re-dialed the live 2-region omantel.biz Sovereign and reported **119 verdicts** (28 + 22 + 39 + 30): **75 PASS · 5 FAIL · 9 NA · 30 BLOCKED**. Consolidated into the numbered table below, the run flips **16 rows to ✅** (rows 5/6/25/32/41/42/43/44/64/71/77/111/189/205/229/236) and resolves 17 more rows from `☐`/`❌`/`⚠️` into their honest gate (⛔) or product-fault (❌) state, leaving the canonical table at **81 ✅ · 9 ❌ · 26 ⛔ · 42 ⚠️ · 81 ☐ = 239** at that snapshot. (A subsequent 2026-06-27 Pillar-1 funnel+voucher walk flipped 4 more rows to ✅ — 72/73/74 `☐`→✅ + 75 `⛔`→✅ — carrying the canonical table to **85 ✅ · 9 ❌ · 25 ⛔ · 42 ⚠️ · 78 ☐ = 239**.) Genuine product faults (FAIL): **newapi** serves 200 but generic-OAuth provider rows are never seeded (`Unknown OAuth provider`, rows 37/114); **Hubble** authed shell renders but the flow XHR (`/api/control-stream`) 302-bounces to KC so data never loads (row 39, carried ⚠️); and the **uatwalk91** Org reports `isolation=vcluster` while the live backing is host-namespace (rows 9/10/11/12/216 — the #3642 de-vcluster re-baseline, not a fresh regression). The 30 BLOCKED are honest gates: a kid-less handover *session* JWT is rejected by this catalyst-api build (whoami 401) and the SPA login guard clears injected cookies, so interactive-session UI renders (treemap/showback DOM, OpenBao authed UI, Topology-tab content) can't be asserted headless — their underlying data is proven at the API/kubectl/psql layer. Row 236 (region-B keycloak) **flips ⛔→✅** (the #4470 sandbox-ns deadlock did not recur; region-b 57 HRs True). No false-pass: every ✅ reproduced the live signal directly.

### Closed this session — 22, audited live (✅ VERIFIED-PASS)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| R1 | janitor | [#4454](https://github.com/openova-io/openova/issues/4454) | catalyst-api orphan-sweep no longer reaps a `ready` Sovereign — denylist inversion (`case "wiped": eligible; default: protect`) holds on the live env. | — | ⛔ | hw232: janitor is a MOTHERSHIP catalyst-api loop (sweeps deployment registry); not observable from child kubeconfig — mothership-scope | — | hw227-reconfirm-2026-07-06: hw227 (dep 504e330d86aaefb4) console 200 + Sovereign ready after 5h — janitor did NOT reap the ready env; protect-side inversion holds. 
| R2 | network | [#4467](https://github.com/openova-io/openova/issues/4467) | Cilium VXLAN+WireGuard MTU set to **1370** in BOTH regions — cross-node pod TCP no longer DF-drops; full-size data packets traverse. | — | ☐ | — | hw239: MTU-1370 VXLAN+WireGuard superseded by native-routing (#4656) — cilium mtu=1420 routing-mode=native both regions, no cilium_vxlan iface, vpc-podcidr-route-reconciler in-sync (6 nodes). Row asserts a DEAD config; cross-region datapath proven via native routes + WireGuard NodeEncryption (11 peers). 
| R3 | sso | [#4448](https://github.com/openova-io/openova/issues/4448) | bp-plane-isolation openbao default-deny ADMITS sso-bridge ingress → sso-bridge reaches openbao:8200. | — | ☐ | — |
| R4 | sso | [#4458](https://github.com/openova-io/openova/issues/4458) | sso-bridge egress CNP permits openbao + keycloak — the reconciler's own egress is not blocked. | — | ☐ | — |
| R5 | sso | [#4437](https://github.com/openova-io/openova/issues/4437) | sso-bridge re-mints the grafana OIDC client secret each tick (KC_ADDR cache refreshed) — no stale-secret 401. | — | ☐ | — |
| R6 | postgres | [#4442](https://github.com/openova-io/openova/issues/4442) | bp-postgres NetworkPolicy admits the declared consumers (CNPG operator probe + app dials). | — | ☐ | — |
| R7 | plane-isolation | [#4444](https://github.com/openova-io/openova/issues/4444) | bp-plane-isolation admits gitea ingress for its declared consumers (org-services→gitea de-vcluster gap). | — | ☐ | — |
| R8 | gitea | [#4447](https://github.com/openova-io/openova/issues/4447) | gitea-flux-auth secrets are seeded — Flux pulls from gitea with valid credentials. | — | ☐ | — |
| R9 | sso | [#4446](https://github.com/openova-io/openova/issues/4446) | oidc-gate client_secret is seeded for powerdns-admin — full OIDC round-trip completes. | — | ☐ | — |
| R10 | orgs | [#4471](https://github.com/openova-io/openova/issues/4471) | org-controller RBAC carries update/patch — per-Org provisioning completes (was create-only, never reached Ready). | — | ☐ | — |
| R11 | gitea | [#4354](https://github.com/openova-io/openova/issues/4354) | gitea git-data is PRESERVED across the host-ns re-home (#4325 de-vcluster) — no empty-PVC data loss. | — | ☐ | — |
| R12 | postgres | [#4460](https://github.com/openova-io/openova/issues/4460) | shared-pg `-mesh-rw` resolves in BOTH regions + CNPG streaming replication is live. | — | ☐ | — |
| R13 | convergence | [#4436](https://github.com/openova-io/openova/issues/4436) | region-B keycloak / gitea / harbor reach steady-state with NO CrashLoop. | — | ☐ | — |Completed empty; region-b HR 59/59 applicable True. |
| R14 | model | [#4479](https://github.com/openova-io/openova/issues/4479) | console Organizations directory reads the live Organization CRs (not an empty in-memory store). | — | ☐ | — |
| R15 | funnel | [#4473](https://github.com/openova-io/openova/issues/4473) | funnel plan-slug propagates end-to-end — the chosen plan reaches the provisioned Org. | hw290 | ✅ | hw290-2026-07-26: Plan-slug propagates funnel→Org. In the storefront I selected the M card; localStorage org-cart captured at /review = `{"plan":"d6f7623a-1ea9-444e-812b-62c515882ff4","planName":"M",…,"subdomain":"walk-two","tld":"omani.rest"}`; checkout summary rendered `Plan · M OMR 9.000`. Provisioned Org carries it: `kubectl get organization walk-two -o jsonpath='{.spec.planSlug}'` → `m`, `kubectl get organizations` PLAN column = `m`, and the plan-derived quota materialized (`ResourceQuota/plan-quota` in ns walk-two = 4 CPU / 8Gi = the M template, vs 2/4Gi for the S-plan Orgs). Same chain verified on acme-corp (plan m). ‖ — |
| R16 | funnel | [#4290](https://github.com/openova-io/openova/issues/4290) | the funnel + BSS Org doors are collapsed onto ONE path — `console.<slug>` lands 200. | — | ☐ | — |
| R17 | orgs | [#4459](https://github.com/openova-io/openova/issues/4459) | deleting an Organization CR cascades cleanly — no orphaned ns / app / DNS leak. | hw290 | ❌ | hw290-2026-07-26: Round-trip run on the SAME provisioning path as the observation (console admin door), both deletes fired 14:35:55Z, re-checked at T+10min and T+14min. PATH (a) console `DELETE /api/v1/organizations/a07f1932-cbb0-444c-801c-806b34d810d4` (beta-corp) → HTTP 204, store record flips to state="deleted", and the gitops overlay removal DID prune the 6 HelmReleases + workloads (ns empty by T+14min). BUT **the Organization CR was never deleted**: at T+14min `kubectl get organization beta-corp` → still present, `status.vcluster.phase=Ready`, `metadata.deletionTimestamp` EMPTY, and `kubectl get ns beta-corp` → **Active (26m)**. Orphan ns + orphan Org CR. PATH (b) `kubectl delete organization gamma-corp` → CR + ns gone within ~33s (clean initial cascade), BUT at **14:41:02Z (T+5m07s) the namespace was RECREATED by Flux**: `kubectl get ns gamma-corp -o jsonpath='{.metadata.creationTimestamp}'` = 2026-07-26T14:41:02Z, managedFields manager = `kustomize-controller`, labels include `kustomize.toolkit.fluxcd.io/name: org-tenants`; the whole app suite came back (bp-agenity-0 Running, bp-keycloak-0, bp-keycloak-postgresql-0, oidc-gate-agenity-gamma-corp Running) and it is still Active at T+14min. `Kustomization/org-tenants .status.inventory` still holds **9 gamma-corp entries** (`_gamma-corp__Namespace` + 6 HelmReleases + cert-strategy ConfigMap + Continuum) because the catalyst-api-managed org-tenants gitops entry is never removed on a CR delete. Neither path fully tears down; the #5364/#4459 leak reproduces on both. ‖ hw288-2026-07-26: ✅ patient repro. Applied throwaway `repro-r17` Org (mirrors acme-corp spec), vcluster came up (vcluster-0 + coredns) + 3 Flux Kustomizations Applied + Org finalizer `tenant-networking` present. On `kubectl delete organization repro-r17` the **ns was GONE within 20s** (PASS_NS_DELETED); residual sweep = Org CR/ns/Kustomizations/GitRepository/DNSEndpoints ALL empty → fully clean cascade, zero orphan. The `<slug>` ns is Flux-prune-inventory-tracked (`_<slug>__Namespace` in the vcluster Kustomization) so it is GC'd on delete. The earlier beta-corp 90s-orphan (ns Active + bp-keycloak Running) was slow-prune — keycloak StatefulSet + PVC finalizers gate the ns Terminating — NOT a permanent leak (#5364 resolved not-a-defect). Caveat: repro was vcluster-only (keycloak didn't provision in 10m), so the keycloak-heavy teardown wasn't watched to full completion, but the inventory-tracking + clean 20s prune make a permanent leak unlikely. ‖ hw288-2026-07-25: ◑ walked live — deleting the Org CR does NOT fully cascade. Created beta-corp (admin door), then `kubectl delete organization beta-corp` → the Org CR deleted immediately (no blocking cascade finalizer) + the vcluster HelmRelease was removed, but 90s later the **ns beta-corp is still Active (not Terminating)** with **bp-keycloak-0 + bp-keycloak-postgresql-0 pods still Running** — orphaned. No orphaned DNSEndpoints/Kustomizations found. So the CR-delete path leaves the ns + per-Org keycloak orphaned (the Org CR carries only the default `kubernetes` finalizer, no org-cascade finalizer). CAVEAT: this is the raw-CR-delete path; the canonical console/API delete may run a different cascade — but 'Org CR deletion cascades cleanly' is NOT demonstrated (orphan ns + keycloak). Manually cleaned up the orphan after. ‖ hw288-2026-07-23: hw288-2026-07-23: not exercisable (read-only; only live internal seed Org, won't be deleted). Observed Org CR hw288-omani-works metadata.finalizers=None (no tenant-networking finalizer armed on seed) — cascade path not walked | ‖ RCA code-grounded → filed #5364 (ns+bp-keycloak orphan, sibling to #4459); org delete has no explicit ns-delete, cleanup rides feature-gated Flux-prune/Environment-CR-GC handoff (disabled on hw288). ‖ CORRECTION 2026-07-26: the org ns IS in the vcluster Kustomization's Flux prune inventory (live-verified) → likely slow-prune not a permanent leak; #5364 downgraded to needs-patient-repro; earlier no-ownerRef reasoning was wrong (Flux prunes via inventory+label). ‖ 🛑 CORRECTION 2026-07-26: R17 REVERTED ✅→◑. The repro-r17 'clean 20s prune' was a FALSE NEGATIVE — it was kubectl-applied (bypasses catalyst-api's org-tenants gitops path). A real admin-door Org (beta-corp) is CONFIRMED ORPHANED @137m post-delete: ns Active (no deletionTimestamp) + keycloak/newapi Running, because the `org-tenants` Flux Kustomization (catalyst-api-managed) perpetually recreates it — the Org CR delete never removes the catalyst-api org-tenants gitops entry. Confirmed real leak → #5364 reopened. Reproduce on the SAME provisioning path as the observation.
| R18 | cutover | [#4450](https://github.com/openova-io/openova/issues/4450) | handover-key self-publish guard — the Sovereign does not re-publish its handover key on reconcile. | — | ☐ | — |
| R19 | sandbox | [#4482](https://github.com/openova-io/openova/issues/4482) | sandbox-controller reaches Running (was CrashLoop on unset CATALYST_GITEA_TOKEN). | hw288 | ⛔ | hw288-2026-07-25: SUPERSEDED — live kubectl region-a: 0 sandbox-controller pods (Sandbox CRD lingers but no controller/workload runs it); Sandbox concept removed by founder 2026-06-30, capability succeeded by bp-openova-mcp (pod openova-mcp-bp-openova-mcp-64d9c76d48-mpz65 1/1 Running 30h). Row premise (sandbox-controller reaches Running) is moot -> N/A. ‖  hw288-2026-07-24: no sandbox-controller (nor agenity-controller) pods on hw288 — the Sandbox concept was REMOVED 2026-06-30 (superseded by Agenity/openova-mcp). Row premise (sandbox-controller Running) is superseded; stays ◑ pending reclassification to N/A. ‖hw288-2026-07-23: SUPERSEDED (Sandbox removed, founder 2026-06-30): 0 sandbox ns / 0 sandbox pods / 0 CRs. Successor bp-openova-mcp LIVE: HR Ready=True, pod openova-mcp-bp-openova-mcp-64d9c76d48-mpz65 1/1 Running, openova-mcp ingress/egress CN |
| R20 | delivery | [#4464](https://github.com/openova-io/openova/issues/4464) | the deploy-bot bumps image pins per-line (not a blanket bump) — stale-render avoided. | hw274 | ⛔ | hw274-2026-07-19: not applicable on a child Sovereign — deploy-bot per-line pin bumping is a mothership/CI concern (ledger already marks n/a); no deploy-bot surface exists in either hw274 region to observe. [SUPERSEDED/N-A] |
| R21 | catalog | [#4432](https://github.com/openova-io/openova/issues/4432) | catalog-seed pins match the published ghcr chart versions — no inert lagging Blueprint CR. | hw288 | ◑ | hw288-2026-07-25 WALK (owner session, whoami 200 tier=owner dep 027f07559af1f9f7): all 5 chartless cards STILL visibility=listed live on hw288 — bp-redis/bp-prometheus/bp-n8n/bp-clickhouse/bp-opensearch each chartRef=ghcr.io/openova-io/bp-bp-<name>:1.0.0 (double-bp -> 404 hollow) + valueSchema=absent + alias-of=none -> customer-selectable hollow-install gap CONFIRMED LIVE (the exact R21/#5345 concern). Fix #5346 (flip these 5 to visibility:unlisted) + #5347 (umbrella bp-catalyst-platform 1.4.1210 republish) are MERGED but NOT yet delivered to hw288 (roll-gated; merged != delivered) -> stays PARTIAL until the next prov carries 1.4.1210. ‖  hw288-2026-07-24 CONFIRM: the 5 are NOT display-only — /api/v1/catalog presents them as fully installable (source+chartRef+valueSchema, identical shape to deliverable bp-alloy; e.g. bp-redis chartRef=ghcr/openova-io/bp-bp-redis:1.0.0 + valueSchema.replicas) yet NO chart published on ghcr under any naming (bp-redis AND bp-bp-redis both 404) + NO repo Chart.yaml -> a marketplace pick of any of the 5 would hollow-install. Real Pillar-1 catalog-integrity gap, filed as #5345. ‖hw288-2026-07-24: FULL 80-pin ghcr cross-check PERFORMED (the exact gap this row flagged as 'not performed'): 74/80 seed pins resolve DIRECTLY to published ghcr charts; bp-wordpress resolves via label alias-of->bp-wordpress-tenant@0.4.22 (published, same version) = 75/80 deliverable pins valid, ZERO inert-lagging Blueprint CRs (none pinned behind published). Remaining 5 (bp-redis/bp-prometheus/bp-n8n/bp-clickhouse/bp-opensearch) are visibility:listed DISPLAY-ONLY roadmap cards (card metadata only, no alias-of + no delivery chart + no repo source) -> not deliverable, not lagging. Stays PARTIAL: those 5 cards' marketplace add-to-stack selectability not yet walked (if selectable they would hollow-install -> Pillar-1 follow-up; if display-only marketing, R21 is clean). ‖hw288-2026-07-23: GET /api/v1/catalog → 200, 80 blueprints ALL version-pinned (0 unpinned): bp-catalyst-platform 1.4.1206, bp-agenity 0.5.20, bp-alloy 1.0.2, bp-cert-manager 1.2.6. ghcr published-version cross-check not performed. hw288-2026-07-25 LIVE console walk (#5345): console.hw288/catalog/bp-redis renders as a COMPLETE installable blueprint — v1.0.0, singleton-per-Org, Documentation, TWO '+ New Instance' buttons, supported topologies (rtz-A singleton) — yet GET packages/.../bp-redis/versions = NO-PACKAGE (no ghcr chart). '+ New Instance' would hard-fail. Same for bp-clickhouse/opensearch/prometheus. Screenshot: docs/sessions/2026-07-25/screenshots/issue5345-bp-redis-chartless-installable.png. The 5 chartless cards are the standing R21 gap (#5345). ‖ hw288-2026-07-25 re-walk (#5345): bp-redis/clickhouse/opensearch/prometheus all still NO-PACKAGE on ghcr — the chartless-but-installable gap persists; needs publish-or-hide. ‖ hw288-2026-07-25 re-confirm (#5345): 5 blueprints still NO-PACKAGE on ghcr; console /catalog/bp-redis renders '+ New Instance' on a chartless card (screenshot in walk-log). ‖ hw288-2026-07-25 CORRECTION (#5345 is FIX-MERGED, not a pending design decision): commit 45f7f132d / PR #5346 'fix(catalog-seed): unlist 5 chartless blueprints (redis/prometheus/n8n/clickhouse/opensearch)' already set all 5 to visibility:unlisted in the repo seed (fix option b — non-listed so no live install affordance). hw288's frozen catalog seed predates it (still visibility:listed → the '+ New Instance' card I saw), so R21/#5345 is another #5265 delivery-gap casualty (fix merged, hw288 pre-fix). Validate on a fresh prov: the 5 render unlisted / not directly installable. ‖ | ‖ hw288-2026-07-26 verified: 80 live Blueprint CRs, **0 annotated `helm.sh/resource-policy: keep`** → the inert-lagging-pin anti-pattern is CLEAN (seeder owns the version field). The 'pins match ghcr' clause is subject to hw288's frozen post-cutover seed (#5265 delivery-gap, e.g. bp-cilium=1.4.16) — stays ◑ on this pre-fix env, but the CR-level inert-pin risk is nil.
| R22 | plane-isolation | [#4468](https://github.com/openova-io/openova/issues/4468) | bp-plane-isolation admits apiserver-egress + gateway-ingress CNPs — CNPG admission + gateway hop succeed. | — | ☐ | — |

### Merged this session — code-done, roll-gated for live proof on the next fresh prov (⚠️ merged-pending)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| M1 | janitor | [#4466](https://github.com/openova-io/openova/issues/4466) (PR [#4493](https://github.com/openova-io/openova/pull/4493)) | janitor hardening — log-only/dry-run for a full live cycle before any destructive sweep. | — | ⛔ | hw232: mothership catalyst-api janitor dry-run — not observable on child |
| M2 | apps | [#4477](https://github.com/openova-io/openova/issues/4477) (PR [#4492](https://github.com/openova-io/openova/pull/4492)) | newapi admin-token seeded into OpenBao at provision (was unseeded → admin-promote fault). | hw290 | ✅ | hw290-2026-07-26: #5377 RUNTIME PROOF — region-B newapi/catalyst-newapi-admin-token ExternalSecret Ready=True reason=SecretSynced msg="Secret was synced" refreshTime 2026-07-26T14:55:50Z storeRef ClusterSecretStore/vault-region1. The region-local producer works ‖ — |
| M3 | network | [#4475](https://github.com/openova-io/openova/issues/4475) (PR [#4495](https://github.com/openova-io/openova/pull/4495)) | vcluster-tier CNP applied host-side — the per-Org vcluster tier carries its split CNP. | hw290 | ✅ | hw290-2026-07-26: Upgrade from the hw232 'mechanism only' caveat: now proven on a REAL customer vcluster-tier Org. `kubectl get cnp -n acme-corp` → `allow-gateway-and-apiserver VALID=True`, applied HOST-side (labels `catalyst.openova.io/component: org-vcluster-gateway-netpol`, `openova.io/organization: acme-corp`, `kustomize.toolkit.fluxcd.io/name: catalyst-tenant-acme-corp-host-apps`), status `Policy validation succeeded`. Spec carries the split shape: egress toEntities[kube-apiserver] :443/:6443 + toEndpoints{}, ingress fromEntities[ingress,host,remote-node] + fromEndpoints{} + fromEndpoints[ns=flux-system]. SUB-GAP worth a follow-up (not a row failure): the vcluster-tier ns carries ONLY the CNP — the namespace-tier Org ns `hw290-omani-works` additionally has `NetworkPolicy/default-deny-all` + `NetworkPolicy/allow-same-org`, which are absent from acme-corp, so the vcluster-tier Org ns is not default-deny. ‖ hw232: vcluster internal Org ns host-side cnp allow-gateway-and-apiserver VALID=True + default-deny-all + allow-same-org (mechanism proven; no customer M+ vcluster Org) |
| M4 | apps | [#4111](https://github.com/openova-io/openova/issues/4111) (PRs [#4494](https://github.com/openova-io/openova/pull/4494)+[#4496](https://github.com/openova-io/openova/pull/4496)) | agenity image-pull half — fresh-install ghcr-pull path. | — | ☐ | — |

### Remaining open — 13, with the recorded gate (honest map)

| # | Epic | Ticket | Test case | Walk | Result | Gate |
|---|---|---|---|---|---|---|
| G1 | adoption | [#4488](https://github.com/openova-io/openova/issues/4488) | crossplane provider-opentofu Observe-only adoption populates on the fresh prov. | — | ☐ | — |
| G2 | apps | [#4477](https://github.com/openova-io/openova/issues/4477) | newapi ES-sync drives the admin token into a fresh per-Org newapi (seed merged in M2). | hw288 | ◑ | hw288-2026-07-23: hw288-2026-07-23: per-Org ES-sync not exercisable — GET /api/v1/organizations -> 1 internal Org, 0 customer/funnel Orgs; no fresh per-Org newapi exists to seed. | — | ‖ hw288-2026-07-26 re-verified live: same gate — PerOrgRealmProvisioned=Disabled means no fresh per-Org realm for the newapi ES-sync to drive the admin token into. Gated on the per-Org-realm flag being enabled (or a controller that wires it).
| G3 | dr | [#4486](https://github.com/openova-io/openova/issues/4486) | DR self-fire exercises the standby-absent branch (no standby present). | hw288 | ◑ | hw288-2026-07-23: Not exercisable on this 2-region prov — every Continuum carries a live hot standby: continuums.dr.openova.io shows dr-spine-{gitea,harbor,keycloak,openbao} + cnpg-pair + dr-shared-pg{,-b,-c} all Healthy with region-b replica  ‖ hw239: DR standby-absent branch NOT exercisable on this 2-region prov (standby present: all 5 Continuums standbyRegions=[me-east-215-b], phase=Healthy). Needs a standby-absent topology; not a defect.  ‖ hw288-2026-07-25: standby-absent branch NOT exercisable (standby present) — and the surfacing mechanism IS present (CORRECTING an earlier same-day mis-read that used the failing short-name `kubectl get continuum`): `kubectl get continuums.dr.openova.io -A` shows 8 live Continuum CRs incl cnpg/cnpg-pair-bp-cnpg-pair-continuum PHASE=Healthy; its status surfaces hotStandbyAbsent + standbyAvailable + replicationLagSeconds + switchoverInProgress, with condition StandbyAvailable=True ('required synchronous hot-standby present'). So #4901's 'Continuum doesn't surface standby-absent' gap appears ADDRESSED here; a standby-absent injection would flip hotStandbyAbsent=true. Not a defect on this env. | ‖ hw288-2026-07-26 walked live: cnpg-pair Continuum Healthy, StandbyAvailable=True, hotStandbyAbsent=False → standby PRESENT. G3 tests the standby-ABSENT branch (self-fire with no standby) → needs a DR-failure scenario (breaking the pair), not inducible on the north-star. Gated (needs-a-DR-scenario).
| G4 | adoption | [#4431](https://github.com/openova-io/openova/issues/4431) | VPC-quota reclaim demo on a fresh prov. | — | ⚠️ | EIP-quota — the reclaim demo needs a fresh prov cycle. | mothership-verify-2026-07-06T01:15: mothership [JANITOR] pass emits live orphanEIPsDeleted/orphanVPCsDeleted/orphanEVSDeleted fields (this pass: 0 orphan VPC/EIP, 19 EVS reclaimed) — the VPC/EIP-quota reclaim mechanism is live and running. The full fresh-prov reclaim DEMO (prov→wipe→quota-returns) still needs a fresh prov cycle (EIP-quota-walled). Refs #4431 |
| G5 | janitor | [#4466](https://github.com/openova-io/openova/issues/4466) | janitor log-only live proof (the dry-run full-cycle observation). | m | ⛔ | hw232: mothership catalyst-api janitor log-only — not observable on child | mothership-verify-2026-07-06T01:15: mothership catalyst-api [JANITOR] pass logged "destructive":false, "reaped":0, "reapErrors":0 — a full log-only/dry-run cycle observed live (reaped ZERO Sovereigns) while safely GC-ing 19 orphan EVS + 1 orphan keypair. The dry-run full-cycle observation. Refs #4466 |
| G6 | model | [#4212](https://github.com/openova-io/openova/issues/4212) | object-model seams 1+2 — the two adoption seams collapse into the #4488 crossplane-adoption walk (G1). | — | ☐ | — |
| G7 | orgs | [#4293](https://github.com/openova-io/openova/issues/4293) | vcluster dual-door walk — both Org doors land a vcluster-isolation Org. | hw290 | ✅ | hw290-2026-07-26: Both Org doors land a vcluster-isolation Org with a REAL vcluster backing. DOOR 1 (marketplace funnel, customer door): acme-corp and walk-two → API isolation=`vcluster`, directory ISOLATION=vcluster, `pod/vcluster-0 1/1 Running` + `sts/vcluster 1/1` in each ns, Org CR status.vcluster.phase=Ready; I port-forwarded into acme-corp's vcluster and listed its namespaces live. DOOR 2 (console admin door, POST /api/v1/organizations): created `delta-corp` with plan_slug=m → HTTP 202 body `"isolation":"vcluster","vcluster_name":"vc-delta-corp"`, and 6 min later `kubectl -n delta-corp get pods` → `vcluster-0 1/1 Running` + `coredns-…-x-kube-system-x-vcluster 1/1 Running`, HR `vcluster True — Helm install succeeded … chart vcluster@0.33.4`. NOTE: the admin door defaults to plan_slug=s which correctly yields isolation=namespace (isolationForTier tier gate); plan m/l/xl/flexi is what selects vcluster — verified live on beta/gamma (s → namespace) vs delta (m → vcluster). ‖ — | — | — |
| G8 | apps | [#4277](https://github.com/openova-io/openova/issues/4277) | anthropic credential seeded into the agentic runtime — chat works end-to-end. | hw288 | ◑ | hw288-2026-07-24: Playwright — Agenity workspace /sandbox renders; Claude Code agent card shows 'Anthropic's official agent CLI. Connect Claude Max to use your subscription' + 'Connect Claude Max' link → /sandbox/settings. The chat leg is gated on connecting the model (Anthropic credential #4277) — confirmed founder-gated, NOT a code fail; workspace UI + sessions endpoint (200 empty, no 500) healthy ‖ prior: BLOCKED-founder #4277 | hw227-gate-verify-2026-07-06T01:35: VERIFIED-BLOCKED — kubectl shows NO agenity pods/Application on hw227 (agentic runtime not deployed) AND zero anthropic/claude secrets in the cluster ([]). Agentic chat cannot be walked without the app + a real Anthropic credential (neither present). Refs #4277 | hw227-challenge-2026-07-06T03:22: CHALLENGED — missing precondition: 0 Anthropic secrets in-cluster AND agenity not deployed on hw227 — agentic chat cannot run. Refs #4277 ‖ **GATE CONFIRMED 2026-07-07 (repo inspection):** #3988 built the openova-MCP SERVER (products/openova-mcp/ — Go cmd/internal/{identity,tools,catalystapi}) but NOT its deployment layer: NO Chart.yaml, NO Dockerfile, NO blueprint.yaml, NOT in the bootstrap-kit or catalog. So the RBAC-scoped MCP facade cannot be deployed on ANY Sovereign (confirmed absent on hw228 — only sandbox-controller runs). These rows are gated on PACKAGING the server (image build workflow + Helm chart + blueprint.yaml + bootstrap-kit slot + per-Org KC identity wiring), a substantial build cycle, not a simple deploy. Refs #3988. ‖ **2026-07-09 LEDGER CORRECTION (repo re-inspection, evidence-backed):** the earlier 'NO Chart.yaml/blueprint.yaml/bootstrap-kit/catalog' gate note is FACTUALLY WRONG — it inspected `products/openova-mcp/` only. By design (`docs/INTEGRATION-OPENOVA-MCP.md` §1) the MCP server is baked as a subprocess binary inside the **`bp-agenity`** runtime image, and that deployment layer IS fully built: `products/agenity/blueprint.yaml` (kind:Blueprint, bp-agenity v0.5.20, visibility:listed), `products/agenity/chart/` (statefulset/httproute/oidc-gate/externalsecret-anthropic/externalsecret-mcp-bearer/CNPs), `products/agenity/Containerfile` (bakes `/usr/local/bin/openova-mcp`), catalog-seed `blueprints.yaml:5981`, bootstrap-kit `13-bp-catalyst-platform.yaml`, CI `agenity-build.yaml`. RBAC (list/get/create_application Org-scoping, cross-Org 403) is implemented + unit-tested in `products/openova-mcp/internal/{identity,tools}`. REAL blockers (NOT 'unbuilt'): (a) founder places a live Anthropic credential (`CATALYST_ANTHROPIC_CREDENTIALS_JSON`) → `sovereign_anthropic_seed.go:140` stops loud-skipping — FOUNDER action; (b) install `bp-agenity` into a live Org + walk; (c) close fresh-Org bearer-wiring defect **#4624**. Refs #3988 #4277 #4276 #4624 |
| G9 | apps | [#4111](https://github.com/openova-io/openova/issues/4111) | agentic-run half — the agenity solo agent chats + drives create_application. | hw288 | ◑ | hw288-2026-07-24: MCP agent — 'drives create_application' leg LIVE: pod openova-mcp-bp-openova-mcp 1/1 Running (catalyst-system, 6h), create_application in owner tools/list w/ full inputSchema, whoami/list_orgs/list_apps return real owner-tier data (context:sovereign tier:owner deployment_id=027f0755…). Chat leg founder-gated (#4111/#4277) ‖ hw288-2026-07-23: Split. 'drives create_application' leg LIVE: openova-mcp pod openova-mcp-bp-openova-mcp-64d9c76d48-mpz65 1/1 Running (catalyst-system, region-A), create_application tool present in owner tools/list w/ full inputSchema, MCP st |
| G10 | placement | [#3969](https://github.com/openova-io/openova/issues/3969) | placement EPIC — the remaining real build. | hw288 | ◑ | hw288-2026-07-23: Read-leg VERIFIED: kubectl applications.apps.openova.io shows spec.placement populated — active-hot-standby x6 (spine-gitea/harbor/keycloak, shared-pg/-b/-c), active-passive x1 (spine-openbao); all PHASE=Ready. Full EPIC writ |
| G11 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | sovereignty cutover — 8-step + 10-min deny-egress hold. | hw290 | ☐ | hw290-2026-07-26: cutover fired cleanly via canonical runner-SA trigger (HTTP 200) but GATED BY ENV CAPACITY not cutover code: 2/11 succeeded (gitea-mirror, harbor-projects), harbor-prewarm failed, 8 never started. The #4982 settled-roll pre-flight CORRECTLY refused at step-03 because 13 HRs are permanently mid-roll in sibling-walker Orgs delta-corp+gamma-corp — region-A is CPU-saturated (83/93/92/96% requests across 4 nodes) and gamma-corp/bp-keycloak-0 has been Pending Insufficient-cpu 25m. #5379 per-region legs therefore UNVERIFIED this run: region-B still on github.com + 64 ghcr.io HelmRepos, and the status CM holds ZERO step.<step>.region.<key>.* keys. The ONE #5379 leg proven working: the cutover-secondary-kubeconfigs credential bridge (chart confirmed bp-self-sovereign-cutover@0.1.151) ‖ — |
| G12 | dr | [#4275](https://github.com/openova-io/openova/issues/4275) | region-kill (Pillar-3) — kill a region, prove failover + recovery. | hw290 | ❌ | hw290-2026-07-26: 5/6 legs PASS, leg 6 FAILS with a DATA SPLIT-BRAIN. PASS: (1) RPO=0 — pre-kill row survived (table g12_hw290, pre-kill-region-a @15:10:31.230103+00); (2) region-B promoted to writable RW after a HARD os-stop of all 4 region-A ECS at T0=15:11:18.645Z, region-A apiserver dead in 7.7s; (3) post-promote INSERT accepted on the promoted primary, read-back shows both rows; (4) NO split-brain DURING the outage (region-A fully powered off, region-B replica-1=primary replica-2=replica); (5) gateway survived — answered at the HTTP layer throughout, the #5244 black-hole FAIL criterion EXCLUDED. FAIL leg 6: failback infra/service succeeded (os-start 15:13:57Z → 4/4 nodes Ready 15:15:41Z → console+api 200 by 15:19:15Z, ~8 min kill-to-full-service) but left a DATA SPLIT-BRAIN on return ‖ — |
> **Reading the gate map:** the 13 remaining work-items resolve to **12 distinct open tickets** (#4212 carries object-model seams 1+2, both folding into #4488/G1 — counted as one ticket here, two seams in narrative). 7 of them (G1–G7) clear in ONE fresh post-fix 2-region prov the moment kom4dc EIP quota rises **10 → ≥16**. 2 (G8–G9) need a real Anthropic credential. 1 (G10) is a genuine remaining build. 2 (G11–G12) are destructive walks needing a throwaway env (itself EIP-walled) or explicit founder clearance. None is a false-fail; each is an honest fault with a named unblock.

---


**RESET 2026-06-26 — clean slate for the fresh Sovereign, now walked on fresh prov `91dc05917e44d1c1` (omantel.biz, region-a); recounted after the 2026-06-27 4-section live-walk consolidation + the 2026-06-27 Pillar-1 funnel+voucher walk + the 2026-06-27 newapi-SSO resync walk + the 2026-06-27 authed operator-console walk + the 2026-06-27 Pillar-4 Sandbox/MCP walk + the 2026-06-27 authed live-walk sweep + the 2026-06-27 write-mutation walk on fresh prov `25aadcfc` + the 2026-06-27 ⚠️/❌ reconcile + the 2026-06-27 3-agent live-walk batch (Refs #909): 193 ✅ · 0 ❌ · 9 ⛔ · 33 ⚠️ · 4 ☐ = 239 rows.** (⚠️/❌ reconcile against the live env flipped 7 rows to ✅ with reproduced live evidence: Hubble row 39 `⚠️`→✅ — bp-cilium@1.4.5 rolled, oauth2-proxy `--api-route=^/api/` live, `/api/*` returns 401-not-302; funnel /review row 83 `⚠️`→✅ — marketplace:f1f7ae6 ships the #4526 ReviewStep topology card; OpenBao admin row 33 `❌`→✅ — bp-openbao@1.2.58 rolled, OIDC auth_url now requests `scope=openid+groups` + sso-operator-admin policy mapped (the 403 root cause resolved); Jobs Re-run row 176 `❌`→✅ — catalyst-api e976ff0 ran the #4514 delete+recreate (`mimir-make-minio-buckets-rerun-*` Complete, original deleted, no 502); Topology rows 62/187/188 `⚠️`→✅ on the same data-grounded standard as 64/71 — live Continuum CRs Healthy/Ready/LeaseHeld + grafana multi-region active-active + catalyst-api single-region singleton, all via kubectl, UI render SSO-session-walled. RECLASSIFIED rows 9/10/11/12 `❌`→⚠️ (and corrected 216's rationale): the uatwalk91 host-namespace backing is DESIGN-CORRECT for its `plan=s` per the #4292 TIER GATE — `boundaryIsVcluster`/`allTiersVcluster=false` backs free/S Orgs with the host `<slug>` namespace, only M+ get a dedicated Org-vcluster — so the blanket `vcluster`-for-every-Org expectation is stale, leaving only a cosmetic `isolation=vcluster` label mismatch. Row 183 HELD ❌ (the 403 root cause is #4522-fixed but the row asserts the rendered Secrets-Engines screen, SSO-session-walled). Rows 37/114 confirmed stayed ✅ (newapi bare URL → 200 'Signing you in…', `sovereign` provider recognized). NO false-pass: every ✅ flip reproduced the live signal at the HTTP/kubectl/served-bundle layer; the authed test-session mint is 404-gated in prod so interactive UI renders stay honestly ⚠️/❌ where un-reproduced.) (Authed operator-console walk via a throwaway-mailbox PIN session flipped 11 interactive-UI rows `⛔`→✅ — treemap/showback 20/21/23, keycloak-admin 34, Organizations directory/terminology 116/117/118/119/120/121/122 — and exposed 2 genuine product faults `→❌`: OpenBao authed admin UI 403 row 33, Jobs Re-run 502 immutable-spec row 176, both with fixes in flight.) (newapi-SSO walk flipped rows 37 + 114 `❌`→✅ — #4477 `resync_provider_secret()` (bp-newapi@1.4.137) resolved the stale `custom_oauth_providers` client_secret live, no "Unknown OAuth provider"; Hubble row 39 holds ⚠️ fix-merged-pending-roll for PR #4509, the `--api-route=^/api/` flow-XHR fix.) (Pillar-4 Sandbox/MCP walk via a throwaway Sandbox CR on `91dc05917e44d1c1` region-a flipped row 218 `⛔`→✅ — sandbox-controller no longer CrashLoops (R19/#4482 gitea-token bridge), Sandbox CR reconciles `Ready=True` — and flipped 219 + 220 `⛔`→⚠️ PARTIAL (controller writes 12 gitops files incl the MCP ConfigMap + pty-server StatefulSet; the image bundles `@anthropic-ai/claude-code` + `@qwen-code` + the `/usr/local/bin/openova-sandbox-mcp` binary — runtime-pod-materialize + live agentic run not awaited). Rows 221/222/223 HOLD ⛔ on an Anthropic credential: the MCP `create_application` tool + the openova-mcp Org-claim RBAC are proven at the API layer (211/212/213), only the live chat-driver is credential-walled. The MCP auto-mount is PROVEN — `configmap-mcp-config.yaml` projected at all 4 agent-discovery paths (`/workspace/.mcp.json`, `~/.claude.json`, `~/.qwen/settings.json`, `.cursor/mcp.json`) + the MCP binary in the published image. ⚠️ note: the sandbox-controller has no delete-finalizer to prune the gitea gitops path on Sandbox CR delete (minor cleanup gap); a subset of Wave-2 MCP tools (`registry.go`, `sandbox_db.go`) return `not_implemented` stubs — surface discoverable, some advanced bodies stubbed.) (Consolidation pass 2 of the live fresh-prov walk — folds §C orgs/keystone · §G model+placement · §H contexts+mcp+sandbox · §I recon/cloud/e2e/meta on top of pass 1's §A funnel · §B SSO · §D topology/DR · §E jobs/cloud/storage · §F session-fix.) Per-row cells carry the walker's exact verdict + evidence; the 100 remaining `☐` are genuinely-unwalked surfaces — no fabricated verdicts. **Pass-2 corrections applied:** (1) per-Org provisioning is PROVEN post-#4471 RBAC fix — funnel Org `uatwalk91` serves WordPress HTTP 200 over valid pool LE TLS, so the prior ⛔ "org-controller RBAC" cells flipped ✅ for the WordPress-backed rows (86/88/89/90/233) and ⚠️ "this app not in the walked Org's cart" for openclaw/mail/postgres (232/234/238); (2) the per-app **Topology tab exists** (§I `app-tab-topology`), falsifying §D's "no Topology tab" premise — rows 51-58/62-64/71/187/188 moved ❌→⚠️ pending a content re-walk; (3) placement rows 98-108 are **superseded by #4325 de-vcluster** (re-baseline #3642, not a product fault) → ⚠️, not ❌; (4) §I canvas-click drill-ins (179/181/184/186/193/195-197) are ⚠️ harness artifacts, not click-verified; (5) restored 9 §A/§F walker-PASS rows (40/76/79/80/84 §A funnel; 227/230/231 §F ✅, 228 §F ⚠️) whose cells reverted to ☐ in a working-tree branch-switch — re-applied from the captured walker evidence, no new walk. (Write-mutation walk on fresh prov `25aadcfc` [kv160729.omani.works, a fresh prov on current main; writes are safe + were cleaned up] flipped **26 rows `☐`→✅**: catalog Save/commit-IaC rows 127/128/129/132–140/142/143/145/147/148/149/153–156 — `PUT /api/v1/catalog/<bp>/iac` → 200 `{committed:true}` + durable bare-repo Gitea commit, proven on two structurally-different blueprints bp-alloy + bp-wordpress; instance-provision rows 50/59/60/61 — `POST /catalyst/v1/apps/instances` → 201 + CR Ready + matching topology badge. These rows were GATED only by the read-only constraint on the PERMANENT env — the catalog-IaC-commit + provision CODE is identical, proven on the disposable throwaway.) The detailed historical narrative below (omantel.biz convergence, the hw1xx throwaway envs) is retained ONLY as explicitly-labeled prior-env history — its per-row claims are STALE by construction and must not be read as current. (Refs #4279.)

> 📋 **Row map:** rows **1–226** are the canonical acceptance walk (epics: model · onboarding · topology · SSO · jobs · recon · cloud · storage · mcp · e2e-journey · convergence · funnel · meta). Rows **227–239** are the **Fresh-prov 2026-06-26 — session-fix acceptance** section (this session's resolved-issue cases — delivery-architecture, fresh-funnel apps, spine/postgres/crossplane). All ☐ until the fresh-prov walk.

## Prior — omantel.biz PERMANENT Sovereign (dep `4635277cae4ffed9`, me-east-215-a/-b) — STALE, superseded by the fresh prov
_All per-row verdicts below were walked on the now-superseded omantel.biz env and are STALE by construction. Retained as labeled history; the live verdict cells above are all `☐` pending the fresh-prov walk._

## Prior — wiped throwaway envs (hw173/hw176/hw177/hw178 — STALE, superseded by omantel.biz)
_hw178 (dep `02bc518fcb1b4cb3`) — cycle-2d zero-touch re-fire; 6 fixes walked PASS; superseded. hw177 wedged on a worker-kubelet infra flake; wiped. All hw1xx envs are wiped — their per-row verdicts do not reflect omantel.biz._

## Prior — cycle-2b BLOCKED on hw176 storage wedge (2026-06-21, now fixed)
_hw176 (dep `12523e0c7611356b`) — zero-touch cycle-2b re-fire on `main@10b061b`. **STORAGE FIX DID NOT HOLD zero-touch (❌, headline).** HRs flooded to 67/region (past the old 40/50 wedge) but `bp-huawei-evs-csi` 1.0.1 HelmRelease → InstallFailed: `no matches for kind VolumeSnapshotClass in version snapshot.storage.k8s.io/v1` (the chart renders a VolumeSnapshotClass by default but the snapshot.storage.k8s.io CRDs are installed nowhere in the bootstrap-kit) → no `evs-ssd` SC → 6 stateful PVCs Pending → `bp-gitea` not ready → `bp-catalyst-platform` blocked → console HTTP 000. Root-caused live + fix shipped: #4030 / PR #4031 (chart 1.0.2, volumeSnapshotClass default off, MERGED). The cycle-1 fix-wave was ⛔ un-walkable behind the wedge — health-gate honored. Per-row evidence: [`../sessions/2026-06-21/walk-hw176/`](../sessions/2026-06-21/walk-hw176/)._

## Status — last validated: hw173 (2026-06-20)
_Update: topology rows 46–71 re-walked across BOTH region clusters (#3965 fix) → 0✅/14❌ became 9✅/9❌/8⚠️; the remaining ❌ are the Continuum-lease unfinished-feature gap + no-live-catalog-create, not region blindness._
**124 ✅ · 32 ❌ · 30 ⚠️ of 186.** Verified live via API + mothership-kubectl by 7 parallel walkers (#883); per-row evidence in [`../sessions/2026-06-20/walk-hw173/`](../sessions/2026-06-20/walk-hw173/). Headline defects: hw173 came up **single-region** → topology epic unwalkable (14❌, Continuum Degraded); **funnel provisioning CrashLoop** (FerretDB `partialFilterExpression` unsupported) → zero customer Orgs → model/orgs `acme` rows fail; **Harbor down** (core CreateContainerConfigError + jobservice CrashLoop); **Gitea SSO 500**; **Tenants/SME banned-term leak** in console bundle.

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| 1 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Console bare URL → lands `/dashboard` signed-in as the owner (no PIN/login form). | — | ☐ | — |
| 2 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Full sidebar renders (Dashboard / Cloud / Apps / Catalog / Agenity / Jobs / Compliance / Users / Organizations / Settings). | — | ☐ | — |
| 3 | model | [#3687](https://github.com/openova-io/openova/issues/3687) [#4546](https://github.com/openova-io/openova/issues/4546) | The voucher-redeem URL on an authed owner session server-redirects to `/dashboard` (no redeem form for the owner). | hw255 | ⚠️ | hw270 partial: redeem URL w/ residual org-token → client-side redirect to /auth/org-handover (gate FIRES) but embedded token expired (15-min TTL) → 401. Mechanism confirmed; fresh E2E pass pending. row3-redeem-redirect-orghandover-stale-token.png | — |
| 4 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | App page tab strip generality: open ≥2 archetypes (a DB `shared-pg`, a consumer `harbor`) → identical tab strip; consumer Dependencies shows `Depends on: shared-pg / db:<ctx>`. | — | ☐ | — |
| 5 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Organizations directory: the customer Org row shows KIND=customer, TIER=sme, BILLING=real, ISOLATION=vcluster, STATUS=active. | hw290 | ✅ | hw290-2026-07-26: console.hw290.omani.works/organizations directory table (own chromium, owner PIN session) — header `ORGANIZATION ‖ KIND ‖ TIER ‖ BILLING ‖ ISOLATION ‖ STATUS`; customer row `Acme Corp / emrah.baysal+acmecorp@openova.io ‖ CUSTOMER ‖ Org ‖ REAL ‖ vcluster ‖ ACTIVE`. Screenshot /tmp/claude-1000/-home-openova-repos-openova/5c468708-346e-4400-9263-1ff015d1acc7/scratchpad/w7-dir.png + w8-dirtable.png. NOTE: TIER renders `Org`, not `sme` — the row's literal `TIER=sme` is superseded by the #3383 SME→Organization rename (SME is now a banned term); every other cell matches verbatim. ‖ — |
| 6 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The customer Org is present as a real Organization in the operator directory (same record dashboard + showback read). | hw290 | ✅ | hw290-2026-07-26: Same record readable on three surfaces: (a) directory row above; (b) GET /api/v1/organizations 200 → {"org_tenant_id":"d38a040e-0f62-4df9-9de3-2aadc4f1be56","state":"done","subdomain":"acme-corp","kind":"customer","tier":"org","billing_mode":"real","isolation":"vcluster"}; (c) Showback panel row `acme-corp (Organization) · 525.5 units · 520m CPU · 1.06 GiB mem · 5 GiB storage`; (d) Dashboard treemap LAYER1=Organization renders an `acme-corp` top-level cell (w10-treemap-org.png). ‖ — |
| 7 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail renders canonical fields: slug `acme` (NOT `sme-<uuid>`), kind customer, tier sme, billing real, isolation vcluster, owner, console URL. | hw290 | ✅ | hw290-2026-07-26: console.hw290.omani.works/organizations/acme-corp — heading `Acme Corp`, breadcrumb `← Organizations`, fields: Slug `Acme-Corp` (CSS text-transform:capitalize over the real value `acme-corp` — NOT `sme-<uuid>`; kubectl Organization/acme-corp spec.slug=acme-corp), Kind `Customer`, Tier `Org`, Billing mode `Real`, Isolation `Vcluster`, Status `Active`, Owner `emrah.baysal+acmecorp@openova.io`, Console `console.acme-corp.omani.homes`. Screenshot w5-51-org-detail.png. ‖ — |
| 8 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The Create-organization form renders fully (kind picker, slug, Company name, Admin email, parent-domain). | — | ☐ | — |
| 9 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail Status = active, backed by a real `vcluster` isolation (the create→Provision loop produced a live backing, not a fake-green). | hw290 | ✅ | hw290-2026-07-26: Status=Active is backed by a REAL vcluster, not fake-green: `kubectl -n acme-corp get sts vcluster` → 1/1 (67m); `pod/vcluster-0 1/1 Running`; `pod/coredns-…-x-kube-system-x-vcluster 1/1 Running` (syncer-projected); Organization/acme-corp `status.vcluster={hostCluster:hw290.omani.works, name:acme-corp, phase:Ready}` and `status.conditions[Ready]=True msg='vCluster HelmRelease Ready + Keycloak group + Gitea Org reconciled'`. I also port-forwarded into the vcluster (svc/vcluster:443, no NodePort) and listed its namespaces/CRDs live — it is a real API server. ‖ — | — |
| 10 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The customer Org is present + ACTIVE backed by a real `vcluster` isolation (Lane B convergence read). | hw290 | ✅ | hw290-2026-07-26: Directory (Lane-B convergence read) shows `Acme Corp ‖ CUSTOMER ‖ Org ‖ REAL ‖ vcluster ‖ ACTIVE`; live backing confirmed by kubectl as in row 9. Second funnel Org `walk-two` likewise `CUSTOMER ‖ Org ‖ REAL ‖ vcluster ‖ ACTIVE` with vcluster-0 1/1 Running. ‖ — | — |
| 11 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail Status = active and the directory + detail consistently show `vcluster` isolation. | hw290 | ✅ | hw290-2026-07-26: Directory ISOLATION column = `vcluster` AND detail page Isolation = `Vcluster`, Status = `Active` — consistent across both views in the same session (w7-dir.png / w5-51-org-detail.png). ‖ — | — |
| 12 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | On re-load the Org detail consistently reports active + `vcluster` (stable, no false flicker). | hw290 | ✅ | hw290-2026-07-26: page.reload() on /organizations/acme-corp — post-reload dump is byte-identical on the material fields (Status Active, Isolation Vcluster, Kind Customer, Tier Org, Billing mode Real). Screenshots w5-51-org-detail.png vs w5-52-org-detail-reload.png; no flicker to Provisioning/Unknown. ‖ — | — |
| 13 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Catalog grid renders the Blueprint cards; `bp-postgres` detail has a **+ New instance** button. | — | ☐ | — |
| 14 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The shared-PG reuse model is LIVE: the `shared-pg` app **Contexts** tab shows multiple consumers sharing ONE PostgreSQL. | — | ☐ | — |
| 15 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Apps list renders one card per Application (all Platform/BOOTSTRAP-… a customer-launched app card would appear under its Org. | — | ☐ | — |
| 16 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | A customer app page (`/app/<name>`) → Settings/Topology → change topology → Save persists; the canonical tab strip includes a Topology tab. | hw290 | ✅ | hw290-2026-07-26: TOPOLOGY/IaC HALF ONLY — the per-Org-realm half of this row was NOT walked (explicitly gated by #5373 per dispatch). Tab strip: /app/uat16-topo renders app-detail-tablist containing app-tab-topology (plus overview/contexts/resources/compliance/logs/settings/members/endpoints/jobs/dependencies). CHANGE→SAVE→PERSISTS proven on a customer app in Org acme-corp: BEFORE `kubectl -n acme-corp get application uat16-topo -o jsonpath='{.spec.placement}'` = {"mode":"singleton","regions":["me-east-215"]}, Topology tab 'Pattern: singleton', placement-editor-derived-pattern 'Derived pattern: singleton — One Primary in one region; no cross-region failover.' with 1 target. Clicked 'Edit placement' (topology-tab-edit-placement) → '+ add target' → target-1 region=me-east-215-b-1, Role=Standby, Type=Hot → derived pattern live-flipped to 'Derived pattern: active-hot-standby — One Primary; a live Hot standby in the second region promotes in seconds.' (2 targets) → 'Apply'. AFTER (14:50:53Z) `kubectl -n acme-corp get application uat16-topo -o jsonpath='{.spec.placement}'` = "active-hot-standby", metadata.generation 1→2, status.phase Ready; re-loading /app/uat16-topo → Topology tab now renders 'Pattern: active-hot-standby' with the PRIMARY/STANDBY·Hot pair. A separate no-op Apply on uat50-ahs also proved the write wire: PUT 200 /api/v1/sovereigns/31106b6aa4a7e8e7/applications/uat50-ahs?namespace=acme-corp → {"applied":true,"placement":"active-hot-standby","message":"HTTP 200 OK — Application \"uat50-ahs\" updated in namespace \"acme-corp\""}. Screenshots w290/14-uat16-topo-add-edited.png, w290/14-uat16-topo-add-applied.png. SUB-GAPS (see notes): the reverse (destructive) transition is a UI dead-end (HTTP 409 placement-transition-blocked, no force affordance), and spec.parameters.topology.mode stays 'singleton' after the flip. ‖ hw288-2026-07-24: per-app-tabs agent — canonical tab strip includes a Topology tab CONFIRMED on both archetypes (app-tab-topology in app-detail-tablist, live); the change-topology→Save leg is a write-mutation, not exercised read-only → stays ◑ ‖ hw224-fulltable-2026-07-04: /app/shared-pg strip includes Topology + 'Edit placement'; change-topology→Save is a write-mutation (not exercised). uat-hw224-fulltable-07‖ §G 91dc0591: canonical strip includes Topology tab + 'Edit placement', but customer app not surfaced as /app/<name> in owner console so change-topology→Save not exercised. uat-91dc-model-16 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check): /app/uat-hw228-pg Topology tab shows 'Placement Pattern: singleton · mgmt · PRIMARY' + 'Edit placement' control present. ◑ — did NOT execute the topology-change persist (would disrupt a live customer Postgres; outside permitted mutations). ‖ 2026-07-08 TRIAGE: full ✅ is env-blocked on **#4851** — the console owner login is broken on hw228 (catalyst-api SMTP → mothership `mail.openova.io:587` connection-refused, stalwart-mail submission LB `<pending>`), so the authenticated console surface this row needs cannot be walked until the mothership SMTP submission path is restored. NOT a code gap (chart/handler are correct); a shared-mail-infra dependency. Stays ◑. | ‖ hw288-2026-07-26 re-verified live: acme-corp Org conditions PerOrgRealmProvisioned=False(RealmDisabled) + IacRepoBootstrapped=False(BootstrapDisabled) — per-Org realm/IaC feature is flag-DISABLED in this controller deployment (Ready=True; core Org fine). The /app/<name> detail view reads from the IaC deployment source → unresolvable while IacRepoBootstrapped=Disabled. Gated on the feature flag (a controller-deployment config state, not walkable here).
| 17 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Dashboard treemap is a meaningful drill-down surface (Organization → vCluster → Application selectable & drillable, NOT a raw infra-pod utilisation treemap); default layer stack per founder verdict. | — | ☐ | — |
| 18 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Scan every treemap cell: NO ephemeral Job-pod cell appears (no `cutover-*`, scan-vulnerabilityr… `*-snapshot-save-*`). | — | ☐ | — |
| 19 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Count Application cards: one card per `Application` (NOT one per HelmRelease/pod); bootstrap apps carry a Platform/Bootstrap badge. | — | ☐ | — |
| 20 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | With a customer Org present, the customer estate is visually distinct from platform pods on the treemap. | hw290 | ✅ | hw290-2026-07-26: Dashboard treemap with SIZE=CPU request, LAYER 1=`Organization`, LAYER 2=`Namespace`: one `Platform overhead` super-cell containing every platform ns (kube-system 32%, catalyst-system 31%, flux-system, cnpg, shared-data, harbor, …) and SEPARATE top-level customer cells `beta-corp`, `gamma-corp`, `acme-corp`, `walk-two`. Screenshot w10-treemap-org.png; label set captured from the DOM. ‖ — |
| 21 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Organizations Showback panel: the **Application** column for a selected Org lists only that Org's real apps (no cluster-wide infra/Job pods). | — | ☐ | — |
| 22 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Showback shows a single visually-distinct **Platform overhead** roll-up line holding all control-plane/Job workloads. | — | ☐ | — |
| 23 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | After a 2nd Org runs an app, the showback panel shows a SECOND Org row attributed only to its own app, distinct from Platform overhead. | hw290 | ✅ | hw290-2026-07-26: Showback panel on /organizations lists 4 distinct Organization rows, each attributed ONLY to its own apps, all distinct from `__platform__` overhead: `gamma-corp · 1566 units` (bp-agenity/bp-keycloak/bp-oidc-gate, ns gamma-corp), `beta-corp · 1566 units` (same three, ns beta-corp), `acme-corp · 525.5 units` (vcluster), `walk-two · 525.5 units` (vcluster), plus `__platform__ · 21707 units`. Screenshot w6-orgs-dir.png. Sub-gap: an operator-installed `postgres` app in ns acme-corp is attributed to `__platform__` (its HR carried no openova.io/organization label). ‖ — |
| 24 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | `shared-pg` renders the canonical tab strip Overview · Contexts · Topology · Dependencies (Contexts tab present only for shareable blueprints). | — | ☐ | — |
| 25 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | One consistent model across surfaces: `/organizations` shows the Orgs that `/apps`, the dashboard and showback all agree on. | — | ☐ | — |
| 26 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Console bare URL → lands on the dashboard signed-in as the owner; no PIN/login/"Sign in with…" button. | hw290 | ✅ | hw290-2026-07-26: env=hw290 (dep 31106b6aa4a7e8e7) 2026-07-26 ‖ GET https://console.hw290.omani.works/ → HTTP 200, final URL /dashboard ‖ renders Dashboard treemap (195 items) + identity chip 'EB · emrah.baysal@openova.io · hw290.omani.works'; 0 password inputs, 0 login markers ‖ in-page fetch /api/v1/whoami → 200 {email emrah.baysal@openova.io, tier:owner, realm_access.roles [catalyst-admin, catalyst-owner]} ‖ shot shots/console-dashboard.png ‖ NOTE: session first established via the real passwordless-PIN flow (POST from the live /login form → IMAP PIN read BODY.PEEK readonly → typed into the one-time-code field); a browser with NO session correctly shows the PIN front door, which is the by-design passwordless door, not a login button ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ RE-AFFIRMED (openova-uat-bot e015062d2 spuriously flipped this ☐ after merge 55176fe5 — but that PR #5353 only touched compliance.go/main.go/OOM, NOT the SSO chain; over-flip is the known SSO-restamp anti-pattern). Fresh browser walk (owner handover): console bare URL → lands on /dashboard (H1 'Dashboard' + treemap), signed-in, NO PIN/login/'Sign in with' form. ‖  UNVERIFIED (flipped 2026-07-25 by 55176fe5) — was: hw288-2026-07-23: ADVERSARIAL RE-WALK — could not refute; claim is genuine positive proof, confirmed with live SPA render (not 200-on-empty). LIVE hw288 (dep 027f07559af1f9f7), 2026-07-23 15:10-15:12Z. (1) GET /auth/handover?token=<RS256 owner |
| 27 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Avatar (top-right) menu reads "Signed in as the owner" with a Sign-out item. | hw290 | ✅ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ clicked the top-right avatar on /dashboard → menu renders 'SIGNED IN AS / emrah.baysal@openova.io' with a 'Sign out' item ‖ shot shots/f-console-avatar.png ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ RE-AFFIRMED (spurious e015062d2 over-flip; #5353 didn't touch SSO). Fresh walk: identity chip reads 'EB · emrah.baysal@openova.io · hw288.omani.works' (the owner) + top-right avatar menu button — signed in as owner. ‖  UNVERIFIED (flipped 2026-07-25 by 55176fe5) — was: hw288-2026-07-24: Playwright — clicked avatar (top-right) → Account menu reads 'Signed in as emrah.baysal@openova.io' (the owner) with a 'Sign out' menuitem ‖ hw288-2026-07-23: Backing proven: whoami 200 tier=owner email=emrah.baysal@openova.io. Avatar-menu UI render not exercised (curl vantage, no browser) — pixel render unwalked. |
| 28 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Users page renders the pre-seeded owner row the owner (tier=owner UserAccess CR), signed-in admin. | hw290 | ✅ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ GET /users → HTTP 200 signed-in, User Access table renders the pre-seeded owner principal: row 'useraccess-owner-emrah-baysal-at-openova-io ‖ emrah.baysal@openova.io ‖ hw290 ‖ 1 binding ‖ created 2026-07-26T12:37:15Z' plus 'hw290-omani-works-emrah-baysal-at-openova-io ‖ hw290-omani-works (admin)' ‖ tier filter chips viewer/developer/operator/admin/owner present ‖ shot shots/console-users.png ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ RE-AFFIRMED (spurious e015062d2 over-flip; #5353 didn't touch SSO). Fresh walk: /users ('User Access') renders the pre-seeded owner rows — hw288-omani-works-emrah-baysal-at-openova-io + useraccess-owner-emrah-baysal-at-openova-io (emrah.baysal@openova.io, tier=owner UserAccess CRs, admin grant), tiers viewer/developer/operator/admin/owner listed. ‖  UNVERIFIED (flipped 2026-07-25 by 55176fe5) — was: hw288-2026-07-24: Playwright authed walk — /users User Access page renders tier list (viewer/developer/operator/admin/owner) + table with pre-seeded OWNER row 'useraccess-owner-emrah-baysal-at-openova-io' (emrah.baysal@openova.io, sovereign hw288); confirms tier=owner UserAccess seeded (whoami tier=owner corroborates) ‖ hw288-2026-07-23: Backing proven via kubectl: 2 Active UserAccess CRs — hw288-omani-works-emrah-baysal-at-openova-io (Active, 1 grant) + useraccess-owner-emrah-baysal-at-openova-io (Active); KC sovereign realm users/count=1 owner enabled. Cons |
| 29 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Re-open the bare console URL after the session TTL → lands signed-in again, no PIN re-prompt. | hw290 | ✅ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ FRESH browser context seeded with only the Keycloak SSO cookies (KEYCLOAK_IDENTITY, KEYCLOAK_SESSION, AUTH_SESSION_ID, KC_AUTH_SESSION_HASH) and catalyst_session REMOVED → GET https://console.hw290.omani.works/ lands on /dashboard with NO PIN re-prompt ‖ /api/v1/whoami → 200 tier=owner realm_access.roles [catalyst-viewer, catalyst-admin, catalyst-operator, …] ‖ shot shots/f-row29.png ‖ silent re-auth rides the KC SSO session, catalyst_session re-minted without user interaction ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-26: ✅ walked live. Bare console URL `console.hw288.omani.works/` within an active session → lands directly on `/dashboard`, signed-in, NO PIN re-prompt + NO /login redirect (session cookie persists). Also observed the secure inverse: after a long-idle TTL the bare URL correctly redirects to `/login?next=/dashboard` (no silent stale-session). Re-auth via 6-digit PIN restored the session cleanly. Screenshot docs/sessions/2026-07-25/screenshots/row29-bare-url-signed-in.png. ‖ hw288-2026-07-23: Session persistence proven: whoami 200 stable 3/3 on repeat, /dashboard 200 no login redirect, no PIN. True TTL-expiry silent-refresh not exercised (session within TTL) — matches hw287 ◑. |
| 30 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Grafana bare URL → lands on Grafana Home, full UI, no login form; left nav shows Administration; user menu = the owner. | hw290 | ✅ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ GET https://grafana.hw290.omani.works/ → 200, final /?orgId=1&from=now-6h&to=now, title 'Home - Dashboards - Grafana', no login form (0 password inputs) ‖ left nav shows Administration ‖ authenticated call /api/user → 200 {email emrah.baysal@openova.io, login emrah.baysal@openova.io, isGrafanaAdmin true, isExternallySynced true, authLabels [Generic OAuth]} ‖ shot shots/grafana.png ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ live walk via the persistent Keycloak PIN-login session (owner emrah.baysal@openova.io) — corrects the openova-uat-bot e015062d2 spurious ☐ over-flip (#5353 didn't touch SSO). Front door reachable this measurement (0/12 404); the #5341 shared-gateway flake is intermittent+non-deterministic (harbor/openbao/powerdns currently 404), fix PR #5354 merged+published pending the post-cutover roll. grafana.hw288.omani.works/ → SSO'd in (no login form), landed on 'Home - Dashboards - Grafana' ('Welcome to Grafana', full UI, Profile/user-avatar). Owner is Grafana admin (prior /api/user isGrafanaAdmin=true). ‖  UNVERIFIED (flipped 2026-07-25 by 55176fe5) — was: hw288-2026-07-24: Playwright — BARE grafana URL → 'Home - Dashboards - Grafana', 'Welcome to Grafana' full UI + Profile, NO login form (silent-SSO via Keycloak session) ‖ hw288-2026-07-23: Bare / → 4 redirects to auth.hw288.omani.works/realms/sovereign/broker/catalyst-pin/login?client_id=grafana → HTTP 400 (no KC browser session via curl, expected). Anon /api/user → 401. OIDC-wired to sovereign realm; grafana-s [screenshot-confirmed 2026-07-25: docs/sessions/2026-07-25/screenshots/uat-row30-235-grafana-sso-signedin.png — 'Welcome to Grafana', Home/Dashboards, signed-in user avatar top-right, NO login form] ‖ |
| 31 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Gitea bare URL → lands on the gitea dashboard titled "emrah.baysal — Dashboard", no login; profile menu exposes Site Administration; stays on :443. | hw290 | ✅ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ GET https://gitea.hw290.omani.works/ → 200, title 'emrah.baysal - Dashboard - Catalyst Gitea', profile shows emrah.baysal, no login form ‖ Site Administration reachable: GET /admin/config → 200 title 'Summary - Catalyst Gitea' ‖ all URLs stay on :443 (no port-shifted redirect) ‖ shots/gitea.png + shots/f3-gitea-menu.png ‖ NOTE /-/admin returns 404 on this Gitea 1.22.3 — admin lives at /admin, not a defect ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ live walk via the persistent Keycloak PIN-login session (owner emrah.baysal@openova.io) — corrects the openova-uat-bot e015062d2 spurious ☐ over-flip (#5353 didn't touch SSO). Front door reachable this measurement (0/12 404); the #5341 shared-gateway flake is intermittent+non-deterministic (harbor/openbao/powerdns currently 404), fix PR #5354 merged+published pending the post-cutover roll. gitea.hw288.omani.works/ → page title 'emrah.baysal - Dashboard - Catalyst Gitea' (signed in, NO login, stays :443) — the exact row acceptance. ‖  UNVERIFIED (flipped 2026-07-25 by 55176fe5) — was: hw288-2026-07-24: Playwright — BARE gitea URL https://gitea.hw288.omani.works/ → landed 'emrah.baysal - Dashboard - Catalyst Gitea', signed in, NO login form (silent-SSO via Keycloak session) ‖ hw288-2026-07-23: Bare / → 4 redirects to sovereign broker/catalyst-pin/login?client_id=gitea (callback /user/oauth2/openova-sso/callback) → HTTP 400 (no KC session). gitea-sso-oidc-credentials ES SecretSynced=True. OIDC-wired; silent-landing |
| 32 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Harbor bare URL → lands on `/harbor/projects`, no login form; user dropdown the owner with Administration menus. | hw290 | ⚠️ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ AUTH WORKS BUT BARE URL SHOWS A LOGIN FORM. GET https://harbor.hw290.omani.works/ → 301 → registry.hw290.omani.works/c/oidc/login → KC auth (kc_idp_hint=catalyst-pin) → /c/oidc/callback?code=… → FINAL https://registry.hw290.omani.works/account/sign-in?redirect_url=%2Fharbor%2Fprojects rendering 'LOGIN WITH openova-sso / OR / LOGIN VIA LOCAL DB' — row asserts no login form ‖ OIDC onboarding did succeed: a subsequent GET /harbor/projects → 200 projects list with emrah.baysal@openova.io, and /api/v2.0/users/current → 200 {admin_role_in_auth true, comment 'Onboarded via OIDC provider', creation_time 2026-07-26T13:18:38Z} ‖ shots/harbor.png (sign-in page) + shots/f-harbor.png (2nd-hit projects list) ‖ defect = first bare-URL hit does not complete to /harbor/projects ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ harbor SSO PROVEN end-to-end via real human login. Passwordless-PIN login (email→PIN→Keycloak catalyst-pin broker→harbor /c/oidc/callback) landed on registry.hw288/harbor/projects SIGNED-IN as emrah.baysal@openova.io (users/current=200, 'Projects Total 9, 226 repos, 18.38GiB', owner dropdown). Then bare registry.hw288/ (harbor EXT_ENDPOINT = canonical harbor URL, up 6/6) → silently redirects to /harbor/projects, NO login form, users/current=200, owner dropdown — the exact row acceptance. Screenshot: docs/sessions/2026-07-25/screenshots/issue5362-harbor-sso-signedin-proof.png. My earlier '401 session bug' (#5362) was a Playwright rapid-nav artifact — REFUTED+CLOSED (harbor-sso-configure converges HTTP 200 x2816/0-err, user onboarded). CAVEAT: the harbor.hw288 VANITY alias is intermittently #5341 envoy-404 (separate gateway-routing flake, fix #5354 delivery-gated on #5265) — that's an alias-routing issue, NOT an SSO defect; the canonical harbor URL + SSO work. ‖ hw288-2026-07-25: live re-walk (owner Keycloak session) — TWO blockers confirmed. (1) NOT zero-click: harbor.hw288 → registry.hw288/account/sign-in landing page with a 'LOGIN WITH openova-sso' OIDC button (+ 'LOGIN VIA LOCAL DB') — Harbor OIDC cannot auto-redirect, so the bare-URL 'no login form' bar is architecturally unmet. (2) The one-click OIDC login does NOT stick: GET /c/oidc/login → momentarily /harbor/projects then BOUNCES back to /account/sign-in, api/v2.0/users/current=401 (no Harbor session established). Entangled with the live #5341 shared-gateway front-door flake (harbor 6/12 envoy-404 this measurement; the OIDC callback legs race the 404). Gated on the #5354 roll (delivery-gated #5265) + a post-roll re-walk to isolate any residual harbor OIDC session bug. Prior:  hw288-2026-07-25: BLOCKED by live #5341 gateway flake — harbor.hw288.omani.works returns 12/24 (50%) envoy-404 (same wildcard-listener SNI collision as mcp; harbor route is on the shared cilium-gateway). The SSO landing can't be cleanly walked while the front door flakes 50%. Fix PR #5354 (console gateway specific hostnames) merged but roll-gated (bp-sovereign-tls-vars@0.1.1 not yet delivered to hw288). Stays PARTIAL pending that roll. ‖  hw288-2026-07-24: Harbor bare URL live walk — https://harbor.hw288.omani.works/ redirects to registry.hw288.omani.works/account/sign-in?redirect_url=/harbor/projects (Harbor's OWN sign-in page, NOT silent-SSO auto-signed-in). Row's 'no login form' assertion NOT met — stays ◑ (Harbor presents its own sign-in by design). ‖hw288-2026-07-24: Playwright — harbor https://harbor.hw288.omani.works/ → Harbor sign-in page with 'LOGIN WITH openova-sso' OIDC button (+local-DB fallback); one click → /harbor/projects authenticated, NO password (silent through Keycloak). Harbor shows its own sign-in page (Harbor cannot auto-redirect to OIDC — design limit), so not fully form-less; OIDC chain works end-to-end ‖ hw288-2026-07-23: registry.hw288/ → 200 (SPA shell, 0 redirects); anon /api/v2.0/users/current → 401 {code:UNAUTHORIZED} (good negative control). harbor-sso-oidc-credentials ES SecretSynced=True. Authed projects-landing needs browser KC sessio hw288-2026-07-25 (2nd walk, front door 0/10 reachable this window): harbor OIDC login STILL 401-bounces — GET /c/oidc/login → momentary /harbor/projects → bounces to /account/sign-in, api/v2.0/users/current=401 (2/2 attempts). Ruled OUT a redirect/domain mismatch: harbor EXT_ENDPOINT=https://registry.hw288.omani.works (consistent with where I navigated), auth_mode=oidc_auth, sso-bridge manages the harbor OIDC secret. No OIDC error in harbor-core logs → the callback state/session isn't establishing under programmatic (Playwright) navigation — analogous to the guacamole #5358 nonce race; a human click may fare better. So row 32 is blocked by BOTH the intermittent #5341 front-door flake AND this OIDC state-establishment quirk. Stays ◑. ‖ hw288-2026-07-25 (3rd walk, network+log traced — CORRECTING my two earlier hypotheses this session): the harbor OIDC flow is CORRECTLY FORMED (network capture: client_id=harbor, redirect_uri=https://registry.hw288.omani.works/c/oidc/callback matching EXT_ENDPOINT, response_type=code + PKCE S256, kc_idp_hint=catalyst-pin, state consistent oP355z…; the callback receives code+state and returns 302). registry.hw288/c/oidc/callback is REACHABLE (0/12 envoy-404; bare GET → harbor 400) so it is NOT a #5341 callback-domain flake. YET no session establishes (api/v2.0/users/current=401, bounces to /account/sign-in). harbor-core logs at too low a level (63 lines total, nothing since 00:00Z) to expose the code-exchange/onboarding failure. So the exact root cause is UNDETERMINED from outside — needs harbor-core debug logging (or a #5354 roll to rule out any residual gateway factor). NOT a confirmed separate defect; NOT confirmed #5341. Stays ◑, root-cause TBD. ‖ [harbor-OIDC session defect now TRACKED as #5362 — flow correct, no session, root cause needs harbor-core debug logs] ‖ hw288-2026-07-25 (4th walk, DEEPER — harbor SSO is actually HEALTHY, refuting my #5362 filing): the harbor-sso-configure Deployment (1/1, 2d1h) continuously PUTs harbor's OIDC config and logs 'OIDC config converged (auth_mode=oidc_auth, HTTP 200)' every 60s — 2816 successes / 0 errors. DB config correct (auto_onboard=true, verify_cert, endpoint/claims), owner user already onboarded (harbor_user). So the code+secret+config are all in sync → harbor SSO works for genuine human logins. The 401 I saw was via Playwright programmatic nav (2/2) = a client-side state-cookie artifact (guacamole-#5358-class), NOT a harbor defect. #5362 downgraded to likely-not-a-defect (needs a human login to close). Row 32's ONLY real blocker is the #5341 front-door flake (harbor.hw288 intermittent envoy-404), delivery-gated on #5354/#5265. ‖ |
| 33 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | OpenBao bare UI → lands in an authenticated Vault session (Secrets engines / dashboard), NO token-entry form (an in-transit "Signing in…" shim is allowed). | hw290 | ⚠️ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ GET https://bao.hw290.omani.works/ → 200, final /ui/vault/secrets, NO token-entry form (0 token inputs) and no unseal prompt — the OIDC leg lands ‖ BUT the authenticated content is denied: page body reads 'Not authorized — Ember Data Request GET /v1/sys/internal/ui/mounts returned a 403 … permission denied'; browser also gets 403 on /v1/sys/internal/ui/resultant-acl, /v1/sys/internal/ui/namespaces, /v1/sys/capabilities-self ‖ Secrets-engines list never renders ‖ openbao-sso-configure logs show 'sso-operator-admin policy written' and 'external group sovereign-admins → sso-operator-admin mapped', so the OIDC token is not picking up the mapped policy ‖ shots/openbao.png + shots/f3-openbao.png ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ OpenBao SSO PROVEN via the CANONICAL URL (harbor-pattern alternate path). The openbao.hw288 vanity alias is #5341 envoy-404 (0/8), but bao.hw288.omani.works (OpenBao's real URL) is 8/8 up: bao.hw288/sso/landing → OIDC (active Keycloak session) established an authenticated Vault session; GET /v1/sys/internal/ui/mounts → 200; bare bao.hw288/ → /ui/vault/secrets rendering the Secrets Engines list (cubbyhole/ …), NO 'Not authorized', session persists on re-entry. Screenshot: docs/sessions/2026-07-25/screenshots/uat-row33-openbao-authenticated-vault.png. CAVEAT: openbao.hw288 vanity alias stays #5341-flaky (fix #5354 delivery-gated) — alias-routing, not an SSO defect; OpenBao SSO + authenticated Vault session work. ‖ UNVERIFIED (flipped 2026-07-25 by 55176fe5) — was: hw288-2026-07-24: Playwright — BARE openbao URL bao.hw288.omani.works → /ui/vault/secrets authenticated Vault session, Secrets Engines listed (cubbyhole/ + secret/ kv), OpenBao 2.3.1, NO token prompt (silent-SSO) ‖ hw288-2026-07-23: GET /v1/sys/health → initialized:true, sealed:false, standby:false, version:2.3.1; bare / → 1 redirect to /sso/landing shim (HTTP 200, allowed in-transit shim). openbao-sso-oidc-credentials ES SecretSynced=True. Authed vault-  hw288-2026-07-25: front door DOWN — openbao.hw288 12/12 envoy-404 (the #5341 shared-gateway wildcard-SNI collision; openbao/powerdns resolve 100% to the console chain lacking their route). Fix #5354 (bp-sovereign-tls-vars@0.1.1) merged+published but UNDELIVERABLE to post-cutover hw288 (frozen catalog #5265). Not walkable until the #5354 chart rolls (needs the #5265 Day-2 refresh or a fresh prov). ‖ hw288-2026-07-25 LIVE curl (#5341): openbao.hw288 404 response header = 'server: envoy' (edge-level, NOT app-404) confirming the shared-gateway wildcard-SNI collision; mcp.hw288/mcp reproduces the flap 7/12 envoy-404 vs 5/12 app-405; console.hw288 0/6 (console gateway clean). Fix #5354 delivery-gated on #5265. ‖ [browser walk 2026-07-25: navigate openbao.hw288 → ERR_HTTP_RESPONSE_CODE_FAILURE — the envoy-404 has no renderable page, so the front door is unreachable, corroborating the 12/12 curl 404.] ‖ hw288-2026-07-25 re-walk: openbao.hw288 still 10/10 envoy-404 (front door down; #5341 gate holds, #5354 delivery-gated on #5265). ‖ hw288-2026-07-25T15:23Z snapshot: openbao.hw288 8/8 envoy-404 (front door down). Full 12-surface snapshot in docs/sessions/2026-07-25/status-uat-walk-log.md. ‖ |
| 34 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Keycloak admin console for the **sovereign** realm → lands inside the admin console (realm overview / Users / Clients), no master-realm login. | hw290 | ✅ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ GET https://auth.hw290.omani.works/admin/sovereign/console/ → 200 lands INSIDE the admin console for the sovereign realm, no master-realm login form ‖ header identity emrah.baysal@openova.io, realm selector 'Sovereign / Current realm', Manage nav (Clients, Client scopes, Realm roles, Users, Groups, Sessions, Events) + Configure nav ‖ shot shots/keycloak-admin.png ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ NEW-LENS live walk via a REAL Keycloak PIN-login session (owner passwordless-PIN flow: POST /auth/pin/issue → IMAP PIN over mothership Stalwart read-only → /login/verify → catalyst_session + Keycloak session; whoami owner). Navigated https://auth.hw288.omani.works/admin/sovereign/console/ → 'Sign in to Sovereign' (NOT master-realm) → 'Catalyst PIN Sign-In' broker SSO'd in → LANDED INSIDE the Keycloak Administration Console: realm 'Sovereign', signed in emrah.baysal@openova.io, full Manage nav (Clients/Client-scopes/Realm-roles/Users/Groups/Sessions/Events) + Configure, 'Welcome to Sovereign' overview. No master-realm login. (Also corrects the openova-uat-bot e015062d2 spurious ☐ over-flip — #5353 didn't touch SSO.) ‖  UNVERIFIED (flipped 2026-07-25 by 55176fe5) — was: hw288-2026-07-24: Playwright — Keycloak sovereign-realm admin console /admin/sovereign/console/ → landed INSIDE console signed in as owner (emrah.baysal@openova.io), 'Welcome to Sovereign' realm overview, full Manage nav (Clients/Client scopes/Realm roles/Users/Groups/Sessions/Events) + Configure (Realm settings/Authentication/Identity providers/User federation), NO login form (silent-SSO; master-realm console correctly requires master creds separately) ‖ hw288-2026-07-23: auth.hw288/admin/sovereign/console/ → 200 title 'Keycloak Administration Console' (admin SPA shell, no 'Sign in'/loginForm markers in served HTML); root / → 302 to /admin/sovereign/console/. Realm fully queryable via master-a |
| 35 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Guacamole bare URL → lands on the Guacamole connections list, signed-in; no Tomcat 404, no `/guacamole/` login page. | hw290 | ❌ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ GET https://guacamole.hw290.omani.works/ → 302 /guacamole/ → KC (client_id=guacamole-gate, kc_idp_hint=catalyst-pin) → /oauth2/callback → FINAL https://guacamole.hw290.omani.works/guacamole/ renders a COMPLETELY BLANK page (DOM innerText = 412 whitespace chars, document.title empty, no connections list) — confirmed on first load AND after a full reload with 20s settle ‖ 9 browser console errors 'Failed to load resource: 403 (Forbidden)'; every static asset is 302'd back into the OAuth flow and its callback 403s, e.g. 403 /oauth2/callback?state=…%3A%2Fguacamole%2Fjquery.min.js, …%2Fapp.js, …%2Ftemplates.js, …%2Fguacamole-common-js%2Fall.min.js ‖ ROOT CAUSE: the gate session cookie is never persisted — after the full dance the jar holds ONLY _oidc_gate_guacamole_csrf, there is NO _oidc_gate_guacamole session cookie, so every asset request restarts auth and the concurrent flows collide on the shared CSRF cookie ‖ #5378 header-mode IS live and its nonce class IS fixed: deploy env HEADER_ENABLED=true, HTTP_AUTH_HEADER=X-Forwarded-Email, and guacamole pod logs show 0 'invalid/old nonce' lines ‖ shots/guacamole.png + shots/f-guacamole-reload.png ‖ hw288-2026-07-25: ◑ ROOT-CAUSED live (owner session + hw288 kubeconfig off mothership PVC → guacamole-server pod logs). Keycloak leg SUCCEEDS (valid id_token: aud=guacamole, email=emrah.baysal@openova.io, groups=[openova-users,sovereign-admins,sovereign-viewers]), but guacamole-auth-sso-openid 1.5.5 REJECTS it — pod log 'o.a.g.a.o.t.TokenValidationService - Rejected OpenID token with invalid/old nonce' timestamped to the exact walk (06:32:09). Catalyst OIDC config is CORRECT (verified pod env: issuer/redirect_uri/jwks/client_id all valid; webapp.replicas=1 so not a multi-replica split). Intermittent nonce-race on the silent-SSO immediate-return path: 47 log lines/38h → 2 rejections vs 2 successes (~50% human; 4/4 for rapid programmatic nav). Filed #5358 (regression vs CLOSED #3374). Fix = guacamole openid-ext bump, delivery-gated on a fresh prov (#5265). ‖ hw288-2026-07-25 (clean-login retest, FRESH Keycloak session, front door 8/8 up): guacamole STILL fails — single clean navigation → valid id_token minted (aud=guacamole, emrah.baysal@openova.io, groups sovereign-admins) but the guacamole SPA renders 'An error has occurred' (no connections list, not signed-in). CONTRAST vs harbor: harbor's '401' was a Playwright rapid-nav artifact that a careful human login RESOLVED (#5362 closed, row 32 ✅) — but guacamole fails even on a careful single-shot with a fresh session, so #5358 is a GENUINE defect, not an automation artifact. Failure has shifted client-side (no new server nonce-rejection this attempt; the Angular token-processing/api-tokens leg errors). #5358 stands; row 35 genuinely blocked until the guacamole openid-ext fix delivers (fresh prov). ‖ | ‖ hw288-2026-07-26 #5358 REPRODUCED live (fresh session): guacamole bare URL → SSO round-trip to Keycloak COMPLETES (valid id_token issued: user emrah.baysal@openova.io, groups sovereign-admins, nonce present) → but guacamole rejects the callback and renders 'ERROR — An error has occurred and this action cannot be completed' instead of the connections list. Confirms the nonce-race is at guacamole's OIDC callback handling (token is valid; guacamole errors). Screenshot docs/sessions/2026-07-25/screenshots/row35-guacamole-5358-sso-error.png.
| 36 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | PowerDNS-Admin bare URL → lands on the dashboard signed-in; no redirect loop, no OAuth error, no `Log In` page. | hw290 | ✅ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ GET https://pdns-admin.hw290.omani.works/ → 200, final /dashboard/, title 'Dashboard - PowerDNS-Admin', signed in as emrah.baysal@openova.io (shown twice in the header with 'Edit Profile ‖ Logout') ‖ no redirect loop, no OAuth error, no Log In page ‖ Administration nav renders (Global Search, Activity, Server Statistics, Server Configuration, Zone Templates, Accounts, Users, API Keys, Settings) ‖ shot shots/pdns-admin.png ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ PowerDNS-Admin SSO PROVEN via the CANONICAL URL (harbor/openbao-pattern alternate path). The powerdns-admin.hw288 vanity alias is #5341 envoy-404 (0/6), but pdns-admin.hw288.omani.works (canonical, 6/6 up): bare URL → /dashboard/ silently signed-in (active Keycloak session), page 'Dashboard - PowerDNS-Admin' shows emrah.baysal@openova.io + Edit-Profile/Logout + Zone Management + Create Zone + Server Statistics — NO login form, NO redirect loop, NO OAuth error (the exact row acceptance). Screenshot: docs/sessions/2026-07-25/screenshots/uat-row36-powerdns-admin-dashboard.png. CAVEAT: powerdns-admin.hw288 vanity alias stays #5341-flaky (fix #5354 delivery-gated) — alias-routing, not an SSO defect. ‖ UNVERIFIED (flipped 2026-07-25 by 55176fe5) — was: hw288-2026-07-24: PowerDNS-Admin bare URL live walk + screenshot (uat-36-pdns-admin-sso-dashboard.png): https://pdns-admin.hw288.omani.works/ → 302 → /dashboard/ signed-in as emrah.baysal@openova.io (Admin), title 'Dashboard - PowerDNS-Admin', NO login form, NO 500, NO OAuth error, NO redirect loop; full nav (Zone Management/Create Zone/Administration). Validates #5334 fix live. ‖hw288-2026-07-23: Bare / → 3 redirects to sovereign broker/catalyst-pin/login?client_id=powerdns-admin (callback /oauth2/callback → /oidc/login) → HTTP 400 (no KC session). oidc-gate-powerdns-admin pod Running; pda-sso-oidc-credentials ES Secr  hw288-2026-07-25: front door DOWN — powerdns-admin.hw288 12/12 envoy-404, identical #5341 shared-gateway collision as row 33. Fix #5354 merged+published, delivery-gated on #5265. Not walkable until the chart rolls. ‖ hw288-2026-07-25 re-walk: powerdns-admin.hw288 still 10/10 envoy-404 (same #5341 gate as row 33). ‖ hw288-2026-07-25T15:23Z snapshot: powerdns-admin.hw288 8/8 envoy-404 (same #5341 gate). ‖ |
| 37 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) [#3858](https://github.com/openova-io/openova/issues/3858) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi bare URL (1st hit) → lands on `/console` signed-in as admin (role 100); no "Unknown OAuth provider", no login page. | hw290 | ⚠️ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ NOT DETERMINISTIC. First observed bare-URL hit: GET https://newapi.hw290.omani.works/ → 302 KC (client_id=newapi-admin, kc_idp_hint=catalyst-pin) → FINAL /login?expired=true rendering 'Log In / Continue with OpenOva SSO / or / Sign in with Email or Username' — i.e. a login page, and /api/user/self 401 ‖ shot shots/newapi-1.png ‖ Repeat in a fresh browser context with the same owner session: FIRST hit landed signed-in on /console/token ‖ shot shots/f-newapi-freshctx-hit1.png ‖ so the 1st-hit landing is flaky rather than uniformly broken; no 'Unknown OAuth provider' was ever seen ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ live walk via the persistent Keycloak session (owner emrah.baysal@openova.io). newapi.hw288.omani.works/ 1st-hit reachable 10/10 →200 → redirected to /console/token, landed SIGNED-IN as user 'sovereign_2' (the 'sovereign' OIDC provider recognized), 'Login successful!' toast, full Token-Management console — no Tomcat/login page. Front door clean this measurement; #5341 shared-gateway flake fix PR #5354 published, pending post-cutover roll. ‖ hw288-2026-07-25 fresh-session re-test: with a freshly-minted console session, newapi /console STILL 302→/login?expired=true — so it's NOT a stale-console-session artifact; newapi genuinely does not silent-SSO auto-land (requires interactive login / doesn't silent-refresh). Confirmed ◑ (real newapi behavior, not test noise). ‖hw288-2026-07-24 re-test: newapi /console still 302→/login?expired=true (does NOT silently refresh an expired session, unlike PowerDNS/Hubble which auto-land). SSO provider registered (no 'Unknown OAuth provider'). Cannot cleanly test the fresh 1st-hit auto-land without clearing cookies (would drop the authed console session). Stays ◑ — stale-session confound; a real silent-refresh gap is possible but not isolable here. ‖hw288-2026-07-24: newapi bare URL live walk — https://newapi.hw288.omani.works/ → /login?expired=true (a STALE-session state in a long-open browser, NOT a clean 1st-hit). Positives: NO 'Unknown OAuth provider' error + 'Continue with OpenOva SSO' button present (provider correctly registered). Silent-SSO auto-landing NOT demonstrable from an expired-session browser — stays ◑ (needs a genuine fresh-browser 1st-hit). ‖hw288-2026-07-23: GET /api/status → 200, setup:true, custom_oauth_providers=[{name:'OpenOva SSO', slug:sovereign, client_id:newapi-admin, authorization_endpoint: auth.hw288/realms/sovereign/...?kc_idp_hint=catalyst-pin, scopes:'openid profile |
| 38 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) [#3858](https://github.com/openova-io/openova/issues/3858) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi bare URL (2nd hit, re-entry) → lands on `/console` again signed-in; NOT an "already bound" / re-link error / `/setup` wizard. | hw290 | ✅ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ 2nd bare-URL hit https://newapi.hw290.omani.works/ → lands signed-in on /console/token ‖ authenticated console chrome renders: user 'sovereign_2' in the sidebar with CHAT (Playground, Chat), CONSOLE (Dashboard, Token Management, Usage Logs, Drawing/Task Logs), PERSONAL CENTER (Wallet Management, Personal Settings) and a working Token Management grid ‖ NOT an 'already bound' / re-link error and NOT the /setup wizard ‖ shot shots/newapi-2.png ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ live walk (persistent Keycloak session). newapi 2nd-hit / re-entry again redirected bare URL → /console/token, still SIGNED-IN as 'sovereign_2' with NO re-auth prompt (session recognized) — the re-entry acceptance. ‖ hw288-2026-07-24 re-test: newapi /console still 302→/login?expired=true (does NOT silently refresh an expired session, unlike PowerDNS/Hubble which auto-land). SSO provider registered (no 'Unknown OAuth provider'). Cannot cleanly test the fresh 1st-hit auto-land without clearing cookies (would drop the authed console session). Stays ◑ — stale-session confound; a real silent-refresh gap is possible but not isolable here. ‖hw288-2026-07-23: Config-real proven (see row 37: OpenOva SSO provider registered, setup:true, no /setup wizard forced). App re-entry / 2nd-hit session behavior not exercised (needs browser) — same limitation as hw287 (which observed expired-s |
| 39 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Hubble bare URL → lands on the Hubble UI, authenticated (not an anonymous/unauth view, no login page). | hw290 | ✅ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ GET https://hubble.hw290.omani.works/ → 302 KC (client_id=hubble-ui, kc_idp_hint=catalyst-pin) → /oauth2/callback → FINAL https://hubble.hw290.omani.works/ HTTP 200, title 'Hubble UI', authenticated view 'Welcome! To begin select one of the namespaces / Choose namespace' ‖ zero 401/403 responses across the load; no login page ‖ shot shots/hubble.png ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ live walk via the persistent Keycloak PIN-login session (owner emrah.baysal@openova.io) — corrects the openova-uat-bot e015062d2 spurious ☐ over-flip (#5353 didn't touch SSO). Front door reachable this measurement (0/12 404); the #5341 shared-gateway flake is intermittent+non-deterministic (harbor/openbao/powerdns currently 404), fix PR #5354 merged+published pending the post-cutover roll. hubble.hw288.omani.works/ → 'Hubble UI' authenticated view ('Welcome! To begin select one of the namespaces' + namespace selector), NOT an anonymous/unauth view, no login page. ‖  UNVERIFIED (flipped 2026-07-25 by 55176fe5) — was: hw288-2026-07-24: Hubble bare URL live walk + screenshot (uat-39-hubble-ui.png): https://hubble.hw288.omani.works/ loads the Hubble UI directly (title 'Hubble UI', NO login form, no redirect) showing the authenticated landing 'Welcome! To begin select one of the namespaces:' — not an anonymous/unauth view. ‖hw288-2026-07-23: Bare / → 2 redirects to sovereign broker/catalyst-pin/login?client_id=hubble-ui (oauth2-proxy /oauth2/callback) → HTTP 400 (no KC session). hubble-ui-oauth2-proxy-oidc ES SecretSynced=True. OIDC-wired; authed landing needs br |
| 40 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Marketplace bare URL → renders the anonymous storefront (public, by design); confirm no spurious login UI is forced. | hw290 | ✅ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ GET https://marketplace.hw290.omani.works/ → 200, title 'Build Your Organization — OpenOva', anonymous storefront renders the 6-step funnel (1 Plan · 2 Stack · 3 Add-ons · 4 Topology · 5 Review · 6 Checkout) with 'Get Started' ‖ no forced login: 0 password inputs, no redirect to KC; only a passive 'Sign in' link is offered ‖ shot shots/marketplace.png ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ RE-AFFIRMED (spurious e015062d2 over-flip; #5353 didn't touch SSO). Fresh check: marketplace.hw288.omani.works/ → HTTP 200 anonymous storefront (public storefront renders, marketplace token present), no spurious login UI forced (console gateway, 0 flake). ‖  UNVERIFIED (flipped 2026-07-25 by 55176fe5) — was: hw288-2026-07-23: hw288 adversarial re-walk CONFIRMS claim (not refuted). GET https://marketplace.hw288.omani.works/ → HTTP 200, num_redirects=0, 25121 bytes real text/html, server=envoy, effective URL unchanged (no redirect). Title = '<title> |
| 41 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Keycloak sovereign realm → Users lists the single owner principal the owner (enabled). | hw290 | ✅ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ Keycloak sovereign realm → Users → user list shows exactly one principal, counter '1 - 1', row 'emrah.baysal@openova.io / emrah.baysal@openova.io' ‖ user detail header shows 'emrah.baysal@openova.io — Enabled' ‖ shots/f-kc-users.png + shots/f3-kc-userGroups.png ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ RE-AFFIRMED via Keycloak realm backing (openova-uat-bot e015062d2 spuriously flipped this ☐ after the #5353 OOM merge — not an SSO-chain change). keycloak-sovereign-realm-config + live /realms/sovereign OIDC discovery (issuer https://auth.hw288.omani.works/realms/sovereign): realm 'sovereign' enabled; user emrah.baysal@openova.io enabled=True — the single owner LOGIN principal (only other entry is service-account-catalyst-api-server, a non-login SA). ‖  UNVERIFIED (flipped 2026-07-25 by 55176fe5) — was: hw288-2026-07-23: ADVERSARIAL RE-WALK CONFIRMED on LIVE hw288 (region-a, keycloak-0 pod, started 2026-07-23T14:00:24Z, 70m old; master-admin token from secret keycloak/catalyst-kc-master-admin-credentials). GET /admin/realms/sovereign → HTTP 2 |
| 42 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Owner user → Groups tab shows membership in `/sovereign-admins` (alongside `/openova-users`) — the source of admin authority. | hw290 | ✅ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ Keycloak → Users → emrah.baysal@openova.io (id eda435ab-e0c7-483e-8b06-39002d2c1a62) → Groups tab, direct membership '1 - 3': openova-users (/openova-users), sovereign-admins (/sovereign-admins), sovereign-viewers (/sovereign-viewers) ‖ membership in /sovereign-admins alongside /openova-users confirmed ‖ shot shots/f3-kc-userGroups.png ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ RE-AFFIRMED via Keycloak realm backing (openova-uat-bot e015062d2 spuriously flipped this ☐ after the #5353 OOM merge — not an SSO-chain change). owner emrah.baysal@openova.io groups = ['/sovereign-admins','/sovereign-viewers'] — membership in /sovereign-admins (the source of admin authority) CONFIRMED. NOTE: the 2nd group on hw288 is /sovereign-viewers, not the row's stale '/openova-users' wording (env drift). ‖  UNVERIFIED (flipped 2026-07-25 by 55176fe5) — was: hw288-2026-07-23: ADVERSARIAL RE-WALK CONFIRMS on live hw288 (region-A KC admin REST, master-realm admin token minted fresh inside keycloak-0 pod via admin-cli, NOT indirect whoami). GET /admin/realms/sovereign/users → 1 user id=cc5c5b95-55b2- |
| 43 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Owner user → Role mapping tab: effective realm roles include `catalyst-admin` (not only default-roles/uma/o… | hw290 | ✅ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ effective realm roles carried by the issued token: /api/v1/whoami on the PIN session → realm_access.roles [catalyst-admin, catalyst-owner]; on the silent re-auth session → [catalyst-viewer, catalyst-admin, catalyst-operator, …] — i.e. NOT only default-roles/uma ‖ Keycloak user Role mapping tab renders the effective/inherited set (account manage-account/view-profile, realm-management create-client/impersonation/manage-authorization/manage-clients/manage-events/manage-identity-providers/manage-realm all Inherited=True) ‖ shot shots/f3-kc-userRoleMapping.png ‖ CAVEAT: the KC role-mapping table paginates 1-10 and catalyst-admin falls past page 1, so the authoritative proof here is the token realm_access plus the group role-mapping in row 44 ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ RE-AFFIRMED via Keycloak realm backing (openova-uat-bot e015062d2 spuriously flipped this ☐ after the #5353 OOM merge — not an SSO-chain change). owner's effective realm roles include catalyst-admin via the group: config group sovereign-admins → realmRoles ['catalyst-admin'], owner ∈ /sovereign-admins → one source (the group), the grant. ‖  UNVERIFIED (flipped 2026-07-25 by 55176fe5) — was: hw288-2026-07-23: ADVERSARIAL RE-WALK, hw288 (dep 027f07559af1f9f7) 2026-07-23 — claim CONFIRMED, refutation FAILED. Authoritative Keycloak-level proof (KC master-admin REST on region-a keycloak-0), not a 200-on-empty or console echo. Owner us |
| 44 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Groups → `/sovereign-admins` → Role mapping: group confers `catalyst-admin` (console) and realm-management `realm-admin` (KC console) — one source, both grants. | hw290 | ✅ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ Keycloak → Groups → sovereign-admins → Role mapping ('1 - 2', Hide inherited roles on): 'realm-management realm-admin — Inherited False — role_realm-admin' AND 'catalyst-admin — Inherited False — Catalyst console: admin tier (BSS + RBAC nav)' ‖ one group confers both the console admin tier and the Keycloak-console realm-admin ‖ shot shots/f-kc-group-rolemapping.png ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ RE-AFFIRMED via Keycloak realm backing (openova-uat-bot e015062d2 spuriously flipped this ☐ after the #5353 OOM merge — not an SSO-chain change). group /sovereign-admins → realmRoles ['catalyst-admin'] CONFIRMED in the realm config (console admin authority). NOTE: the realm-management 'realm-admin' KC-console grant is a client-role mapping (not a realm role) — proven in the prior browser walk; this realm-role config view shows catalyst-admin. ‖  UNVERIFIED (flipped 2026-07-25 by 55176fe5) — was: hw288-2026-07-23: CONFIRMED, not refuted. Walked LIVE on hw288 (region-a keycloak-0, shared KC DB), KC master-admin REST. Group /sovereign-admins live id=be6f78bc-1e6c-401c-a134-0b1052f13861 (matches claim exactly; NOT the stale hw287 id 5d04a |
| 45 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Console Users panel: owner row + the ability to view/manage users renders — proving console admin nav is driven by the realm principal, not a self-signed constant. | hw290 | ✅ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ Console → Users panel renders signed-in with the owner principal driven by the realm identity (whoami tier=owner, realm_access catalyst-admin) not a self-signed local admin ‖ User Access table lists both owner-principal rows, exposes tier filter chips (viewer/developer/operator/admin/owner), a '+ New' create control and per-row Delete — i.e. view AND manage ‖ shot shots/console-users.png ‖ UNVERIFIED (flipped 2026-07-26 by b3c9202e) — was: hw288-2026-07-25: ✅ RE-AFFIRMED (openova-uat-bot e015062d2 spuriously flipped this ☐ after the #5353 OOM merge — not an SSO-chain change). Fresh owner-session walk of the SAME surface as row 28: console /users ('User Access') renders the owner rows (hw288-omani-works-emrah-baysal-at-openova-io + useraccess-owner-emrah-baysal-at-openova-io, emrah.baysal@openova.io, admin grant + 1 binding each) AND the view/manage affordances (tiers viewer/developer/operator/admin/owner, '+ New' → /provision/027f0755…/users/new) — console admin nav driven by the realm principal, not a self-signed constant. ‖  UNVERIFIED (flipped 2026-07-25 by 55176fe5) — was: hw288-2026-07-24: Users-panel UI render WALKED directly (/users): 'User Access' page renders owner row useraccess-owner-emrah-baysal-at-openova-io (USER emrah.baysal@openova.io = realm principal, GRANTS owner, 1 binding) + hw288-omani-works admin-grant row; view/manage controls live (+New, per-row Delete, tier selector viewer/developer/operator/admin/owner). Console admin nav driven by realm principal, not a self-signed constant. ‖hw288-2026-07-23: Realm-principal backing proven: sovereign realm 1 owner user (row 41) + 2 Active owner UserAccess CRs. Console Users-panel API endpoint not located (candidate /api/v1 paths all 404) — UI render not directly walked. |
| 46 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `bp-postgres` catalog detail renders; click **New instance** → the create dialog opens with a topology `<select>`. | — | ☐ | — |
| 47 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) [#3856](https://github.com/openova-io/openova/issues/3856) | Topology `<select>` options read exactly the ONE canonical vocabulary: `singleton`, `active-passive`, `active-hot-standby`, `active-active` (no `single-region`/`act…). | — | ☐ | — |
| 48 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `active-passive` is a selectable option in the create `<select>` (not folded away). | — | ☐ | — |
| 49 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `singleton` is a separate selectable option (single-region single-instance), distinct from the multi-region modes. | — | ☐ | — |
| 50 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Pick `active-hot-standby`, name the instance, Provision → the create succeeds (toast/redirect to the new app card), NOT a red `topology not in supported [...]` error. | hw290 | ✅ | hw290-2026-07-26: hw290 2026-07-26T14:45Z, own chromium (headless, owner session emrah.baysal@openova.io, /api/v1/whoami 200 tier=owner). /catalog/bp-postgres → '+ New instance' → dialog-new-instance: name=uat50-ahs, org=acme-corp (Environment: acme-corp-prod derived), topology select-instance-topology='active-hot-standby' (option ENABLED, labelled 'cnpg-pair · sync replication; bp-continuum switchover (RTO 10s / RPO 0s)'; active-active + active-passive were the [disabled] 'not supported by this blueprint' ones), BOTH region checkboxes ticked (region-checkbox-me-east-215=on, region-checkbox-me-east-215-b-1=on) → 'Create instance'. RESULT: dialog closed, dialog-error=null, NO red 'topology not in supported [...]' error. New instance row rendered on the class page: sov-instance-row-uat50-ahs = 'uat50-ahs ‖ acme-corp ‖ active-hot-standby ‖ Pending'. Live CR: kubectl -n acme-corp get application uat50-ahs → PLACEMENT {"mode":"active-hot-standby","regions":["me-east-215","me-east-215-b-1"]}, phase Provisioning→Ready, HR acme-corp/uat50-ahs-rtz-a Ready=True ('Helm install succeeded ... chart bp-postgres@0.2.13'). Screenshots w290/11-50-form.png, w290/11-50-after-create.png, w290/11-50-instances.png. ‖ hw288-2026-07-24: Provision-create MECHANISM proven end-to-end via the singleton walk (row 59 ✅: +New instance→Create→Application CR Ready). Row 50 specifically asks active-hot-standby provision (2-region cnpg-pair) — not fired separately to avoid a heavier 2-region create; the create-succeeds path is the same code, proven for singleton ‖ hw288-2026-07-24: topology agent — active-hot-standby is an ENABLED valid create option (cnpg-pair·sync·bp-continuum RTO10s/RPO0s), in postgres supported set (would NOT throw 'topology not supported'); live backing proven (shared-pg primary region-a + region-b streaming, Continuum Healthy). Provision POST is a mutation — not fired read-only ‖ hw285-2026-07-23: Provision (POST create mutation) forbidden for read-only walker. active-hot-standby mode proven real: shared-pg cnpg-pair primary 3/3 region-a + shared-pg-replica 3/3 region-b Healthy, Topology tab renders active-hot-standby. Provision leg  ‖ hw278-2026-07-19: N/A read-only verify walker — criterion requires Provision (POST create mutation) forbidden. active-hot-standby backing real: dr-shared-pg cnpg-pair primary 3/3 region-a + replica 3/3 region-b Healthy. Provision leg not exercised. |
| 51 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab renders a per-region placement view listing region-a (active) and region-b (standby) as ONE placement, not two separate instances. | — | ☐ | — |
| 52 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab: the standby (region-b) copy is shown scaled-down / passive while region-a shows the active replica count (not identical hot counts). | — | ☐ | — |
| 53 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Open a per-app Topology tab and read its placement view (singleton apps note no per-region/standby surface). | — | ☐ | — |
| 54 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) [#3856](https://github.com/openova-io/openova/issues/3856) | `shared-pg` Topology tab declared-topology strip renders the canonical mode in ONE vocabulary — header dialect and picker dialect MATCH (no `singleton` header over an `active-hotstandby` chip). | — | ☐ | — |
| 55 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab shows the effective (live) topology next to the declared one — declared vs effective together, not a build-time constant. | — | ☐ | — |
| 56 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab per-region placement + replication block: region-a primary, region-b replica, and a live replication-lag in seconds (not a hardcoded `—`). | — | ☐ | — |
| 57 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab Switchover button is present and armed because a live 2-region cnpg-pair backs the app. | — | ☐ | — |
| 58 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | A **singleton** app (cilium) Topology tab: the DR section / Switchover button does NOT render (honestly hidden, not armed against a phantom region). | — | ☐ | — |
| 59 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Catalog New instance → pick `singleton` → Provision → that app's Topology tab shows single-region placement (no region-b standby, no Switchover). | — | ☐ | — |
| 60 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Catalog New instance → pick `active-hot-standby` → Provision → that app's Topology tab shows a 2-region pair (region-a primary + region-b replica + armed Switchover). | hw290 | ⚠️ | hw290-2026-07-26: RENDER LEG PASSES, RUNTIME PAIR ABSENT. /app/uat50-ahs → Topology tab (data-testid app-tab-topology) renders: 'Placement Pattern: active-hot-standby ‖ me-east-215 · mgmt ● PRIMARY serves writes ──▶ repl me-east-215-b-1 · mgmt ○ STANDBY · Hot live replica' (testids topology-tab-target-card-0/-1 + -role) and an ARMED switchover 'Switch over… promote standby me-east-215-b-1 — runs an RPO/health preflight before you confirm' (topology-tab-dr-switchover-open, enabled). BUT first-hand runtime check contradicts the pair: kubectl -n acme-corp get application uat50-ahs -o json → status.perCluster = [{"cluster":"rtz-A","hr":"uat50-ahs-rtz-a","namespace":"acme-corp","role":"singleton","status":"Ready"}] — ONE entry, role 'singleton'; region-b (hw290-b.kubeconfig) has NO acme-corp namespace at all and `kubectl -n acme-corp get hr` → 'No resources found'; `kubectl get continuum -A` → 'No resources found' cluster-wide; the tab's own DR panel chip reads '○ NOT LIVE'. The pre-existing agent-created twin uat-ahs-hw290 shows the identical shape (perCluster=1×rtz-A role singleton). Also the Ready condition message overstates: 'installed across 2 region(s); Ready=True from downstream HelmRelease(s)' with only 1 downstream HR. Screenshot w290/12-uat50-ahs-topology.png. ‖ hw285-2026-07-23: Provision mutation forbidden (verify-only). Mode proven real: /app/shared-pg Topology renders 2-region pair region-a PRIMARY + region-b STANDBY·Hot + armed 'Switch over…' (promote standby). Provision leg not fired. ‖ hw278-2026-07-19: N/A read-only render walker — needs Catalog New-instance→active-hot-standby→Provision (mutation); verify-only. Mode proven real: shared-pg topology RENDERED 2-region pair (region-a PRIMARY + region-b STANDBY·Hot + armed Switch over…). Provision leg not fired. |
| 61 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Apps grid shows newly-provisioned postgres instances as their own cards, each carrying a topology badge matching the mode picked at create time. | hw285 | ❌ | hw285-2026-07-23: REFUTED — claimed evidence is fabricated (describes UI badges that do not exist in the shipped console). (1) grep core/console/src for '⛓', 'contexts', 'active-passive' → ALL EMPTY. The only topology badge is DataInstances.svelte:60 `<span  |
| 62 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | On an app WITH a live pair, the Topology DR section shows the live Continuum status (Ready / lease holder / standby) from the live API, not a static badge. | — | ☐ | — |
| 63 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | grafana (declares hot-standby, no live DR backing) Topology DR section reads the honest "no live DR backing… Switchover unavailable" state with a disabled (not armed) Switchover button. | hw288 | N/A | hw288-2026-07-24 WAIVED→N/A: premise not reproducible on hw288 — grafana runs in BOTH regions (region-a 1 + region-b 1 pods), declared topology defaults to active-hot-standby (cnpg-pair replication + bp-continuum switchover) → it HAS live DR backing. The tested 'declares-hot-standby-but-UNBACKED → honest no-backing message' state cannot occur here (no hw288 app is hot-standby-without-backing). N/A for this env's config. ‖hw288-2026-07-24: topology agent — premise-mismatch on hw288: grafana is declared SINGLETON (Topology 'Pattern: singleton', no DR section — honest), NOT hot-standby. No app on hw288 is in the declares-hot-standby-but-unbacked state (all active-hot-standby apps have live cnpg-pair/continuum backing), so the specific disabled-switchover-message state is not reproducible here ‖ hw285-2026-07-23: On hw285 grafana does NOT match the row premise: /app/bp-grafana Topology renders 'Pattern: active-active' with 2 PRIMARY cards (both regions serve writes) because grafana runs live in both regions (region-b grafana 3/3 Running). Status hon |
| 64 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab replication-lag field shows a live numeric seconds value (or explicit "no replica"), never a hardcoded `—`. | — | ☐ | — |
| 65 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Cloud/regions view shows the true region count — a healthy 2-region prov reads `Cluster 2/2` with no phantom region-B bubble. | — | ☐ | — |
| 66 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Cloud→Clusters renders 2/2 HEALTHY clusters, one per region (me-east-215-a + me-east-215-b), no phantom region. | — | ☐ | — |
| 67 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | grafana status/overview reports Healthy/Running in both regions — no "cannot resolve write host" crashloop in the app health panel. | — | ☐ | — |
| 68 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | powerdns-admin status reports Healthy/Running — the CNPG-minted DB host resolved (no "could not translate host"). | — | ☐ | — |
| 69 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | keycloak status reports Healthy/Running in both regions — JGroups DB-host resolves, no UnknownHostException in the health panel. | — | ☐ | — |
| 70 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | guacamole status reports Healthy/Running in both regions — no missing-recordings-PVC error surfaced. | — | ☐ | — |
| 71 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Region-kill baseline (before): `shared-pg` Topology tab shows live Continuum Ready, lease held by region-a, region-b standby present, a live replication-lag number. | — | ☐ | — |
| 72 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Operator console BSS → Vouchers page renders the voucher issuance form (code, credit OMR, plan tier, description); no login wall. | — | ☐ | — |
| 73 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | In the voucher form, type a weak code `1234` → Issue → the form rejects it inline ("voucher code must be at least 12 characters"). | — | ☐ | — |
| 74 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Leave the code field empty → Issue → a new voucher row appears with a server-auto-generated high-entropy code, credit, plan tier, `unredeemed 0/1`. | — | ☐ | — |
| 75 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Stranger opens the redeem page with `?code=<CODE>` → sees "Voucher valid · 5000 OMR" with THIS Sovereign's brand chrome (no openova.io / mothership logo). | — | ☐ | — |
| 76 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Open the redeem page with a junk code → a generic "voucher not valid" message (no tombstone / no detail leak). | — | ☐ | — |
| 77 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Redeem page source (DevTools): the HTML shows THIS Sovereign's brand and no `console.openova.io` / `omantel.openova.io` / bare `openova.io` host literal. | — | ☐ | — |
| 78 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Click "Sign up to redeem" → the browser lands on the plan picker grid (`/plans`). | — | ☐ | — |
| 79 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Plans grid shows the tiers (S/M/L/XL/Flexi) with price/CPU/memory → pick plan M → advances to the app catalog (`/apps`). | — | ☐ | — |
| 80 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | App catalog grid (served from THIS Sovereign's catalog) → pick WordPress → advances to add-ons (`/addons`). | — | ☐ | — |
| 81 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Add-ons step shows optional add-ons → leave defaults → Continue → advances to the BCP topology step (`/bcp`). | — | ☐ | — |
| 82 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | BCP topology step shows BOTH Single-region and Active-hot-standby radios (the Pillar-2 BCP choice at signup) → select Active-hot-standby → Continue → review. | — | ☐ | — |
| 83 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Review summary shows the chosen plan (M), app (WordPress), topology (Active-hot-standby), the Org slug, and the pool-TLD → Proceed to checkout. | — | ☐ | — |
| 84 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | On checkout, enter the stranger's email → Send code → type the emailed sign-in code (no password) → the page shows the stranger signed in + Org confirmed. | hw290 | ✅ | hw290-2026-07-26: Own chromium, no session. marketplace.hw290.omani.works/checkout/ → typed `emrah.baysal+walktwo@openova.io` → `Send sign-in code` (POST /api/auth/magic-link 200) → page: `A 6-digit code was sent to emrah.baysal+walktwo@openova.io`. PIN read READ-ONLY over IMAP 45.151.123.50:993 with BODY.PEEK (never marked Seen). Typed the 6 digits (pressSequentially, auto-submit) → POST /api/auth/verify 200 → page shows avatar `emrah.baysal+walktwo E`, `Review and launch your Organization`, the email, and Org confirmed `Subdomain: walk-two.omani.rest`. No password field at any step. Screenshots w4-17-code-prompt.png / w4-18-signed-in.png. ‖ — |
| 85 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Checkout summary: the voucher credit is applied — "Credit covers this order — 0 OMR due" (no Stripe card form) → click Launch / Place order. | hw290 | ✅ | hw290-2026-07-26: Checkout summary after sign-in: `Plan · M OMR 9.000 ‖ Apps (3) Free ‖ + Active hot-standby (BCP) OMR 5.000 ‖ Total (monthly) OMR 14.000 ‖ Credit available -OMR 75.000 ‖ Due now OMR 0.000 ‖ ✓ Credit covers this order — no card charge needed.` then `Launch my Organization` clicked. No card/Stripe input fields present (visible inputs were only `Organization name` and the voucher-code box holding HW290WALKTWO); a VISA/Stripe informational blurb is rendered but collects nothing. Screenshot w4-18-signed-in.png. ‖ — |
| 86 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Provisioning progress timeline advances to Done (Creating Org → Committing manifests → Provisioning vCluster → Deploying WordPress → TLS → Health), no hang/red step. | hw290 | ❌ | hw290-2026-07-26: Reproduced TWICE on hw290 (my walk-two walk 14:24Z and the earlier acme-corp walk 13:2xZ) — the provisioning timeline goes RED, never Done. walk-two: `Provisioning didn't finish — Something went wrong at the Deploying WordPress stage`; steps Creating tenant ✓ → Committing manifests to Git ✓ → Provisioning vCluster ✓ → **Deploying WordPress ✗**: `git commit to per-Org repo failed: commit to per-Org repo walk-two/catalyst-tenant: … POST …/api/v1/repos/walk-two/catalyst-tenant/contents: 500 {"message":"PushOutOfDate Error … ! [rejected] a480a0e7… -> main (non-fast-forward)"}`. acme-corp: identical failure at `Deploying OpenClaw` with the same PushOutOfDate on acme-corp/catalyst-tenant. Held 40× 15s polls (10 min) — terminal, offers only `Try again` / `Back to checkout`. Screenshots w4-tl-*.png. DEFECT: the per-Org GitOps writer does not pull/rebase before pushing, so the 2nd+ app-manifest commit races itself. ‖ — |
| 87 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | After Launch, the marketplace redirects to the per-Org console — URL becomes the per-Org console and loads with publicly-trusted TLS (no cert error / connection-refused). | — | ☐ | — |
| 88 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Per-Org console landing: the stranger is signed in zero-click as the Org owner (their email in the avatar, their Org in the header) — no login/PIN form. | — | ☐ | — |
| 89 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Per-Org console → Applications view shows the purchased WordPress app card Running/Healthy → click Open. | — | ☐ | — |
| 90 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Terminal acceptance: the purchased WordPress app SERVES at its own FQDN the app FQDN — the live rendered WordPress site (not 404/502/cert error). | — | ☐ | — |
| 91 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | While signed in, re-open the marketplace root → the returning-user redirect sends the customer to their own Org console, never to `console.openova.io` / the mothership. | hw290 | ✅ | hw290-2026-07-26: Signed-in customer session (w4-state.json) opened marketplace root https://marketplace.hw290.omani.works/ (HTTP 200) → auto-redirected to `https://marketplace.hw290.omani.works/launching/?host=https%3A%2F%2Fconsole.walk-two.omani.rest&token=…&tenant=12c4979d-…`. The redirect target host is the customer's OWN Org console `console.walk-two.omani.rest` — never console.openova.io / the mothership (nav chain captured, only 2 entries). localStorage org-active-console-host=console.walk-two.omani.rest. NOTE: it lands on the Org's provisioning-status page rather than the console itself only because this Org's provisioning is in the row-86 failed state. ‖ — |
| 92 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | As the signed-in customer, rapidly re-submit checkout/redeem >5× in a few seconds → after ~5 attempts a rate-limit notice appears; a single legitimate redeem is unaffected. | hw290 | ✅ | hw290-2026-07-26: Signed-in customer, 8 back-to-back POSTs to the redeem endpoint `/api/billing/vouchers/redeem-preview` {code:HW290WALKTWO} inside one page eval (sub-second): #1-#5 → 200 with the voucher body; #6/#7/#8 → **429 `{"error":"too many redeem attempts — please wait a few seconds and try again","retry_after":10}`**. A single legitimate redeem was unaffected — the same voucher redeemed cleanly minutes earlier in the funnel walk (times_redeemed=1 on HW290FUNNELACME, HW290WALKTWO consumed by walk-two). Sub-gap (not this row): `/api/auth/magic-link` is NOT rate-limited — 8/8 returned 200. ‖ hw286-2026-07-23: partial — returning-user-redirect + >5x rate-limit-notice + 2nd-customer-Org generality re-walk not exercised this pass (single money-path walk); core E2E proven (see 72-90). |
| 93 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Generality: mint a 2nd voucher and re-walk B.1→B.4 with a different slug (`walk-stranger-two`) and a different pool-TLD (`omani.rest`) → a 2nd Organization provisions and lands signed-in. | hw290 | ⚠️ | hw290-2026-07-26: 2nd voucher minted first-hand in the BSS menu (`HW290WALKTWO`, 75 OMR, Active — w3-00/01/02 screenshots + /api/v1/org/billing/vouchers/list 200) and re-walked B.1→B.4 with a DIFFERENT slug (`walk-two`) and a DIFFERENT pool-TLD (`omani.rest`, chosen from the live /api/v1/sovereign/parent-domains org-pool). Result: a 2nd Organization DID provision and the customer DID land signed-in — Organization/walk-two `CUSTOMER ‖ Org ‖ REAL ‖ vcluster ‖ ACTIVE`, spec.tenantPublic={parentDomain:omani.rest, subdomain:walk-two}, status.vcluster.phase=Ready, vcluster-0 1/1 Running, console.walk-two.omani.rest 200. BUT the provisioning timeline red-failed at the app stage (row 86 PushOutOfDate), so the walk is not end-to-end green. Partial, not pass. ‖ hw286-2026-07-23: partial — returning-user-redirect + >5x rate-limit-notice + 2nd-customer-Org generality re-walk not exercised this pass (single money-path walk); core E2E proven (see 72-90). |
| 94 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The 2nd Org's console lands signed-in on a different TLD (the 2nd-Org console) — identical zero-click contract, no special-casing. | hw278 | ⛔ | hw278-2026-07-20: 2nd-Org console — depends on row-93 second Org (outside the one-Org sanction). The zero-click landing contract itself was proven on the first Org (console.acme275.omani.rest → 200 trusted TLS). |
| 95 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The 2nd Org's purchased app serves at its own different-TLD FQDN (the 2nd-Org app) — two Orgs, two TLDs, two running apps, identical mechanism. | hw278 | ⛔ | hw278-2026-07-20: 2nd-Org app — depends on the row-93/94 second Org (outside the one-Org sanction). The first Org's purchased WordPress itself deploys+serves (row 226/90), so the mechanism is proven on one Org. |
| 96 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Handover URL → lands on `/dashboard` already signed-in as the owner (avatar E), no login form. | — | ☐ | — |
| 97 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Dashboard renders the cluster treemap and the LAYER 1 / LAYER 2 grouping comboboxes are visible. | — | ☐ | — |
| 98 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Click LAYER 1 combobox → select `vCluster`; the treemap regroups into one labelled block per vCluster (`host` / `mgmt` / `rtz` / `dmz`), mgmt block visible + clickable. | hw236 | ⛔ | RE-BASELINED ☐→⛔ (superseded, NOT a product fault): #4325 de-vcluster removed the mgmt/rtz/dmz vClusters (plane-isolation moved to per-component host-ns CNP), so a `vCluster`-grouped treemap no longer has `mgmt`/`rtz`/`dmz` blocks — the hw228 walk confirmed `group_by=vcluster` returns a single `host` block with all 7 named apps as children. This row asserts the DEAD mgmt-vcluster grouping model; consistent with rows 99-107 (already ⛔ for the same reason). Refs #4325 #3642. ‖ hw255-catalog-edit-2026-07-15: re-confirmed on hw255 (read-only): NO mgmt/rtz/dmz vClusters exist (kubectl ns grep → none; de-vcluster #4325 holds), current /cloud is the Lens-based graph (cloud/runtime/…/catalyst selector, no LAYER combobox) (row98-108-cloudview-lens-no-layer-combobox.png) — the row's mgmt-vcluster grouping model stays DEAD; ⛔ re-baseline stands. |
| 99 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **grafana** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; mgmt/host placement re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 100 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **harbor** tile sits inside the **mgmt** block, not **host**. | hw268 | ⛔ | hw268-2026-07-17: superseded by #4325 de-vcluster (mgmt-vcluster placement model dead) — not a product fault; not walked (screenshot(s) docs/sessions/2026-07-17/hw268-authed-console/). ‖ RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 101 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **keycloak** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 102 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **gitea** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 103 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **openbao** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 104 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) [#3831](https://github.com/openova-io/openova/issues/3831) | On the LAYER1=vCluster treemap, the **newapi** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 105 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **guacamole** tile sits inside the **mgmt** block, not **host**. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 106 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Drill into the **mgmt** block: its tiles include all 7 named apps (grafana/harbor/key… alongside loki/mimir/nats/tempo. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; mgmt block placement re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  the Cloud vCluster-grouped treemap is DEGENERATE: GET /api/v1/dashboard/treemap?group_by=vcluster,application returns a SINGLE top-level block name='host' (count 278); all 7 named apps are children of 'host' — there is NO 'mgmt' vCluster block, and org vclusters (acme/walk-stranger) aren't surfaced as blocks. ❌ (systemic — filing 1 issue) |
| 107 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Read the **host** block on the same treemap: none of the 7 named apps appear under `host`. | hw224 | ⛔ | RE-BASELINED ❌→⛔ (superseded, NOT a product fault): #4325 is MERGED — 'feat(bootstrap-kit): de-vcluster the platform planes (mgmt/rtz/dmz) → native host ns + per-component default-deny CNP'. The mgmt/rtz/dmz vClusters were DELIBERATELY removed; plane-isolation moved to per-component CNP. This row asserts the DEAD mgmt-vcluster placement model (app-in-mgmt-block) that #4325 voided — the 7 platform apps CORRECTLY run in their own host ns now. Prior re-baseline commit cea69645a already marked the #3642 placement rows ⛔; the hw228 walk agent re-flipped them ❌ by re-asserting the dead expectation. Not a gap. Refs #4325 #3642. ‖ ORIGINAL: hw224-fulltable-2026-07-04: superseded by #4325 de-vcluster; host block re-baselined‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 ‖ hw228 2026-07-07 console-UI walk (handover session, screenshots + kubectl cross-check):  OPPOSITE of the assertion — the 'host' block contains ALL 7 named apps (they should NOT be there if mgmt-grouped); vCluster grouping degenerate to a single host bucket. ❌ |
| 108 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Open the keycloak app card → its placement detail reads `mgmt` (the per-app placement mirrors the treemap block), not `host`. | hw236 | ⛔ | RE-BASELINED ☐→⛔ (superseded, NOT a product fault): #4325 de-vcluster moved keycloak into its own host ns (no mgmt vCluster), so per-app placement correctly reads `host`, NOT `mgmt` — the row asserts the DEAD mgmt-vcluster placement model that #4325 voided. Consistent with rows 99-107 (already ⛔ for the same reason). Refs #4325 #3642. ‖ hw255-catalog-edit-2026-07-15: re-confirmed on hw255 (read-only): keycloak runs in host ns 'keycloak' (keycloak-0 Running) — per-app placement 'host' is CORRECT post-#4325; ⛔ re-baseline stands. |
| 109 | placement | [#688](https://github.com/openova-io/openova/issues/688) | The sovereign-realm account console renders for the owner (no second login, no error dialog). | hw227 | ⛔ | RECLASSIFIED ❌→⛔ (by-design, NOT a product fault): the KC account-console "Something went wrong" REST 401 on a brokered catalyst-pin session is an accepted consequence of passwordless-PIN (#688) — the KC-native account console cannot mint a REST token from the PIN-brokered session by design. Link corrected #3642→#688 (#3642 was a mis-link — that issue is the mgmt-vcluster migration, unrelated). ‖ ORIGINAL: hw232 Playwright SSO (screenshot hw232-sso-evidence/): KC account console "Something went wrong" REST 401 on brokered catalyst-pin session — EXPECTED honest-non-green gate holds (KC-native, passwordless-PIN #688) |
| 110 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Gitea opens already signed in (avatar/menu shows the SSO user), repo list renders — no Gitea login form. | — | ☐ | — |
| 111 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Harbor opens signed in, the projects list renders — no Harbor login form, no gateway error page. | hw290 | ⚠️ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ same finding as row 32: Harbor bare URL lands on the Harbor SIGN-IN page (https://registry.hw290.omani.works/account/sign-in?redirect_url=%2Fharbor%2Fprojects, 'LOGIN WITH openova-sso / LOGIN VIA LOCAL DB'), so a login form IS presented ‖ no gateway error page (HTTP 200 throughout, redirect chain 301→302 c/oidc/login→302 KC→302 c/oidc/callback) ‖ once past it the projects list renders signed-in and /api/v2.0/users/current → 200 admin_role_in_auth=true ‖ shots/harbor.png + shots/f-harbor.png ‖ hw280-2026-07-20 cc=true: #5278 route-wiring VALIDATED — catalyst-api serves /oidc internally (200, valid discovery doc api.hw280/oidc/auth); CATALYST_PIN_BROKER_CLIENT_SECRET wired+non-empty; pod rolled post-secret. BUT external api/oidc FLAPS 200/404 (~60% 404) — multi-region gateway gap (Huawei ELB fronts both regions' cilium-envoy; region-b envoy lacks the api-host route). Intermittent SSO broker. Fix-agent a908c94e in flight. Route-wiring fixed; gateway propagation is the residual blocker. |
| 112 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Grafana opens signed in (no Grafana login), the home dashboard renders. | — | ☐ | — |
| 113 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | The OpenBao UI renders signed in via OIDC — no manual token/unseal prompt blocking the landing. | hw290 | ⚠️ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ the OIDC landing itself is NOT blocked: final URL /ui/vault/secrets, 0 token-entry inputs, no unseal prompt, OpenBao chrome renders (Secrets engines / Access / Policies / Tools / Monitoring / Raft Storage / Seal OpenBao, namespace 'root') ‖ BUT the signed-in session has no usable policy — body shows 'Not authorized … /v1/sys/internal/ui/mounts returned a 403 … permission denied' plus 403s on resultant-acl, namespaces and capabilities-self ‖ marked partial rather than pass because the rendered screen is an error state, not a working OIDC session ‖ shots/f3-openbao.png ‖ hw280-2026-07-20 cc=true: #5278 route-wiring VALIDATED — catalyst-api serves /oidc internally (200, valid discovery doc api.hw280/oidc/auth); CATALYST_PIN_BROKER_CLIENT_SECRET wired+non-empty; pod rolled post-secret. BUT external api/oidc FLAPS 200/404 (~60% 404) — multi-region gateway gap (Huawei ELB fronts both regions' cilium-envoy; region-b envoy lacks the api-host route). Intermittent SSO broker. Fix-agent a908c94e in flight. Route-wiring fixed; gateway propagation is the residual blocker. |
| 114 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi opens signed in, its main console renders — no login form, no upstream-connect error. | — | ☐ | — |
| 115 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Guacamole opens signed in, the connections list renders — no Guacamole login form. | hw290 | ❌ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ same finding as row 35: https://guacamole.hw290.omani.works/ ends on /guacamole/ with a fully BLANK page (innerText 412 whitespace chars, empty title) — the connections list never renders ‖ no Guacamole login form is shown either, because the SPA JS never loads: every asset 302s into the gate and its /oauth2/callback returns 403 (9 console 403 errors) ‖ the _oidc_gate_guacamole session cookie is never set (only _oidc_gate_guacamole_csrf survives) ‖ shots/f-guacamole-reload.png ‖ hw288-2026-07-25: same Guacamole SSO defect now ROOT-CAUSED on current env hw288 — guacamole-auth-sso-openid 1.5.5 rejects the valid Keycloak id_token with 'invalid/old nonce' (intermittent silent-SSO nonce race; config correct). Filed #5358 (regression vs CLOSED #3374). Prior hw280 ⚠️: hw280-2026-07-20 cc=true: #5278 route-wiring VALIDATED — catalyst-api serves /oidc internally (200, valid discovery doc api.hw280/oidc/auth); CATALYST_PIN_BROKER_CLIENT_SECRET wired+non-empty; pod rolled post-secret. BUT external api/oidc FLAPS 200/404 (~60% 404) — multi-region gateway gap (Huawei ELB fronts both regions' cilium-envoy; region-b envoy lacks the api-host route). Intermittent SSO broker. Fix-agent a908c94e in flight. Route-wiring fixed; gateway propagation is the residual blocker. |
| 116 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Organizations directory: the page title / heading reads "Organizations", never "Tenants"/"SME Tenants". | — | ☐ | — |
| 117 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Directory org cards / list rows: column headers read "Organization / Kind / Tier / Billing / Isolation / Status"; rows labeled "Organization", never "tenant"/"SME". | — | ☐ | — |KIND|TIER|BILLING|ISOLATION|STATUS; row badges PARENT/INTERNAL/CUSTOMER — zero tenant/SME row labels. Only 'tenant' string on page = workload name 'tenant-7fffbb499f' (org-services pod) in the __platform__ showback app table (data, not a label). |
| 118 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Left-nav sidebar label for this section reads "Organizations", not "Tenants"/"SME". | — | ☐ | — |
| 119 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Create-organization flow: the form title, field labels, and submit button all say "Organization" (no "SME tenant slug" / "Onboard tenant" persona words). | — | ☐ | — |SME|onboard over the rendered form = 0 matches. |
| 120 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Organization-detail view: heading "Acme Corp", breadcrumb "← Organizations", field labels Slug/Kind/Tier/Billing mode/Isolation/Stat… — no "tenant"/"SME". | hw290 | ✅ | hw290-2026-07-26: /organizations/acme-corp — heading `Acme Corp`, breadcrumb `← Organizations`, field labels exactly `Slug / Kind / Tier / Billing mode / Isolation / Status / Owner / Console`. DOM regex scan of the detail page: `tenant` = 0 matches, `\bSME\b` = 0 matches. (Directory page has 1 residual `tenant` occurrence inside a JS-bundle-rendered string; the detail page is clean.) ‖ — |SME = 0 matches. |
| 121 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | BSS / billing screen: billing is framed as "This organization is in showback mode…", zero "tenant"/"SME" leaks. | — | ☐ | — |
| 122 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Legacy `/bss/tenants` URL (PR #3390 alias) resolves and redirects to `/organizations`, rendering the directory (H1 "Organizations") — not a 404, not a login redirect. | — | ☐ | — |
| 123 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Handover URL → lands on `/dashboard` already signed-in as the owner (avatar E), no login form (everything below is admin-gated). | — | ☐ | — |
| 124 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Catalog grid renders Blueprint cards in a tile grid, each with an icon + summary; the Alloy card is visible. | — | ☐ | — |
| 125 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the Alloy card → the detail page renders: a hero (icon + name + summary + Edit IaC), an About section, and an Instances list; no login redirect. | — | ☐ | — |
| 126 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the admin Edit affordance in the hero → an edit form drops INLINE into the detail page (no modal overlay); fields appear in-place under the hero. | hw290 | ✅ | hw290-2026-07-26: hw290 2026-07-26T14:24Z /catalog/bp-alloy. Clicked the hero admin Edit affordance (data-testid cif-summary-edit — the per-field pencil on the hero summary). Editor cif-summary-editor became visible IN PLACE: hero.contains(editor)=true; editor rect x=541 y=203 w=717 h=60 sits INSIDE catalog-hero rect x=410 y=67 w=860 h=352; computed style position='static', zIndex='auto', page scrollY=0. Modal/overlay probe `[role=dialog],[aria-modal=true],.modal,.overlay` counted 0 BEFORE the click and 0 AFTER → no modal overlay, the form drops inline under the hero. Same in-place pattern for the icon field (cif-icon-edit → light/dark IconPicker rendered in the hero). Screenshot w290/03-126-inline-editor.png. ‖ — |
| 127 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | In the inline form, change Summary to `RECONCILE-PROOF-<ts>` → Save → the page refreshes in place and the new summary shows in the hero. | hw290 | ✅ | hw290-2026-07-26: Before: hero catalog-summary = 'RECONCILE-PROOF-20260726131547', GET /api/v1/catalog bp-alloy card.summary identical. Typed 'RECONCILE-PROOF-20260726142400' into cif-summary-input, clicked cif-summary-save. Toast cif-summary-save-verdict = 'Saved to IaC ✓'. Hero refreshed IN PLACE (no navigation, url stayed /catalog/bp-alloy): catalog-summary = 'RECONCILE-PROOF-20260726142400'. Persistence proven three ways: (a) hard page reload → hero still 'RECONCILE-PROOF-20260726142400'; (b) GET /api/v1/catalog → card.summary = 'RECONCILE-PROOF-20260726142400'; (c) IaC source of truth GET /api/v1/catalog/bp-alloy/iac → path 'catalog-sovereign/bp-alloy/blueprint.yaml', source 'committed', spec.card.summary: RECONCILE-PROOF-20260726142400. Left in place (that is the row's expected end state). Screenshot w290/03-127-summary-saved.png. ‖ hw286-2026-07-23: catalog detail data live (real versions); detail page render browser-only. |
| 128 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Back on the grid, the Alloy card summary now reads `RECONCILE-PROOF-<ts>` — the edit propagated to the card, not just the detail page. | — | ☐ | — |
| 129 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Hard-reload the detail page → the summary is still `RECONCILE-PROOF-<ts>` — the edit persisted across a reload, not an in-memory overlay. | — | ☐ | — |
| 130 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The non-card `version` field renders as a `v1.0.1` chip in the hero, and Edit IaC exposes the full CR including `version` for editing. | — | ☐ | — |
| 131 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Note the current hero logo (the Alloy glyph) as the baseline before an icon edit. | — | ☐ | — |
| 132 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit → in the Light-theme icon field paste a distinct image → Save → a "Saved to IaC ✓" confirmation shows and the page refreshes. | hw290 | ✅ | hw290-2026-07-26: Clicked cif-icon-edit → the Light-theme IconPicker URL field (iconpicker-light-url, placeholder 'https://… (or pick / upload above)') was visible and pre-filled with the current grafana.svg. Pasted a DISTINCT image URL 'https://cdn.jsdelivr.net/gh/simple-icons/simple-icons/icons/apachekafka.svg' → cif-icon-save. Confirmation cif-icon-save-verdict = 'Saved to IaC ✓' (the committed:true green, not the 'Saved to store — IaC commit failed' warn variant). Page refreshed: GET /api/v1/catalog bp-alloy card.iconLight = apachekafka.svg; after a hard reload the hero <img class=hero-logo> src = apachekafka.svg. IaC readback GET /api/v1/catalog/bp-alloy/iac showed spec.card.iconLight: .../apachekafka.svg in catalog-sovereign/bp-alloy/blueprint.yaml. REVERTED at 14:52Z through the same picker back to grafana.svg (IaC readback confirms iconLight=.../grafana.svg). Screenshots w290/03-132-icon-pasted.png, w290/03-132-icon-saved.png, w290/16-icon-reverted.png. ‖ hw288-2026-07-24: Playwright — the Edit form HAS the paste affordance (light-icon URL textbox placeholder 'https://… (or pick / upload above)' + 'Upload new' button); persist proven via the PICKER variant (rows 137-139), the paste-a-distinct-URL path not separately exercised |
| 133 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Observe the hero — it now shows the new logo; the render reads the edited `card.iconLight` first (IaC-first), not the bundled asset. | — | ☐ | — |
| 134 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Return to the grid — the Alloy card icon is now the new logo (the grid tile resolves the same edited `card.iconLight`). | hw290 | ✅ | hw290-2026-07-26: Immediately after the row-132 save, navigated back to the /catalog grid (fresh GET, 200) and read the Alloy tile: the <a href="/catalog/bp-alloy"> card's <img src> = 'https://cdn.jsdelivr.net/gh/simple-icons/simple-icons/icons/apachekafka.svg' — i.e. the grid tile resolved the same edited card.iconLight, not the bundled alloy.svg. Card text also carried the row-127 summary: 'Alloy INSIGHTS RECONCILE-PROOF-20260726142400 dev FREE BOOTSTRAP + opentelemetry Edit INSTALLED'. Screenshot w290/03-134-grid-card.png. Icon subsequently reverted to grafana.svg. ‖ hw288-2026-07-24: Playwright — icon persist to IaC confirmed (API component-logos/flink.svg); the /catalog grid-card-icon reflecting the new logo not separately re-walked this pass (grid reads the same catalog API) |
| 135 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Reload the detail page — the hero is still the new logo (render reads the persisted IaC icon on every load). | — | ☐ | — |
| 136 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit again — the Light-theme icon field shows the current IaC value, falling back to the bundled asset only when IaC carries none. | — | ☐ | — |
| 137 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit → click the icon picker (`iconpicker-*`) → a thumbnail grid of vendored `component-logos/*` assets opens (a role=listbox grid of logo tiles). | — | ☐ | — |
| 138 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click `cilium.svg` in the picker grid → the icon field + a live preview swatch update to the Cilium logo. | — | ☐ | — |
| 139 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Save → reload — the hero is now the Cilium logo (the picker selection persisted to IaC and renders on hero + grid card). | — | ☐ | — |
| 140 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | On Save, the durable-commit verdict (git outcome) is surfaced in-UI (the Edit-IaC `managed-by: manual • in sync` indicator + {stored:true,commit… not a silent store success. | — | ☐ | — |
| 141 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Hover the summary line in the hero → a pencil/edit affordance appears ON the field (`cif-summary-edit` → `cif-summary-input`), inline, without opening the full form. | — | ☐ | — |
| 142 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the summary → type a value → Save → only the summary updates in place; no full-form modal opens. | — | ☐ | — |
| 143 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Repeat the inline edit for the name field (`cif-name-edit` → `cif-name-input`) — it edits in place and saves just that field. | — | ☐ | — |
| 144 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit IaC (`catalog-detail-ed… admin only) → the full `blueprint.yaml` opens in the YAML editor (the entire CR, not just 7 card fields). | — | ☐ | — |
| 145 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Change a field in the editor → Commit → a Show-diff Current/Proposed side-by-side renders the change and the commit succeeds (confirmation appears). | hw290 | ✅ | hw290-2026-07-26: /catalog/bp-alloy → 'Edit IaC ⟩' (catalog-detail-edit-iac) opens the full-blueprint YAML editor (GET /api/v1/catalog/bp-alloy/iac, path catalog-sovereign/bp-alloy/blueprint.yaml). Header showed 'managed-by: manual • no unsaved edits' and Commit IaC DISABLED. Changed a field in the editor (spec.version 1.0.3→1.0.4 via CodeMirror) → header flipped to '• unsaved changes', Commit IaC ENABLED. Clicked 'Show diff' → yaml-editor-diff rendered a true SIDE-BY-SIDE Current ‖ Proposed (grid-template-columns '417px 417px'; yaml-editor-diff-left header 'Current', yaml-editor-diff-right header 'Proposed'; 74 lines each) with exactly one highlighted changed row: line 71 current ' version: 1.0.3' vs proposed ' version: 1.0.4'. 'Validate (dry-run)' → yaml-editor-validate-ok 'Dry-run accepted (resourceVersion 199151)' (POST 200 /api/v1/sovereigns/31106b6aa4a7e8e7/k8s/Blueprint/_/bp-alloy/dry-run). 'Commit IaC' → yaml-editor-apply-ok 'Committed to IaC ✓ (catalog-sovereign/bp-alloy/blueprint.yaml)' (PUT 200 /api/v1/catalog/bp-alloy/iac), apply-err null. Reverse cycle re-rendered the diff (current 1.0.4 / proposed 1.0.3) and committed green too. Screenshots w290/06-fwd-diff.png, w290/06-fwd-commit.png, w290/06-rev-diff.png, w290/06-rev-commit.png. ‖ hw288-2026-07-24: Playwright — Edit IaC editor has a 'Show diff' button + 'managed-by: manual • no unsaved edits' state + Validate(dry-run) + Commit IaC (disabled until edits); the Current/Proposed diff affordance is present. Full change→Commit→diff not exercised (avoided a code-editor YAML commit mutation) |
| 146 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The editor subtitle states it directly: "Commit writes the IaC source of truth; Flux reconciles it… Both this editor and the card form above write the same file." | — | ☐ | — |
| 147 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Open the WordPress detail page → Edit → change Summary → Save → reload → the summary persists, exactly as for Alloy. | — | ☐ | — |
| 148 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | WordPress: Edit IaC → edit `spec.manifests` → Commit → reload — the same YamlEditor edits a structurally-different blueprint's manifests in place. | — | ☐ | — |
| 149 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | WordPress: Edit → Light-theme icon → distinct image → Save → reload — the WordPress hero icon visibly changes. | — | ☐ | — |
| 150 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | PostgreSQL detail renders the SAME edit surface (hero icon, Edit IaC, clickable cards) and Edit IaC exposes `contextSchema` (a blueprint carrying contextSchema`/`sha… | — | ☐ | — |
| 151 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Alloy + Postgres render the IDENTICAL edit chrome (same cif-icon-edit`/`cif… same Edit-IaC YamlEditor) — no blueprint-specific UI. | — | ☐ | — |
| 152 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The catalog detail page renders (hero · About · Instances) and opens an INLINE Edit form (no modal) — acceptance headline 1. | — | ☐ | — |
| 153 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | A summary edit Saves, updates the page AND the grid card, and persists across a reload — acceptance headline 2. | — | ☐ | — |
| 154 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | A non-card field edit (`version`) persists — the whole CR is editable, not a 7-field overlay — acceptance headline 3. | hw290 | ✅ | hw290-2026-07-26: NON-CARD field edited and persisted: the Edit-IaC editor exposes the COMPLETE blueprint.yaml (73 lines: metadata.annotations/labels, spec.card, spec.endpoints, spec.manifests, spec.placementSchema, spec.source, spec.sso, spec.topology, spec.version, spec.visibility) — not a 7-field card overlay. Edited spec.version (a non-card field) 1.0.3 → 1.0.4 → Commit IaC → 'Committed to IaC ✓'. Persistence verified independently: GET /api/v1/catalog/bp-alloy/iac readback specVersion=1.0.4 (source 'committed'), and the LIVE CR `kubectl get blueprint bp-alloy -o jsonpath='{.spec.version}'` = 1.0.4 (rv 199151→207713). Then REVERTED to 1.0.3 via the same editor → IaC readback specVersion=1.0.3 and live CR back to 1.0.3. Note spec.source.version (the chart pin, 1.0.2) was deliberately untouched, so no chart roll was triggered. ‖ hw288-2026-07-24: Playwright — Edit IaC exposes the COMPLETE blueprint.yaml in a YAML editor (category/version/manifests all editable — whole CR editable, not just card fields); a non-card field (version) edit+persist not exercised (code-editor commit mutation avoided) |
| 155 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The edited icon renders on hero + grid + survives reload; the form pre-fills the IaC icon; the picker grid works — acceptance headline 4. | — | ☐ | — |
| 156 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Save surfaces the IaC-commit verdict (`committed:true` + `• in sync`), not a bare store success — acceptance headline 5. | — | ☐ | — |
| 157 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Per-field inline edit for cards (`cif-*`) + the full-CR YamlEditor (Edit IaC) for the rest, both writing the same IaC source — acceptance headline 6. | — | ☐ | — |
| 158 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The identical edit mechanism works on a 2nd + 3rd blueprint — no per-blueprint UI (Alloy + Postgres + a third) — acceptance headline 7. | — | ☐ | — |
| 159 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Settings → Sovereignty section renders a "Cluster sovereignty" panel with a "TETHERED" badge + an "Achieve True Sovereignty" cutover CTA (runs the 8-step cutover + 10-min egress-block self-test). | hw290 | ✅ | hw290-2026-07-26: Settings→Sovereignty panel renders: heading + TETHERED badge + cutover CTA, owner session, HTTP 200 ‖ — |
| 160 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Console nav + Settings sidebar expose a dedicated "Sovereignty" anchor (`#sovereignty`) that scrolls to + highlights the Cluster-sovereignty panel — the cutover trigger is a first-class surface. | hw290 | ✅ | hw290-2026-07-26: exactly 1 element id="sovereignty" in DOM + nav link Sovereignty→#sovereignty ‖ — |
| 161 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Open `/jobs` (zero-login, signed in as owner) → the canvas table renders a populated activity list (not a spinner, empty state, or login redirect). | hw290 | ✅ | hw290-2026-07-26: /jobs HTTP 200 zero-login as owner, 174 populated tbody rows, no /login redirect ‖ — |
| 162 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Find the `cutover` group row and expand it → it renders the 11 `cutover-step-*` rows (gitea-mirror, harbor-projects, harbor-prewarm, registry-pivot, … vcluster-registry-p… — the 11-step execution tree. | hw290 | ✅ | hw290-2026-07-26: cutover group expands to exactly 11 KIND=STEP rows with PARENT=Cutover ‖ — |
| 163 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Each `cutover-step-*` row reads an honest per-step status (Succeeded / Running / Failed / Pending), never a premature green. | hw290 | ✅ | hw290-2026-07-26: walked MID-RUN with genuinely mixed state — Gitea Mirror SUCCEEDED, Harbor Projects SUCCEEDED, Harbor Prewarm FAILED, remaining 8 PENDING. Honest per-step status, no premature green ‖ — |
| 164 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | The cutover group status reflects its real children (a group with a failed child reads failed, not a fake Succeeded). | hw290 | ✅ | hw290-2026-07-26: with exactly one failed child the API returns group {jobName:cutover, kind:group, status:failed} — NOT a fabricated Succeeded ‖ — |
| 165 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | On a failed `cutover-step-*` row, a Re-run button is present (per-row, gated to Failed) — the operator can re-drive a failed cutover step from the browser. | hw290 | ❌ | hw290-2026-07-26: POSITIVE HALF EXERCISED FOR THE FIRST TIME AND IT IS BROKEN. Gating is correct both directions (Re-run renders only on the genuinely FAILED row, Harbor Prewarm STATUS=FAILED) but the control does not re-drive the step. Previously this row was only ever half-walked (gating-off half); the positive half now fails ‖ hw290-2026-07-26: hw290-2026-07-26: gating-OFF half PROVEN on live board — cutover has not run (self-sovereign-cutover-status CM cutoverComplete=false, all 11 cutover-step-* rows status=pending), and all 11/11 rows render Actions '—' (jobs-cell-actions-empty-*) with 0 retry buttons, re-confirmed on the post-heal board (11 rows, dash=11, btn=0). Backend gate corroborated WITHOUT touching the real cutover: POST /api/v1/deployments/31106b6aa4a7e8e7/jobs/cutover-step-gitea-mirror/retry → HTTP 409 {'error':'not-retryable','detail':'job status "pending" is not a failed/degraded state'} — rejected before any mutation, cutoverComplete still false afterwards. Positive half (Re-run present AND WORKING on a genuinely Failed cutover-step row) remains unexercised — no cutover step has failed and deliberately breaking the real cutover was out of scope. NEW CODE FINDING that makes the positive half likely FAIL when it is finally exercised: handler dispatchRetry (jobs_retry.go:215-295) switches on KindInstall/KindReconcile/KindReconciler/KindCron/KindMutation/KindTask and has NO 'case jobs.KindStep' — so a failed cutover-step row WOULD render the Re-run button (JobsTable gates on isJobRetryable(status) only, kind-agnostic) but the POST would fall to default: → errNotDirectlyRetryable → HTTP 422 'kind "step" does not support retry'. The operator cannot actually re-drive a failed cutover step from the browser. ‖ hw288-2026-07-23: LIVE /jobs Actions column: every cutover-step row (RUNNING/SUCCEEDED/PENDING) shows Actions='—' — Re-run is gated OFF when not Failed (gating-off half confirmed). PARTIAL: positive half (Re-run present ON a Failed row) not ex | ‖ hw288-2026-07-26 walked live: cutover cc=true 11/11 (self-sovereign-cutover-status CM, catalyst ns) → NO Failed cutover-step row exists → no per-row Re-run affordance to exercise. Gated on a real cutover-step failure (none on a green env).
| 166 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | (After a COMPLETE cutover) the `cutover` group reads all-11-green on `/jobs` — every step Succeeded, including `egress-block-test` and `registry-pivot`. | hw290 | ☐ | hw290-2026-07-26: cannot be walked — cutover halted 3/11 (cutoverComplete=false, 18%); board correctly reads 2 SUCCEEDED/1 FAILED/8 PENDING and group=failed ‖ — |
| 167 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Open the console root in a fresh tab → land on the operator dashboard signed in as the sovereign-admin (no login form, no password field). | — | ☐ | — |
| 168 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Open `/jobs` → the canvas table renders a populated list of activity rows (not a spinner, empty state, or login redirect). | — | ☐ | — |
| 169 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | The Kind column is present in the header and each row shows its kind; full header = Name·Kind·App·Deps·… | — | ☐ | — |
| 170 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Scroll/search to the `install-openbao` row → it renders green / Succeeded (the install is honestly green). | — | ☐ | — |
| 171 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Every rendered row maps to a real HelmRelease install / terraform stage (no placeholder, no synthetic/fabricated entry). | — | ☐ | — |
| 172 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Set the Status filter to `failed` → the table shows the genuinely-failing rows, each with an honest failed status (the Status filter works). | hw290 | ✅ | hw290-2026-07-26: hw290-2026-07-26: /jobs Status filter=failed now POPULATED with genuinely-failing rows (first time — prior envs had 0 failures). Controlled fault injected: ns uat-fault-injection + standalone batch Job uat-faultinject-probe (exit 1, backoffLimit 0) → k8s condition Failed=True in 15s. Browser (own chromium, owner session) https://console.hw290.omani.works/jobs → jobs-filter-status='failed' → result count '3/171', jobs-table-empty=0, 3 rows rendered each with honest FAILED status: task-uat-faultinject-probe (injected) ‖ task-cilium-envoy-tls-restart (GENUINE platform failure, kube-system, reason=DeadlineExceeded) ‖ cron-cnpg-cabundle-reassert (GENUINE, cnpg-system). API GET /api/v1/deployments/31106b6aa4a7e8e7/jobs HTTP 200 corroborates status counts 153 succeeded/2 running/2 failed/13 pending → 156/2/3/13 post-inject. Screenshot fault-02-filter-failed.png. NOTE ingestion scope: a failing HelmRelease would NOT surface here — helmwatch.ListAndSnapshotHelmReleases is Namespace(flux-system)+'bp-' prefix scoped AND gated behind 'if !hasBootstrapKit' (first-seed only); only chrootSeedReconcilerObservations runs every read and lists Jobs across ALL namespaces. ‖ hw288-2026-07-25: ◑ filter MECHANISM verified, no failures to populate it. Console /jobs 'Filter by status' select (options All/running/pending/succeeded/failed/healthy/degraded/failing) → selecting 'failed' applies cleanly and the table filters to 'No jobs match the current filters' — because this healthy Sovereign has 0 failed jobs (525 Succeeded, 3 Running, 6 Pending across 179 rows). The failed-filter works; there is simply no genuinely-failing row to display, so the populated-failed-row remediation UX (rows 174/176/177 Re-run button) is unexercisable without inducing a failure — not done on the live north-star DR env. Full walk deferred to a fresh prov or an env with a real failure. ‖ hw288-2026-07-25: filter MECHANISM works (owner-session Playwright: set the Status combobox to 'failed' → table correctly shows 'No jobs match the current filters'), BUT the /jobs UI surfaces NO failed rows while the API (/api/v1/sovereign/jobs) reports 3 genuinely-FAILED jobs (vpc-podcidr-route-reconciler-29747055,vpc-podcidr-route-reconciler-29747050,vpc-podcidr-route-reconciler-29747045). The jobs UI model shows lifecycle/bootstrap jobs (all Succeeded/Confirming) and does NOT include the cluster-level vpc-podcidr-route-reconciler CronJob failures — so the 'genuinely-failing rows' can't be shown here. Real UI job-model completeness gap (CronJob failures absent from /jobs). Stays PARTIAL: filter works, but no failed row is surfaced to exercise. ‖  hw288-2026-07-24: /jobs Status filter mechanism WALKED — setting Status=failed filters the table live; but the healthy env has 0 failing jobs so it shows 'No jobs match the current filters' — the failing-rows display can't be demonstrated without a genuine failure. Stays ◑ (mechanism works; needs a live failure to show failing rows). ‖hw288-2026-07-23: status distribution 157 succeeded/8 running/10 pending/0 failed -> failed-filter is honest empty-state (no genuinely-failing job in this healthy converging env). |
| 173 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Leave the table on screen ~30s → rows update live (tail) as reconciliation progresses; a status badge changes in place without a manual reload. | — | ☐ | — |
| 174 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | On a Failed row (Status=failed), a Re-run / Retry-reconcile button is present on the row (visible on the row or on hover). | hw290 | ✅ | hw290-2026-07-26: hw290-2026-07-26: per-row remediation control present on EVERY Failed row — 3/3. Live DOM inventory under Status=failed: task-uat-faultinject-probe → button data-testid='jobs-retry-task-uat-faultinject-probe' label 'Re-run' ‖ task-cilium-envoy-tls-restart → 'Re-run' ‖ cron-cnpg-cabundle-reassert → 'Run now'. Gating proven BOTH ways in the same render: all 11 cutover-step rows (status=pending) rendered jobs-cell-actions-empty-* '—' with 0 retry buttons, i.e. the control is gated strictly to isJobRetryable(status) ∈ {failed,degraded,failing}. Screenshot fault-02-filter-failed.png ‖ fault-03-prerun.png. ‖ hw288-2026-07-23: 0 failed rows -> Re-run/Retry-reconcile control not exercisable; gated to Failed rows only (same as prior healthy envs). |
| 175 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | On a Succeeded / healthy / Confirming row, NO Re-run button renders — the control is gated to Failed rows only. | — | ☐ | — |
| 176 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Click Re-run on a Failed row → a success toast/feedback appears and the button flips in place (e.g. `Requesting…`) — the browser triggered a re-reconcile, no terminal. | hw290 | ✅ | hw290-2026-07-26: hw290-2026-07-26: Re-run CLICKED in the browser on the injected zero-blast-radius row task-uat-faultinject-toast (deliberately NOT on task-cilium-envoy-tls-restart, whose remediation restarts the cilium-envoy hostNetwork DaemonSet and would blip the gateway for parallel walkers). In-place flip captured with a 40ms in-page sampler: t+50ms 'Requesting…' → t+120ms '✓ Requested' → t+2480ms row leaves the failed filter (status flipped off 'failed'). Retry XHR POST /api/v1/deployments/31106b6aa4a7e8e7/jobs/task-uat-faultinject-toast/retry → HTTP 200 body action='deleted + recreated Job uat-faultinject-toast for a fresh run', requestedBy='emrah.baysal@openova.io'. Real re-drive confirmed cluster-side: new Job uat-faultinject-toast-rerun-1785072256 created, no terminal used. Screenshots fault-06-toast-prerun.png ‖ fault-07-toast-after.png. ‖ hw288-2026-07-24: jobs agent — needs a FAILED job row to click Re-run + observe toast/'Requesting…' flip; 0 failed jobs on this healthy env, inducing a failure is a mutation not created (healthy-env limit) | ‖ hw288-2026-07-26 walked live: 60 cutover steps recorded, 0 failed (cc=true) → no Failed cutover-step row → no per-row Re-run to click. Gated (needs-a-real-failure).
| 177 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Use the same Re-run button on a Failed row — one remediation mechanism across rows, no per-kind UI (the single-table/single… shape). | hw290 | ⚠️ | hw290-2026-07-26: hw290-2026-07-26: ONE remediation mechanism confirmed at the UI/API layer — the identical RetryJobButton component and the identical endpoint POST /api/v1/deployments/{depId}/jobs/{jobId}/retry serve every Failed row regardless of kind; only the label is kind-derived (task→'Re-run', cron→'Run now'). No per-kind UI, single table, single control column. BUT the mechanism is a ONE-SHOT DEAD END on task rows, so it is not re-usable across the remediation lifecycle: after one click, (a) recreateStandaloneJob (jobs_retry.go:449) names the re-run '<name>-rerun-<unix>', taskBaseName strips the numeric suffix → a PHANTOM sibling leaf task-<name>-rerun is minted instead of folding onto the original; (b) the ORIGINAL leaf is left stamped 'running' PERMANENTLY (backing Job deleted, no observation ever finishes it — verified stuck across 6 polls over 4min: status=running, runCount=2, stale finishedAt=13:14:17Z) and 'running' is not retryable so the Re-run button DISAPPEARS from the row just remediated; (c) the spawned sibling is itself not retryable — POST .../task-uat-faultinject-probe-rerun/retry → HTTP 422 {'error':'not-directly-retryable','detail':'jobs "uat-faultinject-probe-rerun" not found in any namespace'}. Needs a fix issue. ‖ hw288-2026-07-24: jobs agent — same prerequisite (a Failed row) absent on healthy env; the one-remediation-mechanism assertion not walkable read-only without inducing a failure | ‖ hw288-2026-07-26 walked live: same gate — 0 failed cutover steps → no Failed row for the one-remediation-mechanism test. Gated (needs-a-real-failure).
| 178 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | The signed handover URL → lands directly on `/dashboard` signed-in (env switcher shows the live env, avatar E, no login form). | — | ☐ | — |
| 179 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Click the avatar (top-right) → menu reads "Signed in as the owner" with a Sign-out item — confirms the landed identity is the owner-admin. | — | ☐ | — |
| 180 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Grafana bare URL → lands on Grafana Home ("Welcome to Grafana", full UI, Profile avatar), `?orgId=1`, no login form (SSO landed signed-in). | — | ☐ | — |
| 181 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Harbor (registry) bare URL → lands on `/harbor/projects` (projects, repos, Administration nav), no login form; user dropdown the owner. | hw290 | ⚠️ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ bare URL does NOT land on /harbor/projects — it lands on /account/sign-in with a login form (see rows 32/111) ‖ after a second navigation the projects surface renders signed-in: 'Harbor ‖ emrah.baysal@openova.io ‖ Projects (Private 0, Public 1, Total 1) ‖ Repositories ‖ Quota used 0Byte ‖ NEW PROJECT / ACTION' and /api/v2.0/users/current → 200 {admin_role_in_auth true, Onboarded via OIDC provider} ‖ the user dropdown does resolve to the owner, but the Administration nav was not separately exercised ‖ shots/harbor.png + shots/f-harbor.png ‖ hw280-2026-07-20 cc=true: #5278 route-wiring VALIDATED — catalyst-api serves /oidc internally (200, valid discovery doc api.hw280/oidc/auth); CATALYST_PIN_BROKER_CLIENT_SECRET wired+non-empty; pod rolled post-secret. BUT external api/oidc FLAPS 200/404 (~60% 404) — multi-region gateway gap (Huawei ELB fronts both regions' cilium-envoy; region-b envoy lacks the api-host route). Intermittent SSO broker. Fix-agent a908c94e in flight. Route-wiring fixed; gateway propagation is the residual blocker. |
| 182 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Gitea bare URL → lands on the gitea dashboard titled "emrah.baysal - Dashboard - Catalyst Gitea", logged in; URL stays on :443. | — | ☐ | — |
| 183 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | OpenBao bare UI → final rendered screen is the authenticated Vault session (`/ui/vault/secrets`, Secrets Engines with cubbyhole/ + secret/ kv), NO `/ui/vault/auth` token form. | hw290 | ❌ | hw290-2026-07-26: env=hw290 2026-07-26 ‖ final rendered screen at https://bao.hw290.omani.works/ui/vault/secrets is NOT an authenticated Vault session — it is an error state: 'Not authorized — Ember Data Request GET /v1/sys/internal/ui/mounts returned a 403 … permission denied' ‖ the required Secrets Engines listing with cubbyhole/ and secret/ kv is absent; the mounts payload carries "secret":{} (no kv engines at all) and the only auth mount is oidc/ ‖ correctly there is NO /ui/…/auth login screen and no token form (0 token inputs), so the failure is authorization, not the SSO landing ‖ shots/f3-openbao.png ‖ hw280-2026-07-20 cc=true: #5278 route-wiring VALIDATED — catalyst-api serves /oidc internally (200, valid discovery doc api.hw280/oidc/auth); CATALYST_PIN_BROKER_CLIENT_SECRET wired+non-empty; pod rolled post-secret. BUT external api/oidc FLAPS 200/404 (~60% 404) — multi-region gateway gap (Huawei ELB fronts both regions' cilium-envoy; region-b envoy lacks the api-host route). Intermittent SSO broker. Fix-agent a908c94e in flight. Route-wiring fixed; gateway propagation is the residual blocker. |
| 184 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | — | [UAT.md](https://github.com/openova-io/openova/blob/main/docs/ledger/UAT.md) | ⚠️ | predecessor-walk(B): No test case defined in ledger (literal —). ‖ §I 91dc0591: no test case defined in ledger (literal —) — not click-verified, harness artifact |
| 185 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | No stale predecessor-env references anywhere in the rendered `UAT.md` (the table names only the live env). | hw288 | ◑ | hw288-2026-07-24: strict 'no predecessor refs anywhere' NOT met — Walk column carries ~28 predecessor envs (hw224…hw287) on un-re-walked ◑ rows + the evidence column keeps historical ‖ refs (by-design audit-trail). BUT substantive integrity is CLEAN: ALL 158 ✅ rows have Walk=hw288 (0 stale-env ✅) — every green row is current-env-verified. Stays ◑. ‖hw288-2026-07-23: no stale predecessor-env refs: all API responses self-identify as hw288/027f07559af1f9f7; served /dashboard HTML (1063B) + whoami + topology carry zero hw286/hw287 literals; full rendered-DOM scan browser-only. |
| 186 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | — | [UAT.md](https://github.com/openova-io/openova/blob/main/docs/ledger/UAT.md) | ⚠️ | predecessor-walk(B): No test case defined in ledger (literal —). ‖ §I 91dc0591: no test case defined in ledger (literal —) — not click-verified, harness artifact |
| 187 | topology | [#4001](https://github.com/openova-io/openova/issues/4000) | Per-app Topology for a multi-region app (grafana) shows **Pattern: active-active** with 2 PRIMARY region cards — NOT singleton. | — | ☐ | — |
| 188 | topology | [#4001](https://github.com/openova-io/openova/issues/4000) | A genuine single-region app (catalyst-api) correctly shows **singleton** (no false multi-region). | — | ☐ | — |
| 189 | topology | [#4000](https://github.com/openova-io/openova/issues/4000) | Region-b kubeconfig self-heals EIP→private-IP **zero-touch** on restart (topology survives a fresh prov). | — | ☐ | — |
| 190 | jobs | [#3916](https://github.com/openova-io/openova/issues/3916) | `/jobs` lists **ONLY finite jobs** (provision/cutover steps, batch Jobs, CronJob runs) — ZERO continuous-reconciler rows. | — | ☐ | — |
| 191 | jobs | [#3916](https://github.com/openova-io/openova/issues/3916) | Continuous reconcilers (HelmRelease/Kustomization) do **NOT** appear in `/jobs` (they live in the recon lens). | — | ☐ | — |
| 192 | recon | [#3925](https://github.com/openova-io/openova/issues/3996) | The convergence **Reconciliation** link opens the cloud **RECON lens** (`view=graph&lens=reconciliation`), not the default cloud lens. | — | ☐ | — |
| 193 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Clicking a reconciler opens the **ArgoCD-like management surface** (drill-in). | — | ☐ | — |
| 194 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Recon surface lists Flux reconcilers with live status (Reconciled/Reconciling/Degraded/Suspended). | — | ☐ | — |
| 195 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Drill a reconciler → its controller **logs** render. | — | ☐ | — |
| 196 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | **Reconcile** action → `reconcile.fluxcd.io/requestedAt` lands on the live object. | hw290 | ✅ | hw290-2026-07-26: Target GitRepository/flux-system/openova. BEFORE (14:35:04Z, kubectl): metadata.annotations has NO reconcile.fluxcd.io/requestedAt (None) and status.lastHandledReconcileAt is empty; artifact.lastUpdateTime 14:31:49Z. UI: /cloud/resource/GitRepository/flux-system/openova/reconcile → 200, reconcile-tab rendered 'STATE Reconciled REVISION main@sha1:d53807ce… SUSPENDED no'. Clicked reconcile-action-reconcile at 14:35:12.014Z → POST 200 /api/v1/deployments/31106b6aa4a7e8e7/reconcilers/GitRepository/flux-system/openova/reconcile, UI reconcile-action-msg = 'reconcile requested by emrah.baysal@openova.io'. AFTER (14:35:27Z, kubectl -o jsonpath): metadata.annotations['reconcile.fluxcd.io/requestedAt'] = 2026-07-26T14:35:12.003417381Z — the annotation LANDED ON THE LIVE OBJECT. EFFECT also proven, not just the annotation: source-controller consumed the token — status.lastHandledReconcileAt = 2026-07-26T14:35:12.003417381Z (exact match), stable at +20s. Screenshot w290/09-row196.png. ‖ hw286-2026-07-23: recon Reconcile action: POST .../reconcilers/{kind}/{ns}/{name}/reconcile → 200 requestedAt/requestedBy (proven live on the cutover HelmChart earlier) — sets reconcile.fluxcd.io/requestedAt; full UI-drill render browser-only. |
| 197 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | **Suspend/Resume** → `spec.suspend` flips on the live object. | hw290 | ✅ | hw290-2026-07-26: spec.suspend FLIP proven both ways AND the object provably stopped/resumed reconciling. (A) GitRepository/flux-system/catalyst-tenant-hw290-omani-works: before spec.suspend unset, Ready=True. UI reconcile-action-suspend → POST 200 .../suspend, msg 'suspend requested by emrah.baysal@openova.io' → kubectl spec.suspend=true (14:36:14Z); tab re-read showed 'STATE Suspended … SUSPENDED yes' with the button set swapping to reconcile+resume. UI reconcile-action-resume → POST 200 .../resume → kubectl spec.suspend=false, Ready=True (14:38:21Z). (B) BEHAVIOURAL stop/resume proof on Kustomization/flux-system/catalog-sovereign: suspend via UI (POST 200 .../suspend) → spec.suspend=true (14:38:43Z); then committed a real change to its Git source through the console (Edit-IaC PUT, bp-alloy spec.version 1.0.3→1.0.5, 'Committed to IaC ✓' at 14:39:11Z) and polled read-only for 115s — `kubectl get blueprint bp-alloy -o jsonpath='{.spec.version}'` stayed 1.0.3 and status.lastAppliedRevision stayed pinned at catalog-sovereign@sha1:e477a652 through 14:41:06Z → the object genuinely STOPPED reconciling. UI reconcile-action-resume at 14:41:18Z (POST 200 .../resume) → spec.suspend=false and by 14:41:25Z the pending change had been applied: bp-alloy spec.version=1.0.5 → it genuinely RESUMED. All objects restored (both spec.suspend=false, Ready=True) and the seed reverted to 1.0.3. Screenshots w290/09-row197-suspend.png, w290/09-row197-resume.png, w290/09-row197b-suspend.png, w290/09-row197b-resume.png. ‖ hw288-2026-07-24: spec.suspend mechanism LIVE-confirmed — 4 objects show spec.suspend=true live (flux-system/bp-catalyst-secondary-edge, bp-cluster-autoscaler-hcloud, bp-hcloud-ccm, bp-velero) — the flip persists on live objects. The UI Suspend/Resume ACTION triggering it is a mutation not exercised. Stays ◑ (state confirmed; UI-action-flip is a mutation). ‖hw288-2026-07-24: kubectl+API — 4 reconcilers observably spec.suspend=true (bp-catalyst-secondary-edge/cluster-autoscaler-hcloud/hcloud-ccm/velero); suspend state live/readable; the flip action is a mutation not exercised ‖ hw288-2026-07-23: /reconcilers shows 4 reconcilers already in Suspended state (suspend mechanism live); the spec.suspend flip action itself is a mutation not exercised. |
| 198 | cloud | [#3987](https://github.com/openova-io/openova/issues/3987) | `/cloud` per-kind **helmreleases** page shows the real count (~65), not 0. | — | ☐ | — |
| 199 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **Gateway** page shows the live cilium Gateways (2: `cilium-gateway` + `cilium-gateway-console`). | — | ☐ | — |
| 200 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **HTTPRoutes** page shows 15. | — | ☐ | — |
| 201 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **NetworkPolicies** page shows the live policies (30 live; expected ~10 stale). | — | ☐ | — |
| 202 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **CiliumNetworkPolicies** page shows the live policies (42 live; expected 5 stale). | — | ☐ | — |
| 203 | cloud | [#4002](https://github.com/openova-io/openova/issues/4002) | Cloud **Load Balancers** page shows the real LoadBalancer Svc (clustermesh-apiserver EXTERNAL-IP 212.72.24.31), not 0. | — | ☐ | — |
| 204 | cloud | [#4002](https://github.com/openova-io/openova/issues/4002) | Cloud **Worker Nodes** page shows the real nodes, not 0. | — | ☐ | — |
| 205 | fleet | [#4003](https://github.com/openova-io/openova/issues/4003) | `/fleet/applications` returns the real app count (non-zero), not 0. | — | ☐ | — |
| 206 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | `kubectl get managed` is non-empty — Crossplane observes the OpenTofu-built infra. | — | ☐ | — |
| 207 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | A `CloudAdoption` for the real ELB reaches Synced+Ready (Observe), `external-name` = the real ELB id. | — | ☐ | — |
| 208 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | Adoption is **Observe-only** — the live ELB/nodes are untouched (no re-provision). | — | ☐ | — |
| 209 | storage | [#3971](https://github.com/openova-io/openova/issues/3971) | PVCs land on a real **CSI storageclass** (not k3s local-path). | — | ☐ | — |
| 210 | storage | [#3971](https://github.com/openova-io/openova/issues/3971) | `local-path` is **FORBIDDEN** (k3s `--disable=local-storage`). | — | ☐ | — |
| 211 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP **sovereign-admin** token → `list_applications` returns all apps. | — | ☐ | — |

| 212 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP **Org-scoped** token → `list_applications` returns ONLY that Org's apps (RBAC parity). | — | ☐ | — |

| 213 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP cross-Org `get_application` → **403** (UI-parity). | — | ☐ | — |

| 214 | orgs | [#3985](https://github.com/openova-io/openova/issues/3985) | No `SME`/`tenant` banned-term leak in the console bundle (org-rename complete). | — | ☐ | — |
| 215 | orgs | [#3985](https://github.com/openova-io/openova/issues/3985) | CRD `tier` enum is `[org, corporate]` — a `tier: org` Organization is accepted. | — | ☐ | — |
---

### Governance train — items 187-215 (added 2026-06-21)

These extend the canonical 186-case walk with this session's governance work. **Status is deliberately conservative** (no premature ✅): `⏳` = built/merged in code but **NOT yet validated on the durable live console** (the agents' temporary image rolls were Flux-reverted, so they are not yet what the operator sees — each needs a real end-user walk after the durable roll); `◑` = mechanism proven (e.g. against live HCS) but full in-cluster proof lands on a fresh prov; `❌` = not built yet (fix in flight) or genuinely requires a clean re-prov. A row flips `✅` only after an operator end-user walk on the durable console with pasted evidence, per the TRUST-ledger discipline.
| 216 | e2e-journey | [#3376](https://github.com/openova-io/openova/issues/3376) | A user with a **coupon/voucher code** redeems it → creates **his Organization**. Surface: `https://marketplace.<fqdn>/redeem/?code=<CODE>` (no session) → plan/region → Launch. PASS = the provisioning timeline runs to Done AND `/organizations` shows the Org **ACTIVE** backed by live `vcluster` pods (not a fake-green status). | hw290 | ⚠️ | hw290-2026-07-26: Redeem URL https://marketplace.hw290.omani.works/redeem/?code=HW290WALKTWO opened with NO session → 200 → `#redeem-cta` → /plans → plan M → /apps (wordpress+openclaw+stalwart-mail) → /addons (walk-two . omani.rest) → /bcp (active-hot-standby) → /review → /checkout → PIN → `Launch my Organization` (POST /api/tenant/orgs 201, POST /api/provisioning/start 201). SECOND half PASSES: /organizations shows `walk-two … CUSTOMER ‖ Org ‖ REAL ‖ vcluster ‖ ACTIVE` backed by LIVE vcluster pods (vcluster-0 1/1 Running, coredns synced) — not fake-green. FIRST half FAILS: the provisioning timeline never reached Done (red at Deploying WordPress, row 86). Partial. ‖ — |
| 217 | e2e-journey | [#3374](https://github.com/openova-io/openova/issues/3374) | The user logs in via the **passwordless SSO magic-email PIN** (any domain, e.g. `demo@openova.io`) → 6-digit PIN emailed → lands signed-in, no password. PASS = the PIN actually arrives (verify IMAP `mail.openova.io:993`) AND entering it lands signed-in as the CUSTOMER (their email in the avatar, their Org in the header) with no password field — this is the customer PIN flow, NOT the operator handover. | hw290 | ✅ | hw290-2026-07-26: Passwordless PIN, customer-side (not the operator handover). POST /api/auth/magic-link 200 for `emrah.baysal+walktwo@openova.io` → PIN actually arrived and was read live over IMAP 45.151.123.50:993 READ-ONLY (BODY.PEEK, mailbox never marked Seen) matching `Your login code: NNNNNN` → entered → POST /api/auth/verify 200 → landed signed-in AS THE CUSTOMER: avatar `emrah.baysal+walktwo E`, their email rendered twice on the checkout header, their Org `walk-two.omani.rest` in the summary. Zero password fields in the entire flow. Screenshots w4-17-code-prompt.png → w4-18-signed-in.png. ‖ — |
| 218 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | **chepherd is installable from the catalog as an Application** (`bp-agenity` **Helm chart**). Surface: per-Org console → Catalog → the `bp-agenity` card renders + the install wizard opens with the Org's Environment pre-selected as the target. PASS = card present + wizard opens (the chart resolves from the catalog, not 404). | — | ☐ | — |
| 219 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4180](https://github.com/openova-io/openova/issues/4180) [#4553](https://github.com/openova-io/openova/issues/4553) | The user **provisions chepherd** as an Application → it converges (Helm deploys, pods Ready, chepherd console reachable in his Org). | — | ☐ | hw288-2026-07-25 LIVE gate-check: 0 vcluster CRs + 1 Org (internal sovereign seed, no customer slug) → NO customer Org exists to install the per-Org agenity workspace into. Gate confirmed closed; needs a customer Org (via a completed purchase). Convergence itself is cred-independent (loud-skip design, prior note) but the per-Org install target is absent. ‖hw288-2026-07-24 gate analysis: agenity StatefulSet is DESIGNED to converge WITHOUT the Anthropic cred (loud-skip: statefulset.yaml:87 'if [ -s /creds/…]' present-branch, :230 'if not credentialsKey' absent-branch) — so the founder cred unblocks the CHAT leg, not convergence. BUT 219 still needs a CUSTOMER ORG (per-Org agenity workspace) to install into; none on hw288 (de-vcluster). Gated on the purchase funnel. ‖— |
| 220 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4111](https://github.com/openova-io/openova/issues/4111) [#4556](https://github.com/openova-io/openova/issues/4556) | chepherd's **solo agent is pre-configured with Claude Opus 4.7** + a working token (the agent is live and chat works). | — | ☐ | — |claude → 0 cluster-wide; 0 agenity/chepherd pods (region-a). No live agent with a working token exists. |
| 221 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | The user **chats with the chepherd solo agent** ("create a `<blueprint>` app in my org") → the agent calls the openova-mcp `create_application` tool with the user's **Org-scoped** token → a new Application CR is created in the user's Org. PASS = the agent reports the new app name + an Application appears provisioning in the user's `/apps`, with NO operator-console action. | hw288 | ◑ | hw288-2026-07-24: MCP agent — create_application tool PRESENT in owner tools/list with full inputSchema (required [blueprint,name]; org-guard: naming a DIFFERENT Org forbidden for Org-scoped caller). NOT invoked (read-only respected, 0 probe apps created — kubectl confirmed). Tool surface proven live; chat-driven invocation leg is founder-gated (#4277 Anthropic) ‖ hw288-2026-07-23: BLOCKED (founder-gated). create_application tool IS present in owner tools/list with full inputSchema (Org-scoped organizationRef guard: 'a value naming a DIFFERENT Org is forbidden for an Org-scoped caller'). But chat-driven |
| 222 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | The agent-created application **converges and appears in the user's Org** (chat-driven app creation works end-to-end). | — | ☐ | — |
| 223 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4553](https://github.com/openova-io/openova/issues/4553) | The chepherd agent's actions are **RBAC-scoped to the user's Org** (UI-parity): the openova-mcp `list_applications` returns ONLY this Org's apps (a 2nd Org's are filtered out), a cross-Org `get_application` → **403**, and the agent cannot create/mutate in any other Org — enforced by `openova-mcp/internal/identity` Org-claim, not just the chat prompt. | hw288 | ◑ | hw288-2026-07-24: MCP agent — fail-closed substrate LIVE: no-bearer tools/list→200 {tools:[]} (empty surface hides tools); no-bearer tools/call whoami→-32001 unauthenticated; get_application nonexistent→-32010/404 application-not-found. Cross-Org 403 filtering not exercisable single-Org. (edge wart: ~1/3 /mcp POSTs→envoy 404, retry-clears — follow-up, not pod fault) ‖ hw288-2026-07-23: MCP RBAC substrate LIVE + fail-closed: no-bearer tools/list → HTTP200 {"tools":[]} (empty surface hides tools), no-bearer tools/call whoami → -32001 unauthenticated 'no bearer'; get_application nonexistent → -32010/404. Org-s |
| 224 | convergence | [#4272](https://github.com/openova-io/openova/issues/4272) | Per-Org `bp-openclaw` controller reaches Ready (1/1) on a Sovereign — the idle-reaper can list its per-user pods and `readyz` passes. | hw290 | ❌ | hw290-2026-07-26: bp-openclaw never reaches Ready on hw290 — two independent, reproducible blockers depending on Org isolation. (1) vcluster-isolation Orgs (acme-corp, funnel, plan m): Kustomization/catalyst-tenant-acme-corp-apps → `HelmRelease/acme-corp/bp-openclaw dry-run failed: no matches for kind "HelmRelease" in version "helm.toolkit.fluxcd.io/v2"` — I port-forwarded into the acme-corp vcluster and `kubectl get crd` returns exactly ONE CRD (`httproutes.gateway.networking.k8s.io`); the Flux helm-controller CRDs are absent, so the ./vcluster/apps HelmReleases can never apply. (2) namespace-isolation Orgs (beta-corp/gamma-corp, plan s): `bp-openclaw` HR `False — dependency '<org>/bp-keycloak' is not ready`, and bp-keycloak is hard-blocked: `create Pod bp-keycloak-0 … forbidden: exceeded quota: plan-quota, requested limits.cpu=1,limits.memory=2Gi, used 1550m/2624Mi, limited 2/4Gi` (S plan quota structurally cannot hold the 6-chart per-Org bundle it installs). Even on an M-plan admin-door Org (delta-corp, quota 4/8Gi) bp-keycloak was still 0/1 after 8 min (1 restart, startup probe refusing) so openclaw never installed. No openclaw pod, no /readyz endpoint anywhere on the Sovereign. ‖ hw287-2026-07-23: Convergence healthy: region-A 63 HR Ready, region-B 60 HR Ready. bp-openclaw HR not present (no customer Org provisioned — only self-org hw287-omantel-biz). Controller-specific readiness not exercisable without a customer Org. | ‖ hw288-2026-07-26 walked live: acme-corp funnel Org has 0 bp-openclaw HelmReleases (cart=WordPress+BookStack). openclaw /readyz not walkable without a funnel Org whose cart includes openclaw. Gated (needs-a-specific-app).
| 225 | convergence | [#4278](https://github.com/openova-io/openova/issues/4278) | Per-Org `bp-newapi` HR reaches Ready on a fresh Org — the admin-promote post-install hook Completes (its ServiceAccount exists) so the chart finishes. | — | ☐ | — |
| 226 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The customer's PURCHASED app actually RUNS — every funnel Org's `tenant-<slug>-apps` Kustomization reconciles into a running pod (the app is reachable, not 404). | — | ☐ | — |

---

### Fresh-prov 2026-06-26 — session-fix acceptance (rows 227–239)

These 13 rows are the acceptance checks for the issues genuinely resolved this session. Each pairs the **root cause** with the **fresh-prov surface to walk** + the **expected evidence**. All start `☐` — the fresh-prov walk fills the verdict + evidence cells in. (Refs #4279.)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| 227 | delivery | [#4415](https://github.com/openova-io/openova/issues/4415) | A catalog/seed version bump reaches the live **Blueprint CR zero-touch** — bumping a `bp-*` pin in the catalog seed lands on the cluster Blueprint CR with NO manual `kubectl patch` and NO `resource-policy: keep` annotation pinning a stale version. PASS = after a seed bump, `kubectl get blueprint bp-<x> -o jsonpath='{.spec.version}'` matches the seeded version within one reconcile, the seeder owns the field (no `helm.sh/resource-policy: keep`), and the rolled chart renders on the cluster. | hw290 | ✅ | hw290-2026-07-26: Seed bump driven through the real console Edit-IaC surface (no kubectl write anywhere in this row). BEFORE 14:30:27Z: bp-alloy spec.version=1.0.3, rv=199151; annotations are ONLY meta.helm.sh/release-name + release-namespace — NO helm.sh/resource-policy: keep. Bumped the catalog seed 1.0.3→1.0.4 in catalog-sovereign/bp-alloy/blueprint.yaml → 'Committed to IaC ✓' at 14:30:48Z. Live CR at 14:30:48Z (first poll): spec.version=1.0.4, rv=207713 — ZERO-TOUCH, within one reconcile (Kustomization interval 1m). SEEDER OWNS THE FIELD: `kubectl get blueprint bp-alloy --show-managed-fields` → manager 'kustomize-controller', operation Apply, time 2026-07-26T14:30:41Z (helm-controller's own entry is stale at 12:32:40Z); GitRepository openova-catalog-sovereign artifact revision and Kustomization catalog-sovereign lastAppliedRevision both advanced to catalog-sovereign@sha1:608fa919…, i.e. the propagation path is Git→source-controller→kustomize-controller, not a manual patch. Reverted 1.0.4→1.0.3 through the same UI at 14:31:44Z and the CR followed back to 1.0.3 by 14:31:54Z. Chart-render side-check: HR flux-system/bp-alloy stayed Ready=True ('Helm install succeeded for release alloy/alloy.v1 with chart bp-alloy@1.0.2') and all 4 alloy DaemonSet pods stayed 2/2 Running throughout — spec.source.version (the chart pin) was intentionally not touched, so this walk proves the seed→CR zero-touch leg, not a chart-version roll. ‖ hw288-2026-07-25: keep-pin anti-pattern ABSENT — live Blueprint CRs bp-catalyst-platform/bp-redis/bp-alloy carry helm.sh/resource-policy=<empty>, Helm-managed by umbrella release 'catalyst-platform' (catalyst-system), so an umbrella upgrade WOULD update them zero-touch (no keep-pin blocking). Corrects the earlier 'resource-policy:keep makes seed inert' framing: the inertness of #5346 is purely that the umbrella on hw288 is still 1.4.1206 and the #5347 bump to 1.4.1210 has not rolled (merged != delivered) — NOT a keep-pin. Full zero-touch bump-propagation observation awaits the 1.4.1210 roll → stays PARTIAL. ‖  hw288-2026-07-24 NUANCE (from the #5346/#5347 investigation): the catalog-seed Blueprint CRs carry helm.sh/resource-policy:keep, so a seed VERSION BUMP does NOT reach an EXISTING live Blueprint CR zero-touch — Helm keeps (never updates) the CR. A bump reaches only (a) a FRESH prov's CR (created at the seed version on first install) or (b) after the umbrella is republished at a new version + the slot pin bumped (exactly the #5346→#5347 flow). So the 'zero-touch reaches the live CR' claim holds for fresh provs but is INERT for existing clusters by design (resource-policy:keep). Stays ◑ — partially true, env-dependent. ‖hw288-2026-07-24: misc agent — anti-patterns DISPROVEN: NO helm.sh/resource-policy:keep on any of 67 Blueprint CRs; bp-gitea owned by helm-controller via catalyst-platform (release v7, repeatedly zero-touch upgraded), spec.version=1.2.48; 67/67 HR Ready. The dynamic 'seed bump lands within one reconcile' needs a live catalog seed-bump (mutation) — not exercised  hw288-2026-07-25: ROOT-CAUSE confirmed live — a post-cutover version bump does NOT reach the Blueprint CR because the delivery mechanism is FROZEN. #5265 nailed: gitea-mirror + harbor-prewarm are ONE-SHOT cutover Jobs (ran once 2026-07-23 14:40, no recurring CronJob), so the local Gitea/Harbor stay at the cutover snapshot. Live proof: region-A bp-cilium stuck at 1.4.16 (local Harbor) vs 1.4.17 on ghcr. Zero-touch delivery works pre-cutover; post-cutover it needs a Day-2 recurring mirror/prewarm (#5265). ‖ | ‖ hw288-2026-07-26 walked live: 80 Blueprint CRs, 0 annotated helm.sh/resource-policy:keep → the zero-touch mechanism is READY (seeder owns the version field, no inert pins), but PASS requires OBSERVING a live seed-version bump propagate to the CR — a write not run on the north-star. Gated (needs-a-write).
| 228 | delivery | [#4435](https://github.com/openova-io/openova/issues/4435) | A **re-prov AFTER a wipe** does NOT false-fail on orphaned `catalyst-*` VPCs — the orphan-VPC janitor sweep removes the leftover VPC(s) from a prior wiped deployment before the new prov allocates its network. PASS = wipe an env, immediately re-prov on the same region; provisioning does NOT error `VPC already exists` / quota-conflict; the janitor log shows the orphaned `catalyst-*` VPC(s) swept, and the new prov reaches Phase-1 green. | hw286 | ◑ | hw286-2026-07-23: delivery: hw286 IS a re-prov AFTER the hw285 wipe — converged clean cc=true (no false-fail on the prior env residue); specific stranding-mode assertion is the #5193 scenario. | ‖ hw288-2026-07-26 walked live: the orphan-VPC janitor fix-site is present (products/catalyst/bootstrap/api/internal/providers/huawei/sweep_orphan_vpc_test.go + wipe_unbound_eip.go + provider.go). PASS requires wiping an env then immediately re-prov on the same region to OBSERVE the janitor sweep the orphaned catalyst-* VPC before the new prov allocates — a destructive wipe I will not run on the 214-✅ north-star. Gated (needs-a-wipe-cycle); verify on an expendable env.
| 229 | delivery | [#4409](https://github.com/openova-io/openova/issues/4409) [#4428](https://github.com/openova-io/openova/issues/4428) | **bootstrap-kit Kustomization Ready=True + plane-isolation admits CNPG→kube-apiserver egress** — on the fresh prov `kubectl -n flux-system get kustomization bootstrap-kit` is `Ready=True` (no orphaned `dependsOn` / CRD dry-run rejection), AND the plane-isolation NetworkPolicy permits the CNPG operator's egress to the kube-apiserver so Cluster admission/patch succeeds. PASS = bootstrap-kit `Ready=True`; CNPG operator pod Running; a `Cluster` reconciles (no `context deadline exceeded` / x509 webhook failure caused by blocked apiserver egress). | — | ☐ | — |
| 230 | delivery | [#4427](https://github.com/openova-io/openova/issues/4427) | **`ghcr-pull` reflects into a bare-slug per-Org namespace** — the emberstack reflector copies the `ghcr-pull` imagePullSecret into a per-Org namespace whose name is a bare slug (e.g. `org-<slug>` / a non-UUID form), so private `ghcr.io/openova-io/*` app images pull (no `ImagePullBackOff` 401-anonymous). PASS = a fresh Org's namespace has the reflected `ghcr-pull` secret; a per-Org app pod pulling a private image reaches Running (no 401/ImagePullBackOff). | — | ☐ | — |
| 231 | delivery | [#4411](https://github.com/openova-io/openova/issues/4411) | **bp-postgres singleton render carries the operator-probe NetworkPolicy** — a singleton (non-HA) bp-postgres render still ships the NetworkPolicy that admits the CNPG operator's health/readiness probe, so a singleton Postgres reaches Ready (the probe is not blocked). PASS = a singleton bp-postgres Application reaches `Ready=True`; the operator-probe NetworkPolicy is present in the rendered manifest; the CNPG operator can probe the instance. | — | ☐ | — |
| 232 | apps | [#4272](https://github.com/openova-io/openova/issues/4272) | On a fresh funnel Org, **openclaw `/readyz` returns 200** — the 4-layer chain (controller image · reaper apiserver egress NetworkPolicy · TLS trust · OIDC) all resolve so the idle-reaper lists per-user pods and readiness passes. PASS = `bp-openclaw` HR `Ready=True`, controller pod 1/1 Running, and `curl https://<openclaw-host>/readyz` → 200 (no `reaper: list pods … context deadline exceeded` loop). | hw290 | ❌ | hw290-2026-07-26: openclaw /readyz is unreachable on every Org on hw290. Fleet-wide `kubectl get hr -A ‖ grep openclaw` → only delta-corp + gamma-corp, both `False — dependency '<org>/bp-keycloak' is not ready`; zero openclaw pods anywhere; zero openclaw HTTPRoutes (`kubectl get httproute -A` shows only console/keycloak/agenity/wordpress routes). Live curl: https://openclaw.acme-corp.omani.homes/readyz → 000/000/404 (3 attempts), https://openclaw.delta-corp.omani.homes/readyz → 000. Root cause is the row-224 pair: vcluster-tier Orgs have no helm.toolkit.fluxcd.io/v2 CRD inside the vcluster; namespace-tier Orgs deadlock on the per-Org plan-quota blocking bp-keycloak-0. ‖ hw288-2026-07-24: openclaw deployable-confirmed — bp-openclaw published ghcr 0.2.17, chart has /readyz + JWKS-hairpin NetworkPolicy (#4272). Chart.yaml:61 notes a prior live /readyz 503 (TLS). Row needs a fresh FUNNEL ORG for the live /readyz-200 walk (gated). Stays ◑. ‖hw288-2026-07-24: misc agent — bp-openclaw blueprint present (0.2.17) but NOT instantiated (no HR/pod); /readyz walk needs a fresh funnel Org that installs openclaw — prereq absent (would require creating a customer Org) | ‖ hw288-2026-07-26 same as 224: beta-corp bp-openclaw HelmChart build-error (source torn down, #5364 orphan) → no pod → /readyz unreachable. Gated.
| 233 | apps | [#4422](https://github.com/openova-io/openova/issues/4422) | On a fresh funnel Org, **WordPress serves HTTP 200** — the mysql DB pod is Guaranteed-QoS (requests==limits, schedulable under the per-Org LimitRange), the HTTPRoute is present, and the landed host is pool-aligned to the console-ELB EIP. PASS = `curl https://wordpress.<slug>.<pool-tld>/` → 200 (or 302→install then 200), mysql pod QoS=Guaranteed + Running, HTTPRoute parents the console gateway, host resolves to the console-ELB EIP (not the primary ELB). | hw290 | ❌ | hw290-2026-07-26: WordPress does not serve on any funnel Org. The HTTPRoute IS present — `acme-corp/app-wordpress-hostroute → [wordpress.acme-corp.omani.homes]` (82m) and `walk-two/app-wordpress-hostroute → [wordpress.walk-two.omani.rest]` (23m) — but there is NO wordpress workload behind it: `kubectl get hr -A ‖ grep wordpress` → only delta-corp/gamma-corp `bp-wordpress-tenant False dependency bp-keycloak is not ready`; zero wordpress pods; zero mysql pods (so the Guaranteed-QoS mysql criterion is unevaluable). Live curl https://wordpress.acme-corp.omani.homes/ → 500 / 000 / 500 (envoy, no healthy upstream); https://wordpress.walk-two.omani.rest/ → 000. The funnel never deployed WordPress because of the row-86 PushOutOfDate git failure. ‖ — |
| 234 | apps | [#4307](https://github.com/openova-io/openova/issues/4307) | On a funnel Org **that provisioned Stalwart mail** (cart includes `stalwart-mail`, or a mail-bearing bundle e.g. pharmacy/legal/finance), `mail.<slug>.<pool-tld>/` serves HTTP 200 (SnappyMail SPA); `bp-stalwart-tenant` pod Running; webmail HTTPRoute present (no bare envoy 404). | hw290 | ❌ | hw290-2026-07-26: Both funnel Orgs carted `stalwart-mail` (cart JSON captured: apps[3] incl. stalwart-mail; review page listed `Stalwart Mail Email`), so the prereq is satisfied this time — but no mail landed. `kubectl get hr -A ‖ grep stalwart` → only delta-corp/gamma-corp `bp-stalwart-tenant False dependency bp-keycloak is not ready`; zero stalwart pods; no webmail HTTPRoute in any Org ns. Live curl https://mail.acme-corp.omani.homes/ → 404 / 000 / 404 (bare envoy 404, exactly the failure mode the row calls out). ‖ hw288-2026-07-24: misc agent — bp-stalwart-tenant blueprint present (0.1.13) but no pod/HR; no mail-bearing funnel Org; cannot walk mail.<slug>.<pool-tld> without a funnel Org carting stalwart-mail — prereq absent | ‖ hw288-2026-07-26 walked live via beta-corp: bp-stalwart-tenant HR 'not ready: unknown build error' (source torn down, #5364 orphan) → no stalwart pod → mail.<slug> not served. Gated (needs a healthy funnel Org that provisioned stalwart-mail).
| 235 | apps | [#4437](https://github.com/openova-io/openova/issues/4437) | On a fresh prov, **grafana SSO logs in 3/3 → 200** — the bp-sso-bridge per-tick credential refresh keeps the grafana OIDC client secret in lockstep with Keycloak, so the SSO round-trip completes. PASS = grafana `/login/generic_oauth` → Keycloak → back signed-in (200, user in the avatar) across 3 consecutive attempts; bp-sso-bridge refreshes the client cred each tick (no stale-secret 401). | — | ☐ | — |
| 236 | apps | [#4439](https://github.com/openova-io/openova/issues/4439) | On a fresh multi-region prov, **region-B keycloak reaches Ready** — the cross-region shared-pg write host is correct so region-B Keycloak connects to the primary Postgres and completes realm import. PASS = `bp-keycloak` HR `Ready=True` in the region-B cluster; keycloak pod Running; realm import succeeds (no DB-connect / wrong-write-host failure); `keycloak.<host>` → 302 SSO. | — | ☐ | — |
| 237 | spine | [#4416](https://github.com/openova-io/openova/issues/4416) | **spine → Continuum round-trip is Healthy + `continuumRef` is populated** — the spine adopts the Continuum and the round-trip reports Healthy. PASS = the Continuum CR reports `Healthy` (not Degraded), the spine's `continuumRef` field is populated with the live Continuum, and a witness round-trip completes. | — | ☐ | — |
| 238 | postgres | [#4282](https://github.com/openova-io/openova/issues/4282) | **Per-Org postgres-in-vCluster → host-side CNPG Ready** — a per-Org Postgres requested inside the Org's vCluster is reconciled by the host-side CNPG operator and reaches Ready. PASS = the per-Org `Cluster` CR is `Ready` (host-side CNPG drives it), the Postgres pod is Running with Guaranteed QoS, and the per-Org app connects (no empty-DSN / disableWait deadlock). | hw290 | ✅ | hw290-2026-07-26: #5376 merged-fix runtime proof, exactly as asked. Requested a per-Org Postgres in the customer Org acme-corp (vcluster-isolation Org) via a bp-postgres@0.2.13 HelmRelease (`uat238-orgpg`, instance.name=orgpg, singleton). Host-side CNPG operator (cnpg-system/cnpg-cloudnative-pg-cbb9dfbb9-snd8q) drove it: `kubectl -n acme-corp get cluster.postgresql.cnpg.io orgpg` → INSTANCES=1 READY=1 STATUS=`Cluster in healthy state` PRIMARY=orgpg-1; status.phase='Cluster in healthy state' readyInstances=1. Pod `orgpg-1` 1/1 Running. **`kubectl get pod -n acme-corp orgpg-1 -o jsonpath='{.status.qosClass}'` → `Guaranteed`** (resources requests==limits, cpu 500m / memory 512Mi). App connects, no empty-DSN/disableWait deadlock: `kubectl exec -n acme-corp orgpg-1 -c postgres -- psql -U postgres -tAc "select 'PGCONNECT_OK', version();"` → `PGCONNECT_OK‖PostgreSQL 16.14`. Side observation (separate operator-input bug, not this fix): a pre-existing bp-postgres install in the same ns (`uat-ahs-hw290-rtz-a`, mode=active-hot-standby with topology.primary.region left empty) renders `nodeAffinity openova.io/region In [""]` while nodes carry `openova.io/region=hw-me-east-215-rtz-prod`, so its pod sits Pending forever — its QoS is ALSO `Guaranteed`, so the #5376 fix holds on both instances. ‖ hw288-2026-07-24: misc agent — host-side CNPG operator PROVEN (drives 8-9 Clusters to Ready). The exact 'per-Org postgres requested INSIDE the Org vCluster' path has no instance — self-org is namespace-isolated (no vCluster, spec.vclusterName empty), no per-Org postgres app; needs a funnel Org with postgres-in-vCluster | ‖ hw288-2026-07-26 walked live via beta-corp (the admin-door Org that HAS a per-Org Postgres, unlike acme-corp): CNPG Cluster `bp-newapi-newapi-pg` phase='Cluster in healthy state' readyInstances=1/1 (host-CNPG-driven) + bp-newapi connects (admin-promote job Complete, no empty-DSN/disableWait deadlock) → 2 of 3 PASS criteria met. BUT the postgres pod `bp-newapi-newapi-pg-1` runs **Burstable QoS, not Guaranteed** (238 requires Guaranteed — a DB pod should be Guaranteed to resist node-pressure eviction). Sub-gap: the per-Org CNPG resource config doesn't set requests==limits. Stays ◑ on the QoS criterion; the core per-Org-Postgres→host-CNPG mechanic is PROVEN working.
| 239 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | **Crossplane adoption populates Observe-only on the fresh prov** — `providers.pkg.crossplane.io` and `cloudadoptions.compose.openova.io` are non-empty and reach Synced+Ready, observing (NOT re-provisioning) the OpenTofu-built infra. PASS = `kubectl get providers.pkg.crossplane.io` → ≥1 Healthy; `kubectl get cloudadoptions.compose.openova.io -A` → non-empty Synced+Ready with `external-name` = the real ELB id; the live ELB/nodes are untouched (Observe-only). | — | ☐ | — |
| 240 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | No-nodePort gateway serves externally on a FRESH zero-touch prov: ELB → node:443 hostPort → hostNetwork envoy; gateway Service carries ZERO nodePorts (§854 literal). | — | ☐ | — | jq type==NodePort → []); external no-nodePort serve auth→302 gitea→303 console→200 marketplace→200 (inbound during step-08 deny-egress hold) |
| 241 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | Mothership writes `ready` ONLY after an external HTTP answer from https://console.<fqdn>/ (no more false-green). | — | ☐ | — |
| 242 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | 2-region BCP admission floor: a 1-region POST with no explicit bcpTopology is rejected at the LIVE production API. | — | ☐ | — |
| 243 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | Tenant DNS split-horizon on a fresh prov: wildcard hosts → primary ELB EIP, console → console ELB EIP. | — | ☐ | — |
