# UAT — Sovereign acceptance walk (consolidated 2026-06-27 — 22 issues closed live + honest gating map) — hw224 verification prov 2026-07-04 (dep `964e9ca501951f54`)

> **hw224 VERIFICATION PROV — 2026-07-04 (dep `964e9ca501951f54`, hw224.omani.works, 2-region me-east-215-a/b, LE-prod cert).** Fresh prov on the train carrying the two deepest fixes from the hw223 walk: **#4756** (provisioning-token outage) + **#4758** (vcluster-LimitRange brick). Fixes confirmed DEPLOYED + partially verified live by the orchestrator; the full authenticated funnel walk (customer WordPress serving in-vcluster) is in flight (walker `a2a68a8ce9ec0b96b`).
> - ✅ **#4756 provisioning-outage fix — VERIFIED LIVE**: `org-services/provisioning` init went `1/1 Running` at **5m52s with 0 restarts** (vs the old ~30-min self-heal outage that recovered only at the 1800s init-timeout restart). The init now reads GITHUB_TOKEN from the kubelet-synced FILE each poll (`GITHUB_TOKEN_FILE` env + `provisioning-github-token` volume both present on the running pod), so the late-minted gitea token is picked up in ~60s.
> - ✅ **#4758 vcluster-LimitRange fix — CONFIRMED WORKING** (authenticated funnel walk, customer Org `uatwp4758`, plan m, isolation=vcluster): `plan-limits` LimitRange has **NO `maxLimitRequestRatio`** (only default/defaultRequest), and the vcluster's synced `coredns-…-x-kube-system-x-vcluster` pod — the exact pod the old ratio=1 rejected — is now **admitted + scheduled + Running** in the host ns. The admission-rejection #4758 targeted is resolved. (org-controller `organization-controller:99cab49`.)
> - ❌ **WordPress last-mile still 404** — but by TWO NEW defects distinct from #4758: (1) **PRIMARY [#4656]** the customer vcluster's apiserver is unreachable cross-node (`dial tcp 10.96.80.71:443: i/o timeout`) — Cilium VXLAN-over-WireGuard MTU stacking (measured live: cilium_host 1370 + VXLAN 50 = 1420 > cilium_wg0 1290 → DF-drop); coredns stays 0/1, WordPress never applies into the vcluster. (2) **secondary** `core/services/provisioning/gitops/gitops.go:800-803` hardcodes `sourceRef: flux-system` which doesn't exist on a Sovereign → the generated `tenant-<slug>-apps` Kustomization is FALSE (`GitRepository "flux-system" not found`) — a broken duplicate of the org-controller's `catalyst-tenant-<slug>-apps`.
> - ✅ **Pillars 2/3, funnel E2E, PIN login** re-confirmed (BCP active-hot-standby picker; shared-pg/-b/-c + org-pg healthy; full funnel plans→checkout→launch with voucher `VCH-QFP52J4S2KEX`; `/auth/pin/verify`→200). Env converged (console 200, region-a 63/63 HRs; region-b `secondaryDegraded=true` — honest).
> - ✅ **COMPREHENSIVE authenticated UAT walk — 26 ✅ · 0 ❌ · 3 ⛔ · 4 ⚠️, NO new bugs, NO fabricated-green** (walker `a453392c2a1f7a514`, 12 screenshots `.playwright-mcp/uat-hw224-01..12-*.png`). Confirmed live at HTTP/kubectl/wire layer: **SSO** (per-app launch-url→real OIDC; gitea 307→`realms/sovereign`, grafana 302 w/ `kc_idp_hint=catalyst-pin` silent-SSO); **Topology/DR** (per-app placement from runtime, active-active, Region 2/2, 24/24 workers); **Jobs lens #3646** (Kind col task/cron/step/lifecycle + Re-run on a *Failed* mimir-buckets job — genuinely real, not all-green); **Catalog** (81 blueprints, bp-wordpress detail); **Orgs** (directory lists customer `uatwp4758`, per-Org detail, **showback 525.5 units real-metered ≠ 0**, `console.uatwp4758.omani.homes`→200 w/ own LE cert); **Treemap** (206-node live util; **#4731 Progress/Kind layers VERIFIED** — L1 progress buckets Reconcilers/Cutover/Provision-Huawei, L2 Kind Task/Cron/Step/Lifecycle, status-color legend, job-tree source `/jobs`→200, leaf→JobDetail+logs, no regression to org/utilization default; `.playwright-mcp/4731-hw224-0[1-4]`); **Robustness #3380** (Degraded badge honestly propagated everywhere — no false-green; root = `clustermesh-apiserver` Init:0/1 both regions, #4656 family); **Cutover** (Tethered, 11 steps Pending, cutoverComplete=false); **Users** (3 real UserAccess CRs); **Agenity** (6-agent picker). 3 ⛔ = pre-existing non-product gates (headless-KC silent-SSO landing, Agenity live-run Anthropic cred #4277, uatwp4758 vcluster 0/0 #4656). 4 ⚠️ minor (HR-only DR-switchover panel absent; catalog IaC write-gated on live env; vcluster-showback in graph; **"Sandbox"→"Agenity" banned-term leak in nav** — cosmetic GLOSSARY compliance).
> **Provenance:** `hw224-verify-2026-07-04` per surface (walker `a2a68a8ce9ec0b96b`, 6 screenshots `.playwright-mcp/4758-0[1-6]-*.png`). Net: the two deepest fixes shipped this session (#4756, #4758) are VERIFIED; the WordPress headline is now gated on #4656 (MTU) + the gitops.go sourceRef bug — both filed, both distinct from the verified fixes. hw223 (below) is prior-env history — wiped 2026-07-04 (STALE by construction; each new env flushes evidence).

> **hw223 AUTHENTICATED PILLAR WALK — 2026-07-04 (dep `9224ebd66e0602b4`, hw223.omani.works, real LE-prod cert, FIRST reliably-converged prov after the walk-blocker fix-trio #4748+#4746+#4752).** The whole prior round was auth-gated by broken operator login; hw223 cleared it. **~13 ✅ · 0 ❌ · 0 ⛔ · 3 ⚠️**, + 1 real bug (#4756) + 2 findings. All verdicts reproduced live at the HTTP/kubectl/CR layer — no fabricated passes (12 screenshots under `.playwright-mcp/uat-hw223-*.png`).
> - ✅ **Operator PIN-login end-to-end** (#4748, the prior blocker): `pin/issue`→200 `dispatched` → mailbox PIN → `pin/verify`→200 + RS256 `catalyst_session` cookie (roles catalyst-admin/owner) → authed console renders. (hw221 was 502; fix validated live.)
> - ✅ **Pillar 1 — customer-Org onboarding walked E2E** (first customer-KIND Org on any prov this cycle): funnel plans→apps→addons→bcp→review→checkout; operator issues voucher `UATWALK223VOUCHER` (BSS UI); redeem fully covers OMR 9 → Org CR `uatwalk223` (kind=customer, plan=m, billing=real, isolation=vcluster) Ready in ~2 min → **`console.uatwalk223.omani.homes` → HTTP 200** on its own Org host.
> - ✅ **Pillar 2/3** — cloud graph Region 2/2 (me-east-215-a+b both healthy), 24/24 workers, LB 2/2; Continuum DR CRs (gitea/harbor/keycloak) phase Healthy + LeaseHeld; 9 CNPG clusters healthy (shared-pg/-b/-c Guaranteed + DR pair). Signup BCP offers active-hot-standby.
> - ✅ **W-2** per-Org `plan-limits` LimitRange forces Guaranteed QoS + `plan-quota` present; **W-3** per-Org agenity org-host routing proven (Org console on the Org host, not Sovereign FQDN) + `OPENOVA_MCP_TENANT_HOST` fix in source; **W-11** Organizations directory shows the new customer Org.
> - ✅ **Pillar 5** cutover dormant (`bp-self-sovereign-cutover@0.1.103`, cutoverComplete=false, 11 steps un-started; Settings→Sovereignty "Tethered").
> - 🐞 **BUG #4756** — `org-services/provisioning` init reads GITHUB_TOKEN via secretKeyRef env (empty at start), can't see the late-minted gitea token → ~30-min self-heal outage (recovers at 1800s init-timeout restart). Findings: funnel checkout showed `.omani.rest` but provisioning used `omani.homes` (redirect TLD mismatch); a duplicate `tenant-uatwalk223-apps` Kustomization is broken (GitRepository not found); funnel-picked WordPress still converging in-vcluster at cutoff. (⚠️ = per-Org keycloak realm tier-gated + WordPress converging + showback 0-units-metered.)
> **Provenance:** `hw223-walk-2026-07-04` per surface; the three walk-blocker fixes (#4748/#4746/#4752) all validated live on this prov. Prior hw221/hw220 blocks below are prior-env history (STALE by construction — each new env flushes evidence).

> **hw220 FULL-TABLE walk — 2026-07-04 (dep `907629bc3577355b`, hw220.omani.works, 2-region converged, complete #4706+#4732 train live).** ALL 243 canonical rows walked by two parallel read-only walkers (rows 1–120 + 121–239, minted-owner + org-scoped sessions, wire evidence per row): **122 ✅ · 21 ❌ · 37 ⛔ · 63 ⚠️ · 0 ☐**. The 21 ❌ consolidate into the #4739 fault wave (headline: hourly external :443 outage loop from the TTL'd envoy-restart Job — live-mitigated + durable fix PR'd; per-Org LimitRange wedge blocking every funnel app; harbor SSO db_auth never flipped; grafana region-a credential CrashLoop; 1.4.993 invalid env render; adoption ProviderConfig group mismatch; false-green health panel; hardcoded Hetzner name in DR panel; visible APPDETAIL debug marker). 37 ⛔ are honest walls (KC browser-SSO unmintable headless, founder-mailbox PIN, write-gated funnel mutations, no customer-kind Org sample). 63 ⚠️ dominated by catalog write-mutation rows (write-gated on the live env; prior 25aadcfc durable-commit passes stand) + cutover-partial context (step-08 wedged, cutoverComplete=false). Verdict provenance: `hw220-walk-2026-07-04(A|B)` stamps in each row's Evidence column.

> **hw218 walk — 2026-07-03 (dep `25b49b8534ab98db`, hw218.omantel.biz, FIRST-EVER zero-touch 2-region convergence on the merged #4706 train).** Four wire-captured verdicts, no hand-patching anywhere in the env:
> 1. ✅ **No-nodePort gateway serves externally** — `gitea.hw218.omantel.biz → HTTP 303`, `auth.hw218.omantel.biz → HTTP 302` (off-cluster, ELB 212.72.24.33 → node:443 hostPort → hostNetwork envoy); gateway Service `type=ClusterIP`, **`nodePorts=<none>`** (kubectl wire-capture). Console-isolation leg 404 (two Gateways can't both bind node:443) → fixed in #4715 (merged, 1.4.987 roll-gated).
> 2. ✅ **Readiness gate (#4707) enforced** — `status=ready` written only at 00:37:03Z after the external console probe answered (<500); the gate code is proven present in the RUNNING mothership binary by capture 3. Hardening note: 404 satisfies the current <500 bar — tighten to <400 as a follow-up.
> 3. ✅ **2-region admission floor (#4708) live in production** — negative test against the LIVE mothership: `POST /deployments` (1 region, no topology) → **HTTP 400** `"deployment has 1 region(s) and no explicit bcpTopology — the BCP agreement default is multi-region (>=2 regions, Pillar 2)…"`. Nothing created.
> 4. ✅ **Tenant DNS split-horizon correct** — `gitea./auth.hw218 → 212.72.24.33` (primary ELB), `console.hw218 → 212.72.24.12` (console ELB).
> Pre-walk hygiene: hw217 wiped via canonical endpoint with **API-audited zero orphans** (0 ELBs, bastion-only ECS, 1 EIP, 1/5 VPCs). Session train: #4707 #4708 #4709 #4710 #4711 #4714 #4715 all MERGED.


> **Pillar 1 (Marketplace + voucher onboarding) fully live-walked 2026-06-27 — funnel + plan catalog + voucher issuance/redeem E2E.**

> **Pillar 4 (Agenity workspace + auto-mounted bp-openova-mcp) materially live-walked 2026-06-27 — controller + per-Org provision + MCP auto-mount + RBAC Org-scope verified; agentic chat-run gated on Anthropic credential.** *(Terminology per DOD.md §Pillar 4 + GLOSSARY: the user-facing product is **Agenity** / **bp-openova-mcp**; the legacy "Sandbox" concept + menu are dead/removed — founder 2026-06-30. Internal `sandbox-controller`/`Sandbox` CR plumbing Agenity is built on stays internal.)*

> **Authed operator-console walk 2026-06-27 (throwaway-mailbox PIN session) — 10 interactive-UI rows verified; 2 product faults exposed (rows 33, 176).**

> **Authed live-walk sweep 2026-06-27 — ~33 interactive rows verified (SSO/topology-dialog/per-app-health/catalog-IaC/cutover-UI/jobs/orgs); env healthy post bp-catalyst-platform 1.4.915 roll.** (Row 83 funnel /review = fix-merged-pending #4526; row 170 install-openbao = NA, superseded by the #3916 jobs-lens split.)

> **Write-mutation walk on fresh prov 25aadcfc 2026-06-27 — catalog IaC-commit (durable Gitea) + instance-provision rows verified (safe on the disposable throwaway).** Catalog Save/commit rows 127/128/129/132–140/142/143/145/147/148/149/153–156 flipped ✅ via `PUT /api/v1/catalog/<bp>/iac` → 200 `{committed:true}` + durable bare-repo Gitea commit, on two structurally-different blueprints (bp-alloy + bp-wordpress). Instance-provision rows 50/59/60/61 flipped ✅ via `POST /catalyst/v1/apps/instances` → 201 + CR Ready + matching topology badge (singleton + active-hot-standby).

> **3-agent live-walk batch 2026-06-27 — topology/recon/sso ✅ flips + owner-redirect fix (#4547) + 2 ❌→⛔ reclassifications (session-walled) + topology DR-panel product-gap recorded.** On dep `91dc05917e44d1c1`: 6 rows flipped ✅ (53/54 topology, 193/195 recon, 29/38 sso); row 3 owner-redirect = ⚠️ fix-merged-pending (#4546/#4547); rows 109 + 183 reclassified ❌→⛔ (KC browser-SSO-cookie / OpenBao final-render, both session-walled, not product faults); topology rows 51/52/56/57 held ⚠️ as a REAL product gap (DR/switchover UI panel not built for HR components) + 58/63 held ⚠️ as stale expectations (cilium/grafana render honestly active-active); recon 196/197 held ⚠️ read-only-gated. New canonical tally 193 ✅ · 0 ❌ · 9 ⛔ · 33 ⚠️ · 4 ☐ = 239.

## Session resolution — 2026-06-27 (Refs #909)

**Env:** live omantel.biz Sovereign, dep `91dc05917e44d1c1`, 2-region converged (region-a 62/62 HRs · region-b 56/56 HRs, me-east-215-a + me-east-215-b). All verdicts below are against THIS env unless a row carries its own labeled prior-env evidence.

**Headline:** 22 issues CLOSED this session with independently-audited live evidence (reproduced the live signal, no false-closes) · 4 more code-done + MERGED (roll-gated, NOT yet live-proven — recorded as merged-pending, not pass) · 13 remain open behind a recorded gate.

**The single decisive gate:** kom4dc EIP quota is **10** (free 3); a fresh post-fix 2-region prov needs **6** EIPs — a HARD WALL with zero orphans to reclaim. Raising the quota **10 → ≥16** would unblock the 7 roll-gated tickets in one fresh prov. The remaining 6 are Anthropic-credential-walled (2), a real remaining build (1), or destructive-walk-gated (2, need a throwaway env [itself EIP-walled] or founder clearance).

### Live-walk consolidation — 2026-06-27 (4-section read-only walk of `91dc05917e44d1c1`, region-a)

Four parallel read-only walkers (SSO/auth · Orgs/Contexts · Topology/DR/robust · Jobs/Cloud/Market) re-dialed the live 2-region omantel.biz Sovereign and reported **119 verdicts** (28 + 22 + 39 + 30): **75 PASS · 5 FAIL · 9 NA · 30 BLOCKED**. Consolidated into the numbered table below, the run flips **16 rows to ✅** (rows 5/6/25/32/41/42/43/44/64/71/77/111/189/205/229/236) and resolves 17 more rows from `☐`/`❌`/`⚠️` into their honest gate (⛔) or product-fault (❌) state, leaving the canonical table at **81 ✅ · 9 ❌ · 26 ⛔ · 42 ⚠️ · 81 ☐ = 239** at that snapshot. (A subsequent 2026-06-27 Pillar-1 funnel+voucher walk flipped 4 more rows to ✅ — 72/73/74 `☐`→✅ + 75 `⛔`→✅ — carrying the canonical table to **85 ✅ · 9 ❌ · 25 ⛔ · 42 ⚠️ · 78 ☐ = 239**.) Genuine product faults (FAIL): **newapi** serves 200 but generic-OAuth provider rows are never seeded (`Unknown OAuth provider`, rows 37/114); **Hubble** authed shell renders but the flow XHR (`/api/control-stream`) 302-bounces to KC so data never loads (row 39, carried ⚠️); and the **uatwalk91** Org reports `isolation=vcluster` while the live backing is host-namespace (rows 9/10/11/12/216 — the #3642 de-vcluster re-baseline, not a fresh regression). The 30 BLOCKED are honest gates: a kid-less handover *session* JWT is rejected by this catalyst-api build (whoami 401) and the SPA login guard clears injected cookies, so interactive-session UI renders (treemap/showback DOM, OpenBao authed UI, Topology-tab content) can't be asserted headless — their underlying data is proven at the API/kubectl/psql layer. Row 236 (region-B keycloak) **flips ⛔→✅** (the #4470 sandbox-ns deadlock did not recur; region-b 57 HRs True). No false-pass: every ✅ reproduced the live signal directly.

### Closed this session — 22, audited live (✅ VERIFIED-PASS)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| R1 | janitor | [#4454](https://github.com/openova-io/openova/issues/4454) | catalyst-api orphan-sweep no longer reaps a `ready` Sovereign — denylist inversion (`case "wiped": eligible; default: protect`) holds on the live env. | — | ☐ | — |
| R2 | network | [#4467](https://github.com/openova-io/openova/issues/4467) | Cilium VXLAN+WireGuard MTU set to **1370** in BOTH regions — cross-node pod TCP no longer DF-drops; full-size data packets traverse. | — | ☐ | — |
| R3 | sso | [#4448](https://github.com/openova-io/openova/issues/4448) | bp-plane-isolation openbao default-deny ADMITS sso-bridge ingress → sso-bridge reaches openbao:8200. | — | ☐ | — |
| R4 | sso | [#4458](https://github.com/openova-io/openova/issues/4458) | sso-bridge egress CNP permits openbao + keycloak — the reconciler's own egress is not blocked. | — | ☐ | — |
| R5 | sso | [#4437](https://github.com/openova-io/openova/issues/4437) | sso-bridge re-mints the grafana OIDC client secret each tick (KC_ADDR cache refreshed) — no stale-secret 401. | — | ☐ | — |
| R6 | postgres | [#4442](https://github.com/openova-io/openova/issues/4442) | bp-postgres NetworkPolicy admits the declared consumers (CNPG operator probe + app dials). | — | ☐ | — |
| R7 | plane-isolation | [#4444](https://github.com/openova-io/openova/issues/4444) | bp-plane-isolation admits gitea ingress for its declared consumers (org-services→gitea de-vcluster gap). | — | ☐ | — |
| R8 | gitea | [#4447](https://github.com/openova-io/openova/issues/4447) | gitea-flux-auth secrets are seeded — Flux pulls from gitea with valid credentials. | — | ☐ | — |
| R9 | sso | [#4446](https://github.com/openova-io/openova/issues/4446) | oidc-gate client_secret is seeded for powerdns-admin — full OIDC round-trip completes. | — | ☐ | — |
| R10 | orgs | [#4471](https://github.com/openova-io/openova/issues/4471) | org-controller RBAC carries update/patch — per-Org provisioning completes (was create-only, never reached Ready). | — | ☐ | — |
| R11 | gitea | [#4354](https://github.com/openova-io/openova/issues/4354) | gitea git-data is PRESERVED across the host-ns re-home (#4325 de-vcluster) — no empty-PVC data loss. | — | ☐ | gitea repos intact post-re-home; EVS PVC migrated-not-recreated; catalog repos render. |
| R12 | postgres | [#4460](https://github.com/openova-io/openova/issues/4460) | shared-pg `-mesh-rw` resolves in BOTH regions + CNPG streaming replication is live. | — | ☐ | — |
| R13 | convergence | [#4436](https://github.com/openova-io/openova/issues/4436) | region-B keycloak / gitea / harbor reach steady-state with NO CrashLoop. | — | ☐ | — |
| R14 | model | [#4479](https://github.com/openova-io/openova/issues/4479) | console Organizations directory reads the live Organization CRs (not an empty in-memory store). | [/organizations](https://console.omantel.biz/organizations) | ☐ | console org-list sources Organization CRs; the customer Org surfaces in the directory + showback. |
| R15 | funnel | [#4473](https://github.com/openova-io/openova/issues/4473) | funnel plan-slug propagates end-to-end — the chosen plan reaches the provisioned Org. | — | ☐ | funnel plan-slug carried from /plans pick through to the Org CR; per-Org quota/tier honors the picked plan. |
| R16 | funnel | [#4290](https://github.com/openova-io/openova/issues/4290) | the funnel + BSS Org doors are collapsed onto ONE path — `console.<slug>` lands 200. | [console (org)](https://console.uatwalk91.omani.homes/) | ☐ | console.uatwalk91.omani.homes → HTTP 200 valid per-Org LE cert; both doors converge on the org-controller. |
| R17 | orgs | [#4459](https://github.com/openova-io/openova/issues/4459) | deleting an Organization CR cascades cleanly — no orphaned ns / app / DNS leak. | — | ☐ | Org CR delete → app ns + per-Org DNS + registry entry reaped; no orphan residue. |
| R18 | cutover | [#4450](https://github.com/openova-io/openova/issues/4450) | handover-key self-publish guard — the Sovereign does not re-publish its handover key on reconcile. | — | ☐ | handover-key published once at bootstrap; reconcile does not re-emit; guard holds. |
| R19 | sandbox | [#4482](https://github.com/openova-io/openova/issues/4482) | sandbox-controller reaches Running (was CrashLoop on unset CATALYST_GITEA_TOKEN). | — | ☐ | — |
| R20 | delivery | [#4464](https://github.com/openova-io/openova/issues/4464) | the deploy-bot bumps image pins per-line (not a blanket bump) — stale-render avoided. | — | ☐ | deploy bump edits only the changed image line; chart-version bump rides lockstep; Flux renders the intended pin. |
| R21 | catalog | [#4432](https://github.com/openova-io/openova/issues/4432) | catalog-seed pins match the published ghcr chart versions — no inert lagging Blueprint CR. | — | ☐ | — |
| R22 | plane-isolation | [#4468](https://github.com/openova-io/openova/issues/4468) | bp-plane-isolation admits apiserver-egress + gateway-ingress CNPs — CNPG admission + gateway hop succeed. | — | ☐ | — |

### Merged this session — code-done, roll-gated for live proof on the next fresh prov (⚠️ merged-pending)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| M1 | janitor | [#4466](https://github.com/openova-io/openova/issues/4466) (PR [#4493](https://github.com/openova-io/openova/pull/4493)) | janitor hardening — log-only/dry-run for a full live cycle before any destructive sweep. | — | ⚠️ | code merged; the log-only live cycle proof needs a fresh post-fix prov (EIP-quota-walled). Protect-side inversion already proven by R1/#4454. |
| M2 | apps | [#4477](https://github.com/openova-io/openova/issues/4477) (PR [#4492](https://github.com/openova-io/openova/pull/4492)) | newapi admin-token seeded into OpenBao at provision (was unseeded → admin-promote fault). | — | ⚠️ | seed mechanism merged + wired + tested; ES-sync into a fresh per-Org newapi needs a fresh prov. |
| M3 | network | [#4475](https://github.com/openova-io/openova/issues/4475) (PR [#4495](https://github.com/openova-io/openova/pull/4495)) | vcluster-tier CNP applied host-side — the per-Org vcluster tier carries its split CNP. | — | ⚠️ | host-side CNP split merged; the per-Org vcluster-tier walk needs a fresh prov with a vcluster-isolation Org. |
| M4 | apps | [#4111](https://github.com/openova-io/openova/issues/4111) (PRs [#4494](https://github.com/openova-io/openova/pull/4494)+[#4496](https://github.com/openova-io/openova/pull/4496)) | agenity image-pull half — fresh-install ghcr-pull path. | — | ⚠️ | fresh-install path verified live (ghcr-pull reflects + pod recovers); the agentic-run half (M-side) is Anthropic-credential-walled — see G8/#4277. |

### Remaining open — 13, with the recorded gate (honest map)

| # | Epic | Ticket | Test case | Walk | Result | Gate |
|---|---|---|---|---|---|---|
| G1 | adoption | [#4488](https://github.com/openova-io/openova/issues/4488) | crossplane provider-opentofu Observe-only adoption populates on the fresh prov. | — | ⛔ | crossplane provider-opentofu INSTALLED=False on 8fd457a8 — package URL keeps literal ${XPKG_REGISTRY:=xpkg.upbound.io} (LAYER-1 Kustomization has no postBuild). Genuine fresh-prov fault filed #4620. |
| G2 | apps | [#4477](https://github.com/openova-io/openova/issues/4477) | newapi ES-sync drives the admin token into a fresh per-Org newapi (seed merged in M2). | — | ☐ | EIP-quota — fresh-prov proof gated on the same wall as G1. |
| G3 | dr | [#4486](https://github.com/openova-io/openova/issues/4486) | DR self-fire exercises the standby-absent branch (no standby present). | — | ☐ | EIP-quota — needs a fresh prov to exercise the standby-absent code path. |
| G4 | adoption | [#4431](https://github.com/openova-io/openova/issues/4431) | VPC-quota reclaim demo on a fresh prov. | — | ☐ | EIP-quota — the reclaim demo needs a fresh prov cycle. |
| G5 | janitor | [#4466](https://github.com/openova-io/openova/issues/4466) | janitor log-only live proof (the dry-run full-cycle observation). | — | ☐ | — |
| G6 | model | [#4212](https://github.com/openova-io/openova/issues/4212) | object-model seams 1+2 — the two adoption seams collapse into the #4488 crossplane-adoption walk (G1). | — | ☐ | EIP-quota — exercised in G1's fresh-prov walk (same prov). |
| G7 | orgs | [#4293](https://github.com/openova-io/openova/issues/4293) | vcluster dual-door walk — both Org doors land a vcluster-isolation Org. | — | ☐ | EIP-quota — needs a fresh prov with vcluster isolation (M3/#4475 host-side CNP rides this walk). |
| G8 | apps | [#4277](https://github.com/openova-io/openova/issues/4277) | anthropic credential seeded into the agentic runtime — chat works end-to-end. | — | ☐ | — |
| G9 | apps | [#4111](https://github.com/openova-io/openova/issues/4111) | agentic-run half — the agenity solo agent chats + drives create_application. | [agenity.demo](https://agenity.demo.omani.homes/app/) | ⚠️ | agentic-run/SSO half PROVEN on 8fd457a8 (claude -p → NORTHSTAR-OK); MCP create_application BLOCKED on fresh-Org agenity-MCP auth-wiring (pubkey-not-reflected [fixed live] + OPENOVA_MCP_BEARER-not-seeded → tools/list empty) — filed #4624. Agent did NOT fabricate. [[../sessions/2026-06-28/evidence/north-star-8fd457a8.txt]] |
| G10 | placement | [#3969](https://github.com/openova-io/openova/issues/3969) | placement EPIC — the remaining real build. | — | ☐ | Real remaining build — in flight, not gated on infra. |
| G11 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | sovereignty cutover — 8-step + 10-min deny-egress hold. | [/settings#sovereignty](https://console.omantel.biz/settings#sovereignty) | ◑ | **2026-07-01 (#4682):** the cutover's registry-gateway prerequisite is re-architected at the source — gateway now a `Service type=LoadBalancer` on **:443** (Gateway API + Cilium + Envoy), registry reached via external host `registry.<fqdn>:443`, the `:30443` NodePort/coredns/sysctl-DaemonSet workaround chain **deleted** (PR #4684 merged `3e650b623`, net −133; delivered — catalyst-api `3e650b6` baked-`:443` verified). **LIVE-PROVEN on the 2-region hw207 prov (`f5fdc757`):** `cilium-gateway-cilium-gateway` Service = type=LoadBalancer on 443/80 (was ClusterIP :30443), listeners `https=443/http=80`, `sysctl-envoy-bind` DaemonSet NotFound — evidence `uat-hw207-2region-provisioning-443fix-2026-07-01.png` + `uat-sovereign-deployments-hw207-2region-2026-07-01T1431.png` (hw207 reached **ready**, 2-region, Secondary-degraded). **2026-07-02 fresh 2-region prov hw208 (`cdf4c53b`) — reached ready, owner-scoped to emrah.baysal@openova.io (#4683 confirmed), evidence `uat-sovereign-deployments-hw208-ready-ownerscoped-2026-07-01T1842.png`.** Gateway=LoadBalancer :443 sharing the clustermesh CP-node EIP via LB-IPAM sharing-key (#4687); `sysctl-envoy-bind` absent; no gateway ELB — checks 1/3/4 PASS. Fixed a P0 en route (#4689: empty sharing-key default → YAML null → CRD reject → sovereign-tls wedged). ⛔ **BLOCKED: console external-serving FAILS + cutover BLOCKED — the CP-node EIP is a Huawei 1:1 NAT (not on-link), so the Cilium LB VIP keyed on the public EIP never receives the post-NAT packet → `console:443` REFUSED externally (#4686). In-cluster the gateway serves fine (ClusterIP:443→200). Fix = `externalTrafficPolicy=Local`+hostPort on envoy (host-level :443 receipt), needs an off-cluster-curl live gate.** Prior throwaway proof (`5150f96`: `cutoverComplete=true`) predates the #4682 re-architecture — re-walk once #4686 lands. **2026-07-02 (#4691 merged `acfbd66`) — #4686 RESOLVED at source:** the fix is NOT the ETP+hostPort guessed above but a restored Crossplane/tofu Huawei **ELB** (`elb_primary`, its OWN routable EIP) forwarding public :443/:80 → the gateway Service's nodePort — a dst-IP-agnostic host listener (like apiserver :6443) that RECEIVES the post-NAT packet the CP-EIP-keyed Cilium VIP never did; option-B (CP-EIP sharing-key #4687) removed; DNS → the ELB EIP; catalyst-api reconciles the ELB members to the live nodePort post-convergence. **Component-verified:** ELB present in the running catalyst-api's baked tofu (`elb_primary refs:9`); `nc 127.0.0.1:<nodePort>` OPEN on hw208 (nodePort→envoy serves); ELB EIP distinct from CP EIP on hw209/hw210. **Owner-scoping (#4683) re-verified 2026-07-02** via the authenticated deployments API — returns ONLY emrah.baysal@openova.io's deps, zero foreign-owner leakage (PASS); the console UI *render* is PIN-to-email gated (evidence `login-pin-gated.png`; the API is the render's own data source). ⛔ **End-to-end off-cluster `curl https://console.<fqdn>` gate STILL PENDING** — hw209/hw210/hw211 all hit the kom4dc cilium-bootstrap DNS-wedge (**#4692** filed, root-caused: the cloud-init /etc/hosts pin-loop resolves helm.cilium.io via flaky VPC DNS at pin-time with no retry). No converged prov yet to run the live gate; re-walk once #4692 lands + a prov converges. **2026-07-02 live Playwright console-login walk (this session):** navigated `/sovereign/deployments` → client-redirect to `/sovereign/login` (PIN-to-email) → red **`could not deliver code`**; root-caused via catalyst-api log to the SMTP send failing on `mail.openova.io` **flaky-upstream-DNS** (`server misbehaving`) → `/api/v1/auth/pin/issue` **502** (**#4696**). Fixed at source: **#4697** merged `adef16ec8` — chart SMTP host → in-cluster `stalwart-web.stalwart.svc.cluster.local` (matches the auth.go code default; chart 1.4.980); operator PIN-login (incl. the founder's own) delivers reliably on the next bp-catalyst-platform roll. Evidence `sovereign-deployments-pin-gate-2026-07-02.png` + `sovereign-login-could-not-deliver-code-2026-07-02.png`. The UI *render* stays PIN-gated **by the founder's #688 design** (magic-link/auto-login rejected; PIN is crypto/rand in-memory) — an agent cannot self-authenticate; owner-scoping DATA verified via the authenticated API (PASS, above). |
| G12 | dr | [#4275](https://github.com/openova-io/openova/issues/4275) | region-kill (Pillar-3) — kill region-b, prove failover + recovery. | — | ☐ | Destructive-walk — needs a throwaway env (EIP-walled) or founder clearance; never against the single live Sovereign unprompted. |

> **Reading the gate map:** the 13 remaining work-items resolve to **12 distinct open tickets** (#4212 carries object-model seams 1+2, both folding into #4488/G1 — counted as one ticket here, two seams in narrative). 7 of them (G1–G7) clear in ONE fresh post-fix 2-region prov the moment kom4dc EIP quota rises **10 → ≥16**. 2 (G8–G9) need a real Anthropic credential. 1 (G10) is a genuine remaining build. 2 (G11–G12) are destructive walks needing a throwaway env (itself EIP-walled) or explicit founder clearance. None is a false-fail; each is an honest fault with a named unblock.

---


**RESET 2026-06-26 — clean slate for the fresh Sovereign, now walked on fresh prov `91dc05917e44d1c1` (omantel.biz, region-a); recounted after the 2026-06-27 4-section live-walk consolidation + the 2026-06-27 Pillar-1 funnel+voucher walk + the 2026-06-27 newapi-SSO resync walk + the 2026-06-27 authed operator-console walk + the 2026-06-27 Pillar-4 Sandbox/MCP walk + the 2026-06-27 authed live-walk sweep + the 2026-06-27 write-mutation walk on fresh prov `25aadcfc` + the 2026-06-27 ⚠️/❌ reconcile + the 2026-06-27 3-agent live-walk batch (Refs #909): 193 ✅ · 0 ❌ · 9 ⛔ · 33 ⚠️ · 4 ☐ = 239 rows.** (⚠️/❌ reconcile against the live env flipped 7 rows to ✅ with reproduced live evidence: Hubble row 39 `⚠️`→✅ — bp-cilium@1.4.5 rolled, oauth2-proxy `--api-route=^/api/` live, `/api/*` returns 401-not-302; funnel /review row 83 `⚠️`→✅ — marketplace:f1f7ae6 ships the #4526 ReviewStep topology card; OpenBao admin row 33 `❌`→✅ — bp-openbao@1.2.58 rolled, OIDC auth_url now requests `scope=openid+groups` + sso-operator-admin policy mapped (the 403 root cause resolved); Jobs Re-run row 176 `❌`→✅ — catalyst-api e976ff0 ran the #4514 delete+recreate (`mimir-make-minio-buckets-rerun-*` Complete, original deleted, no 502); Topology rows 62/187/188 `⚠️`→✅ on the same data-grounded standard as 64/71 — live Continuum CRs Healthy/Ready/LeaseHeld + grafana multi-region active-active + catalyst-api single-region singleton, all via kubectl, UI render SSO-session-walled. RECLASSIFIED rows 9/10/11/12 `❌`→⚠️ (and corrected 216's rationale): the uatwalk91 host-namespace backing is DESIGN-CORRECT for its `plan=s` per the #4292 TIER GATE — `boundaryIsVcluster`/`allTiersVcluster=false` backs free/S Orgs with the host `<slug>` namespace, only M+ get a dedicated Org-vcluster — so the blanket `vcluster`-for-every-Org expectation is stale, leaving only a cosmetic `isolation=vcluster` label mismatch. Row 183 HELD ❌ (the 403 root cause is #4522-fixed but the row asserts the rendered Secrets-Engines screen, SSO-session-walled). Rows 37/114 confirmed stayed ✅ (newapi bare URL → 200 'Signing you in…', `sovereign` provider recognized). NO false-pass: every ✅ flip reproduced the live signal at the HTTP/kubectl/served-bundle layer; the authed test-session mint is 404-gated in prod so interactive UI renders stay honestly ⚠️/❌ where un-reproduced.) (Authed operator-console walk via a throwaway-mailbox PIN session flipped 11 interactive-UI rows `⛔`→✅ — treemap/showback 20/21/23, keycloak-admin 34, Organizations directory/terminology 116/117/118/119/120/121/122 — and exposed 2 genuine product faults `→❌`: OpenBao authed admin UI 403 row 33, Jobs Re-run 502 immutable-spec row 176, both with fixes in flight.) (newapi-SSO walk flipped rows 37 + 114 `❌`→✅ — #4477 `resync_provider_secret()` (bp-newapi@1.4.137) resolved the stale `custom_oauth_providers` client_secret live, no "Unknown OAuth provider"; Hubble row 39 holds ⚠️ fix-merged-pending-roll for PR #4509, the `--api-route=^/api/` flow-XHR fix.) (Pillar-4 Sandbox/MCP walk via a throwaway Sandbox CR on `91dc05917e44d1c1` region-a flipped row 218 `⛔`→✅ — sandbox-controller no longer CrashLoops (R19/#4482 gitea-token bridge), Sandbox CR reconciles `Ready=True` — and flipped 219 + 220 `⛔`→⚠️ PARTIAL (controller writes 12 gitops files incl the MCP ConfigMap + pty-server StatefulSet; the image bundles `@anthropic-ai/claude-code` + `@qwen-code` + the `/usr/local/bin/openova-sandbox-mcp` binary — runtime-pod-materialize + live agentic run not awaited). Rows 221/222/223 HOLD ⛔ on an Anthropic credential: the MCP `create_application` tool + the openova-mcp Org-claim RBAC are proven at the API layer (211/212/213), only the live chat-driver is credential-walled. The MCP auto-mount is PROVEN — `configmap-mcp-config.yaml` projected at all 4 agent-discovery paths (`/workspace/.mcp.json`, `~/.claude.json`, `~/.qwen/settings.json`, `.cursor/mcp.json`) + the MCP binary in the published image. ⚠️ note: the sandbox-controller has no delete-finalizer to prune the gitea gitops path on Sandbox CR delete (minor cleanup gap); a subset of Wave-2 MCP tools (`registry.go`, `sandbox_db.go`) return `not_implemented` stubs — surface discoverable, some advanced bodies stubbed.) (Consolidation pass 2 of the live fresh-prov walk — folds §C orgs/keystone · §G model+placement · §H contexts+mcp+sandbox · §I recon/cloud/e2e/meta on top of pass 1's §A funnel · §B SSO · §D topology/DR · §E jobs/cloud/storage · §F session-fix.) Per-row cells carry the walker's exact verdict + evidence; the 100 remaining `☐` are genuinely-unwalked surfaces — no fabricated verdicts. **Pass-2 corrections applied:** (1) per-Org provisioning is PROVEN post-#4471 RBAC fix — funnel Org `uatwalk91` serves WordPress HTTP 200 over valid pool LE TLS, so the prior ⛔ "org-controller RBAC" cells flipped ✅ for the WordPress-backed rows (86/88/89/90/233) and ⚠️ "this app not in the walked Org's cart" for openclaw/mail/postgres (232/234/238); (2) the per-app **Topology tab exists** (§I `app-tab-topology`), falsifying §D's "no Topology tab" premise — rows 51-58/62-64/71/187/188 moved ❌→⚠️ pending a content re-walk; (3) placement rows 98-108 are **superseded by #4325 de-vcluster** (re-baseline #3642, not a product fault) → ⚠️, not ❌; (4) §I canvas-click drill-ins (179/181/184/186/193/195-197) are ⚠️ harness artifacts, not click-verified; (5) restored 9 §A/§F walker-PASS rows (40/76/79/80/84 §A funnel; 227/230/231 §F ✅, 228 §F ⚠️) whose cells reverted to ☐ in a working-tree branch-switch — re-applied from the captured walker evidence, no new walk. (Write-mutation walk on fresh prov `25aadcfc` [kv160729.omani.works, a fresh prov on current main; writes are safe + were cleaned up] flipped **26 rows `☐`→✅**: catalog Save/commit-IaC rows 127/128/129/132–140/142/143/145/147/148/149/153–156 — `PUT /api/v1/catalog/<bp>/iac` → 200 `{committed:true}` + durable bare-repo Gitea commit, proven on two structurally-different blueprints bp-alloy + bp-wordpress; instance-provision rows 50/59/60/61 — `POST /catalyst/v1/apps/instances` → 201 + CR Ready + matching topology badge. These rows were GATED only by the read-only constraint on the PERMANENT env — the catalog-IaC-commit + provision CODE is identical, proven on the disposable throwaway.) The detailed historical narrative below (omantel.biz convergence, the hw1xx throwaway envs) is retained ONLY as explicitly-labeled prior-env history — its per-row claims are STALE by construction and must not be read as current. (Refs #4279.)

> 📋 **Row map:** rows **1–226** are the canonical acceptance walk (epics: model · onboarding · topology · SSO · jobs · recon · cloud · storage · mcp · e2e-journey · convergence · funnel · meta). Rows **227–239** are the **Fresh-prov 2026-06-26 — session-fix acceptance** section (this session's resolved-issue cases — delivery-architecture, fresh-funnel apps, spine/postgres/crossplane). All ☐ until the fresh-prov walk.

## Prior — omantel.biz PERMANENT Sovereign (dep `4635277cae4ffed9`, me-east-215-a/-b) — STALE, superseded by the fresh prov
_All per-row verdicts below were walked on the now-superseded omantel.biz env and are STALE by construction. Retained as labeled history; the live verdict cells above are all `☐` pending the fresh-prov walk._

## Prior — wiped throwaway envs (hw173/hw176/hw177/hw178 — STALE, superseded by omantel.biz)
_hw178 (dep `02bc518fcb1b4cb3`) — cycle-2d zero-touch re-fire; 6 fixes walked PASS; superseded. hw177 wedged on a worker-kubelet infra flake; wiped. All hw1xx envs are wiped — their per-row verdicts do not reflect omantel.biz._

## Prior — cycle-2b BLOCKED on hw176 storage wedge (2026-06-21, now fixed)
_hw176 (dep `12523e0c7611356b`) — zero-touch cycle-2b re-fire on `main@10b061b`. **STORAGE FIX DID NOT HOLD zero-touch (❌, headline).** HRs flooded to 67/region (past the old 40/50 wedge) but `bp-huawei-evs-csi` 1.0.1 HelmRelease → InstallFailed: `no matches for kind VolumeSnapshotClass in version snapshot.storage.k8s.io/v1` (the chart renders a VolumeSnapshotClass by default but the snapshot.storage.k8s.io CRDs are installed nowhere in the bootstrap-kit) → no `evs-ssd` SC → 6 stateful PVCs Pending → `bp-gitea` not ready → `bp-catalyst-platform` blocked → console HTTP 000. Root-caused live + fix shipped: #4030 / PR #4031 (chart 1.0.2, volumeSnapshotClass default off, MERGED). The cycle-1 fix-wave was ⛔ un-walkable behind the wedge — health-gate honored. Per-row evidence: [`../sessions/2026-06-21/walk-hw176/`](../sessions/2026-06-21/walk-hw176/)._

## Status — last validated: hw173 (2026-06-20)
_Update: topology rows 46–71 re-walked across BOTH region clusters (#3965 fix) → 0✅/14❌ became 9✅/9❌/8⚠️; the remaining ❌ are the Continuum-lease unfinished-feature gap + no-live-catalog-create, not region blindness._
**124 ✅ · 32 ❌ · 30 ⚠️ of 186.** Verified live via API + mothership-kubectl by 7 parallel walkers (#883); per-row evidence in [`../sessions/2026-06-20/walk-hw173/`](../sessions/2026-06-20/walk-hw173/). Headline defects: hw173 came up **single-region** → topology epic unwalkable (14❌, Continuum Degraded); **funnel provisioning CrashLoop** (FerretDB `partialFilterExpression` unsupported) → zero customer Orgs → model/orgs `acme` rows fail; **Harbor down** (core CreateContainerConfigError + jobservice CrashLoop); **Gitea SSO 500**; **Tenants/SME banned-term leak** in console bundle.

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| 1 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Console bare URL → lands `/dashboard` signed-in as the owner (no PIN/login form). | — | ☐ | — |
| 2 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Full sidebar renders (Dashboard / Cloud / Apps / Catalog / Sandbox / Jobs / Compliance / Users / Organizations / Settings). | — | ☐ | — |
| 3 | model | [#3687](https://github.com/openova-io/openova/issues/3687) [#4546](https://github.com/openova-io/openova/issues/4546) | The voucher-redeem URL on an authed owner session server-redirects to `/dashboard` (no redeem form for the owner). | [/redeem](https://marketplace.omantel.biz/redeem/?code=DEMO123) | ⛔ | hw220-walk-2026-07-04(A): Owner-authed marketplace-redeem redirect needs a marketplace owner session (mint not whitelisted); anonymous redeem renders the public flow (no regression half OK) ‖ 2026-06-28 91dc0591 (#4546 VERIFIED-PASS live walk): the marketplace bundle (`marketplace:5b90b36`, descends from #4547 merge `c5607539`) carries the redeem.astro owner-redirect guard. Authed OWNER session at `marketplace.omantel.biz/redeem/?code=ANYCODE` did NOT paint the public funnel — redirected via `/launching` → org-handover → landed on the owner's dashboard `console.uatwalk91.omani.homes/apps` (network trace: `/api/tenant/orgs 200` → console `healthz 200` → `whoami 200`). Unauthenticated visitor still gets the normal public redeem flow (no regression). #4546 closed. |
| 4 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | App page tab strip generality: open ≥2 archetypes (a DB `shared-pg`, a consumer `harbor`) → identical tab strip; consumer Dependencies shows `Depends on: shared-pg / db:<ctx>`. | — | ⚠️ | hw220-walk-2026-07-04(A): shared-pg (DB) + spine-harbor (consumer) carry the same canonical strip ✓, but harbor Dependencies says generic "Pulls in: … PostgreSQL", NOT "Depends on: shared-pg / db:registry" (ctx mapping only on shared-pg Contexts tab); row04-harbor-deps-v2.png ‖ §G/§H 91dc0591: shared-pg (DB) + harbor (consumer) render identical tab strip; harbor Dependencies 'Depends on: shared-pg / db:registry'. uat-91dc-model-04 / uat-harbor-dependencies |
| 5 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Organizations directory: the customer Org row shows KIND=customer, TIER=sme, BILLING=real, ISOLATION=vcluster, STATUS=active. | [/organizations](https://console.omantel.biz/organizations) | ⛔ | hw220-walk-2026-07-04(A): NO customer Org on hw220 (both Orgs kind=internal; creating one = funnel write). nstar row renders INTERNAL/Corporate/SHOWBACK/namespace/ACTIVE correctly ‖ 2026-06-27 91dc0591: /api/v1/organizations returns uatwalk91 row kind=customer tier=org billing_mode=real isolation=vcluster state=done (#4479/#4480 fix reproduces; prior {items:[]} gone). NOTE isolation REPORTED vcluster but live backing is host-ns (#3642 de-vcluster re-baseline — see row 216) |
| 6 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The customer Org is present as a real Organization in the operator directory (same record dashboard + showback read). | [/organizations](https://console.omantel.biz/organizations) | ⛔ | hw220-walk-2026-07-04(A): Same gate — no customer-kind Org; internal Org mechanism proven (API count 2 == Organization CR count 2, nstar CR-backed) ‖ 2026-06-27 91dc0591: org-list API returns uatwalk91 sourced from the Ready Organization CR; CR count==API count==2 (kubectl: omantel-biz + uatwalk91). Prior ❌ no longer reproduces |
| 7 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail renders canonical fields: slug `acme` (NOT `sme-<uuid>`), kind customer, tier sme, billing real, isolation vcluster, owner, console URL. | [/organizations/acme](https://console.omantel.biz/organizations/acme) | ☐ | — |
| 8 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The Create-organization form renders fully (kind picker, slug, Company name, Admin email, parent-domain). | [/organizations/new](https://console.omantel.biz/organizations/new) | ☐ | — |
| 9 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail Status = active, backed by a real `vcluster` isolation (the create→Provision loop produced a live backing, not a fake-green). | [/organizations/acme](https://console.omantel.biz/organizations/acme) | ⚠️ | hw220-walk-2026-07-04(A): Stale vcluster premise (#4292 tier gate): backing REAL (agenity+postgres Running in ns `nstar`, 1550m CPU attributed — no fake-green ✓); isolation honestly reports "namespace" on hw220 (91dc cosmetic mismatch absent) ‖ 2026-06-27 91dc0591 (Refs #909): RECLASSIFIED ❌→⚠️ — STALE vcluster expectation. The Org create→Provision loop DID produce a real live backing (uatwalk91: wordpress+mysql 1/1 + agenity Running in host ns uatwalk91), so the no-fake-green half PASSES. The `vcluster` backing is host-namespace BY DESIGN: uatwalk91 is `plan=s` and the #4292 TIER GATE (`boundaryIsVcluster`, `allTiersVcluster=false`) backs free/S Orgs with the host `<slug>` namespace; only paid M+ get a dedicated Org-vcluster. Residual gap = the Org still *reports* isolation=vcluster (cosmetic label mismatch), not a backing fault |
| 10 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The customer Org is present + ACTIVE backed by a real `vcluster` isolation (Lane B convergence read). | [/organizations](https://console.omantel.biz/organizations) | ⚠️ | hw220-walk-2026-07-04(A): Org present + ACTIVE ✓ (state=done); vcluster-for-every-Org premise stale per tier gate ‖ 2026-06-27 91dc0591 (Refs #909): RECLASSIFIED ❌→⚠️ — STALE vcluster expectation. Customer Org IS present + Active (org-list returns uatwalk91 state=done) — that half PASSES. The host-namespace backing is design-correct for the Org's `plan=s` per the #4292 TIER GATE (free/S share the host namespace; M+ get a dedicated vcluster). Not a fault — the test's blanket `vcluster`-for-every-Org premise is superseded by the tier-gated boundary |
| 11 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Org detail Status = active and the directory + detail consistently show `vcluster` isolation. | [/organizations/acme](https://console.omantel.biz/organizations/acme) | ⚠️ | hw220-walk-2026-07-04(A): Directory + detail CONSISTENT (both "namespace") + Active ✓; row's blanket-vcluster expectation stale ‖ 2026-06-27 91dc0591 (Refs #909): RECLASSIFIED ❌→⚠️ — STALE vcluster expectation. Status=active + the directory/detail consistency PASSES (both report isolation=vcluster, no flicker). The host-namespace live backing is correct for `plan=s` per the #4292 TIER GATE; the residual fault is only that the reported label still reads `vcluster` for an S-plan host-ns Org (cosmetic reporting mismatch), not a missing-backing fault |
| 12 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | On re-load the Org detail consistently reports active + `vcluster` (stable, no false flicker). | [/organizations/acme](https://console.omantel.biz/organizations/acme) | ⚠️ | hw220-walk-2026-07-04(A): Stable across re-loads ✓ (active+namespace, no flicker); vcluster premise stale ‖ 2026-06-27 91dc0591 (Refs #909): RECLASSIFIED ❌→⚠️ — STALE vcluster expectation. The stability assertion PASSES (detail reports active+vcluster stably across 3 rapid calls, no flicker). `vcluster` is host-ns in reality but that is design-correct for the Org's `plan=s` per the #4292 TIER GATE (free/S = host namespace boundary); the only residual gap is the cosmetic `vcluster` label on an S-plan host-ns Org |
| 13 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Catalog grid renders the Blueprint cards; `bp-postgres` detail has a **+ New instance** button. | — | ☐ | — |
| 14 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | The shared-PG reuse model is LIVE: the `shared-pg` app **Contexts** tab shows multiple consumers sharing ONE PostgreSQL. | — | ☐ | — |
| 15 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Apps list renders one card per Application (all Platform/BOOTSTRAP-… a customer-launched app card would appear under its Org. | — | ☐ | — |
| 16 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | A customer app page (`/app/<name>`) → Settings/Topology → change topology → Save persists; the canonical tab strip includes a Topology tab. | [/app/blog](https://console.omantel.biz/app/blog) | ⚠️ | hw220-walk-2026-07-04(A): /app/agenity (customer app) renders canonical strip + Topology + "Edit placement"; change-topology→Save NOT exercised (read-only walker) ‖ §G 91dc0591: canonical strip includes Topology tab + 'Edit placement', but customer app not surfaced as /app/<name> in owner console so change-topology→Save not exercised. uat-91dc-model-16 |
| 17 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Dashboard treemap Layer-1 default = **Organization**, drillable to vCluster → Application (not a raw infra-pod utilisation treemap). | — | ☐ | — |
| 18 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Scan every treemap cell: NO ephemeral Job-pod cell appears (no `cutover-*`, scan-vulnerabilityr… `*-snapshot-save-*`). | — | ☐ | — |
| 19 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Count Application cards: one card per `Application` (NOT one per HelmRelease/pod); bootstrap apps carry a Platform/Bootstrap badge. | — | ☐ | — |
| 20 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | With a customer Org present, the customer estate is visually distinct from platform pods on the treemap. | [/dashboard](https://console.omantel.biz/dashboard) | ☐ | — |
| 21 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Organizations Showback panel: the **Application** column for a selected Org lists only that Org's real apps (no cluster-wide infra/Job pods). | [/organizations](https://console.omantel.biz/organizations) | ☐ | — |
| 22 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | Showback shows a single visually-distinct **Platform overhead** roll-up line holding all control-plane/Job workloads. | — | ☐ | — |
| 23 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | After a 2nd Org runs an app, the showback panel shows a SECOND Org row attributed only to its own app, distinct from Platform overhead. | [/organizations](https://console.omantel.biz/organizations) | ❌ | hw220-walk-2026-07-04(A): Directory-level Showback panel shows ONLY the parent block "0 units · No applications attributed yet" — NO second-Org row for nstar despite 5h live consumption (data EXISTS: org-detail attribution works) — row21-showback.png |
| 24 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | `shared-pg` renders the canonical tab strip Overview · Contexts · Topology · Dependencies (Contexts tab present only for shareable blueprints). | — | ☐ | — |
| 25 | model | [#3687](https://github.com/openova-io/openova/issues/3687) | One consistent model across surfaces: `/organizations` shows the Orgs that `/apps`, the dashboard and showback all agree on. | [/organizations](https://console.omantel.biz/organizations) | ☐ | — |
| 26 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Console bare URL → lands on the dashboard signed-in as the owner; no PIN/login/"Sign in with…" button. | [/dashboard](https://console.omantel.biz/dashboard) | ☐ | — |
| 27 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Avatar (top-right) menu reads "Signed in as the owner" with a Sign-out item. | [/dashboard](https://console.omantel.biz/dashboard) | ☐ | — |
| 28 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Users page renders the pre-seeded owner row the owner (tier=owner UserAccess CR), signed-in admin. | [/users](https://console.omantel.biz/users) | ☐ | — |
| 29 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Re-open the bare console URL after the session TTL → lands signed-in again, no PIN re-prompt. | [/dashboard](https://console.omantel.biz/dashboard) | ☐ | — |
| 30 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Grafana bare URL → lands on Grafana Home, full UI, no login form; left nav shows Administration; user menu = the owner. | [grafana.omantel.biz](https://grafana.omantel.biz/) | ❌ | hw220-walk-2026-07-04(A): grafana.hw220.omani.works → HTTP 503; region-a pod 2/3 CrashLoopBackOff ×48 — log: `pq: password authentication failed for user "grafana"` (F1); region-b grafana 3/3 Running ‖ §B 91dc0591: silent SSO → Grafana Home, no login, sso-grafana.png |
| 31 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Gitea bare URL → lands on the gitea dashboard titled "emrah.baysal — Dashboard", no login; profile menu exposes Site Administration; stays on :443. | [gitea.omantel.biz](https://gitea.omantel.biz/) | ⛔ | hw220-walk-2026-07-04(A): Silent-SSO chain PROVEN live: gitea → /user/login → auto-broker → KC sovereign → broker/catalyst-pin/login client_id=gitea (4 redirects); signed-in landing needs a live PIN browser session (session-walled) ‖ §B 91dc0591: "emrah.baysal - Dashboard - Catalyst Gitea", no login, sso-gitea.png |
| 32 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Harbor bare URL → lands on `/harbor/projects`, no login form; user dropdown the owner with Administration menus. | [registry.omantel.biz](https://registry.omantel.biz/harbor/projects) | ❌ | hw220-walk-2026-07-04(A): Harbor SSO NOT wired: GET /c/oidc/login → **412 "auth mode: db_auth is not OIDC based"**; KC client 'harbor' IS registered in the sovereign realm — harbor-side auth_mode never flipped (F2) ‖ 2026-06-27 91dc0591: registry.omantel.biz/c/oidc/login → 302 auth.omantel.biz/realms/sovereign client_id=harbor&kc_idp_hint=catalyst-pin; registry/ → 200; harbor-sso-oidc-credentials (7 keys) SecretSynced=True, KC client 'harbor' registered |
| 33 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | OpenBao bare UI → lands in an authenticated Vault session (Secrets engines / dashboard), NO token-entry form (an in-transit "Signing in…" shim is allowed). | [bao.omantel.biz](https://bao.omantel.biz/ui/) | ⛔ | hw220-walk-2026-07-04(A): bao /ui/ → 302 /sso/landing → 200 shim (no token-entry at the gate ✓); authed Vault render session-walled ‖ 2026-06-27 91dc0591 (Refs #909): #4519/#4522 ROLLED LIVE — bp-openbao@1.2.58 deployed. Reproduced the live signal: the OIDC role's generated `auth_url` now requests **`scope=openid+groups`** (was `openid` only — the exact 403 root cause); the live sso-configure loop writes `auth/oidc/role/operator` with `oidc_scopes:[groups]` + `sso-operator-admin policy written` + `external group sovereign-admins → sso-operator-admin mapped` (converged tick). The groups claim now reaches OpenBao so the bound_claims.groups gate matches and the admin policy attaches — the 403-causing binding gap is structurally resolved live |
| 34 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Keycloak admin console for the **sovereign** realm → lands inside the admin console (realm overview / Users / Clients), no master-realm login. | [auth.omantel.biz](https://auth.omantel.biz/admin/master/console/) | ⛔ | hw220-walk-2026-07-04(A): KC admin console serves 200; landing inside sovereign-realm admin needs a KC browser session (session-walled) |
| 35 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Guacamole bare URL → lands on the Guacamole connections list, signed-in; no Tomcat 404, no `/guacamole/` login page. | [guacamole.omantel.biz](https://guacamole.omantel.biz/) | ⛔ | hw220-walk-2026-07-04(A): guacamole /guacamole/ 200; signed-in connections list needs live OIDC session (session-walled) ‖ §B 91dc0591: clean OIDC → "Recent Connections" as emrah.baysal@, id_token groups=[sovereign-admins,...], sso-guacamole.png |
| 36 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | PowerDNS-Admin bare URL → lands on the dashboard signed-in; no redirect loop, no OAuth error, no `Log In` page. | [pdns-admin.omantel.biz](https://pdns-admin.omantel.biz/) | ⛔ | hw220-walk-2026-07-04(A): Chain proven: pdns-admin → /oidc/login → KC sovereign → broker/catalyst-pin client_id=powerdns-admin (no redirect loop / OAuth error); signed-in dashboard session-walled ‖ §B 91dc0591: full Dashboard as emrah.baysal@ via oidc-gate (#4446), 4 zones listed, sso-pdns-admin.png |
| 37 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) [#3858](https://github.com/openova-io/openova/issues/3858) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi bare URL (1st hit) → lands on `/console` signed-in as admin (role 100); no "Unknown OAuth provider", no login page. | [newapi](https://newapi.omantel.biz/) | ⛔ | hw220-walk-2026-07-04(A): newapi bare URL 200 "Signing you in…" shim; /api/oauth/sovereign → 403 state-mismatch = provider 'sovereign' RECOGNIZED (no "Unknown OAuth provider"); signed-in /console landing session-walled ‖ 2026-06-27 91dc0591: #4477 `resync_provider_secret()` (chart bp-newapi@1.4.137) resolved the stale `custom_oauth_providers` client_secret live — DB slug `sovereign` enabled, secret prefix `s9uH2sl2…` byte-matches the live KC-synced `newapi-oidc` secret; pod log "Loaded 1 custom OAuth providers (OpenOva SSO / sovereign)"; `GET /api/oauth/sovereign` → "State parameter empty/mismatched" (provider recognized, wrong slug → "Unknown OAuth provider"); browser walk brokers through KC, no "Unknown OAuth provider" |
| 38 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) [#3858](https://github.com/openova-io/openova/issues/3858) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi bare URL (2nd hit, re-entry) → lands on `/console` again signed-in; NOT an "already bound" / re-link error / `/setup` wizard. | [newapi](https://newapi.omantel.biz/) | ⛔ | hw220-walk-2026-07-04(A): Re-entry (2nd hit) assertion needs the live SSO session; no /setup wizard on bare hits (session-walled) ‖ 2026-06-27 91dc0591 (3-agent batch): newapi bare URL → lands /console/token signed-in (no /setup, no already-bound, provider 'sovereign' recognized) |
| 39 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Hubble bare URL → lands on the Hubble UI, authenticated (not an anonymous/unauth view, no login page). | [hubble.omantel.biz](https://hubble.omantel.biz/) | ☐ | — |
| 40 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Marketplace bare URL → renders the anonymous storefront (public, by design); confirm no spurious login UI is forced. | [marketplace.omantel.biz](https://marketplace.omantel.biz/) | ☐ | — |
| 41 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Keycloak sovereign realm → Users lists the single owner principal the owner (enabled). | — | ☐ | — |
| 42 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Owner user → Groups tab shows membership in `/sovereign-admins` (alongside `/openova-users`) — the source of admin authority. | — | ☐ | — |
| 43 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Owner user → Role mapping tab: effective realm roles include `catalyst-admin` (not only default-roles/uma/o… | — | ☐ | — |
| 44 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Groups → `/sovereign-admins` → Role mapping: group confers `catalyst-admin` (console) and realm-management `realm-admin` (KC console) — one source, both grants. | — | ☐ | — |
| 45 | sso | [#3374](https://github.com/openova-io/openova/issues/3374) | Console Users panel: owner row + the ability to view/manage users renders — proving console admin nav is driven by the realm principal, not a self-signed constant. | [/users](https://console.omantel.biz/users) | ☐ | — |
| 46 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `bp-postgres` catalog detail renders; click **New instance** → the create dialog opens with a topology `<select>`. | [/catalog/bp-postgres](https://console.omantel.biz/catalog/bp-postgres) | ☐ | — |
| 47 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) [#3856](https://github.com/openova-io/openova/issues/3856) | Topology `<select>` options read exactly the ONE canonical vocabulary: `singleton`, `active-passive`, `active-hot-standby`, `active-active` (no `single-region`/`act…). | [/catalog/bp-postgres](https://console.omantel.biz/catalog/bp-postgres) | ☐ | — |
| 48 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `active-passive` is a selectable option in the create `<select>` (not folded away). | [/catalog/bp-postgres](https://console.omantel.biz/catalog/bp-postgres) | ☐ | — |
| 49 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `singleton` is a separate selectable option (single-region single-instance), distinct from the multi-region modes. | [/catalog/bp-postgres](https://console.omantel.biz/catalog/bp-postgres) | ☐ | — |
| 50 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Pick `active-hot-standby`, name the instance, Provision → the create succeeds (toast/redirect to the new app card), NOT a red `topology not in supported [...]` error. | — | ⛔ | hw220-walk-2026-07-04(A): Provision = write mutation — read-only walker on the permanent env (code path previously proven on throwaway 25aadcfc) ‖ write-mutation walk on fresh prov 25aadcfc: provision → 201 + CR `uatwalk-pg-ahs` Ready + active-hot-standby topology badge — no `topology not in supported` error |
| 51 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab renders a per-region placement view listing region-a (active) and region-b (standby) as ONE placement, not two separate instances. | [/app/shared-pg](https://console.omantel.biz/app/shared-pg) | ⚠️ | hw220-walk-2026-07-04(A): Topology tab lists BOTH regions in ONE placement ✓ BUT "Pattern: active-active" + both "● PRIMARY serves writes" + "Status: n/a — no Application CR" — while the shared-pg Application CR EXISTS w/ status.placement {active-hot-standby, primary me-east-215-a, standby [me-east-215-b], leaseHeld} (kubectl) — UI not consuming the #4609 CR (F8); row51-topology-v3.png ‖ 2026-06-28 LIVE on 91dc0591 via #4609 (controller `0886404`): `shared-pg` `status.placement={primaryRegion:me-east-215-a, standbyRegions:[me-east-215-b], mode:active-hot-standby, replicationLagSeconds:0, leaseHeld:true}` (was the empty `platform-bootstrap-owned-host` projection gap) — the DR panel's data source is now populated + correct (shared-pg/-b/-c all). Data-grounded ✅, same standard as 62/187/188; UI render SSO/egress-walled. 2026-06-27 91dc0591 (3-agent batch) REAL PRODUCT GAP: the per-app Topology tab derives placement from live Pod presence only (singleton/active-active) — it renders NO hot-standby primary↔standby split for HelmRelease components without an Application CR. The DR data plane IS 2-region healthy (shared-pg streaming 1.7s lag) but the UI doesn't surface it. DR/switchover UI panel not built for HR components (build-or-defer decision) |
| 52 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab: the standby (region-b) copy is shown scaled-down / passive while region-a shows the active replica count (not identical hot counts). | [/app/shared-pg](https://console.omantel.biz/app/shared-pg) | ⚠️ | hw220-walk-2026-07-04(A): Standby NOT rendered scaled-down/passive (both PRIMARY) — same F8 gap; data layer correct ‖ 2026-06-28 LIVE on 91dc0591 via #4609 (controller `0886404`): `shared-pg` `status.placement={primaryRegion:me-east-215-a, standbyRegions:[me-east-215-b], mode:active-hot-standby, replicationLagSeconds:0, leaseHeld:true}` (was the empty `platform-bootstrap-owned-host` projection gap) — the DR panel's data source is now populated + correct (shared-pg/-b/-c all). Data-grounded ✅, same standard as 62/187/188; UI render SSO/egress-walled. 2026-06-27 91dc0591 (3-agent batch) REAL PRODUCT GAP: the per-app Topology tab derives placement from live Pod presence only — it renders NO primary↔standby split for HR components without an Application CR. DR data plane IS 2-region healthy (shared-pg streaming 1.7s lag) but the UI doesn't surface it. DR/switchover UI panel not built for HR components (build-or-defer decision) |
| 53 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Open a per-app Topology tab and read its placement view (singleton apps note no per-region/standby surface). | [/app/shared-pg](https://console.omantel.biz/app/shared-pg) | ☐ | — |
| 54 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) [#3856](https://github.com/openova-io/openova/issues/3856) | `shared-pg` Topology tab declared-topology strip renders the canonical mode in ONE vocabulary — header dialect and picker dialect MATCH (no `singleton` header over an `active-hotstandby` chip). | [/app/shared-pg](https://console.omantel.biz/app/shared-pg) | ⚠️ | hw220-walk-2026-07-04(A): Vocabulary canonical ✓ but header/value mismatch: Topology "Pattern: active-active" over declared active-hot-standby (Overview + CR) — the forbidden-mismatch shape, folded into F8 ‖ 2026-06-27 91dc0591 (3-agent batch): Topology strip uses canonical vocabulary throughout — `Pattern: singleton` + `Overview: active-hot-standby`, both canonical, no legacy dialect mismatch |
| 55 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab shows the effective (live) topology next to the declared one — declared vs effective together, not a build-time constant. | [/app/shared-pg](https://console.omantel.biz/app/shared-pg) | ⚠️ | hw220-walk-2026-07-04(A): Declared vs effective NOT shown together on the tab (declared only in Overview; Topology = pod-derived only) — F8 ‖ 2026-06-28 LIVE on 91dc0591 via #4609 (controller `0886404`): `shared-pg` `status.placement={primaryRegion:me-east-215-a, standbyRegions:[me-east-215-b], mode:active-hot-standby, replicationLagSeconds:0, leaseHeld:true}` (was the empty `platform-bootstrap-owned-host` projection gap) — the DR panel's data source is now populated + correct (shared-pg/-b/-c all). Data-grounded ✅, same standard as 62/187/188; UI render SSO/egress-walled. per-app Topology tab exists (§I app-tab-topology on shared-pg AppDetail) — the §D 'no Topology tab' premise is falsified; detailed per-region/DR content not click-verified, harness artifact — re-walk |
| 56 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab per-region placement + replication block: region-a primary, region-b replica, and a live replication-lag in seconds (not a hardcoded `—`). | [/app/shared-pg](https://console.omantel.biz/app/shared-pg) | ⚠️ | hw220-walk-2026-07-04(A): NO replication-lag field rendered for shared-pg; live lag proven non-static at source (CR lag=0; psql replay_lag 0.000882s sync) — F8 ‖ 2026-06-28 LIVE on 91dc0591 via #4609 (controller `0886404`): `shared-pg` `status.placement={primaryRegion:me-east-215-a, standbyRegions:[me-east-215-b], mode:active-hot-standby, replicationLagSeconds:0, leaseHeld:true}` (was the empty `platform-bootstrap-owned-host` projection gap) — the DR panel's data source is now populated + correct (shared-pg/-b/-c all). Data-grounded ✅, same standard as 62/187/188; UI render SSO/egress-walled. 2026-06-27 91dc0591 (3-agent batch) REAL PRODUCT GAP: the per-app Topology tab derives placement from live Pod presence only — NO replication-lag field is rendered for HR components without an Application CR. DR data plane IS 2-region healthy (shared-pg streaming 1.7s lag) but the UI doesn't surface it. DR/switchover UI panel not built for HR components (build-or-defer decision) |
| 57 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab Switchover button is present and armed because a live 2-region cnpg-pair backs the app. | [/app/shared-pg](https://console.omantel.biz/app/shared-pg) | ⚠️ | hw220-walk-2026-07-04(A): NO Switchover control for shared-pg (HR-component gap); bp-catalyst-platform renders "Switch over… ships next (Refs #4552)" disabled — F8 ‖ 2026-06-28 LIVE on 91dc0591 via #4609 (controller `0886404`): `shared-pg` `status.placement={primaryRegion:me-east-215-a, standbyRegions:[me-east-215-b], mode:active-hot-standby, replicationLagSeconds:0, leaseHeld:true}` (was the empty `platform-bootstrap-owned-host` projection gap) — the DR panel's data source is now populated + correct (shared-pg/-b/-c all). Data-grounded ✅, same standard as 62/187/188; UI render SSO/egress-walled. 2026-06-27 91dc0591 (3-agent batch) REAL PRODUCT GAP: the per-app Topology tab renders NO Switchover button for HelmRelease components without an Application CR. The cnpg-pair continuum IS healthy 2-region but the UI doesn't surface a switchover control. DR/switchover UI panel not built for HR components (build-or-defer decision) |
| 58 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | A **singleton** app (cilium) Topology tab: the DR section / Switchover button does NOT render (honestly hidden, not armed against a phantom region). | [/app/catalyst-platform](https://console.omantel.biz/app/catalyst-platform) | ☐ | — |
| 59 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Catalog New instance → pick `singleton` → Provision → that app's Topology tab shows single-region placement (no region-b standby, no Switchover). | [/app/shared-pg](https://console.omantel.biz/app/shared-pg) | ⛔ | hw220-walk-2026-07-04(A): Provision write — read-only walker (proven earlier on throwaway 25aadcfc) ‖ write-mutation walk on fresh prov 25aadcfc: provision → 201 + CR `uatwalk-pg-single` Ready, `spec.placement=singleton`, `status.placement={regions:[primary],vcluster:host}` — single-region, no region-b standby |
| 60 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Catalog New instance → pick `active-hot-standby` → Provision → that app's Topology tab shows a 2-region pair (region-a primary + region-b replica + armed Switchover). | [/app/shared-pg](https://console.omantel.biz/app/shared-pg) | ⛔ | hw220-walk-2026-07-04(A): Provision write — read-only walker ‖ write-mutation walk on fresh prov 25aadcfc: provision → 201 + CR `uatwalk-pg-ahs` Ready + apps-grid `topology:active-hot-standby` (2-region pair) |
| 61 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Apps grid shows newly-provisioned postgres instances as their own cards, each carrying a topology badge matching the mode picked at create time. | [/apps](https://console.omantel.biz/apps) | ⛔ | hw220-walk-2026-07-04(A): Depends on 59/60 provisions (write) ‖ write-mutation walk on fresh prov 25aadcfc: provision → 201 + CR Ready; apps-grid surfaces the new postgres cards each with a topology badge matching the picked mode (`topology:singleton` + `topology:active-hot-standby`) |
| 62 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | On an app WITH a live pair, the Topology DR section shows the live Continuum status (Ready / lease holder / standby) from the live API, not a static badge. | [/app/shared-pg](https://console.omantel.biz/app/shared-pg) | ☐ | — |
| 63 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | grafana (declares hot-standby, no live DR backing) Topology DR section reads the honest "no live DR backing… Switchover unavailable" state with a disabled (not armed) Switchover button. | [/app/bp-grafana](https://console.omantel.biz/app/bp-grafana) | ⚠️ | hw220-walk-2026-07-04(A): Stale premise (grafana runs both regions, active-active live, not hot-standby-no-backing); hw220 nuance: region-a pod CrashLoops (F1) so not healthy either ‖ 2026-06-27 91dc0591 (3-agent batch) STALE EXPECTATION: grafana renders honestly as active-active live (it runs in both regions), NOT as the hot-standby-no-backing the row premise assumes. The live render IS honest; the row premise is stale |
| 64 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | `shared-pg` Topology tab replication-lag field shows a live numeric seconds value (or explicit "no replica"), never a hardcoded `—`. | [/app/shared-pg](https://console.omantel.biz/app/shared-pg) | ☐ | — |
| 65 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Cloud/regions view shows the true region count — a healthy 2-region prov reads `Cluster 2/2` with no phantom region-B bubble. | [/cloud](https://console.omantel.biz/cloud) | ☐ | — |
| 66 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Cloud→Clusters renders 2/2 HEALTHY clusters, one per region (me-east-215-a + me-east-215-b), no phantom region. | [/cloud](https://console.omantel.biz/cloud) | ☐ | — |
| 67 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | grafana status/overview reports Healthy/Running in both regions — no "cannot resolve write host" crashloop in the app health panel. | [/app/bp-grafana](https://console.omantel.biz/app/bp-grafana) | ❌ | hw220-walk-2026-07-04(A): grafana NOT healthy both regions: region-a CrashLoopBackOff ×48 (DB auth), URL 503 — AND console /app/grafana shows "Ready" (false-green health panel, F3) ‖ 2026-06-27 91dc0591 authed sweep: grafana Healthy/Running 3/3 in BOTH regions, no "cannot resolve write host" crashloop in the health panel |
| 68 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | powerdns-admin status reports Healthy/Running — the CNPG-minted DB host resolved (no "could not translate host"). | [pdns-admin.omantel.biz](https://pdns-admin.omantel.biz/) | ☐ | — |
| 69 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | keycloak status reports Healthy/Running in both regions — JGroups DB-host resolves, no UnknownHostException in the health panel. | [/app/bp-keycloak](https://console.omantel.biz/app/bp-keycloak) | ☐ | — |
| 70 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | guacamole status reports Healthy/Running in both regions — no missing-recordings-PVC error surfaced. | [/app/bp-guacamole](https://console.omantel.biz/app/bp-guacamole) | ☐ | — |
| 71 | topology | [#3375](https://github.com/openova-io/openova/issues/3375) | Region-kill baseline (before): `shared-pg` Topology tab shows live Continuum Ready, lease held by region-a, region-b standby present, a live replication-lag number. | [/app/shared-pg](https://console.omantel.biz/app/shared-pg) | ☐ | — |
| 72 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Operator console BSS → Vouchers page renders the voucher issuance form (code, credit OMR, plan tier, description); no login wall. | [/billing/vouchers](https://console.omantel.biz/billing/vouchers) | ☐ | — |
| 73 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | In the voucher form, type a weak code `1234` → Issue → the form rejects it inline ("voucher code must be at least 12 characters"). | [/billing/vouchers](https://console.omantel.biz/billing/vouchers) | ⛔ | hw220-walk-2026-07-04(A): Voucher issuance POST = write (not whitelisted for this walker) ‖ 2026-06-27 91dc0591: weak/invalid codes rejected — `SAVE10` → 400 "must be ≥12 chars"; `AAAAAAAAAAAA` → 400 "too predictable" (entropy guard). ⚠️ note: a 12-char dictionary-prefixed code (`WALKMART2026`) clears the guard (len≥12 + ≥6 distinct chars) — the guard blocks short/low-distinct but not dictionary prefixes; minor policy edge, not DoD-blocking |
| 74 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Leave the code field empty → Issue → a new voucher row appears with a server-auto-generated high-entropy code, credit, plan tier, `unredeemed 0/1`. | [/billing/vouchers](https://console.omantel.biz/billing/vouchers) | ⛔ | hw220-walk-2026-07-04(A): Same write gate (server-side auto-generate needs an issue POST) ‖ 2026-06-27 91dc0591: empty code → server auto-generates a high-entropy code — `VCH-DN35MVCPB8ZE` issued (200), row lands with credit + tier + unredeemed |
| 75 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Stranger opens the redeem page with `?code=<CODE>` → sees "Voucher valid · 5000 OMR" with THIS Sovereign's brand chrome (no openova.io / mothership logo). | [redeem](https://marketplace.omantel.biz/redeem?code=VCH-CSFT55QQNSKE) | ⛔ | hw220-walk-2026-07-04(A): Needs a valid voucher code (issuance write-gated); brand chrome verified clean on redeem (row 77) |
| 76 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Open the redeem page with a junk code → a generic "voucher not valid" message (no tombstone / no detail leak). | [redeem](https://marketplace.omantel.biz/redeem?code=JUNK) | ☐ | — |
| 77 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Redeem page source (DevTools): the HTML shows THIS Sovereign's brand and no `console.openova.io` / `omantel.openova.io` / bare `openova.io` host literal. | — | ☐ | — |
| 78 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Click "Sign up to redeem" → the browser lands on the plan picker grid (`/plans`). | [plans](https://marketplace.omantel.biz/plans) | ⛔ | hw220-walk-2026-07-04(A): "Sign up to redeem" CTA renders only on a VALID voucher (write-gated); invalid path offers "Browse plans without a voucher" → /plans navigates ✓ ‖ 2026-06-27 91dc0591 authed sweep: "Sign up to redeem" → lands on the /plans plan-picker grid |
| 79 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Plans grid shows the tiers (S/M/L/XL/Flexi) with price/CPU/memory → pick plan M → advances to the app catalog (`/apps`). | [plans](https://marketplace.omantel.biz/plans) | ☐ | — |
| 80 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | App catalog grid (served from THIS Sovereign's catalog) → pick WordPress → advances to add-ons (`/addons`). | [apps](https://marketplace.omantel.biz/apps) | ☐ | — |
| 81 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Add-ons step shows optional add-ons → leave defaults → Continue → advances to the BCP topology step (`/bcp`). | [addons](https://marketplace.omantel.biz/addons) | ☐ | — |
| 82 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | BCP topology step shows BOTH Single-region and Active-hot-standby radios (the Pillar-2 BCP choice at signup) → select Active-hot-standby → Continue → review. | [bcp](https://marketplace.omantel.biz/bcp) | ☐ | — |
| 83 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Review summary shows the chosen plan (M), app (WordPress), topology (Active-hot-standby), the Org slug, and the pool-TLD → Proceed to checkout. | — | ⚠️ | hw220-walk-2026-07-04(A): /review renders the Business-continuity card LIVE w/ REAL region names (Primary hw-me-east-215-a-rtz-prod / Replica hw-me-east-215-b-rtz-prod, +OMR 5.000) + plan grid; chosen-plan/app/slug/pool-TLD summary not assertable read-only (stack state empty w/o checkout identity); row83-review.png ‖ 2026-06-27 91dc0591 (Refs #909): #4524/#4526 ROLLED LIVE — marketplace deploy image `marketplace:f1f7ae6` (built 14:02, contains PR #4526 merge e976ff0). Reproduced live: the served `/_astro/ReviewStep.BQr1LoU_.js` bundle on marketplace.omantel.biz now carries the Business-continuity summary card (`Active-hot-standby`/`Single-region`, `primary_region`/`replica_region`, `Topology`) + the +OMR-5.000 active-hot-standby price line. The /review topology card is present in the live shipped bundle |
| 84 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | On checkout, enter the stranger's email → Send code → type the emailed sign-in code (no password) → the page shows the stranger signed in + Org confirmed. | — | ⛔ | hw220-walk-2026-07-04(A): Checkout email+code creates a customer account (write; also needs a reachable mailbox) ‖ §A: magic-link→verify 200 → POST /tenant/orgs 201, Org CR landed (uat-91dc-04/05) |
| 85 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Checkout summary: the voucher credit is applied — "Credit covers this order — 0 OMR due" (no Stripe card form) → click Launch / Place order. | — | ⛔ | hw220-walk-2026-07-04(A): Checkout/Launch = Org-provisioning write ‖ 2026-06-27 91dc0591 funnel walk (Refs #909): checkout renders "✓ Credit covers this order — no card charge needed" + Due now OMR 0.000, no Stripe card form; "Launch my tenant" → Org `walk-stranger-one` CR + namespace + per-Org console 200. `uat91-row85-credit-covers-0-omr-due.png` |
| 86 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Provisioning progress timeline advances to Done (Creating Org → Committing manifests → Provisioning vCluster → Deploying WordPress → TLS → Health), no hang/red step. | — | ⛔ | hw220-walk-2026-07-04(A): Provisioning timeline needs a live funnel launch (write). Env signal: nstar org-layer steps all "done" but app layer wedged (F5) ‖ §A/§C/§I 91dc0591 (C1, supersedes prior ⛔): post-#4471 org-controller RBAC fix, funnel Org uatwalk91 provisions to running WordPress+MySQL — provisioning timeline reaches a live app (no networking-up wedge) |
| 87 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | After Launch, the marketplace redirects to the per-Org console — URL becomes the per-Org console and loads with publicly-trusted TLS (no cert error / connection-refused). | [console (org)](https://console.orgslug.omani.homes/) | ⚠️ | hw220-walk-2026-07-04(A): Per-Org console serving contract holds: console.nstar.omani.homes → HTTP 200, valid LE cert (CN nstar.omani.homes, notAfter 2026-10-01); the funnel-redirect leg itself unwalked (write-gated) ‖ 2026-06-27 91dc0591 authed sweep: per-Org console.uatwalk91.omani.homes → HTTP 200 with valid per-Org LE TLS (no cert error / connection-refused) |
| 88 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Per-Org console landing: the stranger is signed in zero-click as the Org owner (their email in the avatar, their Org in the header) — no login/PIN form. | [console (org)](https://console.orgslug.omani.homes/dashboard) | ⛔ | hw220-walk-2026-07-04(A): Zero-click owner sign-in needs the org-handover session (not mintable read-only) ‖ §C/§I 91dc0591 (C1, supersedes prior ⛔): per-Org console.uatwalk91.omani.homes → HTTP 200 with valid per-Org LE cert; handover JWT zero-touch sign-in landed (§C Gate 5b 302 /dashboard, whoami tier=owner) |
| 89 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Per-Org console → Applications view shows the purchased WordPress app card Running/Healthy → click Open. | [console (org)](https://console.orgslug.omani.homes/applications) | ⛔ | hw220-walk-2026-07-04(A): Per-Org Applications view needs org-console session; NOTE nstar WordPress is NOT Running anyway (F5) — would fail if walked ‖ §I 91dc0591 (C1, supersedes prior ⛔): uatwalk91 WordPress app card converged Running/Healthy — wordpress.uatwalk91.omani.homes serves HTTP 200 |
| 90 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) [#3860](https://github.com/openova-io/openova/issues/3860) | Terminal acceptance: the purchased WordPress app SERVES at its own FQDN the app FQDN — the live rendered WordPress site (not 404/502/cert error). | [wordpress (org)](https://wordpress.orgslug.omani.homes/) | ❌ | hw220-walk-2026-07-04(A): Per-Org WordPress does NOT serve: wordpress.nstar.omani.homes → curl 000 (no route); root cause F5: nstar bp-keycloak HR "reconciliation in progress" ~12h → bp-wordpress-tenant (+newapi/openclaw/stalwart-tenant) dependency-blocked; nstar Continuum bp-wordpress-tenant Degraded ‖ 2026-06-24 LIVE (demo Org, dep `4635277cae4ffed9`, [#3785](https://github.com/openova-io/openova/issues/3785) / PR [#4204](https://github.com/openova-io/openova/pull/4204)) — **`GET https://wordpress.demo.omani.homes/` → HTTP 302 → `/wp-admin/install.php` → 200** serving the live WordPress site (Playwright: Page Title **"WordPress › Installation"**, language picker + Continue render over public DNS + valid `*.demo.omani.homes` wildcard TLS, no cert error). Was a bare envoy **404**. Root cause: the bp-wordpress-tenant chart shipped only a traefik Ingress (no HTTPRoute on a Cilium Sovereign), and once routed, a K8s NetworkPolicy default-denied the gateway `ingress`-entity hop (503). Fixed at root: chart 0.4.13 renders an HTTPRoute on `cilium-gateway-console` (wildcard TLS, no per-host cert) + a CiliumNetworkPolicy `fromEntities:[ingress]`; overlay emits `gateway.*`. ✅ — §I 91dc0591 (C1): RE-CONFIRMED on fresh prov — wordpress.uatwalk91.omani.homes → HTTP 200 live WordPress over valid `*.omani.homes` wildcard LE TLS. |
| 91 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | While signed in, re-open the marketplace root → the returning-user redirect sends the customer to their own Org console, never to `console.openova.io` / the mothership. | [/](https://marketplace.omantel.biz/) | ⛔ | hw220-walk-2026-07-04(A): Returning-user redirect needs a signed-in marketplace customer session (write-gated) ‖ 2026-06-27 91dc0591 funnel walk (Refs #909): signed-in re-open of the marketplace root → returning-redirect to `console.walk-stranger-one.<sov>` via `/launching`; network log shows ZERO requests to console.openova.io/mothership. `uat91-row91-returning-redirect-to-own-org-console.png`. (Exposed+FIXED a wrong-TLD host-derivation edge — #4564/PR #4565 merged; the "never mothership" contract itself passes.) |
| 92 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | As the signed-in customer, rapidly re-submit checkout/redeem >5× in a few seconds → after ~5 attempts a rate-limit notice appears; a single legitimate redeem is unaffected. | [/checkout](https://marketplace.omantel.biz/checkout) | ⛔ | hw220-walk-2026-07-04(A): Rate-limit probe needs authed checkout POSTs (write) ‖ 2026-06-27 91dc0591 funnel walk (Refs #909): rapid POST `/api/billing/checkout` ×7 with the live customer token → attempts 5/6/7 return HTTP 429 "checkout rate-limit exceeded"; after the 60s window cleared, a single call passed the rate-gate (HTTP 400 plan-format, NOT 429) → single legitimate redeem unaffected. |
| 93 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | Generality: mint a 2nd voucher and re-walk B.1→B.4 with a different slug (`walk-stranger-two`) and a different pool-TLD (`omani.rest`) → a 2nd Organization provisions and lands signed-in. | — | ⛔ | hw220-walk-2026-07-04(A): 2nd voucher + 2nd Org = writes |
| 94 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The 2nd Org's console lands signed-in on a different TLD (the 2nd-Org console) — identical zero-click contract, no special-casing. | [console (org)](https://console.orgslug2.omani.rest/dashboard) | ⛔ | hw220-walk-2026-07-04(A): Depends on 93 |
| 95 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The 2nd Org's purchased app serves at its own different-TLD FQDN (the 2nd-Org app) — two Orgs, two TLDs, two running apps, identical mechanism. | [wordpress (org)](https://wordpress.orgslug2.omani.rest/) | ⛔ | hw220-walk-2026-07-04(A): Depends on 93 |
| 96 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Handover URL → lands on `/dashboard` already signed-in as the owner (avatar E), no login form. | — | ☐ | — |
| 97 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Dashboard renders the cluster treemap and the LAYER 1 / LAYER 2 grouping comboboxes are visible. | — | ☐ | — |
| 98 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Click LAYER 1 combobox → select `vCluster`; the treemap regroups into one labelled block per vCluster (`host` / `mgmt` / `rtz` / `dmz`), mgmt block visible + clickable. | — | ⚠️ | hw220-walk-2026-07-04(A): Superseded by #4325 de-vcluster: NO mgmt/rtz/dmz vclusters exist on hw220 (kubectl ns scan empty) — test asserts a dead placement model ‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 99 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **grafana** tile sits inside the **mgmt** block, not **host**. | — | ⚠️ | hw220-walk-2026-07-04(A): Superseded (#4325) — no mgmt vcluster exists ‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 100 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **harbor** tile sits inside the **mgmt** block, not **host**. | — | ⚠️ | hw220-walk-2026-07-04(A): Superseded (#4325) — no mgmt vcluster exists ‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 101 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **keycloak** tile sits inside the **mgmt** block, not **host**. | — | ⚠️ | hw220-walk-2026-07-04(A): Superseded (#4325) — no mgmt vcluster exists ‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 102 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **gitea** tile sits inside the **mgmt** block, not **host**. | — | ⚠️ | hw220-walk-2026-07-04(A): Superseded (#4325) — no mgmt vcluster exists ‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 103 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **openbao** tile sits inside the **mgmt** block, not **host**. | — | ⚠️ | hw220-walk-2026-07-04(A): Superseded (#4325) — no mgmt vcluster exists ‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 104 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) [#3831](https://github.com/openova-io/openova/issues/3831) | On the LAYER1=vCluster treemap, the **newapi** tile sits inside the **mgmt** block, not **host**. | — | ⚠️ | hw220-walk-2026-07-04(A): Superseded (#4325) — no mgmt vcluster exists ‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 105 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | On the LAYER1=vCluster treemap, the **guacamole** tile sits inside the **mgmt** block, not **host**. | — | ⚠️ | hw220-walk-2026-07-04(A): Superseded (#4325) — no mgmt vcluster exists ‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 106 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Drill into the **mgmt** block: its tiles include all 7 named apps (grafana/harbor/key… alongside loki/mimir/nats/tempo. | — | ⚠️ | hw220-walk-2026-07-04(A): Superseded (#4325) — no mgmt vcluster exists ‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 107 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Read the **host** block on the same treemap: none of the 7 named apps appear under `host`. | — | ⚠️ | hw220-walk-2026-07-04(A): Superseded (#4325) — no mgmt block to read ‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 108 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Open the keycloak app card → its placement detail reads `mgmt` (the per-app placement mirrors the treemap block), not `host`. | — | ⚠️ | hw220-walk-2026-07-04(A): Superseded (#4325); note: create-dialog + treemap selects still carry host/mgmt/dmz/rtz vocab (stale strings, see F10 note) ‖ superseded by #4325 de-vcluster — re-baseline (#3642), not a product fault. §G 91dc0591: mgmt/rtz/dmz vClusters no longer exist (each named app runs in its own host ns); the #3642 test-case asserts a dead mgmt-vcluster model. uat-91dc-placement-98 |
| 109 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | The sovereign-realm account console renders for the owner (no second login, no error dialog). | [auth](https://auth.omantel.biz/realms/sovereign/account) | ⛔ | hw220-walk-2026-07-04(A): KC sovereign account console 302→KC login — stock-correct; handover JWT ≠ KC browser SSO cookie (session-walled, precedent 2026-06-27) ‖ 2026-06-27 91dc0591 (3-agent batch) RECLASSIFIED ❌→⛔ (session-walled, NOT a product fault): the KC sovereign account-console requires a KC browser SSO cookie; the harness drives a handover JWT (a local session token, not a KC browser SSO session) → stock Keycloak correctly demands login. Stock-KC-correct behaviour; the prior ❌ was a harness limitation, not an account-console fault |
| 110 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Gitea opens already signed in (avatar/menu shows the SSO user), repo list renders — no Gitea login form. | — | ⛔ | hw220-walk-2026-07-04(A): Gitea signed-in landing session-walled; silent-SSO broker chain proven live (row 31) ‖ §B 91dc0591: signed-in "emrah.baysal - Dashboard", sso-gitea.png |
| 111 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Harbor opens signed in, the projects list renders — no Harbor login form, no gateway error page. | [harbor](https://registry.omantel.biz) | ❌ | hw220-walk-2026-07-04(A): Harbor signed-in projects list impossible: /c/oidc/login 412 db_auth (F2); registry/ 200 serves only the anonymous shell ‖ 2026-06-27 91dc0591: same chain as row 32 — registry.omantel.biz/c/oidc/login → 302 sovereign realm client_id=harbor; registry/ → 200 (no gateway error); harbor-sso-oidc-credentials synced, KC client 'harbor' registered |
| 112 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Grafana opens signed in (no Grafana login), the home dashboard renders. | — | ❌ | hw220-walk-2026-07-04(A): Grafana signed-in landing impossible: HTTP 503 (F1) ‖ §B 91dc0591: silent SSO → Grafana Home, sso-grafana.png |
| 113 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | The OpenBao UI renders signed in via OIDC — no manual token/unseal prompt blocking the landing. | — | ⛔ | hw220-walk-2026-07-04(A): OpenBao OIDC landing shim 200 (no token prompt at gate); final authed render session-walled ‖ §B 91dc0591: OIDC landing reaches `/ui/vault/secrets` (no token prompt) but then 403 — external-group→policy binding fails, sso-openbao.png |
| 114 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) [#4136](https://github.com/openova-io/openova/issues/4136) | newapi opens signed in, its main console renders — no login form, no upstream-connect error. | [newapi](https://newapi.omantel.biz/) | ⛔ | hw220-walk-2026-07-04(A): newapi signed-in console session-walled; negative halves proven (200 shim, provider 'sovereign' recognized, no upstream-connect error) ‖ 2026-06-27 91dc0591: #4477 `resync_provider_secret()` (chart bp-newapi@1.4.137) resolved the stale `custom_oauth_providers` client_secret live — DB slug `sovereign` enabled, secret prefix `s9uH2sl2…` byte-matches the live KC-synced `newapi-oidc` secret; pod log "Loaded 1 custom OAuth providers (OpenOva SSO / sovereign)"; browser walk of newapi.omantel.biz brokers through KC, no "Unknown OAuth provider" |
| 115 | placement | [#3642](https://github.com/openova-io/openova/issues/3642) | Guacamole opens signed in, the connections list renders — no Guacamole login form. | [guacamole.omantel.biz](https://guacamole.omantel.biz/) | ⛔ | hw220-walk-2026-07-04(A): Guacamole signed-in list session-walled (app serves 200) ‖ §B 91dc0591: signed-in connections list as emrah.baysal@, sso-guacamole.png |
| 116 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Organizations directory: the page title / heading reads "Organizations", never "Tenants"/"SME Tenants". | — | ☐ | — |
| 117 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Directory org cards / list rows: column headers read "Organization / Kind / Tier / Billing / Isolation / Status"; rows labeled "Organization", never "tenant"/"SME". | — | ☐ | — |
| 118 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Left-nav sidebar label for this section reads "Organizations", not "Tenants"/"SME". | — | ☐ | — |
| 119 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Create-organization flow: the form title, field labels, and submit button all say "Organization" (no "SME tenant slug" / "Onboard tenant" persona words). | — | ☐ | — |
| 120 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Organization-detail view: heading "Acme Corp", breadcrumb "← Organizations", field labels Slug/Kind/Tier/Billing mode/Isolation/Stat… — no "tenant"/"SME". | — | ☐ | — |
| 121 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | BSS / billing screen: billing is framed as "This organization is in showback mode…", zero "tenant"/"SME" leaks. | — | ☐ | — |
| 122 | orgs | [#3383](https://github.com/openova-io/openova/issues/3383) | Legacy `/bss/tenants` URL (PR #3390 alias) resolves and redirects to `/organizations`, rendering the directory (H1 "Organizations") — not a 404, not a login redirect. | — | ☐ | — |
| 123 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Handover URL → lands on `/dashboard` already signed-in as the owner (avatar E), no login form (everything below is admin-gated). | — | ☐ | — |
| 124 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Catalog grid renders Blueprint cards in a tile grid, each with an icon + summary; the Alloy card is visible. | — | ☐ | — |
| 125 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the Alloy card → the detail page renders: a hero (icon + name + summary + Edit IaC), an About section, and an Instances list; no login redirect. | — | ☐ | — |
| 126 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the admin Edit affordance in the hero → an edit form drops INLINE into the detail page (no modal overlay); fields appear in-place under the hero. | — | ☐ | — |
| 127 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | In the inline form, change Summary to `RECONCILE-PROOF-<ts>` → Save → the page refreshes in place and the new summary shows in the hero. | — | ⚠️ | hw220-walk-2026-07-04(B): Write-gated on the live env. Endpoint present: `GET /api/v1/catalog/bp-alloy/iac` → 405 (PUT-only route exists). Prior durable pass: 25aadcfc write-walk (`PUT /iac` 200 `{committed:true}` + Gitea commit). ‖ write-mutation walk on fresh prov 25aadcfc — `PUT /api/v1/catalog/bp-alloy/iac` → 200 `{committed:true}` + durable Gitea commit; edited summary persists on re-GET and shows in the hero |
| 128 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Back on the grid, the Alloy card summary now reads `RECONCILE-PROOF-<ts>` — the edit propagated to the card, not just the detail page. | — | ⚠️ | hw220-walk-2026-07-04(B): Same write-gate + prior 25aadcfc evidence. ‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; the edited summary reads back from the same `blueprint.yaml`, so the grid card resolves the propagated value |
| 129 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Hard-reload the detail page → the summary is still `RECONCILE-PROOF-<ts>` — the edit persisted across a reload, not an in-memory overlay. | — | ⚠️ | hw220-walk-2026-07-04(B): Same write-gate + prior 25aadcfc evidence. ‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit (`catalog: edit bp-alloy full IaC #3668`); edit survives re-GET, not an in-memory overlay |
| 130 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The non-card `version` field renders as a `v1.0.1` chip in the hero, and Edit IaC exposes the full CR including `version` for editing. | — | ☐ | — |
| 131 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Note the current hero logo (the Alloy glyph) as the baseline before an icon edit. | — | ☐ | — |
| 132 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit → in the Light-theme icon field paste a distinct image → Save → a "Saved to IaC ✓" confirmation shows and the page refreshes. | — | ⚠️ | hw220-walk-2026-07-04(B): Icon-edit/save/persist rows: write-gated on live env; `PUT /iac` route present (405 on GET); prior durable pass on 25aadcfc (icon edits committed to Gitea, read back). 9 rows. ‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` with edited `card.iconLight` → 200 `{committed:true, path:catalog-sovereign/bp-alloy/blueprint.yaml}` + durable Gitea commit |
| 133 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Observe the hero — it now shows the new logo; the render reads the edited `card.iconLight` first (IaC-first), not the bundled asset. | — | ⚠️ | hw220-walk-2026-07-04(B): Icon-edit/save/persist rows: write-gated on live env; `PUT /iac` route present (405 on GET); prior durable pass on 25aadcfc (icon edits committed to Gitea, read back). 9 rows. ‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; edited `card.iconLight` reads back on re-GET (IaC-first), not the bundled asset |
| 134 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Return to the grid — the Alloy card icon is now the new logo (the grid tile resolves the same edited `card.iconLight`). | — | ⚠️ | hw220-walk-2026-07-04(B): Icon-edit/save/persist rows: write-gated on live env; `PUT /iac` route present (405 on GET); prior durable pass on 25aadcfc (icon edits committed to Gitea, read back). 9 rows. ‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; grid tile + detail hero both resolve the same persisted `card.iconLight` |
| 135 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Reload the detail page — the hero is still the new logo (render reads the persisted IaC icon on every load). | — | ⚠️ | hw220-walk-2026-07-04(B): Icon-edit/save/persist rows: write-gated on live env; `PUT /iac` route present (405 on GET); prior durable pass on 25aadcfc (icon edits committed to Gitea, read back). 9 rows. ‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; the persisted IaC icon reads back on every re-GET, hero stays the new logo |
| 136 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit again — the Light-theme icon field shows the current IaC value, falling back to the bundled asset only when IaC carries none. | — | ⚠️ | hw220-walk-2026-07-04(B): Icon-edit/save/persist rows: write-gated on live env; `PUT /iac` route present (405 on GET); prior durable pass on 25aadcfc (icon edits committed to Gitea, read back). 9 rows. ‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; the Edit form pre-fills the persisted IaC `card.iconLight` value on re-open |
| 137 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit → click the icon picker (`iconpicker-*`) → a thumbnail grid of vendored `component-logos/*` assets opens (a role=listbox grid of logo tiles). | — | ⚠️ | hw220-walk-2026-07-04(B): Icon-edit/save/persist rows: write-gated on live env; `PUT /iac` route present (405 on GET); prior durable pass on 25aadcfc (icon edits committed to Gitea, read back). 9 rows. ‖ write-mutation walk on fresh prov 25aadcfc — icon-picker selection persists via `PUT /iac` → 200 + durable Gitea commit; the picked `component-logos/*` asset reads back on re-GET |
| 138 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click `cilium.svg` in the picker grid → the icon field + a live preview swatch update to the Cilium logo. | — | ⚠️ | hw220-walk-2026-07-04(B): Icon-edit/save/persist rows: write-gated on live env; `PUT /iac` route present (405 on GET); prior durable pass on 25aadcfc (icon edits committed to Gitea, read back). 9 rows. ‖ write-mutation walk on fresh prov 25aadcfc — picked `cilium.svg` saved via `PUT /iac` → 200 + durable Gitea commit; `card.iconLight=cilium.svg` reads back on re-GET |
| 139 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Save → reload — the hero is now the Cilium logo (the picker selection persisted to IaC and renders on hero + grid card). | — | ⚠️ | hw220-walk-2026-07-04(B): Icon-edit/save/persist rows: write-gated on live env; `PUT /iac` route present (405 on GET); prior durable pass on 25aadcfc (icon edits committed to Gitea, read back). 9 rows. ‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` 200 + durable Gitea commit; the picker selection persists to IaC and reads back on hero + grid re-GET |
| 140 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | On Save, the durable-commit verdict (git outcome) is surfaced in-UI (the Edit-IaC `managed-by: manual • in sync` indicator + {stored:true,commit… not a silent store success. | — | ⚠️ | hw220-walk-2026-07-04(B): Icon-edit/save/persist rows: write-gated on live env; `PUT /iac` route present (405 on GET); prior durable pass on 25aadcfc (icon edits committed to Gitea, read back). 9 rows. ‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` returns `{committed:true, path:catalog-sovereign/bp-alloy/blueprint.yaml}` (the durable-commit verdict), not a silent store success; durable Gitea commit verified in the bare repo |
| 141 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Hover the summary line in the hero → a pencil/edit affordance appears ON the field (`cif-summary-edit` → `cif-summary-input`), inline, without opening the full form. | https://console.omantel.biz/catalog/bp-alloy | ☐ | — |
| 142 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click the summary → type a value → Save → only the summary updates in place; no full-form modal opens. | https://console.omantel.biz/catalog/bp-wordpress | ⚠️ | hw220-walk-2026-07-04(B): Inline-summary Save = write-gated; prior 25aadcfc pass (`saveCatalogBlueprintIaC` → `PUT /iac` 200). ‖ write-mutation walk on fresh prov 25aadcfc — inline summary edit on bp-wordpress routes through `saveCatalogBlueprintIaC` → `PUT /iac` → 200 + durable Gitea commit; only the summary field mutates in the same `blueprint.yaml` |
| 143 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Repeat the inline edit for the name field (`cif-name-edit` → `cif-name-input`) — it edits in place and saves just that field. | https://console.omantel.biz/catalog/bp-wordpress | ⚠️ | hw220-walk-2026-07-04(B): Inline-name Save = write-gated; prior 25aadcfc pass. ‖ write-mutation walk on fresh prov 25aadcfc — inline `cif-name` edit on bp-wordpress → `PUT /iac` → 200 + durable Gitea commit; just the name field mutates in the same `blueprint.yaml` |
| 144 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Click Edit IaC (`catalog-detail-ed… admin only) → the full `blueprint.yaml` opens in the YAML editor (the entire CR, not just 7 card fields). | https://console.omantel.biz/catalog/bp-alloy | ☐ | — |
| 145 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Change a field in the editor → Commit → a Show-diff Current/Proposed side-by-side renders the change and the commit succeeds (confirmation appears). | https://console.omantel.biz/catalog/bp-alloy | ⚠️ | hw220-walk-2026-07-04(B): Commit = write-gated; prior 25aadcfc pass (200 `{committed:true}`). ‖ write-mutation walk on fresh prov 25aadcfc — YAML-editor Commit on bp-alloy → `PUT /iac` → 200 `{committed:true}` + durable Gitea commit; the changed field reads back on re-GET |
| 146 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The editor subtitle states it directly: "Commit writes the IaC source of truth; Flux reconciles it… Both this editor and the card form above write the same file." | https://console.omantel.biz/catalog/bp-alloy | ☐ | — |
| 147 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Open the WordPress detail page → Edit → change Summary → Save → reload → the summary persists, exactly as for Alloy. | https://console.omantel.biz/catalog/bp-wordpress | ⚠️ | hw220-walk-2026-07-04(B): WordPress edit/commit rows: write-gated; bp-wordpress detail renders identical chrome (Edit IaC + About). Prior 25aadcfc pass. `catalog-wordpress.png` ‖ write-mutation walk on fresh prov 25aadcfc — bp-wordpress summary edit → `PUT /iac` → 200 + durable Gitea commit; persists on re-GET exactly as for Alloy (same `PUT .../iac` contract) |
| 148 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | WordPress: Edit IaC → edit `spec.manifests` → Commit → reload — the same YamlEditor edits a structurally-different blueprint's manifests in place. | https://console.omantel.biz/catalog/bp-wordpress | ⚠️ | hw220-walk-2026-07-04(B): WordPress edit/commit rows: write-gated; bp-wordpress detail renders identical chrome (Edit IaC + About). Prior 25aadcfc pass. `catalog-wordpress.png` ‖ write-mutation walk on fresh prov 25aadcfc — bp-wordpress `spec.manifests` edit via the same YamlEditor → `PUT /iac` → 200 + durable Gitea commit; the structurally-different blueprint's manifests mutate in place |
| 149 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | WordPress: Edit → Light-theme icon → distinct image → Save → reload — the WordPress hero icon visibly changes. | https://console.omantel.biz/catalog/bp-wordpress | ⚠️ | hw220-walk-2026-07-04(B): WordPress edit/commit rows: write-gated; bp-wordpress detail renders identical chrome (Edit IaC + About). Prior 25aadcfc pass. `catalog-wordpress.png` ‖ write-mutation walk on fresh prov 25aadcfc — bp-wordpress `card.iconLight` edit → `PUT /iac` → 200 + durable Gitea commit; the new icon reads back on re-GET, hero changes |
| 150 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | PostgreSQL detail renders the SAME edit surface (hero icon, Edit IaC, clickable cards) and Edit IaC exposes `contextSchema` (a blueprint carrying contextSchema`/`sha… | https://console.omantel.biz/catalog/bp-postgres | ☐ | — |
| 151 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Alloy + Postgres render the IDENTICAL edit chrome (same cif-icon-edit`/`cif… same Edit-IaC YamlEditor) — no blueprint-specific UI. | https://console.omantel.biz/catalog/bp-postgres | ☐ | — |
| 152 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The catalog detail page renders (hero · About · Instances) and opens an INLINE Edit form (no modal) — acceptance headline 1. | https://console.omantel.biz/catalog/bp-alloy | ☐ | — |
| 153 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | A summary edit Saves, updates the page AND the grid card, and persists across a reload — acceptance headline 2. | https://console.omantel.biz/catalog/bp-wordpress | ⚠️ | hw220-walk-2026-07-04(B): Write-gated acceptance headlines 2/3/4/5; prior 25aadcfc durable-commit passes on both bp-alloy + bp-wordpress. 4 rows. ‖ write-mutation walk on fresh prov 25aadcfc — summary edit → `PUT /iac` → 200 + durable Gitea commit; persists on re-GET (page + grid resolve the same file) — acceptance headline 2 |
| 154 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | A non-card field edit (`version`) persists — the whole CR is editable, not a 7-field overlay — acceptance headline 3. | https://console.omantel.biz/catalog/bp-alloy | ⚠️ | hw220-walk-2026-07-04(B): Write-gated acceptance headlines 2/3/4/5; prior 25aadcfc durable-commit passes on both bp-alloy + bp-wordpress. 4 rows. ‖ write-mutation walk on fresh prov 25aadcfc — non-card `version` edit via Edit-IaC → `PUT /iac` → 200 + durable Gitea commit; the whole CR is editable, persists on re-GET — acceptance headline 3 |
| 155 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The edited icon renders on hero + grid + survives reload; the form pre-fills the IaC icon; the picker grid works — acceptance headline 4. | https://console.omantel.biz/catalog/bp-wordpress | ⚠️ | hw220-walk-2026-07-04(B): Write-gated acceptance headlines 2/3/4/5; prior 25aadcfc durable-commit passes on both bp-alloy + bp-wordpress. 4 rows. ‖ write-mutation walk on fresh prov 25aadcfc — icon edit → `PUT /iac` → 200 + durable Gitea commit; persisted icon reads back on hero + grid re-GET, form pre-fills it — acceptance headline 4 |
| 156 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Save surfaces the IaC-commit verdict (`committed:true` + `• in sync`), not a bare store success — acceptance headline 5. | https://console.omantel.biz/catalog/bp-alloy | ⚠️ | hw220-walk-2026-07-04(B): Write-gated acceptance headlines 2/3/4/5; prior 25aadcfc durable-commit passes on both bp-alloy + bp-wordpress. 4 rows. ‖ write-mutation walk on fresh prov 25aadcfc — `PUT /iac` returns `{committed:true, path:catalog-sovereign/bp-alloy/blueprint.yaml}` + durable Gitea commit, not a bare store success — acceptance headline 5 |
| 157 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | Per-field inline edit for cards (`cif-*`) + the full-CR YamlEditor (Edit IaC) for the rest, both writing the same IaC source — acceptance headline 6. | https://console.omantel.biz/catalog/bp-alloy | ☐ | — |
| 158 | catalog | [#3668](https://github.com/openova-io/openova/issues/3668) | The identical edit mechanism works on a 2nd + 3rd blueprint — no per-blueprint UI (Alloy + Postgres + a third) — acceptance headline 7. | https://console.omantel.biz/catalog/bp-postgres | ☐ | — |
| 159 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Settings → Sovereignty section renders a "Cluster sovereignty" panel with a "TETHERED" badge + an "Achieve True Sovereignty" cutover CTA (runs the 8-step cutover + 10-min egress-block self-test). | [/settings](https://console.omantel.biz/settings#sovereignty) | ☐ | — |
| 160 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Console nav + Settings sidebar expose a dedicated "Sovereignty" anchor (`#sovereignty`) that scrolls to + highlights the Cluster-sovereignty panel — the cutover trigger is a first-class surface. | — | ☐ | — |
| 161 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Open `/jobs` (zero-login, signed in as owner) → the canvas table renders a populated activity list (not a spinner, empty state, or login redirect). | — | ☐ | — |
| 162 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Find the `cutover` group row and expand it → it renders the 11 `cutover-step-*` rows (gitea-mirror, harbor-projects, harbor-prewarm, registry-pivot, … vcluster-registry-p… — the 11-step execution tree. | [/jobs](https://console.omantel.biz/jobs) | ☐ | — |
| 163 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | Each `cutover-step-*` row reads an honest per-step status (Succeeded / Running / Failed / Pending), never a premature green. | [/jobs](https://console.omantel.biz/jobs) | ☐ | — |
| 164 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | The cutover group status reflects its real children (a group with a failed child reads failed, not a fake Succeeded). | [/jobs](https://console.omantel.biz/jobs) | ☐ | — |
| 165 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | On a failed `cutover-step-*` row, a Re-run button is present (per-row, gated to Failed) — the operator can re-drive a failed cutover step from the browser. | [/jobs](https://console.omantel.biz/jobs) | ☐ | — | FAILED" carries a per-row **"Retry reconcile"** button (gated to Failed). `jobs-failed-rerun.png` |
| 166 | cutover | [#3379](https://github.com/openova-io/openova/issues/3379) | (After a COMPLETE cutover) the `cutover` group reads all-11-green on `/jobs` — every step Succeeded, including `egress-block-test` and `registry-pivot`. | [/jobs](https://console.omantel.biz/jobs) | ⛔ | hw220-walk-2026-07-04(B): Honestly gated: cutoverComplete=false on this env — step-08 egress-block-test FAILED (`lastError: watch Job catalyst/cutover-egress-block-test-1783069444: channel closed before terminal condition`), steps 9–11 pending, 63%. All-11-green unwalkable here. (Prior 5150f96 11/11 evidence stands as labeled history.) ‖ Cutover prov `5150f96` 2026-06-28: `cutoverComplete=true`, **11/11** steps `result=success` (incl. `egress-block-test` 671s deny-egress hold green, `registry-pivot`). First clean-run cutoverComplete. |
| 167 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Open the console root in a fresh tab → land on the operator dashboard signed in as the sovereign-admin (no login form, no password field). | — | ☐ | — |
| 168 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Open `/jobs` → the canvas table renders a populated list of activity rows (not a spinner, empty state, or login redirect). | — | ☐ | — |
| 169 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | The Kind column is present in the header and each row shows its kind; full header = Name·Kind·App·Deps·… | — | ☐ | — |
| 170 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Scroll/search to the `install-openbao` row → it renders green / Succeeded (the install is honestly green). | [/jobs](https://console.omantel.biz/jobs) | ⚠️ | hw220-walk-2026-07-04(B): NA-superseded (#3916 lens split) as before; the install IS green: HR bp-openbao Ready=True (chart 1.2.59), openbao-0 1/1 Running. ‖ 2026-06-27 91dc0591 authed sweep: NA — install-openbao on /jobs is superseded by the #3916 jobs-lens split. The install IS green (bp-openbao Ready, openbao-0 1/1) but it now lives in the recon lens, not /jobs |
| 171 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Every rendered row maps to a real HelmRelease install / terraform stage (no placeholder, no synthetic/fabricated entry). | — | ☐ | — |
| 172 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Set the Status filter to `failed` → the table shows the genuinely-failing rows, each with an honest failed status (the Status filter works). | [/jobs](https://console.omantel.biz/jobs) | ☐ | — |
| 173 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Leave the table on screen ~30s → rows update live (tail) as reconciliation progresses; a status badge changes in place without a manual reload. | — | ☐ | — |
| 174 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | On a Failed row (Status=failed), a Re-run / Retry-reconcile button is present on the row (visible on the row or on hover). | [/jobs](https://console.omantel.biz/jobs) | ☐ | — |
| 175 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | On a Succeeded / healthy / Confirming row, NO Re-run button renders — the control is gated to Failed rows only. | [/jobs](https://console.omantel.biz/jobs) | ☐ | — |
| 176 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Click Re-run on a Failed row → a success toast/feedback appears and the button flips in place (e.g. `Requesting…`) — the browser triggered a re-reconcile, no terminal. | [/jobs](https://console.omantel.biz/jobs) | ⚠️ | hw220-walk-2026-07-04(B): Click-Re-run is a live mutation → read-only-gated this walk. Mechanism present (buttons live); prior live pass 91dc0591 (#4514 delete+recreate proven). ‖ 2026-06-27 91dc0591 (Refs #909): #4511/#4514 ROLLED LIVE — catalyst-api image `e976ff0` (contains the #4514 merge 0988d6d; jobs_retry.go KindTask branch now calls recreateStandaloneJob: DELETE old + CREATE `<name>-rerun-<unix>` with immutable fields stripped). Reproduced live on the very Job the PR cited: `mimir/mimir-make-minio-buckets-rerun-1782552889` exists **Complete** (created 09:34:49Z) and the original `mimir-make-minio-buckets` is **NotFound** (deleted by the rerun) — the 502 'spec.template field is immutable' no longer occurs |
| 177 | jobs | [#3646](https://github.com/openova-io/openova/issues/3646) | Use the same Re-run button on a Failed row — one remediation mechanism across rows, no per-kind UI (the single-table/single… shape). | [/jobs](https://console.omantel.biz/jobs) | ☐ | — |
| 178 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | The signed handover URL → lands directly on `/dashboard` signed-in (env switcher shows the live env, avatar E, no login form). | — | ⚠️ | hw220-walk-2026-07-04(B): Signed handover-URL leg not re-fired (no fresh handover link this walk); substance proven via authed session: /dashboard signed-in as owner, env switcher shows hw220.omani.works, full sidebar, no login form. ‖ §I 91dc0591: console.omantel.biz/dashboard 200, owner emrah.baysal@/omantel.biz, full sidebar, no login form |
| 179 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Click the avatar (top-right) → menu reads "Signed in as the owner" with a Sign-out item — confirms the landed identity is the owner-admin. | [console](https://console.omantel.biz/dashboard) | ☐ | — |
| 180 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Grafana bare URL → lands on Grafana Home ("Welcome to Grafana", full UI, Profile avatar), `?orgId=1`, no login form (SSO landed signed-in). | [grafana](https://grafana.omantel.biz/) | ❌ | hw220-walk-2026-07-04(B): **grafana.hw220.omani.works → HTTP 503 externally.** Root cause: region-a grafana container CrashLoopBackOff ×51 — `Error: ✗ pq: password authentication failed for user "grafana"` (stale DB credential vs shared-pg; `grafana-database-env` 15h old). Region-b grafana 3/3 Running. ‖ §I 91dc0591: Grafana → Grafana Home, no login (corroborates §B row 30); no-cookie curl bounces to IdP, owner-session lands Home. sso-grafana.png |
| 181 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Harbor (registry) bare URL → lands on `/harbor/projects` (projects, repos, Administration nav), no login form; user dropdown the owner. | [registry](https://registry.omantel.biz/harbor/projects) | ⚠️ | hw220-walk-2026-07-04(B): registry.hw220.omani.works → HTTP 200 (Harbor SPA shell, no gateway error); owner signed-in render is KC-session-walled headless. ‖ §I 91dc0591: registry.omantel.biz/harbor/projects → 200 (SPA shell), no gateway error; owner-session signed-in not browser-verified (harness artifact) |
| 182 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | Gitea bare URL → lands on the gitea dashboard titled "emrah.baysal - Dashboard - Catalyst Gitea", logged in; URL stays on :443. | [gitea](https://gitea.omantel.biz/) | ⛔ | hw220-walk-2026-07-04(B): Session-walled: gitea.hw220.omani.works → 303 `/user/login` (service alive; completing KC SSO headless not assertable — no KC browser session; console cookie ≠ KC cookie). ‖ §I 91dc0591: Gitea → 'emrah.baysal - Dashboard', logged in, stays :443 (corroborates §B row 31). sso-gitea.png |
| 183 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | OpenBao bare UI → final rendered screen is the authenticated Vault session (`/ui/vault/secrets`, Secrets Engines with cubbyhole/ + secret/ kv), NO `/ui/vault/auth` token form. | [openbao](https://bao.omantel.biz/ui/) | ⛔ | hw220-walk-2026-07-04(B): Session-walled per 2026-06-27 precedent: bao.hw220.omani.works → 302 `/sso/landing` (UI alive); final authed `/ui/vault/secrets` render needs a completed KC session. ‖ 2026-06-27 91dc0591 (3-agent batch) RECLASSIFIED ❌→⛔ (session-walled, NOT a product fault): the 403 policy-binding fault is already FIXED LIVE by #4522 (bp-openbao@1.2.58 OIDC auth_url requests `scope=openid+groups`, sso-operator-admin policy written + sovereign-admins mapped — sibling row 33 ✅). Row 183 differs from row 33 ONLY by asserting the final rendered /ui/vault/secrets Secrets-Engines screen behind a COMPLETED KC SSO session, which is SSO-session-walled headless (test-session 404-gated in prod). Server-side fault resolved; this row is session-walled, not a fault |
| 184 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | — | [UAT.md](https://github.com/openova-io/openova/blob/main/docs/ledger/UAT.md) | ⚠️ | hw220-walk-2026-07-04(B): No test case defined in ledger (literal —). ‖ §I 91dc0591: no test case defined in ledger (literal —) — not click-verified, harness artifact |
| 185 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | No stale predecessor-env references anywhere in the rendered `UAT.md` (the table names only the live env). | [UAT.md](https://github.com/openova-io/openova/blob/main/docs/ledger/UAT.md) | ☐ | — |
| 186 | meta | [#3581](https://github.com/openova-io/openova/issues/3581) | — | [UAT.md](https://github.com/openova-io/openova/blob/main/docs/ledger/UAT.md) | ⚠️ | hw220-walk-2026-07-04(B): No test case defined in ledger (literal —). ‖ §I 91dc0591: no test case defined in ledger (literal —) — not click-verified, harness artifact |
| 187 | topology | [#4001](https://github.com/openova-io/openova/issues/4000) | Per-app Topology for a multi-region app (grafana) shows **Pattern: active-active** with 2 PRIMARY region cards — NOT singleton. | [/app/bp-grafana](https://console.omantel.biz/app/bp-grafana) | ❌ | hw220-walk-2026-07-04(B): Active-active grafana is BROKEN live: region-a pod `grafana-9ddcf597d-8pbc2` 2/3 CrashLoopBackOff (DB auth fail, restarts=51); only region-b serves 3/3. A truthful topology derivation cannot show 2 healthy PRIMARY regions right now. (Same root cause as row 180.) ‖ 2026-06-27 91dc0591 (Refs #909): grafana serves in BOTH regions live — region-a `grafana-578468b687-2jfdf` 3/3 Running + region-b `grafana-6f78dd5f44-6sp6g` 3/3 ready=true Running. The #3982 placement derivation keys on live Pods across both region clusters → 2 region targets, each Primary (multi-primary/active-active per the stateless-replicated rule), NOT singleton. Data-grounded (same standard as 64/71); UI render SSO-session-walled |
| 188 | topology | [#4001](https://github.com/openova-io/openova/issues/4000) | A genuine single-region app (catalyst-api) correctly shows **singleton** (no false multi-region). | [/app/bp-catalyst-platform](https://console.omantel.biz/app/bp-catalyst-platform) | ☐ | — |
| 189 | topology | [#4000](https://github.com/openova-io/openova/issues/4000) | Region-b kubeconfig self-heals EIP→private-IP **zero-touch** on restart (topology survives a fresh prov). | — | ☐ | — |
| 190 | jobs | [#3916](https://github.com/openova-io/openova/issues/3916) | `/jobs` lists **ONLY finite jobs** (provision/cutover steps, batch Jobs, CronJob runs) — ZERO continuous-reconciler rows. | [/jobs](https://console.omantel.biz/jobs) | ☐ | — |
| 191 | jobs | [#3916](https://github.com/openova-io/openova/issues/3916) | Continuous reconcilers (HelmRelease/Kustomization) do **NOT** appear in `/jobs` (they live in the recon lens). | [/jobs](https://console.omantel.biz/jobs) | ☐ | — |
| 192 | recon | [#3925](https://github.com/openova-io/openova/issues/3996) | The convergence **Reconciliation** link opens the cloud **RECON lens** (`view=graph&lens=reconciliation`), not the default cloud lens. | [/cloud?view=graph&lens=reconciliation](https://console.omantel.biz/cloud?view=graph&lens=reconciliation) | ☐ | — |
| 193 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Clicking a reconciler opens the **ArgoCD-like management surface** (drill-in). | [/cloud/resource/helmrelease/mgmt/bp-grafana/reconcile](https://console.omantel.biz/cloud/resource/helmrelease/mgmt/bp-grafana/reconcile) | ☐ | — |
| 194 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Recon surface lists Flux reconcilers with live status (Reconciled/Reconciling/Degraded/Suspended). | [/cloud?view=graph&lens=reconciliation](https://console.omantel.biz/cloud?view=graph&lens=reconciliation) | ☐ | — |
| 195 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | Drill a reconciler → its controller **logs** render. | [/cloud/resource/helmrelease/mgmt/bp-grafana/reconcile](https://console.omantel.biz/cloud/resource/helmrelease/mgmt/bp-grafana/reconcile) | ☐ | — |
| 196 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | **Reconcile** action → `reconcile.fluxcd.io/requestedAt` lands on the live object. | [/cloud/resource/helmrelease/mgmt/bp-grafana/reconcile](https://console.omantel.biz/cloud/resource/helmrelease/mgmt/bp-grafana/reconcile) | ⚠️ | hw220-walk-2026-07-04(B): Read-only-gated (firing Reconcile mutates the live HR). Button wired + present in the drill-in. ‖ 2026-06-27 91dc0591 (3-agent batch): the Reconcile button is wired + present in the drill-in surface; firing it mutates the live HR on the permanent Sovereign, so it's read-only-gated — needs a throwaway-prov to prove `requestedAt` lands |
| 197 | recon | [#3996](https://github.com/openova-io/openova/issues/3996) | **Suspend/Resume** → `spec.suspend` flips on the live object. | [/cloud/resource/helmrelease/mgmt/bp-grafana/reconcile](https://console.omantel.biz/cloud/resource/helmrelease/mgmt/bp-grafana/reconcile) | ⚠️ | hw220-walk-2026-07-04(B): Read-only-gated (Suspend/Resume flips spec.suspend). Button wired + present. ‖ 2026-06-27 91dc0591 (3-agent batch): the Suspend/Resume button is wired + present in the drill-in surface; firing it mutates the live HR on the permanent Sovereign, so it's read-only-gated — needs a throwaway-prov to prove `spec.suspend` flips |
| 198 | cloud | [#3987](https://github.com/openova-io/openova/issues/3987) | `/cloud` per-kind **helmreleases** page shows the real count (~65), not 0. | [/cloud?view=list&kind=helmreleases](https://console.omantel.biz/cloud?view=list&kind=helmreleases) | ☐ | — |
| 199 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **Gateway** page shows the live `cilium-gateway` (1). | — | ☐ | — |
| 200 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **HTTPRoutes** page shows 15. | — | ☐ | — |
| 201 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **NetworkPolicies** page shows the live policies (~10). | — | ☐ | — |
| 202 | cloud | [#3998](https://github.com/openova-io/openova/issues/3998) | Cloud **CiliumNetworkPolicies** page shows 5. | — | ☐ | — |
| 203 | cloud | [#4002](https://github.com/openova-io/openova/issues/4002) | Cloud **Load Balancers** page shows the real ELB (212.72.24.33), not 0. | — | ☐ | — |
| 204 | cloud | [#4002](https://github.com/openova-io/openova/issues/4002) | Cloud **Worker Nodes** page shows the real nodes, not 0. | — | ☐ | — |
| 205 | fleet | [#4003](https://github.com/openova-io/openova/issues/4003) | `/fleet/applications` returns the real app count (non-zero), not 0. | [/api/v1/fleet/applications](https://console.omantel.biz/api/v1/fleet/applications) | ☐ | — |
| 206 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | `kubectl get managed` is non-empty — Crossplane observes the OpenTofu-built infra. | — | ❌ | hw220-walk-2026-07-04(B): `kubectl get managed` → **No resources found**; zero Workspace CRs. Crossplane does NOT observe the tofu-built infra (see 239 root cause). ‖ §D 91dc0591: 0 CloudAdoption CRs applied — claims (24, real Huawei IDs, Observe-only) generated to `adoption-claims.yaml` but applied post-handover; handover never fired |
| 207 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | A `CloudAdoption` for the real ELB reaches Synced+Ready (Observe), `external-name` = the real ELB id. | — | ❌ | hw220-walk-2026-07-04(B): `adopt-loadbalancer-primary`: Synced=True but **Ready=False** ("Claim is waiting for composite resource to become Ready"), ADOPTED empty — never reaches Synced+Ready. All 24 claims in this state 14h. ‖ §D 91dc0591: 0 CloudAdoption CRs / 0 provider-opentofu Providers applied — handover never fired |
| 208 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | Adoption is **Observe-only** — the live ELB/nodes are untouched (no re-provision). | — | ⚠️ | hw220-walk-2026-07-04(B): Observe-only INTENT present (claims carry the ADR-0011 shape; live ELB/nodes untouched) but unprovable as a property of a pipeline that never became Ready. ‖ §D 91dc0591: generated claims carry `managementPolicies:[Observe]` per ADR-0011 but none applied (handover never fired) — cannot confirm live untouched |
| 209 | storage | [#3971](https://github.com/openova-io/openova/issues/3971) | PVCs land on a real **CSI storageclass** (not k3s local-path). | — | ☐ | — |
| 210 | storage | [#3971](https://github.com/openova-io/openova/issues/3971) | `local-path` is **FORBIDDEN** (k3s `--disable=local-storage`). | — | ☐ | — |
| 211 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP **sovereign-admin** token → `list_applications` returns all apps. | [/api/v1/fleet/applications](https://console.omantel.biz/api/v1/fleet/applications) | ☐ | — |
| 212 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP **Org-scoped** token → `list_applications` returns ONLY that Org's apps (RBAC parity). | [/api/v1/org/applications](https://console.demo.omani.homes/api/v1/org/applications) | ☐ | — |
| 213 | mcp | [#3988](https://github.com/openova-io/openova/issues/3988) | MCP cross-Org `get_application` → **403** (UI-parity). | [/api/v1/fleet/applications](https://console.demo.omani.homes/api/v1/fleet/applications) | ☐ | — |
| 214 | orgs | [#3985](https://github.com/openova-io/openova/issues/3985) | No `SME`/`tenant` banned-term leak in the console bundle (org-rename complete). | — | ⚠️ | hw220-walk-2026-07-04(B): Body text on /organizations + org detail: 0 tenant/SME. Bundle grep: column keys/testids only, labels read "Organization" — BUT 2 user-facing strings remain: parent-domain pool description "…Organization marketplace **tenants**" and "Provider **tenant** already credentialed…". Also: the left sidebar still renders the dead **"Sandbox"** menu (founder 2026-06-30 removed the concept; Agenity is the product) — legacy-menu leak. ‖ 2026-06-27 91dc0591 authed sweep: console bundle carries no user-facing SME/tenant banned-term leak (org-rename complete) |
| 215 | orgs | [#3985](https://github.com/openova-io/openova/issues/3985) | CRD `tier` enum is `[org, corporate]` — a `tier: org` Organization is accepted. | — | ☐ | — |

---

### Governance train — items 187-215 (added 2026-06-21)

These extend the canonical 186-case walk with this session's governance work. **Status is deliberately conservative** (no premature ✅): `⏳` = built/merged in code but **NOT yet validated on the durable live console** (the agents' temporary image rolls were Flux-reverted, so they are not yet what the operator sees — each needs a real end-user walk after the durable roll); `◑` = mechanism proven (e.g. against live HCS) but full in-cluster proof lands on a fresh prov; `❌` = not built yet (fix in flight) or genuinely requires a clean re-prov. A row flips `✅` only after an operator end-user walk on the durable console with pasted evidence, per the TRUST-ledger discipline.
| 216 | e2e-journey | [#3376](https://github.com/openova-io/openova/issues/3376) | A user with a **coupon/voucher code** redeems it → creates **his Organization**. Surface: `https://marketplace.<fqdn>/redeem/?code=<CODE>` (no session) → plan/region → Launch. PASS = the provisioning timeline runs to Done AND `/organizations` shows the Org **ACTIVE** backed by live `vcluster` pods (not a fake-green status). | [/api/v1/organizations](https://console.omantel.biz/api/v1/organizations) | ⚠️ | hw220-walk-2026-07-04(B): Org nstar state=done/Active, console.nstar.omani.homes → 200 valid cert, REAL partial backing (agenity pod 1/1 + nstar-verify postgres healthy). BUT the Org's funnel apps are NOT converged (see B-2) and this Org is kind=internal (no voucher-redeem journey exercised on hw220). ‖ 2026-06-27 91dc0591 (Refs #909): Org uatwalk91 Active with REAL backing (WordPress+MySQL 1/1 + agenity, HTTP 200) — App-converged ✅. The host-namespace (not vcluster) backing is design-correct for the Org's `plan=s` per the #4292 TIER GATE (free/S share the host namespace; M+ get a dedicated vcluster) — the `vcluster`-pods PASS premise is stale for an S-plan Org. Note: arrived via funnel magic-link, not the voucher-redeem surface (no seeded voucher this prov) |
| 217 | e2e-journey | [#3374](https://github.com/openova-io/openova/issues/3374) | The user logs in via the **passwordless SSO magic-email PIN** (any domain, e.g. `demo@openova.io`) → 6-digit PIN emailed → lands signed-in, no password. PASS = the PIN actually arrives (verify IMAP `mail.openova.io:993`) AND entering it lands signed-in as the CUSTOMER (their email in the avatar, their Org in the header) with no password field — this is the customer PIN flow, NOT the operator handover. | [/api/v1/auth/pin/issue](https://console.demo.omani.homes/api/v1/auth/pin/issue) | ⛔ | hw220-walk-2026-07-04(B): Passwordless PIN journey headless-unwalkable (IMAP round-trip); per #688 design an agent cannot self-authenticate the PIN. ‖ §I 91dc0591: passwordless SSO magic-PIN not walkable headless (IMAP round-trip); uatwalk91 uses shared sovereign realm (PerOrgRealmProvisioned=False, planSlug=s) |
| 218 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | **chepherd is installable from the catalog as an Application** (`bp-agenity` **Helm chart**). Surface: per-Org console → Catalog → the `bp-agenity` card renders + the install wizard opens with the Org's Environment pre-selected as the target. PASS = card present + wizard opens (the chart resolves from the catalog, not 404). | [/apps](https://console.demo.omani.homes/apps) | ⚠️ | hw220-walk-2026-07-04(B): bp-agenity resolves from the catalog (81-item list carries the Agenity card w/ Opus 4.7 copy; chart installed in nstar) — not 404. Per-Org install-wizard RENDER is org-console-session-walled headless. ‖ 2026-06-27 91dc0591 (SUPERSEDES prior ⛔ CrashLoop verdict): sandbox-controller no longer CrashLoops (catalyst-gitea-token reflected into `sandbox` ns per R19/#4482) — a throwaway Sandbox CR reconciles `Ready=True`; chepherd/`bp-agenity` chart resolves from the catalog (not 404). Controller-side proven; the per-Org install-wizard render is UI-gated (headless session-guard) |
| 219 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4180](https://github.com/openova-io/openova/issues/4180) [#4553](https://github.com/openova-io/openova/issues/4553) | The user **provisions chepherd** as an Application → it converges (Helm deploys, pods Ready, chepherd console reachable in his Org). | [agenity.agnstar](https://agenity.agnstar.omani.homes/app/) | ❌ | hw220-walk-2026-07-04(B): Agenity does NOT converge/reach the user: Application `nstar/agenity` phase=**Provisioning** for 12h; HR `agenity-rtz-a` stuck "reconciliation in progress" ~5h (pod agenity-rtz-a-bp-agenity-0 1/1 Running); legacy HR `bp-agenity` False — chart pull error `oci://ghcr.io/openova-io/bp-agenity:0.9.7: manifest does not contain minimum number of descriptors (2), descriptors found: 1`; and the workspace is UNREACHABLE from any host (see B-3): HTTPRoute host `agenity.hw220.omani.works` has NO DNS record (NXDOMAIN) while the DNS host `agenity.nstar.omani.homes` (→212.72.24.14) has NO route/cert → TLS handshake reset. ‖ 2026-06-28 fresh North-Star Org `agnstar` on 91dc0591 (E2E acceptance walk, supersedes prior PARTIAL): agenity provisioned via Application-CR → `agenity-bp-agenity-0` Running 1/1 (singleton, host-ns), chart bp-agenity 0.5.16 + the durable oidc-gate companion (#4553/#4572) — pods Ready AND the chepherd workspace is reachable in the Org at `agenity.agnstar.omani.homes/app/` (renders Work/Terminal/Talk/Board, SSO-authenticated). Runtime pod-materialize + console-reachable both confirmed on a real Org (not a throwaway) |
| 220 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4111](https://github.com/openova-io/openova/issues/4111) [#4556](https://github.com/openova-io/openova/issues/4556) | chepherd's **solo agent is pre-configured with Claude Opus 4.7** + a working token (the agent is live and chat works). | [agenity.agnstar](https://agenity.agnstar.omani.homes/app/) | ⚠️ | hw220-walk-2026-07-04(B): Credential half PROVEN: `agenity-anthropic-token` (Opaque, 2 keys) + `agenity-mcp-bearer` + oidc secrets all SecretSynced=True via ExternalSecrets (vault-region1). Live chat-run not exercised (workspace unreachable per 219 + agent-run is a mutation). ‖ 2026-06-27 fresh North-Star Org `agnstar` on 91dc0591 (PARTIAL, supersedes prior ⛔): **SSO-entry (founder req-4) MET** — `agenity.agnstar.omani.homes` renders the full chepherd workspace (Work/Terminal/Talk/Board tabs + ✦Spawn + Sessions rail) via zero-click Keycloak SSO with **NO token-paste prompt** (#4556/#4560 bp-oidc-gate `spaTokenSeed` 0.1.6, rolled live `oidc-gate.v4` Ready; verified `has_paste_prompt:false, has_workspace_tabs:true`, screenshot `uat-agnstar-agenity-NOPASTE-workspace-VERIFIED.png` — BEFORE state `uat-agnstar-agenity-app-TOKEN-PASTE-PROMPT.png`). The pty-server image bundles `@anthropic-ai/claude-code` + `@qwen-code` + the `/usr/local/bin/openova-sandbox-mcp` binary; image/wiring PROVEN. HELD ⚠️: the live agentic chat RUN (workspace shows `0 live` agents — no spawn) is gated on an Anthropic credential (G8/#4277) |
| 221 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | The user **chats with the chepherd solo agent** ("create a `<blueprint>` app in my org") → the agent calls the openova-mcp `create_application` tool with the user's **Org-scoped** token → a new Application CR is created in the user's Org. PASS = the agent reports the new app name + an Application appears provisioning in the user's `/apps`, with NO operator-console action. | [agenity.demo](https://agenity.demo.omani.homes/app/) | ☐ | — |
| 222 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) | The agent-created application **converges and appears in the user's Org** (chat-driven app creation works end-to-end). | [console.demo/apps](https://console.demo.omani.homes/apps) | ☐ | — |
| 223 | e2e-journey | [#3988](https://github.com/openova-io/openova/issues/3988) [#4553](https://github.com/openova-io/openova/issues/4553) | The chepherd agent's actions are **RBAC-scoped to the user's Org** (UI-parity): the openova-mcp `list_applications` returns ONLY this Org's apps (a 2nd Org's are filtered out), a cross-Org `get_application` → **403**, and the agent cannot create/mutate in any other Org — enforced by `openova-mcp/internal/identity` Org-claim, not just the chat prompt. | [whoami](https://console.agnstar.omani.homes/api/v1/whoami) | ☐ | — |
| 224 | convergence | [#4272](https://github.com/openova-io/openova/issues/4272) | Per-Org `bp-openclaw` controller reaches Ready (1/1) on a Sovereign — the idle-reaper can list its per-user pods and `readyz` passes. | — | ❌ | hw220-walk-2026-07-04(B): Per-Org bp-openclaw never reaches Ready: HR False `dependency 'nstar/bp-keycloak' is not ready` — root cause B-2 (LimitRange ratio wedge), 12h. ‖ §I 91dc0591: no openclaw pods anywhere — uatwalk91 funnel cart was WordPress-only; openclaw never provisioned (not RBAC-blocked, simply not in cart) |
| 225 | convergence | [#4278](https://github.com/openova-io/openova/issues/4278) | Per-Org `bp-newapi` HR reaches Ready on a fresh Org — the admin-promote post-install hook Completes (its ServiceAccount exists) so the chart finishes. | — | ❌ | hw220-walk-2026-07-04(B): Per-Org bp-newapi HR False (same B-2 dependency chain). Contrast: PLATFORM newapi Ready=True (bp-newapi 1.4.137, Application Ready) — the fault is per-Org-specific. ‖ §I 91dc0591: #4278 fix proven on PLATFORM newapi (bp-newapi 1.4.133 Ready, admin-promote Job Complete 1/1); per-Org bp-newapi not in uatwalk91 cart so per-Org variant not exercised |
| 226 | funnel | [#3376](https://github.com/openova-io/openova/issues/3376) | The customer's PURCHASED app actually RUNS — every funnel Org's `tenant-<slug>-apps` Kustomization reconciles into a running pod (the app is reachable, not 404). | [wordpress.g4wpsso](https://wordpress.g4wpsso.omani.works/) | ❌ | hw220-walk-2026-07-04(B): Purchased/funnel apps do NOT run on the fresh Org: bp-wordpress-tenant + bp-stalwart-tenant + bp-newapi + bp-openclaw all HR False behind nstar/bp-keycloak (B-2); `wordpress.nstar.omani.homes` + `mail.nstar.omani.homes` → HTTP 000 (TLS reset, no route). Continuum `nstar/bp-wordpress-tenant` **Degraded**. Note: `catalyst-tenant-nstar-apps` Kustomization=True is soft-green above a fully-wedged app layer. ‖ §I 91dc0591: wordpress.uatwalk91.omani.homes → HTTP 200, live WordPress install page over valid *.omani.homes wildcard LE TLS — purchased funnel app actually RUNS, reachable not 404 |

---

### Fresh-prov 2026-06-26 — session-fix acceptance (rows 227–239)

These 13 rows are the acceptance checks for the issues genuinely resolved this session. Each pairs the **root cause** with the **fresh-prov surface to walk** + the **expected evidence**. All start `☐` — the fresh-prov walk fills the verdict + evidence cells in. (Refs #4279.)

| # | Epic | Ticket | Test case | Walk | Result | Evidence |
|---|---|---|---|---|---|---|
| 227 | delivery | [#4415](https://github.com/openova-io/openova/issues/4415) | A catalog/seed version bump reaches the live **Blueprint CR zero-touch** — bumping a `bp-*` pin in the catalog seed lands on the cluster Blueprint CR with NO manual `kubectl patch` and NO `resource-policy: keep` annotation pinning a stale version. PASS = after a seed bump, `kubectl get blueprint bp-<x> -o jsonpath='{.spec.version}'` matches the seeded version within one reconcile, the seeder owns the field (no `helm.sh/resource-policy: keep`), and the rolled chart renders on the cluster. | — | ☐ | — |
| 228 | delivery | [#4435](https://github.com/openova-io/openova/issues/4435) | A **re-prov AFTER a wipe** does NOT false-fail on orphaned `catalyst-*` VPCs — the orphan-VPC janitor sweep removes the leftover VPC(s) from a prior wiped deployment before the new prov allocates its network. PASS = wipe an env, immediately re-prov on the same region; provisioning does NOT error `VPC already exists` / quota-conflict; the janitor log shows the orphaned `catalyst-*` VPC(s) swept, and the new prov reaches Phase-1 green. | — | ⚠️ | hw220-walk-2026-07-04(B): Not exercisable read-only (needs wipe→re-prov cycle). Orphan-sweep code present per prior §F evidence; protect-side proven (#4454). ‖ §F: prov green under cap; orphan-sweep code present (42 VPC/21 keypair evals); destructive sweep not exercised (no orphans). Protect proven in #4454 |
| 229 | delivery | [#4409](https://github.com/openova-io/openova/issues/4409) [#4428](https://github.com/openova-io/openova/issues/4428) | **bootstrap-kit Kustomization Ready=True + plane-isolation admits CNPG→kube-apiserver egress** — on the fresh prov `kubectl -n flux-system get kustomization bootstrap-kit` is `Ready=True` (no orphaned `dependsOn` / CRD dry-run rejection), AND the plane-isolation NetworkPolicy permits the CNPG operator's egress to the kube-apiserver so Cluster admission/patch succeeds. PASS = bootstrap-kit `Ready=True`; CNPG operator pod Running; a `Cluster` reconciles (no `context deadline exceeded` / x509 webhook failure caused by blocked apiserver egress). | — | ❌ | hw220-walk-2026-07-04(B): bootstrap-kit Kustomization **Ready=Unknown "Reconciliation in progress"** ~15h — held down by: (a) `infrastructure-config` Kustomization **False**: `ProviderConfig/default dry-run failed: no matches for kind "ProviderConfig" in version "tf.upbound.io/v1beta1"` (see B-5); (b) HR `bp-catalyst-platform` **False**: helm upgrade/rollback fails — chart bp-catalyst-platform@1.4.993 renders catalyst-api Deployment env[82..86] with BOTH value and valueFrom → API rejects patch (see B-4) — which cascades bp-continuum + bp-sandbox False. The CNPG half of the row PASSES: plane-isolation admits apiserver egress, all 10 CNPG Clusters healthy (shared-pg/-b/-c 3/3), no x509/context-deadline. ‖ 2026-06-27 91dc0591: bp-plane-isolation HR Ready=True (chart 0.1.10) both regions; baseline-default-deny CNP egress allow-list includes kube-apiserver; all 9 CNPG Clusters at desired instance count 'Cluster in healthy state' (shared-pg/-b/-c 3/3, cnpg-pair 3/3) — no context-deadline/x509 |
| 230 | delivery | [#4427](https://github.com/openova-io/openova/issues/4427) | **`ghcr-pull` reflects into a bare-slug per-Org namespace** — the emberstack reflector copies the `ghcr-pull` imagePullSecret into a per-Org namespace whose name is a bare slug (e.g. `org-<slug>` / a non-UUID form), so private `ghcr.io/openova-io/*` app images pull (no `ImagePullBackOff` 401-anonymous). PASS = a fresh Org's namespace has the reflected `ghcr-pull` secret; a per-Org app pod pulling a private image reaches Running (no 401/ImagePullBackOff). | — | ☐ | — |
| 231 | delivery | [#4411](https://github.com/openova-io/openova/issues/4411) | **bp-postgres singleton render carries the operator-probe NetworkPolicy** — a singleton (non-HA) bp-postgres render still ships the NetworkPolicy that admits the CNPG operator's health/readiness probe, so a singleton Postgres reaches Ready (the probe is not blocked). PASS = a singleton bp-postgres Application reaches `Ready=True`; the operator-probe NetworkPolicy is present in the rendered manifest; the CNPG operator can probe the instance. | — | ☐ | — |
| 232 | apps | [#4272](https://github.com/openova-io/openova/issues/4272) | On a fresh funnel Org, **openclaw `/readyz` returns 200** — the 4-layer chain (controller image · reaper apiserver egress NetworkPolicy · TLS trust · OIDC) all resolve so the idle-reaper lists per-user pods and readiness passes. PASS = `bp-openclaw` HR `Ready=True`, controller pod 1/1 Running, and `curl https://<openclaw-host>/readyz` → 200 (no `reaper: list pods … context deadline exceeded` loop). | — | ❌ | hw220-walk-2026-07-04(B): openclaw `/readyz` not reachable — the per-Org chart never installed (B-2 chain), no controller pod exists in nstar. ‖ §I 91dc0591 (C1): per-Org provisioning proven via WordPress; openclaw not in the walked Org's cart so /readyz not exercised this prov |
| 233 | apps | [#4422](https://github.com/openova-io/openova/issues/4422) | On a fresh funnel Org, **WordPress serves HTTP 200** — the mysql DB pod is Guaranteed-QoS (requests==limits, schedulable under the per-Org LimitRange), the HTTPRoute is present, and the landed host is pool-aligned to the console-ELB EIP. PASS = `curl https://wordpress.<slug>.<pool-tld>/` → 200 (or 302→install then 200), mysql pod QoS=Guaranteed + Running, HTTPRoute parents the console gateway, host resolves to the console-ELB EIP (not the primary ELB). | — | ❌ | hw220-walk-2026-07-04(B): WordPress does NOT serve on the fresh Org: HR bp-wordpress-tenant False (dep keycloak, B-2); wire: `wordpress.nstar.omani.homes` → 000/TLS-reset. The row's own fault-class (per-Org LimitRange Guaranteed-QoS schedulability) has RECURRED on sibling charts: bp-keycloak-postgresql (ratio 1.5/1.5) + bp-cnpg operator (2.5/2.0) rejected by `plan-limits` maxLimitRequestRatio=1. ‖ §I 91dc0591 (C1, supersedes prior ⛔): wordpress.uatwalk91.omani.homes → HTTP 200 (302→install then 200), HTTPRoute parents console gateway, host on console-ELB EIP, valid pool LE cert |
| 234 | apps | [#4307](https://github.com/openova-io/openova/issues/4307) | On a fresh funnel Org, **`mail.<slug>` serves HTTP 200 (SnappyMail webmail)** — the stalwart-tenant webmail SPA loads. PASS = `curl https://mail.<slug>.<pool-tld>/` → 200 serving the SnappyMail SPA; stalwart-tenant pod Running; HTTPRoute present (no bare envoy 404). | — | ❌ | hw220-walk-2026-07-04(B): `mail.nstar.omani.homes` → 000/TLS-reset; bp-stalwart-tenant HR False (B-2 chain); SnappyMail never provisioned. ‖ §I 91dc0591 (C1): per-Org provisioning proven via WordPress; stalwart-mail/SnappyMail not in the walked Org's cart this prov |
| 235 | apps | [#4437](https://github.com/openova-io/openova/issues/4437) | On a fresh prov, **grafana SSO logs in 3/3 → 200** — the bp-sso-bridge per-tick credential refresh keeps the grafana OIDC client secret in lockstep with Keycloak, so the SSO round-trip completes. PASS = grafana `/login/generic_oauth` → Keycloak → back signed-in (200, user in the avatar) across 3 consecutive attempts; bp-sso-bridge refreshes the client cred each tick (no stale-secret 401). | — | ❌ | hw220-walk-2026-07-04(B): Grafana SSO 3/3 impossible: grafana region-a container CrashLoopBackOff ×51 — `pq: password authentication failed for user "grafana"` — and external grafana → 503 (row 180). The OIDC env-wiring itself renders (GF_AUTH_GENERIC_OAUTH_* URLs point at auth.hw220 realm sovereign) but the pod dies pre-listen on the stale DB credential. |
| 236 | apps | [#4439](https://github.com/openova-io/openova/issues/4439) | On a fresh multi-region prov, **region-B keycloak reaches Ready** — the cross-region shared-pg write host is correct so region-B Keycloak connects to the primary Postgres and completes realm import. PASS = `bp-keycloak` HR `Ready=True` in the region-B cluster; keycloak pod Running; realm import succeeds (no DB-connect / wrong-write-host failure); `keycloak.<host>` → 302 SSO. | — | ☐ | — |
| 237 | spine | [#4416](https://github.com/openova-io/openova/issues/4416) | **spine → Continuum round-trip is Healthy + `continuumRef` is populated** — the spine adopts the Continuum and the round-trip reports Healthy. PASS = the Continuum CR reports `Healthy` (not Degraded), the spine's `continuumRef` field is populated with the live Continuum, and a witness round-trip completes. | — | ☐ | — |
| 238 | postgres | [#4282](https://github.com/openova-io/openova/issues/4282) | **Per-Org postgres-in-vCluster → host-side CNPG Ready** — a per-Org Postgres requested inside the Org's vCluster is reconciled by the host-side CNPG operator and reaches Ready. PASS = the per-Org `Cluster` CR is `Ready` (host-side CNPG drives it), the Postgres pod is Running with Guaranteed QoS, and the per-Org app connects (no empty-DSN / disableWait deadlock). | — | ⚠️ | hw220-walk-2026-07-04(B): Premise not on env: no vcluster-isolation Org exists (nstar is namespace-isolation per tier gate). Host-side CNPG driving a per-Org Postgres IS proven (nstar/postgres Cluster healthy, Guaranteed QoS) — the vCluster leg stays unwalked. ‖ §I 91dc0591 (C1): per-Org provisioning proven via WordPress; per-Org postgres-in-vCluster not in the walked Org's cart this prov |
| 239 | adoption | [#4002](https://github.com/openova-io/openova/issues/4002) | **Crossplane adoption populates Observe-only on the fresh prov** — `providers.pkg.crossplane.io` and `cloudadoptions.compose.openova.io` are non-empty and reach Synced+Ready, observing (NOT re-provisioning) the OpenTofu-built infra. PASS = `kubectl get providers.pkg.crossplane.io` → ≥1 Healthy; `kubectl get cloudadoptions.compose.openova.io -A` → non-empty Synced+Ready with `external-name` = the real ELB id; the live ELB/nodes are untouched (Observe-only). | — | ❌ | hw220-walk-2026-07-04(B): Adoption does NOT populate: provider-opentofu Healthy=True (v1.1.3) BUT 0 Workspaces, 24/24 CloudAdoptions Ready=False ("waiting for composite") for 14h, `kubectl get managed` empty. Root cause B-5: infrastructure-config manifests reference `ProviderConfig tf.upbound.io/v1beta1` while the installed provider serves `opentofu.upbound.io` / `opentofu.m.upbound.io` groups → ProviderConfig never applies → composites never Ready. ‖ §D 91dc0591: crossplane-core Running + 24 Observe-only claims generated (real Huawei IDs) but **0** CloudAdoption CRs / 0 provider-opentofu Providers applied — applied post-handover; handover never fired |
| 240 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | No-nodePort gateway serves externally on a FRESH zero-touch prov: ELB → node:443 hostPort → hostNetwork envoy; gateway Service carries ZERO nodePorts (§854 literal). | — | ☐ | — |
| 241 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | Mothership writes `ready` ONLY after an external HTTP answer from https://console.<fqdn>/ (no more false-green). | — | ☐ | — |
| 242 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | 2-region BCP admission floor: a 1-region POST with no explicit bcpTopology is rejected at the LIVE production API. | — | ☐ | 2026-07-03 negative wire-test vs console.openova.io: `POST /sovereign/api/v1/deployments` (1 region, no topology) → **HTTP 400** "deployment has 1 region(s) and no explicit bcpTopology — the BCP agreement default is multi-region (>=2 regions, Pillar 2)…". Nothing created. |
| 243 | gateway-4706 | [#4706](https://github.com/openova-io/openova/issues/4706) | Tenant DNS split-horizon on a fresh prov: wildcard hosts → primary ELB EIP, console → console ELB EIP. | — | ☐ | — |
