# hw147 — the founder's click-by-click UAT walkthrough (all 5 North Stars + the 2026-06-16 train)

> **What this is.** The **single acceptance instrument** the founder walks on the next fresh prov
> (**hw147**) to validate **100% DoD**. It supersedes `train-2026-06-15.md` (hw146 target) and folds in
> every fix that merged after the hw146 walk. It is organised by **North Star**; the catalog /
> provisioning UX sections (#3598–#3603) lead in, then NS#1–NS#5, then the region-b health
> fix-passenger.
>
> **Source of truth for what's pending.** The hw146 walk report
> (`docs/sessions/2026-06-16/hw146-full-walk.md`, copied onto main in this same PR) records what was
> validated live on hw146 (NS#1, NS#2, 4/5 of NS#3) and what hw147 must finish: the OpenBao SSO
> landing (#3628), NS#4 region-kill, NS#5 cutoverComplete, and the region-b data-wiring cluster (#3631).
>
> **Format law (founder, 2026-06-03 — "this is only a unit test!!!").** Every row in the main body is
> **ONE human UI action**: *Go to a URL → do one click/type → see one screen/state.* There are **no**
> `kubectl` / `grep` / `curl` / `go test` rows in the main body — those are demoted to **Appendix —
> automated checks (NOT acceptance)**. Acceptance is the founder walking the clickable rows.
>
> **Citations.** Routes, button labels, tab names, and field names are quoted **verbatim from the
> deployed console source** `products/catalyst/bootstrap/ui/src/` (React/Vite/TSX — the DEPLOYED
> console; **not** `core/console/`, which is the undeployed Astro tree) with a `file:line` citation per
> surface, re-verified against `main` on 2026-06-16, so a non-engineer can trust each step.
>
> **The merged 2026-06-16 train this doc covers (all MERGED to main):**
> - **#3628** — OpenBao "Open" button lands signed-in via the `/sso/landing` top-level flow
>   (`bp-openbao` **1.2.45**). The hw146 highest-value re-check (NS#3 §SSO row for OpenBao).
> - **#3630** — cutover **mirrors helm charts** to local Harbor (step-03) + **verifies pullability**
>   before the ghcr strip (step-06); cutover chart **0.1.69** (NS#5).
> - **#3631** — region-b cross-region data-wiring: shared-pg **`-mesh-rw`** write host +
>   openbao DR **S3 cred** + **guacd PVC** (`bp-postgres` **0.2.2**) (region-b health §).
>
> **Replace placeholders at walk time.** `<fqdn>` = **`hw147.omani.works`** (so the console is
> `console.hw147.omani.works`, and the per-app hosts are `bao.hw147.omani.works`,
> `grafana.hw147.omani.works`, `registry.hw147.omani.works`, `auth.hw147.omani.works`,
> `gitea.hw147.omani.works`). `<JWT>` = the RS256 handover token (`iss=console.openova.io`,
> `sub=emrah.baysal@openova.io`, `role=sovereign-admin`). `<region-a>` / `<region-b>` = the two cluster
> region codes (e.g. `me-east-215-a` / `me-east-215-b`). `<region-a-kc>` / `<region-b-kc>` = the two
> cluster kubeconfigs (e.g. `/tmp/<fqdn>.kc` / `/tmp/<fqdn>-b.kc`). Tick **☑** a row that matches,
> **☒** one that does not.

---

## Row 0 — zero-login entry (do this once for the whole walk)

**North Star #3, the front door.** The walker reaches the console with **no login form** via the
mint-handover deep-link. Do this once; every section below assumes the session is live (no
re-authentication between sections).

| # | Go to (URL) | Do (click / type) | You should see | ☐ |
|---|---|---|---|---|
| 0.1 | `https://console.<fqdn>/auth/handover?token=<JWT>` | Paste into a **fresh** tab, press Enter | A **302 → `/dashboard`**, **signed in with no login form**; the 10-item left-rail nav renders (Dashboard · Cloud · Apps · Catalog · Sandbox · Jobs · Compliance · Users · Organizations · Settings — `SovereignSidebar.tsx` `FLAT_NAV`) and the avatar **E** shows top-right (`widgets/auth/ProfileMenu.tsx:104`, initial = `session.email[0]`) | ☐ |
| 0.2 | `https://console.<fqdn>/dashboard` | Click the avatar (top-right; `data-testid="profile-menu-avatar"`, `ProfileMenu.tsx:104`) | A menu reading **"Signed in as"** (`ProfileMenu.tsx:150`) then the live email **`emrah.baysal@openova.io`** (`data-testid="profile-menu-email"`, `:161`) + a **Sign out** button (`:184`) — confirms you are the owner-admin, zero clicks | ☐ |

**Row 0 result: ___ / 2.**

---

## Section 1 — Provisioning an Application from the catalog (#3598 / #3599 / #3600)

**What this proves.** An admin creates an Application **from a catalog entry**, and the create wizard
collects **Organization, topology mode, region(s), and vCluster placement** — every fixed-set input is
a **dropdown / checkbox, never free-text** (only the instance *name* is typed) — and the install does
**NOT** error *"namespace not found"* (the Org/Environment namespace is ensured server-side).

> **Route facts.** Catalog page route = `/catalog` (`app/router.tsx:1330`, `consoleCatalogRoute`). A
> catalog card is a `<Link>` (`pages/sovereign/AppsPage.tsx:925`, `data-testid="sov-app-card-<id>"` at
> `:928`) whose target is **computed** at `:919-923` — `const seg = isCatalog ? 'catalog' : 'app'` →
> `/${seg}/${app.id}` (e.g. `/catalog/bp-postgres`). The card buttons are the admin **Edit** chip
> (`:1019`, §3) and, when the Blueprint declares a user-UI endpoint, the **Open** chip (`:1060`, §6) —
> there is **no** "Install/Create" button on the card. The create flow starts from the class page's
> **+ New instance** button (`pages/sovereign/AppDetail/InstancesSection.tsx:100`).

| # | Go to (URL) | Do (click / type) | You should see | ☐ |
|---|---|---|---|---|
| 1.1 | `https://console.<fqdn>/catalog` | Read the left-nav + the page title | A grid of catalog cards; the page title reads **"Catalog"** (`CatalogPage.tsx:194`, `pageTitle="Catalog"`) | ☐ |
| 1.2 | `https://console.<fqdn>/catalog` | Click the **PostgreSQL** card (title **"PostgreSQL"**, badge **⛓ shareable**) — the whole card is the link, no Install button | The card `<Link>` (`AppsPage.tsx:925`, target computed `:919-923`) navigates you to **`/catalog/bp-postgres`** | ☐ |
| 1.3 | `https://console.<fqdn>/catalog/bp-postgres` | Click the **`+ New instance`** button (top-right of the Instances table) | A dialog titled **"New instance of postgres"** (`bp-` stripped, `InstancesSection.tsx:500`; title `:650`; `data-testid="btn-new-instance"` `:100`, label `+ New instance` `:115`) | ☐ |
| 1.4 | (in the dialog) | Read **field 1** **"Instance name"**, type `demo-pg-1` | A free-text input (`data-testid="input-instance-name"`, `:658`; placeholder `my-instance` `:664`) — the comment at `:653` states it is **"the ONLY free-text field (#3600)"** | ☐ |
| 1.5 | (in the dialog) | Open **field 2** **"Organization"** (label `:671`) | A **`<select>` dropdown** (`data-testid="select-instance-org"`, `:673`) of live Orgs `{displayName} ({slug})`, placeholder **"Select an organization…"** (`:680`) — not free-text | ☐ |
| 1.6 | (in the dialog) | Pick the **`platform`** organization | A read-only line **"Environment: platform-prod"** (`data-testid="instance-environment-derived"` `:693`, text `:701`) — the namespace the install targets | ☐ |
| 1.7 | (in the dialog) | Open **field 3** **"Topology mode"** (label `:709`) | A **`<select>` dropdown** (`data-testid="select-instance-topology"`, `:711`) of `{mode} — {description}` from `ALL_MODES = ['single-region','active-active','active-hotstandby']` (`TopologyEditor.tsx:63`) | ☐ |
| 1.8 | (in the dialog) | Select **`active-hotstandby — primary serves; standby ready for switchover`** | The mode is chosen (description from `describeMode`, `TopologyEditor.tsx:348`); the Region(s) legend below flips to require multiple regions | ☐ |
| 1.9 | (in the dialog) | Read **field 4** the **"Region(s)"** fieldset | A **checkbox group** (`data-testid="instance-regions"`, `:727`), not a dropdown; the legend (`:735-737`) reads **"Region(s) — pick at least 2"**; options are `<region-a>` / `<region-b>` (`region-checkbox-<region>`, `:760`) | ☐ |
| 1.10 | (in the dialog) | Tick **both** region checkboxes `<region-a>` and `<region-b>` | Both check; **no** validation error. (One only → **"active-hotstandby needs at least 2 regions."**, `:769-772`) | ☐ |
| 1.11 | (in the dialog) | Open **field 5** **"vCluster / zone"** (label `:780`) | A **`<select>` dropdown** (`data-testid="select-instance-vcluster"`, `:782`): **Default (server picks)** (`:787`) + `host` / `mgmt` / `dmz` / `rtz` (`VCLUSTER_OPTIONS`, `:39`) — not free-text | ☐ |
| 1.12 | (in the dialog) | Select **`rtz`** | `rtz` is the placement zone (`:781-800`; validated server-side against `{host,mgmt,dmz,rtz}`) | ☐ |
| 1.13 | (in the dialog) | Click **"Create instance"** | The button (`data-testid="btn-submit-instance"`, `:862`; label `{submitting ? 'Creating…' : 'Create instance'}` `:875`) shows **"Creating…"**, then the dialog **closes** (`onCreated` → `setDialogOpen(false)` `:294`). **No** *"namespace not found"* error | ☐ |
| 1.14 | `https://console.<fqdn>/catalog/bp-postgres` | Read the **Instances** table | A **new row `demo-pg-1`** (refetched via `qc.invalidateQueries` `:297`); name links to **`/app/demo-pg-1`** | ☐ |
| 1.15 | `https://console.<fqdn>/app/demo-pg-1` | Read the **Placement** row on Overview | **"Placement"** (`<dt>` `AppDetail.tsx:1290`) reads `active-hotstandby` (`<dd data-testid="app-detail-overview-placement">` `:1291`) — the topology persisted onto the Application CR | ☐ |

**Section 1 result: ___ / 15.**

---

## Section 2 — Apps / Catalog navigation split (#3601)

**What this proves.** `/apps` is a **tab-less Apps page** (the old "Deployments | Catalog" tab strip is
gone) and **Catalog** is its own **left-nav entry** at `/catalog`.

| # | Go to (URL) | Do (click / type) | You should see | ☐ |
|---|---|---|---|---|
| 2.1 | `https://console.<fqdn>/dashboard` | Look at the left-nav rail for **Apps** and **Catalog** | Two **separate** entries: **"Apps"** (`SovereignSidebar.tsx:68` label / `:69` `to:'/apps'`) and **"Catalog"** (`:80` label / `:81` `to:'/catalog'`) | ☐ |
| 2.2 | `https://console.<fqdn>/dashboard` | Click **"Apps"** | Navigates to **`/apps`**; the page title reads **"Applications"** (`AppsPage.tsx:505`) | ☐ |
| 2.3 | `https://console.<fqdn>/apps` | Look across the top of the page for any tab strip | **No** "Deployments / Catalog" tabs — only the installed-deployments grid (`AppsPage.tsx:7` header *"the page is TAB-LESS"*; comment `:610-611`) | ☐ |
| 2.4 | `https://console.<fqdn>/apps` | If the grid is empty, find the empty-state link | The only catalog link is the CTA **"Open catalog →"** → `/catalog` (`AppsPage.tsx:650-651`, `data-testid="sov-empty-open-catalog"`) — a link, not a tab | ☐ |
| 2.5 | `https://console.<fqdn>/apps` | Click **"Catalog"** in the nav | Navigates to **`/catalog`** (`router.tsx:1330`); page title **"Catalog"** (`CatalogPage.tsx:194`) | ☐ |
| 2.6 | `https://console.<fqdn>/catalog` | Confirm the URL is a standalone page (not `/apps?tab=...`) | The address bar reads exactly **`/catalog`**; the grid renders as its own page (`consoleCatalogRoute`, `router.tsx:1328-1332`) | ☐ |

**Section 2 result: ___ / 6.**

---

## Section 3 — Editable catalog (#3602 / #3603 · PR #3605)

**What this proves.** An **admin** opens a catalog entry's **edit dialog**, changes its **name**,
**supported topologies**, **light icon**, and **dark icon**; **Save** persists to the data-backed
overlay; the edit **survives a full page reload**.

> **Admin gate.** The per-card **Edit** button renders only when `isAdmin` is true (`CatalogPage.tsx:143`
> `useCatalogAdmin()`, passes `onEdit` only `isAdmin ? … : undefined` `:259`; card chip gated by
> `{onEdit ? (` `AppsPage.tsx:1016`). The handover identity `emrah.baysal@openova.io`
> (`role=sovereign-admin`) satisfies it (`useCatalogAdmin.ts:41-47`). The dialog file is
> `pages/sovereign/CatalogEditDialog.tsx`.

| # | Go to (URL) | Do (click / type) | You should see | ☐ |
|---|---|---|---|---|
| 3.1 | `https://console.<fqdn>/catalog` | Hover the **Grafana** card, find the corner control | A **pencil + "Edit"** chip (`AppsPage.tsx:1019-1022`, `.edit-chip`, `data-testid="sov-app-edit-<id>"`, `title="Edit Grafana catalog entry"`) | ☐ |
| 3.2 | `https://console.<fqdn>/catalog` | Click the **Edit** chip on the Grafana card | A dialog **"Edit catalog entry"** (`CatalogEditDialog.tsx:115`; aria-label *"Edit catalog entry Grafana"* `:111`) | ☐ |
| 3.3 | (in the dialog) | Read **field 1** **"Display name"** (`:122`), change to `Grafana Observability` | A text input (`data-testid="catalog-edit-name"`, `:128`, placeholder `WordPress`) | ☐ |
| 3.4 | (in the dialog) | Read **field 2** **"Summary"** (`:133`) | A `<textarea>` (`data-testid="catalog-edit-summary"`, `:139`, placeholder *"One-line description shown on the card"*) | ☐ |
| 3.5 | (in the dialog) | Read **field 3** **"Supported topologies"** | A row of **checkboxes** per mode — single-region / active-active / active-hotstandby (over `ALL_MODES`, `:148-158`; `data-testid="catalog-edit-topo-<mode>"` `:154`) | ☐ |
| 3.6 | (in the dialog) | Tick the **active-active** checkbox | The `active-active` box checks (`catalog-edit-topo-active-active`) — the set is editable, not static | ☐ |
| 3.7 | (in the dialog) | Read **field 4** **"Light-theme icon"** (`:163`), paste an image URL | A URL input (`data-testid="catalog-edit-icon-light"`, `:170`) + an **"Upload"** picker (`catalog-edit-icon-light-upload-label`) | ☐ |
| 3.8 | (in the dialog) | Read **field 5** **"Dark-theme icon"** (`:186`), paste an image URL | A URL input (`data-testid="catalog-edit-icon-dark"`, `:194`) + an **"Upload"** picker (`catalog-edit-icon-dark-upload-label`) | ☐ |
| 3.9 | (in the dialog) | Click **"Save"** | The button (`data-testid="catalog-edit-save"`, `:225`; label `{saving ? 'Saving…' : 'Save'}` `:227`) shows **"Saving…"**, then the dialog **closes** (`saveCatalogEdit` → `PUT /api/v1/sme/commerce/apps/{id}`, `commerce.api.ts:224,150`) | ☐ |
| 3.10 | `https://console.<fqdn>/catalog` | Read the Grafana card again (no reload) | The card now shows **"Grafana Observability"** + the new icon (overlay applied live, `CatalogPage.tsx:233-239,257-258`) | ☐ |
| 3.11 | `https://console.<fqdn>/catalog` | **Hard-reload** (Ctrl/Cmd-Shift-R), read the Grafana card | The edited name + icon **persist** (re-fetched + merged server-side; `catalog_overlay.go`) — proving it is data-backed, not in-memory | ☐ |

**Section 3 result: ___ / 11.**

> **Honest note.** There is **no** success toast — acceptance is the dialog closing **and** the reloaded
> card showing the new values (3.10–3.11).

---

## Section 4 — North Star #1: every app IN a vCluster

**North Star #1 (founder, verbatim): *"every app IN a vCluster."*** This section shows the placement
surface two ways — the dashboard treemap grouped by vCluster, the `/cloud` topology graph's vCluster
containment, and the per-app **Placement** field. The hw146 walk **PASSED NS#1 live** (3 vClusters
`dmz`/`mgmt`/`rtz` running 1/1, app pods carry the `-x-` vcluster-sync marker).

> **Conformant-state caveat (#3373 Batch-A, ratified).** The route/secret apps
> (`keycloak`/`gitea`/`grafana`/`harbor`/`openbao`) stay on **`host`** by design — vcluster-izing them
> needs the loft.sh CRD-sync license, which breaks the Pillar-5 cutover deny-egress hold. That is the
> **conformant** state, not a defect.

| # | Go to (URL) | Do (click / type) | You should see | ☐ |
|---|---|---|---|---|
| 4.1 | `https://console.<fqdn>/dashboard` | Find the **"Layer 1"** dropdown above the treemap | A `<select>` **"Layer 1"** (`TreemapLayerController.tsx:184`, `treemap-layer-0-select`); defaults are `cluster` then `application` (`Dashboard.tsx:172-174`) | ☐ |
| 4.2 | `https://console.<fqdn>/dashboard` | Set **Layer 1** → **`vCluster`** | The treemap regroups into top-level **vCluster blocks** — `host`, `mgmt`, `dmz`, `rtz` (option value `vcluster`, label **"vCluster"**, `TreemapLayerController.tsx:59`) | ☐ |
| 4.3 | `https://console.<fqdn>/dashboard` | Read the **`mgmt`** block tiles | The mgmt vCluster contains its apps (`loki`/`mimir`/`tempo`/`nats`) — an app sitting **inside** the mgmt vCluster | ☐ |
| 4.4 | `https://console.<fqdn>/dashboard` | Read the **`rtz`** + **`dmz`** block tiles | `rtz` contains `valkey`/`seaweedfs`/`vllm`; `dmz` contains `coraza` — apps contained in their vClusters | ☐ |
| 4.5 | `https://console.<fqdn>/cloud?view=graph` | Read the topology graph; find a vCluster container node | The Cloud graph (`CloudPage.tsx:79-80` default `view=graph`; toggle `data-testid="cloud-page-view-graph"` `:616`) draws **vCluster containers** (`dmz`/`mgmt`/`rtz`) with their app nodes **nested inside** — the visual confirmation an app sits in a vCluster | ☐ |
| 4.6 | `https://console.<fqdn>/app/bp-coraza` | Read the **"Placement"** row on Overview | **"Placement"** (`<dt>` `AppDetail.tsx:1290`, `<dd data-testid="app-detail-overview-placement">` `:1291`) renders the app's declared placement — per-app confirmation placement is first-class | ☐ |

**Section 4 result: ___ / 6.**

---

## Section 5 — North Star #2: 3 shared-PG instances → 3 cards, many-to-many contexts

**North Star #2 (founder, verbatim): *"3 shared PG instances → 3 cards, 6-7 apps many-to-many."*** The
hw146 walk **PASSED NS#2 live** — `shared-pg`, `shared-pg-b`, `shared-pg-c` all 3/3 READY. The Contexts
tab appears on each **⛓ shareable** instance's app-detail page and lists the consuming apps.

> **Tab-label note.** The tab reads **"Contexts"** with a **separate numeric count badge**
> (`AppDetail.tsx:669-677` renders `<TabButton id="contexts" label="Contexts" count={appContexts.length} …/>`,
> shown only when the instance is `appShareable`) — not the literal string "Contexts N". Exact context
> rows are env-specific; tick when the *shape* renders (3 cards · per-instance Contexts tab · distinct
> consumer sets).

| # | Go to (URL) | Do (click / type) | You should see | ☐ |
|---|---|---|---|---|
| 5.1 | `https://console.<fqdn>/catalog/bp-postgres` | Read the hero badges + the **Instances** table | Hero shows **⛓ shareable**; the Instances table lists **3 rows** — `shared-pg`, `shared-pg-b`, `shared-pg-c` (org `platform`), each Ready with an **Open →** link — **3 instances = 3 cards** | ☐ |
| 5.2 | `https://console.<fqdn>/app/shared-pg` | Click the **"Contexts"** tab (with its count badge, `AppDetail.tsx:669-677`) | A table (`data-testid="sov-contexts-table"`, `AppDetail/ContextsTab.tsx:39`) headed **`Context · Occupied by · Credential · Status`** (`AppDetail/ContextsTab.tsx:50-53`) binding **harbor / gitea / keycloak** (3 distinct consumers) | ☐ |
| 5.3 | `https://console.<fqdn>/app/shared-pg-b` | Click the **"Contexts"** tab | A **distinct** consumer set — **grafana / powerdns / powerdns-admin** | ☐ |
| 5.4 | `https://console.<fqdn>/app/shared-pg-c` | Click the **"Contexts"** tab | A third consumer set — **catalyst-platform (sme_*) / newapi / openova-flow** | ☐ |
| 5.5 | (review) | Count the distinct consumer apps across the 3 instances | **6–7+ distinct apps** bind the 3 instances **many-to-many** — one engine, many consumers, isolated per-context credentials | ☐ |

**Section 5 result: ___ / 5.**

---

## Section 6 — North Star #3: zero-login SSO (URL → signed in as admin), 6/6 apps

**North Star #3 (founder, verbatim): *"NO login UI anywhere — URL → signed in as emrah.baysal as
ADMIN; proof = surfing admin panels."*** In a **fresh tab**, a bare app URL or the catalog **Open**
button lands **signed in** as the owner-admin — zero clicks, no PIN/login form. A redirect ending on a
login screen or a 4xx/5xx is a **fail**. hw146 passed **4/5 (Gitea/Grafana/Harbor/Keycloak)**; OpenBao
**failed** (popup-only Vault OIDC callback) and is **fixed by #3628** — the highest-value re-check here.

> **The Open button is the same silent-SSO path everywhere.** Both the catalog card **Open** chip
> (`AppsPage.tsx:1060`, `data-testid="sov-app-open-<id>"` `:1063`, `open-chip` `:1062`) and the
> AppDetail hero **`↗ Open <App>`** button (`LaunchButton variant="hero"`, `AppDetail.tsx:1156`;
> `data-testid="btn-launch-app"` `:1199`; label `Open ${appLabel}` `:1194`; `↗` glyph `:1210-1213`)
> call `launchAppViaSSO(launchKey, externalURL)` → `getLaunchURL` → **`GET /catalyst/v1/apps/{key}/launch-url`**
> (`AppsPage.tsx:793-803`, `AppDetail.tsx:1143-1154`). The backend builds that URL from the Blueprint's
> **`ssoInitPath`**. **OpenBao's `ssoInitPath` is `/sso/landing`** (catalog seed
> `products/catalyst/chart/templates/catalog-seed/blueprints.yaml:3297`, mirroring
> `platform/openbao/blueprint.yaml:116`) — the **#3628 fix**: the on-origin popup-free top-level OIDC
> page (chart `templates/sso-landing.yaml`), **not** the old `/ui/vault/auth?with=oidc` deep-link that
> stranded a top-level tab on `window.opener=null → postMessage` null-deref.

| # | Go to (URL) | Do (click / type) | You should see | ☐ |
|---|---|---|---|---|
| 6.1 | `https://console.<fqdn>/app/bp-grafana` | Read the header, click the **Endpoints** tab | The hero **`↗ Open Grafana`** button (`AppDetail.tsx:1156,1199`) + hint *"Single sign-in — no second login."* (`:614-616`); the **Endpoints** tab (`:684`) lists `grafana.<fqdn> · https · Ready · Open →` | ☐ |
| 6.2 | `https://console.<fqdn>/catalog` | On the **Grafana** card, click the **Open** chip (`sov-app-open-bp-grafana`, `AppsPage.tsx:1063`) | A **new tab** lands on **Grafana → Home / "Welcome to Grafana"**, **no login form**, avatar top-right (lands as ADMIN) | ☐ |
| 6.3 | `https://console.<fqdn>/catalog` | On the **Harbor** card, click the **Open** chip | A new tab auto-redirects to **`registry.<fqdn>/harbor/projects`**, **no login form**, **`emrah.baysal@openova.io`** top-right, **Administration** menu visible | ☐ |
| 6.4 | `https://console.<fqdn>/catalog` | On the **Gitea** card, click the **Open** chip | A new tab shows **"emrah.baysal — Dashboard — Catalyst Gitea"**, logged in, no login form | ☐ |
| 6.5 | `https://console.<fqdn>/catalog` | On the **Keycloak** card, click the **Open** chip | A new tab lands on **`auth.<fqdn>/admin/sovereign/console/`** — the Keycloak **admin console** with the realm nav, no login form | ☐ |
| 6.6 | `https://console.<fqdn>/catalog` | On the **OpenBao** card, click the **Open** chip **(#3628 re-check — the one that failed on hw146)** | A new tab routes through **`bao.<fqdn>/sso/landing`** → silent OIDC → lands on the **Vault UI signed-in (Secrets Engines list, `/ui/vault/secrets`)**. You must **NOT** see the *"Sign in to OpenBao"* token form **nor** a `Cannot read properties of null (reading 'postMessage')` console error | ☐ |
| 6.7 | `https://bao.<fqdn>` | (negative cross-check) Type the **bare** host in a fresh tab, press Enter | The bare path **302s to `/sso/landing`** then lands on **`/ui/vault/secrets`** signed-in — the SAME flow as the Open button, proving both front doors land authenticated | ☐ |
| 6.8 | `https://console.<fqdn>/catalog` → newapi **Open** chip, then `https://newapi.<fqdn>/` a **2nd** time | Click newapi's **Open** chip (lands signed-in, no login form); then type the **bare** `newapi.<fqdn>` host again in a fresh tab and press Enter | The 2nd bare-URL visit is **zero-click** — it lands **straight back signed-in** (newapi UI, no login form) with NO re-login / OIDC-bind prompt. This is the **#3563** regression check: a 2nd bare-URL visit must NOT hit the bind | ☐ |

**Section 6 result: ___ / 8.** Six apps (Grafana, Harbor, Gitea, Keycloak, OpenBao, newapi) each land
signed-in as the owner-admin, zero clicks — including the **OpenBao /sso/landing** fix and the **newapi #3563 zero-click re-login**.

---

## Section 7 — North Star #4: region-kill failover (consumer data preserved)

**North Star #4 (founder, verbatim):** agreed apps are actually multi-region — a region kill is
survived by the standby with **zero consumer data loss**. The console only *displays* region health, so
the kill + promote + re-check are **operator kubectl actions** (rows 7.4–7.6, demoted to operator-run
because they are not browser clicks — see Appendix A7 for the exact commands). The rows below keep the
UI-confirmable parts in the main body and name the operator actions plainly so the founder sees the
whole NS#4 in one place. On hw147 this gets its **clean first validation** (hw146 was mid-cutover, so a
kill there would have been inconclusive). The **#3631 `-mesh-rw`** fix is what lets the region-b
consumer apps keep their DB across the kill.

| # | Go to / run | Do (click / type / action) | You should see | ☐ |
|---|---|---|---|---|
| 7.1 | `https://console.<fqdn>/cloud?view=graph` | Read the **Clusters** / **vClusters** tiles + the topology graph | The Cloud page (`/cloud`, `router.tsx`) shows the **"Clusters"** and **"vClusters"** tiles (`cloud-compute/CloudComputePage.tsx:7,12`, tagline *"k3s / k8s control planes — one per region"*) with **2 clusters** (one per region) + the FlowCanvas graph drawing both regions. *(No literal "Region 2/2" pill — read the per-region cluster count.)* | ☐ |
| 7.2 | `https://console.<fqdn>/app/shared-pg` | Click the **Topology** tab; read the **Change placement** editor | The **Topology** tab (`<TabButton id="topology" label="Topology">` `AppDetail.tsx:678`) opens **"Change placement"** (`TopologyTab.tsx:307-309`; `<TopologyEditor>` `:317`): mode radios single-region / active-active / active-hotstandby; **Regions** checkboxes `<region-a>` + `<region-b>`; **Preview** + **Apply** — the picker is editable, not cosmetic | ☐ |
| 7.3 | `https://console.<fqdn>/app/keycloak` | Read the Overview; note the realm/DB the app consumes | The Keycloak app Overview confirms it consumes the shared-PG (the consumer whose data we will preserve across the kill); note its realm set **master + sovereign** as the baseline to re-check | ☐ |
| 7.4 | **operator — region-b kc** (`<region-b-kc>`) | **Baseline:** confirm the region-b replica is streaming and read the keycloak realm/user counts (Appendix A7) | The region-b replica is **streaming** (`pg_is_in_recovery = t`); keycloak DB present with realms **master + sovereign** and the baseline user count (record it) | ☐ |
| 7.5 | **operator — region-b kc** (the kill) | **Kill the secondary region (me-east-215-b):** cordon the region-b workers, then delete the region-b CNPG pods (Appendix A7) | Region-b is down; the global **`<instance>-mesh-rw`** Service (#3631 / `bp-postgres` 0.2.2 `replica-mesh-service.yaml:73-98`) means the region-a primary still serves writes — the cross-region consumers do not lose their write host | ☐ |
| 7.6 | **operator — promote / re-check** | **Promote + verify data:** if the killed side held a primary, promote the surviving CNPG; re-query keycloak realms + users (Appendix A7) | Promotion completes in **~3 seconds** (the hw128 PASS shape); realms **master + sovereign** and the user count are **identical to the 7.4 baseline → RPO = 0** — the keycloak realm survives the kill (#3572) | ☐ |
| 7.7 | `https://console.<fqdn>/catalog` | After restore (uncordon region-b), click the **Keycloak** Open chip | Keycloak still lands signed-in on the admin console — the consumer is unbroken by the region event; data preserved end-to-end | ☐ |

**Section 7 result: ___ / 7.** A region kill is survived; the keycloak realm + user rows are
byte-identical after promote (RPO = 0), and the `-mesh-rw` write host kept the cross-region consumers
served.

---

## Section 8 — North Star #5: sovereignty cutover (→ cutoverComplete + 600s deny-egress hold)

**North Star #5 (founder):** the Sovereign goes fully independent of the mothership. The operator drives
it from the **Sovereign console**; acceptance is the green **`Sovereign`** badge + **`cutoverComplete=true`**
**and** the witnessed **600-second deny-egress hold** (ADR-0002 — no cutover claim without the
egress-block proof). On hw147 this gets its **clean run** carrying the **#3630** fix (step-03 mirrors
the helm **charts** into local Harbor; step-06 gates the ghcr strip on a real authenticated **`helm
pull`** instead of the never-true OCI HelmRepository Ready condition) — the two defects hw146 surfaced.

> **Precondition — handover first.** The cutover only fires **after handover** (the mother→child
> `tofu-archive` is sealed; #3521). On a fresh prov, finalise handover first
> (`POST https://console.openova.io/sovereign/api/v1/handover/finalise/<deploymentId>` with the owner
> JWT → `tofuArchiveSubmitted: true`) — see Appendix A8 — then the CTA below drives the engine.
>
> **The cutover UI** is the **SovereigntyCard** on **Settings → Sovereignty**
> (`SettingsPage.tsx:110` section label, `:424` `id="sovereignty"` + `data-testid="settings-sovereignty"`,
> `:425` `<SovereigntyCard/>`). The card is
> `products/catalyst/bootstrap/ui/src/widgets/sovereignty/SovereigntyCard.tsx`. The **Start cutover**
> button fires **`POST /api/v1/sovereign/cutover/start`** (the operatorRetry path,
> `widgets/sovereignty/useCutoverEvents.ts:64,79,83`, cookie-auth on the Sovereign console).
>
> **🛑 8-vs-11 nuance (read before row 8.5).** The card copy + progress card are hardcoded to **8 steps**
> (`SovereigntyCard.tsx:196` *"Runs the 8-step cutover"*; `:282` *"an 8-step cutover"*; the modal `<ol>`
> lists 8 at `:289-298`). But the **engine runs 11**: the cutover Blueprint ships 11 ordered step Jobs
> (`platform/self-sovereign-cutover/chart/templates/01-…11-…`) and the status ConfigMap seeds
> `totalSteps: "11"` (`09-cutover-status-configmap.yaml`). The 3 post-egress steps (gitea-token-mint,
> vcluster-registry-pivot, crossplane-provider-pivot) run after the visible 8. **Acceptance is
> `cutoverComplete=true` + the witnessed egress-block, never the visible row count.**

| # | Go to (URL) | Do (click / type) | You should see | ☐ |
|---|---|---|---|---|
| 8.1 | `https://console.<fqdn>/settings#sovereignty` | Scroll to the **Sovereignty** section | A card headed **"Cluster sovereignty"** (`SovereigntyCard.tsx:118-119`) with an amber **"Tethered"** badge (`data-testid="sovereignty-badge"` `:129`; **"Tethered"** `:144`; `data-cutover-state="tethered"` `:97`) | ☐ |
| 8.2 | (Sovereignty card) | Read the primary CTA + helptext | A button **"Achieve True Sovereignty"** (`data-testid="cutover-start-button"` `:186`; label `:193`) + helptext *"Runs the 8-step cutover. ~10 minutes. Includes a 10-minute egress-block self-test against github.com + ghcr.io + harbor.openova.io."* (`:196-198`) — copy says **8**, engine runs **11** | ☐ |
| 8.3 | (Sovereignty card) | Click **"Achieve True Sovereignty"** | A confirm modal **"Achieve True Sovereignty"** (`data-testid="cutover-confirm-modal"` `:273`; title `:279`) listing **8 visible** steps (mirror Gitea, 7 Harbor proxy projects, pre-warm, containerd pivot, Flux GitRepository, 38 HelmRepositories, catalyst-api env, egress-block self-test) (`:289-298`) | ☐ |
| 8.4 | (confirm modal) | Click **"Start cutover"** | The button (`data-testid="cutover-confirm-button"` `:329`; label `:339`) fires **`POST /api/v1/sovereign/cutover/start`** (`useCutoverEvents.ts:83`); the modal closes and the progress card (`CutoverProgressCard`, `:216-218`) mounts | ☐ |
| 8.5 | `https://console.<fqdn>/settings#sovereignty` | Watch the progress card stream the steps | The visible rows flip `running → done`; **server-side the engine runs all 11** (gitea-mirror, harbor-projects, **harbor-prewarm now mirrors images AND charts (#3630)**, registry-pivot, flux-gitrepository-patch, **helmrepository-patches now gates on a real `helm pull` (#3630)**, catalyst-api-env-patch, **egress-block-test = order 8**, then gitea-token-mint, vcluster-registry-pivot, crossplane-provider-pivot = 9–11) | ☐ |
| 8.6 | `https://console.<fqdn>/settings#sovereignty` | During the **~600s** hold, keep the page open and watch | The progress card stays on the **egress-block self-test** step for the full ~10-minute hold without erroring — the deny-egress is held while Flux reconciles stay green (the negative proof is witnessed via Appendix A8) | ☐ |
| 8.7 | `https://console.<fqdn>/settings#sovereignty` | After the hold, re-read the badge + stats | The badge flips green **"Sovereign"** (`:139`, `data-cutover-state="sovereign"`); the stats panel (`data-testid="sovereignty-stats"` `:155`) shows **Mirrored commit** (`:159`), **Harbor projects** (`:165`), **Egress test = passed** (`:169-177`) — i.e. `cutoverComplete=true` | ☐ |
| 8.8 | (negative acceptance) | Open `https://console.<fqdn>/apps`, then the **Grafana** + **Gitea** Open chips on `/catalog` | Nothing the user touches is broken — console signed-in, Grafana + Gitea still land via SSO, now served from the Sovereign's **own local** Gitea + Harbor (the post-independence negative contract) | ☐ |

**Section 8 result: ___ / 8.** Handover → 11-step engine (UI draws 8) → green **Sovereign** badge +
`cutoverComplete=true` + **Egress test = passed**, with the egress-block witnessed during the 600s hold,
and the **#3630** chart-mirror + helm-pull-gate carrying the run past hw146's step-06 wedge.

---

## Section 9 — Region-b health (the #3631 cross-region data-wiring fixes)

**What this proves.** The hw146 walk surfaced a region-b data-wiring cluster — region-b
grafana/powerdns-admin/keycloak CrashLooped on a missing `shared-pg-*-rw` host, openbao-snapshot Jobs
hit `CreateContainerConfigError` for a missing S3 cred, and guacd stayed Pending. **#3631** (`bp-postgres`
0.2.2 + bp-openbao + bp-guacamole) fixes all three. These are **operator-visible** via the **Jobs** view
(failed filter) + the app cards, so they live in the main body as click-by-click rows.

> **The three fixes (cite).** (1) shared-pg **`-mesh-rw`** global write Service stub on the replica side
> (`platform/postgres/chart/templates/replica-mesh-service.yaml:73-98`, #3629/#3631) → the cross-region
> consumers' write host resolves on region-b and routes across the mesh to the primary; the SME mesh
> consumer host is `smePostgres.externalHost` (`products/catalyst/chart/values.yaml:1207`). (2) the
> openbao DR-snapshot save/fetch Jobs get their `seaweedfs-s3-secret` keys
> (`platform/openbao/chart/templates/snapshot-replication.yaml`). (3) guacd falls back to a PVC mount
> (`platform/guacamole/chart/templates/seaweedfs-pvc.yaml`). Replace `<region-b>` at walk time.

| # | Go to (URL) | Do (click / type) | You should see | ☐ |
|---|---|---|---|---|
| 9.1 | `https://console.<fqdn>/jobs` | Open the **Status** filter dropdown, select **`failed`** | The Status filter (`data-testid="jobs-filter-status"`, `JobsTable.tsx:414`, options `All` + the status values incl. `failed` `:417-422`) narrows the **Jobs** table (`data-testid="jobs-table"`) to failed Jobs only | ☐ |
| 9.2 | `https://console.<fqdn>/jobs` (failed filter on) | Scan the failed-Jobs list for **openbao-snapshot** save/fetch | **No** `openbao-snapshot-save` / `…-fetch` row in `CreateContainerConfigError` — the `seaweedfs-s3-secret` seeder (#3631) makes those Jobs **Complete**, so they are absent from the failed list | ☐ |
| 9.3 | `https://console.<fqdn>/jobs` (failed filter on) | Scan the failed-Jobs list for **guacd** | **No** guacd-related failure — guacd is **Running** via the PVC fallback (#3631); it does not appear under the failed filter | ☐ |
| 9.4 | `https://console.<fqdn>/app/grafana` | Read the app status / health on the Overview | Grafana is **healthy** (not CrashLoopBackOff) — its DB write host (`shared-pg-*-rw` via the `-mesh-rw` mesh service) resolves in region-b (#3631) | ☐ |
| 9.5 | `https://console.<fqdn>/app/powerdns-admin` | Read the app status / health on the Overview | powerdns-admin is **healthy** (not CrashLoopBackOff) — same `-mesh-rw` cross-region write host fix | ☐ |
| 9.6 | `https://console.<fqdn>/catalog` | Click the **Keycloak** Open chip (region-b consumer) | Keycloak lands signed-in on the admin console — confirming its DB resolves cross-region (the same fix that lets its realm survive the NS#4 kill) | ☐ |

**Section 9 result: ___ / 6.** Region-b grafana / powerdns-admin / keycloak are healthy on the
`-mesh-rw` write host, openbao-snapshot Jobs Complete, and guacd Runs — the #3631 cluster is clear.

---

## Summary scoreboard

| Section | Feature | Rows | Result |
|---|---|---|---|
| 0 | Zero-login entry (NS#3 front door) | 2 | ___ / 2 |
| 1 | Provisioning from catalog (#3598/#3599/#3600) | 15 | ___ / 15 |
| 2 | Apps / Catalog nav split (#3601) | 6 | ___ / 6 |
| 3 | Editable catalog (#3602/#3603 · PR #3605) | 11 | ___ / 11 |
| 4 | NS#1 — every app in a vCluster (+ /cloud graph) | 6 | ___ / 6 |
| 5 | NS#2 — 3 shared-PG cards, many-to-many | 5 | ___ / 5 |
| 6 | NS#3 — zero-login SSO 5/5 (incl. **OpenBao /sso/landing #3628**) | 7 | ___ / 7 |
| 7 | NS#4 — region-kill failover (**-mesh-rw #3631**) | 7 | ___ / 7 |
| 8 | NS#5 — sovereignty cutover (**chart-mirror + helm-pull gate #3630**) | 8 | ___ / 8 |
| 9 | Region-b health (**#3631** cross-region data-wiring) | 6 | ___ / 6 |
| **Total** | | **73** | **___ / 73** |

---

## Appendix — automated checks (NOT acceptance)

> Demoted per the founder's UAT format law. **Acceptance is the operator walking the clickable rows
> above.** These are supporting machine cross-checks + the exact operator commands for the kubectl-only
> NS#4 / NS#5 / handover steps that have no browser-click equivalent.

- **A1 (create-from-catalog).** The create dialog POSTs `createApplicationInstance(body)` →
  `POST <fqdn>/catalyst/v1/apps/instances` (`catalog.api.ts:917,926`); body carries
  `org`/`name`/`blueprint`/`topology`/`placement`/`backing` (`catalog.api.ts:568-590`). Namespace-ensure
  (#3598) is server-side (`api/internal/handler/namespace_ensure.go`); the UI shows the derived
  `{org}-prod` read-only.
- **A2 (nav split).** `consoleAppsRoute` `path:'/apps'` → `AppsPage`; `consoleCatalogRoute`
  `path:'/catalog'` → `CatalogPage` (`app/router.tsx:1313-1317,1328-1332`). Suites: `AppsPage.test.tsx`,
  `CatalogPage.test.tsx`.
- **A3 (editable catalog).** Save path `saveCatalogEdit(blueprintId, edit)` →
  `PUT /api/v1/sme/commerce/apps/{id}` or `POST …/apps` (`commerce.api.ts:205,224,230`); read-side merge
  `catalog_overlay.go`. Suites: `CatalogPage.edit.test.tsx`, `catalog_overlay_test.go`.
- **A4 (NS#1 placement).** `scripts/audit-placement-conformance.py rendered` → exit 0; treemap dimension
  `vcluster` (`TreemapLayerController.tsx:59`). hw146 live: 3 vClusters `dmz`/`mgmt`/`rtz` 1/1, app pods
  carry the `-x-` vcluster-sync marker (`kubectl get statefulset -A`).
- **A5 (NS#2 contexts).** The 3 instances are bootstrap-HR PostgreSQL `Cluster`s
  (`kubectl get cluster -n shared-data` → `shared-pg`, `shared-pg-b`, `shared-pg-c`, hw146 all 3/3
  READY) reflected as App cards; per-card context counts sum to the many-to-many total.
- **A6 (NS#3 SSO).** Launch resolver `GET /catalyst/v1/apps/{key}/launch-url`. App-side probes:
  `grafana.<fqdn>/api/user` → `isGrafanaAdmin=true`; `registry.<fqdn>/api/v2.0/users/current` →
  `username=emrah.baysal@openova.io`; **OpenBao** lands `/ui/vault/secrets` via `bao.<fqdn>/sso/landing`
  (#3628; `ssoInitPath: "/sso/landing"` at `platform/openbao/blueprint.yaml:116` +
  `catalog-seed/blueprints.yaml:3297`; `bp-openbao` 1.2.45). hw146 evidence (4/5 PASS, OpenBao the
  fixed FAIL): `docs/sessions/2026-06-16/evidence/hw146-{gitea,grafana,harbor,keycloak}-sso-landed.png`.
- **A7 (NS#4 region-kill — operator commands for rows 7.4–7.6).**
  Baseline (region-b kc): `psql -c "select pg_is_in_recovery()"` on the replica + check
  `pg_stat_wal_receiver`; query keycloak DB for realms (`master`+`sovereign`) + user count.
  Kill (region-b kc): `kubectl cordon` region-b workers, then
  `kubectl delete pod -l cnpg.io/cluster=<instance>`. Promote (surviving kc): if needed
  `kubectl patch cluster <instance>-replica --type merge -p '{"spec":{"replica":{"enabled":false}}}'`,
  then re-query realms+users → must equal the baseline (RPO=0; RTO≈3s, hw128 PASS shape). Restore:
  `kubectl uncordon` region-b + revert `replica.enabled=true`. The `-mesh-rw` write host
  (`platform/postgres/chart/templates/replica-mesh-service.yaml:73-98`, #3631) keeps cross-region
  consumers served. Raw capture → `docs/sessions/<date>/evidence/<fqdn>-ns4-region-kill-proof.txt`.
- **A8 (NS#5 cutover — handover + egress-block witness).** Handover (precondition):
  `POST https://console.openova.io/sovereign/api/v1/handover/finalise/<deploymentId>` (owner JWT) →
  `tofuArchiveSubmitted: true`. Start (operatorRetry path): the **Start cutover** button fires
  `POST /api/v1/sovereign/cutover/start` (`useCutoverEvents.ts:79,83`, cookie-auth; in-cluster
  auto-trigger is fail-closed). During step-08, `kubectl get ccnp cutover-egress-block` must return the
  policy **present** for the full 600s hold (denying github.com / ghcr.io / harbor.openova.io while all
  Flux reconciles stay green); `GET /api/v1/sovereign/cutover/status` →
  `cutoverComplete=true` + `egressTestPassed=true` after the hold. Engine `totalSteps: "11"`
  (`platform/self-sovereign-cutover/chart/templates/09-cutover-status-configmap.yaml`); visible list is
  8 (`SovereigntyCard.tsx:289-298`). **#3630**: step-03 (`03-harbor-prewarm-job.yaml`) now skopeo-mirrors
  chart OCI artifacts (Phase A2) in addition to images; step-06 (`06-helmrepository-patches-job.yaml`)
  gates the ghcr strip on a real authenticated `helm pull` of a representative sample (Refs #3627), not
  the never-true `type: oci` HelmRepository Ready condition. Cutover chart 0.1.69.
- **A9 (region-b #3631).** shared-pg `-mesh-rw` stub render:
  `platform/postgres/chart/templates/replica-mesh-service.yaml` (Service `<instance>-mesh-rw`,
  `service.cilium.io/global: "true"`, `cnpg.io/instanceRole: primary` selector — zero local backends on
  the replica side). openbao DR-snapshot S3 cred: `platform/openbao/chart/templates/snapshot-replication.yaml`
  (`seaweedfs-s3-secret`). guacd PVC fallback: `platform/guacamole/chart/templates/seaweedfs-pvc.yaml`.
  `bp-postgres` 0.2.2. hw146 reproduced all three failures live; #3631 is the merged fix riding hw147.

---

> **Provenance.** Every `file:line` above was re-verified against the `main`-line deployed console
> source (`products/catalyst/bootstrap/ui/src/`) and the chart trees on 2026-06-16. The three train
> fixes (#3628 / #3630 / #3631) are all MERGED to main; their tracking issues (#3626 / #3629) close only
> after this hw147 walk lands as evidence on the issue. This doc supersedes
> `docs/ledger/uat-walkthrough/train-2026-06-15.md` (hw146 target). The hw146 walk it builds on is at
> `docs/sessions/2026-06-16/hw146-full-walk.md`.
