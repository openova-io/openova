# #3374 SSO — user acceptance walk (web UI)

> **Env: `hw144.omani.works` · deployment `d8e798bdf1b4256b` · 2026-06-15 · single physical kom4dc region (2 VPCs).**
> Fresh hw144 walk. No hw139 / hw128 evidence is carried over — every ✅ below traces to an
> `hw144-*` screenshot under `docs/sessions/2026-06-15/evidence/`.

**North Star #3 (founder, verbatim):** *"NO login UI anywhere — URL → signed in as emrah.baysal
as ADMIN; proof = surfing admin panels."* **Question Q6 (#3570):** *are the per-app "Open"
buttons restored?* — **YES.** They are present on every app-detail header (`↗ Open <App>`) and on
the **Endpoints** tab (`Open →`); they were never permanently lost.

**Contract:** in a **fresh window**, type the app's bare URL → land **signed in** as the owner with
**admin** rights. Zero clicks, no login / PIN / token form. A redirect that ends on a login screen,
a setup wizard, or a 404 / 500 / 503 is a **fail**.

**Sign-in (once, for the whole walk):** open the handover URL
`https://console.hw144.omani.works/auth/handover?token=<RS256 JWT>` (`iss=console.openova.io`,
`sub=emrah.baysal@openova.io`, `role=sovereign-admin`). It 302-redirects to `/dashboard` and you
are signed in — **no login form**. Every app below is then opened at its **bare public URL** in the
same browser session.

## Walk — every row is one UI action

| Go to (URL) | Then do (click / type) | You should see (screen) | Result |
|---|---|---|---|
| `https://console.hw144.omani.works/auth/handover?token=<JWT>` | Paste the handover URL into a fresh tab and press Enter | 302 → `/dashboard`, signed in, **no login form**; left-rail nav (Dashboard, Cloud, Apps, Sandbox, Jobs, Compliance, Users, Organizations, Settings); env switcher reads **hw144.omani.works**; avatar **E** top-right | ✅ ([hw144-01](../../sessions/2026-06-15/evidence/hw144-01-dashboard-signed-in.png)) |
| `https://console.hw144.omani.works/dashboard` | Click the avatar **E** (top-right, testid `profile-menu-avatar`) | A menu opens reading **"Signed in as emrah.baysal@openova.io"** + a **Sign out** item — confirms the landed identity is the owner | ✅ ([hw144-02](../../sessions/2026-06-15/evidence/hw144-02-identity-emrah-baysal.png)) |
| `https://console.hw144.omani.works/apps` | Look at the Applications grid + the page title | The grid renders **49 deployments INSTALLED**; the page title flips to **"✓ Sovereign ready"** | ✅ ([hw144-03](../../sessions/2026-06-15/evidence/hw144-03-apps-deployments-49.png)) |
| `https://console.hw144.omani.works/app/bp-grafana` | Read the app-detail header + click the **Endpoints** tab | Header shows the **`↗ Open Grafana`** button + "Single sign-in — no second login."; the **Endpoints** tab lists `bp-grafana · grafana.hw144.omani.works · https · Let's Encrypt · Ready · Open →` (blueprint `bp-grafana@1.0.14`) — **Q6: the Open buttons are present** | ✅ ([hw144-09](../../sessions/2026-06-15/evidence/hw144-09-grafana-endpoints-open-button.png)) |
| `https://grafana.hw144.omani.works` | Type the bare URL in a fresh tab and press Enter | Lands on **"Home — Dashboards — Grafana"**, **no login form**, avatar top-right. (`/api/user` → `login=emrah.baysal@openova.io`, `isGrafanaAdmin=true` — lands as ADMIN) | ✅ ([hw144-10](../../sessions/2026-06-15/evidence/hw144-10-grafana-sso-landed-loggedin.png)) |
| `https://registry.hw144.omani.works` | Type the bare URL in a fresh tab and press Enter | Auto-redirects to **`/harbor/projects`**, **no login form**; `emrah.baysal@openova.io` shown top-right; the **Administration** menu (Users / Robot Accounts / Registries / …) is visible. (`/api/v2.0/users/current` → `username=emrah.baysal@openova.io`) | ✅ ([hw144-11](../../sessions/2026-06-15/evidence/hw144-11-harbor-sso-landed-loggedin.png)) |
| `https://bao.hw144.omani.works` | Type the bare URL in a fresh tab and press Enter | OIDC round-trip (`client_id=openbao`, `redirect_uri=/sso/landing`) completes and lands on **`/ui/vault/secrets`** → Secrets Engines (`cubbyhole/`, `secret/`), **no `/ui/vault/auth` login form** (OpenBao 2.3.1). This app historically FAILED the logged-in walk; on hw144 it is clean | ✅ ([hw144-12](../../sessions/2026-06-15/evidence/hw144-12-openbao-sso-landed-loggedin.png)) |
| `https://gitea.hw144.omani.works` | Type the bare URL in a fresh tab and press Enter | Title reads **"emrah.baysal — Dashboard — Catalyst Gitea"**, logged in (Gitea 1.22.3) | ✅ ([hw144-14](../../sessions/2026-06-15/evidence/hw144-14-gitea-sso-landed-emrah.png)) |
| `https://console.hw144.omani.works/app/bp-guacamole` | Read the app-detail header + the **Endpoints** tab | Header shows the **`↗ Open guacamole`** button; the Endpoints tab lists `guacamole.hw144.omani.works · Ready · Open →` (Open buttons present per Q6) | ✅ ([hw144-09](../../sessions/2026-06-15/evidence/hw144-09-grafana-endpoints-open-button.png)) |

**Result: 9 / 9 ✅** zero-click signed-in admin landings + Open-button (Q6) confirmations on hw144.

## North Star #3 — the headline answers (hw144)

- **NO login UI anywhere → URL → signed in as emrah.baysal as ADMIN.** Confirmed: the handover
  URL lands `/dashboard` with no login form (hw144-01), the avatar menu names the owner
  `emrah.baysal@openova.io` (hw144-02), and **grafana / harbor / openbao / gitea** each land
  signed-in zero-click at their bare URL (hw144-10/11/12/14). Admin is proven by surfing the
  admin surfaces: Grafana `isGrafanaAdmin=true`, Harbor Administration menu, OpenBao Secrets
  Engines, Gitea owner dashboard.
- **Q6 — the "Open" buttons are RESTORED (#3570).** Every app-detail header carries
  `↗ Open <App>` and every Endpoints row carries `Open →` (hw144-09). They were never
  permanently lost.

## Honest notes (caveats on otherwise-passing rows)

- **harbor admin-group mapping is a minor follow-up.** Harbor login itself is ✅ (lands signed-in
  as `emrah.baysal@openova.io`, Administration menu visible), but `/api/v2.0/users/current`
  reports `sysadmin:false` — the OIDC **admin-group** mapping has not yet promoted the SSO user to
  Harbor sysadmin. The *login* lands; the *sysadmin role* mapping is a small follow-up, not a login
  fail.
- **newapi (#3564) exposes NO external HTTPRoute** — its app card states "Internal-only services
  reachable cluster-side." There is therefore **no browser SSO walk** for newapi: its "re-login" is
  internal / machine-to-machine, not a bare-URL landing. Stated honestly — newapi is not a
  zero-click-browser-SSO surface on this env, by design.

## Automated cross-checks (NOT acceptance)

These are supporting machine checks, demoted per the founder's UAT format law. **Acceptance is the
operator walking the clickable rows above.**

- `https://grafana.hw144.omani.works/api/user` → `login=emrah.baysal@openova.io`, `isGrafanaAdmin=true`.
- `https://registry.hw144.omani.works/api/v2.0/users/current` → `username=emrah.baysal@openova.io` (`sysadmin:false` — admin-group follow-up).
- openbao OIDC round-trip observed: `client_id=openbao`, `redirect_uri=/sso/landing` → `/ui/vault/secrets`.
