# 2026-06-02 — G113 #2725 silent-SSO closure (codebase)

Session work: chase silent-SSO on hw86 → discover 3 distinct bugs blocking it → ship 3 follow-up PRs → confirm chart pin lockstep on main → hw86 Pod-image swap in flight.

## Bugs found + fixes shipped

| # | Bug | Live evidence | Fix |
|---|---|---|---|
| 1 | catalyst-api chart template `value: {{ printf "https://api.%s" .Values.sovereign.fqdn \| quote }}` rendered `%!s(<nil>)` — `.Values.sovereign.fqdn` path doesn't exist in values.yaml (real FQDN comes via SOVEREIGN_FQDN env from sovereign-fqdn ConfigMap) | hw86 catalyst-api env CATALYST_OIDC_PROVIDER_ISSUER_URL = literal `%!s(<nil>)` | **PR #2729** — `cmd/api/main.go` derives `issuerURL` from SOVEREIGN_FQDN env if CATALYST_OIDC_PROVIDER_ISSUER_URL empty; chart drops the broken templated env entirely. |
| 2 | KC's Identity Provider Redirector execution at level=0 ALTERNATIVE has NO `authenticationConfig` bound by default — `kc_idp_hint=catalyst-pin` is silently ignored, KC falls through to forms execution (username/password screen) | 4-hop probe pre-fix: Hop 2 returned 200 with `<html class="login-pf"><title>Sign in to Sovereign</title>...` instead of 303 to broker endpoint | **PR #2730** — bp-keycloak realm-config ships `authenticatorConfig[catalyst-pin-redirector] {config:{defaultProvider:catalyst-pin}}` + `authenticationFlows[browser]` override binding it to the IDR. Chart 1.4.8 → 1.4.9. |
| 3 | catalyst-api's `buildKCBrokerURL()` emitted `https://auth.<fqdn>/realms/<r>/broker/<alias>/login?kc_idp_hint=...` — that endpoint is **session-scoped** (requires `session_code`+`client_id`+`tab_id` query params populated only mid-flow by KC). Direct invocation → KC `invalidRequestMessage` | KC event log 2026-06-02 20:35:34Z: `IDENTITY_PROVIDER_LOGIN_ERROR error="invalidRequestMessage" identity_provider="catalyst-pin"` | **PR #2731** — `HandlePinVerify` stops emitting `KCBrokerURL`. With IDR `defaultProvider` now bound (PR #2730), realm-session bootstrap happens naturally on first per-app SSO click — no pre-flight needed. |

## Closure path on main

Auto-commits via deploy-bot after PR merges:
- `2026-06-01T20:38:29Z deploy: update catalyst images to e0e210a` (PR #2729 build output)
- `2026-06-01T20:39:19Z deploy(bp-catalyst-platform): bump bootstrap-kit pin 1.4.427 -> 1.4.428`
- `2026-06-01T20:44:25Z deploy: update catalyst images to a776b39` (PR #2731 build output)
- `2026-06-01T20:45:15Z deploy(bp-catalyst-platform): bump bootstrap-kit pin 1.4.428 -> 1.4.429`

Final pins on main:
- products/catalyst/chart/Chart.yaml: 1.4.429
- platform/keycloak/chart/Chart.yaml: 1.4.9
- bootstrap-kit slot 13: 1.4.429 ✓
- bootstrap-kit slot 09: 1.4.9 ✓

## SSO chain (no-cookie probe) — verified live on hw86 mid-session

```
Hop 1: GET https://grafana.hw86.omani.works/login/generic_oauth
       → 302 → https://auth.hw86.omani.works/realms/sovereign/protocol/openid-connect/auth?client_id=grafana&...
Hop 2: GET above (cookie jar)
       → 303 → https://auth.hw86.omani.works/realms/sovereign/broker/catalyst-pin/login?session_code=...
Hop 3: GET above (cookie jar)
       → 303 → https://api.hw86.omani.works/oidc/auth?scope=openid+email+profile+groups&state=...
Hop 4: GET above (cookie jar, no catalyst_session)
       → 302 → https://console.hw86.omani.works/login?next=<encoded>
```

With a real `catalyst_session` cookie (post-PIN), Hop 4 mints a one-time code → KC drops realm session cookie → subsequent per-app SSO is silent.

## Side findings (separate from G113)

- Bastion's systemd-resolved was flaky during session (8.8.8.8 + 1.1.1.1 both timing out from bastion); this caused user-reported "DNS for hw86 broken" symptoms but the A record `console.hw86.omani.works → 212.72.24.43` never actually dropped in PowerDNS. Verified externally via `curl --resolve` = HTTP 200.
- Mothership PDM was on stale image `af4ed5e` (404 on ghcr after package soft-delete); rebuild workflow 26776551091 published `43487f1` + `latest` at 19:20Z. PDM rolled successfully after cleanup of zombie pods (49 Evicted/Failed/ContainerStatusUnknown across cluster).
- Mothership node briefly hit `node.kubernetes.io/disk-pressure:NoSchedule` taint (disk 167.9 → 176.7G during cascade, threshold ~10% free). Talentmesh scaled to zero via Flux suspend (no HRs found — direct kubectl/Helm release, scaled all 20 Deployments to 0 replicas). Disk pressure auto-cleared at 04:49:28Z; Pending count went 64 → 1.

## Closure conditions remaining

- [ ] hw86 catalyst-api Pod (a776b39) 1/1 Running on Harbor proxy-ghcr pull (in flight, ~7m)
- [ ] Operator-walk on hw86: PIN-login → click Grafana → silent SSO (founder verify)
- [ ] (Optional) fresh-prov walk on omani.works from main pins for zero-touch confidence
- [ ] Founder closes #2725

## Memory updates

- `feedback_g113_sso_idr_defaultprovider_fix.md` — updated with closure detail, 3 anti-patterns catalogued (kc_idp_hint silent ignore, broker/login is session-scoped, .Values.sovereign.fqdn nil)

## Refs

#2725 #2727 #2729 #2730 #2731
